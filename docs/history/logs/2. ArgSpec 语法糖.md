> [!note] ME:

把ArgSpec的输入datatype参数名称改成spec, 它解析后变成datatype和name


> [!warning] CODEX:

已按你的要求改好了：`ArgSpec` 现在用 `spec` 作为输入参数名，并解析成 `datatype` + `name`。同时把内部的 `arg_name` 统一为 `name`，并保留了兼容访问（`arg_name` 属性仍可读）。

**改动摘要**
- `ArgSpec` 构造函数改为 `ArgSpec(spec, ...)`，支持 `"Name:type"` 语法糖解析为 `name` + `datatype`
- 字段名从 `arg_name` 统一为 `name`
- `from_dict` 兼容旧 `arg_name` 字段，同时也允许传入 `spec`
- 规则约束/JSON schema 中的签名字段改为 `name`
- 更新了测试、`test.py`、`test.ipynb` 中的注释与示例

**修改的文件**
- `/Users/zhenzhili/symbolic_agent/src/symir/ir/fact_schema.py`
- `/Users/zhenzhili/symbolic_agent/src/symir/rules/constraint_schemas.py`
- `/Users/zhenzhili/symbolic_agent/src/symir/tests/test_llm_rules.py`
- `/Users/zhenzhili/symbolic_agent/src/symir/tests/test_rule_schema_gen.py`
- `/Users/zhenzhili/symbolic_agent/test.py`
- `/Users/zhenzhili/symbolic_agent/test.ipynb`

未运行测试（如果需要我可以跑）。

另外：仓库里依旧有之前的**无关变更**（`src/symir/samples/*.csv` 删除、`samples/` 新增、`test2.ipynb` 新增）。要我忽略它们，还是顺便处理？

接下来你希望我：
1. 提交这些变更并 push  
2. 先跑测试  
3. 继续做其他调整