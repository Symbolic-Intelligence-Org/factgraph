# 03

> Stub: 本文件尚未完成为可执行教程。命令名与当前实现基本对齐，但这里还不是经过逐步核验的工作流文档。

当前应优先参考：

- `src/factpy_kernel/authoring/cli.py`
- `src/factpy_kernel/authoring/docs/01_overview.md`

本页未来应覆盖：

- `apply-execute` 的最小输入组合
- `--registry-dir` / `--apply-request-id` / `--transaction-policy` 的实际用法
- apply 后如何用 `registry-list` / `registry-show` 读取结果
