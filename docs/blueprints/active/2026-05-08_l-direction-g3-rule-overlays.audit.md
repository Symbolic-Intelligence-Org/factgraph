# L Direction G3 — Rule Overlay SDK Shells Audit Log

| Date | Event | Notes |
|---|---|---|
| 2026-05-08 | Draft seeded | Created G3 draft blueprint on `codex/v0.1-l-g3-rule-overlays-2026-05-08` after G2 publish. Captures Round 8 G3 mismatch, G1/G4/G2 inherited constraints, and nine Step 0 questions. No implementation scoped yet. |
| 2026-05-08 | §5.1 locked — three explicit `SDKStore` instance methods | Read-only falsifier pass against existing SDKStore method inventory (36 methods, 35 flat + 1 `views` namespace pre-L), three application-layer rule-overlay runtimes (`check_rule_disable_action` / `check_rule_literal_replace_action` / `check_rule_add_condition_action`) and their three distinct request DTOs (`RuleDisableRequest` / `RuleLiteralReplaceRequest` / `RuleAddConditionRequest`), the G2 §5.8 `#3` heterogeneity reasoning that already rejected polymorphic A-side rule-overlay builders, and the post-L SDK ergonomics redesign target memory. Decision: G3 ships **three explicit `SDKStore` instance methods** (final names locked in §5.7 — placeholder `disable_rule` / `replace_rule_literal` / `add_rule_condition`) with shells under `kernel/sdk/shells/{rule_disable,rule_literal_replace,rule_add_condition}.py` (G2 Phase 0 already activated the subpackage); no nested `store.rule.*` namespace; no polymorphic `store.mutate_rule(kind=...)`; G3 does ship an SDK surface (defer-entirely rejected). Four falsifiers: F1 `views` namespace exists pre-L but is multi-op-on-single-resource pattern, not three-action-on-one-rule analog; mid-L namespace adoption would split L surface into two patterns without consumer signal — PASS reject namespace. F2 polymorphic violates `#3` (three distinct kwarg sets: `(branch_index, atom_index)` vs `(branch_index, atom_index, literal_path, old_literal, new_literal)` vs `(branch_index, added_atom)`) — same false-merge G2 §5.8 already rejected — PASS reject polymorphic. F3 outward-surface noise (36 → 39) acceptable; methods are distinct semantic actions matching A-side runtimes 1:1; verb-first naming consistent with G1/G4/G2 — PASS. F4 §5.1 surface shape is independent of §5.2 input shape; lock is safe under any §5.2 outcome including defer-entirely; Round 8 verdict already presumes G3 ships some SDK surface — PARTIAL but does not block. Forward: §5.2 (target rule input) is the next falsifier and is the critical one because Round 8 flagged raw `RuleSpec` exposure as the dominant mismatch; §5.6 / §5.7 / §5.8 all inherit the three-flat-method shape. |
| 2026-05-08 | §5.2 locked — SDK `Rule` object as target rule input | Read-only falsifier pass against `kernel/application/capability_helpers/rule_overlays.py` (A helpers depend on **full** `RuleSpec` not just `(rule_id, version)` — they store full `rule_spec` in request DTO at `:50-54`, `:89-93`, `:119-123`), `Rule.to_authoring_payload()` at `kernel/sdk/dsl/rule.py:73-93` + `compile_authoring_rule_v1` (`kernel/sdk/store.py:25`) + existing `_compile_rule_input(rule)` (`kernel/sdk/store.py:1040`) + `_run_rule(...)` (`store.py:847-862`) demonstrating the lowering chain `SDK Rule → authoring payload → compiled dict → RuleSpec(...)` works without re-registering or mutating state, the absence of any `(rule_id, version) → RuleSpec` lookup API on `SDKStore` (no global rule registry; `RuleRegistry` is caller-instantiated), `SupportArtifact.rule_refs: tuple[str, ...]` (multi-rule by design — `kernel/core/store/_support.py:103`), and the layer location of `RuleSpec` (`kernel/core/rules/rule_ir.py:28` is substrate IR, NOT `kernel.application.protocol` like the G2 cross-cutting precedent scope). Decision: lock **option B — SDK `Rule` object**. SDK shell methods accept a SDK `Rule` (mirroring G1's `Derivation` input pattern); shell lowers via existing `_compile_rule_input(rule)` to produce `RuleSpec`; passes the `RuleSpec` to A's rule-overlay helpers. Five falsifiers: F1 A helpers require full `RuleSpec` (where body) — required boundary condition for any §5.2 option. F2 SDK `Rule` lowering chain exists end-to-end and is read-only — PASS feasibility for option B. F3 No `(rule_id, version)` lookup API on SDKStore today; adding one is `#6` outward surface without signal — REJECT option C. F4 `SupportArtifact.rule_refs` is multi-rule by design and cannot uniquely identify a single target rule — REJECT option D. F5 `RuleSpec` is substrate IR not application protocol; G2 cross-cutting precedent specifically scoped to `kernel.application.protocol` DTOs; Round 8 independently flagged raw `RuleSpec` as DSL-abstraction mismatch — REJECT option A. Forward: §5.3 (Support input) inherits raw `SupportArtifact` per G2 §5.2 precedent; §5.4 (Overlay/action argument shape) sharpened — caller passes SDK `Rule` + action-specific kwargs, A helper builds `EvaluationOverlay(rule_actions=...)` internally; §5.5 needs new shared `validate_rule(value, *, path)` in `kernel/sdk/shells/_validation.py`; §5.7 final method-name lock with signature shape `(rule, support, *, branch_index, atom_index, ...)`; §5.8 remap covers `RuleCompileError` from `_compile_rule_input` / `RuleSpec(...)` construction → `$.<method>.rule`. G3 confirmed to ship (defer-entirely was rejected by §5.1 falsifier F4 + Round 8 framing). |

## Decision Notes

- G3 starts from the G2 combined baseline: `v0.1-public-surface-helpers-walker-l-g1-l-g4-l-g2-2026-05-08` @ `d658390`.
- The draft deliberately does not answer §5.1-§5.9. Per `feedback_iterative_gap_design`, each Step 0 decision needs a source-grounded falsifier pass before `scoped`.
- Core tension to resolve: A preserves three rule-overlay families under `#3`, while Round 8 flags raw `RuleSpec` exposure and three-sister SDK naming as SDK-layer mismatch.
| 2026-05-08 | §5.3 / §5.5 / §5.6 / §5.9 batch-locked — low-suspense Step 0 questions | Read-only falsifier passes after §5.1 + §5.2 resolved the surface shape and target rule input. **§5.3** (Support input): lock raw `SupportArtifact` per G2 §5.2 cross-cutting precedent; falsifiers PASS — `SupportArtifact` is `@dataclass(frozen=True)`, A helpers (`build_rule_*_request`) accept it directly, composition through `CheckResult.engine_payload` would force Sibling-discipline violation (rejected by G2 §5.2). **§5.5** (Return shape): lock documented passthrough — `RuleDisableResult` / `RuleLiteralReplaceResult` / `RuleAddConditionResult` returned directly; not in `kernel.sdk.__all__` (length still 34); falsifiers PASS — frozen application protocol DTOs, no wrapper signal across G1+G4+G2 cycles. **§5.6** (Module + file layout): lock **three sibling modules** at `kernel/sdk/shells/{rule_disable,rule_literal_replace,rule_add_condition}.py` exporting `sdk_rule_disable / sdk_rule_literal_replace / sdk_rule_add_condition`; **overrides the blueprint's conservative default** (one consolidated `rule_overlays.py`) per falsifier evidence — three modules make Sibling static scans easier (G2 invariant pattern iterates per-module via `G2_MODULES`), preserve `#3` heterogeneity (G2 already shipped two modules for two capabilities), and avoid premature `shells/rule/` sub-subpackage; A's grouping in one helper file is helper-layer coincidence (shared validators) not a contract for shell-layer grouping (each shell calls one A helper, no shared build helper to colocate); post-G3 shell file count = 8 (G1=2 + G4=1 + G2=2 + G3=3). **§5.9** (Tests + invariants): lock three per-method contract test files (`test_sdk_rule_disable.py` / `test_sdk_rule_literal_replace.py` / `test_sdk_rule_add_condition.py`) + one G3 invariant file (`test_sdk_g3_invariants.py`, 6-class mirror of G1+G4+G2); ~13-15 contract tests per method covering happy path, input rejection (raw RuleSpec/Derivation/dict per §5.2 lock), per-family invalid args, ProtocolShapeError request DTO, runtime exception remap, `__all__` non-export, Sibling discipline runtime + static; G3 invariant 6 classes substitute G3-specific constants (`G3_MODULES` list). Forward: Batch 2 covers §5.4 (action arg shapes + overlay/note), §5.7 (method names — public SDK surface, separate review), §5.8 (per-method error path enumeration); §5.7 governs final `SDKStore.<method>` names which gate test docstring-path assertions in §5.9. |
