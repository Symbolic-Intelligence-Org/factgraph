# L Direction G3 — Rule Overlay SDK Shells Audit Log

| Date | Event | Notes |
|---|---|---|
| 2026-05-08 | Draft seeded | Created G3 draft blueprint on `codex/v0.1-l-g3-rule-overlays-2026-05-08` after G2 publish. Captures Round 8 G3 mismatch, G1/G4/G2 inherited constraints, and nine Step 0 questions. No implementation scoped yet. |
| 2026-05-08 | §5.1 locked — three explicit `SDKStore` instance methods | Read-only falsifier pass against existing SDKStore method inventory (36 methods, 35 flat + 1 `views` namespace pre-L), three application-layer rule-overlay runtimes (`check_rule_disable_action` / `check_rule_literal_replace_action` / `check_rule_add_condition_action`) and their three distinct request DTOs (`RuleDisableRequest` / `RuleLiteralReplaceRequest` / `RuleAddConditionRequest`), the G2 §5.8 `#3` heterogeneity reasoning that already rejected polymorphic A-side rule-overlay builders, and the post-L SDK ergonomics redesign target memory. Decision: G3 ships **three explicit `SDKStore` instance methods** (final names locked in §5.7 — placeholder `disable_rule` / `replace_rule_literal` / `add_rule_condition`) with shells under `kernel/sdk/shells/{rule_disable,rule_literal_replace,rule_add_condition}.py` (G2 Phase 0 already activated the subpackage); no nested `store.rule.*` namespace; no polymorphic `store.mutate_rule(kind=...)`; G3 does ship an SDK surface (defer-entirely rejected). Four falsifiers: F1 `views` namespace exists pre-L but is multi-op-on-single-resource pattern, not three-action-on-one-rule analog; mid-L namespace adoption would split L surface into two patterns without consumer signal — PASS reject namespace. F2 polymorphic violates `#3` (three distinct kwarg sets: `(branch_index, atom_index)` vs `(branch_index, atom_index, literal_path, old_literal, new_literal)` vs `(branch_index, added_atom)`) — same false-merge G2 §5.8 already rejected — PASS reject polymorphic. F3 outward-surface noise (36 → 39) acceptable; methods are distinct semantic actions matching A-side runtimes 1:1; verb-first naming consistent with G1/G4/G2 — PASS. F4 §5.1 surface shape is independent of §5.2 input shape; lock is safe under any §5.2 outcome including defer-entirely; Round 8 verdict already presumes G3 ships some SDK surface — PARTIAL but does not block. Forward: §5.2 (target rule input) is the next falsifier and is the critical one because Round 8 flagged raw `RuleSpec` exposure as the dominant mismatch; §5.6 / §5.7 / §5.8 all inherit the three-flat-method shape. |
| 2026-05-08 | §5.2 locked — SDK `Rule` object as target rule input | Read-only falsifier pass against `kernel/application/capability_helpers/rule_overlays.py` (A helpers depend on **full** `RuleSpec` not just `(rule_id, version)` — they store full `rule_spec` in request DTO at `:50-54`, `:89-93`, `:119-123`), `Rule.to_authoring_payload()` at `kernel/sdk/dsl/rule.py:73-93` + `compile_authoring_rule_v1` (`kernel/sdk/store.py:25`) + existing `_compile_rule_input(rule)` (`kernel/sdk/store.py:1040`) + `_run_rule(...)` (`store.py:847-862`) demonstrating the lowering chain `SDK Rule → authoring payload → compiled dict → RuleSpec(...)` works without re-registering or mutating state, the absence of any `(rule_id, version) → RuleSpec` lookup API on `SDKStore` (no global rule registry; `RuleRegistry` is caller-instantiated), `SupportArtifact.rule_refs: tuple[str, ...]` (multi-rule by design — `kernel/core/store/_support.py:103`), and the layer location of `RuleSpec` (`kernel/core/rules/rule_ir.py:28` is substrate IR, NOT `kernel.application.protocol` like the G2 cross-cutting precedent scope). Decision: lock **option B — SDK `Rule` object**. SDK shell methods accept a SDK `Rule` (mirroring G1's `Derivation` input pattern); shell lowers via existing `_compile_rule_input(rule)` to produce `RuleSpec`; passes the `RuleSpec` to A's rule-overlay helpers. Five falsifiers: F1 A helpers require full `RuleSpec` (where body) — required boundary condition for any §5.2 option. F2 SDK `Rule` lowering chain exists end-to-end and is read-only — PASS feasibility for option B. F3 No `(rule_id, version)` lookup API on SDKStore today; adding one is `#6` outward surface without signal — REJECT option C. F4 `SupportArtifact.rule_refs` is multi-rule by design and cannot uniquely identify a single target rule — REJECT option D. F5 `RuleSpec` is substrate IR not application protocol; G2 cross-cutting precedent specifically scoped to `kernel.application.protocol` DTOs; Round 8 independently flagged raw `RuleSpec` as DSL-abstraction mismatch — REJECT option A. Forward: §5.3 (Support input) inherits raw `SupportArtifact` per G2 §5.2 precedent; §5.4 (Overlay/action argument shape) sharpened — caller passes SDK `Rule` + action-specific kwargs, A helper builds `EvaluationOverlay(rule_actions=...)` internally; §5.5 needs new shared `validate_rule(value, *, path)` in `kernel/sdk/shells/_validation.py`; §5.7 final method-name lock with signature shape `(rule, support, *, branch_index, atom_index, ...)`; §5.8 remap covers `RuleCompileError` from `_compile_rule_input` / `RuleSpec(...)` construction → `$.<method>.rule`. G3 confirmed to ship (defer-entirely was rejected by §5.1 falsifier F4 + Round 8 framing). |

## Decision Notes

- G3 starts from the G2 combined baseline: `v0.1-public-surface-helpers-walker-l-g1-l-g4-l-g2-2026-05-08` @ `d658390`.
- The draft deliberately does not answer §5.1-§5.9. Per `feedback_iterative_gap_design`, each Step 0 decision needs a source-grounded falsifier pass before `scoped`.
- Core tension to resolve: A preserves three rule-overlay families under `#3`, while Round 8 flags raw `RuleSpec` exposure and three-sister SDK naming as SDK-layer mismatch.
| 2026-05-08 | §5.3 / §5.5 / §5.6 / §5.9 batch-locked — low-suspense Step 0 questions | Read-only falsifier passes after §5.1 + §5.2 resolved the surface shape and target rule input. **§5.3** (Support input): lock raw `SupportArtifact` per G2 §5.2 cross-cutting precedent; falsifiers PASS — `SupportArtifact` is `@dataclass(frozen=True)`, A helpers (`build_rule_*_request`) accept it directly, composition through `CheckResult.engine_payload` would force Sibling-discipline violation (rejected by G2 §5.2). **§5.5** (Return shape): lock documented passthrough — `RuleDisableResult` / `RuleLiteralReplaceResult` / `RuleAddConditionResult` returned directly; not in `kernel.sdk.__all__` (length still 34); falsifiers PASS — frozen application protocol DTOs, no wrapper signal across G1+G4+G2 cycles. **§5.6** (Module + file layout): lock **three sibling modules** at `kernel/sdk/shells/{rule_disable,rule_literal_replace,rule_add_condition}.py` exporting `sdk_rule_disable / sdk_rule_literal_replace / sdk_rule_add_condition`; **overrides the blueprint's conservative default** (one consolidated `rule_overlays.py`) per falsifier evidence — three modules make Sibling static scans easier (G2 invariant pattern iterates per-module via `G2_MODULES`), preserve `#3` heterogeneity (G2 already shipped two modules for two capabilities), and avoid premature `shells/rule/` sub-subpackage; A's grouping in one helper file is helper-layer coincidence (shared validators) not a contract for shell-layer grouping (each shell calls one A helper, no shared build helper to colocate); post-G3 shell file count = 8 (G1=2 + G4=1 + G2=2 + G3=3). **§5.9** (Tests + invariants): lock three per-method contract test files (`test_sdk_rule_disable.py` / `test_sdk_rule_literal_replace.py` / `test_sdk_rule_add_condition.py`) + one G3 invariant file (`test_sdk_g3_invariants.py`, 6-class mirror of G1+G4+G2); ~13-15 contract tests per method covering happy path, input rejection (raw RuleSpec/Derivation/dict per §5.2 lock), per-family invalid args, ProtocolShapeError request DTO, runtime exception remap, `__all__` non-export, Sibling discipline runtime + static; G3 invariant 6 classes substitute G3-specific constants (`G3_MODULES` list). Forward: Batch 2 covers §5.4 (action arg shapes + overlay/note), §5.7 (method names — public SDK surface, separate review), §5.8 (per-method error path enumeration); §5.7 governs final `SDKStore.<method>` names which gate test docstring-path assertions in §5.9. |
| 2026-05-08 | §5.7 locked — method names: check_rule_disable / check_rule_literal_replace / check_rule_add_condition (Group A) | Read-only falsifier pass against the three rule-overlay runtime sources (read-only what-if confirmed: `project_view_facts(store.ledger, ...)` is the only ledger access, no `apply_write_plan` / no `registry.register` / no persistent state change), existing SDKStore method vocabulary (what-if family: `check` / `diagnose` / `why_not` / `check_fact_overlay` / `recheck_proof_frame` vs persistent-write family: `add` / `set` / `retract` / `accept*` / `ingest`), and SDKStore method-name collision check (no existing methods starting with `disable_`, `replace_`, or `rule_`; both candidate groups lexically collision-free). Decision: **lock Group A** — `SDKStore.check_rule_disable(...)`, `SDKStore.check_rule_literal_replace(...)`, `SDKStore.check_rule_add_condition(...)`. Five falsifiers PASS: F1 runtime semantics confirm what-if (no persistence; method names must signal that); F2 SDK vocab already separates what-if (`check_*` prefix per G1+G2) from persistent-write (`add`/`set`/`retract`) — Group B `add_rule_condition` would semantically collide with `add` family — REJECT Group B; F3 Group A drops only `_action` suffix from application runtime names, preserving 1:1 trace (`check_rule_disable_action` ↔ `check_rule_disable`); Group B reorders tokens, harder to trace — Group A clearly better; F4 no SDKStore name collision for either group, but Group B's `add_rule_condition` shares semantic-field with `add()` persistent write; F5 longest Group A name (`check_rule_literal_replace`, 26 chars) within established envelope (`compile_schema_from_classes` is 27, `recheck_proof_frame` is 19, `validate_provenance` is 19). User-articulated risk (Group B `disable_rule` reads as persistent rule mutation) is real and addressed by Group A. Forward: §5.8 can now lock per-method `$.<method>.<arg>` paths (`$.check_rule_disable.{rule,support,overlay,branch_index,atom_index,request,}` etc.); §5.9 test files keep §5.6-locked names (mirror shell-file names, not method names — same as G2's `test_sdk_fact_overlay.py` for `check_fact_overlay`); shell function names from §5.6 unchanged (`sdk_rule_disable` etc.). Post-L SDK ergonomics redesign may revisit; `check_*` prefix is preserved as the L-Direction what-if convention. |
| 2026-05-08 | §5.4 locked — action arg shapes mirror A; raw RuleLiteralPath / RuleAddedAtom; overlay=None or empty only; note=None exposed | Read-only falsifier pass against `kernel/application/protocol/derivation_fact_overlay.py` (RuleLiteralPath + RuleAddedAtom both `@dataclass(frozen=True)` with strict `__post_init__` validation, located in `kernel.application.protocol` same layer as `EvaluationOverlay` / `SupportArtifact`), `kernel/application/capability_helpers/rule_overlays.py:_request_overlay(...)` (`:150-163`, exact contract: None or empty `EvaluationOverlay()` accepted; non-empty raises `CapabilityHelperError("non-empty overlay not supported")`; non-`EvaluationOverlay` raises `CapabilityHelperError("overlay must be EvaluationOverlay or None")`), and existing `note: str | None` exposure across all three A builders. Decision: lock 1:1 mirror of A signatures with §5.2's `rule` substitution for `rule_spec`. Final SDK signatures: `check_rule_disable(rule, support, *, branch_index, atom_index, overlay=None, note=None)`, `check_rule_literal_replace(rule, support, *, branch_index, atom_index, literal_path, old_literal, new_literal, overlay=None, note=None)`, `check_rule_add_condition(rule, support, *, branch_index, added_atom, overlay=None, note=None)`. Five falsifiers PASS: F1 RuleLiteralPath + RuleAddedAtom satisfy G2 §5.1+§5.2 cross-cutting precedent (frozen application-canonical, no SDK alternative without `#6` violation); F2 no SDK-friendly alternative source-grounded — `old_literal`/`new_literal` Any-typing is necessary not lazy, kind+index pairing in RuleLiteralPath is application-protocol concept; F3 A's overlay contract preserved verbatim — None or empty accepted, non-empty raises CapabilityHelperError mapped at §5.8 to `$.<method>.overlay`; F4 `note=None` already exposed by A, zero new outward commitment; F5 keyword-only after `*` mirrors A and aligns with G1+G4+G2 SDK methods. Forward: §5.8 must enumerate `$.<method>.overlay` path (CapabilityHelperError from A `_request_overlay`), `$.<method>.request` path (ProtocolShapeError from action DTO `__post_init__` for invalid kind+index / atom shape / branch_index / atom_index), `$.<method>.rule` path (from `validate_rule` + `_compile_rule_input` lowering chain), `$.<method>.support` path (from `validate_support_artifact`), `$.<method>.dependencies` path (RuleCompileError from registry resolution if applicable), and base `$.<method>` path (defensive runtime exception remap). §5.8 lock will also decide whether to extend `kernel/sdk/shells/_validation.py` with `validate_optional_evaluation_overlay(value, *, path)` that allows None or to inline the check in each shell. RuleLiteralPath / RuleAddedAtom NOT added to `kernel.sdk.__all__` per §5.5 + §5.6 invariants. |
| 2026-05-08 | §5.8 locked — per-method 6-path remap + Phase 0 shared validator extraction | Read-only falsifier pass against rule-overlay runtimes (each catches `(KeyError, RuleCompileError, TypeError, ValueError, WhereValidationError)` internally and converts to result DTO; SDK never sees these from runtime — verified `rule_disable_runtime.py:129`, `rule_literal_replace_runtime.py:144`, `rule_add_condition_runtime.py:133`), A `_validate_rule_overlay_inputs` + `_request_overlay` exception sources at `kernel/application/capability_helpers/rule_overlays.py:128/130/147/158/160`, and the G2 §5.2 deferred trigger ("until G3/G5 also need them") which now fires for `validate_support_artifact` since all three G3 methods need it. **Decision:** lock per-method 6-path remap + Phase 0 hygiene = shared validator extraction. Three new validators in `kernel/sdk/shells/_validation.py`: `validate_rule(value, *, path)` (mirrors `validate_derivation` from G1 Round 4 Q1), `validate_support_artifact(value, *, path)` (promotes G2 ProofFrame's local helper), `validate_optional_evaluation_overlay(value, *, path)` (sibling to G2's variant; allows None AND rejects non-empty `EvaluationOverlay` so A's `_request_overlay` non-empty path becomes defensive/unreachable from SDK). G2 `proof_frame.py` migrates from local `_validate_support_artifact` to shared (single-line import change; behavior-preserving). Per-method 6-path remap: `$.<method>.rule` (validate_rule + RuleCompileError from `_compile_rule_input` / `RuleSpec(...)`), `$.<method>.support` (validate_support_artifact), `$.<method>.overlay` (validate_optional_evaluation_overlay catches type and non-empty), `$.<method>.dependencies` (RuleCompileError from `_resolve_runtime_registry` — same B.1 pattern as G1), `$.<method>.request` (CapabilityHelperError from A helper + ProtocolShapeError from action/request DTO `__post_init__`), base `$.<method>` (defensive runtime exception remap). Five falsifiers PASS: F1 runtime catches RuleCompileError internally so SDK only sees it from compile/registry boundaries; F2 ProofFrame extraction is byte-identical semantics, behavior-preserving (single import-path change in proof_frame.py); F3 `validate_optional_evaluation_overlay` cleanly separates `$.<method>.overlay` (type + non-empty pre-validation) from `$.<method>.request` (other A helper errors), eliminating the previous overlap risk; F4 `RuleCompileError` source separation between `.rule` (compile/RuleSpec construction) and `.dependencies` (registry resolution) follows G1 B.1 pattern exactly; F5 8-shell Sibling discipline scope (G1=2 + G4=1 + G2=2 + G3=3) is testable via per-module iteration pattern G2 already uses (G3_MODULES constant + per-method runtime patch + per-method static source scan covering 7 sibling shell function names). Forward: Phase 0 hygiene commit lands shared validators + ProofFrame migration; Phase 1/2/3 implement three rule-overlay shells; Phase 4 invariants + docs + cumulative audit; Phase 5 close-out + archive + publish. All 9 §5.x questions now resolved; ready for `draft → scoped` transition. |
| 2026-05-08 | scope-freeze — G3 status `draft → scoped` | All 9 §5.x falsifier passes locked across 7 commits (e648384 §5.1 / 8b36110 §5.2 / ce4ab49 batch §5.3 §5.5 §5.6 §5.9 / 660e61d §5.7 / 476e311 §5.4 / cd3a5f0 §5.8). Cleaned two duplicate `### 5.x` headers (§5.4 + §5.6) introduced during incremental Decision-block edits. §7 acceptance: draft-stage items remain checked; scoped-stage items now all checked (§5.1-§5.9 lock commit references + §8 phase plan filled + status flipped); implementation-stage items added per phase as unchecked targets. §8 implementation plan filled with 6 phases: Phase 0 shared validator extraction (`validate_rule` / `validate_support_artifact` / `validate_optional_evaluation_overlay` to `kernel/sdk/shells/_validation.py` + G2 `proof_frame.py` migration to shared support-artifact validator; G2 §5.2 deferred trigger fires); Phase 1 `sdk_rule_disable`; Phase 2 `sdk_rule_literal_replace`; Phase 3 `sdk_rule_add_condition`; Phase 4 invariants + docs + cumulative audit; Phase 5 close-out + archive + publish. Each phase has explicit scope and audit gate. Per-phase test count expectations: Phase 1 ~14 contract tests, Phase 2 ~15, Phase 3 ~14, Phase 4 +6 invariants. Sacred branches `master` and `v0.1-oss-prep` untouched throughout. G1 + G4 + G2 published snapshot branches NOT modified by Phase 0 hygiene. Status: `draft → scoped`. |
| 2026-05-08 | implementing — Phase 0 landed: shared validator extraction | Behavior-preserving refactor at commit `5437cd6`. Three new shared validators added to `kernel/sdk/shells/_validation.py` per §5.8 lock: `validate_rule(value, *, path)` (mirrors `validate_derivation`), `validate_support_artifact(value, *, path)` (promotes G2 ProofFrame's local helper — fires the G2 §5.2 deferred trigger), `validate_optional_evaluation_overlay(value, *, path)` (sibling to G2's `validate_evaluation_overlay`; allows None AND rejects non-empty `EvaluationOverlay`). `kernel/sdk/shells/proof_frame.py` migrated from local `_validate_support_artifact` to shared `validate_support_artifact` with `path="$.recheck_proof_frame.support_artifact"`; existing 11-test `test_sdk_proof_frame.py` suite passes unchanged (rejection error message `support_artifact must be SupportArtifact` → `support must be SupportArtifact`; existing assertion only checks `SupportArtifact` substring + path, both still hold). 19 new validator unit tests added to `test_sdk_validation.py` (30 total, was 11) covering accept/reject paths, error-message contracts, and `path` parameter forwarding for all three new validators. G2 archive audit log appended with §5.2 deferred-trigger-fired entry on G3 topic branch only — G2 published snapshot branches `v0.1-l-g2-fact-overlay-proofframe-recheck-2026-05-08` and combined `v0.1-public-surface-helpers-walker-l-g1-l-g4-l-g2-2026-05-08` both still at `d658390`, untouched. Verification: 64 targeted tests OK; full kernel 1593 OK / 1 skipped (was 1574 OK / 1 skipped on parent `b8ff4cb`; +19 new validator unit tests, zero regression elsewhere; pre-existing problog adapter import error environment-only and unaffected). ruff clean across `_validation.py` + `proof_frame.py` + `test_sdk_validation.py` + G2 archive audit log. `git diff --check` clean. `kernel.sdk.__all__` length still 34. |
| 2026-05-08 | implementing — Phase 1 landed: `sdk_rule_disable` shell + thin delegate + 16 contract tests + `#P1` runtime-test retrofit | New `src/kernel/sdk/shells/rule_disable.py` (`sdk_rule_disable`) implementing §5.1 / §5.2 / §5.3 / §5.4 / §5.7 / §5.8 locks: validates SDK `Rule` via `validate_rule` → validates raw `SupportArtifact` via `validate_support_artifact` → validates optional `EvaluationOverlay` via `validate_optional_evaluation_overlay` → lowers via `sdk._compile_rule_input(rule)` + `RuleSpec(...)` construction (catches `(SDKStoreError, RuleCompileError, ValueError)` → `$.check_rule_disable.rule`) → resolves dependencies via `sdk._resolve_runtime_registry(rule, explicit_registry=None)` (catches `RuleCompileError` → `$.check_rule_disable.dependencies`) → builds request via `build_rule_disable_request(rule_spec, support, branch_index, atom_index, overlay, note)` (catches `(CapabilityHelperError, ProtocolShapeError)` → `$.check_rule_disable.request`) → dispatches `check_rule_disable_action(request, store=sdk._store, registry=resolved_registry)` (catches defensive `Exception` → `$.check_rule_disable` base path); returns raw `RuleDisableResult` (documented passthrough; not in `kernel.sdk.__all__`, length still 34). New `SDKStore.check_rule_disable(...)` instance method (between `recheck_proof_frame` and `ref` in `store.py`) with thin delegate `from .shells.rule_disable import sdk_rule_disable; return sdk_rule_disable(self, ...)`. TYPE_CHECKING extended to import `RuleDisableResult`. New `src/kernel/tests/test_sdk_rule_disable.py` with 16 contract tests covering: happy path (real SDK Rule + captured SupportArtifact + None overlay); empty `EvaluationOverlay()` accepted; non-Rule rejection (dict); Derivation rejection (sibling DSL but not Rule); non-SupportArtifact rejection (str); non-EvaluationOverlay overlay rejection; non-empty overlay rejected at `$.check_rule_disable.overlay` with "rule-action overlay is constructed internally" hint; RuleCompileError on lowering remaps to `.rule` (B.1+B.2 pattern); RuleCompileError from `_resolve_runtime_registry` remaps to `.dependencies`; CapabilityHelperError from helper remaps to `.request`; ProtocolShapeError from helper remaps to `.request`; defensive `Exception` from runtime remaps to base `$.check_rule_disable`; result DTO + action DTO + request DTO all NOT in `kernel.sdk.__all__`; runtime called with positional request + `store=` + `registry=` kwargs (verified action.branch_index/atom_index forwarded); Sibling discipline runtime patch (5 sibling shells: check / diagnose / why_not / fact_overlay / proof_frame); Sibling static source scan (12 forbidden patterns covering 7 sibling shells incl. future Phase 2 + Phase 3 G3 sisters). Phase 1 audit gate (5 dimensions) PASS: §5.x locks honored / `kernel.sdk.__all__` length still 34 / no `#6` outward surface added / SDK shell does not import action/request DTOs (only `RuleDisableResult` + runtime + helper) / `RuleDisableAction` stays internal to A helper per §5.4 lock. **`#P1` carve-out applied to existing application-runtime boundary test** `test_application_rule_disable_runtime_native.py::RuleDisableRuntimeBoundaryTests::test_no_sdk_rule_disable_surface` — pre-G3 test asserted **no** SDK surface for Rule Disable; retrofit per `#P1` (mirroring G2 Phase 0's G1+G4 retrofits) inverts to `test_sdk_rule_disable_shell_imports_runtime_only_in_shell`: shell file at `kernel/sdk/shells/rule_disable.py` exists, imports `check_rule_disable_action` only in shell file, does NOT import `RuleDisableAction` (action constructed internally by A helper). Pattern-based assertions (`import RuleDisableAction`, `, RuleDisableAction`, `RuleDisableAction(`) tolerate legitimate docstring references to type names (e.g., `SDKStore.check_rule_disable` docstring documents that `note` is forwarded to the `RuleDisableAction`). Carve-out scope: principle id `#P1`, reason "test name + assertion encode pre-G3 'no SDK surface' invariant that G3 §5.6 explicitly inverts", impact "single test file", reviewer ack G3 §5.6 lock at commit `ce4ab49`, historical-handling = retrofit. Verification: 16 targeted G3 contract tests OK; full SDK shell suite (G1+G4+G2+G3 contract tests + invariants + validators) 127 OK; full kernel 1609 OK / 1 skipped (was 1593 OK / 1 skipped on Phase 0 HEAD `5437cd6`; +16 new G3 contract tests, zero regression elsewhere; pre-existing problog adapter import error environment-only and unaffected). ruff clean across `rule_disable.py` + `store.py` + `test_sdk_rule_disable.py` + retrofit test file. `git diff --check` clean. Phase 2 (`sdk_rule_literal_replace`) is next per §8; it will fire similar `#P1` retrofit on `test_application_rule_literal_replace_runtime_native.py::test_no_sdk_rule_literal_replace_surface`. Phase 3 will fire the analogous retrofit on `test_application_rule_add_condition_runtime_native.py::test_no_sdk_rule_add_condition_surface`. |
| 2026-05-08 | implementing — Phase 2 landed: `sdk_rule_literal_replace` shell + thin delegate + 17 contract tests + `#P1` runtime-test retrofit | New `src/kernel/sdk/shells/rule_literal_replace.py` (`sdk_rule_literal_replace`) implementing §5.1 / §5.2 / §5.3 / §5.4 / §5.7 / §5.8 locks: same 7-step shape as Phase 1 with rule-literal-replace-specific action arguments. Validates SDK `Rule` → `SupportArtifact` → optional `EvaluationOverlay` → lowers via `_compile_rule_input` + `RuleSpec(...)` → resolves dependencies → builds via `build_rule_literal_replace_request(rule_spec, support, branch_index, atom_index, literal_path, old_literal, new_literal, overlay, note)` → dispatches `check_rule_literal_replace_action(request, store=, registry=)`. Six §5.8 remap paths landed: `$.check_rule_literal_replace.{rule,support,overlay,dependencies,request,}` + base. Returns raw `RuleLiteralReplaceResult` (passthrough; not in `kernel.sdk.__all__`, length still 34). New `SDKStore.check_rule_literal_replace(...)` instance method (between `check_rule_disable` and `ref` in `store.py`) with thin delegate; TYPE_CHECKING extended to import `RuleLiteralReplaceResult`. New `src/kernel/tests/test_sdk_rule_literal_replace.py` with 17 contract tests covering: happy path; empty `EvaluationOverlay()` accepted; non-Rule rejection (dict); Derivation rejection; non-SupportArtifact rejection; non-EvaluationOverlay overlay rejection; non-empty overlay rejection at `$.check_rule_literal_replace.overlay`; **non-`RuleLiteralPath` `literal_path` rejection at `$.check_rule_literal_replace.request`** (extra test vs Phase 1 — `RuleLiteralPath` slips past SDK pre-validation since no shared validator exists for it; A helper / action DTO `__post_init__` raises `ProtocolShapeError`); RuleCompileError on lowering remaps to `.rule`; RuleCompileError from `_resolve_runtime_registry` remaps to `.dependencies`; CapabilityHelperError from helper remaps to `.request`; ProtocolShapeError from helper remaps to `.request`; defensive Exception from runtime remaps to base; result/action/request DTOs + `RuleLiteralPath` all NOT in `kernel.sdk.__all__`; runtime called with positional request + `store=` + `registry=` kwargs (verified action.literal_path.kind/index forwarded); Sibling discipline runtime patch (6 sibling shells incl. Phase 1 `sdk_rule_disable`); Sibling static source scan (12+ forbidden patterns covering 7 sibling shells incl. future Phase 3 G3 sister `sdk_rule_add_condition`). Phase 2 audit gate (5 dimensions) PASS: §5.x locks honored / `kernel.sdk.__all__` length still 34 / no `#6` outward surface added / SDK shell does not import action/path/request DTOs (only `RuleLiteralReplaceResult` + runtime + helper) / `RuleLiteralReplaceAction` + `RuleLiteralPath` stay internal to A helper per §5.4 lock. **`#P1` carve-out applied to existing application-runtime boundary test** `test_application_rule_literal_replace_runtime_native.py::test_no_sdk_rule_literal_replace_surface` — pre-G3 test asserted **no** SDK surface for Rule Literal Replace; retrofit per `#P1` (mirroring Phase 1's Rule Disable retrofit) inverts to `test_sdk_rule_literal_replace_shell_imports_runtime_only_in_shell`: shell file exists; imports `check_rule_literal_replace_action` only in shell file; does NOT import `RuleLiteralReplaceAction` (action constructed internally by A helper). Pattern-based assertions tolerate docstring references. Carve-out scope: principle id `#P1`, reason "test name + assertion encode pre-G3 'no SDK surface' invariant that G3 §5.6 explicitly inverts", impact "single test file", reviewer ack G3 §5.6 lock at commit `ce4ab49`, historical-handling = retrofit. Verification: 17 targeted G3 Phase 2 contract tests OK; full SDK shell suite `144 OK`; full kernel `1626 OK / 1 skipped` (was 1609 OK / 1 skipped on Phase 1 HEAD `7f761bd`; +17 new G3 contract tests, zero regression elsewhere; pre-existing problog adapter import error environment-only and unaffected). ruff clean across `rule_literal_replace.py` + `store.py` + `test_sdk_rule_literal_replace.py` + retrofit test file. `git diff --check` clean. Phase 3 (`sdk_rule_add_condition`) is next per §8; it will fire the analogous `#P1` retrofit on `test_application_rule_add_condition_runtime_native.py::test_no_sdk_rule_add_condition_surface`. SDKStore method count after Phase 2: 38; G3 SDK shell file count after Phase 2: 2 (rule_disable + rule_literal_replace). |
| 2026-05-08 | implementing — Phase 3 landed: `sdk_rule_add_condition` shell + thin delegate + 17 contract tests + `#P1` runtime-test retrofit | New `src/kernel/sdk/shells/rule_add_condition.py` (`sdk_rule_add_condition`) implementing §5.1 / §5.2 / §5.3 / §5.4 / §5.7 / §5.8 locks: same 7-step shape as Phase 1 / Phase 2 with rule-add-condition-specific action arguments. Note the absence of `atom_index` argument — Add Condition appends a new atom to the branch rather than pointing at an existing locator. Validates SDK `Rule` → `SupportArtifact` → optional `EvaluationOverlay` → lowers via `_compile_rule_input` + `RuleSpec(...)` → resolves dependencies → builds via `build_rule_add_condition_request(rule_spec, support, branch_index, added_atom, overlay, note)` → dispatches `check_rule_add_condition_action(request, store=, registry=)`. Six §5.8 remap paths landed: `$.check_rule_add_condition.{rule,support,overlay,dependencies,request,}` + base. Returns raw `RuleAddConditionResult` (passthrough; not in `kernel.sdk.__all__`, length still 34). New `SDKStore.check_rule_add_condition(...)` instance method (between `check_rule_literal_replace` and `ref` in `store.py`) with thin delegate; TYPE_CHECKING extended to import `RuleAddConditionResult`. New `src/kernel/tests/test_sdk_rule_add_condition.py` with 17 contract tests covering: happy path; empty `EvaluationOverlay()` accepted; non-Rule rejection (dict); Derivation rejection; non-SupportArtifact rejection; non-EvaluationOverlay overlay rejection; non-empty overlay rejection at `$.check_rule_add_condition.overlay`; **non-`RuleAddedAtom` `added_atom` rejection at `$.check_rule_add_condition.request`** (extra test mirroring Phase 2's `RuleLiteralPath` extra — `RuleAddedAtom` slips past SDK pre-validation since no shared validator exists; A helper / action DTO `__post_init__` raises `ProtocolShapeError`); RuleCompileError on lowering remaps to `.rule`; RuleCompileError from `_resolve_runtime_registry` remaps to `.dependencies`; CapabilityHelperError from helper remaps to `.request`; ProtocolShapeError from helper remaps to `.request`; defensive Exception from runtime remaps to base; result/action/request DTOs + `RuleAddedAtom` all NOT in `kernel.sdk.__all__`; runtime called with positional request + `store=` + `registry=` kwargs (verified action.added_atom.atom forwarded); Sibling discipline runtime patch (7 sibling shells — all G1+G4+G2+G3 sisters now exist); Sibling static source scan (14+ forbidden patterns covering 7 sibling shells; G3 8-shell scope now fully active). Phase 3 audit gate (5 dimensions) PASS: §5.x locks honored / `kernel.sdk.__all__` length still 34 / no `#6` outward surface added / SDK shell does not import action/atom/request DTOs (only `RuleAddConditionResult` + runtime + helper) / `RuleAddConditionAction` + `RuleAddedAtom` stay internal to A helper per §5.4 lock. **`#P1` carve-out applied to existing application-runtime boundary test** `test_application_rule_add_condition_runtime_native.py::test_no_sdk_rule_add_condition_surface` — pre-G3 test asserted **no** SDK surface for Rule Add Condition; retrofit per `#P1` (mirroring Phase 1 + Phase 2 retrofits) inverts to `test_sdk_rule_add_condition_shell_imports_runtime_only_in_shell`: shell file exists; imports `check_rule_add_condition_action` only in shell file; does NOT import `RuleAddConditionAction` (action constructed internally by A helper). Pattern-based assertions tolerate docstring references. Carve-out scope: principle id `#P1`, reason "test name + assertion encode pre-G3 'no SDK surface' invariant that G3 §5.6 explicitly inverts", impact "single test file", reviewer ack G3 §5.6 lock at commit `ce4ab49`, historical-handling = retrofit. Verification: 17 targeted G3 Phase 3 contract tests OK; full SDK shell suite `161 OK`; full kernel `1643 OK / 1 skipped` (was 1626 OK / 1 skipped on Phase 2 HEAD `3526cc1`; +17 new G3 contract tests, zero regression elsewhere; pre-existing problog adapter import error environment-only and unaffected). ruff clean across `rule_add_condition.py` + `store.py` + `test_sdk_rule_add_condition.py` + retrofit test file. `git diff --check` clean. Phase 4 (G3 invariants + docs + cumulative audit) is next per §8 — it will land `test_sdk_g3_invariants.py` (6-class mirror of G1+G4+G2 invariants), update SDK API docs (`04_api_surface.md` + `.en.md`) + application overview docs, and run the cumulative G3 strict audit gate. SDKStore method count after Phase 3: 39; G3 SDK shell file count after Phase 3: 3 (rule_disable + rule_literal_replace + rule_add_condition); total `kernel/sdk/shells/` module count: 8. All three `#P1` carve-out retrofits across G3 Phases 1/2/3 complete; no further pre-G3 boundary tests pending. |
| 2026-05-08 | implementing — Phase 4 landed: G3 invariants + docs + cumulative audit | New `src/kernel/tests/test_sdk_g3_invariants.py` mirroring G1/G4/G2 6-class structure under post-G2-Phase-0 `kernel/sdk/shells/` layout: (1) `test_sdk_all_unchanged_and_g3_result_types_not_exported` — `__all__` length still 34, no G3 result/action/request/path/atom DTOs in `kernel.sdk.__all__`, no G3 SDK function names exported; (2) `test_g3_methods_are_instance_methods_and_no_scenario_method_shipped` — three SDKStore methods callable; reserved scenario names (`disable_rule` / `replace_rule_literal` / `add_rule_condition` / `rule_disable` / `rule_literal_replace` / `rule_add_condition` / `mutate_rule`) absent (Group B + polymorphic candidates from §5.7 falsifier all rejected); (3) `test_g3_modules_live_in_shells_subpackage` — three shell files exist at `kernel/sdk/shells/`; flat `kernel/sdk/<x>.py` paths do not; (4) `test_g3_modules_do_not_import_internal_or_walker_layers` — same `FORBIDDEN_PRODUCTION_IMPORT_TEXT` set as G1+G4+G2 (capability_helpers private `_binding`, `_reject_sdk_origin`, walker, audit, frontier); (5) `test_store_methods_remain_thin_delegate_methods` — pattern-based assertion checks call/instantiation patterns only (e.g., asserts `RuleDisableRequest(` / `check_rule_disable_action(` / `validate_rule(` / `validate_support_artifact(` are NOT present in delegate method source) so legitimate docstring references to these type names are not flagged; (6) `test_store_method_docstrings_record_boundary_contracts` — each method docstring includes required type names + all six locked `$.<method>.<arg>` paths. SDK API docs updated: `kernel/sdk/docs/04_api_surface.md` + `.en.md` add three G3 methods to method list (line 67-69 / 69-71), preamble paragraph updated to describe G3 as the eight L methods (was "five") and notes G3 Phases 1/2/3 #P1 retrofits across three application-runtime boundary tests; new paragraph documents G3 §5.1+§5.2 cross-cutting precedent extension (substrate IR `RuleSpec` rejected at SDK boundary; application protocol DTOs like `RuleLiteralPath` / `RuleAddedAtom` accepted per G2 precedent); per-method shape/error-path descriptions added for all three G3 methods. Application overview docs updated: `kernel/application/docs/01_overview.md` + `_en.md` extended L-direction SDK shell paragraph (line 177) to describe G3 lowering chain + helper calls + new validators; "remaining" list now correctly excludes Rule Disable / Rule Literal Replace / Rule Add Condition (paragraph trimmed to round events, ProofFrame diff, frontier); test inventory adds `test_sdk_rule_disable.py` / `test_sdk_rule_literal_replace.py` / `test_sdk_rule_add_condition.py` / `test_sdk_g3_invariants.py`. **Phase 4 cumulative audit gate (5 dimensions) PASS:** §5.1-§5.9 locks all honored across Phase 0/1/2/3/4 (verified by G3 invariant file + per-method contract test files); §6 invariants verified by 6-test G3 invariant file (1:1 mirror of G1+G4+G2); shells/ layout + G1+G4 retrofit invariants holding (G1 + G4 + G2 invariant files all green at this HEAD); public `kernel.sdk.__all__` length still 34 (verified by all four invariant test files asserting the count); G1 + G4 + G2 published snapshot branches untouched at this HEAD — `origin/v0.1-l-g1-check-diagnose-2026-05-08` still at `d6716a0`, `origin/v0.1-l-g4-why-not-frontier-2026-05-08` still at `acb5a6e`, `origin/v0.1-l-g2-fact-overlay-proofframe-recheck-2026-05-08` and `origin/v0.1-public-surface-helpers-walker-l-g1-l-g4-l-g2-2026-05-08` both still at `d658390` (only G2 archive audit log was edited at Phase 0 to record the §5.2 trigger-fired entry on G3 topic forward — does not affect snapshot branches). Verification: 6 G3 invariant tests OK; 105 G1+G2+G3+G4 invariant + G3 contract + validation tests OK; full kernel `1649 OK / 1 skipped` (was 1643 OK / 1 skipped on Phase 3 HEAD `63c47ea`; +6 new G3 invariant tests, zero regression elsewhere; pre-existing problog adapter import error environment-only and unaffected). ruff clean across `test_sdk_g3_invariants.py` + 4 doc files. `git diff --check` clean. Phase 5 (close-out + archive + publish) is next per §8 — it will fill §10 Outcome / Deviations, mark status `implemented`, archive blueprint + audit log, update inventory, and publish `v0.1-l-g3-rule-overlays-2026-05-08` (G3-only) + `v0.1-public-surface-helpers-walker-l-g1-l-g4-l-g2-l-g3-2026-05-08` (Path B combined) pending user authorization. SDKStore method count after Phase 4: 39 (unchanged); G3 SDK shell file count: 3; total `kernel/sdk/shells/` module count: 8. |
