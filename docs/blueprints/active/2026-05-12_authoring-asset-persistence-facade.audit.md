# Task Blueprint Audit: Authoring Asset Persistence Facade

- Blueprint: [2026-05-12_authoring-asset-persistence-facade.md](./2026-05-12_authoring-asset-persistence-facade.md)

## Event Log

| Date | Stage | Event | Notes |
| --- | --- | --- | --- |
| 2026-05-12 | draft | Blueprint created | Draft seed created after Blueprint 1 public `Inference` + `FactGraph.create(...)` and the inference wire/registry vocabulary slice both shipped. Source audit split across registry surface, ref/namespace surface, and application/FactGraph binding. Load-bearing findings: `SDKRegistry` is still public and raw-dict-shaped; `FileAuthoringRegistry` has enough low-level primitives but returns compiler-facing inference payloads with `derivation_id`; current `RuleRef` is a where-clause DSL carrier, not a persisted asset handle; `InferenceRef` is absent and explicitly guarded; `SDKStore` has no authoring registry binding; and no `kernel.application.authoring_runtime` exists yet. |
| 2026-05-12 | draft-polish | Persisted ref naming and schema safeguards added | Added Q17 to lock a naming convention for persisted refs instead of letting Q5/Q6 drift into `SavedRuleRef` + `InferenceRef` asymmetry. Recommended symmetric `SavedRuleRef` / `SavedInferenceRef`. Tightened Q11 so auto schema upsert is idempotent under matching `schema_digest` and raises on mismatch. Documented that `fg.eval.run(saved_ref)` / `fg.eval.evaluate(saved_ref)` is intentionally deferred under Q12. |
| 2026-05-12 | scoped | Scope freeze | Locked A1a + A2c + A3a + A4a + A5b + A6a + A7a + A8a + A9a + A10a + A11b + A12a + A13a + A14a + A15a + A16a + A17a. Blueprint 2 will bind authoring registry to `FactGraph`, introduce `kernel.application.authoring_runtime`, add graph-bound `fg.rules.*` and `fg.inferences.*` persistence, hard-cut public `SDKRegistry`, return/load/list typed `SavedRuleRef` / `SavedInferenceRef`, auto-upsert schema under matching digest, and defer direct runtime execution of saved refs. |
| 2026-05-12 | red-baseline | G1 tests added | Added `test_authoring_asset_persistence_facade` with 28 tests covering namespace methods, saved-ref exports and shapes, save/load/list/get return semantics, registry binding, application runtime imports, schema upsert/mismatch behavior, `SDKRegistry` hard-cut, saved-ref runtime rejection, and preservation guards for direct runtime, `RuleRef` where-clause use, Blueprint 1, inference wire/registry vocabulary, and derivation-named substrate DTOs. Expected baseline: 7 failures + 14 errors + 7 passing guards. Also fixed the stale `run(RuleRef)` API surface doc as Q16 inventory polish. |
| 2026-05-12 | implementation | G2.1 saved refs and application runtime | Added `SavedRuleRef` / `SavedInferenceRef` and exported them from `kernel.sdk`. Added `kernel.application.authoring_runtime` with pure registry-facing save/load/list/get helpers and schema-digest upsert guard. Expected baseline moved to 6 failures + 10 errors + 12 passes; SDK manager binding, registry constructor injection, `SDKRegistry` hard-cut, and saved-ref runtime rejection remain for later G2 commits. |
| 2026-05-12 | implementation | G2.2 graph registry binding | Added `registry_root` and `registry` kwargs to `SDKStore.__init__`, `FactGraph.create(...)`, and `from_schema_classes(...)`, plus conflict validation matching the low-level registry constructor. Expected baseline moved to 6 failures + 7 errors + 15 passes; persistence managers remain unwired. |
| 2026-05-12 | implementation | G2.3 rules persistence facade | Added `fg.rules.save/load/list/get` over `kernel.application.authoring_runtime`, preserving `fg.rules.inspect`. Rule save delegates schema upsert/digest checks to the application layer and returns `SavedRuleRef`; rule load returns SDK `Rule` value objects. Expected baseline moved to 2 failures + 5 errors + 21 passes; `fg.inferences`, public `SDKRegistry` hard-cut, and saved-ref runtime rejection remain. |

## Decision Notes

- 2026-05-12 draft: The central G0 question is registry attachment. `fg.rules.save(...)` and `fg.inferences.save(...)` cannot be clean instance methods unless `FactGraph` is bound to a registry root/object or every call takes an explicit registry.
- 2026-05-12 draft: Reusing current `RuleRef` as a save return type is risky because it already means a where-clause RuleRef atom carrier with hidden object-backed dependency behavior.
- 2026-05-12 draft: Adding `InferenceRef` is a real public API expansion. Current tests explicitly assert it does not exist; G1 must invert that only if G0 locks it.
- 2026-05-12 draft: `SDKRegistry` export fate must be explicit. If `fg.rules.*` / `fg.inferences.*` become the product facade, keeping `SDKRegistry` public creates duplicate persistence surfaces.
- 2026-05-12 draft: The stale docs claim that `fg.eval.run(...)` accepts a direct `RuleRef` should be fixed or explicitly scoped, because code treats `RuleRef` as a where-clause DSL carrier, not a runnable rule selector.
- 2026-05-12 draft polish: Ref naming should be symmetric. If saved refs are introduced, `SavedRuleRef` / `SavedInferenceRef` better communicates "registry-persisted asset handle" than overloading existing `RuleRef` or mixing naming conventions.
- 2026-05-12 G0: Application-first authoring persistence is locked. The SDK facade delegates to `kernel.application.authoring_runtime`; it does not own registry semantics directly and does not delegate through public `SDKRegistry`.
- 2026-05-12 G0: `SavedRuleRef` / `SavedInferenceRef` are load handles, not runtime selectors. `RuleRef` remains a where-clause DSL carrier.
- 2026-05-12 G0: `SDKRegistry` is hard-cut from public SDK exports/docs in this slice because graph-bound `fg.rules.*` / `fg.inferences.*` becomes the product persistence facade.
