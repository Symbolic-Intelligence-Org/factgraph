# L Direction G5 — Round Events + ProofFrame Diff SDK Shell Audit Log

| Date | Event | Notes |
|---|---|---|
| 2026-05-08 | Draft seeded | Created G5 draft blueprint on `codex/v0.1-l-g5-round-events-proofframe-diff-2026-05-08` (branched from G3 topic tip `112401a`, which is forward-only polish on top of G3 published HEAD `cb6d3bd`). Captures: Round 8 G5 mismatch verdict ("recorder lifecycle raises mixed with query DTO returns"), the audit-layer surface that already exists (`kernel.audit.round_events` with `RoundRecorder` + `start_round` / `record_round_event` / `finalize_round` + 5 projection helpers + 7 frozen DTOs; `kernel.audit.proof_frame_diff` with pure `build_proof_frame_diff(...)` + 6 frozen DTOs; `kernel.application.walker.ProofFrameDiffView` Tier 2 advanced importable view), confirmation that `kernel/sdk/` references none of these names today, and inheritance from G1-G4 (8 shell modules under `kernel/sdk/shells/`, 6 shared input validators + 1 boundary normalizer in `_validation.py`, §5.1+§5.2 cross-cutting precedent + G3 substrate-IR-out clarification, Sibling discipline at 8-shell scope, `kernel.sdk.__all__` length 34, Frontier still advanced importable). Records nine Step 0 questions: §5.1 Round events SDK shape (defer / ship recorder / hybrid; conservative default = defer), §5.2 ProofFrame Diff SDK shipped (conservative default = yes), §5.3 Diff input shape (raw `tuple[RoundEvent, ...]` × 2 vs path × 2 vs hybrid; **brings cross-cutting precedent layer question — does `kernel.audit` extend the G2 precedent or get carved out?**), §5.4 Diff return passthrough, §5.5 Module placement + shared-validator decision, §5.6 file naming, §5.7 SDKStore method names (Group A `diff_proof_frames` per Round 8 hint), §5.8 error mapping + Sibling at 9+ shell scope, §5.9 tests + invariants + `#P1` retrofit count. Status `draft`; no SDK code or method change scoped yet. Per `feedback_iterative_gap_design`, each §5.x lock proceeds one at a time with a fresh source-grounded falsifier pass before scope-freeze. Baseline forward-only; published G3 refs `cb6d3bd` and prior G1/G4/G2/G3 snapshots all stay immutable. Sacred branches untouched. |
| 2026-05-08 | §5.1 locked — defer Round events from SDK; G5 narrows to single-method ProofFrame Diff shell | Read-only falsifier pass against (a) all 8 L-Direction method return shapes in `src/kernel/sdk/store.py` (every one returns a `@dataclass(frozen=True)` `*Result` DTO from `kernel.application.protocol`), (b) `RoundRecorder` mutable-state shape at `src/kernel/audit/round_events.py:99-160` (no `frozen=True`; mutable `_events`/`_started`/`_finalized`; `finalize()` writes JSONL via `write_round_events_atomic`), (c) consumer-signal search (`grep` for `SDKStore.\(start\|record\|finalize\)_round` in docs/src returned zero hits), (d) Round 8 audit hint and Batch 6 round-persistence blueprint declaration ("external buffered recorder APIs ... No application capability runtime imports `kernel.audit`"), (e) canonical user-facing example `examples/round_story_full_demo.py` already importing `start_round` / `record_round_event` / `finalize_round` directly from `kernel.audit.round_events`, and (f) test-breakage scan (`grep` for `test_no_sdk_round_events` / `test_no_sdk_proof_frame_diff` returned zero hits; `test_sdk_consumer_boundary.py` has zero references to round_events). Decision: lock **option (A) — defer Round events from SDK entirely**. G5 ships only `SDKStore.diff_proof_frames(...) -> ProofFrameDiff` on the pure query side; capture stays at `kernel.audit.round_events` (`start_round` / `record_round_event` / `finalize_round`) advanced importable. Four falsifiers: F1 PASS — all 8 existing L methods return frozen DTOs; shipping mutable `RoundRecorder` would be the first L method returning a mutable object, a category split with no precedent. F2 PASS — zero consumer signal; `#6` (no outward compat without user signal) supports defer; existing UX already uses `from kernel.audit.round_events import ...` directly. F3 NOT compressible — recorder accumulates state across N calls, sequence numbering depends on `len(self._events)`, finalize writes to disk; cannot collapse to a single pure call without forcing callers to manage state themselves. F4 PASS — defer breaks zero existing tests; no pre-G5 boundary test asserts "no SDK round events surface" so no `#P1` retrofit needed for the recorder side. **Forward implications:** G5 becomes a 1-method shell; phase count drops to 3-4 (Phase 0 hygiene only if §5.5 surfaces shared validators — likely none); §5.5/§5.6/§5.7 simplify to single-file decisions; Sibling discipline scope after G5 = 9 shells; §5.2 nearly settled (diff side ships); §5.3 becomes the critical next decision (cross-cutting precedent layer scope: does frozen `kernel.audit.RoundEvent` cross the SDK boundary as input, extending the G2 precedent, or does `kernel.audit` get carved out?); no `#P1` retrofit on recorder side. The "SDK-only complete round story" goal is honest: G5 ships the query side (diff); capture stays at `kernel.audit`, matching the event-sourcing pattern split. |
| 2026-05-08 | §5.3 locked — raw `tuple[RoundEvent, ...]` × 2 at SDK boundary; explicit cross-cutting precedent layer clarification | Read-only falsifier pass against (a) `RoundEvent` shape at `src/kernel/audit/round_events.py:38-61` — `@dataclass(frozen=True)` with 6 canonical sorted fields + `__post_init__` validation; same shape as G2/G3 raw-DTO inputs that already cross SDK boundary; (b) `kernel.audit` import-DAG position — imports `kernel.application.protocol.common.JSONValue/WarningDTO` (`round_events.py:11`, `proof_frame_diff.py:7-8`) AND `kernel.core.*` (`audit/query.py:6-19`); imported BY `kernel.application.capability_helpers/round_events.py:21` and `kernel.application.walker.views.py:34`; therefore audit is a sibling-DTO layer above protocol vocabulary, not substrate IR; (c) SDK input-pattern audit — all 8 existing L methods take Python objects, zero take filesystem path as method-level input (only `from_schema_classes(..., ledger_path=None)` constructor-level). Decision: lock **option (A) — raw `tuple[RoundEvent, ...]` × 2 at SDK boundary**, mirroring `build_proof_frame_diff(...)` 1:1; SDK shell does no IO; users load via `kernel.audit.load_audit_package` or hold from a fresh recorder. **Cross-cutting precedent layer clarification (made explicit per user request):** the boundary is NOT "only `kernel.application.protocol`"; it is "**frozen canonical DTO above `kernel.core` using `kernel.application.protocol` vocabulary**". This rule (a) keeps `RoundEvent` and supporting `kernel.audit` DTOs (`ProofFrameDiff`, `FrameDelta`, `AtomDelta`, `FrameIdentity`, `FrameStatusChange`, `EventReference`) IN scope, and (b) preserves the G3 §5.2 substrate-IR-out carve-out (`RuleSpec` at `kernel.core.rules.rule_ir` excluded by "above `kernel.core`"). To be encoded verbatim in §6 invariants at scope-freeze. Three falsifiers PASS: F1 (`RoundEvent` is frozen application-canonical with protocol vocabulary); F2 (audit layer position above core, principled rule generalizes G2+G3+G5 cleanly); F3 (no SDK alternative without inventing outward surface — options B/C add new patterns under `#6`). **Forward implications:** §5.4 (return shape) nearly trivial — `ProofFrameDiff` covered by same precedent extension; passthrough confirmed; §5.5 single-shell; §5.6 file `kernel/sdk/shells/proof_frame_diff.py`; §5.7 `diff_proof_frames` Group A; §5.8 paths `$.diff_proof_frames.{round_a_id, round_b_id, round_a_events, round_b_events, warnings, request,}` + base. `ProofFrameDiffError` (a `ValueError` subclass at `proof_frame_diff.py:22`) remaps to `$.diff_proof_frames.request`. |
| 2026-05-08 | §5.2 + §5.4 batch-locked — ship 1 SDK shell with documented passthrough of raw `ProofFrameDiff` | Read-only batched falsifier pass building on §5.1 + §5.3 locks. **§5.2** evidence: `build_proof_frame_diff(*, round_a_id, round_b_id, round_a_events, round_b_events, warnings=(), include_unchanged=False) -> ProofFrameDiff` at `src/kernel/audit/proof_frame_diff.py:160-191` is a pure function with frozen-DTO output and no `Store` parameter — fits the L SDK shape (G2 ProofFrame Recheck pattern: frozen-DTO-in, frozen-DTO-out, no derivation/rule lowering). All 3 falsifiers PASS — F1 signature alignment, F2 input DTOs frozen application-canonical with no SDK alternative (covered by §5.3 layer rule), F3 output DTO frozen with no SDK wrapper. Decision: lock **ship 1 SDK shell** — this was structurally implied by §5.1 (defer recorder); this is structural confirmation. **§5.4** evidence: `ProofFrameDiff` is `@dataclass(frozen=True)` at `proof_frame_diff.py:123-148` with 4 canonical fields + `__post_init__` validation, no mutable state / no IO / no store reference; `ProofFrameDiffView` opt-in audit (`grep` for `ProofFrameDiffView(` shows usage only in `kernel/tests/test_walker_*` and `kernel/application/walker/views.py:376` definition site — zero call sites in `kernel.sdk` or `kernel.application` runtime code); `kernel.sdk.__all__` length verified at HEAD `75f2826` = 34. All 3 falsifiers PASS — F1 frozen+canonical, F2 walker view opt-in (not auto-wrapped), F3 `__all__` boundary unchanged. Decision: lock **documented passthrough of raw `ProofFrameDiff`**; no SDK wrapper; walker view stays advanced importable, opt-in (mirrors G2's `recheck_proof_frame` ↔ `ProofFrameView` opt-in pattern). **Forward implications (§5.2 + §5.4 combined):** final SDK signature shape now fully bounded by §5.1+§5.2+§5.3+§5.4 — `SDKStore.diff_proof_frames(round_a_id, round_b_id, round_a_events, round_b_events, *, warnings=(), include_unchanged=False) -> ProofFrameDiff` (raw passthrough, not in `kernel.sdk.__all__`); §5.5 mechanical (single shell, inline validation); §5.6 → `kernel/sdk/shells/proof_frame_diff.py`; §5.7 → `SDKStore.diff_proof_frames` Group A; §5.8 + §5.9 remain as final substantive batch. Blueprint §5.x order cleanup: misplaced §5.4 block between §5.2 and §5.3 removed; canonical Decision block now lives at the proper §5.4 position after §5.3. |
| 2026-05-08 | §5.5 + §5.6 + §5.7 batch-locked — single shell file, inline validation, `SDKStore.diff_proof_frames(...)` Group A | Read-only mechanical falsifier batch building on §5.1-§5.4. **§5.5** (module placement + shared validator): zero hits for `RoundEvent` / `kernel.audit` in `src/kernel/sdk/shells/` — single-consumer pattern, inline validation sufficient. G4 precedent shows single-method shells get a single shell file (`why_not.py` only). Decision: lock single shell file at `kernel/sdk/shells/proof_frame_diff.py`; no new shared validator (extraction trigger established at G3 Phase 0 only fires when 2+ shells need a shared check). Total `kernel/sdk/shells/` module count after G5 publishes: 9 (current 8 + new). **§5.6** (file naming): `proof_frame_diff.py` collision-free vs existing 8 shell files; cleanly separates from G2's `proof_frame.py` (which holds the *recheck* shell); naming mirrors A-side `kernel.audit.proof_frame_diff` 1:1 (matches G1+G4+G2+G3 pattern of shell file ↔ A-side module). Decision: lock `kernel/sdk/shells/proof_frame_diff.py` with shell function `sdk_diff_proof_frames(sdk, round_a_id, round_b_id, round_a_events, round_b_events, *, warnings=(), include_unchanged=False) -> ProofFrameDiff`. **§5.7** (SDKStore method name): `grep` `^    def (diff_|proof_frame_)` in `src/kernel/sdk/store.py` returns zero hits — no method-name collision (G2's `recheck_proof_frame` starts with `recheck_*`, distinct prefix); Group A `diff_proof_frames` is verb-first (action shape, "diff these proof frames"), consistent with G1/G2/G3/G4 verb-first L methods; Group B `proof_frame_diff` would be noun-shaped and collide with the DTO name `ProofFrameDiff` (same anti-pattern G3 §5.7 rejected for `disable_rule` colliding with `add` family). Round 8 audit hint (`30_recommendation.md:610`) also supports Group A. Decision: lock `SDKStore.diff_proof_frames(round_a_id, round_b_id, round_a_events, round_b_events, *, warnings=(), include_unchanged=False) -> ProofFrameDiff`. **Forward implications:** post-G5-publish counts — `kernel/sdk/shells/` 9 modules; `SDKStore` 40 methods; `kernel.sdk.__all__` length still 34; Sibling discipline scope = 9 shells. Only §5.8 (error mapping) + §5.9 (tests + invariants + `#P1` retrofit count) remain before scope-freeze. |

## Decision Notes

- G5 starts from G3 topic tip `112401a` (G3 published HEAD `cb6d3bd` plus post-publish polish: shared-helper sibling-test symmetry, dropped unreachable `ValueError`, structural-lag note recorded in `memory/current.md` and G3 archive audit log addendum). Forward-only inheritance.
- G5 closes the L Direction sequence (G1 → G4 → G2 → G3 → G5 per Q1 split). After G5, every L-Full slot from the 2026-05-07 round-story-completion target is reachable through `SDKStore.<method>(...)` without dropping to advanced importable.
- The draft deliberately does not answer §5.1-§5.9. Per `feedback_iterative_gap_design`, each Step 0 decision needs a source-grounded falsifier pass before `scoped`.
- Core tensions to resolve in order:
  - **§5.1 first** — ship recorder vs defer is a category decision that determines how many shell files G5 adds (1 vs 3+). Round 8 hinted defer; falsifier confirms or contradicts. Until §5.1 locks, §5.5 / §5.6 / §5.7 / §5.9 file-count and naming questions cannot be finalized.
  - **§5.3 second** — cross-cutting precedent layer scope. The first time `kernel.audit` DTOs cross the SDK boundary; either extend the G2 precedent or carve `kernel.audit` out. This decision binds G5 only but informs any future SDK shell over audit-layer DTOs.
  - **§5.2 / §5.4** — likely low-suspense (almost certainly ship Diff with passthrough return); could be batched after §5.1 + §5.3.
  - Remainder (§5.5-§5.9) follow once §5.1 and §5.3 are settled.
- Frontier stays advanced importable per G4 §5.4 evaluator drift gate.
- Post-L SDK ergonomics redesign (`feedback_sdk_ergonomics_redesign_target`) waits until G5 closes; G5 stays on flat `SDKStore.<method>` pattern with verb-first naming.
