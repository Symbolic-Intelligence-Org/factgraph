# Task Blueprint Audit: Frozen Assertion View Model

- Blueprint: [2026-05-11_frozen-assertion-view-model.md](./2026-05-11_frozen-assertion-view-model.md)

## Event Log

| Date | Stage | Event | Notes |
| --- | --- | --- | --- |
| 2026-05-11 | draft | Blueprint created | Initial scope records candidate model: `View = named frozen assertion-id selection`; separates assertion collection, named view membership, and projection/conflict policy; source-grounding required before any implementation. |
| 2026-05-11 | draft | Frame: non-goals + problem framing | §3 Non-goals strengthened with frozen-only / no dynamic predicate / no runtime-integration first slice bullets. §1 Problem extended with framing: redefining long-term meaning of existing `fg.views`, not a parallel mechanism; current code split between `fg.views` registry and `project_view_facts(...)` is existing design debt not addressed in this slice. |
| 2026-05-11 | draft | §5.1 LOCKED | View identity locked: a FactGraph view is a named frozen assertion-id selection, not a subgraph, not a dynamic predicate, and not a projection policy. This redefines the long-term meaning of existing `fg.views` rather than creating a parallel mechanism; current `ViewSpec` remains legacy projection-policy compatibility pending §5.5 / §5.8. |
| 2026-05-11 | draft | §5.8 LOCKED | Existing `ViewSpec` registry behavior remains compatible in this blueprint. `fg.views.create/update(name, ViewSpec)`, `fg.read.find(..., view=...)`, and `fg.run(..., view=..., return_display_meta=True)` must keep current behavior. `ViewSpec` is documented as legacy projection-policy compatibility; any `ProjectionSpec` rename/migration is deferred. |
| 2026-05-11 | draft | §5.5 LOCKED | Projection policy split locked: view membership is the named visible assertion-id set; `confidence_strategy` / `prefer_source` are projection-policy concerns, not membership. First slice preserves existing `ViewSpec` behavior and defers any `ProjectionSpec` or runtime projection-option migration. |
| 2026-05-11 | draft | §5.7 LOCKED | Rule/runtime integration deferred. Frozen assertion views do not affect `fg.run(rule, view=...)` fact universe, `fg.evaluate(...)`, `project_view_facts(...)`, or application helpers in this slice; current display-meta behavior remains. Closing the SDK `fg.views` registry / core projector split requires a follow-up blueprint. |
| 2026-05-11 | draft | §5.4 LOCKED | View creation/update shape locked: additive `fg.views.create/update(name, asrt_ids=[...])` and `asrts=[...]` overloads ship alongside legacy `ViewSpec` payloads. `asrts` is convenience only; stored membership is immutable deduplicated `asrt_id` set. `get/list` read back mixed legacy `ViewSpec` and non-exported `FrozenAssertionView` entries. `update` is whole-set replacement; `patch` and `diff` are deferred. |
| 2026-05-11 | draft | §5.3 LOCKED | Assertion collection operation model locked: `active` / `history` are base-set selectors; `at(t)` / `version(v)` / `where(...)` / `by_id(...)` filter the current `AssertionRecordSet`; `.one()` / `.first()` / `.all()` are terminals. `FieldAssertions.active/history` remain properties; `FieldAssertions.at/version` stay compatibility shortcuts over the active base set. `.at(t)` keeps ISO 8601 timestamp validation and `[valid_from, valid_to)` business-time semantics. Graph/entity entrypoint shape deferred to §5.2 / §5.6; rule/runtime temporal semantics remain deferred per §5.7. |
| 2026-05-11 | draft | §5.2 LOCKED | Graph-level assertion access locked to narrow by-id lookup: introduce read-only `fg.assertions.by_id(...) -> AssertionRecord | None` and `fg.assertions.by_ids(Iterable[str]) -> AssertionRecordSet` to close the `FrozenAssertionView.asrt_ids` readback loop. Full graph-wide enumeration (`active/history/where/at/version`) and entity-level whole-snapshot assertion collection are not shipped in this slice; richer graph assertion record shape and application/runtime questions deferred. Unknown ids are skipped by default; no `strict=` parameter ships. This is distinct from the archived `fg.read.assertions(...)` selector rejection. |
| 2026-05-11 | draft | §5.6 LOCKED | Read API behavior locked: legacy `ViewSpec` behavior on `fg.read.find(...)` and rule display-meta remains unchanged; `fg.read.get(...)` stays identity-only; frozen assertion views are not consumed by snapshot projection in this slice. `fg.read.find(..., view=<FrozenAssertionView>)` and rule display-meta with a frozen assertion view raise clear SDK errors that point to `fg.views.get(name).asrt_ids` plus `fg.assertions.by_ids(...)`. Entity visibility / field projection / view-scoped history questions are moot in this slice and deferred to follow-up read-scoping work. |
| 2026-05-11 | draft | §6 invariants updated | Original seed §6 replaced with 8-group invariants (ledger/assertion identity; `fg.views` and `FrozenAssertionView`; `AssertionRecordSet` extension; `FieldAssertions` compatibility; `fg.assertions`; read API behavior; runtime/rule integration; public surface/repository safety) reflecting §5.1-§5.8 LOCKED. No new behavior introduced beyond locked decisions; archived assertion-selection blueprint §6 preserved as additive baseline. |
| 2026-05-11 | draft | §7 acceptance updated | Original seed §7 replaced with 3-group acceptance (closed §5 gates / scope-freeze gates / implementation gates). Implementation gates are sub-grouped to mirror §6 eight invariant groups: ledger/identity, `fg.views` and `FrozenAssertionView`, `AssertionRecordSet` extension, `FieldAssertions` compatibility, `fg.assertions`, read API behavior, runtime/rule defer, and public surface/repository safety. |
| 2026-05-11 | draft | §8 implementation plan updated | Replaced draft-stage falsifier plan with 5-phase execution path: Phase 0 pre-scope-freeze scan + frame-pass clarity verification; Phase 1 test scaffolding across behavior-focused SDK test modules mirroring §7 implementation sub-groups; Phase 2 single-cycle implementation in the SDK ergonomics layer (`src/kernel/sdk/facade.py` + `store.py`) for `AssertionRecordSet` extension, `FrozenAssertionView`, `fg.views` overloads, `fg.assertions` namespace, and view-dispatch rejection; Phase 3 SDK docs sync across 4-5 docs; Phase 4 verification across §7 gates, forbidden-path checks, `__all__` stability, §10 close-out, and status flip. Application / authoring / core / `sdk.__init__` changes explicitly forbidden. |
| 2026-05-11 | draft | Pre-scope-freeze scan completed | Scanned SDK docs/examples/notebooks for stale `view` / `ViewSpec` language. Findings are docs-sync work, not §5 re-open triggers: `00_user_guide.en.md` still frames `ViewSpec` as the view model and shows `get/list -> ViewSpec`; `02_readwrite_and_ingest.en.md` describes `find(..., view=...)` only as `ViewSpec | str` projection; `04_api_surface.en.md` still lists `fg.views.create/update(name, view_spec)` and `list() -> dict[str, ViewSpec]`; the working design note needs alignment if it remains referenced. `03_rules_and_derivations.en.md` correctly says `sdk.evaluate(..., view=...)` is unsupported. Release-facing examples do not teach frozen assertion view behavior; archived examples are excluded from first-slice docs sync. All stale items are deferred to Phase 3 docs sync; no §5 decision re-opened. |
| 2026-05-11 | scoped | Scope frozen | Blueprint status moved `draft -> scoped` after §6 / §7 / §8 review, pre-scope-freeze scan, stale-doc handling decision, and frame-pass clarity verification all passed. Implementation may proceed through Phase 1 test scaffolding under the locked §5 decisions and §6 invariants. |
| 2026-05-11 | scoped | Phase 1 test scaffolding added | Added focused SDK tests for frozen assertion view model behavior: `AssertionRecordSet` `.at/.version/.by_id` filters and chainability; `FrozenAssertionView` create/update/get/list semantics and legacy `ViewSpec` compatibility; `fg.assertions.by_id/by_ids` readback; frozen-view rejection in `fg.read.find(...)` / `fg.run(...)`; public surface and `project_view_facts(...)` signature stability. Current red baseline is expected because Phase 2 implementation has not landed: new FrozenAssertionView / fg.assertions / AssertionRecordSet extension tests fail while legacy compatibility and existing boundary tests remain green. |
| 2026-05-11 | scoped | Phase 2 SDK implementation completed | Implemented frozen assertion view model in the SDK ergonomics layer only: `AssertionRecordSet` gained `.at(...)`, `.version(...)`, and `.by_id(...)`; non-exported `FrozenAssertionView` stores membership as `frozenset[str]`; `fg.views.create/update(...)` accept legacy `ViewSpec`, `asrt_ids=[...]`, or `asrts=[...]` as mutually exclusive payloads; `fg.assertions.by_id/by_ids(...)` provides by-id readback; `_resolve_view_spec(...)` rejects frozen views on `fg.read.find(...)` / `fg.run(...)` with workaround guidance. Focused Phase 1 suite is green (31/31); related SDK suites are green (39/39); `ruff` passes. Application / authoring / core / `sdk.__init__` remain untouched. |
| 2026-05-11 | scoped | Phase 3 docs sync completed | Updated SDK docs and the referenced working design note for frozen assertion view semantics: `00_user_guide.en.md` now explains long-term view meaning, legacy `ViewSpec` compatibility, `FrozenAssertionView` creation/update/readback, and frozen-view rejection; `01_concepts.en.md` records `AssertionRecordSet` filters and frozen assertion views; `02_readwrite_and_ingest.en.md` narrows `view=` wording to legacy `ViewSpec` projection and points to `fg.assertions.by_ids(...)`; `04_api_surface.en.md` documents `fg.assertions` and mixed `ViewSpec | FrozenAssertionView` view readback; `read-write-snapshot-assertion-selection.zh.md` distinguishes legacy `ViewSpec`, `FrozenAssertionView`, and field-level `at(t)`. Phase 0 stale patterns are resolved or explicitly marked legacy; no new §5 re-open. |
| 2026-05-11 | implemented | Phase 4 verification and close-out completed | Blueprint status moved `scoped -> implemented`; §7 implementation gates marked passed. Verification: focused frozen-view suite green (31/31), related SDK suites green (39/39), full kernel discovery green (1814 tests, 1 skipped), `ruff` clean, `git diff --check` clean, forbidden paths clean (`src/kernel/application/`, `src/kernel/authoring/`, `src/kernel/core/`, `src/kernel/sdk/__init__.py`, archived blueprints), `kernel.sdk.__all__` remains 35 with `FrozenAssertionView` / `AssertionRecordSet` unexported, and `project_view_facts(...)` signature remains `(ledger, schema_ir)`. §10 Outcome / Deviations filled; archive remains next step after commit. |

## Decision Notes

Append key decisions chronologically, especially:

- scope freeze
- scope expansion or reduction
- implementation blocker
- source-grounding result that changes current view compatibility assumptions
- module docs sync completed
- archive completed
