# Post-L SDK Ergonomics Redesign Audit Log

| Date | Event | Notes |
|---|---|---|
| 2026-05-09 | Draft seeded | Created blueprint on `codex/v0.1-post-l-sdk-ergonomics-redesign-2026-05-09` (branched from post-G5 topic tip `1752372` = G5 published HEAD `d4ceb3e` + publish memory sync `a64045e` + post-publish doc polish `1752372`). **Explicit design-only first framing per user instruction:** no SDK code change, no `kernel.sdk.__all__` change, no migration commitment until §5 reaches `scoped`. The first Step 0 (§5.1) is purely a research deliverable — inventory + option evaluation, NOT direction-lock. Per `feedback_sdk_ergonomics_redesign_target`, OpenAI-style ergonomics is a direction input the user has flagged, not an automatic migration target; falsifier passes must source-ground every choice. Per `feedback_iterative_gap_design`, each §5.x question gets a fresh source-grounded falsifier pass before lock. Captured: §1 Problem (30 user-facing flat-surface methods on `SDKStore`, mixed verb-first L naming vs shorter pre-L verbs, single existing nested namespace `SDKStore.views` as precedent, OpenAI-style direction input deferred during L Direction); §2 Goals (design-only deliverable at scope-freeze, source-ground every choice, preserve current flat compat unless scoped explicitly decides otherwise, all 9 main design questions answered, migration assessment if non-no-change); §3 Non-Goals (no SDK code during draft, no L-shell relitigation, no `kernel.application` / `kernel.audit` / `kernel.core` changes, no Frontier promotion, no recorder promotion, no Path B published-snapshot modification, no automatic OpenAI-style migration); §4 Current Context (4.1 inventory grouped by family with all 30 methods + 5 views sub-methods + properties; 4.2 industry references including SQLAlchemy `Session` counter-pattern raising "Session over knowledge graph vs Client over capabilities" question; 4.3 examples/notebooks impact surface — 4 notebooks + round_story_full_demo + READMEs CN/EN; 4.4 inheritance from L Direction — cross-boundary DTO layer rule, recorder + Frontier advanced importable, Sibling discipline at 9-shell scope, Path B immutability, `kernel.sdk.__all__` length 34, sacred branches; 4.5 memory inputs); §5 Design — 9 Step 0 questions enumerated: §5.1 inventory + evaluate 7 candidate shapes A-G (flat/grouped namespaces/top-level Client/aliases-only overlay/etc.), §5.2 namespace shape if non-flat, §5.3 top-level client class naming, §5.4 compat / deprecation strategy, §5.5 docs / quickstart impact, §5.6 aliases vs replacement, §5.7 the meta-question whether redesign ships at all (explicitly allows "no change" as legitimate scoped outcome), §5.8 roadmap / version stamping, §5.9 tests + invariants; §6 Boundaries (provisional); §7 Acceptance with draft-stage checked + scoped-stage and implementation-stage unchecked; §8 Implementation Plan (TBD post-§5.7 with 3 outcome scenarios: no-change / alias-overlay / replacement); §9 Outcome (placeholder). Status: `draft`. No Phase 1 / lock work authorized yet. Sacred branches `master` and `v0.1-oss-prep` and 5 published L Path B snapshots all stay immutable per `project_release_branch_invariants`. |
| 2026-05-09 | Core thesis locked — D "namespace teaches the conceptual model; compat is hard constraint; industry shapes are evidence not templates" | Per user direction, locked thesis D as the redesign's core principle BEFORE starting §5.1 falsifier pass — without a thesis, §5.1's 7-shape evaluation would be pattern-matching against industry SDKs without a yardstick. Added §0 Core Thesis section to blueprint (positioned before §1 Problem) with: (i) thesis verbatim ("The redesign should make the SDK namespace teach FactPy's conceptual model at first contact. Compatibility is a hard constraint, and industry SDK shapes are evidence, not templates."); (ii) §0.1 implications for §5.x (legibility-first for §5.1, mental-model-reset for §5.3, never-break-flat-default for §5.4, smallest-version-rule for §5.7); (iii) §0.2 rejected alternatives — A "Conservatism first" (overly conservative; ignores that L Direction blueprints already cleared the bar by citing post-L as venue), B "Compatibility purity" (overly compat-bound; collapses to no-op overlay with no design judgment — kept as strategy under §5.4/§5.7, NOT as thesis), C "Conformance-driven" (overly templated; FactPy is knowledge-graph reasoning, may inherit from `Session over unit of work` more than `Client over resources`); (iv) §0.3 explicit non-pre-commitments (does not pre-commit to nesting, to `Client` class name, to migration; does not relitigate L shells). Thesis-derived primary falsifiers added inline: §5.1 new F0 (legibility test on `dir(client)` / IDE autocomplete against §4.1 conceptual layering — first axis, upstream of migration cost F4), §5.2 new F0 (conceptual-layer legibility — grouping that scores high on F1 verb→namespace inference but fragments the layering is rejected), §5.3 new F0 (mental-model-reset test — fresh top-level name vs overloading `SDKStore`; industry-alignment F3 reframed as "evidence to weigh, not template to copy"), §5.4 thesis-derived default ("Never break the flat surface; B/C/D candidates require standalone falsifier"), §5.7 thesis test ("does the chosen shape produce a legibility gain large enough to justify migration cost"; legibility-as-primary-axis encoded in F1 recap). §6 invariants extended with: thesis verbatim as first invariant; F0-legibility primacy across §5.1/§5.2/§5.3; compat-hard-constraint statement above the existing `__all__` length 34 line. §7 Acceptance draft-stage extended with new checked item "§0 Core Thesis locked" recording all the above. Source-grounded reasoning for thesis D: (a) every G1-G5 archive blueprint cited "post-L SDK ergonomics redesign blueprint" as the venue — implicit acknowledgement that L's incremental cadence couldn't decide aggregate shape, so post-L IS the venue; (b) `feedback_sdk_ergonomics_redesign_target` flagged OpenAI-style as direction input, not migration target — falsifier discipline must source-ground; (c) `feedback_narrow_public_api` keeps surface-expansion bar high, which the thesis honors (compat as hard constraint, no-change as legitimate scoped outcome). No SDK code change yet; status remains `draft`. Thesis lock is design-only metadata. Sacred branches and 5 Path B published L snapshots all stay immutable. |
| 2026-05-09 | §5.3 locked — top-level entrypoint class name `FactGraph` (out-of-order from §5.1; 4-round chat-driven F0 falsifier pass; reference-bundle source-grounded post-lock) | 4-round chat-driven falsifier pass under §0 thesis F0 (mental-model-reset). **Round 1** initial enumeration produced candidates `Client`, `FactPyClient`, `Store`, keep `SDKStore`, `Kernel` from existing §5.3 candidates. **Round 2** I expanded to include `FactPy` (eponymous), `Session` (Pythonic), `KnowledgeBase` (AI/Prolog), `FactGraph` (descriptive), with 信达雅 (faithful/expressive/elegant) evaluation axis per user request. Under teaching-primacy §0 thesis, ranked: `FactGraph` (9 chars, teaches domain+structure) > `KnowledgeBase` (13 chars, teaches full domain) > `Session` (Pythonic but pattern-not-domain) > `FactPy` (eponymous, identity-not-model). **Round 3** user raised concern that `factpy.FactGraph` introduces Fact-Fact morpheme overlap; I initially over-rejected on grounds of "Convention 3 violation" — claim corrected in Round 4 by source-grounding: Go has formal "avoid stutter" rule (`effective_go` + Google Go Style Guide; `bytes.Buffer` not `bytes.BytesBuffer`); Python has no equivalent PEP. Python empirical practice prefers no-overlap but explicitly accepts it in major libraries — `tensorflow.Tensor`, `pyspark.sql.SparkSession`, `pyspark.SparkContext`, `pyparsing.ParserElement` — where the repeated morpheme strengthens (rather than dilutes) the abstraction's identity. **Round 4** user locked `FactGraph`. **Post-lock reference-bundle source-grounding:** read [post-routemap direction selection input bundle](../../references/working/post-routemap-direction-selection-input/) (8 files, 2231 lines, 9 verification rounds, design-complete) to source-ground the §5.3 lock against existing project conventions. Bundle findings absorbed into §4.6: (a) `SDK*` family is established (`SDKStore` + `SDKBatchTx` with prefix; descriptive siblings `EntitySnapshot` / `FieldAssertions` / `AssertionNamespace` / `EntityEditor` / `AssertionRecord`) per [30_recommendation.md:570-577](../../references/working/post-routemap-direction-selection-input/30_recommendation.md) — implies `SDKStore` rename has cascade cost, motivating §5.4 likely-permanent-dual-surface; (b) `EntitySnapshot.assertions` namespace pattern at [src/kernel/sdk/facade.py:102-217](../../../src/kernel/sdk/facade.py) is established prior art (`__getattr__` + `FrozenSnapshotError`) for nested namespaces — redesign sub-namespacing reuses this pattern, not invents; (c) `EvidenceGraph` audit substrate at `src/kernel/audit/evidence_graph.py:60` is the unified cross-engine explainability DTO durable as `audit/evidence_graphs.jsonl`, queryable via `AuditQuery.get_candidate_evidence_graph()` — lives in `kernel.audit` advanced importable, NOT in `factpy.__all__`; **F2 verdict: clean** (different word "Fact" vs "Evidence", different package, different concept; users importing `from factpy import FactGraph` do not naturally encounter `EvidenceGraph`); (d) L-Full follow-on at [30_recommendation.md:600+](../../references/working/post-routemap-direction-selection-input/30_recommendation.md) explicitly anticipated this redesign blueprint — L Direction (G1-G5) shipped per Batch 8 §5.5.5 reactivation discipline; redesign trigger met per `#6` "no outward compat without user signal" lessons-learned principle; (e) `import naming (factpy vs kernel.sdk)` deferred per [README.md Round 9 close](../../references/working/post-routemap-direction-selection-input/README.md) — separate parallel question to §5.3 class name, out of §5.3 scope. Decision rationale: F0 mental-model-reset = high (`factpy.FactGraph` teaches "graph of facts" before any docstring read); F1 collision = none (zero hits in `src/`/`docs/`/`examples/` for `FactGraph`); F2 substrate ambiguity = clean (EvidenceGraph isolated to audit advanced importable); F3 industry = `tensorflow.Tensor` / `pyspark.sql.SparkSession` / `pyparsing.ParserElement` precedent for same-morpheme repetition; F4 compat-impact = §5.4 question. Rejected with reasons: `Client` (wrong category signal — not hosted service), `FactPyClient` (compound rejection), `Store` (substrate `kernel.core.store.Store` collision), keep `SDKStore` (fails F0 + substrate name clash), `Kernel` (package namespace `kernel.*` collision), `FactPy` (eponymous wrong category signal — Python eponymous reserved for hosted-service SDK / web framework; no variable-name convention; forecloses future top-level types), `Session` (teaches pattern not domain; FactPy has no commit/rollback so SQLAlchemy migration knowledge partially misleads), `KnowledgeBase` (strong runner-up; lost to FactGraph on conciseness 13→9 chars + "Fact" core-abstraction emphasis vs generic "knowledge"). **§5.3 locked OUT of order from §5.1** — chat-driven F0 falsifier pass was sufficiently exhaustive across 4 rounds with 4 expanded candidate iterations + post-lock reference-bundle source-grounding; §5.1 (shape evaluation) now proceeds with `FactGraph` as the new top-level name (candidate shape D and shape F more naturally framed under `FactGraph`; shape A status quo trivially applies if `FactGraph` is literal alias of `SDKStore`; shape B and E sharply distinguished — E becomes "FactGraph + nested namespaces"). §5.4 inherits constraint that `SDKStore` callable surface stays per thesis hard-constraint; `SDK*` family pattern preservation likely lands §5.4 on permanent dual surface (alias `FactGraph = SDKStore` literal, OR thin subclass, OR dedicated wrapper — §5.4 falsifier confirms). §6 invariants extended with locked-name + relationship-with-SDKStore deferred-to-§5.4. §7 Acceptance scoped-stage marked `[x]` for §5.3. §4.6 added new section recording reference-bundle inputs with cross-references. Sacred branches and 5 Path B published L snapshots all stay immutable. No SDK code change; status remains `draft`. |
| 2026-05-09 | Implementation branch isolation locked — design / impl branches separated | Per user direction "为了防止改动后全线崩盘, 我们在一个新的git 分支实现" (to prevent cascade failures from implementation changes, implementation happens on a dedicated branch). Created sibling branch `codex/v0.1-post-l-sdk-ergonomics-redesign-impl-2026-05-09` at design HEAD `59a5694` via plain `git branch <name> HEAD` (no worktree yet — sequential fork rather than parallel topic, so basic branch ref suffices). Encoded the isolation rule in three blueprint locations: (a) §3 Non-Goals new bullet "No SDK code change on the design branch" — this branch holds blueprint / audit-log commits only, all implementation work post-scope-freeze migrates to the impl branch; (b) §6 Boundaries new invariant "Design / implementation branch isolation" — impl branch is rebased onto design HEAD at scope-freeze time before implementation begins; (c) §6 Boundaries §5.3 lock entry extended with "Design / implementation branch isolation in force" — impl branch is the only authorized location for code changes post-scope-freeze; cross-contamination is a structural invariant violation. **Why now:** even though current branch has only doc-only commits (impossible to break runtime), preemptive branch creation + structural recording prevents a class of failure mode where a future implementation phase commits code into the design branch by mistake (e.g., during rapid `EnterWorktree` switching, or when scope-freeze ambiguity makes "is this design or impl?" unclear). The branch ref now exists; future implementation work can checkout into it cleanly. **Workflow:** continue all §5.x design locks on this branch; at scope-freeze (status `draft → scoped`), checkout impl branch + rebase onto design HEAD + start implementation. **Sacred branches still untouched:** `master`, `v0.1-oss-prep`, and 5 published L Path B snapshots. **Worktree decision:** plain `git branch` ref rather than `git worktree add` — implementation is a sequential fork from design (not parallel topic per `feedback_worktree_parallel_implementation`), so worktree adds operational complexity without immediate value. If parallel design + impl work becomes needed, worktree can be added later via `git worktree add /path codex/v0.1-post-l-sdk-ergonomics-redesign-impl-2026-05-09`. No SDK code change yet; status remains `draft`. |
| 2026-05-09 | §5.1 deliverable — read-only research at HEAD `60af8bc`; inventory verified, 5-shape evaluation under thesis F0/F4/F5; Shape 3 leading, Shape 2 additive-defensible, Shape 1 conservative-default, Shape 4 requires standalone falsifier, Shape 5 weaker than 3 | Per user direction "run §5.1 read-only" with constraints (design branch only; flat `SDKStore.<method>` remains callable unless §5.4 later proves otherwise; OpenAI-style is evidence not template; no implementation on design branch). **Inventory source-grounded** at `src/kernel/sdk/store.py` HEAD `60af8bc`: confirmed 30 user-facing methods on `SDKStore` (1 classmethod `from_schema_classes` + 29 instance methods; matches §4.1 exactly), 4 properties (`store`, `ledger`, `schema_ir`, `views`), 1 nested namespace `SDKStore.views` (5 sub-methods at `_SDKViewsManager` private class `store.py:66-104`); total user-facing callables 35; `kernel.sdk.__all__` length 34 verified by reading `kernel/sdk/__init__.py:30-65`. Mapped 14 conceptual families (Schema authoring/Read/Write-single/Write-batch/Eval/Accept/L-G1/L-G4/L-G2/L-G3/L-G5/Audit/Package/Views) consolidating to ~8 top-level concepts (`schema`/`read`/`write`/`eval`/`what_if`/`audit`/`package`/`views`) under one consolidation. **Migration cost surface source-grounded:** 23 `kernel/tests/test_sdk_*.py` files with ~150+ `SDKStore.<method>` mentions (top: `diff_proof_frames` 7×, `why_not` 6×, `recheck_proof_frame`/rule-overlays/`check_fact_overlay` 4× each, `check`/`diagnose` 3× each per representative file across multiple files); critical insight — 4 chaptered notebooks `examples/0[1-4]_*.ipynb` only contain **1 `SDKStore.<method>` mention total** (just `diff_proof_frames` in 04 notebook); `examples/round_story_full_demo.py` has **0** SDKStore method mentions (uses `kernel.application` / `kernel.core` directly). Notebooks were authored Tier-2-style (advanced importable, predating L Direction); migration cost is concentrated in tests + API docs, NOT in user-facing examples — bounds practical migration cost more than initial §4.3 estimate. **5-shape evaluation per user instruction** mapped to original A-G (Shape 1 = A status quo flat; Shape 2 = B additive nested aliases; Shape 3 = B/C variant canonical-nested-via-docs-lever + flat compat; Shape 4 = C/D replacement-deprecation; Shape 5 = F hybrid/staged). E "new top-level Client class" moot post-§5.3 lock — `FactGraph` IS the new top-level. Each shape evaluated under F0 (`dir(FactGraph)` legibility test, thesis-primary), F4 (migration cost), F5 (Session-vs-Client conceptual fit), with concrete `dir(fg)` projection where applicable. Shape 1 verdict: F0 fails (alphabetical interleaving doesn't teach 14 families), F4 zero, F5 Session-fit; conservative default, falls to thesis. Shape 2 verdict: F0 mixed-partial (namespaces help autocomplete, raw `dir()` still cluttered with both shapes), F4 near-zero, F5 hybrid; most defensible additive under `#6 — no outward compat without user signal`. Shape 3 verdict: F0 better than Shape 2 via docs/quickstart lever (with optional `__dir__` override though that has subtle cost), F4 medium (docs rewrite extensive), F5 Client-leaning with Session-compat; **leading candidate under thesis**. Shape 4 verdict: F0 strong eventually (only namespaces), F4 high (breaks 23 test files + Path B audit confusion), F5 Client-replacing-Session; **violates §0 thesis hard-constraint** (compat is hard constraint), requires §5.4 standalone falsifier, current evidence basis (Batch 8 falsifier #18 PARTIAL) does not yet justify break. Shape 5 verdict: F0 varies (5a split-personality, 5b confusing, 5c phased-cleaner-but-discipline-cost), F4 moderate-high, F5 mixed; weaker than Shape 3 unless specifically motivated by phased rollout. §5.1 lock outcome: inventory complete; 5 shapes evaluated; ranking under thesis = Shape 3 > Shape 2 > Shape 1 (with Shape 4 / 5 requiring specific carve-outs). **§5.1 is research-only — no direction locked.** Shape decision deferred to §5.7 (ship-at-all meta-question) which weighs F0 legibility gain vs F4 migration cost vs §5.4-determined compat strategy. **§5.2 (namespace grouping) is now the substantive next decision** — the 8-concept consolidation needs F0 conceptual-layer-legibility + F1 verb→namespace-inference + F4 layer-rule preservation evaluation; alternatives include splitting `what_if` into 5 sub-namespaces (G1/G2/G3/G4/G5 family structure) or round-story-flow ordering. §7 Acceptance scoped-stage marked `[x]` for §5.1 with summary. No new §6 invariants from §5.1 (research-only). No SDK code change; status remains `draft`. Sacred branches and 5 Path B published L snapshots all stay immutable. |
| 2026-05-09 | §5.2 evaluation — research-only namespace grouping deliverable (8 top-level concepts + Option B split for `what_if`; lock pending user confirmation on 5 placements) | Per user direction "evaluate namespace grouping independently from rollout shape" with 5 specified falsifier axes. **Top-level 8-concept consolidation evaluated:** `schema` / `read` / `write` / `eval` / `what_if` / `audit` / `package` / `views`. F0 (`dir(FactGraph)` teaches model) STRONG — 8 namespace names form coherent mental model without docstring lookup. F1 (placement) PASS with 3 documented decisions: `validate_provenance` → schema (pre-ingest validation), `diff_proof_frames` → audit (post-hoc comparison of persisted rounds, source-grounded at `store.py:719-795` taking `tuple[RoundEvent, ...]` from `kernel.audit.round_events` and returning frozen `kernel.audit.ProofFrameDiff`), `accept*` → eval (workflow-paired with `evaluate*`). F-layer CLEAN at top level; mild dict-vs-DTO heterogeneity in `audit` group (`explain_fact` / `conflicts` return shallow dict; `diff_proof_frames` returns frozen DTO) — structurally OK per G5 §5.3 layer rule (frozen DTO above kernel.core, not "all returns must be DTO"). F-DTO-rule PRESERVED — grouping doesn't change method signatures; G3 substrate-IR exclusion + G5 cross-cutting precedent layer rule intact. F-anti-resource AVOIDED at top level (operation-mode-leaning, closer to SQLAlchemy `Session.<operation>` than OpenAI `client.<resource>.<verb>`); mild local resource-flavor in sub-namespaces where natural (`views.<verb>` existing precedent). **`what_if` split-vs-unified analysis (the user's specific question):** Option A unified — 8 methods directly under `what_if`, F0 PARTIAL (alphabetized + `check_*` prefix appears 5 times, visually noisy), 0 renames, lowest cost. Option B split — G1+G4 direct (`check`, `diagnose`, `why_not`); G2 sub-namespaced as `what_if.fact_overlay.check` + `what_if.fact_overlay.recheck_proof_frame`; G3 sub-namespaced as `what_if.rule.disable` + `what_if.rule.literal_replace` + `what_if.rule.add_condition`; F0 STRONGER (5 entries instead of 8 in `dir(fg.what_if)`; sub-namespaces reflect L family structure; `check_*` prefix collision dropped at sub-level), 5 alias paths added (no renames since flat methods stay callable per Shape 2/3 compat). **Recommendation: Option B (split)** — wins on F0, naming clarity, and future extensibility (adding 10th L method accommodates naturally); loses on migration cost (5 alias paths) and mild local resource-flavor in `what_if.rule.*`/`what_if.fact_overlay.*` (mitigated: localized to sub-namespace, doesn't propagate to top-level). Full grouping skeleton recorded with 8 top-level namespaces + 2 sub-namespaces (`what_if.fact_overlay`, `what_if.rule`) + existing `views`; 30 user-facing methods preserved verbatim at flat level under Shape 2/3 compat default; 5 nested-short alias paths added (`what_if.fact_overlay.check`, `what_if.rule.disable`, `what_if.rule.literal_replace`, `what_if.rule.add_condition`, plus `what_if.fact_overlay.recheck_proof_frame` keeping verb name). **§5.2 is research-only — NO grouping locked yet.** 5 open questions for user confirmation before lock: (1) Option B split for `what_if`? (recommended); (2) `diff_proof_frames` → `audit` (not `what_if`)? (recommended); (3) `validate_provenance` → `schema` (not `audit`)? (recommended); (4) `accept*` stays in `eval` (not split to `write`)? (recommended); (5) `from_schema_classes` + `batch` stay top-level? (recommended). §7 Acceptance scoped-stage NOT marked `[x]` for §5.2 yet — pending user confirmation on the 5 placements. No SDK code change; status remains `draft`. Sacred branches and 5 Path B published L snapshots all stay immutable. |
| 2026-05-09 | §5.2 locked — teaching taxonomy: 8 top-level namespaces + 2 sub-namespaces under `what_if`; all 5 placements confirmed; teaching-taxonomy ≠ shipping-shape framing locked | Per user direction "Lock all 5 recommended placements" with explicit rationale per placement and critical framing addition: **call this the teaching taxonomy, not the shipping shape**. User rationale captured verbatim per placement: (1) `what_if` Option B split = "right teaching shape; keeps top-level concept small while exposing internal structure of what-if work: direct check/diagnose/why-not, plus fact overlay and rule overlay subfamilies"; (2) `diff_proof_frames` → `audit` = "consumes recorded round events and compares persisted proof-frame outcomes; post-hoc audit, not hypothetical evaluation"; (3) `validate_provenance` → `schema` = "validates package/schema/provenance before ingest rather than auditing runtime behavior"; (4) `accept*` stays in `eval` despite mutation = "mental workflow is evaluation lifecycle: evaluate candidates, then accept them. Splitting to `write` would hide the evaluate→accept flow"; (5) `from_schema_classes` + `batch` stay top-level = "entrypoint/context-construction APIs, not ordinary methods inside conceptual namespaces". **User-introduced framing: "teaching taxonomy ≠ shipping shape"** — §5.2 lock is the locked conceptual model that the redesign uses to organize `dir(FactGraph)` legibility, NOT a commitment to runtime materialization. Whether the taxonomy ships as additive aliases (Shape 2), canonical nested + flat compat (Shape 3), docs-only (Shape 1), or some §5.7-resolved variant is a separate decision deferred to §5.4 (compat strategy) and §5.7 (ship-at-all meta-question). This separation prevents accidentally pre-locking the rollout shape via the taxonomy lock. **Locked taxonomy structure** (encoded verbatim in §5.2.5 Decision block + §6 invariants): 8 top-level namespaces (`schema` / `read` / `write` / `eval` / `what_if` / `audit` / `package` / `views`); 2 sub-namespaces under `what_if` (`fact_overlay`, `rule`); top-level direct surface = `from_schema_classes` (cls) + `batch` (cm) + 4 properties (`store`, `ledger`, `schema_ir`, `views`); 30 user-facing methods preserved verbatim at flat `SDKStore.<method>` level per §0 thesis hard-constraint; 5 nested-short alias paths surface in the taxonomy under `what_if` split (`what_if.fact_overlay.check`, `what_if.fact_overlay.recheck_proof_frame`, `what_if.rule.disable`, `what_if.rule.literal_replace`, `what_if.rule.add_condition`). Lock outcome counts: `kernel.sdk.__all__` length unchanged at 34 (taxonomy is conceptual; runtime materialization deferred). **What this lock does NOT decide:** (a) whether taxonomy ships at all (§5.7); (b) how it materializes (§5.4); (c) whether 5 nested-short alias names are exposed at runtime — depends on §5.4/§5.7. Under Shape 1, taxonomy is documentation artifact only; under Shape 2/3, alias paths materialize as runtime attributes via property/`__getattr__` namespace pattern (per `EntitySnapshot.assertions` prior art at `kernel/sdk/facade.py:102-217`). **§6 invariants extended** with locked teaching taxonomy + "teaching taxonomy ≠ shipping shape" separation rule. **§0.3 thesis non-pre-commitments extended** with explicit teaching-taxonomy-vs-shipping-shape separation. **§7 Acceptance scoped-stage marked `[x]`** for §5.2 with full lock summary. No SDK code change; status remains `draft`. Sacred branches and 5 Path B published L snapshots all stay immutable. **§5.2 lock advances workflow to §5.4** (compatibility/deprecation strategy) — the §0.3-flagged "decides how taxonomy materializes" question is now the substantive next decision; §5.7 (ship-at-all meta-question) follows §5.4-§5.6. |
| 2026-05-09 | §5.4 locked — Option 2: permanent additive aliases + docs prefer new taxonomy (no deprecation; flat `SDKStore.<method>` callable forever) | Per user direction with explicit default hypothesis: "Flat `SDKStore.<method>` remains permanently callable; `FactGraph` + teaching taxonomy can only be additive unless a falsifier proves a breaking migration is necessary." User identified Option 2 as leading; my falsifier pass confirmed. **5 strategies evaluated with source-grounded F0/F4/compat verdicts:** (1) permanent additive no docs preference — compat ✅ but F0 weak (flat + taxonomy compete equally); (2) **additive + docs prefer new taxonomy [LOCKED]** — compat ✅, F0 strong via docs lever, medium docs cost = the F0 teaching investment, no runtime noise; (3) soft docs "deprecated" — rejected per **F-no-removal-plan**: "Deprecated" implies removal; per `#6 — no outward compat without user signal` (cited 30_recommendation.md:95 lessons quote `_evaluate_with_overlay`-style internal-not-public preference) and Batch 8 §5.5.5 falsifier #18 PARTIAL ("not now"), no concrete user signal demands flat removal; marking flat "deprecated" without removal plan misleads users; (4) runtime `DeprecationWarning` — rejected per **F-noise-cost**: existing `row_format='tuple'` `DeprecationWarning` precedent at `store.py:2133-2141` was justified because tuple/dict modes produce **different return shapes** (behaviorally distinct legacy mode); flat-vs-nested produces **identical** results under Option 2 alias semantics; adding warnings to every flat method makes 23 `test_sdk_*.py` files emit warnings on every call (`pytest.filterwarnings('error')` consumers fail), Path B published L snapshots' tests authored without warning expectations, notebook execution noisy at every cell — runtime cost unjustified given flat ↔ nested behavioral identity; (5) hard removal — rejected per **F-compat-violation**: violates §0 thesis hard-constraint without standalone falsifier; Path B published snapshots ship flat surface, removal breaks retroactive auditability. **Locked Option 2 falsifier check passes:** compat hard-constraint preserved (flat permanently callable; tests + Path B snapshots untouched); `#6` honored (additive surface only, triggered by `feedback_sdk_ergonomics_redesign_target` signal); F0 thesis legibility achieved via docs lever (quickstart + notebooks + 04_api_surface.md lead with `FactGraph.<namespace>.<method>`); Path B immutability preserved (current/future docs evolve forward; snapshots stay frozen with original flat-only framing); maintenance liability check — aliases delegate to flat via property/manager-class pattern per `views` precedent (`store.py:198-200`) + `EntitySnapshot.assertions` precedent (`facade.py:102-217`), near-zero per-namespace cost; §5.7 ship-at-all preserved (Option 2 lock is conditional on shipping; if Shape 1 elected, Option 2's docs prep doesn't ship). **Locked compatibility strategy details:** flat methods callable permanently with no deprecation/removal/runtime-warning; taxonomy materializes as additive runtime aliases under §5.7-conditional Shape 2 or Shape 3 rollout; docs / quickstart / notebooks / API surface lead with new taxonomy; flat methods documented as "supported foundational API" (NOT "deprecated"); existing flat-method tests stay verbatim (zero churn); new alias-form parity tests added under §5.9 (each alias result == flat-method result); Path B published L snapshots stay immutable with original flat-only docs framing. **Alias implementation pattern locked (per `views` precedent):** sub-namespace attributes use property returning private namespace class (e.g., `_SDKWhatIfManager`, `_SDKWriteManager`) with read-only methods that delegate to flat `SDKStore.<method>`; explicit attribute methods for static analyzability + IDE autocomplete; same pattern G2 used to build `kernel/sdk/shells/` subpackage at G2 §5.5 lock. **Implications recorded for downstream §5.x:** §5.5 docs/quickstart impact SUBSTANTIAL (the F0 teaching investment — README, 4 chaptered notebooks, 04_api_surface.md CN/EN, application overview docs all rewrite to lead with taxonomy); §5.6 aliases-vs-replacement RESOLVED implicitly by §5.4 lock (aliases); §5.7 ship-at-all becomes "is the docs rewrite + alias surface investment worth the F0 legibility gain?" with §5.7 still having authority to elect Shape 1; §5.9 tests + invariants (existing tests verbatim, alias-form parity tests added, sub-namespace property invariants for read-only enforcement + alias-vs-flat parity + `__all__` length unchanged at 34). User rationale captured verbatim: "I expect option 2 to be strongest: it gives the redesign teaching value without breaking published L surfaces or turning current tests into migration churn. Runtime warnings are likely too noisy for a library whose flat surface is already valid and published." §6 invariants extended with §5.4 lock entry. §7 Acceptance scoped-stage marked `[x]` for §5.4 with full rationale + rejected-options summary. No SDK code change; status remains `draft`. Sacred branches and 5 Path B published L snapshots all stay immutable. **§5.4 lock advances workflow to §5.5** (docs / quickstart impact assessment) — the F0 teaching investment substance. |
| 2026-05-09 | §5.5 locked — per-surface doc treatment; documentation is the primary delivery vehicle for the teaching taxonomy | Per user direction with explicit framing: "documentation is the primary delivery vehicle for the teaching taxonomy. Since §5.4 rejects deprecation/warnings, docs must make the new shape canonical without calling the flat shape obsolete." User identified 4-row recommended default treatment; my source-grounded inventory + falsifier pass confirmed and refined. **Source-grounded inventory at HEAD `0ff394f` via `grep -cE "(SDKStore|sdk\.[a-z_]+)"`:** README.md+.en.md = ~14 (4 quickstart calls + ~10 overview); `kernel/sdk/docs/00_user_guide.md`+`.en.md` = **319** (162+157, **largest single rewrite surface**, primary teaching investment); `04_api_surface.md`+`.en.md` = 38 (19+19); `02_readwrite_and_ingest.md`+`.en.md` = 72 (36+36); `03_rules_and_derivations.md`+`.en.md` = 47 (25+22); `01_alignment_matrix.md`+`.en.md` = 24 (12+12); `05_cn_en_consistency_checklist.md` = 2; `kernel/application/docs/01_overview.md`+`_en.md` = 18 (9+9); 4 chaptered notebooks = **only 1 SDK method mention** (`diff_proof_frames` in notebook 04); `round_story_full_demo.py` = 0 SDK method mentions (uses `kernel.application` directly). **3-tier per-surface classification locked:** (1) **Taxonomy-first surfaces** (lead with `FactGraph.<namespace>.<method>`): README CN/EN + 6 SDK doc files (`00_user_guide`, `04_api_surface`, `02_readwrite_and_ingest`, `03_rules_and_derivations`, `01_alignment_matrix`, `05_cn_en_consistency_checklist`); (2) **SDK-presentation-only surfaces** (brief paragraph noting `FactGraph.<namespace>.<method>` taxonomy as SDK ergonomic surface; cross-ref to SDK docs; application content unchanged): `kernel/application/docs/01_overview` CN/EN; (3) **Compatibility/reference sections within taxonomy-first docs** (preserve flat as primary content; note taxonomy as alternative; NOT separate "deprecated" labels): "Foundational API" / "Compatibility reference" sections inline within the taxonomy-first docs. **Untouched surfaces:** notebooks 01/02/03 (Tier-2 advanced importable usage; bypass `SDKStore`); `round_story_full_demo.py`; `kernel.application` / `kernel.audit` / `kernel.core` runtime + module docs (taxonomy is purely SDK-outward); L archive blueprints (G1-G5; historical record per Path B immutability); Path B published L snapshots (immutable per `project_release_branch_invariants`); sacred branches (`master`, `v0.1-oss-prep`). **Conditional updates:** notebook 04 — `diff_proof_frames` → `fg.audit.diff_proof_frames` (1-line update IF Shape 2/3 materializes). **Migration guide decision:** **NO separate v0.1 → v0.2 migration guide file** — per §5.4 lock there's no breaking migration; flat surface stays callable; the taxonomy-first docs IS the migration guidance; release notes (or commit message at publish time) suffice. **3-row falsifier verdicts:** F1 (total surface bounded) — ~12 doc files become taxonomy-aware, 0 application/audit/core narrative changes, 0-1 notebook edits; F2 (separate migration guide?) NO — taxonomy-first docs ARE the guide; F3 (retroactive note on L archive blueprints?) NO — Path B immutability + historical-record convention. **§0 thesis F0 alignment check:** new users land on README → see taxonomy-first quickstart → form correct mental model from line 1; drill into `00_user_guide` → see section-by-section taxonomy walk-through aligned with §5.2 conceptual layers; API reference users → `04_api_surface` gives taxonomy-canonical view + flat compat reference; existing flat users → unchanged compat sections, no surprise; F0 teaching achieved without runtime cost. **§5.4 alignment check:** "flat as foundational, NOT deprecated" framing materializes in every surface (compat sections labeled "Foundational API" / "Compatibility reference", not "Deprecated"); no `DeprecationWarning` references in any doc. **Implications recorded for downstream:** §5.6 aliases-vs-replacement RESOLVED implicitly by §5.4 + §5.5 (aliases delegate to flat; new taxonomy is editorial canonical via docs lever); §5.7 ship-at-all weighs docs rewrite cost (concentrated in `00_user_guide` 319 refs + 5 other taxonomy-first docs) against F0 legibility gain — notebook impact confirmed NEGLIGIBLE per inventory; §5.7 still has authority to elect Shape 1 (no change) which would void the §5.5 rewrite — voiding is acceptable since §5.5 lock is contingent on shipping; §5.9 docs-rewrite parity tests (CN/EN both lead with taxonomy in same sections), lint check that taxonomy-first docs don't accidentally introduce "deprecated" labels, `04_api_surface` taxonomy-canonical + flat-compat-reference structural check. User rationale captured verbatim across 4 recommended-default rows: "README + SDK API docs become taxonomy-first" (extended to include `00_user_guide` + topic docs given inventory); "Notebooks stay mostly unchanged unless they naturally demonstrate a namespace" (confirmed: only 04's `diff_proof_frames` line); "Application overview records the taxonomy as SDK presentation, not application truth" (locked as SDK-presentation-only surface); "Flat API docs remain as compatibility/reference section, not deprecated" (locked as inline compat sections within taxonomy-first docs, NOT separate "deprecated" labels). §6 invariants extended with §5.5 lock entry. §7 Acceptance scoped-stage marked `[x]` for §5.5. No SDK code change; status remains `draft`. Sacred branches and 5 Path B published L snapshots all stay immutable. **§5.5 lock advances workflow to §5.7** (ship-at-all meta-question; §5.6 implicitly resolved). |
