# Post-L SDK Ergonomics Redesign Audit Log

| Date | Event | Notes |
|---|---|---|
| 2026-05-09 | Draft seeded | Created blueprint on `codex/v0.1-post-l-sdk-ergonomics-redesign-2026-05-09` (branched from post-G5 topic tip `1752372` = G5 published HEAD `d4ceb3e` + publish memory sync `a64045e` + post-publish doc polish `1752372`). **Explicit design-only first framing per user instruction:** no SDK code change, no `kernel.sdk.__all__` change, no migration commitment until §5 reaches `scoped`. The first Step 0 (§5.1) is purely a research deliverable — inventory + option evaluation, NOT direction-lock. Per `feedback_sdk_ergonomics_redesign_target`, OpenAI-style ergonomics is a direction input the user has flagged, not an automatic migration target; falsifier passes must source-ground every choice. Per `feedback_iterative_gap_design`, each §5.x question gets a fresh source-grounded falsifier pass before lock. Captured: §1 Problem (30 user-facing flat-surface methods on `SDKStore`, mixed verb-first L naming vs shorter pre-L verbs, single existing nested namespace `SDKStore.views` as precedent, OpenAI-style direction input deferred during L Direction); §2 Goals (design-only deliverable at scope-freeze, source-ground every choice, preserve current flat compat unless scoped explicitly decides otherwise, all 9 main design questions answered, migration assessment if non-no-change); §3 Non-Goals (no SDK code during draft, no L-shell relitigation, no `kernel.application` / `kernel.audit` / `kernel.core` changes, no Frontier promotion, no recorder promotion, no Path B published-snapshot modification, no automatic OpenAI-style migration); §4 Current Context (4.1 inventory grouped by family with all 30 methods + 5 views sub-methods + properties; 4.2 industry references including SQLAlchemy `Session` counter-pattern raising "Session over knowledge graph vs Client over capabilities" question; 4.3 examples/notebooks impact surface — 4 notebooks + round_story_full_demo + READMEs CN/EN; 4.4 inheritance from L Direction — cross-boundary DTO layer rule, recorder + Frontier advanced importable, Sibling discipline at 9-shell scope, Path B immutability, `kernel.sdk.__all__` length 34, sacred branches; 4.5 memory inputs); §5 Design — 9 Step 0 questions enumerated: §5.1 inventory + evaluate 7 candidate shapes A-G (flat/grouped namespaces/top-level Client/aliases-only overlay/etc.), §5.2 namespace shape if non-flat, §5.3 top-level client class naming, §5.4 compat / deprecation strategy, §5.5 docs / quickstart impact, §5.6 aliases vs replacement, §5.7 the meta-question whether redesign ships at all (explicitly allows "no change" as legitimate scoped outcome), §5.8 roadmap / version stamping, §5.9 tests + invariants; §6 Boundaries (provisional); §7 Acceptance with draft-stage checked + scoped-stage and implementation-stage unchecked; §8 Implementation Plan (TBD post-§5.7 with 3 outcome scenarios: no-change / alias-overlay / replacement); §9 Outcome (placeholder). Status: `draft`. No Phase 1 / lock work authorized yet. Sacred branches `master` and `v0.1-oss-prep` and 5 published L Path B snapshots all stay immutable per `project_release_branch_invariants`. |
| 2026-05-09 | Core thesis locked — D "namespace teaches the conceptual model; compat is hard constraint; industry shapes are evidence not templates" | Per user direction, locked thesis D as the redesign's core principle BEFORE starting §5.1 falsifier pass — without a thesis, §5.1's 7-shape evaluation would be pattern-matching against industry SDKs without a yardstick. Added §0 Core Thesis section to blueprint (positioned before §1 Problem) with: (i) thesis verbatim ("The redesign should make the SDK namespace teach FactPy's conceptual model at first contact. Compatibility is a hard constraint, and industry SDK shapes are evidence, not templates."); (ii) §0.1 implications for §5.x (legibility-first for §5.1, mental-model-reset for §5.3, never-break-flat-default for §5.4, smallest-version-rule for §5.7); (iii) §0.2 rejected alternatives — A "Conservatism first" (overly conservative; ignores that L Direction blueprints already cleared the bar by citing post-L as venue), B "Compatibility purity" (overly compat-bound; collapses to no-op overlay with no design judgment — kept as strategy under §5.4/§5.7, NOT as thesis), C "Conformance-driven" (overly templated; FactPy is knowledge-graph reasoning, may inherit from `Session over unit of work` more than `Client over resources`); (iv) §0.3 explicit non-pre-commitments (does not pre-commit to nesting, to `Client` class name, to migration; does not relitigate L shells). Thesis-derived primary falsifiers added inline: §5.1 new F0 (legibility test on `dir(client)` / IDE autocomplete against §4.1 conceptual layering — first axis, upstream of migration cost F4), §5.2 new F0 (conceptual-layer legibility — grouping that scores high on F1 verb→namespace inference but fragments the layering is rejected), §5.3 new F0 (mental-model-reset test — fresh top-level name vs overloading `SDKStore`; industry-alignment F3 reframed as "evidence to weigh, not template to copy"), §5.4 thesis-derived default ("Never break the flat surface; B/C/D candidates require standalone falsifier"), §5.7 thesis test ("does the chosen shape produce a legibility gain large enough to justify migration cost"; legibility-as-primary-axis encoded in F1 recap). §6 invariants extended with: thesis verbatim as first invariant; F0-legibility primacy across §5.1/§5.2/§5.3; compat-hard-constraint statement above the existing `__all__` length 34 line. §7 Acceptance draft-stage extended with new checked item "§0 Core Thesis locked" recording all the above. Source-grounded reasoning for thesis D: (a) every G1-G5 archive blueprint cited "post-L SDK ergonomics redesign blueprint" as the venue — implicit acknowledgement that L's incremental cadence couldn't decide aggregate shape, so post-L IS the venue; (b) `feedback_sdk_ergonomics_redesign_target` flagged OpenAI-style as direction input, not migration target — falsifier discipline must source-ground; (c) `feedback_narrow_public_api` keeps surface-expansion bar high, which the thesis honors (compat as hard constraint, no-change as legitimate scoped outcome). No SDK code change yet; status remains `draft`. Thesis lock is design-only metadata. Sacred branches and 5 Path B published L snapshots all stay immutable. |
