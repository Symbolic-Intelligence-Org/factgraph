# Task Blueprint Audit: ReadPolicy Call-Site Migration and ViewSpec Removal from `fg.views`

- Blueprint: [2026-05-11_readpolicy-call-site-migration.md](./2026-05-11_readpolicy-call-site-migration.md)
- Parent Blueprint: (none — top-level cleanup)

## Event Log

| Date | Stage | Event | Notes |
| --- | --- | --- | --- |
| 2026-05-11 | draft | Design discussion converged on Option α' | User–Claude design discussion (2026-05-11) on the `fg.views` / `ViewSpec` / `FrozenAssertionView` terminology collision. Evaluated transcript proposal (`fg.projections` namespace + `ProjectionSpec` rename + alias-first slice) against alternative **Option α'** (value-object `ReadPolicy` via `policy=` kwarg; no new registry; no `fg.projections`). Convergence on α' driven by three findings: (1) "projection" terminology already occupies 3 kernel-internal meanings (`ViewProjectionError`, schema IR `projection`, write-protocol annotation projection); (2) ViewSpec field semantics are aggregation + source-preference, not relational-algebra projection; (3) value-object with call-site `policy=` is conceptually cleaner than two parallel registries. Source-grounding for (1) recorded in blueprint §4.3; for (2) in §4.2. |
| 2026-05-11 | draft | Pre-release context clarified | User confirmed factpy-kernel is pre-release (rc-stage only, no PyPI publish, no GitHub Release announcement, no known downstream consumer). Triggered default-to-hard-cutover decision recorded in §0.7; eliminated original "§5.6 hard-cut vs grace" tradeoff and original "§5.11 release timing window" deliberation, collapsing §5 to **10 gaps**. |
| 2026-05-11 | draft | Blueprint seed created | Draft seed `2026-05-11_readpolicy-call-site-migration.md` + paired audit. §0 records initial direction as **pending §5 validation** (not LOCKED). §5 lists 10 gaps for iterative cadence per `feedback_iterative_gap_design`. No code, tests, or memory updates. Design branch `v0.1-readpolicy-call-site-migration-2026-05-11` forked at `ace2563` (current `master` HEAD). Paired impl branch deferred to scope-freeze per `feedback_design_impl_branch_isolation`. |
| 2026-05-11 | draft | §5.1 LOCKED | Policy DTO name = `ReadPolicy`. Semantic boundary (locked verbatim): "`ReadPolicy` names the read-time resolution/display policy applied after facts are read, not graph membership. It replaces the old `ViewSpec` name for this policy surface; `view` remains reserved for assertion-membership views." `ReadPolicy` is positioned to absorb future read-time resolution/display fields under one DTO. 4 alternatives rejected: `DisplayPolicy` (narrow), `AggregationPolicy` (partial field coverage), `ResolutionPolicy` (term-overlap with conflict/identity resolution), `ReadDisplayPolicy` (hybrid + verbose). Does NOT lock: module location (§5.3), `__all__` export (§5.3), field set (§5.2), call-site form (§5.5), removal mechanic (§5.6). Source-grounding: `grep` confirmed zero existing `*Policy` class collisions under `src/kernel/`. |
| 2026-05-11 | draft | §5.2 LOCKED | Field set = 3 fields: `respect_revocations: bool = True` (renamed from misnomer `active`), `confidence_strategy: ConfidenceStrategy = "max"`, `prefer_source: str \| None = None`. `__post_init__` mirrors ViewSpec validation with `ReadPolicy.<field>` error prefix. `@dataclass(frozen=True)`, no `slots` / no `kw_only`. `ConfidenceStrategy` type alias stays at `kernel.core.store.types:14` (decoupled from §5.3 ReadPolicy location). Future fields DEFERRED: `tie_breaker`, `merge_rule`, `confidence_propagation`, plus any other non-current field; reopen trigger = concrete use case + separate scoped blueprint or §5 amendment. **Misnomer source-grounding**: `view_spec.active` has exactly one runtime consumer at `src/kernel/sdk/store.py:2070` — `if view_spec.active and ledger.has_active_revocation(...)` — confirming the field actually gates revocation-respect during confidence aggregation, not policy lifecycle. The rename is honest, not cosmetic. Pre-release hard-cut window (§0.7) makes this the cheapest moment to fix the misnomer. **Docs cross-ref**: `respect_revocations` must be prominent in §5.8 docs rewrite (teaches retract ↔ confidence relationship). Does NOT lock: module location (§5.3), `__all__` export (§5.3), call-site shape (§5.5), removal mechanic (§5.6), test plan (§5.9). |
| 2026-05-11 | draft | §5.3 LOCKED | DTO location = `kernel/core/store/types.py` (in-place replace of `ViewSpec`); `ReadPolicy` enters `kernel.sdk.__all__` (35 → 36); `ConfidenceStrategy` stays core-only (NOT exported). Clarifying note (locked verbatim): "`ReadPolicy` is a core value object re-exported by the SDK, not an SDK-only facade type. Core projector and service runtime may consume it directly; user-facing examples should import it from `kernel.sdk`." Rejected L2 (relocate to `kernel/sdk/types.py`) because `project_display_facts` at `kernel/core/view/projector.py:205` is a core function with isinstance type check, and moving ReadPolicy to SDK would force inline-kwargs / Protocol / projector-relocation — all worse than the architectural "violation" of core defining a 3-primitive value object. Source-grounded: ViewSpec has 5 production-code files of references (core types, core projector, sdk store, service runtime 9-sites); `kernel/application/protocol/` + `kernel/audit/` + `src/domains/ecss/` have **zero** references (correcting preliminary §4.7 over-estimate). Implementation cross-refs documented in §5.3 subsection table for Phase 2. Does NOT lock: `fg.views` post-migration + `default` entry (§5.4), call-site form (§5.5), removal mechanic (§5.6), service parse/serialize rewrite (§5.7). |
| 2026-05-11 | draft | §5.4 LOCKED | SDK `fg.views` narrowed to frozen assertion membership only. `_views` initial state = `{}` (built-in `default` removed). Name `"default"` is **not reserved** — users may freely create a frozen view named `"default"` with no special behavior. `create/update` accept only `asrt_ids=` or `asrts=` (mutually exclusive; `view_spec=` parameter removed). Return types narrow: `create/update/get → FrozenAssertionView`, `list → dict[str, FrozenAssertionView]`, `_views: dict[str, FrozenAssertionView]`. `_build_view_entry` ViewSpec dispatch branch removed. `delete` default-special-case branch removed. Anti-misread note locked verbatim: "The name `default` is no longer reserved by `fg.views`. If users create a frozen assertion view named `default`, it has no special behavior; it is just another frozen assertion-id selection." Error-text on missing-entry locked at **semantic** level (missing-view), not literal string — implementation freedom. Rejected D2 (keep as empty `FrozenAssertionView`) because empty-set bookmark is degenerate; rejected D3 (reserve name without built-in) because it occupies surface for a hypothetical future use (`feedback_narrow_public_api`). Source-grounded: service runtime carries parallel `default` handling at `src/service/runtime_v1.py:189, 924, 2631`; these are **explicitly out-of-scope** for §5.4 and routed to §5.7. Does NOT lock: `view=` → `policy=` kwarg migration (§5.5), old-`view=` removal mechanic (§5.6), service-runtime `default` mirror (§5.7), test coverage (§5.9). |
| 2026-05-11 | draft | §5.5 LOCKED | Call-site `policy=` API form = **P1 value-only**. Type signature: `policy: ReadPolicy \| None = None` on both `find(...)` and `run(...)`. `policy=None` mirrors current `view=None` (no policy applied; `find` returns rows without `row.confidence`; `run` without `return_display_meta` is normal eval). Rejected payload types: `dict` (typed-DTO boundary + double validation chain), `str` (would resurrect named-policy registry against §0.3 + §0.5 — recorded as falsifier baseline for future amendments), `FrozenAssertionView` (cross-namespace error; redirect message should point to `policy=ReadPolicy(...)`). Error contract at **semantic** level only; literal strings are implementation freedom. **`return_display_meta` invariant locked verbatim**: "`fg.run(rule, return_display_meta=True, policy=None)` is invalid because display metadata requires a `ReadPolicy`. Callers must pass `policy=ReadPolicy(...)` when requesting display metadata." Source-grounded against `src/kernel/sdk/store.py:1506-1507` guard; literal message can change from "view" to "policy", invariant `return_display_meta=True ⇒ policy is not None` is the lock. `evaluate(...)` continues to reject `policy=` (mechanical mirror of existing `view=` rejection at line 1565); no new design. Helper `_resolve_view_spec` (line 1542) is implementation-free: may be simplified-and-kept or inlined. Implementation cross-refs documented in §5.5 subsection table for Phase 2. Does NOT lock: old `view=` removal mechanic (§5.6), old `ViewSpec` removal (§5.6), service-runtime call-site migration (§5.7), test coverage for rejection paths + display_meta invariant (§5.9). |
| 2026-05-11 | draft | §5.6 LOCKED | Old-API removal mechanic per path: **R1** `ViewSpec` class deleted from `kernel/core/store/types.py:47-59` (no `__getattr__` shim; Python standard `ImportError` suffices). **R2** old `view_spec=` payload on `views.create`/`update` removed by §5.4; constructor name gone via R1; no separate handling. **R3** `find(..., view=...)` adds early-body guard `if "view" in filter_kwargs: raise SDKStoreError("view= was renamed to policy=")` at `kernel/sdk/store.py:561` (`find` retains `**filter_kwargs`; guard runs before filter validation). **R4** `run(..., view=...)` uses **tombstone sentinel** `view: Any = _VIEW_TOMBSTONE` keyword-only param at `kernel/sdk/store.py:1384`; module-level `_VIEW_TOMBSTONE = object()`; guard rejects any explicit pass **including `view=None`**. R4-tombstone locked verbatim: "`fg.run(..., view=...)` receives an explicit tombstone guard, not a bare Python `TypeError`. The guard must reject even `view=None` when supplied explicitly, and point callers to `policy=ReadPolicy(...)`." **R5** `evaluate(...)` replaces single-key view check at `kernel/sdk/store.py:1565` with combined `if "view" in kwargs or "policy" in kwargs: raise` — evaluate retains `**kwargs`; both names rejected with one combined message. Rejected R4-a (Python native TypeError) because `run` is a high-frequency surface users copy from old docs (`fg.run(rule, view="preferred_names", return_display_meta=True)`); native TypeError names the param but doesn't redirect to `policy=ReadPolicy(...)`; keeping symmetry with R3 explicit redirect. Sentinel name (`_VIEW_TOMBSTONE` here) is implementation freedom; the semantic contract `run(view=<anything>) → raise` is the lock. All R1–R5 error texts locked at semantic level only; literal strings free. Implementation cross-refs documented in §5.6 subsection table for Phase 2. Does NOT lock: service-runtime mirror removal (§5.7), parse/serialize body rewrite (§5.7), test coverage for R1–R5 + tombstone behavior (§5.9), docs rewrite of old `view=` examples (§5.8). |
| 2026-05-11 | draft | §5.7 LOCKED | Non-SDK ViewSpec sweep: full production-code grep at HEAD `ace2563` finalized 3 groups, 31 lines total. Group A (`kernel/core/store/types.py`, 4 lines) covered by §5.6 R1; Group B (`kernel/core/view/projector.py`, 6 lines) covered by §5.3 type-swap + §5.2 field-rename cross-refs. `kernel/application/protocol/` + `kernel/audit/` + `src/domains/ecss/` (non-test) confirmed **zero** references. **Group C (`src/service/runtime_v1.py`, 21 lines / 11 logical sites) LOCKED as S2 deep migration**. Service runtime mirrors SDK §5.4 + §5.5: removed `RuntimeSession.views` field + ctor param + default init, all 5 RPC endpoints (`create_runtime_view`/`update_runtime_view`/`delete_runtime_view`/`get_runtime_view`/`list_runtime_views`), `_resolve_runtime_view_spec` function, `"default"` policy entry, `view_name` lookup path. Renamed `_parse_view_spec` → `_parse_read_policy` + `_view_spec_to_dict` → `_read_policy_to_dict` (with wire field `active` → `respect_revocations`). `query_view_facts` flow refactored to inline `_parse_read_policy(dto.get("policy"))`. Wire DTO: `view` key → `policy`; `view_name` key removed; `active` field → `respect_revocations`. Defensive note locked verbatim: "Service runtime must not keep a named policy registry after SDK removes one. A service-level policy registry would recreate the same ambiguity that this blueprint removes from `fg.views`." Future named-policy feature must be a new product-level scoped blueprint, not a residual. Rejected S1 (shallow rename keeping registry) because it creates SDK/service architectural divergence + 5 unnecessary RPC endpoints + `"default"` ghost + `view_name` parity break against §5.5 P1 named-string rejection. Pre-release per §0.7 makes wire breaking changes cheapest now. Does NOT lock: service-runtime tests sweep (§5.9), wire-level docs / external service contract docs (§5.8), `_parse_read_policy` shared-validation refactor (implementation-free). |
| 2026-05-11 | draft | §5.8 LOCKED | Docs + examples rewrite plan. **Release-facing principle locked verbatim**: "Release-facing docs and examples must present `fg.views` only as frozen assertion membership and `ReadPolicy` only as call-site `policy=...`. They must not preserve `ViewSpec` as a documented compatibility path." Source-grounded inventory: 8 files contain `ViewSpec`/`view_spec`/`view=` (5 SDK docs, 2 examples, 1 service doc). **D1**: `examples/05_sdk_assertion_views.ipynb` integrally rewritten (single notebook teaches both frozen views + ReadPolicy). **D2**: `examples/archive/02_rules_and_derivations.ipynb` untouched (archive semantics). **D3**: `src/service/docs/03_runtime_queries_views.md` renamed to **`03_runtime_queries_policy.md`** (not generic `03_runtime_queries.md` — doc focus remains policy/display behavior of runtime queries) + rewrite. **D4**: no doc URL in §5.6 error messages; text-only redirect. **D5**: `respect_revocations` primary teaching in `01_concepts.en.md` + application reference in `02_readwrite_and_ingest.en.md`; **forbidden** from being placed in "advanced" sections. **D6**: §5.4 "default not reserved" note carried in both `_SDKViewsManager` class docstring **and** `01_concepts.en.md` views section. **D7**: 5-commit per-layer batch — concepts / read+write+surface / user-guide top / example notebook / service doc. **D8 (Phase 4 grep gate, locked at semantic level — literal pattern is implementation freedom)**: regex check across release-facing docs + examples + `examples/05_sdk_assertion_views.ipynb` must fail on `ViewSpec`, `view_spec`, `view=<policy/named-view-payload>`, or `fg.views.create(...)` with `ReadPolicy`/`ViewSpec` payload (post-§5.4 semantic violation). Reference command provided. Exemption set: `archive/**` directories, this blueprint itself (migration note documenting removed legacy), test files verifying rejection (§5.9 scope). D8 is the most important §5.8 acceptance gate. Does NOT lock: paragraph-level rewrites (Phase 3 implementation), test docstring rewrites (§5.9), `BREAKING CHANGE:` commit markers (§5.10). |
| 2026-05-11 | draft | §5.9 LOCKED | Test coverage plan. **Absence invariant locked verbatim**: "Tests must assert absence of `ViewSpec` from release-facing SDK imports and examples, not only runtime behavior." Surfaces in (a) T-NEW-6 pytest import-error assertions, (b) CI-b grep gate script, (c) §5.8 D8 release-facing grep gate. Source-grounded existing test inventory: 4 files (27 occurrences total) — `test_sdk_frozen_view_read_runtime_boundaries.py` (12), `test_walker_invariants.py` (7), `test_sdk_frozen_assertion_view.py` (5), `test_phase3_contracts_v1.py` (3). `src/service/tests/*` shows zero ViewSpec/view= references — 5 removed RPC endpoints are currently untested; T-NEW-5 adds new coverage. **T-NEW 6 groups all in scope**: T-NEW-1 ReadPolicy DTO contract; T-NEW-2 `policy=` P1 acceptance + rejection on find/run/evaluate; T-NEW-3 `fg.views` post-migration single-meaning + default-not-reserved; T-NEW-4 `view=` removal redirect guards (R3/R4 tombstone including `view=None`/R5 combined); T-NEW-5 service runtime new shape + 5-endpoint removal verification + wire field rename; T-NEW-6 absence invariant (pytest-level `ImportError` assertions for `from kernel.sdk import ViewSpec` and `from kernel.core.store.types import ViewSpec`). **T-SWEEP**: 4 existing files case-by-case rewrite during Phase 1 test scaffolding. **T-FILE organization**: NEW `src/kernel/tests/test_sdk_read_policy.py` (T-NEW-1+2+4+6); NEW `src/service/tests/test_runtime_query_policy.py` (T-NEW-5); EXTEND `test_sdk_frozen_assertion_view.py` (T-NEW-3). **Q4 CI-b LOCKED — independent grep gate script** `scripts/check_legacy_view_syntax.sh` (not CI-a integration into `release.sh`). CI-b rationale locked verbatim: "The grep gate targets legacy SDK docs/examples/service docs syntax — it is repo hygiene, not necessarily a release-script main-path step. Keep `release.sh` stable; let the independent script be reusable in Phase 4, release checklist, and CI; allowlist tuning should not require editing the release script." Release integration of the script (whether `release.sh` calls it or only manual run) deferred to §5.10. Does NOT lock: per-test assertion text + fixture shape (Phase 1 implementation), T-SWEEP individual delete-vs-rewrite decisions (Phase 1), whether T-NEW-6 lives in dedicated file or `test_sdk_read_policy.py` (implementation freedom). |
| 2026-05-11 | draft | §5.10 LOCKED | Release checklist. **Target version locked verbatim**: "Next release target is `0.1.0rc3`. This migration is kept out of the already-published `0.1.0rc2` meaning so release notes can describe it as a separate API cleanup." Rejected A1 (bundle into rc.2) because rc.2 (master `e46bc08`) already carries semantic = frozen-assertion-view model; bundling would overload release-note narrative; extra `chore(release): bump version to 0.1.0rc3` commit at Phase 2 start is minimal cost for clean separation. Pre-release per §0.7 means no SemVer compat negotiation; clean separation is the only motivation. **`__all__` diff explicitly locked**: 35 → 36 (rc.2 baseline 35 → rc.3 target 36); exactly one entry added (`"ReadPolicy"`); zero removed; `ViewSpec` was never in `__all__` (cf. `project_post_l_redesign_published`); Phase 4 verification = `git diff` against rc.2 base + allowlist sync per `feedback_release_workflow_traps` trap 1. **C-ii LOCKED — manual-only grep gate**: `scripts/check_legacy_view_syntax.sh` invoked manually at Phase 4; `scripts/release.sh` not modified. Rejected C-i (integration into `release.sh`) per §5.9 CI-b rationale; rejected C-iii (GitHub Actions) as over-engineering for pre-release. **D1 LOCKED — no `BREAKING CHANGE:` footer**: `git log` shows no historical use of this footer in repo; introducing new commit-message convention is cross-blueprint decision out of scope; Phase 2 commits land before rc.3 tag so SemVer major unaffected; pre-release per §0.7 makes Conventional-Commits BREAKING-CHANGE signaling redundant. **Release-trap verification matrix** (Phase 4): 3 traps apply (allowlist sync, test-projection imports, deny-pattern grep), 3 traps n/a (same-day re-tag, typing-extensions, EN README projection). Implementation cross-refs: rc.3 bump commit at start of Phase 2; grep gate at Phase 4 (`bash scripts/check_legacy_view_syntax.sh` → exit 0); `__all__` diff at Phase 4; `scripts/release.sh --dry-run` at Phase 4 before tag. Does NOT lock: rc.3 tag timestamp / GitHub Release page (release-time editorial), release-note copy, PyPI publish decision, Phase 1-4 commit order (§8 implementation plan). **§5 COMPLETE — all 10 gaps LOCKED. Ready for scope-freeze (§6 invariants + §7 implementation gates fill + status `draft → scoped`).** |
| 2026-05-11 | scoped | Scope-freeze | Status flipped `draft → scoped`. §6 invariants finalized (8 groups, 30 invariants derived from §5.1–§5.10 LOCKED; each git-diff- / grep- / test-checkable). §7 implementation gates finalized (5 phase blocks Phase 0–4; every gate carries concrete verification command, file artifact, or test assertion). User review pass applied 6 mandatory corrections + 2 reinforcements: (1) G4.3 release dry-run uses explicit `./scripts/release.sh v0.1.0-rc.3 --source-ref ... --dry-run --yes` form, not bare `bash scripts/release.sh --dry-run`; (2) G2.0 first-commit verification uses `git log --reverse --oneline <scope-freeze-commit>..HEAD \| head -1` to remain robust as Phase 2 progresses; (3) I4.1 reworded to avoid clashing with R4 tombstone (`policy=` is the only **accepted** kwarg; `view=` may remain in physical signature only as tombstone redirect); (4) I5.3 unlocks sentinel implementation form (any name/scope is implementation freedom; only "distinguish omitted view from explicit view=None" is locked); (5) G1.5 red baseline includes `test_sdk_frozen_assertion_view.py` since T-NEW-3 lives there; (6) G4.2 diff base = `<scope-freeze-commit>`, not `v0.1.0-rc.2` tag (rc.2 may carry unrelated history); (7+) G3.5 adds stale-link gate `rg "03_runtime_queries_views\.md\|runtime_queries_views"` to catch broken refs after rename; I7.2 refined: "Forbidden matches are old syntax presented as supported usage" — explicit rejection/migration notes allowed when same sentence marks unsupported. Paired impl branch `v0.1-readpolicy-call-site-migration-impl-2026-05-11` is created at scope-freeze HEAD per `feedback_design_impl_branch_isolation`. Design branch from this point accepts only doc-only commits; all implementation work happens on the impl branch. No SDK code, application protocol, or audit module changes in this commit. |
| 2026-05-11 | scoped | Phase 1 G1.4 T-SWEEP triage complete | Case-by-case triage recorded for the 4 existing test files identified in §5.9. `src/kernel/tests/test_sdk_frozen_view_read_runtime_boundaries.py`: rewrite the old dual-mode `ViewSpec`/`FrozenAssertionView` boundary tests into post-migration boundaries — `ReadPolicy` is accepted only through `policy=`, `view=` is rejected as removed syntax, and `policy=FrozenAssertionView(...)` is rejected as cross-namespace misuse. `src/kernel/tests/test_walker_invariants.py`: grep hits are local `view` subTest/case labels rather than SDK `view=` or `ViewSpec`; keep walker semantics intact and rename labels only if the final grep gate would otherwise read them as supported old syntax. `src/kernel/tests/test_sdk_frozen_assertion_view.py`: preserve frozen-membership tests and the Phase 1 T-NEW-3 single-meaning additions; remove/rewrite legacy `ViewSpec` compatibility tests, including the current `view_spec=` dispatch branch and the legacy `default` built-in expectation. `src/domains/ecss/tests/test_phase3_contracts_v1.py`: replace `sdk.run(..., view="default")` usages with `policy=ReadPolicy(...)`; any `return_display_meta=True` call must pass a non-None policy; unrelated `temporal_view` rejection checks remain outside this blueprint. Commit references: G1.1 red tests in `eb954c4`, G1.3 red tests in `dcc57cc`, G1.2 red tests in `e12ebde`. Source-name correction noted for Phase 2 implementation: the service function in code is `project_runtime_view_facts(...)`; earlier shorthand `query_view_facts` in planning text is descriptive only and does not change §5.7 semantics. No production code changed. |
| 2026-05-11 | scoped | Phase 1 G1.5 red baseline confirmed | Phase 1 red baseline confirmed before implementation. Combined direct unittest command `PYTHONPATH=src python -m unittest src.kernel.tests.test_sdk_read_policy src.kernel.tests.test_sdk_frozen_assertion_view src.service.tests.test_runtime_query_policy` ran **62 tests: 12 ok, 24 failures, 26 errors**. This replaces the initially proposed pytest aggregate for the red-baseline record because pytest 9.0.2 in this worktree exits `-1` with no useful output for the same files; direct unittest execution is deterministic and exposes the same expected migration-precondition failures. Expected red modes are present: `ReadPolicy` is not yet importable; `ViewSpec` is still importable from core; `policy=` is still treated as an entity filter or unexpected kwarg; `find(view=...)`/`run(view=...)` do not yet redirect to `policy=ReadPolicy(...)`; the discriminating tombstone test `run(view=None)` fails with the old rule-dispatch error; `fg.views` still has built-in `default` and accepts `view_spec=`; service runtime still exposes named view endpoints, `RuntimeSession.views`, `_resolve_runtime_view_spec`, `_parse_view_spec`, `_view_spec_to_dict`, `view_name`, `active`, and `view_spec` response payloads. The 12 passing tests are pre/post-compatible guard rails (for example `from kernel.sdk import ViewSpec` already fails, `run()` without `view`/`policy` does not trip a tombstone, existing frozen-membership behavior still works). These passing tests are **not** approval for legacy ViewSpec behavior; legacy compatibility tests are explicitly T-SWEEP targets above. Phase 1 is ready for Phase 2 implementation, whose first commit must be the rc.3 version bump per G2.0. |

## Decision Notes

- 2026-05-11: §0 deliberately phrased as **"Initial Direction (pending §5 validation)"**, not "Core Locked Decisions", per audit-cadence discipline. Each of §0.1–§0.7 is a working hypothesis that the corresponding §5.x gate must source-ground before promoting to LOCKED. This preserves the `feedback_iterative_gap_design` cadence — `draft` stage must not contain pre-locked decisions outside the §5 gate sequence.
- 2026-05-11: Rejection of `fg.projections` namespace (§0.5) is the strongest constraint and sets the falsifier baseline for §5.5: any §5.5 outcome that creates a named-string lookup table (registry under a different name) violates §0.3 + §0.5 and must be rejected.
- 2026-05-11: Per `feedback_design_impl_branch_isolation`, paired impl branch `v0.1-readpolicy-call-site-migration-impl-2026-05-11` will be created at design HEAD **at scope-freeze**, not at seed time.
- 2026-05-11: Substrate-level "projection" usage in `kernel.core/view/projector.py` and `kernel.core/schema/schema_ir.py` is **out of scope**; this blueprint does not relitigate or rename those kernel-internal occupations.
- 2026-05-11: The full text of the 2026-05-11 design transcript is intentionally NOT copied into this audit (brevity); substantive findings are summarized in the "Design discussion converged on Option α'" row above. Transcript-derived constraints (e.g. §0.5 rejection of `fg.projections`) appear with source-grounding in the blueprint body, not as raw quotes.
