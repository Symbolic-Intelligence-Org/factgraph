# Task Blueprint Audit: Assertion Selection And CRUD Ergonomics

- Blueprint: [2026-05-10_assertion-selection-crud-ergonomics.md](./2026-05-10_assertion-selection-crud-ergonomics.md)

## Event Log

| Date | Stage | Event | Notes |
| --- | --- | --- | --- |
| 2026-05-10 | draft | Blueprint created | Initial scope records assertion-id discovery and CRUD ergonomics questions after user review of `retract(asrt_id)` teaching path. Keeps precise retract semantics as default; evaluates docs-only fix vs read-side assertion selector helper plus adjacent CRUD improvements. |
| 2026-05-10 | draft | §5.1 LOCKED | Docs-only first slice locked. Existing `AssertionRecord` + `AssertionMeta` can express professional exactly-one selection; gap is documentation teaching, not API capability. `00_user_guide.en.md` retract signature blocker was split into immediate hotfix `1523e41`. Required docs fixes cover `set/add` returning `asrt_id`, snapshot structure (`EntitySnapshot -> FieldAssertions -> AssertionRecord -> asrt_id`), `AssertionMeta` selection, replacing `active_claims[0]`, and exactly-one guard examples. §5.2-§5.7 deferred conditionally, not permanently rejected. |
| 2026-05-10 | draft | §5.2 LOCKED | No new read-side selector helper; existing `tuple[AssertionRecord, ...]` from `EntitySnapshot.field(...)` and `EntitySnapshot.assertions.<field>` paths is sufficient post-§5.1 docs fix; no helper on `FieldAssertions`, no `fg.read.assertions(...)`, no application protocol or SDK surface change; conditional re-open per §5.1. |
| 2026-05-10 | draft | §5.2 REOPENED / LOCKED | User review favored fluent read-side assertion selection. Previous no-helper §5.2 decision superseded while preserving §5.1 docs hotfix baseline. New direction: tuple-compatible assertion collection (tentatively `AssertionRecordSet`) returned by existing `EntitySnapshot.field(...)` / `EntitySnapshot.assertions.<field>` paths and by `.active`, `.history`, `.at(...)`, `.version(...)`; supports read-only `.where(...)`, `.one()`, `.all()`, `.first()`; no `fg.read.assertions(...)`; no application protocol change. |
| 2026-05-10 | draft | §5.3 LOCKED | Exactly-one semantics scoped: `.where(...)` returns the same collection type; `.one()` returns one `AssertionRecord` or raises SDK error on zero/multiple; `.all()` returns a plain tuple; `.first()` is allowed for inspection but docs must not recommend it for retract workflows. |
| 2026-05-10 | draft | §5.4 LOCKED | Write side remains precise: canonical pattern is `fg.write.retract(target.asrt_id)`. No selector/predicate/filter acceptance in `write.retract(...)`; no `AssertionRecord` overload in this slice, though a future explicit-record convenience remains possible if it proves clearer than `.asrt_id`. |
| 2026-05-10 | draft | §5.5 LOCKED | `.where(...)` filter dimensions scoped to equality checks over `AssertionRecord.value`, stable `AssertionMeta` attributes (`source`, `trace_id`, `confidence`), documented raw `version`, and `meta={...}` raw key/value pairs. Multiple filters use AND semantics; predicate DSLs/fuzzy matching/nested raw-meta query languages are out of scope. |
| 2026-05-10 | draft | §5.6 LOCKED | CRUD adjacency handled through docs sync, not new write selectors: `set/add` return `asrt_id`; snapshot hierarchy documents `EntitySnapshot -> FieldAssertions -> AssertionRecordSet -> AssertionRecord`; retract and overlay examples use `.where(...).one()` then pass `target.asrt_id`; no ingest/batch/overlay write semantic changes. |
| 2026-05-10 | draft | §5.7 LOCKED | Namespace/public-surface budget locked: no new top-level SDK export, no `fg.read.*` method, `kernel.sdk.__all__` unchanged. Helper type may be documented as returned-object surface from `FieldAssertions`, not exported as a public constructor; post-L taxonomy unchanged. |
| 2026-05-10 | draft | §5.x clarification pass | Moved §5.1 supersession warning to the top of the partially superseded decision for readability; clarified `AssertionRecordSet` slicing/concatenation/multiplication preserve helper type; added §5.4 re-open trigger for a possible future `AssertionRecord` retract overload; narrowed `version=...` to `AssertionMeta.raw["version"]`. |
| 2026-05-10 | draft | §5.8 LOCKED | Test and compatibility scope locked: assert tuple-compatible behavior across `.active`, `.history`, `.at(...)`, `.version(...)`; preserve iteration/indexing/len/equality/isinstance; preserve helper type through slicing/concatenation/multiplication; cover `.where(...)` filters including chained `.where(...).where(...)` and explicit `None`, `.one()` zero/multiple errors, `.all()` plain tuple, `.first()` optional result, unchanged `write.retract(asrt_id)`, unchanged `kernel.sdk.__all__`, and docs examples avoiding magic ids / positional `records[0]`. |
| 2026-05-10 | draft | §6 invariants updated | Replaced seed bullets with 7 groups of invariants (ledger/identity, SDK read-side assertion selection, selector semantics, SDK write-side, public surface, documentation, repository safety) reflecting §5.1-§5.8 LOCKED decisions; no new behavior introduced beyond the locked returned-object selector helper. |
| 2026-05-10 | draft | §7 acceptance updated | Acceptance split into closed design gates, scope-freeze gates, and implementation gates. Implementation gates cover tuple-compatible `AssertionRecordSet` return paths, tuple compatibility, slice/concat/multiply/chained `.where(...)`, scoped filters including explicit `None`, `.one()` / `.all()` / `.first()`, unchanged `write.retract(asrt_id)`, unchanged `kernel.sdk.__all__`, no new `fg.read.*`, no application protocol change, SDK docs sync, and repository safety. |
| 2026-05-10 | draft | §8 implementation plan updated | Replaced draft-stage falsifier plan with 5-phase execution path: Phase 0 pre-scope-freeze scan and supersession clarity, Phase 1 tests, Phase 2 SDK-facade implementation, Phase 3 SDK docs sync, Phase 4 verification and close. Explicitly forbids application/authoring/core/`sdk.__init__` changes, public export changes, new `fg.read.*`, predicate DSLs, and fuzzy retract semantics. |
| 2026-05-10 | draft | Pre-scope-freeze scan completed | Scanned SDK docs and examples for magic assertion ids, positional retract selection, and stale `tuple[AssertionRecord, ...]` wording. Findings are docs-sync work, not design reopen triggers: `00_user_guide.en.md` has magic `asrt_id="asrt-abc-123"` and stale tuple-return wording; `06_what_if_and_proof.en.md` has positional `active_claims[0]["asrt_id"]`; `07_walker_and_advanced.en.md` has magic `asrt_id="asrt-abc-123"` and was added to Phase 3 target docs. Examples mostly use low-level `set_field(...)` or archived/WIP notebooks; only archived `examples/archive/01_sdk_basics.ipynb` has positional `active[0]` and is outside the release-facing SDK docs pass. §5.1 supersession is explicit in both blueprint body and audit log. |
| 2026-05-10 | scoped | Scope frozen | Blueprint status flipped `draft -> scoped` after all §7 scope-freeze gates passed. Implementation may proceed through Phase 1 test scaffolding under the locked §5 decisions and §6 invariants. |

## Decision Notes

Append key decisions chronologically, especially:

- scope freeze
- scope expansion or reduction
- implementation blocker
- module docs sync completed
- archive completed
