# Task Blueprint Audit: OSS Prep v0.1

- Blueprint: [2026-04-27_oss-prep-v0.1.md](./2026-04-27_oss-prep-v0.1.md)

## Event Log

| Date | Stage | Event | Notes |
| --- | --- | --- | --- |
| 2026-04-27 | draft | Blueprint created | 紧接 namespace-split blueprint 收口创建。Scope 起点是 archived blueprint §10 OS-prep TODOs + memory/current.md 的"下一步方向 优先级 1"。当前形态是 DRAFT 决策矩阵 + 推荐起步顺序,等 scoping 会议把 §5.1 的决议项跑通。 |
| 2026-04-27 | draft | Draft v1 review feedback applied | 用户 review 暴露 4 处(3 P1 + 1 P2):(a) 缺"发布工件模型"决策层 — 没这层,#1 OS scope / #2 包名 / pyproject 调整都无法 scope-able;(b) CI gate 写成"加东西",但 `.github/workflows/factpy-kernel-tests.yml` 仍引用 `src/factpy_kernel/tests` 旧路径,namespace split 执行期遗漏 `.github/` 不在迁移 sed scope,baseline CI 实际 broken,PR #1 此刻 CI 红 / 假绿;(c) repo 清理 acceptance 用 `git status clean`,但 `.gitignore /*` deny-by-default 兜住所有 root 私人产物,假阳性;(d) audit 报告 `factpy-kernel-report-audit-2026-04-20.md` 仍在 root,未按 repo workflow bridge 到 `docs/references/`。Patch:audit 报告 bridged 到 `docs/references/bridges/`;§5.1 决策矩阵重写(新增 #1 发布工件模型 / 拆 #7 → 7a baseline 修 + 7b gates 增强 / #8 改逐项分类 + 实证 check);§5.2 依赖图重画;§5.3 推荐顺序更新(7a 可独立 hotfix);§7 Acceptance 重写,#8 用每项 explicit existence/ls-files 检查替代 git status;§9 Docs To Update 加 bridged path 标记;Related Docs 加 bridged audit report。详见 Decision 5。 |
| 2026-04-27 | draft | CI baseline hotfix landed | 接受 v1 review 中 #7a 的"在 namespace-split branch 加 hotfix"路径(commit `30a29b0`)。修 5 处旧 path 引用 + 合并 extras install + 删 service-routes 空壳;PR #1 在 push `9f0b6de`(空 commit retrigger synchronize)后 CI matrix(3.10 + 3.11)全绿。PR squash merge 后 master HEAD `81c6f77`,baseline CI 在 master 上跑通。**#7a 已完成**,移出本 blueprint 决策矩阵(进 §4 Current Context 作为 done state)。 |
| 2026-04-27 | draft | Draft v2 review feedback applied + #1 scoped | 用户 review v2 暴露 3 P1 + 1 P3:(a) post-merge state stale(§4 Current Context 仍写 HEAD `0f2ee52` / branch `namespace-split` / #7a CI broken,但 PR #1 已 squash merge,master HEAD `81c6f77`,baseline CI 已修绿);(b) 原 #1 发布工件模型混了两个独立 axes(distribution topology vs publication policy),无法收敛单一决议;(c) multi-distribution monorepo 被高估为 v0.1 候选,但生产代码 inter-package deps 紧耦合(agent → kernel + service / service → kernel / domains → kernel)+ pyproject 单 setuptools.find,multi-wheel 不是 packaging switch 而是需独立 inter-distribution dependency design + install UX;(d) §8 implementation plan decision count 写成 10,但矩阵已 11 项。Patch:§4 更新 post-merge 真相 + 加 inter-package deps 显式列出;§5.1 拆 #1 → 新 #1 artifact topology(scoped:single dist)+ 新 #2 publication scope/PyPI policy,multi-dist 显式降级为未来演进路径,其余 #2-#11 顺延为 #3-#12;#7a baseline 修移出矩阵到 §4,只剩 #8 quality gates;§5.2 重画(去 #7a + 加 #1 scoped 标记);§5.3 推荐顺序更新(从 #3 OS visibility 起);§2 Goals 重排;§7 Acceptance 重新编号 #1-#12,#1 标 ✅;§8 implementation plan decision count 12。**#1 scoped 决策**:single distribution / multi namespaces — rationale:与现 pyproject/CI/README/tests 一致,生产代码 inter-package deps 非独立,multi-wheel 需独立 design 工作不在 v0.1 scope,audit 报告关注 buyer/distribution 而非 wheel 拆法。详见 Decision 6。 |
| 2026-04-27 | draft | Draft v3 review feedback applied + #3 direction proposed | 用户 review v3 暴露 1 P2 + 1 P3:(a) #2 publication scope 候选 (c) 仍写"未来 PyPI 选择性发 namespace 子集",在已 scoped single-dist 下 namespace 子集语义属 #3 OS visibility,造成 #2/#3 部分重叠;(b) §9 Docs To Update 中 README 行写"若 #9 决议改写"但 README shape 已是 #11,#9 现在是 repo 根清理。Patch:#2 候选 (c) 改写为"PyPI 发布有节奏"(去掉 namespace 子集语义)+ 显式注明 namespace 子集归 #3;§9 README 行编号 #9 → #11。同时 narrow #3 候选 + 加 acceptance(supported/experimental marking)接收用户对 #3 的推荐方向(全 4 namespaces public,kernel/service primary,agent/domains experimental),但 **#3 尚未 scoped**(待下一轮 review 确认 + 可能补 implementation 明细)。详见 Decision 7。 |
| 2026-04-27 | scoped | #3 confirmed + status moved to scoped | 用户 v4 review 确认 #3 推荐方向,选 (a) 全 4 namespaces public,kernel/service primary,agent/domains experimental(支持级别分层)。同时建议把 status `draft → scoped`,理由:高 leverage scope 骨架(#1 + #3)已锁,剩余 10 项决议在此边界内的实现/发布/文档/合规细节,不再改变 blueprint 基本范围。Patch:§5.1 #3 标 ✅ scoped + acceptance 加 kernel-docs 不需要 experimental 标签的 clarification(避免 module docs 标签过宽);§2 Goals + §5.2 dependency graph + §5.3 推荐顺序 同步更新(#3 移出待决);status `draft` → `scoped`。下一项推 #6 PyReason license check(纯事实查证)。详见 Decision 8。 |
| 2026-04-27 | scoped | #6 PyReason license verified (BSD-2-Clause) | 3 来源 cross-check:**(1) upstream GitHub `main` LICENSE.md** 第一行字面 "BSD 2-Clause License",含 2 条 numbered conditions(BSD-3 多 "No Endorsement" 第 3 条);**(2) 本地 install 3.0.0 `dist-info/LICENSE.md`** 与 upstream 完全相同;**(3) PyPI metadata `License:` 字段** 写 "BSD 3-clause" — **作者 setup.py/pyproject 错标**,与实际 ship 的 LICENSE.md 不一致。**Authoritative source**:upstream GitHub LICENSE.md(法律权威)。**结论**:BSD-2-Clause permissive,与 Apache 2.0 / MIT / BSD-3 都兼容,无 GPL 传染,可进 default extras。**附加约束**:LICENSE.md 末尾 PyReason™ trademark notice(ASU Board of Regents);docs / README 引用 PyReason 时不能用商标暗示 endorsement,但不改 license tier。Patch:§5.1 #6 标 ✅ scoped + 写明 3 来源 cross-check + (b) BSD-3 / (c) GPL/AGPL 已被 (a) 排除;§2 Goals 同步;§5.2 dependency graph 简化(LICENSE → CONTRIBUTING 单边,#6 不再约束 #5);§5.3 推荐顺序 #6 移出;§7 #6 acceptance 改 ✅ + 落地清单(audit log 已记 / pyproject 不调 / docs 措辞约束 / 未来 PyPI metadata 纠正不影响结论)。详见 Decision 9。 |
| 2026-04-27 | scoped | #9 repo 根清理 scoped (1 bridge + 9 keep-local + 0 delete) | 用户给默认分类:1 bridge(已完成,audit 报告)+ 9 keep-local(`archive/` / `out/` / `esa_demo_output/` / `dora_demo_output/` / `test.ipynb` / `context.md` / `关于mvp的思考.md` / `best_conf.csv` / `path_conf.csv`)+ 0 delete。Rationale:9 项均已 untracked(`.gitignore /*` deny-by-default 兜住),git history 干净;bridge 仅适用 durable docs 价值材料(audit 报告满足,其它不满足);delete 是本地清理,与 OS-prep 决议解耦。**实证已通过**:`git ls-files \| grep -E "^(factpy-kernel-report-audit\|best_conf\|path_conf\|test\\.ipynb\|context\\.md\|关于mvp\|archive/\|out/\|esa_demo_output/\|dora_demo_output/)"` 输出空;9 项 keep-local 在本地 `test -e` 全 ✓;1 项 bridge `test -f docs/references/bridges/factpy-kernel-report-audit-2026-04-20.md && ! test -f <root>` ✓。Patch:§5.1 #9 标 ✅ scoped + 列默认分类表;§7 #9 acceptance 改 `[x]` 已通过 + 列实证清单;§2 Goals + §5.3 同步。本地物理删除 9 项 keep-local 是 user 个人选择,**不在 blueprint 决议范围**。详见 Decision 10。 |
| 2026-04-28 | scoped (on hold) | Scoped phase paused pending runtime-authority-cleanup | 在推进剩余 8 项决议(#2 #4-#5 #7-#8 #10-#12)之前,识别出 SDK vs application 的 runtime authority 模糊是 OS publish 前必须解决的高 leverage 问题:OS v0.1 publish 瞬间 SDK 公开 surface 即 v0.1 contract;若 v0.2 才把 runtime 从 SDK 迁到 application,每一个 SDKStore.evaluate / batch / etc. 都是 breaking change + 6-12 月 deprecation cycle。决议(用户明确 stake):application = canonical Python runtime;sdk = product surface / facade / authoring shell。走法 A:暂停本 OS-prep blueprint 的 scoped phase,起新 blueprint `2026-04-28_runtime-authority-cleanup.md` 完整走完(draft → scoped → implementing → implemented),回来再推剩余 8 项 OS-prep 决议。本 blueprint status 保持 `scoped`,但实质 implementation 工作 hold;runtime cleanup 完成后 OS-prep 才继续。详见 Decision 11。 |
| 2026-04-28 | scoped (partial unhold draft) | Runtime cleanup decision phase complete; partial-unhold grouping proposed | runtime-authority cleanup blueprint 已完成 decision phase pass 1(B+C+D)与 pass 2(A/E/F/G/H/I/J),但生产代码 implementation 尚未开始。基于此,OS-prep 可部分 unhold:可并行 scope #5 LICENSE / #8 CI gates / #10 CONTRIBUTING+CoC / #4 包名初筛;#2 PyPI policy / #7 OpenAPI yaml / #11 README public API framing 继续等 runtime cleanup implementation 后定。#12 经 audit-delivery-contract 已收口为保留 `AuditQuery.list_compliance_matrix` convenience + `domains.ecss.compliance` owns row assembly。Patch 更新 §4 / §5.1 / §5.2 / §5.3 / §7 / §8。详见 Decision 12。 |
| 2026-04-28 | scoped (partial unhold implemented) | #5/#8/#10 implemented; #4 initial screen and #12 close-out recorded | Partial-unhold implementation landed as doc/config only:Apache-2.0 LICENSE + pyproject metadata + README license lines;CONTRIBUTING.md / CODE_OF_CONDUCT.md;`dev` extra + staged CI quality steps(blocking application ruff,non-blocking broad ruff/mypy,non-threshold coverage);#4 PyPI initial screen(`factpy-kernel` no matching distribution,`factpy` occupied);#12 remains closed by audit-delivery-contract. Dependency review found `documents` extra PyMuPDF/PyMuPDF4LLM dual AGPL-3.0 or commercial license,so final PyPI policy must decide documents extra handling.详见 Decision 13。 |
| 2026-04-28 | scoped (input refinement) | #2 documents-license choices and #4 default-name input promoted from findings | Two partial-unhold findings were promoted into the blueprint input surface before push:PyMuPDF/PyMuPDF4LLM disposition is now an explicit #2 four-choice input,not an audit-only caveat;#4 now defaults to `factpy-kernel` unless a stronger name appears,while final reservation remains tied to #2.详见 Decision 14。 |
| 2026-04-28 | scoped (post-cleanup unhold) | Runtime-authority cleanup merged; #2/#7/#11 gate released | `runtime-authority-cleanup` reached implemented status at `d9c7fcb` and was merged into `oss-prep-v0.1` via `55df82f`. OS-prep now has the new runtime boundary as current implementation truth:application is canonical Python runtime authority;SDK is product surface / ergonomic facade;service/agent production SDK import boundary is guarded. The previous wait-for-cleanup-implementation gate for #2 PyPI policy / #7 OpenAPI yaml / #11 README OS framing is released,but these items remain unscoped until their own decision pass.详见 Decision 15。 |
| 2026-04-28 | scoped (scope correction) | OSS surface corrected to kernel-only | 用户明确"开源的部分仅包含 kernel 主体,agent 不包含在内"。这 supersedes earlier #1/#3 scoped assumption(single distribution with 4 public namespaces). Patch rescope:#1 = single distribution / `kernel*` package only;#3 = kernel-only OSS surface;#7 = no v0.1 OpenAPI artifact;PyMuPDF/PyMuPDF4LLM AGPL-commercial finding moves from kernel PyPI blocker to private agent documents concern;#12 owner split remains true internally but needs optional/private handling before kernel-only wheel.详见 Decision 16。 |

## Decision Notes

### 关键决策(2026-04-27 draft)

1. **从 namespace-split blueprint 继承未决问题作为 scope 起点**
   - **Why**:archived blueprint §10 OS-prep TODOs(PyReason license / 包名 / LICENSE / CI gate / OpenAPI yaml 归属 / list_compliance_matrix 架构净化)是已识别但当时不在 scope 的工作;namespace 层面已稳定后,这些是自然的下一阶段
   - **影响**:本 blueprint 的 §2 Goals 和 §5.1 决策矩阵直接对应这些未决项

2. **Draft 阶段不预先决定 OS scope / 包名 / LICENSE 等具体决议**
   - **Why**:这些是高 leverage 决策,值得在 scoped 阶段专门讨论(可能涉及商业策略 / 受众定位 / 与 audit 报告 2026-04-20 的市场结论对齐),不在 draft 阶段单方面预设
   - **影响**:§5 Proposed Shape 只列待决策矩阵 + 决策依赖图 + 推荐起步顺序;具体方案在 scoped 时填入

3. **明确 non-goals 防止 scope creep**
   - **Why**:OS prep 容易被"顺便重构 god file" / "顺便加 feature" / "顺便改 namespace 内部" 等不相关工作扩大
   - **影响**:§3 显式列 5 类 non-goal;商业化 / 销售 / 客户叙事归 audit 报告线,不进 OS-prep

4. **工程 trap 提前纳入 §6 Boundaries**
   - **Why**:5 工程 trap(zsh for-in / BSD sed `\b` / pre-flight 实跑 / __init__ re-exports / 跨包相对 import)在 namespace-split blueprint 实施期才发现并补救,本 blueprint 应预先纳入 implementation checklist
   - **影响**:§6 已列工程 trap section;OS-prep 实施时不必再重新踩

5. **Draft v1 review patch — 4 处(3 P1 + 1 P2)**
   - **Why**:用户 review draft v1 发现 4 处结构性问题:
     - **P1.a 缺发布工件模型决策层**:#1 OS scope 单独决议无法落成 pyproject 调整(因为没说是 1 wheel N namespaces / N wheels monorepo / 还是 PyPI 子集)。#1 #2 #3 互相依赖,需要更上层的"发布形态"先决
     - **P1.b CI baseline broken**:`.github/workflows/factpy-kernel-tests.yml` 5 处仍引用 `src/factpy_kernel/tests`(已不存在),namespace split 执行期 `.github/` 不在迁移 sed scope 内,这是 namespace split 的执行期遗漏。OS-prep 不是单纯"加 gates",必须先修 broken baseline
     - **P1.c repo 清理 acceptance 假**:`.gitignore /*` deny-by-default 把 root 私人产物全兜住,`git status clean` 即使一项不清也通过。必须改为逐项实证 check
     - **P2 audit 报告未 bridge**:root 的 `factpy-kernel-report-audit-2026-04-20.md` 是被 §4 Current Context 引用为 scoping 输入的 working reference,按 docs/references/README.md workflow 应先 bridge 到 `docs/references/bridges/` 再被 blueprint 引用
   - **决策**:
     - 立即 bridge audit 报告 to `docs/references/bridges/`(scoping 前完成)
     - §5.1 重写:新增 #1 发布工件模型,拆 #7 为 7a CI baseline 修 + 7b quality gates,#8 repo 清理改逐项分类
     - §5.2 依赖图重画(发布工件模型在最上游,7a 可独立)
     - §5.3 推荐顺序:#1 → #7a(可 hotfix 推进,不依赖 #1)→ #5 PyReason → #8 → 其余
     - §7 Acceptance 改逐项实证,#8 列出每项 explicit `test -f` / `git ls-files` 检查
     - §9 Docs To Update 标记 audit 已 bridged
   - **影响**:draft v2 是真正可 scoping 的形态;PR #1 的 broken CI 现象暴露,scoping 时需决定是否在 namespace-split 上 hotfix CI 还是 PR 红着 merge,等 OS-prep #7a 一并修(实际选择前者,见 Event Log "CI baseline hotfix landed")

6. **Draft v2 review patch + 首项 scoping 决策(#1 artifact topology)**
   - **Why**:用户 review v2 发现 3 P1 + 1 P3:
     - **P1.a post-merge state stale**:§4 Current Context 仍写 HEAD `0f2ee52` / branch `namespace-split` / #7a CI broken,但 PR #1 已 squash merge,master HEAD 是 `81c6f77`,baseline CI 已被 commit `30a29b0` 修复并跑绿;若不更新,scoping 锚定在已解决问题上,Current Context materially wrong
     - **P1.b 原 #1 发布工件模型混两个独立 axes**:distribution topology(single vs multi)与 publication policy(public repo + PyPI subset)互不排斥(可有 public repo + 1 wheel 也可 public repo + N wheels),决策矩阵无法收敛单一决议
     - **P1.c multi-distribution 被高估为 v0.1 候选**:生产代码 inter-package deps 紧耦合(agent → kernel + service / service → kernel / domains → kernel),pyproject 单 setuptools.find,multi-wheel 不是 packaging switch 而是需独立 inter-distribution dependency design + install UX,非 v0.1 工作
     - **P3 §8 implementation plan decision count drift**:写成 10,但矩阵已 11 项(v2 patch 后)
   - **决策**:
     - §4 Current Context 更新 post-merge 真相(HEAD / branch / baseline CI 已修);加 inter-package deps 显式列出(为 #1 决策提供事实依据)
     - §5.1 拆 #1 → 新 #1 artifact topology + 新 #2 publication scope/PyPI policy;multi-dist 显式降级为"未来演进路径,前提是 agent/service 依赖边界先收紧";原 #2-#11 顺延为 #3-#12
     - #7a baseline 修移出矩阵(已完成,放 §4 Current Context);#7b 单独保留为 #8 quality gates,在已修 baseline 之上叠加
     - §5.2 重画(去 #7a;加 #1 scoped 标记;dependency 关系基于新编号)
     - §5.3 推荐顺序从 #3 OS visibility 起(#1 已 scoped,#2 与下游正交)
     - §2 Goals 重排,#1 标 ✅ scoped
     - §7 Acceptance 重新编号 #1-#12,#1 标 ✅;#8 quality gates 明确"在已修 baseline 之上叠加,不动 baseline workflow"
     - §8 implementation plan decision count 改 12(#1 已 scoped,11 项剩余决议)
   - **#1 scoped 决策**:**single distribution / multi namespaces**(v0.1 默认)。Rationale:
     - 与现 pyproject `setuptools.find` topology 一致,无需 packaging 改动
     - 生产代码 inter-package deps 非独立(agent/service/domains 都依赖 kernel),multi-wheel 需独立 inter-distribution dependency design 工作
     - 与现 CI(commit `30a29b0` 5 段 unittest discover)、README(4-package narrative + 5 段测试命令)、tests / examples 假设一致
     - audit 报告 2026-04-20 真正在意的是 buyer / distribution / vertical packaging,不是 wheel 拆得多漂亮
     - Multi-dist 作为未来演进路径保留(non-v0.1)
   - **影响**:11 项剩余决议可基于 single-dist 假设具体化;最高 leverage 剩余决议是 #3 OS visibility scope(影响 wheel 内含 namespaces / OpenAPI yaml / README OS 形态 / list_compliance_matrix)

7. **Draft v3 review patch — 2 处(1 P2 + 1 P3)+ #3 direction 提案**
   - **Why**:用户 review v3 发现 2 处收口:
     - **P2 #2 (c) 仍 leak namespace subset 语义**:#2 publication scope/PyPI policy 候选 (c) "公开 repo + 未来 PyPI 选择性发 namespace 子集" — 在 #1 已 scoped single-dist 后,namespace 子集是 #3 OS visibility scope 的语义,#2 不该重复;否则 #2/#3 概念重叠
     - **P3 §9 Docs To Update README 编号 drift**:旧 README 行写"若 #9 决议改写",但 README shape 在 v3 已是 #11(原 #10 顺延),#9 现在是 repo 根清理。小漂移但会让 scoping notes 后续混乱
   - **决策**:
     - #2 (c) 改写为"PyPI 发布有节奏"(如稳定 release tag 才推 PyPI,daily / commit 不推);candidate 维度从"namespace 子集"换成"PyPI 节奏";末尾显式注明"namespace 子集 不在本项;那是 #3"
     - §9 Docs To Update README 行编号 #9 → #11
     - **同时接收用户对 #3 OS visibility scope 的推荐方向**(narrow candidates + 加 acceptance):
       - candidate (a) 改写为"**全 4 个 namespaces public,kernel/service primary,agent/domains experimental**(支持级别分层,**当前推荐方向**待 scoped 时确认)",含 4 条 rationale(与现 single dist/tests/examples 一致 / 不需 package exclusion / audit 报告关注通用 narrative 弱 / 支持级别分层避免错承诺稳定 API)
       - candidate (b) 改写为 "kernel + service only";(c) 改写为 "仅 kernel"
       - §7 #3 acceptance 加"若选 (a) 支持级别分层,**README + module docs 显式标出 supported(kernel/service)vs experimental(agent/domains)**"+ `grep` 实证命令
   - **#3 status**:**direction 已提案、未正式 scoped**。要 scoped 需:(i) 用户下一轮 review 确认推荐方向;(ii) 可能补"哪些 module docs 必须加 supported/experimental tag"的具体清单(实施期细节,可在 scoping 末段补)。
   - **影响**:v3 → v4 的 patch 是 close-out 性质;#3 推荐方向就位后,scoped 推进基本无障碍;剩余 10 项决议(#2 #4-#12)可基于 single dist + 4 namespaces public 的假设并行讨论

8. **#3 confirmed + blueprint status `draft → scoped`**
   - **Why**:用户 v4 review 确认 #3 推荐方向 (a)。同时判断高 leverage scope 骨架已锁:
     - #1 artifact topology ✅ scoped(single dist)
     - #3 OS visibility scope ✅ scoped(全 4 namespaces public + 支持级别分层)
     - 剩余 10 项决议(#2 #4-#12)都在此边界内,属"实现/发布/文档/合规细节",不再改变本 blueprint 基本范围
   - **决策**:
     - §5.1 #3 标 ✅ scoped(candidate (a))
     - §7 #3 acceptance 加 **kernel-docs 不需要 experimental 标签** clarification(避免实施期把 "所有 module docs 都要打标签" 理解过宽);具体落地 = `src/agent/{docs,extraction/docs,service/docs}/README.md` + `src/domains/ecss/docs/README.md` 标 experimental;`src/kernel/AGENTS.md` + `src/kernel/*/docs/` 不动(默认 stable supported tier)
     - §2 Goals 同步:#3 标 ✅
     - §5.2 dependency graph 重画(#1 + #3 在最上层,余项 cascading;#12 list_compliance_matrix 因 #3 含 domains public 排除 (c) delete 选项)
     - §5.3 推荐起步顺序:#3 移出,从 #6 PyReason 起
     - Status `draft → scoped`
   - **影响**:scope 边界锁定;剩余决议可在 scoped phase 内并行推进,不产生 scope 漂移风险
   - **下一项**:#6 PyReason license 查证(纯事实,无依赖,~10 分钟)

9. **#6 PyReason license 查证 ✅ 收敛(BSD-2-Clause)**
   - **Why**:用户预查发现 3 来源不一致(local install metadata `BSD 3-clause` / PyPI page `BSD License` classifier / upstream README `BSD-2-Clause`),提议把 #6 scoped 成可落 audit 的具体核验动作:upstream LICENSE 文件为主证据 + PyPI metadata cross-check + audit log 记录 authoritative source 与 why
   - **核验执行**:
     - **(1) upstream GitHub `main` LICENSE.md**(`https://raw.githubusercontent.com/lab-v2/pyreason/main/LICENSE.md`):字面第一行 "BSD 2-Clause License",含 2 条 numbered conditions(`Redistributions of source code` + `Redistributions in binary form`),无 BSD-3 标志性的 "No Endorsement" 第 3 条
     - **(2) 本地 install 3.0.0 `dist-info/LICENSE.md`**(`/Users/zhenzhili/miniforge3/lib/python3.10/site-packages/pyreason-3.0.0.dist-info/LICENSE.md`):与 upstream 字节级相同
     - **(3) PyPI metadata `License:` 字段**:写 "BSD 3-clause" — **作者错标**,与实际 ship 的 LICENSE.md 不一致;`Classifier: License :: OSI Approved :: BSD License` 是泛 BSD 标签,不区分 2 vs 3
   - **Authoritative source**:upstream GitHub LICENSE.md。理由:法律上 license 以 LICENSE 文件文本为准(SPDX 识别 + 法庭可证),metadata 字符串只是 packaging convention,作者笔误不影响法律真相;且 (1) 与 (2) 字节级一致 = distribution 实际 ship 的就是 BSD-2
   - **决议**:**#6 = BSD-2-Clause (verified)**,permissive BSD family,无 GPL 风险
   - **影响**:
     - 与本 blueprint #5 LICENSE 任何候选(Apache 2.0 / MIT / BSD-3)都兼容 → #6 不约束 #5
     - 可保留 PyReason 在 default extras(若现有)而无 license 风险
     - **附加约束**:LICENSE.md 末尾 PyReason™ / PyReason Design Logo™ trademark notice(ASU Board of Regents);docs / README 引用 PyReason 时**不能**用商标暗示 endorsement(避免 "Powered by PyReason™" 类措辞,除非走 trademark permission flow);引用为 "compatible with PyReason adapter" / "via lab-v2/pyreason" 即可
     - 未来 PyPI 上游若纠正 metadata "BSD 3-clause" → "BSD-2-Clause",本结论不变
   - **下一项**:#9 repo 清理(独立,逐项推进)

10. **#9 repo 根清理 ✅ 收敛(1 bridge + 9 keep-local + 0 delete)**
    - **Why**:用户给默认分类。Rationale 是事实驱动的:
      - 9 项 keep-local 候选**已全部 untracked**(`.gitignore /*` deny-by-default 兜住,git history 未污染)
      - bridge 应仅适用于有 durable docs/references 价值的材料(audit 报告满足:它是 materially affects scope 的 working reference;其它 9 项是用户的个人工作材料 / output / 实验配置,无项目级价值)
      - delete 是本地清理动作,**不是 repo truth 工作**;强行 delete 等于在 blueprint 里做用户的本地卫生
    - **决议**:
      - **bridge**(1 项,已完成):`factpy-kernel-report-audit-2026-04-20.md` → `docs/references/bridges/`
      - **keep-local**(9 项):`archive/` / `out/` / `esa_demo_output/` / `dora_demo_output/`(生成物 / 工作输出)+ `test.ipynb` / `context.md` / `关于mvp的思考.md`(个人工作材料)+ `best_conf.csv` / `path_conf.csv`(实验/本地配置产物)
      - **delete**:0 项(blueprint 决议层面)
    - **实证已通过**(2026-04-27):
      - `git ls-files | grep -E "^(factpy-kernel-report-audit|best_conf|path_conf|test\.ipynb|context\.md|关于mvp|archive/|out/|esa_demo_output/|dora_demo_output/)"` 输出空 ✓(git history 干净)
      - 9 项 keep-local `test -e <path>` 全 ✓(本地存在)
      - 1 项 bridge:`docs/references/bridges/factpy-kernel-report-audit-2026-04-20.md` 存在 ✓ + root 不存在 ✓
    - **影响**:#9 已 scoped 且 acceptance 已通过,无 implementation 工作残留
    - **本地物理删除决策**:本 blueprint **不**主动删 9 项 keep-local;若用户想顺手清本地工作区,是 user 个人选择,与 blueprint 决议解耦,不影响 acceptance

11. **Scoped phase paused — 转 runtime-authority-cleanup blueprint 优先**
    - **Why**:在准备推 #2 #4-#5 #7-#8 #10-#12 这 8 项剩余决议时,识别 SDK vs application 的 runtime authority 模糊是更高 leverage 问题:
      - 现状是 option 3(both layers coexist with partial authority),最差状态;`test_sdk_*_application_delegate.py` 表明 SDK partially delegates,但公开 API 仍以 SDK 为准,边界全模糊
      - OS publish 的瞬间 SDK 公开 surface = v0.1 contract;若 v0.2 才动 runtime authority,SDKStore.evaluate / batch / 等每个 API 都是 breaking change,需 deprecation cycle(6-12 月才敢 remove)
      - 与 namespace-split blueprint § 10 audit 报告点名的 god files(sdk/batch.py 1838 / sdk/store.py 1499)是同一个问题的两面 — SDK 装了不该装的 runtime 责任,导致 god files
      - 与已 scoped 的 D1 kernel 定义("面向 Python 接入方的可审计推理 substrate")的 layer 分清要求也契合
    - **Position(用户明确 stake)**:`application` = canonical Python runtime authority;`sdk` = product surface / facade / authoring shell;`service` 应依赖 application 而非 SDK;新 runtime 能力默认进 application/core,SDK 包 ergonomic wrapper
    - **决策(走法 A)**:
      - 本 OS-prep blueprint status 保持 `scoped`(scope 骨架决议没变,#1 + #3 + #6 + #9 仍有效)
      - 但 implementation 工作 **hold**:不推 #2 #4-#5 #7-#8 #10-#12 的剩余决议,直到 runtime cleanup 完成
      - 起新 blueprint `docs/blueprints/active/2026-04-28_runtime-authority-cleanup.md`(draft → scoped → implementing → implemented 完整流程)
      - cleanup blueprint 完成后回 OS-prep,在新 SDK / application 边界基础上推剩余 8 项决议
    - **影响**:
      - OS publish 时间推迟 ~1-2 周(同等量级 namespace-split 工作)
      - 但 OS v0.1 SDK 是干净 facade,长期 API 稳;比 v0.1 publish 后再做 breaking change 显著划算
      - cleanup blueprint 自带 god files 拆分子任务(不能整块搬,否则 application 接 god files)
      - 1023 tests 基线必须仍绿;`test_sdk_*_application_delegate.py` 测试可能需 rewrite

12. **Partial unhold after runtime-authority decision phase + #12 close-out proposal**
    - **Why**:
      - runtime-authority cleanup 已经完成 decision phase(A-J scoped),这足以区分哪些 OS-prep 决策依赖 runtime implementation,哪些只是 repo/release hygiene
      - implementation 尚未开始,所以涉及 SDK/application public API framing 的 OS-prep 决策仍不应最终落地
      - audit-delivery-contract 已单独修正 audit/static/ECSS owner 文档,使 #12 不必继续阻塞在 runtime cleanup 上
    - **决策 / 分组**:
      - **可并行 scope / implement**:
        - #5 LICENSE:不依赖 SDK/application runtime shape
        - #8 CI quality gates:在现 baseline 上加 lint/type/coverage gates,与 runtime authority implementation 正交
        - #10 CONTRIBUTING / CODE_OF_CONDUCT:OS repo hygiene
        - #4 包名初筛 / 可用性查证:命名候选和可用性可先做;若与 PyPI 发布强绑定,最终确认可并入 #2
        - #12 list_compliance_matrix:已由 audit-delivery-contract 收口,只需 verification close-out
      - **等待 runtime cleanup implementation 后再定**:
        - #2 PyPI policy:会受最终 SDK/application public surface 和 README framing 影响
        - #7 OpenAPI yaml:service public route / composed spec 口径应等 runtime-facing service shape 稳定
        - #11 README OS 形态:public API framing 直接依赖 runtime cleanup 落地后的 SDK/application boundary
    - **#12 scoped decision**:
      - Keep `AuditQuery.list_compliance_matrix(...)` as audit package query convenience
      - Keep ECSS row assembly in `domains.ecss.compliance`
      - Keep lazy import inside audit query to avoid import-time dependency from kernel to domains
      - Do not move audit package reading into domains;that would increase coupling
      - Do not delete compliance matrix query because #3 exposes domains/ecss as public experimental surface
    - **Evidence**:
      - `src/kernel/audit/query.py` lazy-imports `domains.ecss.compliance` inside `list_compliance_matrix`
      - `src/domains/ecss/compliance.py` owns `build_compliance_matrix_rows(...)`
      - `src/kernel/audit/docs/03_audit_package_contract.md` documents audit query convenience vs domain row ownership
      - `src/domains/ecss/docs/README.md` and `01_overview.md` document `domains.ecss.compliance` ownership
   - **Impact**:
     - OS-prep is no longer globally blocked;only #2/#7/#11 stay runtime-implementation dependent
     - #12 can be treated as closed unless implementation review finds hidden kernel→domains coupling beyond lazy query convenience

13. **Partial-unhold implementation — LICENSE, repo hygiene, staged quality gates, package-name initial screen**
   - **Why**:
     - runtime-authority cleanup implementation is still pending,so OS-prep must not finalize SDK/API-facing publication claims
     - #5/#8/#10/#4-initial/#12 are independent enough to implement or verify as repo hygiene and release-prep evidence
     - Tool reality must drive CI design:adding broad blocking gates before measuring baseline would make the existing green test baseline red
   - **#4 initial package-name screen**:
     - `python -m pip index versions factpy-kernel` → `ERROR: No matching distribution found for factpy-kernel`
     - `python -m pip index versions factpy` → `factpy (0.1.0)`,so the shorter `factpy` name is occupied and should not be treated as an easy rename target
     - PyPI page cross-check shows `factpy 0.1.0` is a Factorio calculator released May 16, 2020
     - **Decision**:`factpy-kernel` remains the initial candidate;final package name / reservation check stays with #2 PyPI policy
   - **#5 project license**:
     - **Decision**:Apache-2.0 for project code
     - Implemented root `LICENSE`
     - Added `pyproject.toml` license metadata + Apache classifier
     - Added README.md / README.en.md license line
     - **Dependency review caveat**:local installed metadata snapshot shows permissive/Apache/MIT/BSD for core/service/extraction/observability main deps, but `pymupdf` and `pymupdf4llm` report `Dual Licensed - GNU AFFERO GPL 3.0 or Artifex Commercial License`
     - **Impact**:`documents` extra handling is a release blocker for #2:options include keep optional with explicit disclosure,remove from publishable default,swap parser,or require commercial terms for productized document parsing
   - **#8 staged CI quality gates**:
     - Measured baseline:
       - `python -m ruff check src tools` → 667 errors
       - `python -m ruff check src/kernel src/service` → 33 errors
       - `python -m ruff check src/kernel/application` → clean
       - `python -m mypy src/kernel src/service` → 431 errors
     - **Decision**:make only `ruff check src/kernel/application` blocking now;add broad ruff + mypy as `continue-on-error` audit/report steps;add coverage report without threshold
     - Rationale:CI now surfaces quality debt without breaking the current PR test baseline;future cleanup can ratchet these checks to blocking as scopes become clean
   - **#10 repo hygiene**:
     - Added `CONTRIBUTING.md` with blueprint workflow, setup, test baseline, staged quality checks, and security pointer
     - Added `CODE_OF_CONDUCT.md` with concise professional conduct policy
     - Updated `docs/README.md` with root governance entries
   - **#12 verification close-out**:
     - No code changes needed;closure remains based on audit-delivery-contract evidence:
       - `AuditQuery.list_compliance_matrix(...)` lazy imports domains compliance only inside the method
       - `domains.ecss.compliance` owns row assembly
       - audit package and ECSS docs now record this owner split
   - **Impact**:
     - Partial-unhold group is complete except for final package-name release decision
     - Remaining OS-prep blockers are now narrower:#2 PyPI policy, #7 OpenAPI yaml, #11 README public API framing, final #4, and documents extra license disposition

14. **Surface findings promoted into #2/#4 input surface**
   - **Why**:
     - The PyMuPDF / PyMuPDF4LLM AGPL-commercial finding is a release-policy constraint,not merely a docs warning
     - If this stays only in the audit log, #2 could be scoped later as "publish cadence" while missing the mixed-license distribution question
     - #4 initial screen is strong enough to set a default candidate,even though formal reservation must still happen at publish time
   - **#2 input refinement**:
     - Added explicit `documents` extra disposition choices:
       - keep `documents` extra with explicit license boundary disclosure
       - keep document parsing outside PyPI / GitHub-only install
       - replace PyMuPDF stack with permissive parser such as pypdf or pdfplumber
       - remove `documents` extra from v0.1
     - #2 acceptance now requires publication policy and documents-extra disposition to be scoped together
   - **#4 input refinement**:
     - Default candidate is `factpy-kernel`
     - `factpy` is occupied and should not be treated as a cheap rename
     - Final availability / reservation remains tied to #2 PyPI policy

15. **Runtime-authority cleanup implemented and merged — #2/#7/#11 unblocked**
   - **Why**:
     - Decision 11 held OS-prep runtime-dependent decisions until SDK/application public runtime authority stopped drifting.
     - Runtime-authority cleanup is now implemented and merged into this branch:application owns canonical Python runtime contracts/executors;SDK remains the product surface / authoring shell / ergonomic facade.
     - The cleanup blueprint explicitly closed its Outcome / Deviations and signaled that OS-prep #2 / #7 / #11 are no longer blocked on runtime implementation.
   - **Evidence**:
     - `runtime-authority-cleanup` HEAD `d9c7fcb`:docs aligned,cleanup blueprint status `implemented`,1093-test baseline recorded.
     - Merge commit `55df82f` brings the full cleanup chain into `oss-prep-v0.1`.
     - SDK adapter switches are in place for derivation/query/ingest;service/agent SDK import boundary guard is present in `src/kernel/tests/test_sdk_consumer_boundary.py`.
     - Module docs now state `application = canonical Python runtime authority` and `sdk = product surface`.
   - **Decision / state change**:
     - Release the wait-for-cleanup-implementation gate on #2 PyPI policy, #7 OpenAPI yaml, and #11 README OS framing.
     - Do **not** mark #2 / #7 / #11 scoped in this patch. Each still needs a dedicated decision pass:
       - #2 must choose publication policy and `documents` extra disposition together.
       - #7 must decide whether OS v0.1 publishes live OpenAPI only, checked-in yaml, composed spec, or no HTTP contract.
       - #11 must decide whether to keep the current README shape or rewrite for external readers under the new application/SDK boundary.
     - Final #4 package-name reservation remains tied to #2 publication policy.
   - **Impact**:
     - OS-prep is now back in active scoped work rather than runtime-cleanup hold.
     - Current test baseline for this branch is 1093 tests,not the older 1023 namespace-split baseline.
     - The next high-leverage pass was expected to be #2;Decision 16 supersedes the documents-extra part after the OSS surface was corrected to kernel-only.

16. **OSS surface correction — v0.1 open-source package is kernel-only**
   - **Why**:
     - 用户 clarified that the open-source portion only includes the kernel body;agent is not included.
     - In this repo's current topology,agent/service/domains are top-level packages under `src/`,and `setuptools.find` currently includes all packages unless restricted.
     - Therefore the earlier scoped #1/#3 decision(single distribution with all 4 namespaces public,with agent/domains experimental) was based on the wrong release surface.
   - **Corrected decisions**:
     - **#1 Artifact topology**:single distribution, but the publishable v0.1 wheel must include only `kernel*` import packages. The previous "single distribution / multi namespaces" decision is superseded for OSS v0.1.
     - **#3 OS visibility scope**:kernel-only. `agent`, `service`, and `domains` remain monorepo private / future surfaces,not v0.1 OSS public packages.
     - **#7 OpenAPI yaml**:closed as no v0.1 artifact. Kernel-only OSS has no HTTP delivery surface;service/agent OpenAPI contracts are private/out-of-scope for v0.1.
   - **#2 impact**:
     - PyPI policy no longer needs to carry `documents` extra / PyMuPDF disposition as a kernel blocker.
     - PyMuPDF / PyMuPDF4LLM remain AGPL-commercial dependencies in the private agent document parser surface;they must not appear in kernel-only PyPI metadata or OSS install instructions.
     - #2 now needs to decide publication cadence and verify package inclusion:wheel metadata and installed import packages should expose `kernel`,not `agent`, `service`, or `domains`.
   - **#11 impact**:
     - README must be rewritten from the current 4-package monorepo narrative into kernel-first external reader framing.
     - Private monorepo surfaces may be mentioned only as internal/future context,not as v0.1 OSS install targets.
   - **#12 impact**:
     - audit-delivery-contract's owner split remains correct internally:`domains.ecss.compliance` owns ECSS row assembly.
     - Because `domains` is not in the kernel-only wheel,`AuditQuery.list_compliance_matrix(...)` cannot be presented as a guaranteed OSS public contract unless optional-domain behavior is explicitly documented/tested.
   - **Implementation consequences**:
     - `pyproject.toml` package discovery must change from all `src/` packages to `kernel*` only before PyPI release.
     - README / README.en install instructions must drop `.[extraction,documents]` as OSS quickstart instructions.
     - CI can continue testing private monorepo packages, but release validation must add wheel inspection / installed import checks for kernel-only packaging.

### 未决问题 / partial-unhold state

- §5.1 已 scoped / implemented:#1(rescoped kernel-only package) / #3(rescoped kernel-only visibility) / #5(project license) / #6 / #7(no OpenAPI artifact) / #8(staged) / #9 / #10
- #4 已完成 initial screen:`factpy-kernel` 暂无 matching PyPI distribution;final name / reservation 仍等 #2
- runtime cleanup implementation gate 已释放:#2 / #11 可恢复 scoping;它们仍未决
- #12 internal owner split scoped,但 kernel-only OSS public contract action 仍待定(optional/private/hide)
- 与 PyPI publishing 强绑定的 final #4 仍随 #2 同期处理
- PyMuPDF / PyMuPDF4LLM AGPL-commercial license disposition is private agent/future surface;kernel-only wheel must not include it
- Acceptance(§7)和 Implementation Plan(§8)在 decision phase 完成后填具体值
- **CI hotfix 决策已 closed**(见 Event Log "CI baseline hotfix landed";commit `30a29b0`)
- **Hold 状态**:runtime-authority cleanup hold 已解除;#2/#11/#12 进入下一轮 scoped decision pass(#7 已因 kernel-only scope closed as no v0.1 OpenAPI artifact)

### 执行约束(继承自 namespace-split blueprint)

- 工程 trap checklist:见 §6 Boundaries
- archived blueprints 一字不改
- 1093 tests 不可回退(当前 `oss-prep-v0.1` baseline after cleanup merge)
- Bash tool 实际是 zsh,所有循环用 `while IFS= read -r f`
- macOS BSD sed 不支持 `\b`
