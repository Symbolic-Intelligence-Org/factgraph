# Audit Log — L Direction G4 (Why-not + Frontier SDK Shell)

| Date | Status | Event | Details |
|------|--------|-------|---------|
| 2026-05-08 | draft | Blueprint created | G4 draft opened after G1 shipped and Q1 validation extraction landed (`d6716a0`). Source inputs: Round 8 SDK conventions audit (`G4 Why-not + Frontier: clean`, with `universe -> why_not` naming correction and Frontier advanced-importable warning), G1 archived blueprint/audit, A capability helpers (`build_why_not_candidate_universe`, `build_frontier_view_facts`), Why-not application runtime/protocol, and core Frontier blueprint/drift gates. Draft records seven Step 0 questions: Why-not SDK input shape, candidate universe input shape, return shape, Frontier placement, module/method placement, error mapping, and test layout/invariants. Status remains `draft`; no SDK method or code change scoped yet. |
| 2026-05-08 | draft | §5.1 locked — Why-not SDK input shape | Read-only falsifier pass against `kernel.application.protocol.derivation_why_not.WhyNotUniverseRequest`, `kernel.application.why_not_runtime.check_why_not_universe(...)`, `kernel.application.capability_helpers.why_not.build_why_not_candidate_universe(...)`, and G1 archived §5.7 + Round 4 Q1 lock (`kernel.sdk._validation.validate_derivation`). All four §5.1 falsifiers (F1 explicit-universe contract / F2 G1 lowering reusable / F3 reject `CompiledDerivationPlan` / F4 reject store-wide discovery) PASS. Decision: lock option 1 — `SDKStore.why_not(derivation, candidates, *, engine="native", registry=None) -> WhyNotUniverseResult` — accept SDK `Derivation` + explicit candidate universe; reuse G1 lowering chain (`_compile_derivation_input(...)` -> `_compiled_derivation_plan_to_application(...)` -> A's `build_why_not_candidate_universe(plan, candidates)` -> runtime `check_why_not_universe(request, store, registry)`). No `CompiledDerivationPlan` at SDK boundary; no store-wide auto-discovery. Forward: §5.2 (candidate row form) is now sharpened to "which row forms does the SDK `candidates` argument accept" and is the next falsifier. Status remains `draft` pending §5.2-§5.7. |
| 2026-05-08 | draft | §5.2 locked — candidate universe row forms | Read-only falsifier pass against A's public helper exports and implementation. Decision: `SDKStore.why_not(..., candidates, ...)` accepts `Sequence[Mapping[str, Any] \| Sequence[Any]]`, directly mirroring `build_why_not_candidate_universe(plan, candidates)`. Five falsifiers PASS: F1 no A private helper import needed because `build_why_not_candidate_universe` is public and internally owns `_candidate_values(...)` + `normalize_binding_items(...)`; F2 SDK entity snapshots / refs / DSL objects are not needed for first slice (A tests prove mapping + sequence rows sufficient); F3 direct `BindingItems` input is rejected as application canonical leakage and ambiguous with sequence-row positional interpretation; F4 malformed candidate rows are already `CapabilityHelperError` and will remap to `SDKStoreError(path="$.why_not.candidates") from exc`; F5 SDK-owned candidate-universe DTO would add outward surface under `#6` without signal. Forward: §5.3 return shape is next; §5.6 must lock remap of A helper errors for candidate failures. |
| 2026-05-08 | draft | §5.3 locked — Why-not return shape | Read-only falsifier pass against G1 archived §5.2 lock, current `kernel.sdk.__all__` runtime state (length 34; `CheckResult` / `DiagnoseResult` / `WhyNotUniverseResult` all absent), and `WhyNotUniverseResult` class shape at `kernel.application.protocol.derivation_why_not:294-332`. Decision: documented passthrough — `SDKStore.why_not(...) -> WhyNotUniverseResult` returns the raw application protocol DTO; not re-exported from `kernel.sdk.__all__`; result type documented in shell docstring + `04_api_surface.md`; SDK callers either import `WhyNotUniverseResult` from `kernel.application.protocol` themselves or use it via attribute access. Three falsifiers PASS: F1 G1 raw-passthrough precedent in force (verified at runtime); F2 `WhyNotUniverseResult` already absent from `kernel.sdk.__all__`, lock requires preservation; F3 `WhyNotUniverseResult` is `@dataclass(frozen=True)` over application-canonical types (literal status / tuple bindings / structured `WhyNotRedRow` rows / `ErrorDTO`+`WarningDTO` tuples), no mutable internals to redact, no source signal justifying SDK-owned wrapper under `#6` no-outward-compat-without-signal. Forward: §5.4 (Frontier placement) is independent of return shape and is the next falsifier; §5.6 (error mapping) must cover `WhyNotRuntimeError(ValueError)` from `why_not_runtime:35` plus G1's existing remap targets; G4 acceptance will include `kernel.sdk.__all__`-unchanged invariant test mirroring G1's. |
| 2026-05-08 | draft | §5.4 locked — Frontier remains advanced importable | Read-only falsifier pass against Round 8 G4 verdict, core Frontier blueprint/drift gates, A projection helper, and application no-opt-in tests. Decision: G4 first slice ships **no Frontier SDK method**; only `SDKStore.why_not(...)` is in scope. Frontier remains advanced importable via `kernel.core.rules.frontier.evaluate_native_where_frontier(...)` plus `kernel.application.capability_helpers.build_frontier_view_facts(...)`; SDK docs may mention this path but `kernel.sdk` must not import core Frontier. Four falsifiers PASS: F1 Round 8 explicitly said Frontier should remain advanced importable rather than enter SDK facade; F2 Frontier blueprint non-goals excluded SDK shell/application capability and close-out requires future application consumers to open a scoped blueprint before importing Frontier; `test_10_application_layer_does_not_opt_into_frontier_trace` enforces this; F3 combined Why-not+Frontier would false-merge `WhyNotUniverseResult` green/red board with `NativeWhereFrontierEvaluation` aggregate sparse rows (`#3`); F4 projection-only `SDKStore.frontier_view_facts(...)` would just expose intermediate `view_facts` while callers still import core Frontier, adding outward surface with little SDK value. Forward: no `kernel/sdk/frontier.py`, no `test_sdk_frontier.py`, no `SDKStore.frontier(...)`; §5.5 module placement only needs to decide the Why-not shell file and whether flat modules remain acceptable. |
| 2026-05-08 | draft | §5.5 locked — flat module layout, G2 inherits shells/ trigger | Read-only falsifier pass against G1 archived §5.5 forward-trigger wording (`G1 archive:309`), current `src/kernel/sdk/` flat module inventory (`check.py`, `diagnose.py`, `_validation.py`, plus pre-G1 `store.py`, `facade.py`, `ingest.py`, `compile.py`, etc.), and the G1 invariant test `test_g1_modules_are_flat_and_no_shells_package_exists` enforcing flat layout. Decision: lock continued flat layout — `src/kernel/sdk/why_not.py` exporting `sdk_why_not(...)`, mirroring G1's pattern verbatim. SDKStore method delegates via `from .why_not import sdk_why_not`. No `kernel/sdk/shells/` migration in G4. Four falsifiers PASS: F1 post-G4 shell file count is 3 (at threshold but not over; G4 only adds 1 file because Frontier deferred per §5.4); F2 migration cost is high — touches G1 archived invariant test, requires `#P1` carve-out, multi-file refactor + 3+ test files + `_validation.py` sibling reorganization, dwarfing G4's 1-file scope; F3 thin SDKStore delegate preserved under either option (G1 pattern carries verbatim); F4 G1 §5.5 forward trigger originally named "G2" but Q1 ordering changed to G1 -> G4 -> G2; G4 inherits trigger semantics by file-count signal and re-records ownership to G2 Step 0. Forward: G2 Step 0 forward trigger re-recorded — before G2's Step 0 lock, G2 MUST evaluate `kernel/sdk/shells/` migration as G2 Phase 0 hygiene, including explicit `#P1` carve-out for the G1 invariant test update; G4 invariant tests will mirror G1 (assert flat layout for `why_not.py`, no `shells/` directory, `kernel.sdk.__all__` unchanged, `WhyNotUniverseResult` not exported); §5.6 (error mapping) is the next falsifier. |
| 2026-05-08 | draft | §5.6 locked — SDKStoreError remap paths | Read-only falsifier pass against G1 `check.py` / `diagnose.py` remap pattern, `kernel.application.protocol.derivation_why_not.WhyNotUniverseRequest.__post_init__`, `kernel.application.why_not_runtime.WhyNotRuntimeError`, and A's public `build_why_not_candidate_universe(...)`. Decision: every lower-layer exception crossing `SDKStore.why_not(...)` becomes `SDKStoreError(...) from exc`; no new SDK error subclass. Locked paths: `validate_derivation(...)` already uses `$.why_not.derivation`; `_compiled_derivation_plan_to_application(...)` `ValueError` -> `$.why_not.derivation`; `_resolve_runtime_registry(...)` `RuleCompileError` -> `$.why_not.dependencies`; `build_why_not_candidate_universe(...)` `CapabilityHelperError` -> `$.why_not.candidates`; `WhyNotUniverseRequest(...)` `ProtocolShapeError` -> `$.why_not.request`; `check_why_not_universe(...)` `WhyNotRuntimeError` -> `$.why_not`. Five falsifiers PASS: runtime errors are application-layer `ValueError` subclasses and must not leak; request DTO construction can raise `ProtocolShapeError` for plan/head/candidate/engine shape and must be wrapped; derivation lowering and dependency resolution reuse landed G1 B.2/B.1 patterns; candidate errors preserve A `CapabilityHelperError` cause chains; Frontier error mapping is inactive because §5.4 ships no Frontier SDK method. Forward: implementation tests must cover all remap paths and assert `__cause__` chaining. |
| 2026-05-08 | draft | §5.7 locked — test layout and invariants | Read-only falsifier pass against G1 test inventory (`test_sdk_check.py` 16 tests / `test_sdk_diagnose.py` 19 tests / `test_sdk_g1_invariants.py` 6-test single class) and G1 §5.6 flat-test lock. Decision: G4 ships exactly two flat test files — `src/kernel/tests/test_sdk_why_not.py` (≈ 16-18 contract tests covering each §5.1-§5.6 lock + Sibling-discipline static + runtime tests) and `src/kernel/tests/test_sdk_g4_invariants.py` (6 invariant classes 1-to-1 mirror of G1's: `__all__` unchanged + `WhyNotUniverseResult` not exported / instance-method placement / flat-module-no-shells / no-internal-walker-audit-imports / thin-delegate / docstring-boundary-contract). No `test_sdk_frontier.py` per §5.4 lock. Five falsifiers PASS: F1 G1 naming convention symmetric (`test_sdk_{capability}.py` + `test_sdk_g{N}_invariants.py`); F2 each §5.1-§5.6 lock has at least one test (mapping in blueprint Decision); F3 each G1 invariant has 1-to-1 G4 mirror; F4 Sibling discipline preserved as runtime-patch + static-source-scan two-form check (mirrors G1 Diagnose); F5 tests stay flat under `src/kernel/tests/`, fixtures inline per file (Round 4 C.7 accepted; G2 Phase 0 hygiene is the venue for shared fixture extraction if needed). Forward: all §5.1-§5.7 locked; status transition `draft -> scoped` is the next step (separate decision, not auto-promoted); §7 acceptance + §8 implementation plan to be filled at scoping time. |
| 2026-05-08 | scoped | Status transition — G4 scope frozen | Full blueprint review after §5.1-§5.7 locks. Updated status `draft -> scoped`; converted draft-stage §7 acceptance to completed; filled scoped-stage acceptance with Phase 0-3 gates; replaced §8 placeholder with executable four-phase implementation plan (Phase 0 skeleton/delegate, Phase 1 real `sdk_why_not(...)`, Phase 2 invariants/docs, Phase 3 close-out/archive/publish); replaced stale "Forbidden until scoped" language with implementation-time boundaries. Frontier remains out of scope; README quickstart and `kernel.sdk.__all__` remain untouched unless a later blueprint scopes them. |
| 2026-05-08 | implementing | Phase 0 landed — skeleton + delegation hook | Added `src/kernel/sdk/why_not.py` with `sdk_why_not(sdk, derivation, candidates, *, engine="native", registry=None) -> WhyNotUniverseResult` placeholder raising `NotImplementedError("...Phase 0 skeleton.")` (locked §5.1 signature; locked §5.3 return type imported runtime). Extended `kernel/sdk/store.py` `TYPE_CHECKING` block to include `WhyNotUniverseResult`; added `Sequence` to `collections.abc` import; inserted `SDKStore.why_not(...)` instance method between `diagnose` and `ref` (thin delegate `from .why_not import sdk_why_not; return sdk_why_not(self, ...)`, mirrors G1 pattern verbatim; docstring records boundary contract per §6 / §5.7 invariant). Added `src/kernel/tests/test_sdk_why_not.py` with 3 Phase 0 skeleton tests (method exists / delegate reaches stub / module function raises with Phase 0 marker). Added `src/kernel/tests/test_sdk_g4_invariants.py` with 4 Phase 0 invariant tests (`__all__` unchanged + `WhyNotUniverseResult` not exported per §5.3, instance-method placement per §5.4, no Frontier SDK module per §5.4, flat layout + no shells/ per §5.5). Phase 2 fills the remaining four invariants (no-internal-walker-audit-imports, thin-delegate static check, docstring-boundary-contract, Q1 Sibling source scan). Phase 0 audit gate verifies all 5 dimensions: flat module placement (PASS), delegate shape (PASS), locked signature (PASS), no `__all__` expansion (PASS, length still 34 verified at runtime), no runtime dispatch (PASS, sdk_why_not raises before any lowering / build / runtime call). Verification: targeted 7 tests OK; full kernel suite 1521 OK / 1 skipped (was 1514, +7 new G4 tests); ruff clean on `why_not.py`, `store.py`, `test_sdk_why_not.py`, `test_sdk_g4_invariants.py`; `git diff --check` clean. Status advances `scoped -> implementing`; Phase 1 (real `sdk_why_not(...)` body + full §5.1-§5.6 contract tests) is next. |
| 2026-05-08 | implementing | Phase 1 landed — real `sdk_why_not(...)` implementation | Replaced the Phase 0 placeholder with real `SDKStore.why_not(...)` lowering and dispatch. Implementation validates SDK `Derivation` via `validate_derivation(..., path="$.why_not.derivation")`, lowers through G1 `_compile_derivation_input(...)` + `_compiled_derivation_plan_to_application(...)`, resolves runtime registry, normalizes explicit candidate rows through A's public `build_why_not_candidate_universe(...)`, constructs `WhyNotUniverseRequest(...)`, dispatches `check_why_not_universe(...)`, and returns raw `WhyNotUniverseResult` (documented passthrough; no SDK wrapper; no `kernel.sdk.__all__` change). Implemented all §5.6 remap paths with cause chaining: derivation `ValueError` -> `$.why_not.derivation`, dependency `RuleCompileError` -> `$.why_not.dependencies`, candidate `CapabilityHelperError` -> `$.why_not.candidates`, request `ProtocolShapeError` -> `$.why_not.request`, runtime `WhyNotRuntimeError` -> `$.why_not`. Expanded `test_sdk_why_not.py` from 3 skeleton tests to 17 contract tests covering mapping rows, sequence rows, empty universe, invalid SDK surface inputs (`Rule` / `CompiledDerivationPlan` / multi-head `Derivation`), malformed candidates, all remap paths, engine propagation, registry propagation, raw result passthrough, result not exported from `kernel.sdk`, and Q1 Sibling independence via runtime patch + static source scan. Verification: G4/G1 SDK targeted suite 73 tests OK; Why-not application/helper regression suite 109 tests OK; full kernel suite 1535 OK / 1 skipped; ruff clean on `why_not.py` + `test_sdk_why_not.py`; `git diff --check` clean. Phase 1 audit gate PASS on error remaps, layer imports, return shape, and Q1 Sibling independence. Phase 2 invariants/docs remain. |
