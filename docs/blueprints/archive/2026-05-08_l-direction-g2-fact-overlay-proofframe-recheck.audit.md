# Audit Log — L Direction G2 (Fact Overlay + ProofFrame Recheck SDK Shell)

| Date | Status | Event | Details |
|------|--------|-------|---------|
| 2026-05-08 | draft | Blueprint created | G2 draft opened after G4 shipped + post-publish polish landed (`acb5a6e`) and Path B combined snapshot `v0.1-public-surface-helpers-walker-l-g1-l-g4-2026-05-08` published. Source inputs: Round 8 SDK conventions audit (G2 verdict: **pending Step 0 shape decision** — `SupportArtifact` + `EvaluationOverlay` raw-DTO boundary question), G1 + G4 archived blueprints/audits, A capability helpers (`build_fact_value_override` shipped Batch 2; ProofFrame Recheck has no A-side builder), Fact Overlay + ProofFrame Recheck application runtime/protocol DTOs (`FactOverlayCheckRequest` / `FactOverlayCheckResult` at `kernel.application.protocol.derivation_fact_overlay`, `ProofFrameRecheckRequest` / `ProofFrameRecheckResult` at `kernel.application.protocol.proofframe`), G1+G4 §5.5 inherited `kernel/sdk/shells/` migration trigger. Draft records eight Step 0 questions: §5.1 Fact Overlay SDK input shape, §5.2 ProofFrame Recheck SDK input shape (raw `SupportArtifact` boundary), §5.3 Fact Overlay return shape, §5.4 ProofFrame Recheck return shape, §5.5 module placement + shells/ migration evaluation (**inherited mandatory trigger**), §5.6 module file naming, §5.7 SDKStore method names, §5.8 error mapping + Q3/Batch-4 Sibling discipline at SDK layer. Status remains `draft`; no SDK method or code change scoped yet. |
| 2026-05-08 | draft | §5.1 locked — Fact Overlay SDK input shape | Read-only falsifier pass against `EvaluationOverlay` / `FactOverlayCheckRequest` protocol validation, Fact Overlay runtime tests, Batch 2 capability helpers, and G1 §5.7. Decision: `SDKStore.check_fact_overlay(derivation, binding, overlay, *, engine="native", registry=None) -> FactOverlayCheckResult`; accept SDK `Derivation`, `$`-prefixed binding mapping, and raw `EvaluationOverlay` protocol DTO. Rationale: `EvaluationOverlay` is author-time intent (`fact_actions` + `rule_actions`) rather than an already-lowered application plan, so G1's `CompiledDerivationPlan` rejection does not apply. Four falsifiers PASS: F1 `EvaluationOverlay` is frozen intent shape and `FactOverlayCheckRequest` accepts it directly; F2 protocol/runtime tests already use raw `EvaluationOverlay` as ergonomic intent object; F3 Batch 2 helpers build the action pieces and a parallel SDK overlay wrapper would add outward surface under `#6` without signal; F4 protocol validation rejects malformed overlay shapes and §5.8 will remap request/runtime errors to `SDKStoreError`. Forward: implementation validates `derivation` + `binding` through shared SDK validators, constructs `FactOverlayCheckRequest(plan, binding, overlay, engine)`, and does not accept `CompiledDerivationPlan`, raw request DTOs, per-action kwargs, or SDK-owned overlay DTOs in this slice. |
| 2026-05-08 | draft | §5.2 locked — ProofFrame Recheck SDK input shape | Read-only falsifier pass against `kernel.core.store._support.SupportArtifact` (`_support.py:96-126`), `kernel.application.protocol.derivation_check.CheckResult.engine_payload` union, `kernel.application.proofframe_runtime.recheck_proof_frame(...)`, and the Q1 Sibling discipline locks from G1 §5.1 + G4 §5.2. Decision: `SDKStore.recheck_proof_frame(support_artifact, overlay) -> ProofFrameRecheckResult`; accept raw `SupportArtifact` + raw `EvaluationOverlay` directly. SDK shell never wraps `SupportArtifact`, never extracts it from a `CheckResult` argument, and never calls `sdk_check` internally. Four falsifiers PASS: F1 `SupportArtifact` is `@dataclass(frozen=True)` with canonical sorted fields and no SDK-layer state; F2 composition (option 3) would force unpacking `CheckResult.engine_payload` (which is `SupportArtifact \| ProvenanceEnvelope` union) at SDK layer and either reject non-native or call `sdk_check` internally — both break Sibling discipline; F3 no source signal warrants an SDK wrapper (mirrors G4 §5.3 reasoning for `WhyNotUniverseResult`); F4 locking option 1 keeps `sdk_proof_frame_recheck` from importing or calling any other SDK shell. Forward: §5.4 return-shape lock becomes a clean documented passthrough; §5.8 error mapping is narrower (no lowering / no registry / no candidate helper — only validate + request DTO + runtime); the cross-cutting precedent established by §5.1 + §5.2 — "raw application protocol DTOs may cross the SDK boundary as input when (a) frozen application-canonical AND (b) no SDK alternative without inventing new outward surface or breaking Sibling" — is recorded for future G blueprints. |
| 2026-05-08 | draft | §5.3 / §5.4 / §5.7 locked — return passthrough + method names | Low-suspense falsifier pass after §5.1 + §5.2 resolved the input surfaces. Decisions: `SDKStore.check_fact_overlay(...) -> FactOverlayCheckResult`, `SDKStore.recheck_proof_frame(...) -> ProofFrameRecheckResult`, and method names `check_fact_overlay` / `recheck_proof_frame`. Runtime checks confirmed `kernel.sdk.__all__` remains length 34, neither `FactOverlayCheckResult` nor `ProofFrameRecheckResult` is exported or attached to `kernel.sdk`, and neither method name currently exists on `SDKStore`. Source checks confirmed both result DTOs are frozen application protocol dataclasses with canonical result fields and no SDK store / registry / walker / lazy substrate state (`derivation_fact_overlay.py:367-415`, `proofframe.py:120-139`). G2 therefore follows the G1 + G4 documented-passthrough precedent and avoids wrapper / re-export commitments under `#6`. Method naming keeps the current `SDKStore.<method>` pattern and leaves global OpenAI-style SDK ergonomics / namespace redesign to the post-L redesign blueprint. |
| 2026-05-08 | draft | §5.5 locked — `kernel/sdk/shells/` migration as G2 Phase 0 hygiene | Read-only falsifier pass against the G1+G4 inherited shells/ migration trigger, current SDK module imports, test patch paths, and archived G1+G4 invariant tests. Decision: **migrate** — G2 Phase 0 hygiene moves `check.py` / `diagnose.py` / `why_not.py` / `_validation.py` into `kernel/sdk/shells/` **before** any G2 implementation lands. `_validation.py` moves with shells (only consumers are shells; future re-promotion possible). Five falsifiers PASS: F1 migration is mechanical and bounded (~30-50 lines across ~7 files: 4 source files moved, 3 store.py delegate imports, ~15 test patch paths, 2 invariant module-list constants, 1 Sibling static-check assertion); F2 thin SDKStore delegate pattern preserved with one-character changes (`from .check` → `from .shells.check`); F3 G1+G4 invariant tests are retrofit (not deprecated) per `#P1` rule 8 — flat-layout assertions invert to shells/-layout assertions, fully sanctioned by G1 §5.5 archived forward trigger; F4 post-migration verification plan testable (targeted + full kernel + ruff); F5 defer alternative structurally weaker (post-G2 = 5 flat files at threshold; G3 would push to 6-8; trigger explicitly named "third/fourth file" already overdue). Forward: §5.6 file naming becomes mechanical (`shells/fact_overlay.py` + `shells/proof_frame.py`); §8 Phase 0 = the migration commit, no behavior change, before any G2 impl; `#P1` carve-out audit entries to land in G1+G4 archived audit logs as part of Phase 0; published G1+G4 snapshot branches (`v0.1-l-g1-...` and `v0.1-l-g4-...`) are NOT touched (migration only on G2 topic branch and forward). |
| 2026-05-08 | draft | §5.6 / §5.8 locked — shell file names + error paths / Sibling discipline | Final Step 0 falsifier pass for G2 implementation shape. §5.6 decision: G2 adds `kernel/sdk/shells/fact_overlay.py` (`sdk_fact_overlay_check(...)`) and `kernel/sdk/shells/proof_frame.py` (`sdk_proof_frame_recheck(...)`) after Phase 0 creates the `shells/` package. §5.8 decision: Fact Overlay remaps SDK input validation (`$.check_fact_overlay.derivation` / `.binding`), plan conversion `ValueError` (`$.check_fact_overlay.derivation`), registry `RuleCompileError` (`$.check_fact_overlay.dependencies`), and request `ProtocolShapeError` (`$.check_fact_overlay.request`) to `SDKStoreError(... ) from exc`; runtime `check_fact_overlay_binding(...)` result DTOs, including `invalid_request`, pass through. ProofFrame Recheck remaps support / overlay validation (`$.recheck_proof_frame.support_artifact` / `.overlay`), request `ProtocolShapeError` (`$.recheck_proof_frame.request`), and unexpected `recheck_proof_frame(...)` exceptions (`$.recheck_proof_frame`). Source checks: `FactOverlayCheckRequest.__post_init__` raises `ProtocolShapeError` for bad request shape (`derivation_fact_overlay.py:282-307`); `check_fact_overlay_binding(...)` encodes unsupported/invalid/runtime phase failures as `FactOverlayCheckResult(status="invalid_request")` (`fact_overlay_runtime.py:27-104`, `:478-489`); `ProofFrameRecheckRequest` only validates `SupportArtifact` + `EvaluationOverlay` (`proofframe.py:85-94`); `recheck_proof_frame(...)` deletes registry and returns unknown-frame results for unsupported support/rule-action cases (`proofframe_runtime.py:30-45`). Both G2 shells must not call any sibling SDK shell (`sdk_check`, `sdk_diagnose`, `sdk_why_not`, or each other); implementation tests will static-scan and runtime-patch application dispatch directly. |
| 2026-05-08 | scoped | Scope freeze | All §5.1-§5.8 falsifier passes are locked. Status advanced `draft` → `scoped`; §7 scoped-stage acceptance added; §8 implementation plan filled with five phases: Phase 0 shells/ migration hygiene + `#P1` carve-out audit entries for G1/G4 invariant retrofit, Phase 1 Fact Overlay SDK shell, Phase 2 ProofFrame Recheck SDK shell, Phase 3 invariants/docs/cumulative audit, Phase 4 close-out/archive/publish. No implementation code has landed yet on G2; Phase 0 is now authorized as the first implementation commit. |
| 2026-05-08 | implementing | Phase 0 landed — `kernel/sdk/shells/` migration hygiene | `git mv` four shells (`check.py`, `diagnose.py`, `why_not.py`, `_validation.py`) into `kernel/sdk/shells/` plus new empty `__init__.py`. Cross-package import fixes inside shells (`from .errors`/`.store`/`.dsl` → `from ..errors`/`..store`/`..dsl`); SDKStore delegate imports updated to `from .shells.<x>`. Test patch paths rewritten across `test_sdk_check.py` / `test_sdk_diagnose.py` / `test_sdk_why_not.py` (~22 paths total) to `kernel.sdk.shells.<x>.<member>`. Q1 Sibling static-check assertions broadened to also reject `from kernel.sdk.shells.check` / `from kernel.sdk.shells.diagnose`. `test_sdk_validation.py` import retargeted. G1 + G4 archived invariants retrofit per `#P1`: `test_g{1,4}_modules_are_flat_and_no_shells_package_exists` → `test_g{1,4}_modules_live_in_shells_subpackage` with inverted assertions; `G1_MODULES` / `G4_MODULES` constants retargeted; thin-delegate text assertions updated to `from .shells.<x> import sdk_<x>`; `test_sdk_store_why_not_is_instance_method` simplified to `assertFalse(hasattr(kernel_sdk, "why_not"))`; `test_no_frontier_sdk_module_or_method_exists` extended to also reject `kernel/sdk/shells/frontier.py`. `#P1` carve-out audit-log entries appended to `docs/blueprints/archive/2026-05-08_l-direction-g1-check-diagnose.audit.md` + `docs/blueprints/archive/2026-05-08_l-direction-g4-why-not-frontier.audit.md` (principle id / reason / scope / impact / reviewer ack / historical-handling = retrofit). Docs updated: `kernel/application/docs/01_overview{,_en}.md` `kernel.sdk._validation` → `kernel.sdk.shells._validation`. Verification: targeted SDK suite 77 OK; full kernel 1539 OK / 1 skipped (zero regression); ruff clean; `git diff --check` clean. Commit: `dad109b` on `codex/v0.1-l-g2-fact-overlay-proofframe-recheck-2026-05-08`. G1 + G4 published snapshot branches NOT touched. |
| 2026-05-08 | implementing | Phase 1 landed — Fact Overlay SDK shell | New `src/kernel/sdk/shells/fact_overlay.py` (sdk_fact_overlay_check) implementing §5.1 / §5.3 / §5.6 / §5.8 locks: validates SDK `Derivation` + `$`-prefixed binding mapping via shared validators; lowers via G1 chain (`_compile_derivation_input` → `_compiled_derivation_plan_to_application`); resolves runtime registry; normalizes binding to canonical `BindingItems` via public `kernel.core.store._support.normalize_binding_items`; constructs `FactOverlayCheckRequest(plan, binding, overlay, engine)` directly with raw `EvaluationOverlay`; dispatches `check_fact_overlay_binding(request, store, registry)`; returns raw `FactOverlayCheckResult` (documented passthrough; not in `__all__`). All five §5.8 remap paths landed as `SDKStoreError(...) from exc`: `$.check_fact_overlay.derivation` (input + multi-plan + ValueError engine_ext), `$.check_fact_overlay.binding`, `$.check_fact_overlay.dependencies` (RuleCompileError), `$.check_fact_overlay.request` (ProtocolShapeError including malformed overlay). Runtime returns `invalid_request` results for empty overlay / rule_actions / unsupported runtime conditions — passed through unchanged. New `SDKStore.check_fact_overlay(...)` instance method (between `why_not` and `ref` in `store.py`) with thin delegate `from .shells.fact_overlay import sdk_fact_overlay_check; return sdk_fact_overlay_check(self, ...)`. TYPE_CHECKING extended to import `FactOverlayCheckResult`. New `src/kernel/tests/test_sdk_fact_overlay.py` with 15 contract tests covering: happy path with seeded store + real overlay; rule_actions/empty-overlay invalid_request passthrough; Rule rejection; CompiledDerivationPlan rejection; multi-head rejection; binding-must-be-mapping; binding-keys-must-be-dollar-prefixed; non-EvaluationOverlay request remap; engine_ext conflict (B.2 pattern); RuleCompileError dependency (B.1 pattern); engine passthrough; registry passthrough; `__all__` non-export; Q3 Sibling runtime patch + static source scan. Phase 1 audit gate (5 dimensions) PASS: locked input shapes / locked return passthrough / all 5 remap paths exercised / Q3 Sibling enforced both runtime + static / no `__all__` change. Verification: 15 targeted Fact Overlay tests OK; full kernel 1554 OK / 1 skipped (was 1539; +15 new tests, zero regression); ruff clean; `git diff --check` clean. Phase 2 (ProofFrame Recheck) is next per §8. |
| 2026-05-08 | implementing | Phase 2 landed — ProofFrame Recheck SDK shell | New `src/kernel/sdk/shells/proof_frame.py` (sdk_proof_frame_recheck) implementing §5.2 / §5.4 / §5.6 / §5.8 locks: validates `support_artifact` is `SupportArtifact` and `overlay` is `EvaluationOverlay` via local module-private validators (per user guidance — extraction to `shells/_validation.py` deferred until G3 / G5 also need them); constructs `ProofFrameRecheckRequest(support_artifact, overlay)` directly (no derivation lowering, no registry resolution, no engine arg — operates on already-captured support); dispatches `recheck_proof_frame(request, store=...)`; returns raw `ProofFrameRecheckResult` (documented passthrough; not in `__all__`). Four §5.8 remap paths landed as `SDKStoreError(...) from exc`: `$.recheck_proof_frame.support_artifact` (non-SupportArtifact), `$.recheck_proof_frame.overlay` (non-EvaluationOverlay including `tuple[FactValueOverride, ...]` form which the application request DTO would accept but G2 §5.2 rejects at SDK boundary), `$.recheck_proof_frame.request` (ProtocolShapeError from request DTO), and `$.recheck_proof_frame` (defensive `try/except Exception` around runtime — current `kernel.application.proofframe_runtime` source has zero raise paths but §5.8 covers the contract for forward-compat). New `SDKStore.recheck_proof_frame(...)` instance method (between `check_fact_overlay` and `ref` in `store.py`) with thin delegate `from .shells.proof_frame import sdk_proof_frame_recheck; return sdk_proof_frame_recheck(self, support_artifact, overlay)`. TYPE_CHECKING extended to import `ProofFrameRecheckResult`. New `src/kernel/tests/test_sdk_proof_frame.py` with 11 contract tests covering: happy path with seeded store + real SupportArtifact (captured via `sdk.check(...).evidence_envelope.engine_payload`) + real overlay; non-SupportArtifact rejection (str / None); non-EvaluationOverlay rejection; tuple-form overlay rejection (sharper than the application request DTO's tolerance); ProtocolShapeError remap via patched `ProofFrameRecheckRequest`; runtime RuntimeError remap; `__all__` non-export; runtime called with only `store=` kwarg (no registry, no engine); Q3/Batch-4 Sibling runtime patch (sdk_check / sdk_diagnose / sdk_why_not / sdk_fact_overlay_check none invoked); Q3/Batch-4 Sibling static source scan. Phase 2 audit gate (5 dimensions) PASS: locked input shapes / locked return passthrough / all 4 remap paths exercised / Sibling discipline enforced both runtime + static / no `__all__` change. Verification: 11 targeted ProofFrame tests OK; full kernel 1565 OK / 1 skipped (was 1554; +11 new tests, zero regression); ruff clean; `git diff --check` clean. Phase 3 (G2 invariants + docs + cumulative audit) is next per §8. |
| 2026-05-08 | implementing | Phase 3 landed — G2 invariants + docs + cumulative audit | New `src/kernel/tests/test_sdk_g2_invariants.py` mirroring G1/G4 6-class structure under post-migration shells/ layout: (1) `test_sdk_all_unchanged_and_g2_result_types_not_exported` — `__all__` length still 34, neither `FactOverlayCheckResult` / `ProofFrameRecheckResult` nor any related overlay/action DTOs are exported, neither `check_fact_overlay` nor `recheck_proof_frame` is in `__all__`; (2) `test_g2_methods_are_instance_methods_and_no_scenario_method_shipped` — both methods callable on `SDKStore`; reserved scenario names (`explain` / `fact_overlay` / `overlay_check` / `proof_frame` / `recheck`) absent; (3) `test_g2_modules_live_in_shells_subpackage` — `kernel/sdk/shells/{fact_overlay,proof_frame}.py` exist; flat layout files do not; (4) `test_g2_modules_do_not_import_internal_or_walker_layers` — same `FORBIDDEN_PRODUCTION_IMPORT_TEXT` set as G1+G4 (capability_helpers private `_binding`, `_reject_sdk_origin`, walker, audit, frontier); (5) `test_store_methods_remain_thin_delegate_methods` — both methods only `from .shells.<x> import sdk_<x>; return sdk_<x>(self, ...)`; assertions check instantiation patterns (`FactOverlayCheckRequest(`, `ProofFrameRecheckRequest(`, validator-call patterns) so legitimate docstring references to the type names don't false-trigger; (6) `test_store_method_docstrings_record_boundary_contracts` — both docstrings include all required type names + all locked `$.<cap>.<arg>` paths. Docs updated: `kernel/sdk/docs/04_api_surface.md` + `.en.md` add `check_fact_overlay(...)` / `recheck_proof_frame(...)` to method list and add full per-method shape/error-path descriptions; preamble paragraph notes G2 shells migration into `kernel/sdk/shells/` with G1+G4 `#P1` carve-out. Test inventories in `kernel/application/docs/01_overview.md` + `_en.md` add `test_sdk_fact_overlay.py`, `test_sdk_proof_frame.py`, `test_sdk_g2_invariants.py`. Phase 3 cumulative audit gate (5 dimensions) PASS: §5.1-§5.8 locks all honored across Phase 0/1/2/3; §6 invariants verified by 6-test G2 invariant file (mirror of G1+G4); shells/ layout + G1+G4 retrofit invariants holding; public `kernel.sdk.__all__` length still 34 (verified by every G1+G4+G2 invariant test that asserts the count); G1+G4 published snapshot branches untouched (migration + retrofit only on G2 topic). Verification: 6 targeted G2 invariant tests OK; full kernel 1571 OK / 1 skipped (was 1565; +6 new G2 invariant tests, zero regression); ruff clean; `git diff --check` clean. Phase 4 (close-out + archive + snapshot publish) is next per §8. |
| 2026-05-08 | implemented | Phase 4 close-out — implemented and archived | Filled §10 Outcome / Deviations with final landed surface (`SDKStore.check_fact_overlay(...)` + `SDKStore.recheck_proof_frame(...)` shells under `kernel/sdk/shells/`); 5-phase commit chain catalogue (Phase 0 `dad109b` migration + Phase 1 `cab15a9` Fact Overlay + Phase 2 `6e88f56` ProofFrame + Phase 3 `385bb01` invariants/docs/cumulative); 4 deviation entries (shells/ activation under `#P1`, local validators per user guidance, defensive runtime try/except, docstring `FactOverlayCheckRequest` reference handled by pattern-based assertion); test catalogue (15+11+6+11 = ~43 G2-relevant tests, full kernel 1571 OK / 1 skipped); boundary outcomes (`__all__` length still 34; `kernel/sdk/shells/` activated with 6 files; README quickstart untouched; Frontier remains advanced importable; G1+G4 published snapshot branches NOT touched); forward triggers for G3 + G5 + future validator extraction. Status flipped `scoped → implemented`; blueprint + audit log moved from `docs/blueprints/active/` to `docs/blueprints/archive/`; archive inventory updated with G2 row. Snapshot publish to `origin/v0.1-l-g2-fact-overlay-proofframe-recheck-2026-05-08` is intentionally separate from this close-out commit per user request — keeps GitHub branch strategy decoupled from documentation archival. |
