# Task Blueprint Audit: Primary-Anchor Domain Read

- Blueprint: [2026-05-10_primary-anchor-domain-read.md](./2026-05-10_primary-anchor-domain-read.md)
- Parent Blueprint: [2026-05-10_primary-identity-domain-semantics.md](./2026-05-10_primary-identity-domain-semantics.md)

## Event Log

| Date | Stage | Event | Notes |
| --- | --- | --- | --- |
| 2026-05-10 | draft | Spin-off blueprint created from parent §5.5 / §5.6 | Parent judgment recommended separating read-layer concerns from primary-first completion; this seed carries §5.5 (Read layer) and §5.6 (Return type) plus newly seeded §5.A / §5.D / §5.E / §5.F / §5.G open questions. No code, tests, release refs, or memory updates. |
| 2026-05-10 | draft | §5.A LOCKED | Primary-anchor read support ships through relaxed `find(...)` partial identity filters; no new read method, no new DTO, `get(...)` unchanged, `idref_v1` remains full-coordinate. |
| 2026-05-10 | draft | §5.B LOCKED | API surface remains existing `find(...)`; partial identity filters are accepted through existing `**filters`; return type stays `list[EntitySnapshot]`; ordering for partial identity filters has no public sort guarantee; no new method, DTO, or performance guarantee. |
| 2026-05-10 | draft | §5.C LOCKED | No new DTO; primary-anchor reads reuse existing `list[EntitySnapshot]` return shape from `find(...)`; partial-identity results must expose full recovered snapshot identity with `identity_available=True`; empty result remains `[]`; `kernel.sdk.__all__` unchanged. |
| 2026-05-10 | draft | §5.D LOCKED | No lookup-by-domain helper; callers receive `list[EntitySnapshot]` and select coordinates by ordinary Python filtering over `snapshot.identity`; no helper added to `EntitySnapshot` or `fg.read`. |
| 2026-05-10 | draft | §5.E LOCKED | No logical-entity-ref or primary-only ref token; primary-anchor reads are query/filter behavior over existing full-coordinate refs; `idref_v1` and `EntitySnapshot.ref` remain full-coordinate. |
| 2026-05-10 | draft | §5.F LOCKED | Partial-identity filtering implemented in SDK facade over existing application `find` hydration; deliberate scope-bounded exception to application-first runtime authority; no `EntityReadRequest` / application protocol extension; partial results must hydrate full `snapshot.identity` with `identity_available=True`. |
| 2026-05-10 | draft | §5.G LOCKED | No primary-anchor write API; read-side partial identity filtering has no write-side parity in this blueprint; complete-coordinate write requirement from parent remains unchanged. |
| 2026-05-10 | draft | §6 invariants updated | Original 7-bullet seed §6 replaced with 6-group invariants (substrate / SDK read-path / SDK write-path / application protocol / rule authoring / repository safety) reflecting §5.A-§5.G LOCKED decisions; no new behavior introduced. |
| 2026-05-10 | draft | §7 acceptance updated | Acceptance split into 3 groups (closed §5 gates / scope-freeze gates / implementation gates) reflecting §5.A-§5.G LOCKED decisions, including no new API/DTO, SDK-facade-only partial identity filtering, full snapshot identity visibility, and repository safety. |
| 2026-05-10 | draft | §8 implementation plan updated | Replaced seed plan with post-LOCKED 5-phase execution path (pre-scope-freeze + test scaffolding + SDK-facade-only implementation + docs sync + verification/close) reflecting §5.A-§5.G and §7 gates. |
| 2026-05-10 | draft | Pre-scope-freeze scan completed | Scanned release-facing docs/examples for `find(...)` identity-filter wording. Two live SDK docs still teach the old all-or-nothing identity-filter rule: `src/kernel/sdk/docs/01_concepts.en.md` and `src/kernel/sdk/docs/02_readwrite_and_ingest.en.md`; both are explicitly deferred to Phase 3 docs sync. Examples did not contain conflicting `find(...)` identity-filter teaching. |
| 2026-05-10 | scoped | Scope-freeze | Status flipped `draft → scoped`; all §7 scope-freeze gates passed. Implementation may proceed through Phase 1 test scaffolding under the locked §5.A-§5.G decisions and §6 invariants. |
| 2026-05-10 | scoped | Phase 1 test scaffolding | Added focused SDK tests for partial identity `find(...)` behavior, full identity preservation, AND semantics, unknown filter errors, empty and zero-filter behavior, full snapshot identity visibility, unchanged `get(...)`, unchanged application protocol behavior, and no new public SDK names/helpers. Initial red baseline: 5 expected partial-identity errors on the old all-or-nothing identity-filter rejection. |
| 2026-05-10 | scoped | Phase 2 implementation | Implemented SDK-facade-only partial identity filtering in `src/kernel/sdk/facade.py`; full-identity filters retain the get-backed path, partial-identity filters use existing application `find` hydration and SDK identity matching, with full recovered identity exposed on SDK snapshots. No application protocol, authoring, substrate, storage/index, or export change. |
| 2026-05-10 | scoped | Phase 3 docs sync | Updated the two stale SDK docs found by the pre-scope-freeze scan: `src/kernel/sdk/docs/01_concepts.en.md` and `src/kernel/sdk/docs/02_readwrite_and_ingest.en.md`. No `00_user_guide.en.md` change was needed for this behavior. |
| 2026-05-10 | implemented | Phase 4 verification and close | Focused SDK/application suite: 29 OK. Full kernel discovery: 1774 OK / 1 skipped plus the pre-existing Problog cold-import circularity error (1776 total). Ruff focused check and scoped `git diff --check` passed. Status flipped `scoped → implemented`; release refs and `v0.1.0-rc.1` remain untouched. |

## Decision Notes

- 2026-05-10: Spin-off created per parent blueprint judgment recommendation #1. Parent §5.5 / §5.6 questions are out-of-scope for the parent and tracked here instead. Parent §5.5 / §5.6 sections in the parent blueprint should be marked DEFERRED with a pointer to this audit log when the parent's iterative gap pass reaches them.
- 2026-05-10: The seed default treated a primary-anchor read API as a NEW capability per [project_application_first_runtime_authority.md](../../../memory/project_application_first_runtime_authority.md), implying application-layer DTO + pure function design before SDK surface. §5.F later superseded that default with a deliberate, scope-bounded exception: partial-identity filtering is implemented in SDK facade over existing application `find` hydration, with no application protocol extension.
- 2026-05-10: Parent blueprint's §6 invariant (`idref_v1 = all identity fields`) is preserved as input here; if §5.E proves a separate logical-entity-ref token is required, this blueprint must immediately re-scope and reopen the parent invariant.
- 2026-05-10: §5.G primary-anchor write API is recorded as DEFAULT NO and OUT-OF-SCOPE; recorded only to prevent quiet drift, not as an active design question.
