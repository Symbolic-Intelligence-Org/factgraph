# Reconstructed Archive Audit: <title>

- Blueprint: [<same-basename>.md](./<same-basename>.md)
- Archive Mode: reconstructed
- Historical Source:
  - [docs/blueprint_history/<source>.md](../../blueprint_history/<source>.md)

## Historical Notes (Reconstructed)

只记录能够从原始蓝图或当前模块 docs 中提炼出来的历史里程碑。  
如果具体日期未知，可以使用 `unknown` 或描述性时间段，不要伪造精确时间。

| Approx Date | Historical Note | Evidence |
| --- | --- | --- |
| unknown | Source blueprint existed before the modern workflow. | Original historical blueprint |

## Migration Events (Verified)

这里只记录当前这次回填迁移中真实发生的事件。

| Date | Event | Notes |
| --- | --- | --- |
| YYYY-MM-DD | reconstructed archive created | Backfilled from `docs/blueprint_history/` using the legacy reconstructed archive template. |

## Decision Notes

- 为什么值得回填成 archive 条目：
- 哪些内容被保留为 rationale：
- 哪些内容因为不可验证而没有升级为 acceptance：
