# Monorepo development dependencies.
#
# These dependencies support private companion surfaces under src/agent,
# src/service, and src/domains for local development and CI. They are
# intentionally not exposed as factpy-kernel PyPI extras; the v0.1
# publishable wheel is kernel-only.

-e .[dev]

# service
fastapi>=0.110,<1
uvicorn>=0.29,<1
httpx>=0.27,<1
python-multipart>=0.0.5

# agent document parsing / extraction
# PyMuPDF and PyMuPDF4LLM are AGPL/commercial dual-licensed. They are
# private monorepo development dependencies, not kernel PyPI metadata.
pymupdf>=1.24
pymupdf4llm>=0.0.17
python-docx>=1.1
instructor>=1.6
litellm>=1.50
mistralai>=1,<2

# observability
langfuse>=2.0
