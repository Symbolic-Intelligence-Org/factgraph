# PyReason Adapter Module

本目录预留给 `src/factpy_kernel/adapters/pyreason` 的模块级实现说明。

当前阶段：

- 仅实现 adapter-local provenance spike
- 不进入 `core/`、`service/`、`audit/` 正式契约
- 当前实现真相仍以 `src/factpy_kernel/adapters/docs/03_pyreason_adapter.md` 为目标文档；本 README 只作为模块入口

