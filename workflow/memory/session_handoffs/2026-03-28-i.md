# Session Handoff: 2026-03-28-i

Supersedes `2026-03-28-h.md`. **Closeout handoff**: EvidenceGraph unified explain blueprint is now implemented and archived.

## 1. Current Stage

This session chain completed the full EvidenceGraph line:

| Step | Result | Commit |
|------|--------|--------|
| 1 | Data model + invariants | `227d7be` |
| 2 | PyReason -> EvidenceGraph (`timeline`) | `be565fb` |
| 3 | ProbLog -> EvidenceGraph (`tree`) | `3dbf35c` |
| 4 | Unified HTML renderer (`tree` + `timeline`) | `3db9fb5` |
| 5 | Souffle -> EvidenceGraph (`tree`) | `bf38776` |
| 6 | Static UI wiring (`candidate evidence page`) | `950ced7` |

What now exists:

- `audit/evidence_graph.py`
  - frozen `EvidenceNode` / `EvidenceEdge` / `EvidenceGraph`
  - shared tree / timeline HTML renderer
- adapter converters
  - `pyreason_trace_to_evidence_graph(...)`
  - `problog_trace_to_evidence_graph(...)`
  - `souffle_proof_tree_to_evidence_graph(...)`
- static candidate evidence page
  - keeps the existing Souffle provenance tree block
  - can append a unified EvidenceGraph section when `evidence_graph` is available

## 2. Important Boundary

The blueprint is implemented, but the final static delivery boundary is narrower than the broadest possible reading:

- `Souffle`
  - audit/static can already show unified EvidenceGraph by reconstructing from `provenance_tree`
- `PyReason` / `ProbLog`
  - both have real `EvidenceGraph` converters and renderer compatibility
  - but audit package / static site do **not** yet have a direct reader lane for their provenance envelopes
  - they show up on the candidate page only when a caller passes `evidence_graph` directly

This is intentional and recorded in the archived blueprint outcome:

- no `EvidenceGraph` durable serialization in v1
- no new audit reader contract for provenance envelopes in v1
- Souffle legacy tree viewer remains untouched

## 3. Git State

- Branch: `master`
- HEAD: `950ced7`
- Working tree: clean except untracked `.claude/launch.json`
- Tests: **559**, all green

## 4. Blueprint State

- Archived:
  - `docs/blueprints/archive/2026-03-28_evidence-graph-unified-explain.md`
  - `docs/blueprints/archive/2026-03-28_evidence-graph-unified-explain.audit.md`

No active work remains under this blueprint.

## 5. If a Future Agent Reopens This Line

Use a new blueprint. The most obvious next slice is:

- audit package / static delivery for non-Souffle engine EvidenceGraphs
  - either serialize `EvidenceGraph` into the package
  - or teach `audit/` to read provenance envelopes and convert them on demand

Do not reopen the archived blueprint just to extend this scope.

## 6. Reading List

1. This handoff
2. `docs/blueprints/archive/2026-03-28_evidence-graph-unified-explain.md`
3. `src/factpy_kernel/audit/evidence_graph.py`
4. `src/factpy_kernel/audit/static_ui.py`
5. `src/factpy_kernel/audit/docs/02_evidence_graph.md`
6. `src/factpy_kernel/adapters/pyreason/provenance.py`
7. `src/factpy_kernel/adapters/problog/provenance.py`
8. `src/factpy_kernel/adapters/souffle/provenance.py`
