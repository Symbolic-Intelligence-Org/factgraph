# Session Handoff: 2026-03-29

Supersedes `2026-03-28-i.md`. **Design branch fully closed.** All backlog items implemented or explicitly deferred. Documentation audit clean. 580 tests green.

## 1. Current Stage

This session completed the remaining design branch work:
- ProbLogRuleExt + body_confidences migration (`ae2a5e8`)
- ProbLog probability canonical read lane (`19f4cde`)
- ProbLog probability first-class write lane (`0e2b0cb`, `dae7f0b`)
- 6-agent documentation audit + 11 stale items fixed (`8b05a99`)
- Unified framework demo notebook (`25fb207` → `09f2585`)

**Current position**: Design branch is fully closed. No active implementation blueprints. No backlog items remaining. Active blueprints are all decision/reference documents only.

## 2. Capability Baseline

### Core Framework (unchanged from prior)
- Certainty v1 (FROZEN — 16 contracts)
- Souffle Provenance (complete)
- Provenance Coverage + Bridge (complete)

### Annotation Store (complete)
- Four-layer architecture: Claim / Annotation / Legacy / Provenance
- Dual-write: whitelisted meta → annotation_rows + meta_rows
- `confidence` = derived summary; `probability` = semantic observed (NEW)
- Revocation dual-writes annotations

### Multi-Engine Execution Surface (complete)
- `Store.evaluate(mode="pyreason/problog/souffle")` unified dispatch
- `Rule.engine_ext` + `Derivation.engine_ext` (EngineExtBase)
- `engine_options` call-time config (timesteps / timeout)

### PyReason Adapter (complete)
- Batch API, bounded materialization, head_bound, body_predicate_bounds
- Node→add_fact, edge→graph attr materialization
- Bounded-seed warning, real-engine E2E verified

### ProbLog Adapter (complete)
- `ProbLogRuleExt(branch_probabilities=...)` replaces body_confidences
- `probability` as first-class write key → `shared/semantic/probability` (NEW)
- Export reads: problog/semantic → shared/semantic → meta.confidence → 1.0
- Real ProbLog CLI E2E verified

### Engine Provenance Surface (complete)
- ProvenanceEnvelope carrier for PyReason + ProbLog
- `pyreason_provenance_v1` / `problog_provenance_v1` support kinds
- Runtime flat explain dispatches engine provenance

### EvidenceGraph (complete)
- Unified data model: EvidenceNode / EvidenceEdge / EvidenceGraph
- 3 converters: Souffle / PyReason / ProbLog → EvidenceGraph
- 2 renderers: tree + CSS grid timeline
- Audit package: evidence_graphs.jsonl export + reader + query
- Static UI: candidate evidence page renders durable EvidenceGraph

## 3. Git State

- Branch: `master`
- HEAD: `dae7f0b`
- Working tree: **clean** (only `.claude/launch.json` untracked)
- **580 tests, all green**

## 4. Blueprint Status

### Active (decision/reference only — no implementation pending)

| Blueprint | Notes |
|-----------|-------|
| `2026-03-22_architectural-decisions-v2.md` | ADR reference |
| `2026-03-22_ecss-domain-validation-and-souffle-provenance-poc.md` | Original mother (implemented) |
| `2026-03-26_assertion-annotation-store-decision.md` | 6 annotation decisions |
| `2026-03-27_multi-engine-execution-surface-decision.md` | 9 decisions (D1-D9) |
| `2026-03-27_multi-engine-execution-surface-design-rationale.md` | Design rationale |
| `2026-03-27_value-carrying-semantics-v1-decision.md` | D-VC1~5 |
| `2026-03-27_engine-options-runtime-dispatch-decision.md` | engine_options |

**No active implementation blueprints.**

### Key Archived (this session)

- `problog-rule-ext-branch-probabilities` — ProbLogRuleExt + body_confidences migration
- `problog-fact-probability-canonical-lane` — Export reads canonical annotation first
- `problog-probability-write-lane` — probability as first-class write key
- `design-branch-closeout` — Audit delivery + PyReasonRuleDef removal

### Full Archive Inventory

`docs/blueprints/archive/README.md` — 157 entries

## 5. Test Baseline

- **580 tests, all green**
- `PYTHONPATH=src python -m unittest discover -s src/factpy_kernel/tests -p "test_*.py"`

## 6. Key Files (this session)

| File | Role |
|------|------|
| `adapters/problog/rule_ext.py` | ProbLogRuleExt + resolve helper (NEW) |
| `adapters/problog/engine_eval.py` | Dedicated evaluator + timeout |
| `adapters/problog/problog_export.py` | probability read priority chain |
| `adapters/problog/provenance.py` | ProbLogTraceV0 + EvidenceGraph converter (NEW) |
| `core/evidence/write_protocol.py` | probability as first-class key |
| `core/store/_support.py` | ProvenanceEnvelope carrier |
| `audit/evidence_graph.py` | EvidenceGraph DTO + converters + renderer |
| `examples/unified_framework_demo.ipynb` | Comprehensive demo (NEW) |

## 7. Frozen Contracts

Prior contracts 1-57 remain frozen. This session adds:

58. **ProbLogRuleExt.branch_probabilities** — typed OR-branch weighting, replaces body_confidences
59. **body_confidences removed from shared evaluate** — only SDK/runtime bridge remains
60. **probability as first-class write key** — `shared/semantic/probability` annotation (origin=observed)
61. **probability → confidence auto-derivation** — only if confidence not explicitly set
62. **ProbLog export read priority** — problog/semantic → shared/semantic → meta.confidence → 1.0

## 8. Known Gaps

### EvidenceGraph summary/NL (deferred)
- Carrier and tree/timeline renderers exist
- Text summary and natural language explanation not implemented
- Should be done uniformly for all engines when needed

### ProbLog provenance summary/NL (deferred)
- ProbLogTraceV0 carrier exists
- No text-based explanation pipeline

### Query.engine_ext (deferred)
- No real consumer yet

## 9. Collaboration Protocol

1. Blueprint-driven workflow
2. Hook restriction: non-.md edits in `src/factpy_kernel/` blocked; provide code for user
3. Scope discipline: each blueprint does ONE thing
4. Contract-first: freeze decisions before implementation
5. Docs sync at implementation close
6. probability/bound/active_from are fact semantic properties, not engine parameters
7. engine_ext = definition-time; engine_options = call-time
8. EvidenceGraph is shared explain IR; tree is one rendering mode

## 10. What the Next Agent Should Do

### First action
```bash
PYTHONPATH=src python -m unittest discover -s src/factpy_kernel/tests -p "test_*.py"
```
Expected: **580 tests**, OK.

### Recommended next direction
This design branch is fully closed. The next step should be a **new mother blueprint** based on validated capabilities, not continuation of this branch. Possible directions:
- EvidenceGraph summary/NL (unified text explain)
- Domain-specific demo expansion (ECSS/DORA with real Souffle + PyReason)
- Production hardening (error surface, logging, metrics)

### What NOT to do
- Do not reopen any of 62 frozen contracts
- Do not put engine-specific params in shared meta_rows
- Do not serialize engine_ext into to_authoring_payload()
- Do not use meta.confidence for ProbLog probability input (use meta.probability)
- Do not manually construct EvidenceGraph in user-facing code

## 11. Reading List

1. This handoff
2. `src/factpy_kernel/adapters/docs/02_problog_adapter.md`
3. `src/factpy_kernel/adapters/docs/03_pyreason_adapter.md`
4. `src/factpy_kernel/core/docs/01_architecture.md`
5. `src/factpy_kernel/audit/docs/02_evidence_graph.md`
6. `examples/unified_framework_demo.ipynb`
7. `docs/blueprints/archive/README.md`
