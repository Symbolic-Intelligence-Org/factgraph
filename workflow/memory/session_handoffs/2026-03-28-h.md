# Session Handoff: 2026-03-28-h

Supersedes `2026-03-28-g.md`. **Phase boundary handoff**: Engine provenance surface landed for both PyReason and ProbLog. EvidenceGraph unified explain representation Step 1 complete. Next: converters (Step 2-4) + renderer (Step 5).

## 1. Current Stage

This session completed 4 major capability lines after the Multi-Engine Semantic Delivery mother blueprint was archived:

**A. Rule.engine_ext alignment** (`c06390e`)
- Shared `Rule` now carries `engine_ext: EngineExtBase | None`
- `PyReasonRuleDef` deprecated as compatibility shim
- All demos migrated to `Rule(engine_ext=PyReasonRuleExt(...))`

**B. PyReason root cause diagnosis + fixes** (`a3174ce`, `8ad1f96`, `6fd7093`, `0724f53`)
- Root cause: default body clause threshold `[1,1]` rejects bounded seeds
- Fix: `body_predicate_bounds` + `head_bound` on `PyReasonRuleExt`
- Root cause: edge labels must be graph attributes, node labels via `add_fact()`
- Demos rewritten as boolean propagation with side-channel uncertainty

**C. ProbLog eval surface** (`cde477f`)
- `evaluate_problog()` extracted to dedicated `engine_eval.py`
- `engine_options={"timeout": N}` threads to ProbLog CLI
- Tab-format output parser fix
- Real ProbLog CLI E2E verified

**D. Engine provenance surface** (`ef717a6`)
- `ProvenanceEnvelope` carrier in `core/store/_support.py`
- Store provenance registry + `explain_ref()` in `runtime.py`
- PyReason: `pyreason_provenance_v1` support kind, atom_trace forced
- ProbLog: `problog_provenance_v1` support kind, `--trace` CLI parser
- Runtime flat explain dispatches engine provenance
- Tree/NL/HTML explicitly unsupported (not faked)

**E. EvidenceGraph Step 1** (`227d7be`)
- `EvidenceNode` / `EvidenceEdge` / `EvidenceGraph` frozen dataclasses
- Layout hints: `tree` | `timeline`
- Invariants: unique IDs, root exists, edge endpoints valid
- Placed in `audit/evidence_graph.py` (consumer-layer DTO)

**Current position**: EvidenceGraph data model frozen. Next: Step 2-4 (converters for PyReason/ProbLog/Souffle) + Step 5 (unified renderer).

## 2. Key Numbers

- **547 tests**, all green
- **HEAD**: `227d7be`
- Working tree: clean (only `.claude/launch.json` untracked)

## 3. Active Blueprints

| Blueprint | Notes |
|-----------|-------|
| `2026-03-28_evidence-graph-unified-explain.md` | **Scoped, Step 1 done, Steps 2-5 remaining** |
| `2026-03-22_architectural-decisions-v2.md` | ADR reference |
| `2026-03-26_assertion-annotation-store-decision.md` | 6 annotation decisions |
| `2026-03-27_multi-engine-execution-surface-decision.md` | 9 decisions (D1-D9) |
| `2026-03-27_value-carrying-semantics-v1-decision.md` | D-VC1~5 |
| `2026-03-27_engine-options-runtime-dispatch-decision.md` | engine_options design |

## 4. What the Next Agent Should Do

### First action
```bash
PYTHONPATH=src python -m unittest discover -s src/factpy_kernel/tests -p "test_*.py"
```
Expected: **547 tests**, OK.

### Recommended next direction: EvidenceGraph Steps 2-5

Read `docs/blueprints/active/2026-03-28_evidence-graph-unified-explain.md`

- **Step 2**: `pyreason_trace_to_evidence_graph()` — convert `PyReasonTraceV0` to `EvidenceGraph(layout_hint="timeline")`
- **Step 3**: `problog_trace_to_evidence_graph()` — convert `ProbLogTraceV0` to `EvidenceGraph(layout_hint="tree")`
- **Step 4**: `souffle_proof_tree_to_evidence_graph()` — wrap existing `SouffleProofTreeV0` (DO NOT refactor Souffle pipeline)
- **Step 5**: Unified renderer — tree view + timeline view, dispatched by `layout_hint`

### What NOT to do
- Do not refactor existing Souffle tree viewer pipeline
- Do not serialize EvidenceGraph to audit package yet (format not frozen)
- Do not add `dag` layout_hint (no renderer/consumer)
- Do not add `rule_fire` node_kind (not compatible with current node semantics)
- Do not reopen any frozen contracts

## 5. Reading List

1. This handoff
2. `docs/blueprints/active/2026-03-28_evidence-graph-unified-explain.md` — active blueprint
3. `src/factpy_kernel/audit/evidence_graph.py` — EvidenceGraph data model
4. `src/factpy_kernel/audit/docs/02_evidence_graph.md` — module docs
5. `src/factpy_kernel/adapters/pyreason/provenance.py` — PyReasonTraceV0 (Step 2 input)
6. `src/factpy_kernel/adapters/problog/provenance.py` — ProbLogTraceV0 (Step 3 input)
7. `src/factpy_kernel/adapters/docs/03_pyreason_adapter.md` — PyReason capabilities
8. `src/factpy_kernel/adapters/docs/02_problog_adapter.md` — ProbLog capabilities
