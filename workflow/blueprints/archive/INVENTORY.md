# Archived Blueprints Inventory

`workflow/blueprints/archive/` 的导航索引。归档约定 + reconstructed entry 规则见 [`workflow/blueprints/README.md`](../README.md)。

## Inventory

最后更新：2026-05-30 (slice-2-identity-claim-emission archived)

| Date | Blueprint | Status | Summary |
|------|-----------|--------|---------|
| 2026-05-29 | slice-2-identity-claim-emission | implemented | Slice 2 Identity-as-Claim Core implementation cycle per ADR-IC (`2d0866ed` adopted 2026-05-29) + ADR-DOCS §4.2.2 (`bb6a2c90`). Forked from Slice 1 close `9cef674b` on `v0.2.0-blueprint-slice-2-identity-claim-emission-2026-05-29`. Three-layer INV-7c enforcement landed: application source-of-truth `application/retract_guard.py` (NEW module @ `187a2918` Step 2) with `RetractGuardError` + `classify_retract_target` + `check_retract_allowed` using only existing `Ledger.get_claim` API (P1 #1 Q-PR1 lock); SDK shell fail-fast `SDKStore.retract` wrap @ `12475859` Step 3; application ingest `_apply_retract` wrap @ `16aeff69` Step 4 with `index: SchemaIndex` pass-through (P1 #2 fix); application entity_write `_apply_op` retract branch wrap @ `a4853a0a` Step 5 reusing existing `index` parameter. Layer 2 Identity field write reject (`IdentityEditor.set/add/retract` + `plan_write_command:is_identity_field`) updated to ADR-IC §4.1 adopted wording @ `c2d659c1` Step 6 with `code="INV_7C_IDENTITY_PROTECTED"` shared across Layer 2 + Layer 3 (single caller branching source). `SchemaIndex.identity_pred_ids` / `exists_pred_ids` frozenset fields + `protected_anchor_pred_ids` union property landed @ `73993ebd` Step 1 in `application/schema_runtime.py` (SF9 two independent disjoint frozensets per ADR-IC §4.3.1; public naming per P2 #1 amend). `:exists` existence-claim transitional guard explicitly NON INV-7c per ADR-IC §4.4.2 with `code="EXISTENCE_CLAIM_TRANSITIONAL_GUARD"` and lifecycle decoupled from INV-7c. Shadow store `_identity_values_by_e_ref` documented as LEGACY / INTERNAL COMPATIBILITY only @ `d46b9fe7` Step 7 with explicit 2-branch semantics (unseen → fail-fast `UNRESOLVABLE_E_REF`; seen → lazy materialization through `_materialization_ops`) and forward direction to ADR-IC §4.2.4 `fg.entities.create` eager emission + shadow store removal Slice 3a carry-forward. Emission contract atomic + dedup tests @ `ab168063` Step 8 (NEW `tests/test_emission_contract.py` 11 tests) verified across `fg.ref+fg.set` / `SDKBatchTx.commit` / `EntityEditor.commit` three shipped paths; N Identity + 1 `:exists` + 1 Field atomic emission; dedup via `_materialization_ops:materialized_refs` + `entity_visible` checks; pred ID conventions confirmed via probe (snake_case `<owner>:<field>` for Identity/Field + Capitalized `<EntityType>:exists`). Load-bearing docs sync @ `200987f1` Step 9 + amend @ `ebb03348`: `src/factgraph/sdk/docs/04_api_surface.en.md` §1.5 error codes table +3 rows + new §7 Identity Claim Emission and Reject Semantics (~130 lines, 4 sub-sections covering emission contract + INV-7c reject behavior table + existence-claim transitional guard + shadow store legacy positioning) with old §7 renamed §8; `workflow/design/design-points/active/identity-mechanism-redesign.zh.md` §5.2 ADR-IC adopted wording sync with 4-row sub-decision mapping + three-layer enforcement landed status with commit SHAs + Q-PR1 carve-out preservation + §13 added §13.4 Slice 1 + Slice 2 landed status (Slice 1 Form I @ `9cef674b` + Slice 2 7-row commit map + carry-forward list + Q-PR1 boundary). Step 9 amend (P1 + P2x2) corrected §5.2 Q3 row to split shipped (SchemaIndex frozensets) vs carry-forward (schema-evolution hook → ADR-API Q14), §13.4.1 Slice 1 branch direction (dropped misleading `→ master`; sacred master untouched), and §13.4.2 carry-forward list (removed `_DataMember.pattern` which already shipped in Slice 1). Q-PR1 carve-out preserved through all 21 commits: zero diff in `core/evidence/write_protocol.py` + `core/store/ledger.py` + `core/store/_builders.py` + `adapters/pyreason/` + `core/derivation/accept.py` (SF5 + SF11 internal-rollback intentionally unguarded per N11) against Slice 1 close `9cef674b..HEAD` final grep returns empty. SF1-SF12 all locked through implementation. Verification: cumulative Slice 2 suite 59 passed in 0.31s across 7 NEW test files (6 cache + 11 retract guard + 7 SDK shell + 8 ingest + 8 entity_write + 8 wording + 11 emission); `compileall` clean; sacred master `562c74195df43e933bed92a3ff25de94dd8ce666` unchanged through all 21 commits; dirty baseline (4 M + 1 D + 2 untracked, unrelated to identity-as-claim) preserved; branch lineage ancestors verified (ADR-IC `2d0866ed` + ADR-DOCS `bb6a2c90` + synthesis `d0036e1f` + Slice 1 close `9cef674b`). Carry-forward dependencies: ADR-API Q14 schema-evolution hook (`fg.schema.register/extend/apply` wire cache hook); ADR-API Q10 `fg.entities.create/delete/exists` namespace migration (Slice 3a) — Slice 2 error wording references future API as user migration guidance only without implementation dependency (P1 #3 amend); Step 2+ `:exists` removal per ADR-IC §4.4.4 forward-pointer (existence-claim transitional guard retires in lockstep without affecting INV-7c); Step 2+ shadow store removal per ADR-IC §4.2.4 eager-emission evolution. Commit lineage: draft `f9373eea`, amend `c7b2a041`, amend round 2 `16bd1439`, scoped `a831fd00`, Step 1 `73993ebd`, Step 2 `187a2918`, Step 3 `12475859`, Step 4 `16aeff69`, Step 5 `a4853a0a`, Step 6 `c2d659c1`, Step 6 backfill `975b8a9a`, Step 7 `d46b9fe7`, Step 7 backfill `6d68e48e`, Step 8 `ab168063`, Step 8 backfill `05c9bb55`, Step 9 `200987f1`, Step 9 backfill `44d153af`, Step 9 amend `ebb03348`, Step 9 amend backfill `5ee6cbb1`, Step 10 close `d17eb262`, Step 10 backfill `0f4afb1c`. |
| 2026-05-28 | adapter-module-docs-alignment | implemented | Docs-only adapter module docs alignment after T8-D round 4/5 named `src/factgraph/adapters/docs/02_problog_adapter.md` and `03_pyreason_adapter.md` as future work. Updated the ProbLog adapter docs for T10-1 C76 `uncertainty_projection`, explicit default reject, point-projection policies, raw `raw_kind` / `bound` projection, and T8-C-1 row provenance graphs with `PROBLOG_PROVENANCE_KIND`, `derives` edges, and namespaced `engine_meta["problog"]`. Updated the PyReason adapter docs for T10-2-A C78 `iteration_count`, T10-2-B C74 `derived_bound` / `atom_bounds`, T10-3-A `fact_boundaries`, T10-3-B `time_binned`, strict `bin_size` rules, and the T8-D round 5 boundary that adapter-level `pyreason_trace_to_evidence_graph(...)` is advanced/module-level while rich row-result temporal evidence remains future Form 2 / T8-C-2 work. Quickstarts, SDK guide, audit docs, runtime, tests, governance, dirty baseline, and Form 2 schema design were untouched. Verification: focused docs-only suite 140 OK; full discover `2038 tests / 72 failures / 231 errors`, matching baseline with zero composition shift; `git diff --check` clean; dirty baseline preserved as 4 M + 1 D + 6 U; sacred master unchanged. Draft `d5ffea35`, scoped `ffe12a81`, ProbLog docs `dd8a5a2f`, PyReason docs `601e6ec0`, closure `4bf91594`. |
| 2026-05-28 | t8-d-round5-pyreason-evidence-deferred-state-docs | implemented | Docs-only T8-D round 5 user-facing evidence clarification after T8-D round 4 canonical semantics docs. Updated only `docs/official/kernel/quickstart/evidence.md` with a narrow PyReason deferred-state paragraph: PyReason inference, bounds, and temporal materialization are shipped through the evaluation path, but rich row-level temporal evidence remains deferred to a future Form 2 design cycle. Current PyReason row evidence is documented as the safe single-`NODE_CONCLUSION` row anchor fallback, not a timeline and not proof of timestep-by-timestep state changes. The adapter-level `factgraph.adapters.pyreason.provenance.pyreason_trace_to_evidence_graph(...)` helper is named only as an advanced importable escape hatch and not the main quickstart path. SDK guide edit was explicitly declined because it already points PyReason row-level graph readers to the evidence quickstart as future evidence work. T8-D round 3 ProbLog row-provenance wording, T8-D round 1 `Inference` / `Branch` stability wording, T8-D round 4 canonical semantics docs, runtime, tests, adapter docs, audit docs, governance, dirty baseline, and Form 2 schema design were untouched. Verification: focused docs-only suite 140 OK; full discover `2038 tests / 72 failures / 231 errors`, matching baseline with zero composition shift; `git diff --check` clean; dirty baseline preserved as 4 M + 1 D + 6 U; sacred master unchanged. Draft/scoped `d421a61d` / `581e4759`, implementation `61f8b99d`, closure `43212b4b`. |
| 2026-05-28 | t8-d-round4-pyreason-problog-canonical-user-docs | implemented | Docs-only T8-D round 4 user-facing alignment after T10 semantics completion. Updated `docs/official/kernel/quickstart/semantics.md` to teach the five shipped semantics cycles: ProbLog C76 `uncertainty_projection` with explicit default reject and point-projection policies; PyReason C78 `iteration_count` with wrapper default `1` vs no-profile engine default; C74 `derived_bound` / `atom_bounds` with application atom ids and asymmetric compatibility; C77 `fact_boundaries` canonical alias; and C77 `time_binned` with strict `bin_size` whitelist, exact universe divisibility, timezone-aware datetime rules, and conflict behavior. Updated `assertions.md` with a concise ProbLog raw-uncertainty projection note. Cleaned `rules-and-inferences.md` so `Rule` / `RuleExpr` / `fg.read.match(...)` / `fg.eval.evaluate(...)` lead, `Inference` is a v0.2 compatibility surface, and `Query` is no longer taught as a user-facing projection path. Updated the SDK user guide with concise canonical semantics summaries and quickstart pointers. Evidence quickstart, audit docs, adapter docs, runtime, tests, governance, and dirty baseline were untouched. Verification: focused docs-only suite 140 OK; full discover `2038 tests / 72 failures / 231 errors`, matching baseline with zero composition shift; `git diff --check` clean; dirty baseline preserved as 4 M + 1 D + 6 U; sacred master unchanged. Draft `fa63e851`, scoped `abce9fb9`, PyReason docs `be12f426`, ProbLog docs `d24bfe00`, rules cleanup `1545964a`, SDK guide `b4824443`, closure `7f2e42ba`. |
| 2026-05-28 | t10-3-b-time-binned-migration | implemented | Runtime implementation cycle for T10-3-B / C77 PyReason `time_binned` temporal projection after T10-3-A shipped at `e7bab90f`. Shipped `SemanticsProfile.temporal_projection` mode `time_binned` with strict `bin_size` validation: ISO subset `P<n>D` / `PT<n>H` / `PT<n>M` plus exact short forms `1d`, `1h`, `15m`, `1m`; ambiguous/prose/month/year/seconds/combined/decimal/zero forms reject. Added `_materialize_time_binned(...)` with explicit `universe`, exact universe/bin-size divisibility, microsecond integer arithmetic, ISO date-as-midnight-UTC parsing, timezone-aware datetime parsing, floor-start / ceil-end fact bin mapping, open-ended `valid_to` as `active_to=None`, and out-of-universe rejection. The PyReason adapter consumes `time_binned` through a distinct temporal branch, reusing T10-3-A dynamic carrier wording and existing iteration/engine-options conflict helpers. Existing `fact_boundaries`, `valid_time_boundaries`, `fixed_timesteps`, T10-2-A C78 `iteration_count`, and T10-2-B C74 rule params remain unchanged. T10 canonical PyReason semantics C74/C77/C78 are now complete; T8-C-2 PyReason evidence remains gated on D11/Form 2. Verification: focused PyReason suite 103 OK; full discover `2038 tests / 72 failures / 231 errors` versus T10-3-A baseline `2029 / 72 / 231`, so +9 tests and no failure/error regression; ruff clean; `git diff --check` clean; dirty baseline preserved as 4 M + 1 D + 6 U; sacred master unchanged. Draft `c07510ea`, scoped `8a723f44`, profile/parser `8f80ad7c`, materializer `f6ab296e`, adapter `e35abe66`, tests `c70574f6`, closure `2ae5412b`. |
| 2026-05-28 | t10-3-a-fact-boundaries-migration | implemented | Runtime implementation cycle for T10-3-A / C77 PyReason canonical `fact_boundaries` alias after T10-3 inventory archived at `da896f0c`. Shipped `fact_boundaries` as an input-spelling-preserving canonical alias to the existing valid-time-boundary substrate: `SemanticsProfile.temporal_projection` accepts mode `fact_boundaries`, preserves normalized mode spelling, and reuses `_normalize_valid_time_boundaries(...)`; the PyReason adapter consumes both `valid_time_boundaries` and `fact_boundaries` through `_materialize_valid_time_boundaries(...)` with dynamic conflict carrier strings. Legacy `valid_time_boundaries`, `fixed_timesteps`, T10-2-A C78 `iteration_count`, and T10-2-B C74 canonical rule params remain unchanged. `time_binned` remains T10-3-B. Verification: focused PyReason suite 94 OK; full discover `2029 tests / 72 failures / 231 errors` versus T10-2-B baseline `2025 / 72 / 231`, so +4 tests and no failure/error regression; ruff clean; `git diff --check` clean; dirty baseline preserved as 4 M + 1 D + 6 U; sacred master unchanged. Draft `30240f16`, scoped `30c253a9`, profile `c5ea4a4b`, adapter `3d6fc258`, tests `03f94aba`, closure `b28957d0`. |
| 2026-05-28 | t10-3-pyreason-temporal-migration-inventory | implemented | Design-only T10-3 pre-implementation inventory for PyReason C77 temporal migration after T10-2-A C78 shipped at `65cc79a3` and T10-2-B C74 shipped at `92fd6013`. Confirmed C77 is partial / legacy: SDK shell and lowering pass through `temporal_projection`; `SemanticsProfile` and the PyReason adapter support `none`, `fixed_timesteps`, and legacy `valid_time_boundaries`; canonical `fact_boundaries` and `time_binned` are missing. Selected `fact_boundaries` as the first implementation slice, as a canonical alias to the existing valid-time-boundary substrate while preserving legacy `valid_time_boundaries`. Classified `time_binned` as a true new mode requiring strict `bin_size` validation and binned materialization, not a spelling alias. Preserved `fixed_timesteps` as compatibility alias when canonical `iteration_count` is absent and kept T10-2-A's explicit iteration/temporal conflict behavior. Recommended split: T10-3-A `fact_boundaries` alias/compatibility, then T10-3-B `time_binned`. T8-C-2 PyReason evidence remains gated after T10-3-A; after T10-3-B, C74/C77/C78 semantics gates are complete but D11/Form 2 remains. Four untracked active design-point files were grep-classified as adjacent, not blocking. Verification: focused PyReason no-op suite 90 OK; `git diff --check` clean; dirty baseline preserved as 4 M + 1 D + 6 U; sacred master unchanged. Draft `bd24d2fb`, scoped `bfb28b83`, closure `cb673459`. |
| 2026-05-28 | t10-2-b-pyreason-canonical-rule-params | implemented | Runtime implementation cycle for T10-2-B / C74 PyReason canonical rule params after T10-2-A C78 shipped at `65cc79a3`. Shipped public `PyReasonSemantics.derived_bound` and `atom_bounds` while preserving legacy `head_bound` / `branch_bounds`; SDK lowering now converts canonical application atom ids `<rule_id>:atom_<index>` into existing PyReason `rule_projection["pyreason"]` entries such as `body_atom:0:<index>`. The existing PyReason rule-extension adapter path already consumed the lowered `head:0`, `branch:{index}`, and `body_atom:{branch}:{atom}` targets, so no new adapter carrier was required. `derived_bound` + `head_bound` now rejects explicitly; `atom_bounds` + `branch_bounds` coexists because they target body-atom thresholds vs branch head intervals. T8-B witness keys (`b<n>.a<n>:pred_id`) remain unused and physically isolated. Legacy Inference / non-application-Rule contexts reject canonical `atom_bounds` rather than guessing branch structure. Verification: focused PyReason suite 90 OK; full discover `2025 tests / 72 failures / 231 errors` versus T10-2-A baseline `2019 / 72 / 231`, so +6 tests and no failure/error regression; ruff clean; `git diff --check` clean; dirty baseline preserved as 4 M + 1 D + 6 U; sacred master unchanged. Draft `109c4c6f`, scoped `87429f04`, SDK shell `e7787478`, lowering/conversion `e5ae7ad1`, tests `e158bf7b`, closure `96b4b254`. |
| 2026-05-28 | t10-2-a-pyreason-iteration-count-migration | implemented | Runtime implementation cycle for T10-2-A / C78 PyReason canonical `iteration_count`. Shipped public `PyReasonSemantics.iteration_count: int = 1`, optional top-level `SemanticsProfile.iteration_count`, SDK preview/runtime lowering with the scoped omission rule for implicit default `1` plus legacy temporal timesteps, and adapter consumption into PyReason run `timesteps`. Legacy `temporal_projection.fixed_timesteps` remains an alias/fallback when canonical C78 is absent; explicit canonical `iteration_count` now rejects with both `fixed_timesteps` and `valid_time_boundaries`. No C74 atom-bound conversion, C77 temporal rename, evidence/user docs, audit docs, governance, dirty-baseline, or sacred-master changes. Verification: focused PyReason suite 84 OK; full discover `2019 tests / 72 failures / 231 errors` versus baseline `2013 / 72 / 231`, so +6 tests and no failure/error regression; ruff clean; `git diff --check` clean. Draft `0b76ec4f`, scoped `3914be69`, SDK shell `8953f3ec`, profile/lowering `c5cca89b`, adapter `1742bba2`, tests `f8d90307`, closure `3989e843`. |
| 2026-05-28 | t10-2-pyreason-canonical-migration-inventory | implemented | Design-only T10-2 pre-implementation inventory for PyReason C74 + C78 canonical migration. Confirmed T10 inventory's C74/C78 classification: C74 remains partial/legacy (`timestep_delay` shipped end-to-end; canonical `derived_bound` is only legacy `head_bound`; canonical full-atom-id `atom_bounds` is missing), and C78 canonical `iteration_count` is missing while legacy `temporal_projection.fixed_timesteps` currently feeds PyReason engine timesteps. Source-backed three distinct atom-id conventions: application full atom ids (`<rule_id>:atom_<index>`), PyReason positional profile targets (`head:0`, `branch:{index}`, `body_atom:{branch}:{atom}`, `rule`), and T8-B evidence witness keys (`b<n>.a<n>:pred_id`); future C74 needs a conversion layer and must not reuse witness keys. Determined `fixed_timesteps` is decouplable: T10-2-A should introduce canonical C78 `iteration_count` first and preserve/alias legacy `fixed_timesteps`, then T10-2-B should add canonical C74 `derived_bound` / `atom_bounds` conversion. T8-C-2 PyReason evidence remains gated after T10-2 by T10-3 C77 and D11/Form 2 evidence design. Four untracked active design-point files were grep-classified as adjacent but not blocking; dirty baseline preserved as 4 M + 1 D + 6 U. Verification: focused PyReason no-op suite 78 OK, `git diff --check` clean, sacred master unchanged. Draft `138a8461`, baseline amend `3efff8b9`, scoped `f9737cd4`, closure `05f7ee63`. |
| 2026-05-28 | memory-current-compaction | implemented | S-class repo-local memory housekeeping cycle after T8-D round 3 pushed at `c23ce097`. Edited exactly `workflow/memory/current.md`; no runtime, tests, governance, sibling workflow files, Claude auto-memory `MEMORY.md`, archive history files beyond this inventory update, dirty-baseline files, or sacred-master refs changed. Source-backed Step 4.6 classified `current.md` as operational handoff, not the historical ledger: detailed cycle history remains owned by this archive inventory, `CHANGELOG.md`, and archived blueprint pairs. Implementation `eb7f5edb` compacted `current.md` from 132 lines to the 80-line hard target, repaired stale local/pending-push anchors, refreshed published branch head from `cde072fa` to `c23ce097`, preserved the complete `4 M + 1 D + 5 U` dirty-baseline file list, replaced long milestone tables with grouped bullets, and kept current next-work candidates visible. Verification: `wc -l workflow/memory/current.md` = 80, stale local-pending grep returned no matches, `git diff --check` clean, dirty baseline preserved, sacred master unchanged. Draft `0af67764`, scoped `95b2cdbb`, implementation `eb7f5edb`, closure `ef92284c`. |
| 2026-05-28 | t8-d-round3-problog-user-docs | implemented | Docs-only T8-D round 3 user-facing alignment cycle after T8-C-1 ProbLog row-result provenance graphs shipped at `5ffd4850`. Edited exactly the two user-facing evidence docs from T8-D rounds 1/2: `docs/official/kernel/quickstart/evidence.md` and `src/factgraph/sdk/docs/00_user_guide.en.md`; no runtime, tests, audit-module docs, governance, release, service/OpenAPI, Database/view, dirty-baseline, or sacred-master files changed. Quickstart §6 now teaches shipped row-level graph shapes: native/Souffle remain Form 1 with `EDGE_SUPPORTS`, while ProbLog passed rows are row provenance graphs with a separate `NODE_SEED --derives--> NODE_PREMISE --derives--> NODE_CONCLUSION` shape, `EDGE_DERIVES`, exact row/result audit metadata, and high-level `engine_meta["problog"]` placement. ProbLog was removed from the future boundary; PyReason row-level alignment, aggregate envelopes, failed/why-not/counterfactual graphs, match witness output, cross-row dedup, sessions/signatures/ACL, `EDGE_UPDATES`, `dag`, and `rule_fire` remain future. SDK guide stays concise and delegates full DTO details to the quickstart. Verification: five-module focused docs baseline 67 OK, `git diff --check` clean, dirty baseline preserved as 4 M + 1 D + 5 U, sacred master unchanged. Draft `e13686db`, scoped `38ae43a9`, quickstart docs `902f1b76`, SDK guide `6098b9a8`, closure `073a8b87`. |
| 2026-05-28 | t8-c-1-problog-evidence-enrichment-runtime | implemented | Runtime implementation cycle for T8-C-1 ProbLog row-result evidence enrichment after T10-1 C76 shipped at `cde072fa` and the T8-C-1 inventory archived at `bd5baeec`. Shipped trace-payload projection memory by recording export-time probability/projection decisions under `ProvenanceEnvelope.payload["uncertainty_projections"]` while preserving `_claim_probability(...) -> float` and direct helper-call compatibility. Added private frozen `_row_provenance_envelopes` plumbing parallel to `_row_support_artifacts`; `_FORM1_ROW_SUPPORT_KINDS` and `_WITNESS_BEARING_SUPPORT_KINDS` were not widened. ProbLog passed rows now build row-result provenance `EvidenceGraph`s with existing ProbLog trace topology, `EDGE_DERIVES`, exact T8-A 14-key top-level metadata, and namespaced `engine_meta["problog"]` containing trace summary and projection decisions. Existing candidate/readback converter flat `engine_meta` was preserved physically: `provenance.py` and `tests/test_problog_evidence_graph.py` were not touched. Audit module docs now mark ProbLog row provenance graphs as shipped; user-facing docs remain deferred to T8-D round 3. In-cycle lazy import fix `73c26f5f` resolved a protocol/adapter import cycle without broadening scope. Verification: focused suite 117 OK; full discover `2013 tests / 72 failures / 231 errors` versus T10-1 baseline `2011 / 72 / 231`, so no failure/error composition regression; touched-file ruff clean; `git diff --check` clean; dirty baseline preserved as 4 M + 1 D + 5 U; sacred master unchanged. Draft `29f32cb1`, scoped `6ac4f2cb`, projection producer `bc0b0b96`, private context `70f305be`, row bridge `b3ec909b`, tests `22a06ea5`, audit docs `a916a856`, lazy import fix `73c26f5f`, closure `e2266279`. |
| 2026-05-27 | t8-c-1-problog-evidence-enrichment-inventory | implemented | Design-only T8-C-1 pre-implementation inventory cycle after T10-1 C76 shipped at `cde072fa`. Verified that ProbLog remains provenance-bearing, not witness-bearing: row-result evidence cannot use the native/Souffle `_FORM1_ROW_SUPPORT_KINDS` path and should not widen it. Found that existing `problog_trace_to_evidence_graph(...)` is used by tests and legacy service audit-package materialization, but not by `EvaluateRow.explain()`; ProbLog rows still fall back to single-node row evidence until a future runtime bridge ships. Selected trace-payload attachment as the future projection-decision memory channel, because ProbLog export applies C76 policy before the `.pl` program collapses the decision to a point probability. Selected private provenance row context plus `_build_passed_row_evidence_graph(...)` dispatch as the future row bridge, preserving T8-A metadata validation gates. Locked metadata ownership: top-level row graph metadata remains the exact T8-A 14-key set; ProbLog trace summary and uncertainty projection decisions belong under namespaced `engine_meta["problog"]`. Preserved the current candidate converter's flat adapter-local `engine_meta` shape and kept C119 full multi-path DAG plus C136 aggregate envelope deferred. Future T8-C-1 runtime is estimated M-class, with ProbLog adapter projection memory, SDK/protocol row provenance plumbing, row-result metadata wrapper, focused tests, and audit docs; user-facing docs should wait for T8-D round 3 after runtime ships. Verification: focused no-op baseline 38 OK, `git diff --check` clean, dirty baseline preserved as 4 M + 1 D + 4 U, sacred master unchanged. Draft `ab7972df`, scoped `8d3a1634`, closure `bfb5faf1`. |
| 2026-05-27 | t10-1-problog-uncertainty-projection | implemented | Runtime implementation cycle for C76 ProbLog uncertainty projection. Shipped the full three-layer C76 slice: `ProbLogSemantics.uncertainty_projection` SDK shell, SDK lowering into canonical `SemanticsProfile.uncertainty_projection`, and ProbLog adapter consumption of `shared/semantic/raw_kind` + `shared/semantic/bound` during point-probability export. Included the two pre-existing ProbLog migration fixture errors in scope and fixed them without weakening C110 legacy `confidence` rejection. v1 policy support is explicit and non-silent: `reject` raises `ProbLogExportError`; `lower`, `midpoint`, and `upper` materialize point probabilities; degenerate `identity_probability` supports probabilistic `[p, p]`; interval-valued `probability_interval` / `possibility_interval` are rejected for ProbLog point export. Step 4.7 found a baseline-equivalence blocker where two fixed fixture errors were replaced by two direct-call `_claim_probability(...)` `TypeError`s; amend `e7cad68c` restored direct-call defaults to the reject projection, turning full discover from net-equivalent 233 errors into a real improvement to 231 errors. Verification: 24 targeted ProbLog tests OK, 113 focused tests OK, ruff clean, `git diff --check` clean, full discover 2011 tests / 72 failures / 231 errors versus T10 inventory baseline 2004 / 72 / 233. T10-1 unblocks T8-C-1 from the C76 side only; ProbLog evidence enrichment remains a future T8-C-1 cycle. No user/audit docs, governance, EvidenceGraph schema, 14-key metadata, T8-A/B/D behavior, dirty-baseline, or sacred-master changes. Draft `cee6f53f`, scoped `c8dfe1ab`, fixture `4d3ed74e`, SDK `bb619279`, adapter `b7823086`, tests `efcaa36f`, amend `e7cad68c`, closure `9964cdaf`. |
| 2026-05-27 | workflow-docs-sync-policy | abandoned | Governance docs-only policy clarification cycle abandoned after Step 4.6 and before implementation by explicit user decision. Draft/scoped inventory explored a middle path for future multi-file shipped-state/status docs sync, but the policy was not adopted because changing workflow rules was judged too risky relative to the current priority of design/runtime landing. No governance implementation occurred: `CLAUDE.md`, `workflow/AGENTS.md`, `workflow/blueprints/README.md`, root `AGENTS.md`, and `workflow/CADENCE.md` were not edited. The scoped proposal is historical rationale only and does not reclassify prior docs sync commits `d281a5f8`, `0a2e1926`, or `3a8afee6`, nor create future workflow policy. Dirty baseline and sacred master preserved. Draft `deb0b1ff`; scoped `40d468cf`; abandoned `63ecee94`. |
| 2026-05-27 | t10-semantics-adapter-inventory | implemented | Design-only T10 pre-implementation inventory cycle for C74/C76/C77/C78 adapter execution semantics. Verified and refined both reviewer hints: C77 is partial under legacy `valid_time_boundaries` / `fixed_timesteps` names while canonical `fact_boundaries` and `time_binned` are missing; C76 is a broader gap than initially stated because ProbLog lacks the SDK `uncertainty_projection` shell, SDK lowering, and adapter `raw_kind + bound` consumption, with only generic core `SemanticsProfile.uncertainty_projection` substrate shipped. Classified C74/C76/C77/C78 per layer: C74 is partial/legacy (`timestep_delay` shipped, `derived_bound` as `head_bound`, `atom_bounds` not full atom ids), C76 missing at C76-specific layers, C77 partial/legacy, and C78 missing with legacy timesteps substrate. Compared four split options and selected staged hybrid: T10-1 ProbLog C76 full three-layer ship, T10-2 PyReason C74 + C78 canonical shell/migration, T10-3 PyReason C77 temporal rename + `time_binned`. Refined T8-C unblock rules: T8-C-1 requires full C76, not partial carrier substrate; T8-C-2 requires C74 + C77, with C78 required for multi-round PyReason enrichment. Recorded two migration insights: three atom-id conventions currently differ (`<rule_id>:atom_<index>`, `body_atom:{branch}:{atom}`, `b<n>.a<n>:pred_id`) so future C74 needs a conversion layer rather than reusing T8-B witness keys; C77 and C78 are orthogonal by design but legacy `fixed_timesteps` couples iteration count into temporal projection and must be decoupled. Output shape is blueprint-only split plan; no design-point note, runtime, tests, user docs, release, service/OpenAPI, SDK API, database/view, dirty-baseline, or sacred-master files changed. Verification: focused adapter-semantics baseline 46 run / 44 OK / 2 pre-existing ProbLog migration errors from legacy `meta[confidence]` writes, `git diff --check` clean, dirty baseline preserved as 4 M + 1 D + 3 U, sacred master unchanged. Draft `1e1baf06`, scoped `2fcbc377`, closure `a288571a`. |
| 2026-05-27 | t8-c-engine-enrichment-inventory | implemented | Design-only T8-C pre-implementation inventory cycle. Verified the reviewer SupportArtifact-free finding with reproducible grep and source refs: ProbLog and PyReason adapters do not create `SupportArtifact` values; native/Souffle remain witness-bearing while ProbLog/PyReason are provenance-bearing. Current ProbLog evidence is an adapter trace/provenance tree (`EDGE_DERIVES`) with adapter-local metadata (`event_count`, `answer_count`, `root_goal`, `answer_probability`) and 0 overlap with the §10.3 14-key row-result metadata bridge. Current PyReason evidence is an adapter timeline/event graph (`EDGE_UPDATES`) with adapter-local metadata (`timesteps`, event counts, root component fields) and 0 overlap with §10.3; Form 2 remains sketch-only and deferred to D11. Compared four architecture paths and selected adapter-side metadata bridge / provenance row bridge, not T8-B-style SupportArtifact widening or broad protocol allowlist extension. Future implementation splits into T8-C-1 ProbLog after C76 or a ProbLog-specific semantics lock, and T8-C-2 PyReason after D11/Form 2 plus C74/C77/C78 locks. Full C119 ProbLog multi-path DAG, C136 aggregate envelope, and Nemo remain deferred. Engine-specific future fields should be namespaced inside `engine_meta`; no engine_meta flattening and no top-level 14-key metadata changes. Output shape is blueprint-only split plan; no design-point note, runtime, tests, user docs, release, service/OpenAPI, SDK API, database/view, dirty-baseline, or sacred-master files changed. Verification: focused no-op adapter/audit evidence baseline 19 OK, `git diff --check` clean, dirty baseline preserved as 4 M + 1 D + 3 U, sacred master unchanged. Draft `e3bb4ecc`, scoped `17d07d48`, closure `d6b4eed3`. |
| 2026-05-27 | t8-d-round2-souffle-user-docs | implemented | Docs-only S-cycle that completed the user-facing follow-up registered by T8-B-2. Edited exactly the same two user-facing docs as T8-D first round: `docs/official/kernel/quickstart/evidence.md` and `src/factgraph/sdk/docs/00_user_guide.en.md`; no runtime, tests, audit-module docs, release, service/OpenAPI, database/view, dirty-baseline, or sacred-master files changed. Quickstart §6 now teaches shipped row-level Form 1 graphs for native and Souffle passed rows, preserves the ASCII topology `NODE_SEED --supports--> NODE_PREMISE --supports--> NODE_CONCLUSION`, extends the `winning_path_only` wording to OR-shaped native or Souffle evaluation, and removes only Souffle from the deferred row-level Form 1 boundary. ProbLog/PyReason and all other T8-D deferred categories remain marked future; the other current-boundaries bullets, 14-key metadata prose grouping, hard/soft failure explanation, and DTO chain were preserved. SDK guide keeps the concise-summary + quickstart-link split and narrows future adapter wording from ambiguous "adapter Form 1 graphs" to "ProbLog/PyReason Form 1 graphs." Verification: focused no-op evidence baseline 38 OK, `git diff --check` clean, dirty baseline preserved as 4 M + 1 D + 3 U, sacred master unchanged. Draft `102cfee4`, scoped `e2924665`, quickstart docs `9616c28d`, SDK docs `476eaabb`, closure `f853b1f3`. |
| 2026-05-27 | t8-b-2-souffle-form1-conformance | implemented | Completed the Souffle lane for T8-B row-level Form 1 evidence. Step 4.6 verified reviewer due diligence with source refs: Souffle `SupportArtifact`s are built through the native support builder and copy `root_result_kind`, `binding_items`, `pred_witnesses`, `non_fact_steps`, `rule_refs`, and `rule_ref_edges`, while `SOUFFLE_WITNESS_KIND` is already witness-bearing in core. Runtime shipped the low-end option (a) shared helper extension: `_build_native_form1_evidence_graph(...)` was renamed to `_build_form1_evidence_graph(...)`, its body remained unchanged, and protocol-local `_FORM1_ROW_SUPPORT_KINDS` now accepts native + Souffle. The protocol-local allowlist intentionally does not import core `_WITNESS_BEARING_SUPPORT_KINDS`: core witness-bearing support and protocol row Form 1 readiness are separate concepts that may diverge for future adapters. `SDKStore._row_support_artifacts_for_candidates(...)` now admits Souffle through the same explicit allowlist; `_build_passed_row_evidence_graph(...)` and `_explain_live_row(...)` preserve all T8-A 14-key metadata validation gates. Tests add Souffle row Form 1 protocol coverage for support kind, node kinds, `EDGE_SUPPORTS` direction, exact graph metadata, `run_id` absence, `alternative_paths.mode == "winning_path_only"`, and intra-graph seed reuse. Audit docs now say native and Souffle row explanations produce live row-level Form 1 graphs while Souffle proof-tree readback still uses the adapter proof-tree converter. User-facing quickstart / SDK docs are deferred to T8-D round 2. Verification: focused suite 112 OK, touched-file ruff clean, `git diff --check` clean; full discover matches T8-B-1 baseline at 2004 tests / 72 failures / 233 errors. No `EvidenceGraph` schema, node/edge kind, metadata-contract, T8-C, ProbLog/PyReason, aggregate, failed/why-not, match witness, service/OpenAPI, release, database/view, quickstart/SDK user-doc, dirty-baseline, or sacred-master changes. Dirty baseline preserved as 4 M + 1 D + 3 U, including deleted `workflow/working/.gitkeep` and untracked `workflow/design/design-points/active/identity-and-data-model-redesign.zh.md` not introduced by this cycle. Draft `6f3f20c7`, scoped `5f67b109`, runtime `08afd76c`, tests `2a90c338`, audit docs `8eae09d1`, closure `0ca1f313`. |
| 2026-05-27 | t8-d-evidence-docs-alignment | implemented | Docs-only T8-D alignment cycle that closed the user-facing loop for T8-A metadata validation foundation and T8-B-1 native Form 1 topology. Edited exactly two docs: `docs/official/kernel/quickstart/evidence.md` and `src/factgraph/sdk/docs/00_user_guide.en.md`; all runtime, tests, audit-module docs, release machinery, dirty-baseline files, and sacred master stayed untouched. The quickstart now teaches the sessionless three-layer audit model, `run_id` envelope-only boundary, current v1 14-key graph metadata bridge through prose grouping rather than set notation, DTO-first application guidance, soft `GRAPH_VALIDATION_FAILED` handling, native row Form 1 topology via `NODE_SEED --supports--> NODE_PREMISE --supports--> NODE_CONCLUSION`, the user-visible `alternative_paths.mode == "winning_path_only"` marker, stable renderer/DTO invariants, and the complete deferred boundary map for adapter Form 1, aggregates, failed/why-not/counterfactual graphs, match witness output, cross-row seed reuse, session logs/interactions, signatures, ACL, `x-evidence-key`, salience/impact, `EDGE_DERIVES`/`EDGE_UPDATES`, `dag`, and `rule_fire`. The SDK user guide adds only a concise summary plus a quickstart link, avoiding duplicate drift. Verification: focused no-op evidence baseline 36 OK, `git diff --check` clean, implementation diff from scoped touched only the two docs. Draft `14d1129f`, scoped `df20a64e`, quickstart docs `518c4e51`, SDK docs `6a890e8e`, closure `87e1d570`. |
| 2026-05-27 | t8-b-form1-topology | implemented | T8-B first implementation tranche, narrowed in Step 4.6 to **T8-B-1 native Form 1 topology** with Souffle deferred to T8-B-2 / adapter-specific alignment. Shipped native row-level Form 1 `EvidenceGraph`s for passed `EvaluateRow.explain()` when private native support context exists: root `NODE_CONCLUSION`, selected-branch `NODE_PREMISE` nodes, assertion `NODE_SEED` nodes, and `EDGE_SUPPORTS` seed -> premise -> conclusion. Reused existing native `SupportArtifact` / support capture substrate; did not rewrite candidate evidence tree or change `EvidenceGraph` schema. SDK result construction now passes private `_row_support_artifacts` into `EvaluateResult` with `_` prefix and `repr=False` / `compare=False` / `hash=False`, preserving public result shape, repr/equality/hash behavior, and `_evidence_metadata_for_row_result(row, result)` signature. `_build_passed_row_evidence_graph(...)` remains the single T8-A metadata validation gate: it validates the 14-key metadata first, delegates to a private native Form 1 helper when support exists, and falls back to the existing single-conclusion graph for detached/manual rows without support. C129 winning-path-only OR is now runtime-visible on native row graph root metadata; intra-graph seed reuse is implemented; C136 aggregate count-only envelope remains deferred because current `NonFactStep` lacks matched-count contributor state. Audit docs were updated to distinguish live native row Form 1 graphs from candidate evidence tree readback APIs; `tests/sdk/test_rule_expr_evaluate.py` gained a stronger SDK end-to-end Form 1 assertion as a test-scope enhancement. Verification: focused audit/candidate/Souffle/protocol/SDK suite 126 OK; touched-file ruff clean; `git diff --check` clean. Full discover delta was investigated: current HEAD and scoped `31405dea` both report 2004 tests with 72 failures / 233 errors and identical sorted `ERROR:` / `FAIL:` names, so the earlier 234-error observation was transient. No Souffle/ProbLog/PyReason adapter topology, T8-C enrichment, T8-D product docs, match witness, service/OpenAPI, release, database/view, dirty-baseline, or sacred-master changes. Draft `34dfcd3d`, scoped `31405dea`, runtime `d1597344`, tests `31eab30c`, docs `b99c978a`, closure `134696e0`. |
| 2026-05-27 | t8-a-metadata-validation-foundation | implemented | First T8 implementation slice after the T8 split inventory. Shipped the metadata/validation foundation inside `src/factgraph/application/protocol/evaluate_result.py` without public API changes: `_evidence_metadata_payload_for_row_result(...)` is now the single source of truth for the §10.3 14-key graph metadata payload; `_evidence_metadata_for_row_result(row, result)` keeps its existing signature and follows build -> freeze -> validate; `_validate_evidence_metadata_for_row_result(...)` exact-set checks the 14-key contract and per-key values by regenerating the expected payload from the same `EvaluateResult` + row context. C135 is now runtime-enforced: `run_id` remains envelope-only, graph metadata is checked against the row/result envelope, and consistency validation is always-on without Python `assert`, env flags, public validator objects, or SDK/audit API surface. Gate wiring in `_build_passed_row_evidence_graph(...)` and `_explain_live_row(...)` preserves existing failure semantics: builder `ValueError` or bad-metadata `EvidenceGraph` becomes `Explanation(status="unsupported", code="GRAPH_VALIDATION_FAILED")`, while non-`EvidenceGraph` builder returns remain hard `ProtocolShapeError` contract violations. Tests cover missing key, extra key including `run_id`, and wrong-value metadata failures; focused protocol/audit suite 34 OK, engine evidence suite 9 OK, touched-file ruff clean, `git diff --check` clean. No docs/public API, `EvidenceGraph` schema, T8-B/T8-C/T8-D topology/enrichment, service/OpenAPI, release, match, database/view, dirty-baseline, or sacred-master changes. Draft `1ad49592`, scoped `35d6fe61`, runtime A-1 `b78a4091`, runtime A-2 `916f0813`, tests `406a7001`, closure `410c2fce`. |
| 2026-05-27 | t8-implementation-split-inventory | implemented | Design-only planning meta-cycle that quantified evidence §15's T8-A/B/C/D implementation split after T7. Kept the top-level §15 split and selected Option B: the archived blueprint/audit pair is the durable planning artifact, with no new design-point note and no runtime/test/docs implementation. Corrected the handoff's rough 10-row estimate by mapping all 12 T7 contract-matrix rows to shipped / T8-A / T8-C / T8-D / future buckets. T8-A remains the first recommended slice and may run as one blueprint with two commits or split into A-1 metadata builder + sufficiency checker and A-2 strict validation gate + debug assertion if Step 4.6 scope grows; estimated boundary is `evaluate_result.py`, `evidence_graph.py`, and protocol/audit graph tests. T8-B should reuse existing candidate evidence tree, candidate step, and Souffle provenance surfaces before any rewrite. T8-C is gated by T10 or engine-specific semantics locks; ProbLog/PyReason should be separate sub-cycles unless explicitly scoped out. T8-D may run after any shipped T8 behavior and must document only shipped surfaces. D-series mapping: T8-A activates no D-item directly; T8-B may activate D18/D12 only if scope broadens; T8-C may activate D11/D13/D6/D7; D20 match witness seam stays non-T8 future. No-op baseline audit graph/render tests 20 OK, `git diff --check` clean, no runtime/test/docs/release/dirty-baseline/sacred-master changes. Draft `2a7f082f`, scoped `87dfd635`, closure `947567bd`. |
| 2026-05-27 | t7-evidence-audit-rendering-bridge | implemented | Design-to-shipped bridge after T6 evidence Phase B. Source-backed scoped inventory mapped T6 §10/§11 contracts to shipped audit runtime and narrowed T7 to two bridge-sized runtime changes: `render_evidence_graph_html(...)` now rejects non-`EvidenceGraph` inputs with `ValueError("graph must be EvidenceGraph")`, and tree/timeline reference renderers emit a non-blocking large-graph warning when a graph has more than 250 nodes or more than 500 edges. Tests add strict renderer type/threshold coverage and enforce the exact 14-key §10.3 `EvidenceGraph.metadata` bridge with `run_id` remaining envelope-only. Audit docs now describe the sessionless three-layer contract, safe JSON `evidence_graph_from_dict(...) -> render` path, large-graph handoff, T8 boundary, and D20 future match witness seam without defining an API. Central metadata sufficiency remains T8-A; T8-A/B/C/D graph construction/enrichment/product-doc slices remain out of scope. Focused verification: audit graph/render 20 OK, engine evidence graphs 9 OK, related audit/match/evaluate-result suite 106 OK, touched-file ruff clean, `git diff --check` clean. Full discover baseline recorded as 2001 tests with unrelated frontier/legacy/why_not/redesign-invariant failures. No service/OpenAPI, SDK API shape, database/view runtime, release machinery, dirty-baseline, or sacred-master changes. Draft `c9979476`, scoped `5c5187e2`, runtime bridge `c6b76878`, tests `04697038`, docs `3d77dc94`, closure `ae4fe79c`. |
| 2026-05-27 | t11-2-10-or-ruleexpr-match-runtime | implemented | Follow-up to T11.2.9 that completes M5 by shipping OR `RuleExpr` support for `fg.read.match(...)`. Runtime stayed inside `src/factgraph/sdk/match_runtime.py`: `_MatchPlan` now stores uniform branch bodies and partial ports; constraints are distributed to every effective branch; one branch emits one-level where IR and multiple branches emit OR-of-AND two-level IR for existing `evaluate_where(...)`; M21 connectivity now checks each effective branch independently; constrained partial ports raise explicit `SDKStoreError`; projected `idref_v1` de-dup and limit-after-dedup semantics are preserved. Tests in `tests/test_sdk_read_match_runtime.py` cover pure OR, mixed AND+OR, duplicate entity across branches, per-branch disconnected reject, partial-port reject, distributed constraints, limit-after-union, and empty OR construction. Docs/design updated `match-api-design.zh.md`, rules quickstart, SDK rules docs, and CHANGELOG so OR support is shipped while witness/query/method-level-view/cross-entity tuple/service surfaces remain deferred. Added a one-line runtime comment documenting lowered where IR Var naming (`"$..."`). Verification: 25 match/attach tests OK, 121 RuleExpr/match focused tests OK, touched-file ruff clean, `git diff --check` clean. No core `where_eval` / `where_ast` / RuleExpr lowering, service, EvidenceGraph, release, dirty-baseline, or sacred-master changes. Draft `298f4581`, scoped `ece08453`, runtime `b7bd48f0`, tests `5b41f341`, docs/design `dd88f1a0`, IR convention note `be759a1b`, closure `3a31995b`. |
| 2026-05-27 | n10-reference-index-cleanup | implemented | Single-actor Claude-drafted S-scope cycle that rebased `docs/references/README.md` onto current workflow paths. Governance lines: `src/factpy_kernel/*/docs/` -> `src/factgraph/*/docs/`; `docs/blueprints/` -> `workflow/blueprints/`; `docs/blueprint_history/` -> `workflow/heritage/blueprint_history/`; `docs/blueprints/archive/` -> `workflow/blueprints/archive/`; `docs/architecture_principles.md` -> `workflow/foundations/architecture_principles.md`. Design-points subsection: replaced inline per-note listing (4 files at non-existent paths) with canonical pointer to `workflow/design/design-points/{active,archive}/`, retaining the subsection header as a breadcrumb and explicitly flagging dirty-baseline `working/design-points/readme.md` as non-authoritative. Added two previously unindexed `working/` bundles: `post-routemap-direction-selection-input/` (2026-05-07 strategic A+B input bundle, 8 files) and `factgraph-namespace-test-proposals/` (2026-05-14 namespace-test review packet, 4 .md + 3 .py). Anchor date refreshed to "2026-05-27 N10 cleanup 后"; brittle "全 56 个文件" count claim softened to scope-statement form. Pre-implementation re-read amended scope (`a4c7de15`) to include the second unindexed bundle for symmetry, keeping class S. Historical bundle sub-READMEs preserved verbatim (time-frozen). Dirty-baseline `working/design-points/readme.md` left untouched and recorded as follow-up O-N10-1; historical bundle path strings recorded as follow-up O-N10-2. No production / tests / examples / notebooks / SDK / service / agent / adapter changes. Dirty baseline preserved at 4 modified + 1 untracked; sacred `master` unchanged at `562c74195df43e933bed92a3ff25de94dd8ce666`. Draft `e5570dfb`, scoped `ef63ebcb`, scope amend `a4c7de15`, implementation `06ada1b2`, closure `d3cf1280`. |
| 2026-05-27 | t11-2-9-match-runtime-implementation | implemented | N9 design-to-runtime closure for T11.2.7 match API. Shipped first-tranche `fg.read.match(EntityCls, Rule \| AND RuleExpr, *, limit=None, **port_constraints) -> tuple[EntitySnapshot, ...]` via new `src/factgraph/sdk/match_runtime.py` plus `_SDKReadManager.match(...)` / `SDKStore.match(...)`; preserved M1-M21 by returning plain distinct snapshots(no wrapper DTO), using canonical `idref_v1` comparison with snapshot-to-ref normalization, supporting literal and own-class Field constraints, enforcing connectivity before matching to avoid silent cross products, honoring attach-time view scope through the active ledger, and keeping method-level `view=`, witness/assertion output, Query adapters, OR RuleExpr, cross-entity tuple output, service/OpenAPI, and EvidenceGraph changes deferred. Added `tests/test_sdk_read_match_runtime.py` and attach-view coverage; focused match+attach suite 22 OK and broader RuleExpr/match/attach suite 108 OK. Updated read-write and rules quickstarts, SDK docs, and CHANGELOG so match is shipped while witness/method-level-view/as_of/EvidenceGraph/adapter semantics remain deferred. Full `unittest discover tests` was also run and recorded as 1998 tests with unrelated legacy/frontier/why-not/stale-invariant failures; focused gates are the cycle verification. Draft `90b6c59e`, scoped `ee59414d`, runtime core `4dad8a33`, tests `37e3662c`, docs `49960bef`, closure `f5d80f08`. |
| 2026-05-27 | t6-evidence-phase-b-design | implemented | Evidence Phase B design skeleton completion. Replaced `evidence-tree-rainbird-style-v1.zh.md` §10 Audit Channel with a sessionless v1 audit contract: `EvaluateResult` envelope vs row `Explanation` vs durable `EvidenceGraph.metadata`, explicit `run_id` envelope-only stance, validation/roundtrip, immutability, audit package boundary, and v2+ channel deferrals. Replaced §11 Rendering with reference renderer vs product UI split, tree/timeline layout matrix, minimal/empty/invalid/unsupported cases, large graph guidance (`>250` nodes or `>500` edges) as warning/handoff only, safe JSON `from_dict -> render` path, and custom UI obligations. Expanded §14 from D1-D19 into owner/trigger/v1-boundary/implementation-hint registry, added D20 match witness seam without API shape, and added §15 T8-A/B/C/D implementation split proposal as proposal-only (not binding execution order). Reconciled stale internal `metadata 15 fields` / `schema_version required` wording to §10.3 source of truth and narrow-fixed parent §7 from old `docs/references/...` path to the active design path. No production/test/release/notebook changes; dirty baseline preserved; sacred `master` untouched. Draft `7f1a9e85`, scoped `3c9fb398`, §10 impl `fccb21ee`, §11 impl `2309cab0`, §14/§15 impl `42e5e281`, parent pointer `8a37e132`, closure `88469834`. |
| 2026-05-26 | t11-3-v0-2-release-machinery | implemented | v0.2.0 release machinery cycle. Renamed PyPI distribution metadata from `factpy-kernel` to `factgraph` (`pyproject.toml` `name="factgraph"`, `version="0.2.0rc1`), added prominent README/CHANGELOG install migration (`pip uninstall factpy-kernel && pip install factgraph`), and left the old package/releases untouched with no shim. Migrated release tooling from old `src/kernel` assumptions to the current `src/factgraph` public surface: workflow renamed to `factgraph-tests.yml`, `release.sh` verifies the focused factgraph gate, projection script scans `src/factgraph`, and allowlist now projects 271 files. Release-facing stale text fixed in SECURITY, SDK user guide, and audit optional-domain error; `src/factgraph/authoring/cli.py` unused imports removed as a release-ruff side effect (no behavior change). Dry-run from clean auxiliary worktree at `7a6fee63` passed: projection 271 files, editable install + focused tests `187 OK`, dry-run release commit `402d098d`, no remote refs pushed, local dry-run milestone/tag/release branch cleaned. Live release / PyPI / GitHub Release remains a separate explicit user gate. Draft `589a2f4e`, scoped `f7660183`, metadata impl `4f4051c0`, release machinery impl `7a6fee63`, closure `ad6c8968`. |
| 2026-05-26 | t11-2-6-sdk-assertion-property-access | implemented | Release-path SDK ergonomics slice for the dirty `src/factgraph/sdk/facade.py` + companion test pair classified by T11.2.5 and decoupled by T11.2.7. Landed property-first field assertion access (`snapshot.field("name").active`, `.history`, `.all`), preserved legacy `.active()` / `.all()` through `AssertionRecordSet.__call__() -> self`, added collision-free `snap.assertions.<field>` proxy access, and synced six assertion-access docs files. Tests cover property/call identity, reserved method precedence, dunder lookup, unknown fields, and invalid chain misuse. Verification: focused `test_sdk_assertion_record_set_view_filters` 7 OK, touched-file ruff clean, `git diff --check` clean, field-level stale-call docs grep clean. Draft `da2bdc0a`, scoped `fdf8a78b`, implementation `1e7b4949`, closure `41abd906`. |
| 2026-05-26 | t11-2-7-match-api-design | implemented | Release-path design cycle for old Query -> match migration before deciding the dirty `facade.py` assertion-ergonomics pair. Created active `workflow/design/design-points/active/match-api-design.zh.md` and a parent §6 cross-link in `rule-expression-and-proof-attempt.zh.md`. Final V4 shape: `fg.read.match(EntityCls, template, *, limit=None, **port_constraints) -> tuple[EntityCls snapshot, ...]`; EntityCls is the match-side head, templates are application `Rule` / `RuleExpr`, output is distinct materialized snapshots, kwargs accept literals or own-class Field descriptors, attach-time view scope is preserved, method-level `view=` remains rejected, and `fg.eval.run` deletion remains future hard-cut. `b1f52ede` added the §5.4 pattern-connectivity invariant to prevent silent cartesian products from disconnected templates. Dirty `facade.py` is explicitly independent of match and may land/stash/revert separately. Draft `83e452f3`, scoped `1d3dfe0f`, ergonomics amend `babd1046`, initial design `80d62bb3`, V4 rework `ef34d618`, connectivity amend `b1f52ede`, closure `297e5134`. |
| 2026-05-26 | t11-2-5-dirty-baseline-triage | implemented | Release-path prerequisite before T11.3: classified the standing dirty baseline without editing it. Final verdicts: `src/factgraph/sdk/facade.py` + `tests/test_sdk_assertion_record_set_view_filters.py` must land together in dedicated `T11.2.6 SDK assertion property access` behavior slice or be explicitly stashed/reverted by the user; three notebooks defer to notebook namespace/output cleanup; `docs/references/working/design-points/readme.md` defers to reference-index cleanup; `rainbird-ai sdk code/` remains untracked and ignored unless a future provenance/license review adopts it. Added T11.3 handoff to repo-local memory: classification is complete, but `scripts/release.sh` still requires tracked dirty files to be landed, stashed, or explicitly reverted before dry-runs. No release machinery, production, test, notebook, or external reference files changed. Draft `f0174d4e`, scoped `ca45729a`, closure `76aa5a6a`. |
| 2026-05-26 | t11-2-cross-doc-metadata-unblock | implemented | Release-path cross-doc unblock for database/view Step 1-6 and I10/A10: confirmed there are no literal S1-S6 markers and mapped them to database-view §16 Step 1-6; accepted `EvaluateResult.view_snapshot_digest` as the v0.2 public metadata bridge with EvidenceGraph metadata durable copy; left exact `db_id` / `tx_id` / `schema_digest` / `data_digest` / `view_digest` tuple fields as v2/internal future activation; fixed stale view-scoped-attach wording in official Database quickstart, core store docs, and SDK docs; archived the implemented T11.1 blueprint pair as lifecycle cleanup. No production, tests, service, OpenAPI, adapter, SDK API, example, or notebook changes. Draft `e1bdfa43`, scoped `4283cc3c`, implementation `3210cf53`, closure `4f934676`. |
| 2026-05-26 | t11-1-attach-view-scope | implemented | T11.1 Database/view release-track slice: shipped `FactGraph.attach(db, view=view)` for durable Database views, read-only view-attached runtimes, attach-time schema/db/base-tx/assertion validation, shared view-scoped read/evaluate behavior, and method-level `view=` rejection hints. Preserved exact `schema_classes` digest contract, rejected SDK in-memory views for attach, and kept method-level `view=` / `Database.as_of(...)` deferred. Draft `7d17c7ae`, schema-contract amends `c4336762` + `b30c9c59`, scoped `55b89f3b`, baseline `88b3d729`, feat `76f46ada`, closure `ea86f5d9`; archived during T11.2 lifecycle cleanup. |
| 2026-05-26 | t12-minimal-housekeeping | implemented | Cross-flip housekeeping cycle after T5/T11.1 publication. T12.1 retained `rule-expression-and-proof-track-plan.zh.md` in active as a historical T1-T5 decomposition index while marking future scheduling superseded by `post-t5-completion-roadmap.zh.md`; T12.2 moved D16-D26 from `proposed` to `adopted` with `Implementation Anchors:` metadata and populated `workflow/design/decisions/README.md`; T12.3 added a compact active design-point inventory; T12.4 compacted repo-local `workflow/memory/current.md` from 168,593 bytes to 5,447 bytes. Global Claude `MEMORY.md` remains a follow-up because it is outside repo writable roots. Draft `03698d9c`, scoped `7a1d31a0`, implementation `6c7f9c1c`, closure `a97a1bab`. |
| 2026-05-26 | quickstart-why-explanation-batch-fix | implemented | Self-owned doc-quality batch cycle on T11.1 branch: closes 21 findings (8 High + 13 Medium severity) + the cross-cutting C110 canonical quantitative carrier anchor identified by sub-agent audit `acbc5b94ba313ef05`. 9 quickstart files modified (+244/-24 LOC). New §"Canonical quantitative carrier" in `assertions.md` is the canonical contract anchor; `semantics.md` and `evidence.md` cross-link forward without contract duplication. Key landed pieces: `build_application_rule` vs `Rule` factory-vs-data-shape framing in rules-and-inferences.md (parallel to schema_classes-vs-schema_ir in database.md); `Inference` labeled v0.2 compatibility surface; `RuleRef` framed as protocol-only (rejected by build_application_rule per C9); `fg.inferences` empty namespace explicit compatibility placeholder; `FactGraph.attach` deliberate no-`rules=` contract; `Identity(primary_key=True)` and `Identity(default=)` semantics; `idref_v1` token versioned scheme + non-parseable invariant; retract append-only WHY anchored to evidence reproducibility; schema.add additive-only design boundary; `EvidenceGraph` named DAG/cycle-detection/engine-meta invariants; `DetachedRowError` distinguished from `status=failed`; closed-head naming; SemanticsProfile wrapper lowering relationship; AND-only-body invariant for application Rule; frozen views vs durable views design boundary. 5 Low-severity findings deferred. No production files; no SDK API changes; no shipped-behavior changes. Scoped `0b5d4d33`, implementation `dd367166`, closure `29583532`. |
| 2026-05-26 | quickstart-database-view-rebase-onto-t11-1 | implemented | Self-owned reconciliation cycle on T11.1 branch: cherry-picked T5 branch's `quickstart-database-view` content (new `docs/official/kernel/quickstart/database.md` +388 LOC) and amended it for post-T11.1 shipped reality of `FactGraph.attach(db, schema_classes=[...], view=view)` (T11.1 `76f46ada`). Three amends: A1 removed the wrong `# FactGraph.attach(db, view=view, ...)   # not shipped` line; A2 rewrote the "Do not pass view=" checklist bullet to scope-correctly say view= is attach-time only; A3 added new "View-scoped attach" section with read-only / mutation-reject coverage. Also re-introduced cross-link paragraphs in `index.md` (database.md as #5), `persistence.md` (cross-link sentence), `assertions.md` (cross-link paragraph), and `namespace-map.md` (cross-link in attach row). 7-step end-to-end smoke green (Database.create → writable attach → commit_assertions → create_view → view-scoped attach → mutation reject → reattach). T5 branch's 8 commits (`bf161c62..c77d0442`) remain as historical record on `v0.2.0-t5-result-evidence-explain-audit-2026-05-25`; T11.1 is the canonical published line. Scoped + inventory `20bae7ba`, implementation `1c54f587`, closure `65962d97`. |
| 2026-05-26 | quickstart-rules-ports-ruleexpr | implemented | Self-owned docs cycle on T11.1 branch: expanded `docs/official/kernel/quickstart/rules-and-inferences.md` with three new sections (~330 LOC) — Understanding ports (three-variable distinction, `port_types` inference, `desc="%port_name"` rendering); Composing rules with RuleExpr (`rule.as_("alias")`, `&` / `|`, `RuleExpr.all` / `.any`, `.join_by_ports`, `.join(eq)`, AND-spine reject); Choosing the right head (arity invariant, cross-rule head selection, `Rule.projection` non-eval-head call-out). Recorded two audit-only design vs shipped drifts (same-named-port ambiguous reject; `Rule.projection` not an eval head). 18-assertion end-to-end smoke green incl. 5 negative-path rejects. Implementation `4cdd4e54`, closure `118dc819`. |
| 2026-05-22 | cross-doc-seams-quickstart-refresh | implemented | Documentation-only refresh on Slice 7C implementation branch: rewrote quickstart persistence around current workspace save/load and migration CLI, aligned rules/inferences and namespace map after registry adapter removal, preserved runtime `registry=None` evidence examples, and added current-truth pointers to rule-expression/evidence-tree design-point notes; implementation `69850a2d`, closure `d2cd83e4` + `c7ab669a`. |
| 2026-05-20 | db-identity-substrate | implemented | DB/view Slice 1: introduced Database boundary, DatabaseValue, canonical tx/data/assertion identity protocols, canonical durable AssertionRecord, active-only no-view data_digest, MetaRow-based assertion identity, and focused tests; implementation `6e4642d7`, closure `f3c4375f`. |
| 2026-05-20 | db-workspace-physical-layout | implemented | DB/view Slice 2: moved durable Database workspace layout to `db/objects/tx/`, `db/objects/schema/`, `db/refs/head.txt`, and `db/assertions.db`; `Database.head()` resolves head ref through tx object; registry preserved during transition; implementation `203b4f21`, closure `b606a126`. |
| 2026-05-20 | db-view-shape-persistence | implemented | DB/view Slice 3: introduced canonical 6-field Database-owned FrozenAssertionView record, view_digest with VIEW_V1_PREFIX byte protocol, anonymous content-addressed `views/objects/<64hex>.json` persistence, `Database.create_view(...)` substrate API (new-layout workspace only, current-head-only, claim-existence validation), and tests; implementation `c51ed752`, closure `9bf55beb`. |
| 2026-03-15 | blueprint-workflow-foundation | archived | 建立可持续、可审计的 blueprint 工作流 |
| 2026-03-15 | historical-blueprint-status-backfill | archived | 回填 blueprint_history/ 的状态一致性 |
| 2026-03-15 | legacy-reconstructed-archive-rules | archived (reconstructed) | 定义 reconstructed archive 迁移规则 |
| 2026-03-15 | overall-system-blueprint | superseded | 早期跨层总蓝图；已由 2026-03-22 阶段母图接替当前 framing 角色 |
| 2026-03-16 | souffle-annotation-benchmark-spec | archived | Souffle annotation benchmark 规格 |
| 2026-03-16 | souffle-backed-annotation-kernel-spike | implemented | Souffle-backed reasoning kernel spike |
| 2026-03-16 | temporal-hybrid-reasoning-blueprint | superseded | 早期 temporal/hybrid 母蓝图；当前阶段已收敛到 ECSS fit 与 provenance feasibility |
| 2026-03-17 | artifact-sidecar-store | implemented | File-backed durable explain carrier |
| 2026-03-17 | audit-package-artifact-export | implemented | Explain artifact audit package 导出 |
| 2026-03-17 | candidate-id-support-backref | implemented | Candidate→support backref 补齐 |
| 2026-03-17 | durable-artifact-storage | implemented | Durable artifact 存储方案 |
| 2026-03-17 | explain-ref-service-unification | implemented | Service explain handle 统一 |
| 2026-03-17 | reference-docs-workflow | archived | Reference docs 工作流定义 |
| 2026-03-17 | rule-run-trace-schema-contract | implemented | Rule run trace schema contract |
| 2026-03-17 | run-rule-trace-capture | implemented | Rule trace capture at evaluate time |
| 2026-03-17 | runtime-service-explain-readback | implemented | Service 层 explain readback |
| 2026-03-17 | runtime-traceability-explainability-blueprint | superseded | Traceability/explainability 母蓝图；子实现已落地，当前方向改为 domain validation + engine provenance |
| 2026-03-17 | sidecar-retention-gc | implemented | Sidecar retention + TTL GC |
| 2026-03-17 | souffle-annotation-kernel-prototype | implemented | Souffle annotation kernel prototype |
| 2026-03-17 | support-artifact-native-capture | implemented | Native derivation support artifact capture |
| 2026-03-17 | support-artifact-readback | implemented | Support artifact readback 接口 |
| 2026-03-18 | aml-aggregation-materialization-walkthrough | implemented | AML event aggregation materialization walkthrough |
| 2026-03-18 | aml-case-review-walkthrough | implemented | AML case review walkthrough |
| 2026-03-18 | aml-event-aggregation-semantics | implemented | AML event aggregation semantics |
| 2026-03-18 | aml-obligation-trigger-semantics | implemented | AML obligation trigger semantics |
| 2026-03-18 | aml-suspicious-account-anchoring | implemented | AML suspicious account anchoring |
| 2026-03-18 | aml-transaction-feed-materialization-walkthrough | implemented | AML transaction feed materialization walkthrough |
| 2026-03-18 | aml-transaction-feed-normalization-anchoring | implemented | AML transaction feed normalization anchoring |
| 2026-03-18 | aml-trigger-walkthrough | implemented | AML trigger walkthrough |
| 2026-03-18 | audit-compliance-matrix-ui | implemented | Audit compliance matrix static UI |
| 2026-03-18 | audit-rule-run-explain-summary-export | implemented | Audit rule run explain summary export |
| 2026-03-18 | clinical-deterioration-uncertainty-anchoring | implemented | Clinical deterioration uncertainty anchoring |
| 2026-03-18 | clinical-weak-signal-walkthrough | implemented | Clinical weak signal walkthrough |
| 2026-03-18 | conflicting-multi-note-evidence-walkthrough | implemented | Conflicting multi-note evidence walkthrough |
| 2026-03-18 | conflicting-narrative-evidence-anchoring | implemented | Conflicting narrative evidence anchoring |
| 2026-03-18 | correlated-multi-note-review-walkthrough | implemented | Correlated multi-note review walkthrough |
| 2026-03-18 | cross-domain-validation-stage-handoff-refresh | implemented | Cross-domain validation handoff refresh |
| 2026-03-18 | document-evidence-extraction-anchoring | implemented | Document evidence extraction anchoring |
| 2026-03-18 | ecss-requirement-authoring-surface | implemented | ECSS requirement authoring surface |
| 2026-03-18 | ecss-scenario-anchoring | implemented | ECSS scenario anchoring |
| 2026-03-18 | ecss-vcd-compliance-delivery | implemented | ECSS VCD compliance delivery |
| 2026-03-18 | engine-witness-parity | implemented | Engine witness parity |
| 2026-03-18 | form-document-extraction-walkthrough | implemented | Form document extraction walkthrough |
| 2026-03-18 | free-text-narrative-evidence-anchoring | implemented | Free-text narrative evidence anchoring |
| 2026-03-18 | mixed-source-case-pack-anchoring | implemented | Mixed-source case pack anchoring |
| 2026-03-18 | mixed-source-case-pack-walkthrough | implemented | Mixed-source case pack walkthrough |
| 2026-03-18 | multi-note-narrative-synthesis-anchoring | implemented | Multi-note narrative synthesis anchoring |
| 2026-03-18 | native-candidate-evidence-tree-v1 | implemented | Candidate evidence tree v1 |
| 2026-03-18 | process-safety-shutdown-anchoring | implemented | Process safety shutdown anchoring |
| 2026-03-18 | process-safety-shutdown-walkthrough | implemented | Process safety shutdown walkthrough |
| 2026-03-18 | rule-run-explain-narrative | implemented | Rule run explain narrative |
| 2026-03-18 | rule-run-explain-narrative-parity | implemented | Rule run explain narrative parity |
| 2026-03-18 | rule-run-nl-explain | implemented | Rule run NL explain |
| 2026-03-18 | runtime-rule-run-explain-summary-api | implemented | Runtime rule run explain summary API |
| 2026-03-18 | runtime-structured-json-explain-api | scoped | Runtime structured JSON explain API |
| 2026-03-18 | runtime-traceability-evidence-tree-realignment | implemented | Traceability evidence tree realignment |
| 2026-03-18 | scenario-a-audit-delivery-shape | scoped | Scenario A audit delivery shape |
| 2026-03-18 | scenario-a-composite-reference-check | implemented | Scenario A composite reference check |
| 2026-03-18 | scenario-a-temporal-semantics | implemented | Scenario A temporal semantics |
| 2026-03-18 | scenario-a-uncertainty-and-confidence | implemented | Scenario A uncertainty and confidence |
| 2026-03-18 | session-handoff-refresh | implemented | Session handoff state refresh |
| 2026-03-18 | single-note-narrative-extraction-walkthrough | implemented | Single-note narrative extraction walkthrough |
| 2026-03-19 | blueprint-workflow-active-archive-cleanup | implemented | Blueprint active→archive cleanup |
| 2026-03-19 | candidate-evidence-tree-provenance-source-taxonomy | implemented | Evidence tree provenance/source taxonomy |
| 2026-03-19 | engine-candidate-explain-tree-degraded-node-shape | implemented | Engine degraded tree node shape |
| 2026-03-19 | evidence-tree-stage-handoff-refresh | implemented | Evidence tree stage handoff refresh |
| 2026-03-19 | native-candidate-evidence-tree-recursive-proof-semantics | implemented | Recursive proof semantics |
| 2026-03-19 | native-candidate-evidence-tree-richer-unresolved-taxonomy | implemented | Richer unresolved taxonomy |
| 2026-03-19 | native-candidate-evidence-tree-v2 | implemented | Evidence tree v2 |
| 2026-03-19 | native-candidate-evidence-tree-winning-branch-narrowing | implemented | Winning branch narrowing |
| 2026-03-19 | native-candidate-evidence-tree-winning-branch-semantics | implemented | Winning branch semantics |
| 2026-03-19 | native-derivation-ruleref-execution-decision | implemented | RuleRef execution decision |
| 2026-03-19 | native-where-ruleref-execution-substrate | implemented | Where RuleRef execution substrate |
| 2026-03-20 | candidate-confidence-kind | implemented | Candidate confidence_kind 语义化 |
| 2026-03-20 | candidate-evidence-tree-nl-explain | implemented | Candidate evidence tree NL explain |
| 2026-03-20 | candidate-evidence-tree-salience-impact | scoped | Candidate evidence tree salience/impact |
| 2026-03-20 | certainty-propagation-prototype | implemented | Certainty propagation prototype |
| 2026-03-20 | certainty-weight-vocabulary | implemented | Certainty weight vocabulary |
| 2026-03-20 | engine-partial-witness-adapter-contract | implemented | Engine partial witness adapter contract |
| 2026-03-20 | engine-partial-witness-audit-static-surface | implemented | Engine partial witness audit+static surface |
| 2026-03-20 | live-evidence-url-runtime-permalink | implemented | Live evidence URL runtime permalink |
| 2026-03-20 | rule-condition-weight-metadata | implemented | Rule condition weight metadata |
| 2026-03-21 | certainty-audit-static-delivery | implemented | Certainty audit+static export-time materialization |
| 2026-03-21 | certainty-aware-narrative-nl-delivery | implemented | Certainty-aware narrative/NL delivery |
| 2026-03-21 | certainty-summary-explain-delivery | implemented | Certainty summary explain delivery |
| 2026-03-21 | confidence-kind-certainty-routing | implemented | Resolver protocol + create-time certainty routing |
| 2026-03-21 | certainty-runtime-boundary-cleanup | implemented | Certainty helper extraction + archive inventory |
| 2026-03-21 | certainty-salience-ranking-v1 | implemented | Salience ranking: impact 升序 + bottleneck 标注 + NL weakest-condition |
| 2026-03-21 | phase3-test-decomposition | implemented | 10K-line test monolith → 7 capability files |
| 2026-03-21 | fact-confidence-to-evidence-tree | implemented | Fact-level confidence carrier: meta.confidence → assertion_fact → condition_confidence=max(children) |
| 2026-03-21 | certainty-additive-aggregation-v1 | implemented | Second aggregation strategy: weighted additive contribution alongside bottleneck/min |
| 2026-03-22 | docs-memory-and-blueprint-realignment | archived | design→active mother blueprint、handoff→memory、旧 active 母蓝图 superseded 归档 |
| 2026-03-22 | souffle-provenance-adapter-v0 | implemented | Adapter-local Souffle `-t explain` parser/runner helper；验证递归链、否定叶子与规则编号，不触碰 core/service/audit 契约 |
| 2026-03-23 | ruleref-souffle-query-export | implemented | 为 query-bearing package export 增加 registry-backed `ruleref` 编译；真实 ECSS top-level compliance 已可产出 Souffle proof tree |
| 2026-03-23 | provenance-audit-consumer-surface | implemented | Souffle provenance 进入正式 audit pipeline：`provenance_trees.jsonl` 物化、AuditQuery 读取、candidate static page 渲染 `Engine Provenance` section |
| 2026-03-23 | provenance-coverage-and-bridge | implemented | Provenance consumer surface 收口：`provenance_statuses.jsonl`、candidate badge / truncation UX、package-level coverage summary 与 landing page coverage metrics |
| 2026-03-23 | esa-demo-packaging | implemented | Standalone ESA demo script + static HTML polish + positioning doc + walkthrough doc; 9 ECSS rules, 2 missions, provenance, 26-page audit site |
| 2026-03-26 | pyreason-provenance-adapter-v0-spike | implemented | PyReason adapter-local provenance carrier (event log, not proof tree); `PyReasonTraceEventV0` / `PyReasonTraceV0`; verified on pyreason==3.0.0 |
| 2026-03-26 | pyreason-rule-ext-v0 | implemented | Adapter-local `PyReasonRuleDef` / `PyReasonRuleExt` / `PyReasonFactDef` + minimal WHERE→PyReason compile helper; runner and demo now accept typed defs while preserving legacy tuples |
| 2026-03-26 | pyreason-runner-v0 | implemented | Reusable runner helper: session -> graph -> reason -> trace -> derived_session; integration demo now delegates to runner |
| 2026-03-26 | pyreason-session-batch-api-and-annotations | implemented | `PyReasonSession.batch()` entity-level API + annotation template generation + batch-based integration demo refresh |
| 2026-03-26 | pyreason-session-annotation-accept-v0 | implemented | Adapter-local accept helper that persists `PyReasonSession` facts to Ledger and materializes `pyreason/semantic/*` annotations via real assertion `asrt_id` binding |
| 2026-03-26 | unified-fact-write-and-edge-api | implemented | Relationship SDK type + schema_ir compilation + PyReason engine-specific write session (Phase 1) |
| 2026-03-26 | annotation-store-v0-schema-and-ledger | implemented | Assertion Annotation Store: `AnnotationRow` + `annotation_rows` DDL + shared whitelist dual-write in `write_protocol` + legacy `meta_rows` projection; 51 new tests |
| 2026-03-27 | pyreason-execution-surface-v0-decision | superseded | Original 4 decisions (D1-D4) for PyReason execution surface; superseded by `multi-engine-execution-surface-decision` after office hours redesign revealed shadow pipeline problem |
| 2026-03-27 | multi-engine-execution-surface-impl | implemented | PyReason joined the shared evaluate/accept surface via `engine_ext` passthrough, WhereIR compiler, engine evaluator, pending annotation side-channel, and SDK end-to-end coverage |
| 2026-03-27 | pyreason-execution-surface-closeout | implemented | Closed the remaining `engine_ext` type-guard gap and formalized reusable post-accept PyReason annotation persistence |
| 2026-03-27 | pyreason-real-engine-validation | implemented | L1 gate passed: root cause was stale numba cache (144 files), not version incompatibility. Full real-engine chain validated: evaluate (166.6s first JIT) → 2 CandidateSet → accept → 6 annotations persisted |
| 2026-03-27 | engine-options-impl | implemented | Shared evaluate dispatch now accepts call-time `engine_options`; SDK keeps it out of authored payloads, native rejects non-empty options, and PyReason exposes `timesteps`-only runtime config with adapter-local validation |
| 2026-03-27 | annotation-consumer-migration-l2 | implemented | Assertion annotation consumer lane: export + reader + query index + static UI panel |
| 2026-03-27 | problog-semantic-annotation-parity-l4 | implemented | L4: ProbLog results write `problog/semantic/probability` annotations; enters unified audit/static consumer pipeline |
| 2026-03-27 | multi-engine-semantic-delivery | implemented | Mother blueprint: all gates (1-3) closed, all direction lines (L1-L4) complete, engine_options future branch also done. 499 tests green |
| 2026-03-28 | engine-provenance-surface-spike | implemented | Shared provenance envelope lane for PyReason/ProbLog runtime candidate explain; Store/runtime now records and reads back engine-native provenance carriers |
| 2026-03-28 | problog-timeout-eval-surface | implemented | ProbLog eval split + runtime `engine_options.timeout` support + parser fix for tab-format CLI output |
| 2026-03-28 | pyreason-graph-fact-materialization-fix | implemented | Real-engine fix: graph build becomes structure-only and node facts lower through explicit `add_fact(...)` registration |
| 2026-03-28 | pyreason-node-edge-channel-split | implemented | PyReason runner now splits node facts and edge labels into different engine channels to match real propagation semantics |
| 2026-03-28 | pyreason-threshold-aware-clause-bounds | implemented | Added `body_predicate_bounds` compile surface so bounded seeds can participate through explicit clause intervals |
| 2026-03-28 | pyreason-static-head-bounds | implemented | Added static `head_bound` support for compiled PyReason rule heads |
| 2026-03-28 | pyreason-seed-warning-and-demo-reframe | implemented | Narrowed bounded-seed warning to threshold semantics and reframed demo/docs around the real engine boundary |
| 2026-03-28 | pyreason-annotation-gap-remediation | implemented | Closed annotation/design audit gaps around bounded materialization, revocation dual-write, accept validation, and static UI derivation display |
| 2026-03-28 | rule-engine-ext-alignment | implemented | Shared `Rule.engine_ext` became the preferred PyReason rule carrier; adapter compilation/runner/tests/docs aligned to it |
| 2026-03-28 | examples-sdk-schema-cleanup | implemented | Examples and notebooks refreshed to current SDK/schema usage and low-signal legacy scaffolding removed |
| 2026-03-28 | evidence-graph-unified-explain | implemented | Unified `EvidenceGraph` DTO + converters + renderer + candidate-page integration landed across Souffle/PyReason/ProbLog |
| 2026-03-28 | design-branch-closeout | implemented | Final branch closeout: durable `EvidenceGraph` audit delivery, `PyReasonRuleDef` removal, explicit deferred-backlog closure, and archive inventory sync |
| 2026-03-29 | problog-rule-ext-branch-probabilities | implemented | ProbLog branch weighting moved from shared `body_confidences` routing into typed `ProbLogRuleExt`, with SDK/runtime bridge and shared-core signature cleanup |
| 2026-03-29 | problog-fact-probability-canonical-lane | implemented | ProbLog export now prefers `problog/semantic/probability` for fact-level probability and only falls back to `meta.confidence` for legacy data |
| 2026-03-29 | problog-probability-write-lane | implemented | `probability` as first-class `write_protocol` key: `shared/semantic/probability` annotation, auto-derives confidence, ProbLog export reads shared lane |
| 2026-03-29 | pyreason-global-state-isolation | implemented | F-PR-1 fix: `threading.Lock` + `try/finally` for PyReason global singleton isolation and exception-safe cleanup in `run_pyreason()` |
| 2026-03-29 | replace-field-atomicity | implemented | F-CORE-1 fix: preflight validation in `replace_field()` ensures old assertion stays active if new assertion validation fails |
| 2026-03-29 | candidate-support-digest-collision | implemented | F-CORE-2 fix: `_remember_candidate_support()` now raises on same `candidate_id` with different `support_digest` while preserving idempotent same-digest re-registration |
| 2026-03-29 | certainty-child-artifact-eligibility | implemented | F-CORE-3 fix: `check_certainty_artifact_eligibility()` now rejects missing child support artifacts instead of treating them as eligible leaf children |
| 2026-03-29 | core-audit-batch | implemented | Batch closeout for F-CORE-4/5 and F-EG-1/3/4: certainty routing notes, EvidenceGraph cycle/duplicate checks, narrowed static UI exception handling |
| 2026-03-29 | pyreason-adapter-batch | implemented | Batch closeout for F-PR-2/3/4/5/6: bound bool guard, `confidence=0.0` parity, safer pred name parsing, correct annotation binding, shared helper extraction |
| 2026-03-29 | problog-adapter-batch | implemented | Batch closeout for F-PL-1/2/3/4/5: per-candidate trace payload copies, safer result parsing notes, correct annotation binding, bool-confidence fallback, shared parsing extraction |
| 2026-03-30 | evidence-graph-timeline-edges | implemented | F-EG-2 fix: timeline layout now surfaces incoming edge annotations on cards so EvidenceGraph edges remain visible outside tree layout |
| 2026-03-31 | evidence-explain-depth | implemented | Phase 1: fact_meta 嵌入 assertion_fact + source_lines narrative/NL/HTML；Phase 2: explain-steps 端点（三引擎 flat step list）+ render_evidence_steps_html helper；709 tests green |
| 2026-03-31 | 07-explain-steps-demo | implemented | 07 notebook：加 explain_runtime_steps import + source meta for Alice + §2.5/§3.4/§4.3 steps cells + source provenance cells；34→41 cells | Phase 1: fact_meta 嵌入 assertion_fact + source_lines narrative/NL/HTML；Phase 2: explain-steps 端点（三引擎 flat step list）+ render_evidence_steps_html helper；709 tests green |
| 2026-03-31 | rule-label-in-steps | implemented | `support_section.rule_ref_ids` 注入到 evidence tree；`rule_apply` steps 现在输出 `detail.rule_ref_ids` 并使用规则名描述；715 tests green |
| 2026-03-31 | ephemeral-rule-authoring | implemented | `RuntimeSession.ephemeral_rules` + register/list/clear handlers/routes + native evaluate-time registry merge；FS 优先 skip；`evaluate -> accept -> explain-steps` 端到端覆盖；731 tests green |
| 2026-03-31 | llm-integration-surface | implemented | 母蓝图收口：Milestone A（named rule_apply）+ Milestone B（session-scoped ephemeral rules）均关闭；G2 保留为可选增强，G3 继续 deferred |
| 2026-03-31 | runtime-session-schema-readback | implemented | 新增 `GET /v1/runtime/sessions/{session_id}/schema`，返回 `schema_digest + schema_ir`；`GET /sessions/{id}` 保持轻量；735 tests green |
| 2026-04-01 | ephemeral-rule-hardening | implemented | D1 upsert replace、D2 register-time unknown predicate fail、D3 stable `details.error_code/remediation_hint`；`run/evaluate` 错误更适合 agent 恢复；743 tests green |
| 2026-04-01 | session-agent-inventory | implemented | 新增 `GET /sessions/{id}/rules` 与 `GET /sessions/{id}/candidates`；session 级 rule inventory + candidate rediscovery readback 落地；754 tests green |
| 2026-04-09 | agent-layer1-control-plane-mvp | implemented | 新建 `src/factpy_kernel/agent/` Layer 1 控制面：AgentSession/DraftManager/CandidatePayloadCache/KGReadTools/ExplainTools/recovery/framework skeleton；771 tests green |
| 2026-04-09 | agent-layer2-read-first-agent | implemented | RuntimeAPI evaluate/accept 扩展 + EvaluateTools + ReadReviewOrchestrator + 16-tool registry；evaluate→cache→review→accept loop 落地；783 tests green |
| 2026-04-10 | agent-layer3a-structured-write | implemented | RuntimeAPI write_fact 扩展 + WriteTools + ReadReviewOrchestrator structured commit methods + 20-tool registry；confirmed FactDraft → write → committed/checkpoint 落地；795 tests green |
| 2026-04-10 | agent-w2a-exact-retract | implemented | RuntimeAPI retract_fact 扩展 + WriteTools/ReadReviewOrchestrator exact retract 路径 + 22-tool registry；preview→confirm→retract→checkpoint 落地；807 tests green |
| 2026-04-10 | agent-layer4a-native-rule-authoring | implemented | `RuleTools` + orchestrator rule flow + 27-tool registry；validate/compile-preview → ephemeral register → native evaluate → steps review → accept 落地 |
| 2026-04-10 | agent-layer4b-conservative-engine-routing | implemented | `EngineRoutingAdvisor` + `RuleSpec.routing_hint` + orchestrator routing wrapper + 31-tool registry；non-native recommendation 不注册 ephemeral，native override 继续走 Layer 4A；825 tests green |
| 2026-04-10 | agent-layer4c1-document-staging | implemented | 新增 `agent/documents/` 子包 + deterministic staging DTO/parsers/clarity + orchestrator facade + 33-tool registry；4C1 文档 staging 与 runtime 完全解耦；852 tests green |
| 2026-04-10 | agent-layer4c2-draft-bundle-review | implemented | `DraftBundle` / `BundleManager` / `FactDraftSpec` + checkpoint 三元组 + orchestrator bundle review/commit + 39-tool registry；document fact draft 的 batch review/approval 落地；867 tests green |
| 2026-04-10 | agent-layer4c3a-single-segment-extraction | implemented | 新增 `agent/extraction/` 子包 + single-segment LLM extraction + deterministic validation + orchestrator convenience path + 41-tool registry；`DocumentSegment -> FactDraftSpec[] -> bundle` 路径落地；884 tests green |
| 2026-04-10 | agent-layer4c3b-batch-extraction | implemented | `BatchExtractor` + batch metrics / errors + orchestrator batch convenience path + 43-tool registry；单文档 multi-segment extraction → bundle 聚合链路落地；898 tests green |
| 2026-04-10 | agent-layer4c3c-entity-resolution | implemented | `EntityResolver` + merged provenance carrier + orchestrator resolve/bundle convenience path + 45-tool registry；单文档 extraction 去重合并与 multi-segment provenance 保留落地；911 tests green |
| 2026-04-10 | agent-langfuse-minimal-observability | implemented | 新增 `agent/observability/` 子包 + extraction 三层 Langfuse/no-op tracer hook；默认零开销、依赖感知降级、稳定字段 trace；911+ tests green |
| 2026-04-11 | kernel-p0-production-readiness | implemented | H-01 API key auth、H-02 ledger thread-local SQLite + post-commit hooks + shared read lock、H-03 secret hygiene templates/docs/scripts；942 tests green |
| 2026-04-11 | kernel-extraction-response-model-openai-strict-fix | implemented | 4C3-a strict-schema blocker fix：`entity_identity` / `field_values` 改为 typed entry sub-models，validation 正规化回 canonical shapes，新增 OpenAI strict-schema guard tests；959 tests green，B3 real extraction 离开 `Invalid schema` |
| 2026-04-11 | agent-extraction-prompt-schema-alignment-fix | implemented | 4C3-a prompt/schema alignment fix：predicate schema summary 改为 `subject=... field_values=[...]`、predicate arg positional fallback、system prompt 明确 `entity_identity`/`field_values` contract、length mismatch detail 丰富化；967 tests green，B3 rerun 首次产出 valid specs |
| 2026-04-11 | agent-extraction-prompt-residual-patterns-fix | implemented | 4C3-a prompt examples fix：新增两组 WRONG/RIGHT examples 直接打掉 residual `subject leakage` / `multi-entry overpacking` 模式；972 tests green，B3 combined valid rate 57%→76% |
| 2026-03-22 | ecss-domain-validation-and-souffle-provenance-poc | implemented | ECSS domain validation POC with Souffle provenance integration |
| 2026-03-26 | assertion-annotation-store-decision | decision-closed | Froze annotation data model: AnnotationRow schema, shared/adapter namespaces, confidence_kind routing, meta normalization |
| 2026-03-27 | engine-options-runtime-dispatch-decision | decision-closed | Froze engine_options runtime dispatch: timeout/timesteps normalization, per-engine option validation |
| 2026-03-27 | multi-engine-execution-surface-decision | decision-closed | Froze multi-engine execution surface: shared evaluate() entry, mode-based dispatch, CandidateSet normalization |
| 2026-03-27 | multi-engine-execution-surface-design-rationale | decision-closed | Design rationale companion for multi-engine execution surface decision |
| 2026-03-27 | value-carrying-semantics-v1-decision | decision-closed | Froze value-carrying semantics: bounded numeric predicates, PyReason interval model, existence vs value distinction |
| 2026-05-03 | check-operation | implemented | Application-first Check operation; native via `evaluate_native_where` + non-native via evaluate-then-match (souffle SupportArtifact / problog+pyreason ProvenanceEnvelope); Option IV representability gate; `branch_atom_projection=None` reserved slot; first capability shipped on `v0.1-redesign-2026-05-03`; 73 focused + 782 total tests green |
| 2026-05-04 | diagnose-operation | implemented | Application-first Diagnose operation; native pass/fail plus atom-localized failure payload; souffle/problog/pyreason coarse dispatch with observable `EVIDENCE_LOOKUP_MISS`; Q1 Sibling no-Check-call invariant; all 7 §7-Diagnose gates covered; 84 focused + 866 total tests green |
| 2026-05-04 | fact-overlay-capability | implemented | Application-first Fact Overlay Check; native baseline + overlay-applied double-run over projected facts; assertion-scoped `FactValueOverride`; non-native `ENGINE_OVERLAY_NOT_SUPPORTED`; no ledger writes / no live cache contamination / Sibling no-Check-call gates; 72 focused + 944 total tests green |
| 2026-05-05 | why-not-universe-diagnose-capability | implemented | Application-first Why-not Universe Diagnose; explicit finite candidate universe → ordered green/red partition; red rows map Diagnose into Why-not-owned diagnostics; native atom-localized, non-native coarse/unavailable when representable; no evaluator hook / no Check delegation / no ledger write; 99 focused + 1043 total tests green |
| 2026-05-05 | evaluator-frontier-trace-capability | implemented | Native-only evaluator substrate for aggregate failed-frontier tracing; separate `evaluate_native_where_frontier(...)` entrypoint with success parity, sparse per-branch frontier rows, no env dump / trace kwargs / application back-dependency; 36 focused + 1079 total tests green |
| 2026-05-05 | capabilities-e2e-demo | implemented | Deterministic assertion-bearing script composing Check, Diagnose, Fact Overlay Check, Why-not Universe Diagnose, and Evaluator Frontier Trace on one `Person` fixture; smoke test imports and runs demo; 1 demo smoke + 143 related capability tests green |
| 2026-05-05 | canonical-round-story | implemented | Batch 1 round-story wording alignment: Q1-Q5 canonical questions now appear across demo script, notebook markdown, examples README, evidence tutorial §1/§7/§8, and a short capability decision tree; no `src/` changes; focused demo/unit/ruff checks green |
| 2026-05-05 | capability-ergonomics | implemented | Batch 2 application-layer helpers: Fact Overlay override builder, Why-not candidate-universe normalizer, and Store-to-frontier `view_facts` projection helper; demo consumes helpers; no SDK/protocol/runtime algorithm changes; 1088 kernel tests green |
| 2026-05-05 | evaluation-overlay | implemented | Batch 3 EvaluationOverlay core narrowed by Step 0.A to replace/remove; added `EvaluationOverlay` + `FactRemoveAction`, legacy `FactValueOverride` compatibility, projected-row remove runtime, and application-layer remove/overlay helpers; no SDK changes; full kernel 1111 OK / 1 skipped |
| 2026-05-05 | proofframe-rechecker | implemented | Batch 4 ProofFrame Rechecker narrowed by Step 0.A/0.B: Shape B DTOs, native `recheck_proof_frame(...)`, deterministic narrative renderer, strict `not`→`unknown`, no helpers, no SDK/service/agent or shipped capability runtime drift; 33 focused + 1151 full kernel tests OK / 1 skipped |
| 2026-05-05 | rule-disable | implemented | Batch 5a Rule Disable: Shape A `EvaluationOverlay.rule_actions`,single-action native `check_rule_disable_action(...)`,variant rows plus original-frame ProofFrame,Fact Overlay/ProofFrame rule-action rejection guards,no variant support capture,no SDK/service/agent drift; post-archive hardening verified with 145 focused + 1194 full kernel tests OK / 1 skipped |
| 2026-05-05 | rule-condition-replace | implemented | Batch 5b Rule Literal Replace: narrow Const-to-Const native `RuleLiteralReplaceAction`,separate `RuleLiteralReplaceRequest/Result`,single-action `check_rule_literal_replace_action(...)`,variant rows plus original-frame ProofFrame,lower-level `evaluate_where(..., literal_replacements=...)`,Rule Disable non-disable guard,no SDK/service/agent or frontier drift; 194 focused + 1228 full kernel tests OK / 1 skipped |
| 2026-05-05 | add-condition-binding-planner | implemented | Batch 5c Rule Add Condition: narrow filter-only `RuleAddConditionAction`,separate `RuleAddConditionRequest/Result`,single-action `check_rule_add_condition_action(...)`,synthetic `b{branch}.add{action}:{kind}` ProofFrame verdict,lower-level `evaluate_where(..., added_conditions=...)`,new-variable binding planner deferred,no SDK/service/agent or ProofFrame protocol drift; 192 focused + 1265 full kernel tests OK / 1 skipped |
| 2026-05-05 | round-persistence | implemented | Batch 6 Durable Round Persistence: optional `audit/round_events.jsonl`,`RoundEvent` / `RoundSummary`,external buffered recorder with atomic finalize,5 first-slice capability event projections(Check/Diagnose/Fact Overlay/Why-not/ProofFrame),lenient reader warnings,and `AuditQuery` round-event methods; no application runtime audit imports,no SDK/service/agent drift; focused audit tests green |
| 2026-05-06 | evidence-diff | implemented | Batch 7 Evidence Diff: query-derived L4 ProofFrame diff over Batch 6 `proof_frame_result` rows,`ProofFrameDiff` / `FrameDelta` / `AtomDelta` DTOs,`AuditQuery.diff_proof_frames(...)`,partial-round and future-kind warnings,RuleRef degenerate marker,no L5 aggregation/index,no SDK/service/agent/application-runtime/ProofFrame/rule-action drift; 29 focused audit + 1288 full kernel tests OK / 1 skipped |
| 2026-05-06 | public-surface | implemented | Batch 8 Public Surface Decision:docs/checklist-only close-out;`kernel.sdk` remains product public surface,`kernel.application` and `kernel.audit` documented as advanced importable surfaces,no SDK shell/service route/release projection expansion/package-scope change;projection script passed(261 files),README quickstarts print `Alice`,no Python code or release-branch drift |
| 2026-05-06 | round-story-demo-refresh | implemented | Canonical current examples refresh: archived sectional root examples under `examples/archive/`,added assertion-bearing `round_story_full_demo.py` plus notebook wrapper,rewrote examples README,and replaced old demo smoke test with exact phase-summary coverage; no SDK/service/release-surface expansion |
| 2026-05-07 | round-story-demo-suite | implemented | Chaptered notebook suite: replaced root all-in-one notebook with four self-contained inline notebooks (01 SDK+Check+Diagnose / 02 Fact Overlay+Why-not+Frontier / 03 ProofFrame+rule overlays / 04 round persistence+ProofFrame diff); each notebook imports the real `kernel.application` / `kernel.audit` / `kernel.sdk` APIs directly and asserts on every capability call; `round_story_full_demo.py` retained as smoke target; one mid-implementation reversal (shell-style → inline-style) recorded in §6 deviation; no SDK/service/release-surface expansion |
| 2026-05-07 | application-ergonomic-helpers-extension | implemented | Application-layer ergonomic helpers extension: 8-helper coverage achieved (3 Batch 2 helpers preserved pre-Gap β + 5 new Phase 1-5 builders — `build_check_request` / `build_diagnose_request` / `build_proof_frame_recheck_request` / 3 rule overlay sub-builders / `build_round_event_payload`); Phase 0-6 across 13 commits with 6 strict audits + final cumulative close-out audit; 292 focused tests; `capability_helpers/` package layout with shared `_binding.py` (origin-package rule + cycle guard + dataclass walk for nested SDK + class-object detection); Gap β `OriginPackageError` enforced for Phase 1-5 builders (Batch 2 helpers preserve pre-Gap β behavior, only static no-`kernel.sdk` import enforced); 5-kind round event narrowing (`check_result` / `diagnose_result` / `fact_overlay_result` / `why_not_result` / `proof_frame_result`); rule overlay `note=None` pass-through recorded |
| 2026-05-07 | walker-mechanism | implemented | Application-layer walker mechanism (Tier 2 advanced importable): 8 view classes — IRBodyWalker / IRAtomView (B1 IR walker) + FrozenTupleView + AtomKeyView (B2 keys) + SupportArtifactView / AssertionView (B2 cross-reference) + ProofFrameView + ProofFrameDiffView (B2 per-DTO views); Phase 0-7 across 14 commits with 5 strict audits + final cumulative pre-close-out audit; 126 focused tests + 116 subtests; `#P1` carve-out for FrozenTupleView content-equality (vs IRAtomView surface/underlying split); shared `_freeze.py` recursive freezer with cycle detection + unhashable rejection; B3 audit/store stream walker reserved future-only with dormant `UnboundedStreamError`; published snapshot at `v0.1-public-surface-walker-2026-05-08` |
| 2026-05-08 | l-direction-g1-check-diagnose | implemented | First L direction (Tier 1 SDK shell) blueprint: `SDKStore.check(derivation, binding, *, engine="native", registry=None) -> CheckResult` + `SDKStore.diagnose(...) -> DiagnoseResult` instance methods over existing `Derivation` lowering pipeline (`_compile_derivation_input` + `_compiled_derivation_plan_to_application`); documented passthrough returns raw `CheckResult` / `DiagnoseResult`, NOT re-exported from `kernel.sdk.__all__` (length unchanged at 34); `CapabilityHelperError` + `OriginPackageError` from A's helpers remap to `SDKStoreError` with `__cause__` chaining and `path="$.check"` / `$.diagnose`; Q1 Sibling discipline verified runtime + static (sdk_diagnose does not call sibling Check shell); B walker dependencies inactive for v1 per §5.2 lock; Phase 0-3 across 5 commits with phase-end strict audits (0 blocker / 0 clarify / 0 minor each) + final cumulative audit; 37 focused G1 tests + 1499 full kernel green / 1 skipped; §5.7 falsifier surfaced and corrected blueprint `Rule → Derivation` drift in §1/§2/§3/§4.4/§5.7/§6 as Step 0 architectural refinement; establishes SDK-shell template for G2-G5 with G2 Step 0 trigger to re-evaluate `kernel/sdk/shells/` subpackage migration |
| 2026-05-08 | l-direction-g4-why-not-frontier | implemented | Second L direction (Tier 1 SDK shell) blueprint: `SDKStore.why_not(derivation, candidates, *, engine="native", registry=None) -> WhyNotUniverseResult` over explicit finite candidate universes; accepts SDK `Derivation` only and candidate rows as `Sequence[Mapping[str, Any] \| Sequence[Any]]` mirroring A's `build_why_not_candidate_universe(...)`; returns raw application `WhyNotUniverseResult` documented passthrough, not re-exported from `kernel.sdk.__all__` (length unchanged at 34); maps derivation, dependency, candidate, request DTO, and runtime errors to `SDKStoreError` with `__cause__` chaining and `$.why_not*` paths; Frontier remains advanced importable (no `SDKStore.frontier`, no SDK/core frontier import); Phase 0-3 with strict phase audits, 76 SDK shell targeted tests + 1538 full kernel tests OK / 1 skipped; G2 inherits the `kernel/sdk/shells/` migration trigger before adding more SDK shell files |
| 2026-05-08 | l-direction-g2-fact-overlay-proofframe-recheck | implemented | Third L direction (Tier 1 SDK shell) blueprint: `SDKStore.check_fact_overlay(derivation, binding, overlay, *, engine="native", registry=None) -> FactOverlayCheckResult` + `SDKStore.recheck_proof_frame(support_artifact, overlay) -> ProofFrameRecheckResult` instance methods. SDK accepts SDK `Derivation` + `$`-prefixed binding mapping + raw `EvaluationOverlay` for Fact Overlay, and raw `SupportArtifact` + raw `EvaluationOverlay` for ProofFrame Recheck (per §5.1 + §5.2 cross-cutting precedent: raw application protocol DTOs may cross the SDK boundary as input when frozen application-canonical AND no SDK alternative without inventing new outward surface or breaking Sibling discipline); returns raw application `FactOverlayCheckResult` / `ProofFrameRecheckResult` (documented passthrough; not in `kernel.sdk.__all__`, length still 34); 5+4 `SDKStoreError` remap paths (`$.check_fact_overlay.{derivation,binding,dependencies,request}` + `$.recheck_proof_frame.{support_artifact,overlay,request,}`) with `__cause__` chaining; Q3/Batch-4 Sibling discipline enforced static + runtime (no SDK shell calls another). **Phase 0 hygiene activated `kernel/sdk/shells/` subpackage**, migrating G1+G4 shells (`check.py` / `diagnose.py` / `why_not.py` / `_validation.py`) plus G2 new files; G1 + G4 archived invariant tests retrofit per `#P1` carve-out (`test_g{1,4}_modules_are_flat_and_no_shells_package_exists` → `test_g{1,4}_modules_live_in_shells_subpackage`, assertions inverted); G1+G4 published snapshot branches NOT touched. 26 G2 contract tests + 6 G2 invariants + retrofit G1+G4 invariants; full kernel 1571 OK / 1 skipped; ruff clean. Validators kept local in `proof_frame.py` per user guidance; extraction to `shells/_validation.py` deferred until G3/G5 also need them. G3 (rule overlays) inherits the active `kernel/sdk/shells/` subpackage and the §5.1+§5.2 cross-cutting precedent. |
| 2026-05-08 | l-direction-g3-rule-overlays | implemented | Fourth L direction (Tier 1 SDK shell) blueprint: three rule-overlay methods `SDKStore.check_rule_disable(rule, support, *, branch_index, atom_index, overlay=None, note=None) -> RuleDisableResult`, `SDKStore.check_rule_literal_replace(rule, support, *, branch_index, atom_index, literal_path, old_literal, new_literal, overlay=None, note=None) -> RuleLiteralReplaceResult`, `SDKStore.check_rule_add_condition(rule, support, *, branch_index, added_atom, overlay=None, note=None) -> RuleAddConditionResult`. SDK accepts SDK `Rule` (lowered through existing `_compile_rule_input` to `RuleSpec` per §5.2 — substrate IR `RuleSpec` rejected at SDK boundary, scoping G2 §5.1+§5.2 cross-cutting precedent to `kernel.application.protocol` DTOs only) + raw `SupportArtifact` + raw `RuleLiteralPath` / `RuleAddedAtom` + optional `EvaluationOverlay` (None or empty only; rule-action overlay constructed internally by A helper); returns raw application result DTOs (documented passthrough; `kernel.sdk.__all__` length still 34). Six `SDKStoreError` remap paths per method: `$.<method>.{rule,support,overlay,dependencies,request}` + base `$.<method>`. Method naming locks Group A (`check_rule_*`) over Group B (`disable_rule` / `replace_rule_literal` / `add_rule_condition`) per §5.7 falsifier — Group A signals what-if semantics consistent with G1 `check` / G2 `check_fact_overlay`; Group B `disable_rule` would semantically collide with persistent-write family `add` / `set` / `retract`. **Phase 0 fired the G2 §5.2 deferred validator-extraction trigger** — promoted ProofFrame Recheck's local `_validate_support_artifact` to shared `validate_support_artifact` in `kernel/sdk/shells/_validation.py`, plus added `validate_rule` and `validate_optional_evaluation_overlay`; ProofFrame Recheck migrated to shared validator (behavior-preserving). 16+17+17 = 50 contract tests across three rule-overlay methods (Phases 1/2/3) + 6 G3 invariants (Phase 4 mirror of G1/G4/G2) + 19 new shared validator unit tests (Phase 0; total `test_sdk_validation.py` = 30 tests) + 3 `#P1` retrofits on pre-G3 application-runtime boundary tests `test_no_sdk_rule_{disable,literal_replace,add_condition}_surface` (one per Phase 1/2/3, mirroring G2 Phase 0's G1+G4 retrofits). Full 8-shell Sibling discipline scope active (each new shell tests against all 7 sister shells via runtime patch + static source scan). 6-phase commit chain: Phase 0 `5437cd6` / Phase 1 `7f761bd` / Phase 2 `3526cc1` / Phase 3 `63c47ea` / Phase 4 `df772e2` / close-out (this commit). Phase-end test counts: 1593 → 1609 → 1626 → 1643 → 1649 OK (zero regression at every gate). G1 + G4 + G2 published snapshot branches verified untouched at every phase. SDKStore method count after G3: 39; total `kernel/sdk/shells/` modules: 8 (5 prior + 3 G3). G3 §5.1+§5.2 substrate-IR-out clarification available for G5 + post-L SDK ergonomics redesign. |
| 2026-05-09 | l-direction-g5-round-events-proofframe-diff | implemented | Fifth and final L direction (Tier 1 SDK shell) blueprint, **closes the L Direction sequence G1→G4→G2→G3→G5**: single SDKStore method `SDKStore.diff_proof_frames(round_a_id, round_b_id, round_a_events, round_b_events, *, warnings=(), include_unchanged=False) -> ProofFrameDiff`. SDK accepts two non-empty round-id strings + two raw `tuple[RoundEvent, ...]` (`kernel.audit.round_events.RoundEvent`, frozen audit DTOs covered by the new layer rule) + optional `tuple[WarningDTO, ...]` + boolean `include_unchanged`; returns raw application-canonical `ProofFrameDiff` (`kernel.audit.proof_frame_diff.ProofFrameDiff`); not in `kernel.sdk.__all__` (length still 34). SDK shell is **pure** — no Store / no registry / no engine arg / no IO — mirroring `kernel.audit.proof_frame_diff.build_proof_frame_diff(...)` 1:1. **Round events recorder lifecycle (`start_round` / `record_round_event` / `finalize_round`) explicitly deferred to `kernel.audit.round_events` advanced importable per §5.1** (recorder is mutable / stateful / persistence-adjacent; would be the first L method returning a non-frozen object; existing UX `examples/round_story_full_demo.py` already imports it directly). **8-path** `SDKStoreError` remap (post-pre-publish-Blocker-fix): `$.diff_proof_frames.{round_a_id,round_b_id,round_a_events,round_b_events,warnings,include_unchanged,request,}` + base — **6 inline `isinstance` pre-validation paths** + `ProofFrameDiffError` from helper-internal validation to `.request` + defensive `Exception` to base. The pre-publish verification round added the `.include_unchanged` boundary (strict `isinstance(.., bool)`; `1`/`0` rejected, matching the `return_display_meta` SDK precedent) — original lock at `2f81b56` was 7-path and missed the typed-bool boundary. NO `ProtocolShapeError` / `.dependencies` / `CapabilityHelperError` paths (no intermediate request DTO; no derivation lowering; no A helper between SDK and `build_*`). Method naming locks Group A `diff_proof_frames` over Group B `proof_frame_diff` per §5.7 falsifier — Group A is verb-first action shape; Group B would collide with the DTO name `ProofFrameDiff` (same anti-pattern G3 §5.7 rejected). **§5.3 cross-cutting precedent layer rule encoded verbatim in §6 invariants:** "frozen canonical DTO above `kernel.core` using `kernel.application.protocol` vocabulary" — keeps `kernel.audit` frozen DTOs (`RoundEvent`, `ProofFrameDiff`, `FrameDelta`, `AtomDelta`, `FrameIdentity`, `FrameStatusChange`, `EventReference`) IN scope; preserves G3 substrate-IR-out carve-out (`RuleSpec` excluded by "above `kernel.core`"). **18 contract tests** (16 at original Phase 1 close + 2 added in pre-publish polish for the new `.include_unchanged` boundary) + 6 G5 invariants (with **novel audit-import allowlist check** at class 4 instead of G1-G3's blanket `kernel.audit` reject — allows `kernel.audit.proof_frame_diff` and `kernel.audit.round_events` as the explicit A-side dependencies, distinct from broader `kernel.audit` privates) + **zero `#P1` retrofits** (audit-layer tests never asserted "no SDK surface"). Full 9-shell Sibling discipline scope active forward-only convention (G5 shell tests against all 8 prior sisters; prior shells' tests stay frozen at their publication moment per Path B immutability). 3-phase commit chain + 1 pre-publish polish: Phase 1 `a961e1d` / Phase 2 `754d2a2` / Phase 3 close-out `480ebc6` / pre-publish polish `d4ceb3e` (published HEAD). Phase-end test counts: 1677 → 1683 → 1685 OK (zero regression). G1+G4+G2+G3 published snapshot branches verified untouched at every phase. SDKStore method count after G5: 40; total `kernel/sdk/shells/` modules: 9. **L Direction sequence closed; post-L SDK ergonomics redesign blueprint now unblocked** (per `feedback_sdk_ergonomics_redesign_target`). Cold-import circularity around `kernel.audit.proof_frame_diff` (pre-existing, worked around inline in test) and `store.py:1024/1154` legacy paths (out-of-scope flag from G3 verification round) tracked for separate post-L hygiene blueprint. |
| 2026-05-09 | post-l-sdk-ergonomics-redesign | implemented | Post-L SDK ergonomics redesign after L Direction close-out: exports `FactGraph` as a literal alias of `SDKStore` and organizes the existing 30 flat user-facing methods through 8 taxonomy namespaces + 2 `what_if` sub-namespaces (`schema`, `read`, `write`, `eval`, `what_if`, `what_if.fact_overlay`, `what_if.rule`, `audit`, `package`, `views`). Runtime change is additive only: flat `SDKStore.<method>` / `FactGraph.<method>` remains permanent foundational API with no deprecation warning, no removal plan, no package rename, and no shell-behavior rewrite. `kernel.sdk.__all__` intentionally changes 34→35 with only `FactGraph`; private manager classes stay out of `__all__` and enforce read-only mutation guards. Phase 1 (`af99c87`) landed `FactGraph` + 9 manager classes + alias parity/namespace shape tests; Phase 2 (`31889ae`) landed taxonomy-first docs + redesign invariants; Phase 2 audit-fix (`380e391`) corrected stale README L Direction wording and stale test-count baseline; close-out archives this blueprint and prepares publish refs only. Focused redesign + G1-G5 invariant suite: 98 OK; full discovery records the known environment-only `kernel.adapters.problog` cold-start circular import error. Publish pending explicit user authorization for `v0.1-post-l-sdk-ergonomics-redesign-2026-05-09` and Path B 6th combined snapshot `v0.1-public-surface-helpers-walker-l-g1-l-g4-l-g2-l-g3-l-g5-post-l-redesign-2026-05-09`. |
| 2026-05-10 | primary-identity-domain-semantics | implemented | Primary-first SDK batch identity semantics: `tx.entity(...)` now requires primary identity at handle creation after literal/default_factory materialization; multi-primary anchors must be complete upfront; `bind(...)` remains mutating non-primary completion and rejects primary identity kwargs; entity/handle writes require full coordinates while flat assertion-id retraction stays unaffected. Storage remains full-coordinate `idref_v1`; read APIs and rule authoring are unchanged. Added 10 focused tests in `test_sdk_batch_primary_identity.py`; focused primary/batch/read/schema suite passed; `ruff` and `git diff --check` clean; full discovery still reports the pre-existing Problog cold-import circularity. Read-side primary-anchor/domain collection design is carried by archived spin-off `2026-05-10_primary-anchor-domain-read`. |
| 2026-05-10 | primary-anchor-domain-read | implemented | Read-side primary-anchor support via relaxed SDK `find(...)` partial identity filters: no new read method, DTO, helper, logical ref, or export; `get(...)` remains full-coordinate; partial results expose full `snapshot.identity` with `identity_available=True`; implementation is SDK-facade-only over existing application `mode="find"` hydration. Application protocol, storage/index, authoring, write path, release refs, and `v0.1.0-rc.1` remain unchanged. Added `test_sdk_find_partial_identity.py`; focused SDK/application suite 29 OK; full discovery still reports the pre-existing Problog cold-import circularity. |
| 2026-05-10 | assertion-selection-crud-ergonomics | implemented | SDK assertion selection ergonomics: `EntitySnapshot.field(...).active/history/at/version` and `EntitySnapshot.assertions.<field>` now return tuple-compatible `AssertionRecordSet` helpers with `.where(...)`, `.one()`, `.all()`, and `.first()` while preserving tuple iteration/indexing/slicing/concatenation/multiplication. `write.retract(asrt_id)` remains asrt-id-only; no selector/predicate retract overload, no new `fg.read.*` method, no new `kernel.sdk.__all__` export, and no application/core/authoring changes. Added `test_sdk_assertion_record_set.py`; focused helper suite 8 OK, related SDK suite 55 OK, `ruff` and `git diff --check` clean; full discovery still reports the pre-existing Problog cold-import circularity. |
| 2026-05-11 | frozen-assertion-view-model | implemented | Frozen assertion view model first slice: `fg.views.create/update(...)` now accepts legacy `ViewSpec`, `asrt_ids=[...]`, or `asrts=[...]` as mutually exclusive payloads; `FrozenAssertionView.asrt_ids` stores immutable `frozenset[str]` membership; `fg.assertions.by_id/by_ids(...)` provides readback; `AssertionRecordSet` gains additive `.at(...)`, `.version(...)`, and `.by_id(...)` filters. Legacy `ViewSpec` projection behavior remains compatible; frozen views are rejected by `fg.read.find(...)` / `fg.run(..., return_display_meta=True)` with by-id workaround guidance; no application/authoring/core/`sdk.__init__` changes and `kernel.sdk.__all__` remains 35. Focused frozen-view suite 31 OK, related SDK suite 39 OK, full kernel discovery 1814 OK / 1 skipped. |
| 2026-05-11 | readpolicy-call-site-migration | implemented | ReadPolicy call-site migration: removed `ViewSpec` from the public and core import surface; added `ReadPolicy` as the read-time resolution/display policy DTO exported by `kernel.sdk` (`__all__` 35→36); `find(...)` and `run(...)` now use value-only `policy=ReadPolicy(...)` while old `view=` calls tombstone with redirect semantics, including explicit `run(view=None)`. `fg.views` is frozen assertion membership only, has no built-in or reserved `default`, and no longer accepts policy payloads. Service runtime removed the named policy registry, `RuntimeSession.views`, `view_name`, and the five runtime-view endpoints; wire policy is inline under `policy` with `respect_revocations`. SDK/service docs, OpenAPI, and `examples/05_sdk_assertion_views.ipynb` were synced; `scripts/check_legacy_view_syntax.sh` added as the release-facing grep gate. Critical rejection suite 11 OK, focused ReadPolicy/frozen/service suite 67 OK, kernel unittest discovery 1848 OK / 1 skipped, release dry-run for `v0.1.0-rc.3` passed. |
| 2026-05-11 | uncertainty-transmission-layer | implemented | Phase 1 raw uncertainty data contract: user-authored raw uncertainty now uses paired `raw_kind` / `bound` meta, mirrored to `meta_rows` for assertion selection and projected canonically to `shared/semantic/raw_kind` and `shared/semantic/bound`; `probability`, `bound_lower`, and `bound_upper` are removed write meta keys; `confidence` and adapter ProbLog/PyReason output lanes remain unchanged. Focused write / SDK suite 38 OK; ProbLog exporter 9 OK and PyReason session 60 OK with the known import-order workaround. |
| 2026-05-11 | derivation-mode-to-callsite | implemented | Track 3 / A1 public-surface decomposition: removed public SDK `Derivation.mode`, stopped derivation payload mode emission, rejected structured derivation `mode`, retained compiled `native` only as an internal bridge, and rewired service runtime derivation evaluation to request-level `engine` while rejecting `derivation.mode` and top-level service `mode`. SDK call-site `evaluate(..., mode=...)` remains the user-facing engine selector; release-facing docs no longer teach `Derivation(..., mode=...)`. G1 red baseline 5 failures became green; targeted migration/adapters/service suite 84 OK, A1 test 7 OK, import-order-compatible explain/declaration checks 48 OK and 23 OK. |
| 2026-05-11 | branch-confidence-decomposition | implemented | Track 3 / A2 public-surface decomposition: replaced public SDK `Body` with `Branch`, removed branch-level `confidence`, rejected public authoring/service `body_confidences`, and kept internal compiled `body_confidences` only as the temporary ProbLog bridge. `Branch([...])` is now structure-only; `ProbLogRuleExt.branch_probabilities` remains the temporary public fallback until A3 / Phase C. G1 red baseline 7 failures became green; A2 targeted suite 10 OK, A2 + ProbLog + SDK invariant + A1 regression suite 72 OK, import-order-compatible artifact/evidence domain suite 50 OK, release-facing stale syntax grep has zero public `Body` hits. |
| 2026-05-11 | engine-ext-decomposition | implemented | Track 3 / A3 public-surface decomposition: removed public SDK `Rule.engine_ext` / `Derivation.engine_ext`, stopped SDK evaluate/shells from reading object-level `engine_ext`, rejected public authoring/service `engine_ext`, and kept `EngineExtBase`, `CompiledDerivationPlan.engine_ext`, core evaluate bridge, ProbLog extension internals, PyReason extension internals, and the A2 `legacy_body_confidences` bridge as internal-only transitional paths. Release-facing docs no longer teach public `Rule(..., engine_ext=...)` / `Derivation(..., engine_ext=...)`; remaining mentions are rejection, adapter/internal bridge, or future SemanticsProfile context. G1 red baseline 8 failures became green; A3 targeted suite 12 OK, targeted A3 + ProbLog/PyReason/SDK regression suite 240 OK with known import primer order, stale-syntax grep clean. |
| 2026-05-12 | condition-weights-decomposition | implemented | Track 3 / A4 boundary-clarification slice: retained public `Rule.condition_weights`, authoring/service/agent payload support, registry preservation, confidence-kind routing, and certainty-summary materialization while reclassifying weights as certainty/explain projection input rather than engine adapter semantics or `where` execution semantics. Release-facing docs now point future runtime configuration for this lane to `SemanticsProfile.certainty_projection`; March 2026 boundary remains intact (`condition_weights` stays out of `where`, `where_ast`, evaluator IR, ProbLog syntax, and PyReason syntax). A4 guard suite 9 OK, declaration metadata + core certainty annotation suite 43 OK, A4 + certainty explain contract suite 50 OK, docs grep gates clean. |
| 2026-05-12 | semantics-profile-scaffolding | implemented | Track 3 / B scaffolding slice: introduced `kernel.core.semantics.SemanticsProfile` as a frozen core value object with version / engine / fallback validation, generic `rule_projection` shape checks, locked uncertainty projection policies, B-time temporal `mode="none"` boundary, and pure `inspect_semantics_profile(...)`. SDK and service runtime reject `semantics` / `semantics_profile` in B; `SemanticsProfile` is not exported from `kernel.sdk`, application protocol gains no profile field, adapters do not import or consume it, and A1-A4 internal bridges remain intact. Standalone design note records future `engine=` naming preference for Track 3 / E. B suite 24 OK, A1-A4+B focused suite 62 OK, docs grep gates clean. |
| 2026-05-12 | problog-semantics-profile-migration | implemented | Track 3 / C adapter-consumption slice: ProbLog now consumes `SemanticsProfile.rule_projection.problog` through the core `Store.evaluate(..., mode="problog", semantics_profile=...)` path, normalizing `kind="branch_probability"` / `target="branch:{index}"` entries into adapter-local `ProbLogRuleExt.branch_probabilities`. Omitted branches default to `1.0`; profile, `ProbLogRuleExt`, and legacy `body_confidences` carriers must materialize matching tuples; exporter remains profile-agnostic; SDK/service profile kwargs remain rejected until E; PyReason remains deferred to D. C focused suite 15 OK, B semantics suite 24 OK, ProbLog bridge/eval suite 19 OK with known import primer, docs grep gates clean. |
| 2026-05-12 | pyreason-semantics-profile-migration | implemented | Track 3 / D adapter-consumption slice: PyReason now consumes `SemanticsProfile.rule_projection.pyreason` and `SemanticsProfile.temporal_projection` through the core `Store.evaluate(..., mode="pyreason", semantics_profile=...)` path, normalizing body/head interval and `timestep_delay` entries into `PyReasonRuleExt` and mapping `fixed_timesteps` / `valid_time_boundaries` into PyReason run timesteps plus EDB `active_from` / `active_to`. SDK/service profile kwargs remain rejected until E; C's ProbLog profile path remains green; Souffle stays profile-agnostic; no profile-derived values are stored. D focused suite 25 OK, B+C+D suite 66 OK, A1-A4+B+C+D focused suite 104 OK, PyReason regression 61 OK with known import primer, docs grep gates clean. |
| 2026-05-12 | sdk-service-semantics-callsite | implemented | Track 3 / E final integration slice: SDK and service runtime now expose the public profile call-site as `engine=` plus `semantics=`, exporting `SemanticsProfile` from `kernel.sdk`, adding `fg.eval.inspect_semantics(profile)`, accepting top-level service `semantics` JSON, and mapping public names to internal `mode=` / `semantics_profile` runtime fields. Public SDK `mode=` and `semantics_profile=` are hard-rejected; unsupported engines and profile/engine mismatches reject without silent ignore; Check / Diagnose / Fact Overlay / Why-not shells remain profile-free by design. E suite 22 OK, B+C+D+E focused suite 88 OK, combined focused regression 222 OK, public stale-teaching grep clean. Track 3 complete. |
| 2026-05-12 | branch-identity-rule-inspect | implemented | Track 1 post-Track-3 substrate slice: added optional keyword-only `Branch([...], id=...)`, pure `fg.rules.inspect(rule_or_derivation)`, inspect-only branch metadata with fallback `b0` / `b1` and atom ids, and public single-head `Derivation` hard-cut at construction plus SDK evaluate/registry boundaries. No authoring payload, rule IR, compiled plan, registry state, or adapter branch-id propagation; `condition_weights` remains positional. Track 1 targeted suite 15 OK, affected SDK/shell suite 101 OK, Track 3 preservation suite 88 OK, stale SDK `__all__` invariant cleanup 81 OK, full kernel discovery 1994 OK / 1 skipped, docs grep gates clean. |
| 2026-05-12 | public-semantics-api-redesign | implemented | Track 2 public semantics API slice: added SDK-local `ProbLogSemantics` and `PyReasonSemantics` wrappers, engine auto-derivation from `semantics=`, branch-id lowering into canonical `SemanticsProfile`, wrapper inspection preview, compiled/service canonical-shape boundaries, and SDK `__all__` migration. `SemanticsProfile` remains public advanced/canonical; what-if shells remain profile-free; PyReason `branch_bounds` was intentionally left for the follow-up Track 3-post slice and is now completed by `pyreason-branch-bounds-carrier`. Track 2 suite 17 OK, Track 1+B/C/D/E preservation 103 OK, SDK `__all__` invariant suite 81 OK, combined focused suite 201 OK, docs grep gates clean. |
| 2026-05-12 | pyreason-branch-bounds-carrier | implemented | Track 3-post final post-Track-3 slice: added public `PyReasonSemantics.branch_bounds`, SDK explicit/fallback branch-id lowering into canonical `rule_projection.pyreason` `branch:{index}` interval entries, adapter-local `PyReasonRuleExt.branch_head_bounds`, and per-branch PyReason head interval output. Branch-specific bounds override global `head_bound`; empty maps are no-op; service/compiled canonical boundaries remain intact. Track 3-post suite 16 OK, Track 2 suite 17 OK, Track 1+B/C/D/E preservation 103 OK, PyReason regression 71 OK, docs grep gates clean. Post-Track-3 3-track plan complete. |
| 2026-05-12 | public-inference-factgraph-create | implemented | First lifecycle/asset cleanup slice after the post-Track-3 plan: hard-cut public SDK `Derivation` to `Inference`, added canonical `FactGraph.create(schema_classes=[...])`, renamed public what-if shell parameters to `inference=`, removed public compiled-plan SDK escape hatches while preserving private substrate capability, and renamed SDK docs to `03_rules_and_inferences.en.md`. Substrate vocabulary remains `derivation_id`, `derivation_v1`, `CompiledDerivationPlan`, and service/registry/application names until future slices. Public inference suite 16 OK, full kernel discovery 2042 OK / 1 skipped, `git diff --check` clean. |
| 2026-05-12 | inference-wire-registry-vocabulary | implemented | Lifecycle/asset cleanup follow-up: hard-cut service runtime routes to `/inferences/evaluate` and `/inferences/accept`, renamed runtime request/envelope keys to `inference` / `inference_id`, moved registry service and SDK/FileAuthoringRegistry public methods to inference vocabulary, and changed registry manifest/path/per-spec JSON to `inferences` / `inference_id` with a FileAuthoringRegistry translation seam back to compiler `derivation_id`. Candidate payloads and application/core/proof substrate remain derivation-named by design. Blueprint suite 15 OK, kernel discovery 2057 OK / 1 skipped, docs stale public-route grep clean. |
| 2026-05-12 | authoring-asset-persistence-facade | implemented | Lifecycle/asset Blueprint 2: added graph-bound authoring registry attachment via `FactGraph.create(..., registry_root=...)` / `registry=`, introduced `kernel.application.authoring_runtime`, exported `SavedRuleRef` / `SavedInferenceRef`, added `fg.rules.save/load/list/get` and persistence-only `fg.inferences.save/load/list/get`, preserved `fg.rules.inspect`, added schema auto-upsert with digest mismatch rejection, and removed `SDKRegistry` from public `kernel.sdk` while preserving `kernel.sdk.registry.SDKRegistry` as an advanced/internal path. Authoring facade + Blueprint 1 + wire/registry + Track 1 suite 74 OK; SDK export invariant-focused suites 97 OK; stale public `SDKRegistry` / `register_derivation` grep clean. Blueprint 3 workspace lifecycle is unblocked. |
| 2026-05-12 | factgraph-workspace-lifecycle | implemented | Lifecycle/assets Blueprint 3 capstone: added `FactGraph.create(..., path=...)`, compact v1 workspace layout (`factgraph_workspace.json`, `ledger.db`, `registry/`), Level 4 save scope, `fg.save(path=None)`, `FactGraph.load(path, schema_classes=[...])`, and `kernel.application.workspace_runtime`. Load validates manifest, ledger, registry, and provided-class schema digests; `from_schema_classes(...)` remains lower-level without `path=`; package export, artifact sidecars, views, audit/evidence, and query persistence remain explicit non-goals. Workspace suite 29 OK, lifecycle focused preservation 103 OK, SDK invariant suite 81 OK, docs grep/diff checks clean. Lifecycle/assets sequence complete. |
| 2026-05-13 | v0.1.0-rc.3-release | implemented | Release-prep dry-run for `v0.1.0-rc.3`: confirmed rc.3 is the next candidate after existing rc.2, synced the release surface allowlist for lifecycle/assets renames and semantics/runtime modules, and verified `scripts/release.sh v0.1.0-rc.3 --source-ref HEAD --dry-run --yes` from clean source `fa982858`. Projection produced 361 files; staging verification passed 1633 OK / 1 skipped; dry-run release commit would be `d048c6ac`; no refs were pushed. |
| 2026-05-13 | schema-mutation-lifecycle | implemented | Additive schema mutation first slice: added `fg.schema.add(EntityCls)` / `fg.schema.add(schema_classes=[...])`, public `SchemaAddResult`, and `kernel.application.schema_mutation_runtime` with strict seven-category additive validation. Active SDK/core schema state, ledger schema digest, and graph-bound registry schema IR update immediately; workspace manifest digest updates only on explicit `fg.save(...)`. Re-adding equivalent classes is no-op; delete/deprecate/update/migrate remain deferred. Schema mutation suite 39 OK, lifecycle/assets preservation 88 OK, SDK invariants 57 OK. |
| 2026-05-13 | schema-field-add-lifecycle | implemented | Additive schema mutation second slice: `fg.schema.add(...)` now accepts same-entity replacement classes that add non-identity fields, returns `SchemaAddResult.added_fields`, rejects superseded classes/descriptors, preserves no-backfill absence semantics (`None` / `()`), and reuses ledger/registry digest anchors plus workspace save-time behavior. Field-add suite 33 OK, schema/lifecycle preservation + SDK invariant stack 217 OK, docs synced. |
| 2026-05-13 | confidence-evidence-meta-release-cleanup | implemented | Defensive release cleanup: legacy `CandidateSet.confidence` / `confidence_kind` and generic `meta.confidence` remain internal/legacy carriers only; accept no longer persists them to assertion meta, write protocol no longer projects them to shared annotations, service candidate DTOs omit them by default, ProbLog/PyReason use adapter-native semantic lanes, and candidate evidence trees do not lift generic confidence into default evidence/certainty display. Confidence cleanup baseline 11 OK; full kernel discovery 2198 OK / 1 skipped. |
| 2026-05-12 | rule-query-inference-head-semantics | implemented | Documentation-only design capture: records the future Rule / Query / Inference head-semantics redesign concern, keeps it non-authoritative under `docs/references/working/design-points/`, and leaves current implementation/module docs unchanged. |
| 2026-05-13 | release-documentation-readiness | implemented | Release documentation readiness cleanup: refreshed root README and examples README for `FactGraph.create`, public `Inference`, 10 SDK namespaces, current SDK shells, authoring facades, schema mutation, and workspace lifecycle; removed default legacy confidence fields from service candidate examples; reframed remaining `derivation_id` / `derivation_version` as substrate; removed extraction route/tag/components from the kernel OpenAPI. Focused stale-grep gate, OpenAPI parse/ref check, and `git diff --check` all passed. |
| 2026-05-13 | official-kernel-docstrings-and-tutorials | implemented | Official kernel docs slice: added public SDK hover docstrings, created the quickstart-only official docs tree under `docs/official/kernel/`, folded temporary concepts/reference material back into quickstart pages, added per-page Syntax checklists plus `quickstart/namespace-map.md`, and updated the docs baseline to enforce the quickstart-only tree. Quickstart Python blocks OK, official docs baseline 7 OK, focused preservation suite 276 OK. |
