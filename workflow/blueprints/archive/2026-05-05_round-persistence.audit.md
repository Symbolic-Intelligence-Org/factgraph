# Task Blueprint Audit: Durable Round Persistence(Batch 6)

- Blueprint: [2026-05-05_round-persistence.md](./2026-05-05_round-persistence.md)

## Event Log

| Date | Stage | Event | Notes |
| --- | --- | --- | --- |
| 2026-05-06 | draft | Blueprint created | Initial Batch 6 draft opened on `v0.1-round-persistence-2026-05-05` off `9ab282d`. Scope is deliberately Step-0-first:the draft does not assume a universal event schema,does not modify Store/sidecar semantics,and keeps Batch 7 diff/aggregation out of scope. |
| 2026-05-06 | draft | Pre-commit framing review pass 1 | Added three persistence-specific falsifiers before draft commit:event source mechanism(runtime emit vs wrapper/explicit recorder),explicit round lifecycle vs implicit time window,and atomic finalize vs streaming append. These are true Step 0 forks for persistence and now appear in §5.1 plus §5.3 carry-overs. |
| 2026-05-06 | draft | Step 0.A spike completed | Synthesized 15 falsifier verdicts from source-grounded evidence(2 parallel Explore agents covering `kernel.audit` + `kernel.adapters.souffle.package` + `kernel.core.store._artifact_sidecar` + 9 application capability outputs). Verdicts:8 TRUE / 6 FALSE / 1 PARTIAL. Selected Path A(optional `round_events.jsonl` extension within existing audit package). Recorded 10 constraints frozen for Step 0.B and 8 carry-over decisions remaining. Status stays `draft` until Step 0.B finalizes. New §5.4 added to blueprint(no §6-§10 renumber). |
| 2026-05-06 | draft | Step 0.A wording fix pass 1 | Two consistency fixes per user review,no verdict change:(1)§5.4.3 item #1 reframed to clarify Path A frozen vs kill-criteria interaction(`frozen unless a §5.4.2 kill criterion fires`),resolving an apparent contradiction between §5.4.2 and §5.4.3;(2)§5.4.1 #12 reason tightened to remove unverified `module derivable from rule_id metadata` claim and reframe aggregation key derivation as Batch 7's responsibility(if any group-by key not derivable from persisted fields,Batch 7 adds mapping/index — not Batch 6 pre-baking). |
| 2026-05-06 | draft | Step 0.B spike completed | First-slice scope = S3(verdict + ProofFrame)per user decision;5 capability event kinds(`check_result`/`diagnose_result`/`fact_overlay_result`/`why_not_result`/`proof_frame_result`)+ 1 lifecycle marker(`round_finalized`). New §5.5 freezes:event identity(round_id + sequence + event_ts + kind + schema_version);per-kind payload projection(field-level schemas with opaque digests for plan/overlay/SupportArtifact);BindingItems projection helper contract(impl-time selection,not Step 0.B-frozen);AuditQuery extension(4 new round-event methods on separate namespace);6 error-handling rules;13 drift-gate tests including T8 application→audit one-direction import gate and T13 atomic finalize via tempfile+os.replace;partial-round detection via `round_finalized` event presence(no separate manifest file). Frontier and rule actions explicitly deferred with reactivation triggers documented. Status stays `draft`;separate scope-freeze commit will transition to `scoped` upon user approval. |
| 2026-05-06 | draft | Step 0.B hardening pass 1 | Four fixes per user review:(P1a)added `round_started` lifecycle marker at sequence 0 to satisfy Step 0.A #14 explicit-bracket constraint(was implicit-start violation);capability events now sequence 1..N-1,`round_finalized` last;`started_at` derives from `round_started.payload.started_at`. (P1b)added `AuditPackageData.round_event_warnings: tuple[WarningDTO, ...]` field + `AuditQuery.list_round_event_warnings()` method + §5.5.6 lenient row reader paragraph(`round_events.jsonl` independent from required-ledger `_read_jsonl`,never raises on row failures). (P2)source-verified and aligned §5.5.3 payload field names with actual DTOs:`OverlayCheckDiff` uses `status_changed`+`matched_count_delta`+`bindings_added`+`bindings_removed`(was `flipped`);`WhyNotRedRow` uses `binding`(was `row_binding`);`WhyNotRowDiagnostic` expanded to full `status`+`failure_kind`+`diagnostic_granularity`+`atom_locator`+`errors`+`warnings` shape with `WhyNotAtomLocator` containing `branch_index`+`failed_atom_index`+`attempted_binding`. (P3)deferred `flush_round_partial(...)` from Batch 6 with explicit follow-up contract clause. Added 2 drift gates:T14(`round_started` at sequence 0 enforcement)+ T15(`round_finalized.payload.event_count` consistency);amended T1 to cover lifecycle markers. Total drift gates 13 → 15. |
| 2026-05-06 | scoped | Scope freeze | Status moved `draft` → `scoped` after Step 0.A and Step 0.B were recorded and reviewed. Implementation is now authorized for the S3 first slice:optional `round_events.jsonl`,explicit round bracket(`round_started` / `round_finalized`),5 capability event kinds(Check / Diagnose / Fact Overlay / Why-not / ProofFrame),external recorder/wrapper,AuditQuery round-event extension,and 15 drift gates. |
| 2026-05-06 | implemented | Implementation close-out | Added `kernel.audit.round_events` with `RoundEvent` / `RoundSummary`,per-kind JSON projection helpers,external buffered recorder APIs,and atomic finalize via tempfile + `os.replace`. Integrated optional `round_events` manifest key into `load_audit_package(...)`,added lenient row parsing with `round_event_warnings`,and extended `AuditQuery` with the scoped round-event namespace. Updated audit/application module docs and added focused `test_audit_round_events.py` coverage for round-trip,old-package compatibility,malformed/duplicate/unknown/gap handling,recorder lifecycle errors,atomic finalize,projection helpers,and application-runtime no-`kernel.audit` import drift gate. Frontier and rule-action event families remain deferred;no SDK/service/agent or application runtime imports from audit. |
| 2026-05-06 | implemented | Post-archive hardening pass 1 | Five fixes per code review on shipped Batch 6 implementation:(P2.1) `project_diagnose_event_payload` now persists the actual `DiagnoseAtomLocator` fields(`branch_index` + `failed_atom_index` + `attempted_binding`)instead of a fabricated `atom_key` projection;diagnose projection test rewritten to use the real DTO. (P2.2) `write_round_events_atomic` now merges with existing rows from other `round_id` values before atomic replace,so multi-round packages preserve prior rounds(was overwritten);regression `test_multi_round_packages_preserve_prior_rounds`. (P2.3) `round_event_from_row` now downgrades `schema_version >= "2.0"` rows to the `future:{kind}` namespace per §5.5.6,so v1 consumers cannot accidentally parse v2 payloads as v1;regression `test_v2_schema_rows_treated_as_unknown_kind`. (P2.4) `RoundRecorder.finalize` now performs the atomic write before mutating recorder state,making the operation retry-safe on package-level write failure;regression `test_finalize_is_retry_safe_on_write_failure`. (P2.5) Drift-gate coverage expanded with a full first-slice round-trip(T1/T2/T9/T15)exercising all 5 capability kinds + 2 lifecycle markers + schema_version uniformity + `round_finalized.event_count` consistency. (P3) §7 acceptance checkboxes ticked to reflect satisfied gates. Tests:13 → 17 focused OK + full kernel OK + ruff clean;no public surface change;no scope creep into Batch 7. |

## Decision Notes

### Initial Framing

Batch 6 is the first persistence-facing batch after the rule-action series. The draft therefore treats "capability-event JSONL audit trail" as a falsifiable proposal,not as a preselected implementation. The main false-merge risk is conflating existing audit package ledgers,ArtifactSidecar payload persistence,and new application capability result events into one schema without proving they share lifecycle identity.

Default lean is Path A(optional `round_events.jsonl` inside existing audit package)because the parent plan requires compatibility with `AuditQuery` / `load_audit_package`,but Path B(separate round package)and Path C(suspend/abandon as premature)remain valid Step 0 outcomes.
