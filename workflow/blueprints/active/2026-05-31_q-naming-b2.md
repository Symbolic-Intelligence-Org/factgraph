# Q-NAMING-B2 Blueprint: Field/wire/key cascade (atom→condition + branch→case + engine_payload→proof + diagnostic keys + WhyNot wording)

- Status: scoped
- Created: 2026-05-31
- Last Updated: 2026-05-31
- Related Modules:
  - `src/factgraph/application/protocol/` (DTO field renames — primary blast radius)
  - `src/factgraph/application/` (runtime + capability helpers — secondary blast radius)
  - `src/factgraph/core/store/` (`_support.py` diagnostic key format strings — `_support_capture.py` + `_support.py` prefix logic)
  - `src/factgraph/core/rules/` (`ruleref_substrate.py` diagnostic key emit)
  - `src/factgraph/sdk/` (consumers + `store.py` fallback id pattern)
  - `src/factgraph/authoring/` (`rule_compile.py` diagnostic key emit)
- Related Docs:
  - [`workflow/design/decisions/active/2026-05-31_q1-naming-api-polish-decision.md`](../../design/decisions/active/2026-05-31_q1-naming-api-polish-decision.md) — Q-NAMING decision §4.3.2 + §4.3.4 + §4.3.5 + §4.3.6 + §4.8 invariants (adopted at `f1a017ec`)
  - [`workflow/audit/active/2026-05-31_naming-polish-feasibility.md`](../../audit/active/2026-05-31_naming-polish-feasibility.md) — feasibility audit §3 (Batch B cascade) at `10de33b9`
  - [`workflow/blueprints/archive/2026-05-31_q-naming-ad.md`](../archive/2026-05-31_q-naming-ad.md) — Q-NAMING-AD Phase 1 precedent
  - [`workflow/blueprints/archive/2026-05-31_q-naming-c.md`](../archive/2026-05-31_q-naming-c.md) — Q-NAMING-C Phase 2 precedent
  - [`workflow/blueprints/archive/2026-05-31_q-naming-e.md`](../archive/2026-05-31_q-naming-e.md) — Q-NAMING-E Phase 3 Red-slice precedent: Q-PR1 PF-R1 detection + EmitSpec + Step 4.7 P1 fix pattern
  - [`workflow/blueprints/archive/2026-05-31_q-naming-b1.md`](../archive/2026-05-31_q-naming-b1.md) — Q-NAMING-B1 Phase 4 precedent: class-only safe rename + lower-snake helper boundary discipline
  - [`workflow/foundations/architecture_principles.md`](../../foundations/architecture_principles.md) §2.1 layer authority
- Audit Log:
  - [2026-05-31_q-naming-b2.audit.md](./2026-05-31_q-naming-b2.audit.md)

## 1. Problem

Q-NAMING decision §4.3.2 (atom→condition cascade field-level) + §4.3.4 (branch→case cascade with wire/persistence hard-cut) + §4.3.5 (WhyNot wording narrow target) + §4.3.6 (hard-cut at v0.2) lock the **field-level + wire-level + diagnostic-key cascade** for v0.2.0 alpha. Per Q-NAMING §4.9, this is **Phase 5 (Q-NAMING-B2)**: **Red risk**, stop-gated after B1.

User entered Phase 5 via "推进" 2026-05-31 after Q-NAMING-B1 archive at `30bd31c8`. Stop gate cleared.

**B1/B2 boundary is now reversed** (critical for execution):

- **Q-NAMING-B1 (Phase 4, archived at `30bd31c8`)** renamed only the **class symbols** (top-level type names + their TypeAliases): `SupportArtifact→ProofReceipt`, `EvaluationOverlay→FactOverlay`, descriptor/locator classes, etc. **12 class/type symbol renames, no field/wire changes.**
- **Q-NAMING-B2 (this phase)** renames the **fields, wire-serialized payload keys, runtime variable names, and diagnostic key strings** that B1 explicitly preserved per its §5.2 B1/B2 boundary discipline. Class symbols stay unchanged (already renamed in B1).

**Red risk classification** per §4.9 Phase 5 reflects:

- Wire/persistence field rename = hard-cut per Sub-Q1 + §4.3.6 (no alias, no dual-emit, recorded payloads and diagnostic keys change in v0.2; acceptable because alpha)
- Massive blast radius: `branch_index` alone ships 220 src + 216 tests = **436 hits / 60 unique files** (Step 4.1 grep); `atom_index` 152 + 126 = 278 hits / 41 files; `atom_key` 73 + 68 = 141 hits / 33 files
- Cross-cutting impact across `application/`, `core/store/`, `core/rules/`, `authoring/`, `sdk/`, all DTO fields + runtime payloads + diagnostic key emission sites
- Diagnostic key string format change `b{branch_index}.a{atom_index}` → `c{case_index}.c{condition_index}` = 10 enumerated emit sites + downstream consumers (parsers, validators, error messages) must coordinate
- Likely Step 4.7 P1 fix needed per Q-NAMING-E precedent (Red slices historically surface fixture migration misses in independent test re-run)

**Q-PR1 sacred 5-path Step 4.1 scan** (refined per Step 4.2 P2-1): all 7 B2 long-form field names (`atom_index`, `atom_key`, `_atom_index_from_key`, `materialized_atom_index`, `branch_index`, `runtime_branch_index`, `engine_payload`) return **0 hits** across `core/evidence/write_protocol.py`, `core/store/ledger.py`, `core/store/_builders.py`, `adapters/pyreason/`, `core/derivation/accept.py`. **No PF-R1-style Q-PR1 blocker for B2 field renames.** HOWEVER, **diagnostic-prefix Q-PR1 variants exist** and must explicitly preserve per N20 (added Step 4.2 P2-1):

- `pyreason/where_compile.py:56` uses `rule_name = base_name if len(branches) == 1 else f"{base_name}_b{branch_idx}"` — PyReason rule_name suffix `_b{branch_idx}` (NOT diagnostic key) **must preserve** per Q-PR1 0-diff
- `pyreason/where_compile.py:55, 63, 216-220` + `pyreason/rule_ext.py:191-197, 202-203` use `branch_idx` / `atom_idx` short-form local variables + parameters extensively (~32 hits in Q-PR1 sacred). These short-form `_idx` identifiers are PyReason adapter internal naming, **NOT in B2 scope** per N7 layer authority + Q-PR1 0-diff (contrast `branch_index` / `atom_index` long-form which IS B2 scope).

The "Red" classification comes from wire/persistence scale + cross-cutting cascade + diagnostic key ecosystem scope, NOT from Q-PR1 sacred conflict on B2 field renames themselves. Step 4.3 preflight 2.a re-confirms long-form field names = 0 hits + 2.h classifies all `b{...}` / `_b` / `branch_idx` variants per Q-PR1 carve-out vs B2 scope decision.

Per Q-NAMING §4.8.1 hard-cut: no aliases, no dual-emit, old field names removed in same commit as new names introduced. Per N7 layer authority: lower-level core / authoring / adapter internal names preserved where they don't appear on the renamed public field surface.

The Q-NAMING-AD + Q-NAMING-C + Q-NAMING-E + Q-NAMING-B1 precedents (PF-R1 Q-PR1 detection + Sub-Q3 single-slice + per-field classification + AD/C/E/B1 inherited preservation) inform this slice's discipline.

## 2. Goals

**Per Q-NAMING §4.3.2 — atom→condition cascade field-level (4 renames; class-level already shipped in B1)**:

- G1: Rename `atom_index` → `condition_index` field/parameter/dict-key across all DTO + runtime sites. Approximate footprint: 278 hits / 41 unique files (Step 4.1 grep; preflight 2.a pins exact distribution).
- G2: Rename `atom_key` → `condition_key` field/parameter/dict-key. Approximate footprint: 141 hits / 33 files.
- G3: Rename `_atom_index_from_key` → `_condition_index_from_key` private helper function. Approximate footprint: 3 hits / 3 files.
- G4: Rename `materialized_atom_index` → `materialized_condition_index` field/parameter. Approximate footprint: 10 hits / 3 files.

**Per Q-NAMING §4.3.4 — branch→case cascade with wire/persistence hard-cut (3 renames + diagnostic key format)**:

- G5: Rename `branch_index` → `case_index` field/parameter/dict-key across DTO fields, walker state, support capture, evaluation rows. Approximate footprint: 436 hits / 60 unique files (heaviest single symbol in B2).
- G6: Rename `runtime_branch_index` → `runtime_case_index` runtime payload field. Approximate footprint: 6 hits / 3 files.
- G7: Rename `engine_payload` → `proof` wire/serialized payload field. Approximate footprint: 46 hits / 17 files. Hard-cut per Sub-Q1 (alpha, no historical consumers). **[NARROWED Step 4.2 P2-4]**: G7 scope is restricted to `EvidenceEnvelope.engine_payload`-style payload field rename ONLY. Per shipped reality grep, multiple existing **structured `"proof"` keys belong to separate provenance/evidence-tree schemas and are PRESERVED** unchanged:
  - `adapters/souffle/provenance.py:268` `proof_payload = payload.get("proof")` — Souffle provenance JSON key (separate Souffle proof-tree schema)
  - `tests/test_souffle_provenance_v0.py`, `tests/test_souffle_evidence_graph.py` — JSON fixture keys `"proof"` (mirror Souffle schema)
  - `core/store/_candidate_evidence_tree_narrative.py:90, 346, 350` — `("proof",)` role tuple + `value.get("proof")` + `out["proof"]` (candidate evidence tree role enumeration)
  - `core/store/_candidate_evidence_tree_summary.py:26, 27, 125, 128` — `"proof_goal": "proof"` / `"proof_leaf": "proof"` role mapping + `"proof"` count aggregation key
  - These are NOT the `EvidenceEnvelope.engine_payload` wire field; the rename targets only the FactGraph application protocol `engine_payload` field. Preflight 2.f classifies each `"proof"` use per spot-check.
- G8: Rewrite diagnostic key string format `b{branch_index}.a{atom_index}` → `c{case_index}.c{condition_index}` across 10 enumerated f-string emit sites + parser/carrier/export ecosystem (see G14 added Step 4.2 P2-2 for ecosystem scope). Hard-cut per §4.3.6 (recorded payloads change in v0.2). Sites: `core/rules/ruleref_substrate.py:149`, `core/store/_support.py:212+226`, `core/store/_support_capture.py:44`, `authoring/rule_compile.py:203`, `sdk/store.py:3639` (**variant `f"b{idx}"` fallback id — preflight 2.h scope decision**), `application/rule_literal_replace_runtime.py:322`, `application/rule_add_condition_runtime.py:301` (**variant `f"b{...}.add{action_index}:..."` — preflight 2.h scope decision**), `application/rule_disable_runtime.py:213`, `application/protocol/rule_expr_lowering.py:723` (**variant `branch_id=f"b{idx}"` — branch-id namespace identifier per P3-1; preflight 2.h locks per separate decision from canonical key format**). **Q-PR1 sacred carve-out per P2-1**: `pyreason/where_compile.py:56` `_b{branch_idx}` rule_name suffix is NOT in G8 scope (Q-PR1 0-diff preserve).

**Per Q-NAMING §4.3.5 — WhyNot wording narrow target (TBD locks per Step 4.2)**:

- G9: WhyNot partition row types + status field values rename. **[LOCKED Step 4.2 P2-3]** Targets locked to align with existing Diagnose `passed`/`failed` convention shipped at `why_not_runtime.py:388` (`if diagnose_result.status == "passed":`) — avoid introducing new vocabulary. Step 4.1 enumerated sites:
  - `class WhyNotRedRow` at `application/protocol/derivation_why_not.py:284` → `WhyNotFailedRow`
  - `green` / `red` field names + `field_name="green"/"red"` validation calls at `derivation_why_not.py:131, 133, 312, 313, 318, 319` → `passed` / `failed`
  - Error code `WHY_NOT_DIAGNOSE_PASSED_RED_BINDING` at `why_not_runtime.py:391` → `WHY_NOT_DIAGNOSE_PASSED_FAILED_BINDING` (preserves "Diagnose passed a binding that Why-not classified as failed" semantics)
  - **Rationale (locked at Step 4.2 P2-3)**: shipped Diagnose convention already uses `passed`/`failed` status vocabulary at `why_not_runtime.py:388`; unifying WhyNot partition labels reduces mixed vocabulary. Alternative `holds`/`misses` (positive-query semantics) rejected as introducing new vocabulary not grounded in shipped code. Alternative `accepted`/`rejected` also rejected. **Note**: per §4.8.4 + §4.3.5 NARROW target, only WhyNot partition row types / status field values are renamed; "green"/"red" mentions in docs/CSS/UI/historical material are NOT in scope.

- G14 (added Step 4.2 P2-2): **Diagnostic key ecosystem migration** — parser/carrier/export surfaces coordinated with G8 format change. Step 4.1 enumerated ecosystem sites:
  - **`application/walker/keys.py:12`** `_ATOM_KEY_RE = re.compile(r"^b(?P<branch>\d+)\.a(?P<atom>\d+):(?P<payload>.+)$")` → `_CONDITION_KEY_RE` with `r"^c(?P<case>\d+)\.c(?P<condition>\d+):(?P<payload>.+)$"` (note: TypeAlias `AtomKeyKind` if exists similarly)
  - **`application/walker/keys.py:15`** `class AtomKeyView` → `class ConditionKeyView`
  - **`application/walker/keys.py:156`** `def parse_atom_key` → `def parse_condition_key`
  - **`application/walker/__init__.py:39, 40, 53, 68`** + **`application/__init__.py:58, 66, 118, 189`** export updates: `AtomKeyView` → `ConditionKeyView`, `parse_atom_key` → `parse_condition_key`
  - **`core/store/_support.py:37`** `PredWitness.pred_atom_key` → `pred_condition_key`
  - **`core/store/_support.py:49`** `NonFactStep.step_key` — **PRESERVE** (no atom suffix; out of B2 N19 substring scope)
  - **`core/store/_support.py:67`** `RuleRefEdge.ruleref_atom_key` → `ruleref_condition_key`
  - **`core/rules/ruleref_types.py:31`** `ruleref_atom_key` field → `ruleref_condition_key`
  - **`core/store/_candidate_evidence_tree.py`** ~15 sites using `pred_atom_key` + `ruleref_atom_key` → propagate field rename
  - **`application/diagnose_runtime.py:764-806`** local parser `_parse_branch_index` + uses `pred_atom_key`/`step_key` — update field references + parser regex if duplicated
  - **`application/derivation_check_runtime.py:382-420`** similar local parser — update field references + parser regex if duplicated
  - **`core/rules/ruleref_substrate.py:149`** `ruleref_atom_key=f"b{branch_index}.a{atom_index}:ruleref"` already in G8 emit-site list — coordinates field name + format simultaneously
  - **`core/rules/_trace.py`** — preflight 2.k re-grep to enumerate any `*_atom_key` references not yet caught

**Cross-cutting per §4.8 invariants**:

- G10: Hard-cut per §4.8.1 — no aliases, no dual-emit, old field names removed in same commit as new names introduced.
- G11: Update affected module docs (`src/factgraph/*/docs/`) + current SDK quickstarts / examples (`docs/`) + tests + N-7-style proactive scan for current tutorials in same slice per CADENCE Stage 4.8.
- G12: Q-PR1 sacred 5 paths 0-diff vs `4c472b50` preserved through all commits; sacred master `562c74195df4...` unchanged; dirty baseline 8 entries preserved.

## 3. Non-goals

- N1: Q-NAMING-AD scope re-litigation (all AD work archived; B2 inherits AD state untouched).
- N2: Q-NAMING-C scope re-litigation (all C work archived; inherits unchanged).
- N3: Q-NAMING-E scope re-litigation (all E work archived; Branch→Case + Rule.when + Inference.when + EmitSpec all preserved per N14).
- N4: Q-NAMING-B1 scope re-litigation (all B1 class/type symbol renames archived at `30bd31c8`; 12 new names `ProofReceipt`/`FactOverlay`/etc. preserved per N18).
- N5: §4.7 engine config renames (ProbLog/PyReason Semantics → Config, SemanticsProfile → EngineProfile, semantics= → config=). Reserved for Q-NAMING-F (Phase 6, stop-gated).
- N6: PDF Module 1 Schema renames per §4.1 REJECT.
- N7: Lower-level core / authoring / adapter internal symbol name renames where they do NOT appear on the renamed public field surface. Per architecture_principles.md §2.1 layer authority + Q-NAMING N7 precedent inherited from AD/C/E/B1.
- N8: `atom_bounds` PyReason-domain syntax per §4.3.3 + §4.8.2. The `atom_bounds` field on `PyReasonSemantics` SDK wrapper at `sdk/semantics.py:195` and its SDK→adapter translation at `sdk/store.py:4055+` preserve `atom_*` naming because they reflect PyReason's own native terminology, not FactGraph rule-DSL terminology.
- N9: Historical workflow / archive material per §4.8.5 carve-out (`docs/references/working/**`, `workflow/heritage/`, `workflow/blueprints/archive/`, `workflow/audit/archive/`, `examples/archive/`). Only current SDK docs, quickstarts, current examples, and active tests migrate.
- N10: Q-PR1 sacred 5 paths — must remain 0-diff vs `4c472b50` (`core/evidence/write_protocol.py`, `core/store/ledger.py`, `core/store/_builders.py`, `adapters/pyreason/`, `core/derivation/accept.py`).
- N11: Adding new public methods, namespaced managers, or restructuring class hierarchies. Q-NAMING-B2 is a **pure field/wire/key rename slice** — DTO fields + variable names + dict keys + diagnostic key strings change but class definitions + method signatures (other than parameter name) + public method surface unchanged.
- N12: Reverting or modifying Q-NAMING-AD inherited surfaces (per Q-NAMING-B1 N12).
- N13: Reverting or modifying Q-NAMING-C inherited surfaces (per Q-NAMING-B1 N13).
- N14: Reverting or modifying Q-NAMING-E inherited surfaces (per Q-NAMING-B1 N14).
- N15: Resurrecting `fg.what_if` public access (Q-NAMING-C N15 inherited).
- N16: Modifying `SDKStore.commit_assertions` (Q-NAMING-C N16 inherited; G7a LOCKED preserved).
- N17: Renaming `Query.where` / `Query.where_ir` (Q-NAMING-E N17 inherited; Query carve-out preserved).
- N18: Reverting or modifying Q-NAMING-B1 inherited surfaces — all 12 new B1 class symbols preserved (`ProofReceipt`, `FactOverlay`, `ReplaceFact`, `RemoveFact`, `ConditionDescriptor`, `DiagnoseConditionLocator`, `WhyNotConditionLocator`, `ProofFrameConditionVerdict`, `AddedCondition`, `AddedConditionKind`, `ConditionPath`, `ConditionPathKind`); 4 lower-snake `support_artifact_*` helper function names (now operating on `ProofReceipt`) preserved per B1 §5.2 (`support_artifact_to_dict`, `support_artifact_from_dict`, `support_artifact_bytes`, `compute_support_digest`); `_row_support_artifacts` field preserved per B1 §5.2 (not in B2 §4.3.2/§4.3.4 explicit field list).
- N19: Field names NOT in §4.3.2 / §4.3.4 explicit list: `added_atom` (B1 instance field) — even though contains "atom" substring, NOT renamed per spec literal enumeration; `atom_verdicts` — even though contains "atom", NOT renamed per spec; `atom_locator` — even though contains "atom", NOT renamed per spec (it's a parameter name on already-renamed-class `DiagnoseConditionLocator` / `WhyNotConditionLocator`); `literal_path` (B1 instance field) — NOT renamed per spec; `step_key` (`_support.py:49` `NonFactStep.step_key`) — preserve (no atom suffix); `branch_id` field (distinct from `branch_index` per P3-1) — see N21. **Discipline**: B2 renames ONLY the 7 specific long-form symbols + 10 diagnostic key sites + WhyNot narrow target + G14 ecosystem migration. Any other `atom_*` / `branch_*` substring containment is out of scope unless explicitly enumerated.
- **N20 (added Step 4.2 P2-1)**: Q-PR1 sacred short-form variables PRESERVE — `pyreason/where_compile.py` + `pyreason/rule_ext.py` use `branch_idx` / `atom_idx` (short-form `_idx`, NOT long-form `_index`) as local iter variables + parameters across ~32 hits in Q-PR1 sacred paths. These are PyReason adapter internal naming, **NOT** in B2 scope per N7 layer authority + Q-PR1 0-diff. Also the rule_name suffix pattern `_b{branch_idx}` at `where_compile.py:56` is PyReason-internal naming, NOT a FactGraph diagnostic key, and is PRESERVED.
- **N21 (added Step 4.2 P3-1)**: `branch_id` field (distinct identifier, not branch_index integer) at `application/protocol/rule_expr_lowering.py:100, 142, 163, 181` PRESERVE field name unchanged. However the `b{idx}` string template used inside `branch_id=f"b{idx}"` is the **branch-id namespace identifier** that is logically separate from canonical diagnostic key format `b{branch}.a{atom}:...`. Preflight 2.h locks decision per 3-category split: (1) canonical diagnostic key format → `c{case}.c{condition}:...` per G8; (2) branch-id namespace `b0`/`b1` → likely `c{idx}` for consistency with case_index semantics (preflight default; user/Codex may override); (3) Q-PR1 adapter `_b{branch_idx}` suffix → PRESERVE per N20.

## 4. Current Context

- **7 B2 field/helper sites — Step 4.1 grep**:
  - `atom_index`: 152 src + 126 tests = 278 hits / 41 unique files
  - `atom_key`: 73 src + 68 tests = 141 hits / 33 files
  - `_atom_index_from_key`: 3 src + 0 tests = 3 hits / 3 files
  - `materialized_atom_index`: 7 src + 3 tests = 10 hits / 3 files
  - `branch_index`: 220 src + 216 tests = 436 hits / 60 files (heaviest in B2)
  - `runtime_branch_index`: 5 src + 1 tests = 6 hits / 3 files
  - `engine_payload`: 16 src + 30 tests = 46 hits / 17 files
  - **Total estimated**: ~920 hits across these 7 symbols (Step 4.1 grep raw; preflight 2.b pins deduped per-file footprint distribution)

- **10 diagnostic key emit sites — Step 4.1 grep** (`f"b{...}.a{...}:..."` or variant):
  - `core/rules/ruleref_substrate.py:149` (`ruleref_atom_key=f"b{branch_index}.a{atom_index}:ruleref"` — interface-coupled prefix, downstream consumer `_support_capture.py:44`)
  - `core/store/_support.py:212` (`return f"b{branch_index}.a{atom_index}:{pred_id}"`)
  - `core/store/_support.py:226` (`return f"b{branch_index}.a{atom_index}:{kind}"`)
  - `core/store/_support_capture.py:44` (`if not edge.ruleref_atom_key.startswith(f"b{selected_branch_index}.")` — prefix-match consumer)
  - `authoring/rule_compile.py:203` (`out.add(f"b{branch_index}.a{atom_index}")`)
  - `sdk/store.py:3639` (`fallback_id = f"b{idx}"` — variant pattern, not full `.a{}` form; preflight 2.h confirms whether in scope)
  - `application/rule_literal_replace_runtime.py:322` (`return f"b{action.branch_index}.a{action.atom_index}:"`)
  - `application/rule_add_condition_runtime.py:301` (`return f"b{action.branch_index}.add{action_index}:{action.added_atom.atom[0]}"` — variant `add{action_index}` instead of `a{atom_index}`; preflight 2.h confirms scope)
  - `application/rule_disable_runtime.py:213` (`return f"b{action.branch_index}.a{action.atom_index}:"`)
  - `application/protocol/rule_expr_lowering.py:723` (`return tuple(replace(branch, branch_id=f"b{idx}") for idx, branch in enumerate(branches))` — variant `b{idx}` for branch_id; preflight 2.h confirms scope)

- **WhyNot narrow target sites — Step 4.1 grep**:
  - `application/protocol/derivation_why_not.py:131` (`green = _validate_unique_bindings(green, field_name="green")`)
  - `application/protocol/derivation_why_not.py:133` (`red_bindings = _validate_unique_bindings(red_bindings, field_name="red")`)
  - `application/protocol/derivation_why_not.py:209` (`class WhyNotRowDiagnostic` — may need rename if green/red wording inside, preflight 2.i verifies)
  - `application/protocol/derivation_why_not.py:284` (`class WhyNotRedRow`)
  - `application/protocol/derivation_why_not.py:312-313, 318-319` (green/red field assignment + validation)
  - `application/why_not_runtime.py:391` (error code `WHY_NOT_DIAGNOSE_PASSED_RED_BINDING`)

- **Deduped file footprint** (Step 4.1, excluding WhyNot): 44 src + 42 tests = 86 unique files. WhyNot wording adds ~5-8 more unique files (preflight 2.b refines).

- **Broader scope spillover beyond `src/factgraph/` + `tests/`** (added Step 4.2 P3-2): Step 4.1 grep confirms B2 fields appear in non-factgraph + non-tests locations:
  - `src/service`: `atom_key` 2 files
  - `examples/`: `branch_index` 6 files, `atom_index` 3 files, `atom_key` 3 files, `engine_payload` 4 files
  - `tutorials/`: `branch_index` 1 file, `atom_index` 1 file, `atom_key` 1 file
  - `docs/`: `branch_index` 9 files, `atom_index` 6 files, `atom_key` 3 files, `engine_payload` 4 files
  - `src/domains`: 0 files for these 3 symbols (sample); preflight 2.b re-verifies for all 7 + WhyNot
  - **Implication**: Step 4.6.5 pre-impl grep must enumerate ALL subdirs (per Q-NAMING-B1 N-7 precedent extended): `src/factgraph`, `src/service`, `src/domains`, `src/agent` (verify), `tests/`, `examples/`, `tutorials/`, `docs/`. Total spillover footprint likely adds 15-30 more files to migration cohort.

- **No shipped naming conflicts for 7 new names** (Step 4.1 grep):
  - `condition_index`: 0 hits in `src/` + `tests/`
  - `condition_key`: 0 hits
  - `_condition_index_from_key`: 0 hits
  - `materialized_condition_index`: 0 hits
  - `case_index`: 0 hits
  - `runtime_case_index`: 0 hits
  - `proof`: 23 src + 5 tests files (English-word usage in docs/READMEs/narrative; one structural usage at `core/store/_candidate_evidence_tree_summary.py:128` `"proof": int(node_count_by_role.get("proof", 0))` as dict key — preflight 2.f assesses whether field-name shadowing or coexisting concept). **No symbol/class/function definition collision at module top level.**

- **Q-PR1 sacred 5 paths × 7 B2 field names = 0 hits across all combinations** (Step 4.1 grep). **No PF-R1-style blocker** (same positive finding as Q-NAMING-B1). The "Red" classification stems from wire/persistence scale + cascade coordination, NOT from Q-PR1 sacred trap. This is critical to surface at Step 4.3 preflight since it informs slice cadence decisions (single-slice viable, contrast Q-NAMING-E which had Q-PR1 trap forcing complex Option D).

- **AD/C/E/B1 inherited contracts**: all 18 specific contracts (AD's 9 surfaces + C's 4 + N15/N16 + E's 5 + N17 + B1's 12 class symbols + 4 support_artifact_* helpers) preserved untouched per N12/N13/N14/N18.

- **当前已知约束**:
  - Q-PR1 sacred 5 paths at 0-diff vs `4c472b50`
  - Sacred `master` at `562c74195df43e933bed92a3ff25de94dd8ce666`
  - Dirty baseline (4 M + 2 D + 2 untracked) preserved
  - Hard-cut per §4.8.1: no aliases, no dual-emit
  - Layer authority per architecture_principles.md §2.1
  - WhyNot rename targets **[LOCKED Step 4.2 P2-3]**: `green→passed`, `red→failed`, `WhyNotRedRow→WhyNotFailedRow`, error code `WHY_NOT_DIAGNOSE_PASSED_FAILED_BINDING` (aligns with shipped Diagnose convention at `why_not_runtime.py:388`)

- **当前相关历史蓝图**:
  - Q-NAMING-AD archived at `7c48648f` — Phase 1 precedent (Yellow)
  - Q-NAMING-C archived at `23389a2b` — Phase 2 precedent (Yellow)
  - Q-NAMING-E archived at `121a375d` — Phase 3 Red precedent (Q-PR1 trap, EmitSpec, Step 4.7 P1 fix)
  - Q-NAMING-B1 archived at `30bd31c8` — Phase 4 Yellow precedent (class-only, 0 Required PF, single-feat-commit)
  - Slice 6 form-i debt cleanup at `4c472b50` — Q-PR1 sacred baseline

## 5. Proposed Shape

### §5.1 Rename layout per Goal

11 field/helper/format renames + WhyNot narrow target list (all share hard-cut pattern; preflight 2.c verifies):

| Goal | Old | New | File:line scope | Approx hits |
|---|---|---|---|---|
| G1 | `atom_index` | `condition_index` | DTO fields + runtime + tests + docs | 278 |
| G2 | `atom_key` | `condition_key` | DTO fields + runtime + tests + docs | 141 |
| G3 | `_atom_index_from_key` | `_condition_index_from_key` | private helper function — `core/store/_explain_support.py` + `application/protocol/evaluate_result.py` (preflight 2.b pins exact sites) | 3 |
| G4 | `materialized_atom_index` | `materialized_condition_index` | DTO field + runtime | 10 |
| G5 | `branch_index` | `case_index` | DTO fields + walker state + support capture + evaluation rows | 436 |
| G6 | `runtime_branch_index` | `runtime_case_index` | runtime payload field | 6 |
| G7 | `engine_payload` | `proof` | wire/serialized payload field — DTO + persistence + diagnostics | 46 |
| G8 | `f"b{branch_index}.a{atom_index}:..."` | `f"c{case_index}.c{condition_index}:..."` | 10 enumerated f-string emit sites + downstream prefix-match consumers | — |
| G9 | WhyNot narrow target | **[LOCKED Step 4.2 P2-3]** `green→passed`, `red→failed`, `WhyNotRedRow→WhyNotFailedRow`, error code `WHY_NOT_DIAGNOSE_PASSED_FAILED_BINDING` | `application/protocol/derivation_why_not.py` + `application/why_not_runtime.py` + tests | ~30 |

**§5.1.1 Public export surfaces**:

Per Q-NAMING-B1 §5.1.1 precedent, each rename must update corresponding `__init__.py` re-export modules where applicable. Step 4.1 scan deferred to preflight 2.b for exact export coverage; most B2 renames are field-level (not exported as standalone symbols), but:

- `application/protocol/__init__.py` may re-export `WhyNotRedRow` if G9 renames class — verify at preflight 2.b
- `engine_payload` field appears on multiple DTOs but is not a standalone export
- Other fields (`atom_index`, `branch_index`, etc.) are not standalone exports

### §5.2 Field-vs-string-vs-helper boundary (critical B2 discipline)

**Q-NAMING-B2 IS field-level rename**:

- Instance/dataclass field name (`field: Type` → renamed `field: Type`)
- Function/method parameter name (`def f(branch_index: int) → def f(case_index: int)`)
- Dict key strings (`{"atom_index": ...}` → `{"condition_index": ...}`)
- Local variable names within method bodies (yes — including loop iterators if named `branch_index`/`atom_index`)
- f-string template variable references within diagnostic key emission (G8)
- Test fixture field assignments + assertion text
- Module docstring field references

**Q-NAMING-B2 IS NOT class symbol rename** (already done in B1):

- `class SupportArtifact:` → already renamed to `class ProofReceipt:` in B1
- `class AtomDescriptor:` → already renamed to `class ConditionDescriptor:` in B1
- All 12 B1 class symbols preserved untouched in B2 per N18

**Q-NAMING-B2 IS NOT broad atom/branch substring sweep** (per N19):

The shipped code has many `atom_*` / `branch_*` substring containment fields that are NOT in §4.3.2/§4.3.4 explicit list. These are PRESERVED in B2 (deferred to potential future slice or leave permanently):

- **`added_atom` field** (B1 preserved): `derivation_fact_overlay.py:217` instance field on `AddConditionAction` class — NOT renamed per spec literal enumeration ("`atom_index`" / "`atom_key`" / "`_atom_index_from_key`" / "`materialized_atom_index`" only)
- **`atom_verdicts` field**: `proofframe.py` field on `ProofFrame` — NOT renamed per spec
- **`atom_locator` parameter**: on already-renamed classes `DiagnoseConditionLocator` / `WhyNotConditionLocator` — NOT renamed per spec
- **`literal_path` field** (B1 preserved): `derivation_fact_overlay.py:185` — NOT renamed per spec; also unrelated to atom-cascade
- **`support_artifact_*` lower-snake helpers** (B1 preserved): `_support.py:229/265/302/311` — NOT renamed per spec; B1 §5.2 G1 helper policy locked
- **`_row_support_artifacts` field** (B1 preserved): `evaluate_result.py:183` — NOT renamed per spec
- **`atom_bounds` field** per §4.3.3 + §4.8.2 + N8: PyReason-domain syntax preserved
- **`branch_id` field** when used as identifier (not index): preflight 2.h discriminates `branch_id` from `branch_index` per-site

**Implementation discipline** (per CADENCE Rule 2 + N19 boundary):

- Mechanical rename targets ONLY the 7 explicit field/helper symbols + 10 diagnostic key sites + WhyNot narrow target
- Substring containment (`atom_*` / `branch_*` not in spec) is PRESERVED unchanged
- Cross-rename ordering: when `branch_index` and `atom_index` co-occur in same function signature or dataclass, both rename simultaneously with the f-string format change (G8) in same edit
- Preflight 2.d verifies each rename target is field-level + enumerated; substring sweeps are explicitly out of scope

### §5.2.1 Error message + log message policy

Per Q-NAMING-B1 §5.2.1 precedent extended for field renames:

- **Field-name token in error message text → UPDATE** to new field name (e.g., `"atom_index must be ..."` → `"condition_index must be ..."`; `"branch_index out of range"` → `"case_index out of range"`)
- **Class-name token in error message → PRESERVE** as already-renamed B1 form (e.g., `"expected ProofReceipt"` stays as-is from B1)
- **Diagnostic key in error message** (e.g., `"$.b0.a0:rule_disable failed"`) → UPDATE to new format `"$.c0.c0:rule_disable failed"` per G8 hard-cut
- **Logger format strings**: same policy — update field-name tokens and diagnostic key formats; preserve class-name tokens

Preflight 2.d enumerates known error-message sites for spot-check.

### §5.3 No-naming-conflict verification (G1-G9)

7 new field names + WhyNot targets verified at Step 4.1 scan:

- `condition_index` (G1): 0 hits in `src/` + `tests/`
- `condition_key` (G2): 0 hits
- `_condition_index_from_key` (G3): 0 hits
- `materialized_condition_index` (G4): 0 hits
- `case_index` (G5): 0 hits
- `runtime_case_index` (G6): 0 hits
- `proof` (G7): 23 src files + 5 tests files. **[NARROWED Step 4.2 P2-4]** G7 scope is restricted to `EvidenceEnvelope.engine_payload`-style payload field only. Multiple existing **structured `"proof"` keys belong to separate provenance/evidence-tree schemas and are PRESERVED**:
  - `adapters/souffle/provenance.py:268` `proof_payload = payload.get("proof")` — Souffle provenance JSON schema
  - `tests/test_souffle_provenance_v0.py`, `tests/test_souffle_evidence_graph.py` — JSON fixture `"proof"` keys (mirror Souffle schema)
  - `core/store/_candidate_evidence_tree_narrative.py:90, 346, 350` — role tuple `("proof",)` + `value.get("proof")` + `out["proof"]` (candidate evidence tree role enumeration)
  - `core/store/_candidate_evidence_tree_summary.py:26, 27, 125, 128` — `"proof_goal": "proof"` / `"proof_leaf": "proof"` role mapping + count aggregation key
  - **No symbol/class/function definition collision at module top level.** Preflight 2.f classifies each `"proof"` use per spot check + locks UPDATE vs PRESERVE per source per §5.3 + §8 2.f extended table.
- WhyNot new targets **[LOCKED Step 4.2 P2-3]**: `passed` / `failed` / `WhyNotFailedRow` / `WHY_NOT_DIAGNOSE_PASSED_FAILED_BINDING`. Preflight 2.f + 2.i re-verify no conflict + cover all sites per §5.6 table.

**Preliminary determination**: no shipped symbol shadowing risk for B2 long-form fields; `proof` collision boundary classified per Step 4.2 P2-4 narrow scope (only EvidenceEnvelope.engine_payload renamed; provenance/evidence-tree `"proof"` schemas PRESERVED).

### §5.4 Q-PR1 boundary verification (critical, per Q-NAMING-E PF-R1 precedent)

Step 4.1 scan results (positive finding):

- All 7 B2 field names (`atom_index`, `atom_key`, `_atom_index_from_key`, `materialized_atom_index`, `branch_index`, `runtime_branch_index`, `engine_payload`) return **0 hits** across Q-PR1 sacred 5 paths (`write_protocol.py`, `ledger.py`, `_builders.py`, `adapters/pyreason/`, `accept.py`)
- WhyNot wording sites are confined to `application/protocol/derivation_why_not.py` + `application/why_not_runtime.py` — neither in Q-PR1 sacred

**No PF-R1-style Q-PR1 blocker expected**. Preflight 2.a re-confirms via independent grep across all 7 symbols + WhyNot wording.

This contrasts with Q-NAMING-E which had PF-R1 forcing G4 Option D preserve (legacy `Rule.where` field touched by Q-PR1 sacred `pyreason/rule_ext.py:101`). B2 has no such trap despite Red classification.

### §5.5 Diagnostic key format hard-cut (G8 — 10 sites, cross-rename coordination)

Per §4.3.4 + §4.3.6: diagnostic key string format `b{branch_index}.a{atom_index}` → `c{case_index}.c{condition_index}` is hard-cut per Sub-Q1. Recorded payloads change in v0.2 (acceptable because alpha; no historical consumers).

**Co-ordination requirement**: All 10 emit sites + downstream prefix-match consumers (e.g., `_support_capture.py:44` `startswith(f"b{...}.")`) must rewrite together. Cross-rename ordering critical:

1. Update all emit sites' f-string templates: `f"b{branch_index}.a{atom_index}:..."` → `f"c{case_index}.c{condition_index}:..."`
2. Update prefix-match consumers: `startswith(f"b{...}.")` → `startswith(f"c{...}.")`
3. Update variant-form sites (preflight 2.h locks decision per **3-category split added Step 4.2 P3-1**):
   - **Category A (canonical diagnostic key)**: `f"b{branch_index}.a{atom_index}:..."` pattern — RENAME to `c{case_index}.c{condition_index}:...` per G8
   - **Category B (branch-id namespace identifier)** **[LOCKED Step 4.4 PF-r1 Option A]**: `branch_id=f"b{idx}"` at `protocol/rule_expr_lowering.py:722` (inside `_assign_branch_ids` function lines 718-722) + `fallback_id = f"b{idx}"` at `sdk/store.py:3639` (Case.id fallback per Q-NAMING-E renamed `Case.id` error message context lines 3633-3645) — **LOCKED to UPDATE prefix `b` → `c` (template `f"c{idx}"`)** for semantic consistency with Case class supremacy from Q-NAMING-E (Case is the post-E public name; `b{idx}` template generates strings semantically aligned with OLD "Branch" name, contradicting Case supremacy). Field name `branch_id` remains PRESERVED per N21 (only the template string content `b`→`c`); class field accessors unchanged. Alternative PRESERVE rejected at Step 4.4 PF-r1: no strong reason given Case class supremacy from Q-NAMING-E.
   - **Category C (Q-PR1 sacred adapter suffix)**: `_b{branch_idx}` at `pyreason/where_compile.py:56` + `branch_idx`/`atom_idx` short-form local vars across `pyreason/where_compile.py` + `pyreason/rule_ext.py` (~32 hits) — **PRESERVE per N20 + Q-PR1 0-diff**
   - **Variant `add{action_index}` at `rule_add_condition_runtime.py:301`** (`f"b{action.branch_index}.add{action_index}:..."`): `b` prefix updates to `c` (Category A); `add` prefix unchanged (action-specific semantic); `branch_index` field rename per G5 propagates to the f-string
4. Test fixtures + golden snapshots: update all `"b0.a0"`-style literal strings in test assertions + JSON fixtures + canonical golden files

### §5.5.1 Diagnostic key ecosystem decision (added Step 4.2 P2-2 + [EXPANDED Step 4.4 PF-R1])

G8 emit-site format change requires coordinated migration of the diagnostic-key parser/carrier/export ecosystem per G14. Without this coordination, the key STRING format would change to `c{case}.c{condition}` while parser/class/field names still say `Atom*` — creating a mixed-vocabulary inconsistency.

**[EXPANDED Step 4.4 PF-R1]** Step 4.3 preflight `5b40fa2e` revealed G14 ecosystem footprint is **57 deduped files** — substantially larger than initial Step 4.2 enumeration (~15 sites). Full enumeration per G14:

**Parser ecosystem** (3 module-level + 8 export sites):
- `application/walker/keys.py:12` `_ATOM_KEY_RE` regex literal must update both pattern (`r"^b..."` → `r"^c..."`) and module-level constant name (`_ATOM_KEY_RE` → `_CONDITION_KEY_RE`)
- `application/walker/keys.py:15` `class AtomKeyView` → `class ConditionKeyView` (field accessors `.branch_index` / `.atom_index` also rename per G5/G1)
- `application/walker/keys.py:156` `def parse_atom_key` → `def parse_condition_key` (return type annotation updates)
- Public re-exports: 8 sites across `application/walker/__init__.py:39, 40, 53, 68` + `application/__init__.py:58, 66, 118, 189`

**Carrier field ecosystem** (preflight `5b40fa2e` per-cluster file counts):
- `core/store/_support.py:37` `PredWitness.pred_atom_key` → `pred_condition_key`
- `core/store/_support.py:49` `NonFactStep.step_key` → **PRESERVE** (no atom suffix; N19 boundary)
- `core/store/_support.py:67` `RuleRefEdge.ruleref_atom_key` → `ruleref_condition_key`
- `core/rules/ruleref_types.py:31` `ruleref_atom_key` → `ruleref_condition_key`
- `core/store/_candidate_evidence_tree.py` — **21 hits** (preflight refined from blueprint ~15 estimate)
- `core/rules/_trace.py` — **22 hits** (NOT in Step 4.2 enumeration; preflight `5b40fa2e` PF-R1 surfaces)
- `core/rules/rule_ir.py` — **5 hits** (NOT in Step 4.2 enumeration; PF-R1 new finding)
- `core/rules/frontier.py` — **1 hit** (NOT in Step 4.2 enumeration; PF-R1 new finding)
- `core/annotation/_certainty.py` — references atom_key family (NOT in Step 4.2 enumeration; PF-R1 new finding)

**Adapter consumer ecosystem** (NEW finding per PF-R1):
- `adapters/souffle/where_compile.py` — references atom_key family
- `adapters/souffle/engine_eval.py` — references atom_key family

**SDK shells ecosystem** (NEW finding per PF-R1):
- `sdk/shells/diagnose.py` — references atom_key family

**Application runtime ecosystem** (most NOT in Step 4.2 enumeration):
- `application/proofframe_runtime.py`
- `application/rule_add_condition_runtime.py`
- `application/rule_disable_runtime.py`
- `application/rule_literal_replace_runtime.py`
- `application/protocol/evaluate_result.py`
- `application/walker/errors.py`
- `application/walker/views.py`
- `application/walker/ir.py`

**Local parser ecosystem** (Diagnose + Check siblings have their own local copy of branch parser):
- `application/diagnose_runtime.py:764-806` local helper `_derive_branch_index_from_artifact` + `_parse_branch_index` uses `pred_atom_key` / `step_key` — update field references; if regex duplicated, update pattern
- `application/derivation_check_runtime.py:382-420` similar local helpers — update

**Test ecosystem** (per Step 4.3 preflight 57-file deduped count — 18 unique `tests/` files + 6 `src/domains/ecss/tests/` files):
- `tests/test_walker_keys.py` + `test_walker_views_*.py` + `test_walker_invariants.py`
- `tests/test_application_*_runtime_*.py` (multiple)
- `tests/test_capability_helpers_*.py` (multiple)
- `tests/test_souffle_witness_where_compile_v1.py`
- `tests/test_core_annotation_certainty.py`
- `tests/application/protocol/test_evaluate_result_dtos.py`
- `src/domains/ecss/tests/test_*_contracts.py` × 6 files

**Service ecosystem** (NEW finding per PF-R1):
- `src/service/static_ui.py` — references atom_key family
- `src/service/docs/03_runtime_queries_policy.md` — current docs ref

**[PERSISTENCE JSON DICT-KEY CASCADE per Step 4.4 PF-r2]**:

`pred_atom_key` and `ruleref_atom_key` are NOT only Python attribute names — they are persisted JSON dict keys in support artifact serialization. `core/store/_support.py:229` `support_artifact_to_dict(artifact: ProofReceipt)` emits JSON containing `{"pred_atom_key": ..., "ruleref_atom_key": ...}`; `support_artifact_from_dict` at line 265 reads same keys. B1-preserved helper function names (`support_artifact_to_dict` / `_from_dict` / `_bytes` / `compute_support_digest`) propagate B2 field renames into the JSON dict-key change. Therefore G14 ecosystem rename `pred_atom_key → pred_condition_key` + `ruleref_atom_key → ruleref_condition_key` **changes persisted JSON schema dict keys** — same hard-cut wire/persistence category as G7 `engine_payload → proof`. Per §4.3.6 hard-cut acceptable for alpha; budget for test fixture + JSON snapshot regeneration.

**[CADENCE BUDGET per Step 4.4 PF-R1]**: 57-file G14 ecosystem + 132-file total B2 deduped footprint (preflight `5b40fa2e` PF-v10) larger than B1 (114 files); 3-commit pattern budgeted per Q-NAMING-E Red slice precedent (main feat + likely P1 fix + docs align). Single-feat-commit theoretically viable per Q-NAMING-B1 precedent BUT ecosystem cascade + persistence JSON dict-key change + extensive fixture/snapshot regeneration risk surfaces fixture migration misses during Step 4.7 Claude review.

**Preflight 2.k mandatory** (added Step 4.2 P2-2): enumerate all `*_atom_key` / `_ATOM_KEY_RE` / `AtomKey*` / `parse_atom*` references across `src/` + `tests/`; lock per-site target (RENAME vs PRESERVE per N19); confirm 8 export sites + 5 carrier field sites + 2 local parser site clusters covered.

### §5.6 WhyNot wording narrow target (G9 — [LOCKED Step 4.2 P2-3])

Per §4.3.5 NARROW target + §4.8.4 — only WhyNot partition row types / status field values renamed. Step 4.2 P2-3 LOCKED targets aligned with shipped Diagnose convention at `why_not_runtime.py:388` (`if diagnose_result.status == "passed":`) to avoid introducing new vocabulary. Step 4.1 enumerated sites:

**In-scope sites (LOCKED targets per Step 4.2 P2-3)**:

| Site | Old | LOCKED new |
|---|---|---|
| `derivation_why_not.py:131` `field_name="green"` | `green` | `passed` |
| `derivation_why_not.py:133` `field_name="red"` | `red` | `failed` |
| `derivation_why_not.py:284` `class WhyNotRedRow` | `WhyNotRedRow` | `WhyNotFailedRow` |
| `derivation_why_not.py:312` `field_name="green"` | `green` | `passed` |
| `derivation_why_not.py:313` `field_name="red"` | `red` | `failed` |
| `derivation_why_not.py:318` `"green"` assignment key | `green` | `passed` |
| `derivation_why_not.py:319` `"red"` assignment key | `red` | `failed` |
| `why_not_runtime.py:391` `code="WHY_NOT_DIAGNOSE_PASSED_RED_BINDING"` | `RED_BINDING` | `FAILED_BINDING` (final code: `WHY_NOT_DIAGNOSE_PASSED_FAILED_BINDING` — reads as "Diagnose passed a binding that Why-not classified as failed") |
| `derivation_why_not.py:209` `class WhyNotRowDiagnostic` | (verify content uses green/red) | preflight 2.i confirms whether internal references to green/red exist; if yes, propagate `passed`/`failed`; class name itself preserved |

**LOCKED rationale (Step 4.2 P2-3)**: shipped Diagnose convention already uses `passed`/`failed` status vocabulary at `why_not_runtime.py:388`; unifying WhyNot partition labels reduces mixed-vocabulary inconsistency. Rejected alternatives:

- `holds`/`misses` (positive-query semantics): rejected as introducing new vocabulary not grounded in shipped code
- `accepted`/`rejected` (acceptance semantics): rejected for same reason

**Out-of-scope per §4.3.5 narrow + §4.8.4**: "green"/"red" mentions in docs (CSS / UI / colored text), historical material, test names if not referencing the renamed field/class. Preflight 2.i discriminates per-site.

### §5.7 Docs + tests + examples migration (extended Step 4.2 P3-2)

Per Q-NAMING decision §4.8.5 (inherited from B1 N-3 enumeration): current SDK docs (`src/factgraph/*/docs/`), current public docs (`docs/`), current quickstarts, current example notebooks not in dirty baseline, and active tests migrate in this slice. Historical material under `workflow/heritage/` / `workflow/blueprints/archive/` / `workflow/audit/archive/` / `docs/references/working/` / `examples/archive/` is carved out.

**Step 4.6.5 must enumerate ALL non-historical subdirs (per Step 4.2 P3-2 extension)**, not only `src/factgraph` + `tests/`:

- `src/factgraph/` (primary scope: 44 src files at Step 4.1 grep)
- `src/service/` (`atom_key` 2 files confirmed at Step 4.1 — preflight 2.b enumerates for all 7 + WhyNot)
- `src/domains/` (0 files for sampled symbols; preflight 2.b confirms for all 7)
- `src/agent/` (preflight 2.b verifies)
- `tests/` (primary scope: 42 src files; preflight refines)
- `examples/` (Step 4.1: 6 files branch_index + 3 atom_index + 4 engine_payload; deduped likely 4-8 files; N-7 precedent applies)
- `tutorials/` (Step 4.1: 1-2 files; N-7 precedent)
- `docs/` (Step 4.1: 9 branch_index + 6 atom_index + 4 engine_payload current docs; deduped likely 10-15 files; per B1 N-3 ground truth: `docs/references/external/`, `docs/official/kernel/quickstart/`, root `docs/quickstart/`)

N-7-style proactive scan during implementation per Q-NAMING-B1 precedent: `examples/round_story_full_demo.py` + `tutorials/capability-decision-tree.cn.md` + `tutorials/evidence-pipeline.cn.md` likely contain `branch_index` / `atom_index` references; Step 4.6.5 pre-impl grep enumerates.

Step 4.6.5 pre-impl grep mandatory for this Red slice — distinguishes current vs historical doc paths + full test cohort + diagnostic key fixture sites + spillover subdirs.

### §5.8 Step 4.6.5 pre-implementation grep inventory

Populated at Step 4.6.5 on implementation branch `v0.2.0-impl-q-naming-b2-2026-05-31`. The authoritative grep set uses the union of the 7 long-form fields, G14 ecosystem names, G8 diagnostic key literals, and G9 WhyNot locked names across `src/factgraph`, `tests`, `src/service`, `src/domains`, `examples`, `tutorials`, and `docs`.

- **N-1 Active tests**: 56 active test files require review/migration (`tests/` + `src/domains/ecss/tests/`). This includes B2 field tests, G14 ecosystem tests, WhyNot tests, and canonical diagnostic key fixture tests. The 6 ECSS domain tests from PF-R1 are in-scope.
- **N-2 Active source**: 57 active source files require review/migration across `src/factgraph` + `src/service`. This includes the PF-R1 G14 clusters (`core/rules/_trace.py`, `core/rules/rule_ir.py`, `core/rules/frontier.py`, `core/store/_candidate_evidence_tree.py`, `adapters/souffle/*`, `sdk/shells/diagnose.py`) plus the primary B2 DTO/runtime source files.
- **N-3 Current docs/examples/tutorials**: 11 current non-historical documentation and example/tutorial files require review: `examples/03_proofframe_rule_overlays.ipynb`, `examples/04_round_persistence_diff.ipynb`, `examples/round_story_full_demo.py`, `tutorials/evidence-pipeline.cn.md`, `src/factgraph/application/docs/01_overview_en.md`, `src/factgraph/audit/docs/01_overview.en.md`, `src/factgraph/audit/docs/03_audit_package_contract.md`, `src/factgraph/core/docs/01_architecture.en.md`, `src/factgraph/sdk/docs/01_concepts.en.md`, `src/factgraph/sdk/docs/07_walker_and_advanced.en.md`, and `src/service/docs/03_runtime_queries_policy.md`.
- **N-4 Q-PR1 verification**: long-form B2 fields remain 0 hits across Q-PR1 sacred paths. Q-PR1 short-form variants remain carve-outs per N20: `branch_idx`, `atom_idx`, and `_b{branch_idx}` in `adapters/pyreason/` are preserved.
- **N-5 Cross-cutting cluster**: 56 active files contain both `branch_index` and `atom_index`; these require simultaneous field rename plus diagnostic key update to avoid mixed vocabulary.
- **N-6 Historical / dirty carve-outs**: 20 matched files are out of implementation scope: `docs/references/working/**`, `examples/archive/**`, and dirty notebooks `examples/01_sdk_check_diagnose.ipynb` + `examples/02_overlay_why_not_frontier.ipynb`. They are classified only, not edited.
- **N-7 Examples/tutorials proactive scan**: in-scope current examples/tutorials are `examples/03_proofframe_rule_overlays.ipynb`, `examples/04_round_persistence_diff.ipynb`, `examples/round_story_full_demo.py`, and `tutorials/evidence-pipeline.cn.md`. Dirty notebooks and archive examples remain untouched.
- **N-8 Diagnostic key canonical fixtures / snapshots**: 82 active files contain persistence or key-literal fixture material (`engine_payload`, `pred_atom_key`, `ruleref_atom_key`, `"bN.aN"` literals, or `"proof"` JSON keys). Implementation must regenerate or update these in the same commit family, while preserving the P2-4 separate `"proof"` schemas.
- **N-9 Persistence schema digests**: no new blocker found beyond PF-r2. B2 intentionally changes persisted support-artifact JSON dict keys (`pred_atom_key` / `ruleref_atom_key`) and field payloads as alpha hard-cut; test fixture and snapshot regeneration is part of Step 4.7.

### §5.9 Q-NAMING-AD + C + E + B1 precedent reuse

Patterns from prior Phase 1/2/3/4 slices applicable here:

- **Hard-cut per Sub-Q1 + §4.8.1** (no alias, no dual-emit, old removed in same commit)
- **N7 layer authority preservation** (AD/C/E/B1 inherited): core IR + low-level protocol details retain internal names where they don't appear on the renamed public field surface
- **PF-R1 Q-PR1 sacred detection discipline** (Q-NAMING-E precedent): preflight verifies each field name against Q-PR1 paths before lock
- **Test/example same-slice migration** (audit §3 mandate per Sub-Q3 single-slice cadence)
- **AD/C/E/B1 inherited preservation invariants** (N12-N18 explicit)
- **Step 4.7 P1 fix pattern** (Q-NAMING-E precedent for Red slices): Codex independent test re-run during Step 4.7 review is likely to surface 30-50 fixture migration misses requiring P1 fix commit; budget for 3-commit pattern (main feat + P1 fix + docs align)
- **N-7 proactive examples/tutorials extension** (Q-NAMING-B1 precedent): implementation surfaces current examples/tutorials beyond initial §5.6 enumeration; migrate in same feat commit

## 6. Boundaries And Invariants

- **必须保持的边界**:
  - Q-PR1 sacred 5 paths: 0-diff vs `4c472b50` after every commit
  - Sacred master `562c74195df43e933bed92a3ff25de94dd8ce666` — never modified
  - Dirty baseline preserved — never `git add` baseline files
  - Layer authority per architecture_principles.md §2.1: application protocol field rename; core IR preserved per N7
  - **AD slice contracts** (N12): `fg.assertion_views`, `FrozenAssertionSet`, etc. preserved
  - **C slice contracts** (N13): 20 flat shells removed, namespaced manager rewires preserved, `commit_assertions` byte-identical preserved
  - **E slice contracts** (N14): `Case` class, `Rule.when`, `Inference.when`, `EmitSpec` preserved
  - **B1 slice contracts** (N18): 12 class symbols + 4 support_artifact_* helpers + B1 preserved fields (`_row_support_artifacts`, `literal_path`, `added_atom`, `atom_verdicts`) preserved
  - **B2 field-vs-substring-containment boundary** (§5.2): rename ONLY the 7 explicit symbols + 10 diagnostic key sites + WhyNot narrow target; `atom_*` / `branch_*` substring containment outside spec PRESERVED
- **明确不做的内容**:
  - N1-N19 above
  - Any class symbol rename (B1 scope, already shipped)
  - Any method rename (out of scope; rename-only slice)
  - Any class body restructure
  - Any new class introduction
  - `atom_bounds` (PyReason-domain, §4.3.3 + N8)
- **兼容性约束**:
  - Hard-cut per §4.8.1 — no aliases, no dual-emit, no deprecation shims
  - Alpha release, no historical user protection burden
  - Old field name removed in same commit as new name introduced
  - Diagnostic key format change is hard-cut (recorded payloads change v0.2; per §4.3.6 acceptable)
  - All renames + diagnostic key + WhyNot wording land in same single feat commit unless Step 4.7 review surfaces P1 fix (3-commit pattern per Red slice precedent)

## 7. Acceptance

- [ ] G1: `atom_index` removed; `condition_index` present at all DTO field + parameter + dict-key sites
- [ ] G2: `atom_key` removed; `condition_key` present at all sites
- [ ] G3: `_atom_index_from_key` removed; `_condition_index_from_key` present
- [ ] G4: `materialized_atom_index` removed; `materialized_condition_index` present
- [ ] G5: `branch_index` removed; `case_index` present at all DTO + walker + support capture + evaluation row sites
- [ ] G6: `runtime_branch_index` removed; `runtime_case_index` present
- [ ] G7: `engine_payload` removed; `proof` present at all wire/serialized payload sites
- [ ] G8: All 10 diagnostic key emit sites rewritten from `f"b{branch_index}.a{atom_index}:..."` (or variant) to `f"c{case_index}.c{condition_index}:..."`; prefix-match consumers updated; test fixtures + JSON snapshots regenerated
- [ ] G9: WhyNot wording narrow target migrated **per Step 4.2 P2-3 LOCKED defaults**: `green→passed`, `red→failed`, `WhyNotRedRow→WhyNotFailedRow`, error code `WHY_NOT_DIAGNOSE_PASSED_FAILED_BINDING` (aligns with shipped Diagnose convention at `why_not_runtime.py:388`)
- [ ] G10: Hard-cut honored — no aliases, no dual-emit, old field names removed same commit
- [ ] G11: Module docs + current SDK docs + current quickstarts + current examples + N-7 tutorials + active tests migrated **including spillover subdirs `src/service` + `src/domains` + `src/agent` + `examples/` + `tutorials/` + `docs/` per Step 4.2 P3-2**
- [ ] G12: Q-PR1 sacred 5 paths 0-diff vs `4c472b50` confirmed at impl commit HEAD **including N20 preservation of `_b{branch_idx}` + `branch_idx`/`atom_idx` short-form locals**
- [ ] **G14 (added Step 4.2 P2-2): Diagnostic key ecosystem migrated** — `AtomKeyView→ConditionKeyView` + `parse_atom_key→parse_condition_key` + `_ATOM_KEY_RE→_CONDITION_KEY_RE` (with regex pattern update) + `pred_atom_key→pred_condition_key` + `ruleref_atom_key→ruleref_condition_key` + 8 export sites + 2 local parser site clusters; `step_key` PRESERVED per N19
- [ ] **G15 (added Step 4.2 P2-4): `proof` collision boundary honored** — only `EvidenceEnvelope.engine_payload` and equivalent payload field renamed; Souffle provenance `"proof"` keys + candidate evidence tree role `"proof"` keys + ProbLog proof tree role keys all PRESERVED per §5.3 + preflight 2.f classification
- [ ] **G16 (added Step 4.2 P3-1): Branch-id namespace decision per preflight 2.h Category B** — locked rename target for `branch_id=f"b{idx}"` namespace identifier vs preserve; N21 `branch_id` field name PRESERVED
- [ ] Sacred master `562c74...` unchanged through slice
- [ ] Dirty baseline preserved (no baseline file in `git diff --cached`)
- [ ] All targeted Q-NAMING-B2 verification completed; known baseline failures recorded in §10 with no new regression
- [ ] No push without explicit user authorization
- [ ] AD slice contracts preserved (N12 honored)
- [ ] C slice contracts preserved (N13 honored)
- [ ] E slice contracts preserved (N14 honored)
- [ ] B1 slice contracts preserved (N18 honored — 12 class symbols + 4 support_artifact helpers + 4 B1 preserved fields untouched)
- [ ] B2 field-vs-substring boundary honored (§5.2) — no `added_atom` / `atom_verdicts` / `atom_locator` / `literal_path` / `support_artifact_*` / `atom_bounds` renames; preserved unchanged
- [ ] Error/log message policy honored (§5.2.1) — field-name tokens updated; class-name tokens preserved; diagnostic key tokens reformatted
- [ ] Scope grep verify: 0 occurrences of any 7 old field names + old diagnostic key format `b{N}.a{N}` in `src/` + `tests/` + current docs (excluding historical/archive carve-outs + variant-form pre-existing patterns confirmed at preflight 2.h)
- [ ] Q-PR1 boundary verify: 0 references to any 7 old field names or new field names within Q-PR1 sacred paths

## 8. Implementation Plan

1. **Step 4.2 review (Codex)**: surface 3-6 tightenings per CADENCE Rule 1 + Rule 2 + scope discipline. Cross-check AD/C/E/B1 inherited state untouched. Particularly probe: (a) §5.2 field-vs-substring boundary clarity, (b) Q-PR1 sacred verification methodology, (c) WhyNot wording Sub-Q lock decision, (d) `engine_payload → proof` `proof` collision assessment with existing English-word usage, (e) diagnostic key variant-form sites scope (sdk/store.py:3639 `f"b{idx}"`, rule_add_condition_runtime.py:301 `add{action_index}` variant, rule_expr_lowering.py:723 `branch_id=f"b{idx}"`), (f) cross-rename ordering for `branch_index` + `atom_index` co-occurrence sites.
2. **Step 4.3 preflight (Claude per AD/C/E/B1 precedent)**: independent branch `v0.2.0-q-naming-b2-preflight-2026-05-31`. Re-read all blueprint-referenced shipped files at preflight-row drafting time per Rule 1. Build 5-bucket severity findings table. **Mandatory preflight findings**:
   - 2.a **Q-PR1 sacred verification (per Q-NAMING-E PF-R1 precedent + Step 4.2 P2-1 extension)**: re-grep all 7 long-form field names + WhyNot wording against Q-PR1 sacred 5 paths; expected 0 hits per Step 4.1 scan. **Additionally re-grep diagnostic-prefix variants** in Q-PR1 sacred: `b{`, `_b`, `branch_idx`, `atom_idx` (short-form), `_ATOM_KEY_RE`-style patterns. Confirm `pyreason/where_compile.py:56` `_b{branch_idx}` rule_name suffix + `branch_idx`/`atom_idx` short-form locals (~32 hits) are scoped OUT per N20 + Q-PR1 0-diff. If any long-form rename target returns hits, lock new option similar to Q-NAMING-E G4 Option D preserve.
   - 2.b **Exact footprint distribution + export module list**: refine Step 4.1 approximate hit counts to deduped per-file counts per symbol; verify any `__init__.py` re-export that needs migration (especially WhyNot row type if class renamed in G9).
   - 2.c **Field-level vs class-level discrimination**: verify each B2 rename target is field/parameter/dict-key only; confirm 12 B1 class symbols + 4 helper functions remain untouched per N18.
   - 2.d **§5.2 boundary verification (extended Q-NAMING-B1 §5.2 P2-2 precedent)**: confirm `added_atom`, `atom_verdicts`, `atom_locator`, `literal_path`, `support_artifact_*`, `_row_support_artifacts`, `atom_bounds`, `branch_id` (when not branch_index) all preserved per N18 + N19. Per §5.2.1 error message policy: enumerate field-name token UPDATE sites + diagnostic key token sites + class-name token PRESERVE sites.
   - 2.e **Diagnostic key cross-cutting cluster**: verify all 10 emit sites + prefix-match consumers (`_support_capture.py:44`) + variant-form sites (sdk/store.py:3639, rule_add_condition_runtime.py:301, rule_expr_lowering.py:723). Confirm which variants are in scope vs preserve.
   - 2.f **Naming conflict re-verification (extended Step 4.2 P2-4 for `proof` structural classification)**: re-grep 7 new field names against full `src/` + `tests/` + `docs/`. **For `proof` collision**, classify each shipped `"proof"` structural use per spot check + lock UPDATE vs PRESERVE per source:
     - `adapters/souffle/provenance.py:268` `proof_payload = payload.get("proof")` — Souffle provenance JSON schema = **PRESERVE**
     - `tests/test_souffle_provenance_v0.py`, `tests/test_souffle_evidence_graph.py` JSON fixtures `"proof"` = **PRESERVE** (mirror Souffle schema)
     - `core/store/_candidate_evidence_tree_narrative.py:90, 346, 350` role tuple `("proof",)` + `value.get("proof")` + `out["proof"]` = **PRESERVE** (candidate evidence tree role enumeration; not EvidenceEnvelope.engine_payload)
     - `core/store/_candidate_evidence_tree_summary.py:26, 27, 125, 128` role mapping `"proof_goal": "proof"` / `"proof_leaf": "proof"` + count key = **PRESERVE**
     - `EvidenceEnvelope.engine_payload` / equivalent wire payload field = **UPDATE per G7**
     - Confirm no module-level `def proof` / `class Proof` / top-level field `proof: Type` collision created by G7 rename.
   - 2.g **AD + C + E + B1 inherited state preservation**: confirm Q-NAMING-B2 scope does NOT touch any AD or C or E or B1 contract surface.
   - 2.h **Variant-form diagnostic key scope decision (extended Step 4.2 P3-1 — 3-category split; LOCKED Step 4.4 PF-r1 Category B Option A)**: lock per Category A/B/C per §5.5 step 3:
     - **Category A (canonical diagnostic key)**: `f"b{branch_index}.a{atom_index}:..."` at 9 emit sites — confirmed UPDATE to `c{case_index}.c{condition_index}:...` per G8
     - **Category B (branch-id namespace)** **[LOCKED Step 4.4 PF-r1 Option A]**: `branch_id=f"b{idx}"` at `protocol/rule_expr_lowering.py:722` (inside `_assign_branch_ids` lines 718-722) + `fallback_id = f"b{idx}"` at `sdk/store.py:3639` (Case.id fallback) — **LOCKED UPDATE `b` → `c` (template `f"c{idx}"`)** for Case class supremacy. Field name `branch_id` PRESERVED per N21.
     - **Category C (Q-PR1 sacred adapter)**: `_b{branch_idx}` rule_name suffix at `pyreason/where_compile.py:56` + `branch_idx`/`atom_idx` short-form locals (preflight `5b40fa2e` refined to 38 hits = `branch_idx` 27 + `atom_idx` 10 + `_b{branch_idx}` 1) — PRESERVE per N20 + Q-PR1 0-diff invariant.
     - Variant `add{action_index}` at `rule_add_condition_runtime.py:301`: `b` → `c` per Category A; `add` prefix preserved (action-specific); `branch_index` field rename per G5.
   - 2.i **WhyNot narrow target enumeration**: re-verify all WhyNot wording sites per §5.6 table; confirm Step 4.2 locked targets; surface any out-of-scope "green"/"red" mentions that should NOT migrate.
   - 2.j **Persistence schema digest check (per §4.5 AD pre-lock cross-ref)**: re-confirm no blocker on `branch_index → case_index` impact on persisted digests beyond what AD pre-lock at `4874a76e` already surfaced.
   - 2.k **Diagnostic key ecosystem enumeration (added Step 4.2 P2-2)**: re-grep `*_atom_key` (field/parameter names) + `_ATOM_KEY_RE` (regex constants) + `AtomKey*` (class symbols) + `parse_atom*` (function names) across `src/` + `tests/`; confirm 8 export sites (`walker/__init__.py` + `application/__init__.py`) + 5 carrier field sites (`PredWitness.pred_atom_key` + `RuleRefEdge.ruleref_atom_key` + `ruleref_types.py:31` + ~15 callsites in `_candidate_evidence_tree.py`) + 2 local parser site clusters (`diagnose_runtime.py:764-806` + `derivation_check_runtime.py:382-420`). Lock per-site target (RENAME per G14 vs PRESERVE per N19); confirm `step_key` (no atom suffix) PRESERVED. Surface any `core/rules/_trace.py` references not yet caught.
   - 2.l **Sub-Q3 single-slice decision**: confirm all renames + G14 ecosystem migration can land in one feat commit (likely viable per Q-NAMING-B1 precedent) OR identify defensible split. Red slice may need 3-commit pattern (feat + P1 fix + docs align per Q-NAMING-E precedent) — budget accordingly. With G14 ecosystem migration adding parser/class/export coordination, splitting per (G1-G9 field rename) + (G14 ecosystem) is a viable defensive option if Step 4.7 surfaces P1 fix.
3. **Step 4.4 preflight amendment (on blueprint branch)**: apply Required + Recommended PFs.
4. **Step 4.5 self-check (lightweight)**: PF coverage verification, no commit.
5. **Step 4.6 scoped anchor**: `Status: draft` → `Status: scoped` single small commit + audit log event row.
6. **Step 4.6.5 pre-impl grep amendment (MANDATORY for Red slice)**: pre-impl grep covering all 7 field names + 10 diagnostic key sites + WhyNot wording + canonical fixture/snapshot sites. Surface any consumer beyond §5.8 enumeration as N-1, N-2, ... Apply Option 2 (no status rollback).
7. **Step 4.7 implementation (Codex 承接)** on branch `v0.2.0-impl-q-naming-b2-2026-05-31`:
   - 7.1-7.4 Rename §4.3.2 atom-cascade (G1-G4)
   - 7.5-7.7 Rename §4.3.4 branch-cascade (G5-G7)
   - 7.8 Rewrite §4.3.4 diagnostic key format (G8 — 10 sites + prefix-match consumers + variant-form sites + test fixtures + JSON snapshots)
   - 7.9 Apply §4.3.5 WhyNot wording narrow target (G9 — per Step 4.2 lock)
   - 7.10 Update `__init__.py` exports if applicable (preflight 2.b confirmed)
   - 7.11 Migrate tests (one-pass per file, code + test together; budget for 3-commit pattern per Red slice)
   - 7.12 Update module docs (`src/factgraph/*/docs/`) + current SDK docs + quickstarts + current examples + N-7 tutorials/examples per §5.7
   - 7.13 Lint pass (`ruff check` targeted)
   - 7.14 Single feat commit (or 3-commit pattern per CADENCE if Step 4.7 review surfaces P1 fix per Q-NAMING-E precedent). **Implementation discipline reminder** (per §5.2 + §5.2.1): field rename only; class symbols + B1 helpers + B1 preserved fields + substring-containment-outside-spec PRESERVED; error message field-name tokens update; class-name tokens preserve; diagnostic key tokens reformat.
8. **Step 4.7 review (Claude)**: independent test re-run (CRITICAL for Red slice per Q-NAMING-E lesson — Codex's test counts must be verified independently), scope grep (verify all 7 old field names + old diagnostic key format → 0 in current code), cross-check PF / N findings, cross-check AD + C + E + B1 contract preservation, verify class-level not touched (B1/B2 boundary in reverse).
9. **Step 4.8 closure**: `Status: scoped` → `Status: implemented` + §10 Outcome / Deviations filled + audit log "implemented" event.
10. **Step 4.9 archive**: `git mv` blueprint + audit log pair from `active/` to `archive/` + `INVENTORY.md` entry + audit log archived event row.

## 9. Docs To Update

- `src/factgraph/sdk/docs/` (Step 4.6.5 enumerates exact set; likely includes 01_concepts.en.md, 04_api_surface.en.md, 07_walker_and_advanced.en.md)
- `src/factgraph/application/protocol/docs/` (verify exists)
- `src/factgraph/application/docs/` (01_overview_en.md likely)
- `src/factgraph/application/walker/docs/`
- `src/factgraph/audit/docs/`
- `src/factgraph/core/store/docs/` (likely diagnostic key documentation)
- `src/factgraph/core/docs/` (01_architecture.en.md likely)
- `docs/quickstart/` (or equivalent)
- `docs/official/kernel/quickstart/` — Step 4.6.5 enumerates exact touched files
- `docs/references/external/` (per B1 N-3 — verify any external comparison refs use B2 field names)
- `src/service/docs/03_runtime_queries_policy.md` (per B1 N-3 — extensive `engine_payload` references)
- N-7 examples/tutorials per Step 4.6.5 enumeration
- `docs/README.md` — only if new persistent docs entry introduced (none expected)
- Historical material under `workflow/heritage/` / `workflow/blueprints/archive/` / `workflow/audit/archive/` / `docs/references/working/` — NOT touched per Q-NAMING §4.8.5

## 10. Outcome / Deviations

**Step 4.4 PF locks** (per Step 4.3 preflight `5b40fa2e`):

- **PF-R1 G14 ecosystem expansion LOCK**: blueprint §5.5.1 expanded to enumerate full 57 deduped files (vs Step 4.2 ~15 estimate); new clusters surfaced: `core/rules/_trace.py` (22 hits), `core/rules/rule_ir.py` (5 hits), `core/rules/frontier.py` (1 hit), `core/annotation/_certainty.py`, `adapters/souffle/{where_compile,engine_eval}.py`, `sdk/shells/diagnose.py`, 8 `application/` runtime/walker files, 6 `src/domains/ecss/tests/` files, 2 `src/service/` files. Total B2 deduped footprint = 132 files (vs B1's 114).
- **PF-r1 Category B branch-id namespace Option A LOCK**: `branch_id=f"b{idx}"` template (`rule_expr_lowering.py:722` + `sdk/store.py:3639`) → `f"c{idx}"` for Case class supremacy from Q-NAMING-E. Field name `branch_id` PRESERVED per N21.
- **PF-r2 persistence JSON dict-key cascade LOCK**: `pred_atom_key`/`ruleref_atom_key` are persisted JSON dict keys via B1-preserved `support_artifact_to_dict`/`_from_dict` helpers. Rename = wire/persistence hard-cut category like G7. Test fixture + JSON snapshot regeneration budgeted into Step 4.7.

**Step 4.7 implementation discipline carry-forward** (per Step 4.3 preflight PF-s1 at `5b40fa2e`):

- **G14 ecosystem rename ordering** for largest clusters: (1) Carrier classes first — `PredWitness.pred_atom_key → pred_condition_key` + `RuleRefEdge.ruleref_atom_key → ruleref_condition_key` at `_support.py:37, 67` + `ruleref_types.py:31`; (2) Propagate through `_candidate_evidence_tree.py` (21 sites) + `_trace.py` (22 sites) + `rule_ir.py` (5 sites); (3) Update `walker/keys.py` regex pattern + class + parser; (4) Update 8 export sites in `walker/__init__.py` + `application/__init__.py`; (5) Update local parsers in `diagnose_runtime.py:764-806` + `derivation_check_runtime.py:382-420`; (6) Update B1-preserved `support_artifact_to_dict`/`_from_dict` JSON dict-key emission/parsing per PF-r2 persistence cascade; (7) Regenerate test JSON fixtures + canonical golden snapshots.
- **Cadence budget**: 3-commit pattern (main feat + likely P1 fix + docs align) per Q-NAMING-E Red precedent. Single-feat-commit theoretically viable per Q-NAMING-B1 precedent BUT 57-file ecosystem cascade + persistence JSON dict-key change + extensive fixture/snapshot regeneration risk surfaces fixture migration misses during Step 4.7 Claude review. Defer cadence final decision to Step 4.7 review.

(Step 4.8 closure outcome / deviations to be filled at implementation closure.)
