# Q-NAMING-F Blueprint Audit: Engine config / semantics rename (ProbLog/PyReason Semantics→Config + SemanticsProfile→EngineProfile + semantics=→config= + branch_*→case_*)

- Blueprint: [2026-05-31_q-naming-f.md](./2026-05-31_q-naming-f.md)

## Event Log

| Date | Stage | Event | Notes |
| --- | --- | --- | --- |
| 2026-05-31 | draft | Blueprint created | Initial scope recorded covering Q-NAMING decision §4.7.1 (3 class renames: SemanticsProfile + ProbLogSemantics + PyReasonSemantics) + §4.7.2 (semantics= → config= keyword) + §4.7.3 (engine payload field renames: branch_probabilities → case_probabilities + branch_bounds → case_bounds — cross-ref §4.3.4 B2) + §4.7.4 (SDK helper rename: inspect_semantics → preview_config) + §4.7.7 (semantics_digest → config_digest persistence digest field). Phase 6 of 6 sub-slices per Q-NAMING §4.9 ordering — FINAL phase; closes the Q-NAMING decision routemap. **Red risk** per §4.9 Phase 6 + cross-cutting wire/persistence change + cross-cutting cluster across SDK/application/adapter consumption. Branch `v0.2.0-blueprint-q-naming-f-2026-05-31` forked from Q-NAMING-B2 archive HEAD `0b4c8aad` (inherits Q-NAMING-AD + C + E + B1 + B2 complete lifecycles + N1-N23 invariants). Sacred Q-PR1 5-path 0-diff preserved at draft commit; sacred master `562c74195df4...` unchanged; dirty baseline (4 M + 2 D + 2 untracked) preserved. Step 4.1 grep summary: SemanticsProfile 153 hits / 51 files (heaviest), PyReasonSemantics 55/29, ProbLogSemantics 30/28, semantics= keyword 44 hits, branch_probabilities 53/18, branch_bounds 39/14, semantics_digest 11 files, inspect_semantics 9/10. Deduped F footprint: ~63 unique files (58 src/factgraph + tests + src/service + src/domains + 5 examples/tutorials/docs) — significantly smaller than Q-NAMING-B2 (132 files) due to scope concentration in semantics modules. **CRITICAL Step 4.1 PF-R1 finding**: SemanticsProfile has 29 hits in Q-PR1 sacred `adapters/pyreason/engine_eval.py` (lines 9 import + 54/287/384 type annotations + 293/387 isinstance + 295/299/389/393 error messages + 310/315/319/339/372 carrier strings) — **same pattern as Q-NAMING-E PF-R1 G4 legacy Rule.where Q-PR1 trap**; G1 likely locks Option D (preserve SemanticsProfile entirely) at Step 4.3 preflight 2.a. All other F symbols (ProbLogSemantics, PyReasonSemantics, branch_probabilities, branch_bounds, semantics=, semantics_digest, inspect_semantics) return 0 Q-PR1 sacred hits — proceed with rename. head_bound 21 Q-PR1 hits + atom_bounds 4 files in active code: BOTH out of F scope per §4.7.5 N9 (head_bound CLEANUP not rename) + §4.7.6 N8 (atom_bounds PyReason-domain preserved). No naming conflicts (7 new F target names: ProbLogConfig, PyReasonConfig, EngineProfile, case_probabilities, case_bounds, preview_config, config_digest all return 0 hits in shipped src/+tests/). Likely Step 4.7 2-3 commit pattern (main feat + P1 fix budget per Q-NAMING-E + B2 Red precedent). |
| 2026-05-31 | draft (amended) | Step 4.2 review tightening | Codex Step 4.2 review surfaced 4 Required + 2 Recommended findings, all shipped-code-grounded via Rule 1 re-reads; all 6 applied in this single doc-only amend commit. (P2-1) G1 PF-R1 trap undercounted — blueprint only cited `adapters/pyreason/engine_eval.py` (15 sites) but Q-PR1 sacred `adapters/pyreason/rule_ext.py` ALSO imports/checks/strings SemanticsProfile: line 7 import + lines 78, 142 type annotations + line 146 isinstance check + lines 148, 152 error messages + lines 90, 169 carrier identifier strings (`SemanticsProfile.rule_projection.pyreason and PyReasonRuleExt` + `SemanticsProfile.rule_projection.pyreason[{idx}]`). Total Q-PR1 sacred SemanticsProfile hits = ~22 distinct sites; G1 Option D preserve is even stronger than initial draft. Amended §1/§4/§5.2/§8 preflight 2.a with both sacred files. (P2-2) G1 file path corrected + export sites under-specified — `SemanticsProfile` class lives at [`core/semantics/profile.py:26`](../../../src/factgraph/core/semantics/profile.py) NOT `core/semantics.py` (the latter is a package, not a module). Exports under Option D ALL PRESERVED: [`core/semantics/__init__.py:3+5`](../../../src/factgraph/core/semantics/__init__.py) `from .profile import SemanticsProfile, inspect_semantics_profile` + `__all__`; [`sdk/__init__.py:51+97`](../../../src/factgraph/sdk/__init__.py) `from factgraph.core.semantics import SemanticsProfile` + `__all__`. Amended G1 file path + added §5.1.1-style export coverage to G1 row + §8 preflight 2.b export site enumeration. (P2-3) G8 digest scope needs precise decision table — `semantics_digest` is not just one field; cluster includes: DTO field at `evaluate_result.py:73+173+207-208+349+358-359+373+474+488`; helper function `semantics_digest_for(profile)` declared at `evaluate_result.py:511`, exported at line 1416, imported at `sdk/store.py:53`, called at `sdk/store.py:2681`; dict keys `evaluate_semantics_digest` + `explain_semantics_digest` at `sdk/store.py:2531+2532` + `evaluate_result.py:1247+1248`; service serialization key `"semantics_digest"` at `src/service/runtime_v1.py:2451`; sibling JSON dict + kwargs at `sdk/store.py:2529+2687+2718+2731`. All RENAME together to `config_digest` / `config_digest_for` / `evaluate_config_digest` / `explain_config_digest` per B1 P2-1 convention extended. Amended §5.1 G8 with decision table + §8 preflight 2.i with 10 deduped files enumeration. (P2-4) `config=` keyword collision needs explicit carve-out taxonomy — shipped code already uses `config=` keyword INTERNALLY for adapter `PyReasonRunConfig` plumbing at Q-PR1 sacred `adapters/pyreason/runner.py:412` + `engine_eval.py:97` (`PyReasonRunConfig` class at `runner.py:29`, type at `runner.py:46/345/351`, imported at `engine_eval.py:11`, constructor at `engine_eval.py:135/154`, `resolve_pyreason_run_config(...) -> PyReasonRunConfig` at `engine_eval.py:133`). These are OUT of G4 scope per Q-PR1 0-diff + N7 layer authority. Added N24 explicit carve-out taxonomy + §8 preflight 2.e expansion with per-call-site classification table. (P3-1) G7 "top-level inspect_semantics" wording is stale — shipped reality is `fg.eval.inspect_semantics` namespaced manager method at `sdk/store.py:1302` + private `_inspect_semantics` SDK helper at `sdk/store.py:2263` + error message at line 2272. No flat top-level shell exists post Q-NAMING-C (archived at `23389a2b` deleted 20 flat shells). Reworded G7 to "`fg.eval.inspect_semantics` manager method + private SDK helper + docs/tests" with preserve note for `inspect_semantics_profile(...)` core helper (separate from SDK manager). (P3-2) `head_bound` cleanup wording tightened — N9 + §5.3 head_bound preserve language clarified: F does NOT touch head_bound in any way during implementation (no code, no docs, no related field migration). §4.7.5 "CLEANUP" language refers to potential future Q-NAMING followup OUTSIDE F scope. F implementers MUST NOT attempt "cleanup" code change inside sacred adapter paths. Sacred Q-PR1 0-diff preserved through amend; master unchanged; dirty baseline preserved. |
| 2026-05-31 | draft (amended) | Step 4.4 preflight amendment | Step 4.3 preflight at `ddbeec94` on independent artifact branch `v0.2.0-q-naming-f-preflight-2026-05-31` (forked from `0f779c0e`) surfaced 1 Required (PF-R1) + 2 Recommended (PF-r1, PF-r2) + 8 Verified (PF-v1-v8) + 1 Scoped-detail (PF-s1) + 0 Abandonment findings — CADENCE healthy distribution. Codex Step 4.3 review PASS verdict at `ddbeec94`. All 3 LOCK amendments + Decision Notes applied in this single doc-only amend commit on blueprint branch (NOT preflight branch — per Slice 7B Option A learning). (PF-R1 LOCK G1 Option D PRESERVE) Confirmed Q-PR1 sacred 29 distinct hits across TWO sacred files: `engine_eval.py` 21 sites (lines 9 import + 54/287/384 type annotations + 293/387 isinstance + 295/299/389/393 error msgs + 310/315/319/339/372 carrier strings + 402/469/473/476/485/551 additional refs) + `rule_ext.py` 8 sites (lines 7 import + 78/142 type annotations + 90/169 carrier strings + 146 isinstance + 148/152 error msgs). Per Q-PR1 0-diff + §4.8.1 no-alias + isinstance/carrier blocking, Option D preserve SemanticsProfile entirely is only viable resolution (same structural pattern as Q-NAMING-E PF-R1 G4 Rule.where precedent). G1 implementation footprint reduces from 51 files to 0 files migration; all SemanticsProfile references preserved unchanged. Blueprint §2 G1 + §5.2 + §7 G1 acceptance + §8 preflight 2.a + §10 carry-forward amended. (PF-r1 LOCK G8 11-file digest cluster) Step 4.3 preflight grep refined footprint from blueprint estimate 10 → 11 deduped files: `application/protocol/evaluate_result.py` + `sdk/store.py` + `src/service/runtime_v1.py` + 3 doc files (`audit/docs/02_evidence_graph.md` + `sdk/docs/03_rules_and_inferences.en.md` + `src/service/docs/03_runtime_queries_policy.md`) + 4 tests (`test_problog_semantics_profile_migration.py`, `test_rule_expr_evaluate.py`, `test_evaluate_result_dtos.py`, `test_evaluate_result_digests.py`) + `docs/official/kernel/quickstart/evidence.md`. Per B2 PF-r2 precedent: persistence JSON dict-key cascade applies. Blueprint §5.1 G8 + §8 preflight 2.i amended with 11-file enumeration. (PF-r2 LOCK N24 config= per-call-site classification) Confirmed 2 Q-PR1 sacred adapter-internal `config=` sites (`runner.py:412` + `engine_eval.py:97`) + PyReasonRunConfig class at `runner.py:29` + type at lines 46/345/351 + import at `engine_eval.py:11` + constructor at lines 135/154 + resolver at line 133 — all preserve per N24 + Q-PR1. Blueprint §8 preflight 2.e + §10 carry-forward amended with per-call-site classification requirement for Step 4.7 implementation. PF-v1-v8 verified findings recorded in Decision Notes below per CADENCE verified-findings convention. PF-s1 cross-rename ordering deferred to Step 4.7 §10 closure for implementation choice recording. Sacred Q-PR1 0-diff preserved through amend; master `562c74195df4...` unchanged; dirty baseline preserved; preflight branch at `ddbeec94` untouched. |
| 2026-05-31 | scoped | Preflight amendments and self-check passed | PF-R1 G1 Option D PRESERVE LOCK + PF-r1 G8 11-file cluster LOCK + PF-r2 N24 config= per-call-site LOCK + PF-v1-v8 verified + 1 Required addressed, all covered by `936f248f` (Step 4.4 amend) + `900ffbcd` (Step 4.4 fast-pass cleanup). Step 4.5 self-check confirmed: PF coverage matrix consistent (PF-R1 = 33 mentions; PF-r1 = 4 sites; PF-r2 = 6 sites; PF-v1-v8 = 10 references in audit log; PF-s1 = 3 references); invariant coverage heavy (Q-PR1 = 58 mentions; N13-N24 inherited = 42 mentions; `ddbeec94` preflight HEAD = 11 references across blueprint + audit; LOCKED Step 4.4 banner = 7 sites); §1-§10 main section structure continuous; §5.1-§5.6 subsection structure complete (8 subsections); 0 stale "Default lock expectation" / "trap expected" / "likely PF-R1" / "OR rename" residuals (all cleared in `900ffbcd` cleanup); sacred Q-PR1 5-path 0-diff preserved; sacred master `562c74195df4...` untouched; dirty baseline 8 entries preserved. Scope frozen at ~58 deduped file footprint (51 SemanticsProfile + Option D LOCK reduces actual migration to ~30-40 files) with 8 goals (G1-G8 + G9-G11 cross-cutting) + 24 non-goals (N1-N24) + 10 mandatory preflight items (2.a-2.j) all locked. Ready for Step 4.6.5 pre-impl grep (Codex 承接, MANDATORY for Red slice per Q-NAMING-AD/C/E/B1/B2 precedent — full N-1 to N-9 inventory covering: G8 11-file digest cluster + G2/G3 SDK wrapper rename + G4 N24 per-call-site classification table + G5/G6 PyReasonConfig field rename + G7 namespaced manager rename + G1 Option D PRESERVE site enumeration + persistence JSON dict-key cascade per PF-r2 B2 precedent + N-7 examples/tutorials proactive scan) + Step 4.7 implementation on `v0.2.0-impl-q-naming-f-2026-05-31` (2-3 commit pattern budgeted per Q-NAMING-E + B2 Red precedent; under Option D LOCK actual migration footprint reduces from 58 → ~30-40 files = smaller risk surface than B2). |
| 2026-06-01 | scoped (amended) | Step 4.6.5 pre-impl grep inventory | Codex performed mandatory Red-slice Step 4.6.5 inventory on implementation branch `v0.2.0-impl-q-naming-f-2026-05-31` forked from scoped blueprint `f104fea8`. Blueprint §5.7 was populated with N-1 through N-9 inventory. Key results: G1 SemanticsProfile remains Option D PRESERVE across 50 active files and 29 Q-PR1 sacred hits; G2/G3 active wrapper migration cohort = 27/28 files; G4 public `semantics=` migration distinct from N24 adapter/test `config=` preserve bucket; G5/G6 active field cohorts = 16/13 files; G7 active cohort = 13 files; G8 digest cluster confirmed exactly 11 files with DTO field + helper + dict keys + service serialization + docs/tests. Q-PR1 re-verification: non-G1 F targets still 0 hits; N24 adapter-internal `config=` at `engine_eval.py:97` and `runner.py:412` preserved; N9 head_bound/branch_head_bounds and N8 atom_bounds remain out of F scope; N20 inherited short-form remains preserve-only. Current docs cohort expanded explicitly to docs/official quickstarts, SDK docs, adapter docs, core semantics docs, audit evidence graph doc, and service runtime policy docs. Historical carve-outs remain `docs/references/working/**` and `examples/archive/**`. CADENCE Option 2 honored: Status remains `scoped`, no rollback, no implementation started. Sacred master unchanged; Q-PR1 5-path 0-diff preserved; dirty baseline preserved. |

## Decision Notes

### 2026-05-31 — Initial scope freeze rationale

- **Red-risk scope per Q-NAMING §4.9 Phase 6**: Engine config / semantics rename with hard-cut at v0.2 per Sub-Q1 + §4.7.7 (no alias, no dual-emit, recorded digests + adapter payload field names change in v0.2; acceptable because alpha). Phase 6 is FINAL — closes Q-NAMING 6-phase routemap.
- **8 symbol renames per §4.7.1 (3) + §4.7.2 (1 keyword) + §4.7.3 (2 fields) + §4.7.4 (1 helper) + §4.7.7 (1 digest)**:
  - G1 (§4.7.1): SemanticsProfile → EngineProfile — **likely Option D preserve per PF-R1 Q-PR1 trap**
  - G2 (§4.7.1): ProbLogSemantics → ProbLogConfig
  - G3 (§4.7.1): PyReasonSemantics → PyReasonConfig
  - G4 (§4.7.2): semantics= keyword → config=
  - G5 (§4.7.3 cross-ref §4.3.4 B2): branch_probabilities → case_probabilities
  - G6 (§4.7.3 cross-ref §4.3.4 B2): branch_bounds → case_bounds
  - G7 (§4.7.4): inspect_semantics → preview_config
  - G8 (§4.7.7): semantics_digest → config_digest persistence digest field
- **Critical PF-R1 Q-PR1 trap on G1 SemanticsProfile** (Step 4.1 grep): 29 hits in `adapters/pyreason/engine_eval.py` (line 9 import + 5 type annotation parameters + 2 isinstance checks + 4 error message string mentions + 5 carrier identifier strings referencing `SemanticsProfile.temporal_projection.*` / `SemanticsProfile.iteration_count`). Per Q-PR1 0-diff invariant + §4.8.1 hard-cut no-alias rule + isinstance-check blocking carve-out, **Option D preserve SemanticsProfile entirely** is the expected default lock per Q-NAMING-E PF-R1 precedent. Step 4.3 preflight 2.a formally LOCKS the resolution. If Option D locked, F §4.7.1 G1 SemanticsProfile rename DEFERRED indefinitely; other 7 F renames proceed independently.
- **Positive Step 4.1 findings for non-G1 symbols**: ProbLogSemantics + PyReasonSemantics + branch_probabilities + branch_bounds + semantics= + semantics_digest + inspect_semantics all return 0 Q-PR1 sacred hits. No PF-R1 blocker for these 7 renames.
- **No naming conflicts**: 7 new F target names (ProbLogConfig, PyReasonConfig, EngineProfile, case_probabilities, case_bounds, preview_config, config_digest) all return 0 hits in shipped `src/factgraph` + `tests/`. Preflight 2.f re-verifies.
- **atom_bounds + head_bound carve-outs**: §4.7.6 N8 atom_bounds (PyReason-domain syntax, 4 files preserved) + §4.7.5 N9 head_bound (CLEANUP not rename, 38 hits including 21 Q-PR1) both explicitly out of F scope.
- **§4.7.3 cross-ref §4.3.4 B2**: `branch_probabilities → case_probabilities` + `branch_bounds → case_bounds` align with B2 N22 semantic supremacy (B2 shipped `case_index` / Category B `f"c{idx}"` template; F now finishes case-cascade for engine payload). NO conflict because B2 did NOT rename these names (deferred to F per §4.7.3 scope).
- **N1-N23 inherited preservation**: AD/C/E/B1/B2 inherited contracts all preserved (added N22 for B2 + N23 for F §4.7.3 cross-ref clarity).
- **Q-NAMING-E + B2 precedent reuse**: PF-R1 Q-PR1 detection + Option D preserve (E precedent) + persistence digest hard-cut + Step 4.7 P1 fix budget for Red slices (B2 precedent: 2-commit actually used; B1 1-commit; budget 2-3).
- **Sub-Q3 cadence**: 8 renames likely land in single feat commit (subject to G1 Option D reducing to 7 renames); 2-3 commit pattern budgeted per Q-NAMING-E + B2 Red precedent if Step 4.7 review surfaces fixture migration miss.
- **Phase 6 completion implication**: F archival closes Q-NAMING decision routemap; no further Q-NAMING phases planned.

### 2026-05-31 — Step 4.3 preflight verified findings record (PF-v1 through PF-v8 + PF-s1)

Per CADENCE verified-findings convention, the following Step 4.3 preflight findings (artifact at `workflow/audit/active/2026-05-31_q-naming-f-preflight.md` @ commit `ddbeec94`) require no blueprint amendment but are recorded as Decision Notes for closure §10 cross-reference:

- **PF-v1 (Q-PR1 sacred 5-path verification for 6 of 7 long-form F symbols + semantics= keyword = 0 hits each)**: ProbLogSemantics (0), PyReasonSemantics (0), branch_probabilities (0), branch_bounds (0), inspect_semantics (0), semantics_digest (0), semantics= keyword (0). Only SemanticsProfile has Q-PR1 sacred hits per PF-R1. **No additional Q-PR1 blocker beyond PF-R1**.

- **PF-v2 (N8 atom_bounds + N9 head_bound + N20 short-form carve-outs preserved)**: atom_bounds = 4 files total, 0 in `adapters/pyreason/` (confirms N8 PyReason-domain syntax preserved; appears on PyReasonSemantics wrapper at `sdk/semantics.py:195` per blueprint N8). head_bound = 8 files total, 2 in `adapters/pyreason/` (where_compile.py + rule_ext.py per N9 preserve). branch_head_bounds = 5 files total, 2 in pyreason (same preserve discipline). N20 short-form variants per B2 inherited (`branch_idx`/`atom_idx` 38 hits in pyreason adapter) preserved per Q-PR1 0-diff.

- **PF-v3 (zero shipped naming conflicts for 6 of 7 new F target names; `config_digest` has 0 hits everywhere)**: All 6 other new names (EngineProfile, ProbLogConfig, PyReasonConfig, case_probabilities, case_bounds, preview_config) hit only `docs/references/working/change-requests-2026-05-27/change-requests.md` — explicit §4.8.5 historical carve-out + part of dirty baseline (untracked) per §4.8.8. Same pattern as B2 PF-v3 (`condition_index`/`case_index` in same proposal doc). Not runtime/test conflicts.

- **PF-v4 (B1/B2 inherited surfaces preserved per N19/N22)**: 12 B1 class symbols all present (ProofReceipt 40 files, FactOverlay 21, ConditionPath 9, etc.). B2 7 long-form field renames preserved (case_index 30 files, condition_index 23 files); G14 ecosystem preserved (ConditionKeyView, parse_condition_key, pred_condition_key, ruleref_condition_key); G15 PRESERVED proof schemas; G16 Category B `f"c{idx}"` template; G9 WhyNot LOCKED (passed/failed/WhyNotFailedRow). All AD/C/E inherited surfaces also preserved (FrozenAssertionSet 7, save_workspace 9, commit_assertions 4, assertion_views 6, EmitSpec 4, Case 13). N13-N23 all intact through F.

- **PF-v5 (per-symbol classification verified)**: 3 classes (SemanticsProfile at `core/semantics/profile.py:26`, ProbLogSemantics at `sdk/semantics.py:119`, PyReasonSemantics at `sdk/semantics.py:173`) + 1 keyword arg (`semantics=`) + 2 fields (`branch_probabilities` at `sdk/semantics.py:137` + `branch_bounds` at line 186 — both `dict[str, float]` fields on SDK wrapper classes) + 1 namespaced manager method (`inspect_semantics` at `sdk/store.py:1302` + private `_inspect_semantics` helper at line 2263) + 1 DTO field cluster (`semantics_digest` at `evaluate_result.py:173` + helper function `semantics_digest_for` at line 511 + dict keys + service serialization). Total 8 distinct rename target categories.

- **PF-v6 (footprint refined to 58 deduped files)**: across 7 long-form symbols + semantics= keyword; refined from Step 4.1 blueprint estimate of 63. Per-symbol unique file counts: SemanticsProfile 51 (heaviest), PyReasonSemantics 29, ProbLogSemantics 28, branch_probabilities 18, semantics= keyword 16, branch_bounds 14, semantics_digest 11, inspect_semantics 10. **Significantly smaller than B2 (132 files)** because scope concentrates in semantics modules. Under PF-R1 Option D LOCK, SemanticsProfile 51 files do NOT migrate (preserve entirely) — actual F implementation footprint reduces to ~30-40 files for the 6 other symbols.

- **PF-v7 (G8 semantics_digest cluster fully enumerated — 11 deduped files)**: refined from blueprint §5.1 G8 footprint estimate of 10. See PF-r1 for full enumeration. Per B2 PF-r2 precedent: persistence JSON dict-key cascade applies; budget test fixture + JSON snapshot regeneration.

- **PF-v8 (export sites verified for Option D PRESERVE under PF-R1)**: `core/semantics/__init__.py:3` `from .profile import SemanticsProfile, inspect_semantics_profile` + line 5 `__all__ = ["SemanticsProfile", "inspect_semantics_profile"]` — both UNCHANGED per Option D. `sdk/__init__.py` lines 31 `from .semantics import ProbLogSemantics, PyReasonSemantics` (G2/G3 RENAME target) + 51 `from factgraph.core.semantics import SemanticsProfile` (UNCHANGED per Option D) + 97 `"SemanticsProfile"` UNCHANGED + 104/105 `"ProbLogSemantics"`/`"PyReasonSemantics"` RENAME to `"ProbLogConfig"`/`"PyReasonConfig"`. `application/protocol/__init__.py` does NOT re-export semantics-related symbols (verified empty grep).

- **PF-s1 (cross-rename ordering for Step 4.7, deferred to §10 closure)**: Per blueprint §8 Step 7.1-7.8 sequence + PF-R1 Option D LOCK refinement: (1) G1 SemanticsProfile PRESERVE entirely (no code changes for G1); (2) G2 ProbLogSemantics → ProbLogConfig (class rename at `sdk/semantics.py:119` + sdk/__init__.py:31 import + __all__:104); (3) G3 PyReasonSemantics → PyReasonConfig (class rename at line 173 + sdk/__init__.py:31 + __all__:105); (4) G4 semantics= → config= keyword (with N24 carve-out per per-call-site classification — preserve Q-PR1 adapter `config=` usage); (5) G5 branch_probabilities → case_probabilities (field rename on PyReasonConfig at line 137); (6) G6 branch_bounds → case_bounds (field rename on PyReasonConfig at line 186); (7) G7 inspect_semantics → preview_config (namespaced manager method at `sdk/store.py:1302` + private helper at 2263; preserve core helper `inspect_semantics_profile` per Option D); (8) G8 semantics_digest → config_digest cluster (11 files coordinated rename including DTO field + helper function + dict keys + service + tests + docs). Implementation discipline: ordering prevents intermediate broken states; G1 preserve simplifies coordination (no SemanticsProfile reference chains to update).

### Cross-flip role assignment (per user 2026-05-31 推进 directive + AD/C/E/B1/B2 precedent)

User directive "推进" 2026-05-31 implicitly preserves AD/C/E/B1/B2 cross-flip pattern unless explicitly changed:

- Step 4.1 blueprint draft → Claude (this commit)
- Step 4.2 review → Codex
- Step 4.3 preflight drafter → Claude (per AD/C/E/B1/B2 precedent — likely surfaces PF-R1 G1 Option D LOCK)
- Step 4.4 preflight amendment → Claude (per AD/C/E/B1/B2 precedent)
- Step 4.5 self-check → Claude (doc-only)
- Step 4.6 scoped anchor → Claude (per AD/C/E/B1/B2 precedent)
- Step 4.6.5 pre-impl grep → Codex (承接 implementation prep, AD/C/E/B1/B2 precedent — MANDATORY for Red slice)
- Step 4.7 implementation → Codex (per user directive)
- Step 4.7 review → Claude (per user directive — CRITICAL independent test re-run for Red slice per Q-NAMING-E + B2 lesson)
- Step 4.8 closure → Codex or Claude (per CADENCE no strict assignment)
- Step 4.9 archive → Codex or Claude (per CADENCE no strict assignment — closes Q-NAMING 6-phase routemap)

If user wants to flip any role assignment, they may signal at any handoff point.
