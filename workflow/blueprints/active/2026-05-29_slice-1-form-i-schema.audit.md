# Task Blueprint Audit: Slice 1 — Form I Schema Refactor

- Blueprint: [2026-05-29_slice-1-form-i-schema.md](./2026-05-29_slice-1-form-i-schema.md)

## Event Log

| Date | Stage | Event | Notes |
|---|---|---|---|
| 2026-05-29 | draft | Blueprint created | Drafted after Stage 3 synthesis @ `d0036e1f` + reviewer L7-L11 lock-in choices + preflight code audit v2 covering descriptor / parser / schema compile / schema runtime / rule lowering / derivation compile / protocol / store.ref second default path / facade / batch / value validation / sdk export surfaces. Scope Freeze §6.1 captures 6 binding locks(SF1-SF6)from reviewer 2026-05-29. Class re-estimated as M/L boundary based on N6-N14 rule-lowering semantic rewrite + 325 test callsite migrations + load-bearing docs. Implementation Plan §8 split into 14 steps each at commit boundary per `feedback_smaller_batch_design_blueprints` rule-touching cadence. Q-PR1 zero-dependency confirmed per §6.3 + §7.12. Branch `v0.2.0-blueprint-slice-1-form-i-schema-2026-05-29` forked from synthesis `d0036e1f`. Sacred master `562c7419` and dirty baseline preserved per CADENCE ritual. Commit: `5d211efc` |
| 2026-05-29 | draft | Blueprint amended per reviewer P1×3 + P2 findings(still draft)| User reviewer 2026-05-29 post-draft review 返回 4 findings — 都是实质 gap,不改决策方向但需补 scope/integration/acceptance。**(P1 #1 write-time validation 覆盖面太窄)** 原 §5.13 / §7.9 / Step 11 只挂在 `FieldEditor.set/add`,但 shipped 主要写入路径(`SDKStore.set/add` @ `sdk/store.py:1920/1963` → `_apply_field_mutation` @ `2002` → `apply_write_plan` → `_apply_op` @ `application/entity_write.py:382` → `set_field/add_field`)在 facade 之下汇聚;`fg.write.set/add`(`_SDKWriteManager` @ `sdk/store.py:568-585`)是 SDKStore 别名;`WireBatchPlan.apply` @ `sdk/batch.py:418` 行 430-433 调 `sdk.set/add` 同路径;只挂 FieldEditor 会被 fg.set / fg.write.set / batch.apply 绕过。**修复**:§5.13 重写 integration 点为 `_apply_op` 中央应用层路径,FieldEditor 经 SDKStore 自然覆盖;§7.9 加 coverage validation 6 项(fg.set / fg.add / fg.write.set / fg.write.add / SDKBatchTx / FieldEditor)+ 显式不覆盖 5 项(write_protocol direct / ingest / batch materialization / derivation accept / pyreason adapter);Step 11 集成点同步改。**(P1 #2 Identity(pattern/description) 半死参数)** §5.1 接受 Identity(description, pattern)但 §5.4 只锁 `_compile_field` / `_compile_relationship_field` 写 enum_values/pattern;`_compile_identity_field` / `_compile_identity_predicate` 未锁,Identity(pattern=...)在 schema_ir 中丢失。**修复**:§5.4 加 Identity compile-time `description` + `pattern` 传递;Identity pattern compile-time regex syntax + pattern-on-non-string 校验也适用;Identity write-time enforcement 留 Slice 2(ADR-IC 写入路径成型后),但 Slice 1 必须产出 schema truth;§7.3 加 Identity pattern compile checks + SchemaIndex.PredicateInfo 携带 description/pattern。**(P1 #3 Literal[float] 与 schema canonicalization 冲突)** `schema_ir.canonicalize_schema_ir_jcs` 通过 `_reject_floats`(@ `core/schema/schema_ir.py:217-229`)拒绝任何 float;`enum_values` 含 float 会让 schema digest 崩。**修复**:§5.2 + §5.4 + Step 1.3 + Step 3.3/3.4 + §7.1 全加 float Literal enum reject;`float64` 普通字段仍允许,只是不允许 Literal[float] enum;Step 2+ 若需要 float enum 走单独 canonical encoding ADR。**(P2 step ordering 长红分支)** 原 Step 12 callsite migration 让 Step 1 commit 后 10 个 commit 期间 tests 在 import 阶段崩(`Field(cardinality=)` 在 Entity fixture class 定义里).无法 per-commit verification。**修复**:Step 1 重新框架为 "breaking-atomic commit" — descriptor signature change + 全部 test fixture 迁移 + 全部 Entity-using load-bearing doc example 迁移(NOT NEW Form I content)同一 commit 落地;Step 12 缩为 "NEW Form I docs content"(overview + 类型推断 + dual-layer usage 新内容)。Step 1.12 verify green branch 显式 gate。**Cascade**:§6.1 SF3 加 cross-entity-type same-name reject 负向(`User.id == Order.id`);§6.1 SF6 加 `workflow/audit/active/` 默认不迁移;§7.8 加 SF3 negative cases 5 项(同字段 accept + 4 reject cases 含 cross-entity);Step 0 加 pause-and-amend trigger 4 项(non-zero Identity-headed derivation / exotic allow_identity_defaults caller / bridges/ load-bearing reference / 意外 callsite directory);Step 0.4 加 explicit `docs/references/bridges/` status verification checklist;§9.4 重写为 unconditional + conditional exclude 形式 + Step 0.4 checklist 引用。No structural changes to §1/§2/§3/§4。方向不动,scope/integration/acceptance 补全。Commit: `b26febff` |
| 2026-05-29 | scoped | Blueprint scoped after reviewer amendment verification | Verified the reviewer P1×3 + P2 fixes are present in the blueprint:central write-time validation at `application/entity_write.py:_apply_op`,Identity `description/pattern` schema truth + compile checks,float Literal enum reject,breaking-atomic Step 1 ordering,cross-entity same-name Identity equality reject,and SF6 `workflow/audit/active/` exclusion. No further design changes added in this transition;status advanced from `draft` to `scoped`. Commit: `b3a44c28` |
| 2026-05-29 | scoped | **Step 0 pre-impl grep gate executed — BLOCKER trigger 0.1 fires** | Per blueprint §8 Step 0 + pause-and-amend trigger logic. **0.1 derivation Identity-head grep**:shipped `authoring/derivation_compile.py:203-217` enforces "head must not include **primary_key** Identity fields" + "head **MUST** include **non-primary** Identity fields"(via `_identity_field_names()` returning `(primary_keys, non_primary)` tuple at line 555-584)。`src/domains/ecss/tests/test_phase3_contracts_v1.py:268-288` `test_cross_coordinate_requires_explicit_non_primary_identity` 显式依赖 non-primary Identity 在 head(`locale: str = Identity()` non-primary,`User.name(locale=$x, name=$y)` 要求 locale 在 head)— shipped 主线设计 pattern。同文件 line 177-221 `test_head_primary_key_implicit_and_strict_compile_errors` 验证 primary_key forbid in head + non-primary required in head 双向 contract。**Form I + SF4 strict 冲突**:Form I 取消 primary/non-primary 区分(SF2 "all Identity = anchor bundle");SF4 strict 要求 "head 不可含任何 Identity field"。则 shipped non-primary-Identity-in-head 模式(如 ECSS locale)在 Form I 下变 illegal。**migration 选项**(需 reviewer L8 amendment verdict):(A) ECSS-style test 删除/改写(测试 pre-Form-I 语义,Form I 下失去意义);(B) `locale: str = Identity()` → `locale: str = Field()`(转 mutable attribute,改 ECSS 业务 model);(C) cross-coordinate disambiguation 移到 body(规则结构改);(D) SF4 放松为 "head 不可含 anchor 唯一 key(原 primary_key)" 但允许其他 Identity field — 实质保持 shipped 二分,违反 SF2 anchor-bundle。**0.2 callsite count refined**(精确 directory bucket):`Field(cardinality=` 与 `Identity(primary_key=` 主要分布:74+9+4+4+3+3+3+1+1 = 102 文件 MIGRATE scope(含 tests / sdk-docs / official / blueprints active / decisions / design-points / src factgraph)+ ~70 OTHER 文件(主要 archive/ .claude/worktrees/ workflow/memory/ workflow/design/design-points/archive/ tools/ tutorials/ examples/ 根 .ipynb)。**SF6 scope expansion 必要**(blueprint §6.1 / §9.4 amend):**sibling packages `src/service/`(3F+3I+0d+0df) `src/agent/`(1F+1I+0+0) `src/domains/`(0F+1I+0+0) 必须加入 migration scope** — 它们 `from factgraph` import 并使用 Form I descriptors,descriptor signature change 后 import 阶段会崩。当前 SF6 仅列 `src/factgraph/sdk/docs/` + `docs/official/kernel/`,需扩成 "all `src/*` 含 factgraph imports"。`examples/`(13F+13I+1 factgraph import)disposition:active examples 应迁移;`examples/archive/` 排除(SF6 隐含)。`tools/`(3F+4I + **0** factgraph imports)+ `tutorials/`(1F+1I + 0 imports)= 文本引用(.md / 注释),不会 runtime 崩,但 docs consistency 应迁移。**0.3 EntitySelector + allow_identity_defaults 全部 caller**:发现 1 个未在 preflight / 蓝图 §5.7 列出的 callsite:`application/entity_view.py:332` `allow_identity_defaults=False`(query-mode pattern,跟 facade.py:617 同形,但 preflight 漏掉)。其他全部在已知 §5.7 范围(facade.py 560/617 + materialize_identity 283/315/326/347/381 + protocol/schema_runtime.py 23/29)。Tests:`test_application_schema_runtime.py` + `test_application_entity_write.py` + `test_application_entity_view.py` 三个 test files。**0.4 `docs/references/bridges/` status verification**:2 个 file(`factpy-kernel-report-audit-2026-04-20.md` + `symir-blueprint-extraction.md`);6 个 active workflow 引用都把 bridges/ 视为 **historical context** 而非 current implementation truth(`2026-05-22_q1-docs-workflow-split.md` 明确 "explicitly parked deferred subtrees until future obsidian-integration slice";`2026-04-27_oss-prep-v0.1.md` + `2026-04-28_runtime-authority-cleanup.md` 引用为 2026-04-20 historical 自审计 / god-files audit 报告)。Verdict:**bridges/ 非 load-bearing**,SF6 conditional exclude 适用,无需迁移。**0.5 audit log finding record**(本行)。**Pause-and-amend trigger fires**:0.1 derivation Identity-head 非零(ECSS + 可能 cross-coordinate 等其他 test 等)— 必须**退回 Stage 4 blueprint amend**,reviewer 确定 L8/SF4 migration 路径后才能继续 Step 1。0.3 entity_view.py:332 minor amend(§5.7 callsite 补一行)— 不阻塞主线,但跟 0.1 amend 同 commit 落地。SF6 sibling packages + examples/ scope 扩展 — 必要 amend(§6.1 SF6 + §9.4 migrate 范围)。Commit: `53745c02` |
| 2026-05-29 | scoped | **Blueprint amended per Step 0 reviewer verdict round 2 — dirty archive guard + Step 1/10 fusion** | User reviewer 2026-05-29 round-2 post-amend review 落 2 项 amend:**(amend 1 — dirty notebook guard archive 显式)**§6.2 dirty notebook guard 现明确区分:`examples/`(non-archive)+ dirty(2 files)→ stash/migrate/pop;`examples/`(non-archive)+ clean → 正常 migrate;`examples/archive/`(any state including dirty)→ **绝对不动**(per SF6 + L10 reviewer verdict);`examples/archive/01_sdk_basics.ipynb` 当前 dirty 但 SF6 excluded — Step 1 必须显式不触碰,即使 dirty 状态。Step 1.12 verify green branch 前 grep verify archive 状态 diff 未变。**(amend 2 — Step 1 / Step 10 ECSS fusion)**用户反对 "同 ECSS file 在 Step 1.8 schema fixture + Step 10 rule head 分两步改" — rule file touched twice 增 merge risk + Step 1 后可能仍红。technical analysis(blueprint amend 中显式):shipped `derivation_compile.py:212-217` 在 Form I schema(无 primary_key=True)下变成"all identity = non_primary,head 必须含全部 identity";ECSS rule head Form I final shape(per Option C `head` 不含 Identity)在 shipped compiler 下直接 reject;ECSS rule head 迁移 + 现行 shipped compiler 不兼容。User 锁 "no double-touch ECSS file" + "Step 1 breaking-atomic green" + "Step 10 keeps compiler semantic rewrite" 三者 mutually 矛盾 — fixed point 唯一解 = **fuse derivation_compile.py Option C rewrite into Step 1.8.bis**。Step 10 主体 rewrite 移到 Step 1.8.bis(6 sub-steps:`_identity_field_names` 单 list / `_lower_field_head_with_schema` drop non-primary required + drop allowed non-primary set + drop required_field_terms non-primary / `_lower_entity_head_with_schema` forbid all identity / `_infer_unique_entity_binding_var` ambiguity reject / `_disambiguate_entity_binding_with_head_terms` remove / line 632 error message rewrite)。Step 10 reframed 为"beyond-Step-1 Option C additional tests + edge cases + beyond-ECSS rule sweep + post-fusion regression verify"(5 sub-steps)。Step 1 size 增 — 但 single mega-atomic commit 满足:(a) green branch invariant;(b) no double-touch ECSS file;(c) ECSS rule head Form I final shape immediate(no intermediate state)。**Section changes**:§6.2 dirty notebook guard 4 file 显式列 + archive exclude 显式;§8 Step 1.8 ECSS sub-bullet 改 "single-touch lock + schema fixture + rule head Form I final rewrite + 新增 ambiguity reject test";§8 加新 Step 1.8.bis(Why fused rationale + 6 sub-steps + Step 10 reframe pointer);§8 Step 10 完全重写 5 sub-steps(additional tests + beyond-ECSS sweep + regression verify);§8 Step 1 exclude 表加 "examples/archive/ 即使 dirty 也不动"。Status maintained `scoped`。No design direction change — implementation ordering refinement only per reviewer no-double-touch + green-branch reconciliation。Commit: TBD post-stage |
| 2026-05-29 | scoped | **Blueprint amended per Step 0 reviewer verdict round 1 — Status maintained scoped** | User reviewer 2026-05-29 post-Step-0 verdict 落 6 项 locks。**(L8/SF4 = Option C)**:不放松 SF2/SF4;cross-coordinate disambiguation **migrate to body / entity binding side**;head 不携带任何 Identity;derivation compiler 必须从 body uniquely determine target entity anchor bundle;**不能唯一确定 → reject**(ambiguous binding error)。不选 B(`locale Identity → Field`)— 会改变 idref_v1 anchor encoding,domain model 不应作为编译语义迁移的默认。ECSS 测试重写:`test_head_primary_key_implicit_and_strict_compile_errors`(177-221) 改 verify Form I "head forbids any Identity";`test_cross_coordinate_requires_explicit_non_primary_identity`(268-288) delete or replace with Form I body-anchor binding test。Schema 保持 `locale: str = Identity()`(不改 domain model)。**(SF6 expansion 确认)**:加入 migration scope `src/service/`,`src/agent/`,`src/domains/` — active runtime/source surface,descriptor breaking 后 import 阶段炸,必须 Step 1 breaking-atomic 迁移。**(§5.7 callsite amend 确认)**:加 `application/entity_view.py:332 allow_identity_defaults=False`,归入 SF5 end-to-end removal scope。**(bridges/ exclude 确认)**:维持 SF6 conditional exclude;audit log Step 0 evidence 已记录,无需再次扩 SF6 表。**(examples/ disposition)**:`examples/`(non-archive)迁移,`examples/archive/` 排除;**关键 dirty notebook guard** — examples/ 下已有 4 个 unrelated dirty notebooks(`01_sdk_check_diagnose.ipynb` / `02_overlay_why_not_frontier.ipynb` / `archive/01_sdk_basics.ipynb` modified + 1 untracked),implementation 必须 targeted(`git stash push -- <file>` → migrate → `git stash pop` 模式)— 绝对禁止覆盖用户 notebook output / metadata 变更。**(tools + tutorials disposition)**:`tutorials/` 迁移(视为 user-facing docs/tutorial surface);`tools/` Step 1 breaking-atomic **不**迁移(Step 0 verified 0 factgraph runtime imports — 纯文本/注释引用,留 Slice 4 consolidated docs cleanup),除非 future Step 0 discovery 发现 executable/runtime import path。**Blueprint amend 落点**:§5.7 加 entity_view.py:332 + 全部 callsite 5 项 list;§5.12 重写为 Option C(`_identity_field_names` 单 list / `_lower_field_head_with_schema` drop non-primary required + `allowed = value_keys` only / `_lower_entity_head_with_schema` forbid all identity / `_infer_unique_entity_binding_var` reject ambiguous body / ECSS test migration);§6.1 SF4 重写包含 Option C migration directive;§6.1 SF5 含 entity_view.py;§6.1 SF6 expansion include sibling packages + tutorials + examples non-archive + dirty notebook guard reference;§6.2 加 dirty notebook guard 显式步骤(stash → migrate → pop);§7.5 加 entity_view.py + final allow_identity_defaults grep 0;§7.8 加 Option C ambiguity reject + ECSS test migration verification 4 项;§8 Step 0 加 "executed @ 53745c02 with verdict" marker;§8 Step 1.8 expand fixture scope to include sibling packages + tutorials + examples per dirty notebook guard;§8 Step 10 重写为 Option C 10 sub-steps 含 ECSS migration + beyond-ECSS catalog sweep;§9.4 重写 unconditional + conditional exclude 各 enumeration + bridges/ verdict reference + tools/ 留 Slice 4 markers。Status maintained `scoped`(no design direction change;reviewer 6 项 verdict 全部 captured in blueprint constraints)。Commit: TBD post-stage |

| 2026-05-29 | scoped | **Blueprint scope corrected to factgraph-only implementation** | User clarified that Slice 1 should not operate on examples and should not involve `src/service/`,`src/agent/`,or `src/domains`;implementation should only look at `src/factgraph` plus load-bearing workflow/design docs. Blueprint amended accordingly:§6.1 SF6 now states factgraph-only implementation scope and records sibling packages/examples/tutorials/tools as out-of-scope downstream drift;§6.2 dirty notebook guard changed from stash/migrate/pop to "do not touch examples at all";§8 Step 1 now requires breaking-atomic migration only within `src/factgraph`;§8 Step 10 owns only `src/factgraph/authoring/derivation_compile.py` Option C semantics and targeted factgraph tests,with ECSS/domain tests explicitly out of scope;§9.3/§9.4 move public quickstarts/examples/tutorials/tools/sibling packages out of Slice 1. Status maintained `scoped`. Commit: TBD post-stage |
| 2026-05-29 | implementation | **Step 1 factgraph-only Form I descriptor surface implemented** | Implemented scoped Step 1 within `src/factgraph` only. `sdk/schema.py` now has internal `_DataMember`, `Identity(description, pattern)` with legacy kwarg rejection, `Field(description, pattern)` with inferred read-only `cardinality`, `_AnnotationPlan` inference for scalar/list-set-frozenset/tuple/Literal annotations, and rejects Optional/Union/dict/float Literal enum/pathological pattern cases. `authoring/schema_compile.py` carries Identity/Field/Relationship `description`/`pattern`/`enum_values` into predicate schema truth and rejects float enum/pattern-on-non-string. Migrated old Form I callsites in `src/factgraph` docs only; no edits to `examples/`, `src/service/`, `src/agent/`, `src/domains/`, `tutorials/`, or `tools/`. Verification: `PYTHONPATH=src python -m compileall -q src/factgraph`; import + descriptor positive/negative snippets; compiled schema predicate assertions for identity pattern + enum values. Commit: TBD post-stage |
| 2026-05-29 | implementation | **Step 1 reviewer amendment — list Literal propagation + string dict reject + design-point example sync** | Addressed reviewer findings after `e720b641`. P1:`list[Literal[...]]`, `set[Literal[...]]`, `frozenset[Literal[...]]`, and `tuple[Literal[...], ...]` now propagate `enum_values` into the multi-cardinality `_AnnotationPlan` instead of rejecting Literal inner plans. P2:string-form `dict[...]` / `Dict[...]` annotations now reject instead of falling through to `entity_ref` when `from __future__ import annotations` stringifies annotations. P3:migrated the §8.1 baseline example code block in `identity-mechanism-redesign.zh.md` to Form I while leaving historical-rationale references to old API intact. Commit: TBD post-stage |
| 2026-05-29 | implementation | **Step 2 parser symmetric extension implemented** | Implemented blueprint Step 2 in `authoring/schema_dsl_parse.py`. The text DSL now uses `_AnnotationPlan`, accepts `Identity(description, pattern)` / `Field(description, pattern)`, rejects removed `primary_key/default/default_factory/cardinality` kwargs with Form I migration messages, infers scalar vs multi cardinality from annotation syntax, propagates `Literal[...]` enum values including `list[Literal[...]]` and `tuple[Literal[...], ...]`, rejects Optional/Union/dict annotations, rejects float Literal enum, and enforces pattern regex syntax + string-only pattern constraints. Also polished the §8.1 design-point wording from "current three layers" to "pre-Form-I three layers" to remove reviewer P4 ambiguity. Verification: parser positive/negative DSL snippet + `compile_authoring_schema_v1`; `PYTHONPATH=src python -m compileall -q src/factgraph`; `git diff --check`. Commit: TBD post-stage |
| 2026-05-29 | implementation | **Step 2 reviewer amendment — unknown generic strictness aligned** | Addressed reviewer P5 after `53c2c510`. Python class path now rejects unknown generic annotations such as `Annotated[str, "doc"]` instead of silently falling through to `entity_ref`; string-form AST path uses the same strict reject for unknown `Subscript` heads. This preserves the C3 parser/class accept-reject symmetry lock for unsupported generic annotations. Verification: live + string-form `Annotated[...]` reject snippets. Commit: TBD post-stage |
| 2026-05-29 | implementation | **Step 4 application schema runtime simplification implemented** | Implemented blueprint Step 4 in `application/schema_runtime.py`. `IdentityFieldInfo` now only carries `name` + `type_domain`; `PredicateInfo` now carries `description`, `enum_values`, and `pattern`; `build_schema_index` drops `primary_key/default/default_factory` reads and populates predicate schema truth; `materialize_identity` now requires every identity field and no longer accepts `allow_identity_defaults`; `_materialize_default_factory` and the `uuid4` import were removed; `resolve_selector` no longer propagates the removed kwarg. Verification: targeted schema_runtime snippet covering identity predicate pattern/description, field enum_values, incomplete identity error, resolved selector encoding, and removed kwarg TypeError; `PYTHONPATH=src python -m compileall -q src/factgraph`; `git diff --check`. Commit: TBD post-stage |
| 2026-05-29 | implementation | **Step 5 protocol cleanup implemented** | Implemented blueprint Step 5 across `application/protocol/schema_runtime.py`, `sdk/facade.py`, and `application/entity_view.py`. Removed `EntitySelector.allow_identity_defaults` and its bool validator; removed the two facade construction kwargs; simplified `sdk_get` identity validation so every Identity field is required; removed the `entity_view.py` `materialize_identity(... allow_identity_defaults=False)` callsite. Verified `EntitySelector(... allow_identity_defaults=True)` now TypeErrors, normal selector construction still works, `rg allow_identity_defaults src/factgraph` returns 0 hits, `PYTHONPATH=src python -m compileall -q src/factgraph`, and `git diff --check`. Commit: TBD post-stage |
| 2026-05-29 | implementation | **Step 6 SDKStore identity default cleanup implemented** | Implemented blueprint Step 6 in `sdk/store.py`. `SDKStore.ref` docstring now requires every identity value explicitly; the `default` and `default_factory="uuid4"` materialization branches were removed; `_default_uuid4_for_tag` was deleted; the remaining `default_factory='uuid4'` error path disappeared; closed-head entity-ref identity recovery now iterates all `entity.identity_fields` instead of filtering by the removed `primary_key` attribute. Verification: Form I `FactGraph.create` + `fg.ref` full bundle + `fg.set`/`fg.add`/Literal field smoke; partial `fg.ref` raises missing identity; `PYTHONPATH=src python -m compileall -q src/factgraph`; `git diff --check`. Commit: TBD post-stage |
| 2026-05-29 | implementation | **Step 7 facade + batch cascade implemented** | Implemented blueprint Step 7 in `sdk/batch.py` after confirming the `facade.py` cleanup landed in Step 5. `SDKBatchTx.entity(...)` now requires a complete identity bundle up front and raises on missing Identity values; `_materialize_identity_values` no longer materializes `default` or `default_factory="uuid4"`; `_default_uuid4_for_tag` was deleted; primary-key-only checks/rejects were removed so batch uses the complete Form I identity bundle. Verification: batch smoke with full-bundle writes + partial `tx.entity(...)` rejection; `rg` confirms no non-dataclass `primary_key/default_factory/allow_identity_defaults` batch remnants; `PYTHONPATH=src python -m compileall -q src/factgraph`; `git diff --check`. Commit: TBD post-stage |
| 2026-05-29 | implementation | **Step 8 remaining primary-key consumers replaced** | Implemented blueprint Step 8. `application/protocol/evaluate_result.py` and `application/protocol/rule_expr_inspect.py` now close/check entity-ref ports against the full Form I identity bundle instead of filtering `primary_key`; `application/schema_mutation_runtime.py` dropped the discovered zombie `primary_key` stable-projection key. Blueprint §5.10 / §7.7 / §8 Step 8 were amended to record the discovered schema mutation projection surface. Verification: targeted protocol snippets covering full-bundle closure success + partial-bundle failure; schema mutation stable projection no longer contains `primary_key`; `rg` confirms remaining `primary_key` hits are Step 9/10 only plus historical docs/audit; `PYTHONPATH=src python -m compileall -q src/factgraph`; `git diff --check`. Commit: TBD post-stage |

## Decision Notes

### 2026-05-29 — Preflight code audit v1(initial triage)

Identified 7 surface sections(A-G):descriptor signatures + annotation inference + parser kwargs + schema_compile + schema_ir validation + write-time validation + sdk `__all__`. Triage table built with shipped-vs-target rows. Reviewer feedback flagged 3 missing item categories prompting v2 expansion。

### 2026-05-29 — Preflight code audit v2(extended triage)

Added 3 sections(N/O/P):
- **N. Primary-key semantic consumers**:`IdentityFieldInfo` + `where_schema_lowering.py` (lines 36 / 46-57 / 90 / 247-282) + `derivation_compile.py` (lines 203 / 276 / 555-590 / 632) + 3 other sites(`sdk/store.py:2807`,`evaluate_result.py:793`,`rule_expr_inspect.py:232`)
- **O. Identity-default consumers**:`materialize_identity` + `_materialize_default_factory` + `SDKStore.ref` parallel default path at 1909-1912 + `EntitySelector.allow_identity_defaults` protocol field
- **P. Test + doc callsite migration burden**:Field(cardinality=) tests 202 + docs 65;Identity(primary_key=) tests 115 + docs 38;Identity(default=) tests 6 + docs 6;Identity(default_factory=) 0+0

Class re-estimated **M/L boundary**(not pure descriptor M)。

### 2026-05-29 — Reviewer Scope Freeze lock-ins(SF1-SF6)

User reviewer locked 6 scope-freeze items per response 2026-05-29:

- **SF1**:Form I removes primary/default/default_factory semantics completely — no zombie surface
- **SF2**:All Identity fields are immutable anchor-bundle members — no primary vs non-primary
- **SF3**:Cross-coordinate attribute equality may compare same Identity field only — no implicit full-bundle expansion(L7 lock;rejected default suggestion "anchor-bundle equivalence" as semantically too implicit)
- **SF4**:Derivation heads may not include any Identity field(Option A;L8 lock;requires pre-impl grep for shipped Identity-headed rules)
- **SF5**:Identity defaults removed end-to-end(L9 lock;reviewer added `SDKStore.ref` path beyond initial preflight)
- **SF6**:Historical/reference docs excluded with precise scope(L10 lock):must migrate `tests/` + `src/factgraph/sdk/docs/` + `docs/official/kernel/` + active design/decision/blueprint load-bearing;exclude `workflow/heritage/` + `workflow/blueprints/archive/` + `docs/references/working/` + `docs/references/bridges/`(if not current implementation truth)

Additional reviewer findings:
- `EntitySelector.allow_identity_defaults` is a frozen protocol field that needs explicit removal(initial preflight covered only `materialize_identity` kwarg)
- `SDKStore.ref` lines 1909-1912 has its OWN parallel default-materialization path independent of `materialize_identity` — bigger O-section gap

### 2026-05-29 — Class size re-estimate M/L boundary

Initial Slice 1 was estimated by ADR-FI §4.5 as "≈ 250-450 lines code" — pure descriptor refactor estimate。Preflight v2 revealed:
- Rule-lowering and derivation-compile semantic rewrite per SF3 + SF4(~110-180 lines)
- Two parallel default paths instead of one(SDKStore.ref + materialize_identity)— ~50 additional lines
- 325 test callsite migrations + ~110 doc migrations(per SF6 scope)— mechanical but high volume
- Load-bearing docs ~150-250 lines(per ADR-DOCS §4.2.1)

Final estimate ≈ 600-950 lines code change + 325 test migrations + 150-250 lines load-bearing docs。Class **M/L boundary**。Implementation Plan §8 splits into 14 steps(0-13)each at commit boundary to keep individual commits reviewable and rule-touching changes isolated。

### 2026-05-29 — Pre-impl grep(Step 0)is gating

Step 0 pre-impl grep produces:
- 0.1 Migration list for any shipped derivation rules where head body includes any Identity field — if 0,SF4 Option A is free lunch;if > 0,each rule becomes Step 10.5 migration item with no semantics change to that rule
- 0.2 Per-pattern callsite count baseline(used in §10 Outcome before/after comparison)
- 0.3 `EntitySelector(allow_identity_defaults=...)` exotic caller catalog(if any外 of core paths)

Step 0 lands as 第一 commit(audit log row update only,no code change)— ensures pre-impl preflight findings are recorded before any descriptor work begins。

### 2026-05-29 — Q-PR1 carve-out preservation re-verified

Per meta-ADR §4.4 hard rule + ADR-INV9 §4.4 三项绑定 carve-out + ADR-SYS-B §4.7.2:
- §6.3 explicitly lists 6 files / paths that must show zero diff:`core/evidence/write_protocol.py`,`core/store/ledger.py`,`core/store/_builders.py`,`adapters/pyreason/*`,`claims.rest_terms`,INV-9 runtime strict
- §7.12 acceptance criterion enforces this via diff inspection at slice close

This Slice 1 has zero PyReason / adapter / ledger / write_protocol dependency by construction。
