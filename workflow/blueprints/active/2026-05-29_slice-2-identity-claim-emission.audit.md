# Task Blueprint Audit: Slice 2 — Identity-as-Claim Core(Identity Claim emission + INV-7c reject + cache + `:exists` transitional guard)

- Blueprint: [2026-05-29_slice-2-identity-claim-emission.md](./2026-05-29_slice-2-identity-claim-emission.md)

## Event Log

| Date | Stage | Event | Notes |
|---|---|---|---|
| 2026-05-29 | draft | Blueprint created | Drafted after Slice 1 Form I close(`9cef674b` pushed to origin)+ ADR-IC(`2d0866ed`)+ ADR-DOCS(`bb6a2c90`)+ preflight code audit covering 5 retract_by_asrt call sites(write_protocol:170/226 protocol — Q-PR1 carve-out NOT touched;sdk/store.py:2125 SDK shell;ingest_runtime.py:169 application ingest;entity_write.py:412 application entity_write;derivation/accept.py:401 internal rollback — SF11 classification NOT touched)。Scope Freeze §6.1 captures 12 binding locks(SF1-SF12)。Implementation Plan §8 split into 11 steps each at commit boundary。Q-PR1 zero-dependency confirmed per §6.3 + §7.11。Branch `v0.2.0-blueprint-slice-2-identity-claim-emission-2026-05-29` forked from Slice 1 close `9cef674b`. Sacred master `562c7419` and dirty baseline preserved per CADENCE ritual. Commit: `f9373eea` |
| 2026-05-29 | draft | Blueprint amended per reviewer P1×3 + P2×2(still draft)| User reviewer post-draft review 2026-05-29 返回 3 P1 + 2 P2 findings — 都是实质 gap,不改决策方向但需补 scope/integration/acceptance。**(P1 #1 Ledger helper vs Q-PR1 0-diff 冲突)** 原 §5.2 retract guard `classify_retract_target` 使用 `ledger.get_claim_pred_id(asrt_id)` 或 "add minimal helper to Ledger" — 违反 SF5 / `core/store/ledger.py` 0 diff。**修复**:用现有 `Ledger.get_claim(asrt_id)` API,读 `claim.pred_id`;NO new Ledger helper。同步 unknown asrt classification 改为 `"unprotected"`(而非 `"field"`)— 让 pass-through 语义明确,避免 acceptance 中 "unknown → field" 语义假阳性。Step 2.2 + §7.2 同步修。**(P1 #2 `store.schema_index` attr 不存在)** 原 §5.4 ingest + §5.5 entity_write pseudo-code 用 `store.schema_index`,shipped `Store` 没这个属性。**修复**:`_apply_retract` signature 加 `index: SchemaIndex` 参数,从上游 `_apply_item`(已持 index)pass-through;`entity_write._apply_op` 复用 Slice 1 Step 11 已添加的 `index` 参数(NO new parameter)。Step 4/5 + §7.4/§7.5 acceptance 同步改。**(P1 #3 `fg.entities.create/delete` API 未 shipped)** 原 §7.9 + Step 8 contract tests 要求 `fg.entities.create(User, ...)` 和 `fg.entities.delete(e_ref)` — 但这两个 API 是 Slice 3a ADR-API Q10 namespace migration scope,Slice 2 不应依赖 unshipped API。**修复**:Slice 2 tests 改用 shipped path —`fg.ref(...) + fg.set(...)` / `EntityEditor.commit()` / `SDKBatchTx.commit()`,verify N Identity Claims + `:exists` co-emission;`fg.entities.delete` full-entity revoke acceptance **移到 carry-forward**(标 "future whole-entity delete path once Slice 3a lands");error wording 保留对 `fg.entities.delete` 引用作为 user migration guidance,但 implementation/acceptance 不依赖 unshipped API。§10 Outcome carry-forward 显式加 ADR-API Q10 entry。**(P2 #1 cache field 命名统一)** 原 §5.1 mix `_identity_pred_ids` 和 `identity_pred_ids`(ADR-IC §4.3.1 `_...` 是 implementation sketch)。**修复**:Slice 2 SchemaIndex frozen dataclass 使用 **public 命名**(无下划线)— `identity_pred_ids` / `exists_pred_ids` / `protected_anchor_pred_ids` property;跟其他 SchemaIndex 公开字段(`entities` / `field_predicates` / `predicates_by_id`)命名风格一致;§5.1 加 explicit note about ADR vs implementation naming choice。**(P2 #2 tests scope 精确)** SF7 + §9.4 原表述未明确 legacy carryover boundary。**修复**:SF7 加补充 — Slice 2 close acceptance 只要求 **新增 / 相关 tests 通过**(Slice 2-relevant tests only);不要求 legacy `tests/` 全量 green;legacy pre-Slice-1 fixtures(`Identity(primary_key=)` 等)留 carryover technical debt,不在 Slice 2 清理范围(Slice 3a namespace migration 或 docstring-only legacy cleanup slice 处理)。**Cascade**:§5.1 cache naming + ADR sketch vs implementation note;§5.2 helper signature(use `ledger.get_claim` + `"unprotected"` classification)+ avoid double lookup note;§5.4 ingest signature 加 `index` parameter + caller chain update + Step 0.1 preflight verify gate;§5.5 entity_write reuse existing `index` parameter + EntityWriteError mapping with `code` propagation;§6.1 SF7 P2 #2 boundary clarification;§7.2 unknown asrt → unprotected pass-through;§7.4 ingest index parameter wiring;§7.5 entity_write existing index parameter;§7.9 重写为 shipped-path tests + `fg.entities.delete` carry-forward note;§10 Outcome carry-forward 加 ADR-API Q10 entry;Step 2.2 / Step 4 / Step 5 / Step 8 sub-step 同步改。No structural changes to §1/§2/§3 goals/non-goals(direction unchanged)。方向不动,scope/integration/acceptance 补全。Commit: `c7b2a041` |
| 2026-05-29 | draft | Blueprint amended round 2 — SF1/SF9 命名同步(still draft)| User reviewer round-2 review 2026-05-29 confirmed P1×3 fully resolved + P2 #2 OK;最后一个 P2 sync cleanup:§6.1 SF1 + SF9 表中仍 carry `_identity_pred_ids` / `_exists_pred_ids` / `_protected_anchor_pred_ids` underscore 命名(从 ADR-IC §4.3.1 sketch 残留),跟 §5.1 + §7.1 已锁定的 SchemaIndex public 命名(`identity_pred_ids` / `exists_pred_ids` / `protected_anchor_pred_ids`)冲突 — Scope Freeze 是 implementation binding,不能跟 implementation sections 冲突。**修复**:SF1 + SF9 改为 public names + 括号说明 ADR-IC sketch vs Slice 2 implementation naming choice。No structural / 设计方向 change — pure naming consistency cleanup。Commit: `16bd1439` |
| 2026-05-29 | scoped | Blueprint scoped after reviewer round-2 verification | Verified reviewer round-2 P2 SF1/SF9 naming sync fix landed at `16bd1439`. All findings from prior rounds(P1×3 + P2×2 from round 1;P2 SF1/SF9 sync from round 2)resolved。Reviewer round-2 verdict:"amend SF1/SF9 命名同步 + 可选回填 audit commit 后,可以 scoped。这次不需要再改设计方向"。No design direction change in transition;status advanced from `draft` to `scoped`。SchemaIndex public field names locked across all sections(§5.1 / §6.1 SF1+SF9 / §7.1)。Per-commit verification:sacred master `562c7419` 不动;dirty baseline 保留(4 M + 1 D + 2 untracked);branch `v0.2.0-blueprint-slice-2-identity-claim-emission-2026-05-29` 接 round-2 commit `16bd1439`,fork from Slice 1 close `9cef674b`。Status `scoped` 后下一步 = Step 0 pre-impl grep gate(verify 5 retract_by_asrt call sites + verify `_apply_item` chain holds `index: SchemaIndex` parameter — per §5.4 P1 #2 amend Step 0.1 verify gate)。Commit: `a831fd00` |
| 2026-05-29 | scoped | **Step 0 pre-impl grep gate — matches preflight,read-only no commit** | User reviewer 执行 Step 0 read-only verification(no file change,no commit)。**0.1 retract_by_asrt 5 call sites verified**:application/entity_write.py:412(application entity_write wrap target)+ application/ingest_runtime.py:169(application ingest wrap target — P1 #2 amend)+ sdk/store.py:2125(SDK shell wrap target)+ core/evidence/write_protocol.py:170/226(protocol/core direct path,0 diff target — Q-PR1 carve-out)+ core/derivation/accept.py:401(internal rollback,0 diff target — SF11 classification)。**0.1 P1 #2 chain verification**:`_apply_item(..., index=index)` already holds `SchemaIndex`;`_apply_retract` can receive `index` by pass-through。P1 #2 implementation path 有效。**0.2 baseline shipped confirmed**:`_materialization_ops` / `record_exists` / `PlannedOpDTO` 全部 shipped per ADR-IC §6.2。**0.3 no additional unguarded retract_by_asrt application paths found** beyond entity_write + ingest_runtime;application `asrt_id` grep found 仅 read/view/overlay/proof-frame references,no additional write retract_by_asrt 入口。**0.4 docs-only mention noted**:`src/factgraph/core/docs/01_architecture.en.md` mentions `retract_by_asrt(...)` 是 docs-only,非 code path,跳过。**Verdict**:matches preflight,no blueprint amend needed,可以推进 Step 1。No commit(read-only verification per Slice 2 cadence variation — different from Slice 1 Step 0 which committed audit row)。Step 0 outcome recorded in this audit row。 |
| 2026-05-29 | implementing | **Step 1 SchemaIndex cache extension implemented** | Implemented blueprint Step 1 per §8 + SF1 + SF9。`application/schema_runtime.py`:**(1)** SchemaIndex 加 `identity_pred_ids: frozenset[str]` + `exists_pred_ids: frozenset[str]` 两个独立 frozenset fields + `protected_anchor_pred_ids` @property union helper(public 命名 per P2 #1 amend,跟现有 SchemaIndex 公开字段风格一致;ADR-IC §4.3.1 `_..._pred_ids` 是 sketch);docstrings 标 INV-7c 与 existence-claim transitional guard 边界。**(2)** `build_schema_index` 加单 pass over `predicates_by_id.values()` populate 两个 frozenset:`info.is_identity_field` → `identity_pred_ids`;`info.is_entity_exists` → `exists_pred_ids`。低开销:无需 schema_ir 重新遍历。**(3)** NEW test file `tests/test_application_schema_runtime_cache.py`(6 tests,per SF7 新 test file 在 scope):(a)SchemaIndex frozenset fields 存在 + protected_anchor 是 property 非 field;(b)identity_pred_ids 正确 populate from is_identity_field;(c)exists_pred_ids 正确 populate(注:exists predicate 用 capitalized EntityType per shipped schema_compile convention — User:exists,非 user:exists);(d)protected_anchor_pred_ids union 正确;(e)identity ∩ exists = ∅ disjoint(SF9 two independent sets);(f)O(1) membership lookup hit/miss 6 cases。**Verification**:`PYTHONPATH=src python -m pytest tests/test_application_schema_runtime_cache.py -v` → 6 passed in 0.14s;`PYTHONPATH=src python -m compileall -q src/factgraph` clean;downstream import smoke(schema_runtime + entity_view + entity_write + sdk.facade/batch/store)6 modules 全 import clean;Slice 1 Form I round-trip(`fg.ref + fg.set + fg.get`)smoke pass。Per-commit verification:sacred master `562c7419` 不动;dirty baseline 保留;branch HEAD on Step 1 implementation commit。No out-of-scope edits(只动 application/schema_runtime.py + 新 test file)。Q-PR1 carve-out preserved(no diff in core/evidence/write_protocol.py / core/store/ledger.py / adapters/pyreason/* / core/derivation/accept.py:401 / claims.rest_terms)。Commit: `73993ebd` |
| 2026-05-29 | implementing | **Step 2 retract guard helper implemented** | Implemented blueprint Step 2 per §8 + §5.2 + SF2 + SF11 + P1 #1。**NEW module** `src/factgraph/application/retract_guard.py`:**(1)** `RetractClassification = Literal["identity", "exists", "unprotected"]` type alias(per P1 #1 naming — `"unprotected"` 让 pass-through 语义明确)。**(2)** `RetractGuardError(Exception)` — **NOT** inheriting from any SDK-layer exception per SF2 three-layer model;fields `code` / `asrt_id` / `pred_id` / `classification`(`"identity"` 或 `"exists"`)。**(3)** `classify_retract_target(asrt_id, *, ledger, schema_index)` — uses ONLY existing `Ledger.get_claim(asrt_id)` API per P1 #1 + Q-PR1 carve-out;unknown asrt(`get_claim` returns None)→ `"unprotected"`;`identity_pred_ids` 命中 → `"identity"`;`exists_pred_ids` 命中 → `"exists"`;其他 → `"unprotected"`。**(4)** `check_retract_allowed(asrt_id, *, ledger, schema_index)` — single Ledger lookup(inline classification,no double `get_claim`)+ raise INV_7C_IDENTITY_PROTECTED / EXISTENCE_CLAIM_TRANSITIONAL_GUARD / pass-through;module docstring 标 三层 enforcement consumer 跟 Q-PR1 carve-out boundary。**(5)** NEW test file `tests/test_application_retract_guard.py`(11 tests,per SF7):4 classification cases × 2 functions(classify + check)+ distinguishable code distinction + RetractGuardError NOT inheriting SDK error MRO check + stub Ledger 只暴露 `get_claim` 来 verify helper 不调任何其他 Ledger API(P1 #1 enforcement test)。**Verification**:`PYTHONPATH=src python -m pytest tests/test_application_retract_guard.py -v` → 11 passed in 0.16s;`PYTHONPATH=src python -m compileall -q src/factgraph` clean;`git diff src/factgraph/core/store/ledger.py` 为空 — Q-PR1 carve-out preserved(P1 #1 锁的 ledger 0 diff)。Per-commit verification:sacred master `562c7419` 不动;dirty baseline 保留;branch HEAD on Step 2 implementation commit。No out-of-scope edits(只动 application/retract_guard.py NEW + 新 test file)。Q-PR1 carve-out preserved(`core/evidence/write_protocol.py` / `core/store/ledger.py` / `adapters/pyreason/*` / `core/derivation/accept.py:401` / `claims.rest_terms` 全 0 diff)。Commit: `187a2918` |
| 2026-05-29 | implementing | **Step 3 SDK shell retract wrap implemented** | Implemented blueprint Step 3 per §8 + §5.3 + SF2 + SF12。`src/factgraph/sdk/store.py`:**(1)** 新 import `from factgraph.application.retract_guard import RetractGuardError, check_retract_allowed`。**(2)** `SDKStore.retract` wrap:在 `_reject_attached_write("fg.retract")` 后、existing `retract_by_asrt` WriteProtocolError mapping 之前,加 `check_retract_allowed(asrt_id, ledger=self._store.ledger, schema_index=self._application_schema_index)` call + `RetractGuardError` mapping to `SDKStoreError`。**(3)** Identity classification 映射:`SDKStoreError(... INV-7c ... fg.entities.create ... fg.entities.delete ... INV-7a ... ADR-IC §4.1 ...)` + `code="INV_7C_IDENTITY_PROTECTED"`;含 delete+create migration hint(per ADR-IC §4.1 wording);`fg.entities.delete` reference 作为 user migration guidance(per P1 #3 amend — Slice 3a future API,implementation 不依赖)。**(4)** Exists classification 映射:`SDKStoreError(... :exists Claim ... transitional ... fg.entities.delete ... ADR-IC §4.4 ...)` + `code="EXISTENCE_CLAIM_TRANSITIONAL_GUARD"`;explicitly **NOT** 含 `INV-7c` 字串(per SF10 — `:exists` guard 非 INV-7c)。**(5)** Field Claim / unknown asrt → pass-through 到 existing `retract_by_asrt` + `WriteProtocolError` mapping(`"ASSERTION_NOT_FOUND"` code for unknown asrt)— 零行为变化。**(6)** NEW test file `tests/test_sdk_retract_guard_integration.py`(7 integration tests,per SF7):Form I `RetractGuardUser` 实体走完整 SDK shell path — Field Claim succeeds(`fg.retract(field_asrt)` 返回 revoker_id str)/ Identity Claim raises INV-7c with wording markers(`INV-7c`,`fg.entities.create`,`fg.entities.delete`,`INV-7a`,`ADR-IC §4.1`)/ `:exists` Claim raises transitional guard with wording markers(`:exists Claim`,`transitional`,`fg.entities.delete`,`ADR-IC §4.4`;NOT containing `INV-7c`)/ Unknown asrt passes-through to `ASSERTION_NOT_FOUND` / distinguishable codes + messages / tenant_id Identity field also protected(SF2 + Slice 1 all-Identity-is-anchor)/ guard runs BEFORE retract_by_asrt(Identity Claim 在 ledger 中未变动 verified by `find_claims` count before/after)。Note:test fixture entity name 用 `RetractGuardUser`(PascalCase 无下划线 prefix)— shipped pred_id 命名规则是 owner_prefix snake_case(`retract_guard_user:user_id`);`:exists` predicate 仍用 EntityType 原样(`RetractGuardUser:exists`)— 跟 Step 1 测试 fixture 同 convention 注释。**Verification**:`PYTHONPATH=src python -m pytest tests/test_sdk_retract_guard_integration.py tests/test_application_retract_guard.py tests/test_application_schema_runtime_cache.py -v` → 24 passed in 0.22s(7 Step 3 integration + 11 Step 2 unit + 6 Step 1 unit cumulative);`PYTHONPATH=src python -m compileall -q src/factgraph` clean;`git diff src/factgraph/core/store/ledger.py` 为空 — Q-PR1 carve-out preserved(P1 #1 锁)。Per-commit verification:sacred master `562c7419` 不动;dirty baseline 保留;branch HEAD on Step 3 implementation commit。No out-of-scope edits(只动 `sdk/store.py` import + retract method + 新 test file)。Q-PR1 carve-out preserved(`core/evidence/write_protocol.py` / `core/store/ledger.py` / `adapters/pyreason/*` / `core/derivation/accept.py:401` / `claims.rest_terms` 全 0 diff)。Commit: `12475859` |
| 2026-05-30 | implementing | **Step 4 application ingest path wrap implemented** | Implemented blueprint Step 4 per §8 + §5.4 + SF3 + P1 #2。`src/factgraph/application/ingest_runtime.py`:**(1)** 新 import `from .retract_guard import RetractGuardError, check_retract_allowed`。**(2)** `_apply_retract` signature 加 `index: SchemaIndex` parameter(per P1 #2 amend — `store.schema_index` attr 不存在,通过 parameter pass-through 而非 store attr access)。**(3)** `_apply_item` caller 在 line 101 加 `index=index` pass-through(已 verified Step 0 — shipped `_apply_item` 已持 `index: SchemaIndex` parameter)。**(4)** `_apply_retract` body wrap:在 existing `retract_by_asrt` try/except 之前,加 `check_retract_allowed(item.assertion_id, ledger=store.ledger, schema_index=index)` call + `RetractGuardError` mapping to `ErrorDTO`。**(5)** Identity classification → `ErrorDTO(code=guard_exc.code, ...)` with `code="INV_7C_IDENTITY_PROTECTED"` propagated **directly**(per reviewer Step 4 lock — NOT wrapped as `INGEST_RETRACT_FAILED`);message ingest-style 简洁(`"Identity Claim ... is immutable per INV-7c; Identity bundle modification requires delete + recreate of the entity. See ADR-IC §4.1."`);`details` 含 `assertion_id` / `pred_id` / `classification`。**(6)** Exists classification → `ErrorDTO(code="EXISTENCE_CLAIM_TRANSITIONAL_GUARD", ...)`;message 含 `":exists Claim"` / `"transitional guard"` / `"ADR-IC §4.4"`;explicitly **NOT** 含 `"INV-7c"` per SF10。**(7)** Field Claim / unknown asrt → pass-through to existing `retract_by_asrt` + `INGEST_RETRACT_FAILED` mapping(原 ingest behavior 保留 — Field Claim 成功 retract 返回 revoker_id;unknown asrt 走 INGEST_RETRACT_FAILED)。**(8)** NEW test file `tests/test_application_ingest_retract_guard.py`(8 tests,per SF7):走 `apply_ingest_request` 完整 ingest path — Identity Claim → `ErrorDTO(INV_7C_IDENTITY_PROTECTED)` code 直接 propagate(verify ≠ INGEST_RETRACT_FAILED)/ `:exists` Claim → `ErrorDTO(EXISTENCE_CLAIM_TRANSITIONAL_GUARD)` 含 transitional + ADR-IC §4.4,不含 INV-7c / Field Claim retract succeeds with revoker_id / unknown asrt → INGEST_RETRACT_FAILED(downstream behavior preserved)/ Identity + exists distinguishable codes 在 same batch / guard runs BEFORE retract_by_asrt(Identity Claim ledger count unchanged after failure)/ tenant_id Identity also protected(SF2 + Slice 1 all-Identity-is-anchor)/ failed retract in collect mode does NOT abort batch — 1 error + 1 success 在同 IngestRequest 内。Test 修正记录:初版误 import `AssertionRef`(speculative,not used in test),移除后 collection 通过。**Verification**:`PYTHONPATH=src python -m pytest tests/test_application_ingest_retract_guard.py tests/test_sdk_retract_guard_integration.py tests/test_application_retract_guard.py tests/test_application_schema_runtime_cache.py -v` → 32 passed in 0.23s(8 Step 4 + 7 Step 3 + 11 Step 2 + 6 Step 1 cumulative);`PYTHONPATH=src python -m compileall -q src/factgraph` clean;`git diff src/factgraph/core/store/ledger.py src/factgraph/core/evidence/write_protocol.py` 为空 — Q-PR1 carve-out preserved。Per-commit verification:sacred master `562c7419` 不动;dirty baseline 保留;branch HEAD on Step 4 implementation commit。No out-of-scope edits(只动 `application/ingest_runtime.py` import + 2 methods + 新 test file)。Q-PR1 carve-out preserved(`core/evidence/write_protocol.py` / `core/store/ledger.py` / `adapters/pyreason/*` / `core/derivation/accept.py:401` / `claims.rest_terms` 全 0 diff)。Commit: `16aeff69` |
| 2026-05-30 | implementing | **Step 5 application entity_write path wrap implemented** | Implemented blueprint Step 5 per §8 + §5.5 + SF3 + SF12。`src/factgraph/application/entity_write.py`:**(1)** 新 import `from .retract_guard import RetractGuardError, check_retract_allowed`。**(2)** `_apply_op` retract branch wrap:在 `assert op.assertion_id is not None` 之后、existing `retract_by_asrt` call 之前,加 `check_retract_allowed(op.assertion_id, ledger=store.ledger, schema_index=index)` call + `RetractGuardError` mapping to `EntityWriteError`。**(3)** **复用 existing `index: SchemaIndex` parameter**(per Slice 1 Step 11 baseline 已添加)— NO new parameter,符合 §5.5 设计。**(4)** Identity classification → `EntityWriteError(code=guard_exc.code, ...)` with `code="INV_7C_IDENTITY_PROTECTED"` 直接 propagate(per reviewer Step 5 lock — code 不被吞掉);message 跟 ingest path 一致(ingest-style 简洁 INV-7c + delete+recreate + ADR-IC §4.1);`path=("planned_ops", "assertion_id")`;`details` 含 `assertion_id` / `pred_id` / `classification`。**(5)** Exists classification → `EntityWriteError(code="EXISTENCE_CLAIM_TRANSITIONAL_GUARD", ...)`;message 含 `":exists Claim"` / `"transitional guard"` / `"ADR-IC §4.4"`;explicitly **NOT** 含 `"INV-7c"` per SF10。**(6)** Field Claim / unknown asrt → pass-through 到 existing `retract_by_asrt`(unknown asrt surfaces `WriteProtocolError` directly 因 _apply_op 不 wrap WriteProtocolError — 跟 ingest path 不同 wrapping pattern,这是 shipped behavior 保留)。**(7)** NEW test file `tests/test_application_entity_write_retract_guard.py`(8 tests,per SF7):走 `_apply_op` 直接 invocation with PlannedOpDTO(op="retract")— Identity Claim → EntityWriteError(INV_7C_IDENTITY_PROTECTED) code direct(verify ≠ ENTITY_WRITE_FAILED)/ `:exists` Claim → EntityWriteError(EXISTENCE_CLAIM_TRANSITIONAL_GUARD)含 transitional + ADR-IC §4.4,不含 INV-7c / Field Claim retract succeeds with revoker_id / unknown asrt → WriteProtocolError surfaces directly(不被 _apply_op 吞)/ Identity vs exists distinguishable codes / guard runs BEFORE retract_by_asrt(Identity Claim ledger count unchanged after failure)/ tenant_id Identity also protected(SF2 + Slice 1 all-Identity-is-anchor)/ EntityWriteError code propagates directly,not swallowed as ENTITY_WRITE_FAILED。Test 修正记录:**(a)** 初版 PlannedOpDTO.target 误用 `EntitySelector`(unresolved selector)而非 `EntityRef`(resolved with encoded_ref)— 改 import `EntityRef from factgraph.application.protocol` + 加 `encoded_ref=e_ref` parameter。**(b)** PlannedOpDTO retract op 协议 shape 还要求 `field: FieldPath`(尽管 `_apply_op` retract branch 仅消费 `op.assertion_id` + `op.meta` — 协议 shape 要求 vs 实际 consumer 差异)— import `FieldPath` + 构造 fixture `FieldPath(entity_type="EntityWriteGuardUser", field_name="name")` 满足协议 shape 校验(任何 valid field of the entity 都行)。**(c)** linter 自动 sed 修正了 9 处 `_retract_op` call sites 加 `e_ref=e_ref`(基于 `_asrt_id` suffix pattern);另有 2 处 unknown asrt + field retract tests 因变量名不含 `_asrt_id` 未被自动捕获,手动改正。**Verification**:`PYTHONPATH=src python -m pytest tests/test_application_entity_write_retract_guard.py tests/test_application_ingest_retract_guard.py tests/test_sdk_retract_guard_integration.py tests/test_application_retract_guard.py tests/test_application_schema_runtime_cache.py` → 40 passed in 0.26s(8 Step 5 + 8 Step 4 + 7 Step 3 + 11 Step 2 + 6 Step 1 cumulative);`PYTHONPATH=src python -m compileall -q src/factgraph` clean;`git diff src/factgraph/core/store/ledger.py src/factgraph/core/evidence/write_protocol.py src/factgraph/core/derivation/accept.py` 为空 — Q-PR1 carve-out preserved。Per-commit verification:sacred master `562c7419` 不动;dirty baseline 保留;branch HEAD on Step 5 implementation commit。No out-of-scope edits(只动 `application/entity_write.py` import + retract branch + 新 test file)。Q-PR1 carve-out preserved(`core/evidence/write_protocol.py` / `core/store/ledger.py` / `adapters/pyreason/*` / `core/derivation/accept.py:401` / `claims.rest_terms` 全 0 diff)。**三层 enforcement 全部 shipped**:application source-of-truth(Step 2 helper)+ SDK shell fail-fast(Step 3)+ application ingest path(Step 4)+ application entity_write path(Step 5)— SF3 完整满足,bulk ingest 不可绕过 guard,entity_write path 不可绕过 guard。Commit: `a4853a0a` |
| 2026-05-30 | implementing | **Step 6 Identity reject wording update implemented** + Event Log reorder | Implemented blueprint Step 6 per §8 + §5.7 + SF12 + ADR-IC §4.1。**(1)** `src/factgraph/sdk/facade.py:IdentityEditor.set` 文案更新 — 加 `INV-7c` / `INV-7a`(Identity immutable anchor invariant)/ `fg.entities.delete(e_ref) + fg.entities.create(EntityCls, **new_identity_kwargs)` migration hint(future Slice 3a API,implementation 不依赖 — per P1 #3 amend)/ `ADR-IC §4.1` 引用;移除 stale `"open a new editor with different identity"` 文案;加 `code="INV_7C_IDENTITY_PROTECTED"`(Layer 2 SDK shell + Layer 3 retract guard 共享同 code,便于 caller branching)。`add` / `retract` 委托 `set`,共享文案 + code。**(2)** `src/factgraph/application/entity_write.py:plan_write_command` `is_identity_field` check 文案更新 — 同步 INV-7c + INV-7a + delete+create migration hint + ADR-IC §4.1 wording;`code` 从 stale `IDENTITY_FIELD_MUTATION_NOT_SUPPORTED` 改为 `INV_7C_IDENTITY_PROTECTED`(跟 SDK shell + Layer 3 retract guard 共享同 code,defense-in-depth at application source-of-truth)。**(3)** NEW test file `tests/test_identity_reject_wording.py`(8 tests,per SF7):**IdentityEditor wording**(SDK shell Layer 2)— `editor.user_id.set("bob")` raises SDKStoreError 含 INV-7c / INV-7a / fg.entities.delete / fg.entities.create / ADR-IC §4.1 markers + code=INV_7C_IDENTITY_PROTECTED;tenant_id Identity field 也 rejected(SF2 + all-Identity-is-anchor)— stale `"open a new editor"` 文案 absent verified;add/retract 委托 set 共享 wording。**plan_write_command wording**(application source-of-truth Layer 2)— direct invocation of `plan_write_command(EntityWriteCommand(...))` with Identity FieldMutation → `plan.can_apply=False` + `plan.errors[0]` 含 INV-7c / INV-7a / fg.entities.delete / fg.entities.create / ADR-IC §4.1 markers + IdentityRejectUser.user_id 出现 + code=INV_7C_IDENTITY_PROTECTED + stale code IDENTITY_FIELD_MUTATION_NOT_SUPPORTED 已替换 verified。**Layer 2 + Layer 3 consistency** — both paths(plan_write_command + fg.retract on Identity Claim asrt)surface code=INV_7C_IDENTITY_PROTECTED — 单一 caller branching 来源 per ADR-IC §4.1。**Test path 修正**:初版 IdentityEditor tests 走 `fg.edit(...)`,但 `fg.ref(...)` 不自动 materialize entity → EntityNotFoundError;fix 是先 `fg.set(IdentityRejectUser.name, e_ref, "Alice")` 触发 Form I anchors materialization,再 `fg.edit(...)`。初版 plan_write_command tests 走 `fg.set(IdentityRejectUser.user_id, ...)`,但 SDK shell `_schema_pred_for_field` 在参数校验层 reject Identity descriptor("field must be sdk.Field descriptor"),never reaching plan_write_command;fix 是 direct invocation of `plan_write_command(...)` with EntityWriteCommand containing Identity-targeted FieldMutation — bypassing SDK shell parameter validation,exercises application source-of-truth check at line 187(defense in depth — 两层独立 reject Identity mutation)。**(4)** Event Log 顺序修正(per reviewer non-blocking comment):原 Step 5 row 排在 Step 4 row 前,违反阅读顺序;swap 后 Step 4(`16aeff69`)→ Step 5(`a4853a0a`)→ Step 6;Step 4/Step 5 row 中 `Commit: TBD post-stage` 占位也回填实际 commit SHAs(`16aeff69` / `a4853a0a`)。**Verification**:`PYTHONPATH=src python -m pytest tests/test_identity_reject_wording.py tests/test_application_entity_write_retract_guard.py tests/test_application_ingest_retract_guard.py tests/test_sdk_retract_guard_integration.py tests/test_application_retract_guard.py tests/test_application_schema_runtime_cache.py` → 48 passed in 0.24s(8 Step 6 + 8 Step 5 + 8 Step 4 + 7 Step 3 + 11 Step 2 + 6 Step 1 cumulative);`PYTHONPATH=src python -m compileall -q src/factgraph` clean;`git diff src/factgraph/core/store/ledger.py src/factgraph/core/evidence/write_protocol.py src/factgraph/core/derivation/accept.py` 为空 — Q-PR1 carve-out preserved。Per-commit verification:sacred master `562c7419` 不动;dirty baseline 保留;branch HEAD on Step 6 implementation commit。No out-of-scope edits(只动 `application/entity_write.py` is_identity_field branch + `sdk/facade.py:IdentityEditor` + 新 test file + audit log reorder/Step 6 row)。Q-PR1 carve-out preserved(`core/evidence/write_protocol.py` / `core/store/ledger.py` / `adapters/pyreason/*` / `core/derivation/accept.py:401` / `claims.rest_terms` 全 0 diff)。Commit: `c2d659c1` |
| 2026-05-30 | implementing | **Step 7 shadow store legacy documentation(doc-only)** | Implemented blueprint Step 7 per §8 + §5.8 + G5 + N8 + ADR-IC §4.2.3/§4.2.4。`src/factgraph/sdk/store.py` 在 `_identity_values_by_e_ref` 字段声明(post-amend at line 943 — 原 spec 引用 line 923,因 Step 1-6 历史 diff 偏移)前加 16-行 class-level 注释 block,**精确按 blueprint §5.8 wording**:**(1)** 顶部标 `LEGACY / INTERNAL COMPATIBILITY only`;**(2)** 引用 ADR-IC §4.2.3 — shadow store NOT part of Layer 2 fields API contract,只是 compatibility detail 让 `fg.set(Field, e_ref_string, value)` 在 prior `sdk.ref()` 已 seen e_ref 时 succeed;**(3)** 显式两支 e_ref 路径:**(a)** e_ref NOT in shadow store → fail-fast `UNRESOLVABLE_E_REF`(per ADR-IC §4.2.1 emission input contract;raised at `_apply_field_mutation` target check + `_build_application_write_value` entity_ref value check — explicit cross-ref to 2 raise sites);**(b)** e_ref in shadow store → lazy materialization through `_materialization_ops`(legacy compat path 自动 emit Identity Claims + `:exists` on first Field write);**(4)** Step 2+ direction(per ADR-IC §4.2.4)— eager emission at `fg.entities.create(...)` + shadow store removal;Slice 2 显式 NOT remove shadow store(Slice 3a ADR-API Q10 namespace migration carry-forward;compatibility preservation in Slice 2)。**No behavior change**(Step 7.2 — doc-only)。**No new tests**(doc-only per Step 7 spec)。**Verification**:`PYTHONPATH=src python -m compileall -q src/factgraph` clean;cumulative Slice 2 suite `PYTHONPATH=src python -m pytest tests/test_application_schema_runtime_cache.py tests/test_application_retract_guard.py tests/test_sdk_retract_guard_integration.py tests/test_application_ingest_retract_guard.py tests/test_application_entity_write_retract_guard.py tests/test_identity_reject_wording.py` → 48 passed in 0.26s(0 regression — doc-only confirmed);`git diff src/factgraph/sdk/store.py` 只显示 16-行 comment block insertion at class field init,无任何 statement / behavior 变更;`git diff src/factgraph/core/store/ledger.py src/factgraph/core/evidence/write_protocol.py src/factgraph/core/derivation/accept.py src/factgraph/core/store/_builders.py src/factgraph/adapters/pyreason/` 为空 — Q-PR1 carve-out preserved。Per-commit verification:sacred master `562c7419` 不动;dirty baseline 保留;branch HEAD on Step 7 implementation commit。No out-of-scope edits(只动 `sdk/store.py` 一处 comment + 本 audit row)。Q-PR1 carve-out preserved(`core/evidence/write_protocol.py` / `core/store/ledger.py` / `adapters/pyreason/*` / `core/derivation/accept.py:401` / `claims.rest_terms` 全 0 diff)。Commit: `d46b9fe7` |
| 2026-05-30 | implementing | **Step 8 emission contract tests implemented(tests-only)** | Implemented blueprint Step 8 per §8 + §7.9 + SF7 + SF8 + ADR-IC §4.2(emission contract)+ P1 #3 amend(`fg.entities.create/delete` 不在 scope,Slice 3a ADR-API Q10 carry-forward)。**NEW test file** `tests/test_emission_contract.py`(11 tests,tests-only,zero src/ diff):**(8.1 fg.ref + fg.set atomic emission)** 2 tests — `fg.ref(EmissionUser, user_id=, tenant_id=)` + first `fg.set(EmissionUser.name, ...)` atomic emit 4 Claims(2 Identity + 1 :exists + 1 Field)verified by `Counter(c.pred_id for c in ledger.find_claims(e_ref=e_ref))` + Identity bundle 每个 Claim 携带正确 value verified via `rest_terms`(per SF8 complete bundle contract)。**(8.2 EntityEditor.commit() routing)** 2 tests — `fg.edit + editor.name.set + editor.commit()` 在已 materialize entity 上加 Field Claim,Identity / :exists 不 re-emit;关键 invariant 测试:`fg.edit()` 拒绝 unmaterialized entity(EntityNotFoundError)— confirms EntityEditor 不是 materialization path,只是 edit-existing-entity path(per ADR-IC §4.2.3 materialization route 是 fg.ref + fg.set / SDKBatchTx)。**(8.3 SDKBatchTx.commit() atomic emission)** 2 tests — single-entity batch `tx.entity + handle.field.set + tx.commit()` atomic emit 4 Claims;multi-entity batch 验证每个 entity 都获完整 bundle + :exists,e_refs distinct,cross-entity indexed separately(per ADR-IC §4.2 atomic guarantee)。**(8.4 Dedup)** 2 tests — `fg.set + fg.add` 同 e_ref → Identity Claims emit ONLY ONCE(verified by Counter on second-write count unchanged);batch tx 内 3-op staging(1 set + 2 add)→ Identity / :exists exactly 1 despite multiple Field operations(per `_materialization_ops:materialized_refs` 内 dedup + 第二次 plan_write_command call 的 target_visible 检查)。**(8.5 Lazy materialization through shadow store)** 1 test — `fg.ref()` populate shadow store 但 NOT emit ledger Claims(`fg._identity_values_by_e_ref[e_ref]` 持 identity dict + `ledger.find_claims(e_ref=)` 长度 0)— first Field write 才触发 lazy materialization;直接对应 Step 7 注释的 "e_ref in shadow store → lazy materialization through `_materialization_ops`" 分支。**(8.6 Unseen e_ref fail-fast)** 2 tests — 外部构造的 e_ref(`"idref_v1:EmissionUser:fakehash..."`)直接 `fg.set` 触发 `SDKStoreError(code="UNRESOLVABLE_E_REF")` + 含 `sdk.ref` migration hint;同样 fail-fast 路径 also covers entity_ref 字段 value(`_build_application_write_value` 内 raise — Step 7 注释里两个 raise 站点 cross-ref 之一);直接对应 Step 7 注释的 "e_ref NOT in shadow store → fail-fast UNRESOLVABLE_E_REF" 分支(per ADR-IC §4.2.1 emission input contract)。**(8.7 NOT TESTED)** comment block at end of test file 显式记录 `fg.entities.create/delete` 全实体 API paths 是 Slice 3a ADR-API Q10 namespace migration carry-forward(per P1 #3 amend);Slice 2 error wording 引用 `fg.entities.delete` 作为 user migration guidance only,implementation/acceptance 不依赖 unshipped API。**Pred ID 命名实证确认**:probe via `python -c` 确认 shipped conventions — Identity / Field 用 snake_case owner_prefix(`emission_user:user_id` / `emission_user:tenant_id` / `emission_user:name` / `emission_user:tags`),:exists 用 EntityType as-is(`EmissionUser:exists` capitalized);blueprint §7.9 line 543 提到 `pred_id="user:exists"` 是 lowercase,实际 shipped 是 capitalized(已在 test file 顶部 docstring + 常量声明 align with shipped behavior)。**Verification**:`PYTHONPATH=src python -m pytest tests/test_emission_contract.py tests/test_identity_reject_wording.py tests/test_application_entity_write_retract_guard.py tests/test_application_ingest_retract_guard.py tests/test_sdk_retract_guard_integration.py tests/test_application_retract_guard.py tests/test_application_schema_runtime_cache.py` → 59 passed in 0.30s(11 Step 8 + 8 Step 6 + 8 Step 5 + 8 Step 4 + 7 Step 3 + 11 Step 2 + 6 Step 1 cumulative);`PYTHONPATH=src python -m compileall -q src/factgraph` clean(no src/ change anyway);`git status --short src/` 显示零 src 改动(tests-only Step);`git diff src/factgraph/core/store/ledger.py src/factgraph/core/evidence/write_protocol.py src/factgraph/core/derivation/accept.py src/factgraph/core/store/_builders.py src/factgraph/adapters/pyreason/` 为空 — Q-PR1 carve-out preserved。Per-commit verification:sacred master `562c7419` 不动;dirty baseline 保留;branch HEAD on Step 8 implementation commit。No out-of-scope edits(只动 新 test file + 本 audit row)。Q-PR1 carve-out preserved(`core/evidence/write_protocol.py` / `core/store/ledger.py` / `adapters/pyreason/*` / `core/derivation/accept.py:401` / `claims.rest_terms` 全 0 diff)。**SF8 emission contract 完整验证**:N Identity bundle + 1 `:exists` + 1 Field 跨 3 个 shipped paths(fg.ref+fg.set / SDKBatchTx.commit / EntityEditor.commit)全部 atomic + dedup 验证;shadow store legacy 2 分支(seen / unseen)实证覆盖 Step 7 注释。Commit: `ab168063` |
| 2026-05-30 | implementing | **Step 9 load-bearing docs sync(docs-only)** | Implemented blueprint Step 9 per §8 + §7.10 + G6 + SF6 + ADR-DOCS §4.2.2。**两个 load-bearing docs 同步**,wider docs polish 显式 NOT in scope(per SF6 — Slice 4)。**(1) `src/factgraph/sdk/docs/04_api_surface.en.md`**:**(1a)** §1.5 Error codes 表加 3 行(`INV_7C_IDENTITY_PROTECTED` / `EXISTENCE_CLAIM_TRANSITIONAL_GUARD` / `UNRESOLVABLE_E_REF` — 三个 Slice 2 引入的 error code 显式 enumerate,Where-it-appears 列 cross-ref §7)。**(1b)** 新增 §7 "Identity Claim Emission and Reject Semantics"(four sub-sections,~130 lines)— §7.1 Emission contract(per ADR-IC §4.2 + §4.2.1 emission input contract 完整 identity bundle 要求;atomic emission table N Identity + 1 :exists + 1 Field;pred ID convention `<snake_owner_prefix>:<field_name>` for Identity/Field + `<EntityType>:exists` for :exists capitalized);三 materialization paths table(`fg.ref + fg.set/add` / `SDKBatchTx.commit` 通过 `RecordExistsOp` 自动注入 / `EntityEditor.commit` 不是 materialization path — `fg.edit` pre-validates :exists);dedup 两层(_materialization_ops materialized_refs + entity_visible);`fg.entities.create` 显式 Slice 3a carry-forward 标注。§7.2 INV-7c reject 行为表 — 6 行覆盖 IdentityEditor + plan_write_command + retract guard 三个独立 layer 的所有 raise 站点;`code="INV_7C_IDENTITY_PROTECTED"` 共享 single source 的 caller branching 锁定;error message anatomy(INV-7c / INV-7a / fg.entities.delete / fg.entities.create / ADR-IC §4.1 markers);Q-PR1 carve-out 边界说明 — 协议/核心/pyreason intentionally unguarded。§7.3 existence-claim transitional guard — explicitly NON INV-7c(per ADR-IC §4.4.2 naming);两个 guard lifecycle 独立(INV-7c permanent;exists guard tied to :exists co-emission lifecycle Step 2+ 同步退役);`code="EXISTENCE_CLAIM_TRANSITIONAL_GUARD"` + 文案不含 INV-7c。§7.4 Shadow store legacy positioning — 三行表覆盖 `e_ref NOT in shadow store`(fail-fast UNRESOLVABLE_E_REF 在两个 raise 站点)/ `e_ref in shadow store + target not visible`(lazy materialization)/ `e_ref in shadow store + target visible`(Field write only,dedup);forward direction `fg.entities.create` eager emission + shadow store removal per ADR-IC §4.2.4 Slice 3a carry-forward 显式。**(1c)** 旧 §7 "What's Not in the SDK" renumber 为 §8(单一 section header 修改,doc 内无 forward refs 验证 via grep)。**(2) `workflow/design/design-points/active/identity-mechanism-redesign.zh.md`**:**(2a)** §5.2 末加 "Slice 2 ADR-IC 锁定 + 落地状态(2026-05-30)" 子段(在 INV-7c schema evolution 段之后,§5.3 INV-8 段之前)— 4 列表 mapping ADR-IC §4.1/§4.2/§4.3/§4.4 sub-decision 到本 §5.2 对应段;Slice 2 三层 enforcement landed status with commit SHAs(Step 2 `187a2918` retract_guard / Step 3 `12475859` SDK shell / Step 4 `16aeff69` ingest / Step 5 `a4853a0a` entity_write / Step 6 `c2d659c1` 文案 + code 统一);Q-PR1 carve-out preserved 文件清单;error code 统一锁定;error message markers(per ADR-IC §4.1 adopted wording);shadow store legacy 定位 cross-ref Step 7 commit `d46b9fe7`。**(2b)** §13 加 §13.4 "Slice 1 + Slice 2 落地状态(2026-05-30)" 三个子段 — §13.4.1 Slice 1 Form I shipped(@ `9cef674b`)+ §13.1 in-scope schema-相关项 status;§13.4.2 Slice 2 Identity-as-Claim shipped 详表(7 行 mapping §13.1 INV-7a/INV-7b/INV-7c/策略 C/schema evolution/exists guard/shadow store/load-bearing docs 全部 ↔ Slice 2 commit SHAs)+ carry-forward 项显式列(`fg.entities.create/delete/exists` Slice 3a + `AssertionView` 统一等后续 slice);§13.4.3 Slice 2 Q-PR1 carve-out preservation 边界说明。**No code change**(docs-only Step 9.3)。**No new tests**(docs-only)。**Wider docs polish 显式 NOT in Step 9 scope**(per SF6 + 用户 review 关注点):只动 2 个 load-bearing docs;其他 docs(`01_concepts.en.md` / `02_readwrite_and_ingest.en.md` etc. + ADR-IC + design-points 其他段 + module docs 其他段 + workflow/foundations docs etc.)留 Slice 4 wider polish round。**Verification**:`PYTHONPATH=src python -m compileall -q src/factgraph` clean(docs-only,无 source change anyway);累计 Slice 2 suite `PYTHONPATH=src python -m pytest tests/test_application_schema_runtime_cache.py tests/test_application_retract_guard.py tests/test_sdk_retract_guard_integration.py tests/test_application_ingest_retract_guard.py tests/test_application_entity_write_retract_guard.py tests/test_identity_reject_wording.py tests/test_emission_contract.py` → 59 passed in 0.29s(零回归);`git status --short src/` 显示只有 `src/factgraph/sdk/docs/04_api_surface.en.md` modified;`git diff --stat src/factgraph/core/store/ledger.py src/factgraph/core/evidence/write_protocol.py src/factgraph/core/derivation/accept.py src/factgraph/core/store/_builders.py src/factgraph/adapters/pyreason/` 为空 — Q-PR1 carve-out preserved。Per-commit verification:sacred master `562c7419` 不动;dirty baseline 保留;branch HEAD on Step 9 implementation commit。No out-of-scope edits(只动 2 load-bearing docs + audit row + backfill)。Q-PR1 carve-out preserved(`core/evidence/write_protocol.py` / `core/store/ledger.py` / `adapters/pyreason/*` / `core/derivation/accept.py:401` / `claims.rest_terms` 全 0 diff)。Commit: `200987f1` |
| 2026-05-30 | implementing | **Step 9 amend — docs precision fix(P1 + P2×2 from review,docs-only)** | User reviewer 在 Step 9 review pass 时识别 3 个 docs precision 问题(全部 close-readiness 阻塞性低,但 load-bearing 文档一致性必须修;reviewer 明确 NOT 进 Step 10 前先修):**(P1 §5.2 ADR-IC table Q3 row impl-site 误述)** 原 col2 "锁定内容" 写 `application/state` 2 个 frozenset fields + `Store.__init__` build + `fg.schema.extend/register` hook(直引 ADR-IC §4.5 implementation surface sketch,混 ADR lock 跟 impl site),原 col3 "本 §5.2 对应段" 写 "策略 C 表格(已实施 — schema_runtime.py)" 简化失精(漏 schema-evolution hook 是 carry-forward 不是 shipped)。**修复**:col2 改述 ADR lock 双层(hybrid init 在 `SchemaIndex` 构造期 build frozensets + schema-evolution hook 在 `fg.schema.extend/register` 触发 cache rebuild),col3 显式 split shipped vs carry-forward — `SchemaIndex.identity_pred_ids` + `exists_pred_ids` + `protected_anchor_pred_ids` property 已实施(`application/schema_runtime.py` Slice 2 Step 1 @ `73993ebd`);schema-evolution hook **carry-forward 到 ADR-API Q14**(per Slice 2 SF4 + N1;当前 schema 已禁 Identity/Field 互转 + Identity 新增 → immediate hook 不需要,延后安全)。**(P2 #1 §13.4.1 Slice 1 branch direction 错)** 原 "Branch:`v0.2.0-blueprint-slice-1-form-i-schema-refactor-2026-05-29` → master" 两处不准:(a)branch 名错 — 实际 branch 是 `v0.2.0-blueprint-slice-1-form-i-schema-2026-05-29`(无 `-refactor` 后缀;`git branch | grep slice-1` 验证);(b)`→ master` 暗示 Slice 1 merged to master,但 sacred master `562c7419` 明确未动,Slice 1 是 branch close + pushed to origin。**修复**:Branch 名 corrected `v0.2.0-blueprint-slice-1-form-i-schema-2026-05-29` @ `9cef674b`;direction "→ master" 删掉,改述 "branch close + pushed to origin;sacred master `562c7419` 未动"。**(P2 #2 §13.4.2 carry-forward 列 `_DataMember.pattern` 误列)** 原 carry-forward 列出 `AssertionView` 统一 / `_meta` 统一输入 / `_DataMember.pattern` / `fg.schema.register / extend / apply` 三分;但 `_DataMember.pattern` 已在 Slice 1 Form I 随 `Identity(..., pattern=)` / `Field(..., pattern=)` 落地(per §15 决策日志 2026-05-28 [x] 采纳 `Identity(..., pattern=r"...")` → `_DataMember.pattern`);留在 carry-forward 列误导未来 reviewer / planner。**修复**:`_DataMember.pattern` 从 carry-forward 列移除;追加显式注 "(注:`_DataMember.pattern` 已在 Slice 1 Form I 随 `Identity(..., pattern=)` / `Field(..., pattern=)` 落地,不属 carry-forward)";保留 `AssertionView` / `_meta` / `fg.schema.register|extend|apply` 三分作为真实未落项。**Verification**:`git diff workflow/design/design-points/active/identity-mechanism-redesign.zh.md` 显示 3 处 chunked edits(line 347 Q3 row + line 1540 branch + line 1572 carry-forward — 跟 reviewer 指认 3 处一一对应);`PYTHONPATH=src python -m compileall -q src/factgraph` clean(docs-only,无 src change anyway);累计 Slice 2 suite 59 passed in 0.30s(零回归);`git status --short src/` 空(src 无任何 diff);`git diff --stat src/factgraph/core/store/ledger.py src/factgraph/core/evidence/write_protocol.py src/factgraph/core/derivation/accept.py src/factgraph/core/store/_builders.py src/factgraph/adapters/pyreason/` 为空 — Q-PR1 carve-out preserved。Per-commit verification:sacred master `562c7419` 不动;dirty baseline 保留;branch HEAD on Step 9 amend commit。No out-of-scope edits(只动 design-point doc 3 处 + 本 audit row + backfill)。**No code / test diff**;**No 04_api_surface.en.md diff**(该 doc Step 9 内容 reviewer 已 verify clean,本 amend 只触 zh.md)。Q-PR1 carve-out preserved。Commit: `ebb03348` |
| 2026-05-30 | **implemented** | **Step 10 final acceptance + §10 Outcome + Status: scoped → implemented(Slice 2 close)** | Implemented blueprint Step 10 per §8 + §10.1-§10.7。**(10.1)** §7 acceptance checks 全部通过 — cumulative Slice 2 suite `PYTHONPATH=src python -m pytest tests/test_application_schema_runtime_cache.py tests/test_application_retract_guard.py tests/test_sdk_retract_guard_integration.py tests/test_application_ingest_retract_guard.py tests/test_application_entity_write_retract_guard.py tests/test_identity_reject_wording.py tests/test_emission_contract.py -v` → 59 passed in 0.31s(6 cache + 11 retract guard unit + 7 SDK shell integration + 8 ingest + 8 entity_write + 8 wording + 11 emission contract)— §7.1-§7.5 + §7.7-§7.9 全 covered;§7.6 + §7.11(Q-PR1 carve-out)+ §7.12(per-commit ritual)single-sweep grep verified。**(10.2)** §6.1 Scope Freeze 12 items(SF1-SF12)final status verified:**SF1**(SchemaIndex 2 frozensets + protected_anchor_pred_ids property)✓ shipped @ `73993ebd`;**SF2**(three-layer enforcement model — application source-of-truth + SDK shell fail-fast + protocol/core intentionally unguarded)✓ shipped @ `187a2918`+`12475859`+`16aeff69`+`a4853a0a`+`c2d659c1`(三层全部 +Identity field write reject);**SF3**(application-layer retract guard 覆盖 3 个 application-or-above entry points — SDKStore.retract + ingest + entity_write)✓ shipped;**SF4**(NO `fg.schema.register/extend/apply` stub;ADR-API Q14 carry-forward)✓ — Step 9 amend Q3 row 显式 confirm `application/schema_runtime.py` 不引入 hook stub,延后到 ADR-API Q14;**SF5**(Q-PR1 carve-out 0 diff)✓ verified via `git diff --name-only 9cef674b..HEAD -- src/factgraph/core/evidence/write_protocol.py src/factgraph/core/store/ledger.py src/factgraph/core/store/_builders.py src/factgraph/adapters/pyreason/ src/factgraph/core/derivation/accept.py` returns empty;`claims.rest_terms` 0 diff also verified via secondary grep;**SF6**(load-bearing docs only — 04_api_surface.en.md + identity-mechanism-redesign.zh.md §5.2/§13;wider docs polish Slice 4)✓ Step 9 + amend landed exactly these 2 docs,无 wider polish scope creep;**SF7**(tests/ IN scope;只要 Slice 2-relevant tests 通过,不要求 legacy tests/ 全量 green)✓ 59 Slice 2-relevant tests passing;**SF8**(emission contract MUST carry complete identity bundle;Layer 2 fields API 不作为 emission path;shadow store legacy)✓ Step 8 11 tests + §7.4 / §7.1 doc + §13.4 design-point sync 验证;**SF9**(两个独立 frozensets — identity_pred_ids 跟 exists_pred_ids 概念分离;protected_anchor_pred_ids 仅是 read helper)✓ shipped @ `73993ebd` + disjoint test 覆盖;**SF10**(`:exists` guard 命名 = existence-claim transitional guard;NOT INV-7c;`code="EXISTENCE_CLAIM_TRANSITIONAL_GUARD"`)✓ 三层 enforcement + 文案显式 NOT 含 INV-7c verified by 6+ tests across Step 3/4/5/6;**SF11**(`core/derivation/accept.py:401` intentionally unguarded;internal rollback)✓ 0 diff confirmed via §7.11 grep + §6.3 classification preserved in §10.7;**SF12**(error message wording per ADR-IC §4.1 — INV-7c reference + delete+create migration hint;`:exists` reject 含 existence-claim transitional guard + ADR-IC §4.4 pointer)✓ Step 6 8 tests verified all 5 wording markers(INV-7c / INV-7a / fg.entities.delete / fg.entities.create / ADR-IC §4.1)+ `:exists` 文案显式 NOT 含 INV-7c。**(10.3)** §6.3 Q-PR1 carve-out 0 diff via grep — already covered in 10.2 SF5 verification;5 sacred paths(`core/evidence/write_protocol.py` / `core/store/ledger.py` / `core/store/_builders.py` / `adapters/pyreason/` / `core/derivation/accept.py`)全 0 diff against Slice 1 close `9cef674b`。**(10.4)** §6.3 internal-rollback `core/derivation/accept.py:401` 0 diff + SF11 classification preserved — already covered;`core/derivation/accept.py` 在 Q-PR1 grep 列表中 0 diff confirmed;§10.7 显式 record SF11 rationale(derivation rollback 不 touch Identity Claims;intentionally unguarded;若 engine 错误产生 Identity Claim 是 engine bug 留 Step 2+ ADR)。**(10.5)** §10 Outcome filled with 10 subsections:§10.1 最终落地结果(commit lineage table — 9 steps + 1 amend + 4 audit backfills + 本 Step 10 commit);§10.2 与 blueprint 不同地方(无方向 deviation;3 个 docs precision amend 都是 close-readiness fix,1 个 pred ID lowercase 例文 historical discrepancy 已 acknowledge);§10.3-§10.8 6 个 verification confirmation subsections;§10.9 carry-forward dependencies 4 项(ADR-API Q14 schema-evolution hook + ADR-API Q10 fg.entities.create/delete namespace migration + Step 2+ :exists removal per ADR-IC §4.4.4 + Step 2+ shadow store removal per ADR-IC §4.2.4);§10.10 归档说明 6 步 archive cadence。**(10.6)** Blueprint `Status: scoped` → `implemented`(line 3);`Last Updated: 2026-05-29` → `2026-05-30`(line 5)。**Branch lineage verification**:`git merge-base --is-ancestor` 全 4 个 required commits 都 ancestor of HEAD — ADR-IC `2d0866ed` ✓ / ADR-DOCS `bb6a2c90` ✓ / synthesis `d0036e1f` ✓ / Slice 1 close `9cef674b` ✓(per §7.12 G8)。**Per-commit verification**:sacred master `562c74195df43e933bed92a3ff25de94dd8ce666` 不动 ✓;dirty baseline(4 M + 1 D + 2 untracked)保留 ✓;branch HEAD on Step 10 commit。No out-of-scope edits(只动 blueprint Status field + §10 Outcome + 本 audit row + backfill)。Q-PR1 carve-out preserved(全 0 diff 在 5 sacred paths)。**Slice 2 implementation complete**:三层 INV-7c enforcement(application source-of-truth + SDK shell + 2 application 写入路径)+ existence-claim transitional guard(NON INV-7c per ADR-IC §4.4)+ SchemaIndex frozensets cache(SF9 两独立 sets)+ shadow store legacy 文档化(per ADR-IC §4.2.3)+ emission contract atomic + dedup 全 3 路径(per ADR-IC §4.2)+ load-bearing docs sync。Carry-forward 全部 4 项 explicitly recorded in §10.9。Slice 2 close。下一步 = archive cadence(blueprint → `archive/` + audit log 同步 + INVENTORY 更新)。Commit: TBD post-stage |

## Decision Notes

### 2026-05-29 — Preflight code audit

Located all relevant sites per ADR-IC §6.2 citations + 2 new findings:
- **Section A**(emission)+**Section B**(Layer 2 reject)— ALREADY shipped baseline;Slice 2 mainly formalizes via contract tests + docs + error message updates
- **Section C**(Layer 3 asrt_id reject)— NOT shipped main new work
- **Section D**(cache)— `SchemaIndex` 需要扩展;PredicateInfo flags already extended in Slice 1
- **Section E**(shadow store legacy)— Already shipped fail-fast + lazy paths;Slice 2 documents legacy positioning
- **Section F**(`:exists` co-emission + rule layer)— Already shipped read paths;rule layer NOT modified per N5
- **Section G**(protocol/core direct paths)— Q-PR1 carve-out + internal rollback classification

### 2026-05-29 — Reviewer P1+P2 Scope Freeze verdict

**P1 finding**: `application/ingest_runtime.py:169 _apply_retract` direct `retract_by_asrt` call bypass risk — bulk ingest 可绕过 Identity/exists guard。

**P2 finding**: "Application source-of-truth" wording precision —
- application-layer source-of-truth = **shared retract guard helper**(NEW `application/retract_guard.py`)
- SDK shell fail-fast = `SDKStore.retract`(consumer)
- application ingest path = `ingest_runtime._apply_retract`(consumer)
- application entity_write path = `entity_write._apply_op` retract branch(consumer)
- protocol/core direct path = schema-agnostic,intentionally unguarded per Q-PR1 carve-out + ADR-FI §4.4.2

**N11 classification finding**:`core/derivation/accept.py:401` derivation rollback `retract_by_asrt` direct call — internal rollback path,NOT user-facing;Slice 2 不 guard 但 blueprint **MUST** explicitly classify(防 reviewer 误判为漏)。

**Resolution embedded in SF2 + SF3 + SF11**:
- SF2:三层 enforcement model(helper + SDK shell + intentionally-unguarded protocol/core)
- SF3:application-layer guard MUST cover all 3 application-or-above entry points
- SF11:`core/derivation/accept.py:401` 显式 classified as intentionally-unguarded internal rollback;blueprint §6.3 + §10 Outcome 必须记录

### 2026-05-29 — 6 OQ verdicts embedded

OQ1 ✓ SchemaIndex hosts caches(SF1)
OQ2 ✓ Three-layer enforcement(SF2 + P2 precision)
OQ3 ✓ NO stub for `fg.schema.register/extend/apply`;hook-ready contract documented;wiring deferred to ADR-API Q14(SF4)
OQ4 ✓ Q-PR1 carve-out preserved(SF5)
OQ5 ✓ Slice 2 load-bearing docs scope only(SF6)
OQ6 ✓ Tests directory IN scope for Slice 2;Slice 1 SF6 NOT inherited(SF7)

### 2026-05-29 — Class size estimate M

Initial scope per ADR-IC §4.5 was ~150-300 lines。After P1+P2 verdict expansion(application ingest guard + entity_write guard + shared helper):
- SchemaIndex cache extension(~30-50 lines)
- Application retract guard helper(~100-150 lines incl. tests)
- SDK shell wrap(~30 lines)
- Application ingest wrap(~30 lines)
- Application entity_write wrap(~20 lines)
- Error messages update(~30 lines)
- Shadow store comments(~20 lines)
- Contract tests(~150-250 lines)
- Load-bearing docs(~150-200 lines)

Total ≈ 250-400 lines code + 150-250 lines tests + 150-200 lines docs ≈ **M class**(smaller than Slice 1 M/L boundary — Slice 2 is more contract-formalization than semantic rewrite)。

### 2026-05-29 — Q-PR1 carve-out preservation strategy

Per meta-ADR §4.4 hard rule + ADR-INV9 §4.4 三项绑定 carve-out + ADR-SYS-B §4.7.2:
- §6.3 explicitly lists 6 files/paths that must show zero diff:`core/evidence/write_protocol.py`,`core/store/ledger.py`,`core/store/_builders.py`,`adapters/pyreason/*`,`claims.rest_terms`,INV-9 runtime strict
- **+1 additional path classified as intentionally-unguarded(SF11)**:`core/derivation/accept.py:401` derivation rollback `retract_by_asrt` direct call — NOT user-facing
- §7.11 acceptance criterion enforces zero-diff via grep at slice close

Slice 2 has zero PyReason / adapter / ledger / write_protocol / internal derivation rollback dependency by construction。

### 2026-05-29 — Carry-forward dependencies recorded

- **ADR-API Q14 schema-evolution hook**(SF4):cache hook is "hook-ready contract" documented in blueprint §5.10;ADR-API Q14 implementation slice wires `fg.schema.register/extend/apply` to trigger cache rebuild per ADR-IC §4.3.3
- **Step 2+ `:exists` removal**(per ADR-IC §4.4.4 forward-pointer):`exists_pred_ids` 变 empty frozenset 时,`protected_anchor_pred_ids` 退化为 `identity_pred_ids` only;INV-7c 范围不动;`existence-claim transitional guard` reject 路径 dead code 清理
- **Step 2+ shadow store removal**(per ADR-IC §4.2.4 eager-emission 演化方向):`_identity_values_by_e_ref` 删除 + `fg.fields.set(Field, e_ref_string)` 若 e_ref 未 materialized → raise EntityNotInitializedError(显式 contract)
