# Baseline Drift Cleanup Meta-Blueprint Audit: 189 pre-existing failures across 27 test files

- Blueprint: [2026-06-01_baseline-drift-cleanup.md](./2026-06-01_baseline-drift-cleanup.md)

## Event Log

| Date | Stage | Event | Notes |
| --- | --- | --- | --- |
| 2026-06-01 | draft | Meta-blueprint created + fresh failure census | Post Q-NAMING 6-phase routemap completion (AD/C/E/B1/B2/F all archived + pushed origin), launching baseline drift cleanup cycle per user 2026-06-01 directive (方案 C quick wins + 方案 A meta-blueprint framework). Branch `v0.2.0-blueprint-baseline-drift-cleanup-2026-06-01` forked from Q-NAMING-F archive HEAD `10b10981` (inherits AD + C + E + B1 + B2 + F complete lifecycles + N1-N24 invariants from F). Sacred Q-PR1 5-path 0-diff preserved at draft commit; sacred master `562c74195df4...` unchanged; dirty baseline (4 M + 2 D + 2 untracked) preserved. **Fresh `tests/` cohort census 2026-06-01**: 189 failed / 2294 passed / 32 skipped / 1037 subtests passed. Error category breakdown (per traceback grep): 102 `NoneType.proof` (SS7, chain-blocked by SS5), 68 `meta[confidence] removed` (SS5), 66 `SDKStore.ref` + 18 `.read` + 4 `.retract` + 2 `.set` + 2 `.get` = 92 SDKStore shape (SS4), 44 `Rule.where` TypeError (SS1), 24 `ReadPolicy` import (SS3), 20 `_eval_eq_atom` + 4 `_eval_arith_atom` = 24 evaluator drift (SS6), 6 `engine_options=` T5 (SS2), 4 `0 != 1` + 4 `'failed' != 'passed'` + 2 sha256 + 2 idref_v1 + 2×2 Tuples + 2 `EvaluateRow.support_kind` = ~17 misc (SS8). Total error instances ~378 across 189 distinct failures across 27 unique failing test files. Sub-slice sequence locked per 方案 C quick wins first: SS1 (44 ✓ small) → SS2 (6 ✓ tiny) → SS3 (24 ✓ small) → SS4 (92 medium, audit-first) → SS5 (68 medium, unblocks SS7) → SS6 (24 medium) → SS7 (102 → ~0 expected via upstream propagation) → SS8 (~17 individual cases). Target: 189 → ~0 baseline failures. Lightweight per-SS mini-cadence per §5.1 (no full 9-stage CADENCE per SS unless surfacing Red-risk findings); single audit log tracks cumulative state. |
| 2026-06-01 | draft (amended) | Step 4.2 review tightening | Codex Step 4.2 review surfaced 4 Required + 2 Recommended findings, all shipped-code-grounded via Rule 1 re-reads; all 6 applied in this single doc-only amend commit. (P2-1) SS1 Rule(where=) scope needs constructor-type discrimination — fresh grep confirms `where=` has multiple legitimate buckets per Q-NAMING-E inherited: aggregate helpers `agg_count/sum/min/max/mean(*, where=...)` at `sdk/dsl/expr.py:329-349` + Query.where at `sdk/dsl/rule.py:242` (N17 preserved) + legacy DSL Rule.where preserved per Q-NAMING-E PF-R1 G4 Option D + PyReason adapter test wrappers (different namespace). SS1 narrowed to "traceback-confirmed application `Rule(...)` constructor failures ONLY" (public `factgraph.sdk.Rule` per Q-NAMING-E §4.6.2) with explicit carve-outs for legacy DSL / aggregate / Query / adapter tests. Step 4.3 preflight 2.b verifies per-file callsite traceback. (P2-2) SS2 ordering + file enumeration unstable — fresh grep confirms `engine_options=` appears in 11 test files + 12+ src files (mostly legitimate error-message strings per Q-NAMING-F G4, NOT migration targets) — initial "1 file" estimate wrong. Plus SS2 chain-blocked by SS5 but ordered BEFORE SS5 in initial draft. Reordered: SS2 now AFTER SS5 in sequence table per P2-2; SS2 = "classification only until SS5 ships; then re-census + migrate residual engine_options= test fixtures (likely 0-2 remaining)". Default lock: 0 expected delta until SS5 unblocks chain. (P2-3) SS3 ReadPolicy target should default to retired-surface handling — shipped source confirms `_READPOLICY_REMOVED_MESSAGE = "ReadPolicy was removed..."` at `sdk/store.py:126`; test file `tests/test_sdk_read_policy.py` is described as "Phase 1 G1.1 red-baseline tests for the ReadPolicy migration blueprint" with comment "All ReadPolicy imports are dynamic ... so file collects cleanly while ReadPolicy does not yet exist" — pre-implementation TDD tests for migration design that ultimately diverged. LOCKED default: retire/quarantine `tests/test_sdk_read_policy.py` (rename to `_retired/` with archive header) OR rewrite to assert `ReadPolicy` removal (`assertNotIn` + `assertRaises(ImportError)`). NOT hunt for replacement import. Step 4.3 preflight 2.c locks final approach. (P2-4) SS4 must prohibit restoring flat SDKStore shells by default — shipped docs confirm namespaced canonical surfaces per Q-NAMING-C: `fg.entities.ref/get`, `fg.fields.set`, `fg.assertions.retract` at `sdk/docs/00_user_guide.en.md:60+` + `sdk/docs/04_api_surface.en.md:23+187+335+350+521` ("only public assertion-id mutation entry"). Q-NAMING-C archive `23389a2b` deleted 20 flat shells per design. LOCKED default: migrate tests to namespaced managers (sdk.ref→sdk.entities.ref, sdk.set→sdk.fields.set, sdk.get→sdk.entities.get, sdk.retract→sdk.assertions.retract, sdk.read→audit-required at preflight 2.d). **Restoring flat shells `SDKStore.ref/.read/.retract/.set/.get` is OUT of SS4 scope — would require separate Red blueprint per Q-NAMING-C precedent**. (P3-1) SS4 count internally inconsistent — line 45 + 210 said "88+" but listed counts 66+18+4+2+2=92. Corrected to "92 observed error instances" everywhere. (P3-2) Define canonical test runner/census command — added explicit `PYTHONPATH=src python -m pytest tests/ --tb=no -q --no-header 2>&1 \| tail -5` as canonical census command + expected output format + per-SS commit footer convention citing pre→post delta. Other runners (uv/pip-editable) DEPRECATED for census purposes. Also updated §4.2 sub-slice sequence table to reflect new SS1→SS3→SS4→SS5→SS2→SS6→SS7→SS8 order. Sacred Q-PR1 0-diff preserved through amend; master unchanged; dirty baseline preserved. |
| 2026-06-01 | draft (amended) | Step 4.4 preflight amendment | Step 4.3 preflight at `f8dd647f` on independent artifact branch `v0.2.0-baseline-drift-cleanup-preflight-2026-06-01` (forked from `aaac284f`) surfaced 1 Required (PF-R1) + 2 Recommended (PF-r1, PF-r2) + 7 Verified (PF-v1-v7) + 1 Scoped-detail (PF-s1) + 0 Abandonment findings. Codex Step 4.3 review PASS verdict at `f8dd647f` with 1 required cleanup on PF-s1 trajectory wording (linear subtraction not valid for chained categories). All 3 LOCK amendments + recensus-gated trajectory revision + PF-v1-v7 Decision Notes applied in this single doc-only amend commit on blueprint branch (NOT preflight branch — per Slice 7B Option A learning). (PF-R1 LOCK SS7 chain hypothesis DISPROVEN) Independent grep at preflight revealed only 5 of 102 (~5%) NoneType.proof failures have meta[confidence] in immediate setup chain — NOT 70-100% as initial hypothesis predicted. 102 errors concentrated in 4 SDK rule overlay test files (`test_sdk_rule_literal_replace.py` 15 + `test_sdk_rule_add_condition.py` 15 + `test_sdk_rule_disable.py` 14 + `test_sdk_proof_frame.py` 7 = 51) + 51 in other files — INDEPENDENT setup failures where `result.evidence_envelope` returns None for reasons unrelated to Uncertainty Phase 1 chain. SS7 reframed as DIRECT ACTIVE INVESTIGATION (not auto-deferred); blueprint §2 SS7 + §4.2 trajectory + §10 carry-forward updated. (PF-r1 LOCK SS6 SOURCE TOUCH AUTHORIZED) Preflight traceback origin grep confirms 11 callsites in `src/factgraph/core/rules/frontier.py` SOURCE file + 1 in `application/diagnose_runtime.py` + 1 in `test_t5_why_not_quarantine.py` call helpers with wrong arity. Helper signatures at `where_eval.py:526` (`_eval_eq_atom(view_facts, envs, atom, *, ast_gate_on)`) + line 857 (`_eval_arith_atom(view_facts, envs, atom, *, ast_gate_on)`) confirm `atom: tuple[Any, ...]` REQUIRED positional. SS6 IS the first cleanup sub-slice to invoke §6 source touch authorization clause; per-callsite rationale required at audit log. Blueprint §2 SS6 + §6 + §10 carry-forward updated. (PF-r2 LOCK SS4 sdk.read.match → sdk.entities.match) Per shipped docs `sdk/docs/04_api_surface.en.md:557` ("Use `fg.entities.where(...)` for simple snapshot reads and `fg.entities.match(...)` for match"). Complete SS4 namespaced migration map LOCKED: `sdk.ref → sdk.entities.ref`, `sdk.get → sdk.entities.get`, `sdk.set → sdk.fields.set`, `sdk.read.match → sdk.entities.match`, `sdk.retract → sdk.assertions.retract`. Blueprint §2 SS4 + §10 carry-forward updated. (PF-s1 recensus-gated trajectory revision per Codex Step 4.3 cleanup request) Linear subtraction (e.g., `~29 → ~121 (-68 direct)`) replaced with direction-based outcomes (DIRECT DROP / CLASSIFICATION-ONLY / RECENSUS REQUIRED / DIRECT INVESTIGATION / FINAL MISC) because chain effects + overlapping categories violate linear arithmetic. SS1/SS3 retain numerical estimates (cleanly independent); SS4/SS5/SS7 use recensus-gated outcomes; canonical census discipline reaffirmed per §5.1. PF-v1-v7 verified findings recorded in Decision Notes below per CADENCE verified-findings convention. PF-s1 cross-rename ordering deferred to Step 4.7 §10 closure for implementation choice recording. Sacred Q-PR1 0-diff preserved through amend; master `562c74195df4...` unchanged; dirty baseline preserved; preflight branch at `f8dd647f` untouched. |
| 2026-06-01 | scoped | Preflight amendments and self-check passed | PF-R1 SS7 chain DISPROVEN LOCK + PF-r1 SS6 SOURCE TOUCH AUTHORIZATION + PF-r2 SS4 sdk.read.match → sdk.entities.match LOCK + PF-s1 recensus-gated trajectory + PF-v1-v7 verified, all covered by `d260a98f` (Step 4.4 amend) + `81443e0d` (Step 4.4 fast-pass cleanup). Step 4.5 self-check confirmed: PF coverage matrix consistent (PF-R1 = 10 mentions; PF-r1 = 5 sites; PF-r2 = 2 sites; PF-s1 = 4 references; PF-v1-v7 = 9 references in audit log); invariant coverage (Q-PR1 = 10 mentions; N1-N24 inherited = 19 mentions; preflight HEAD `f8dd647f` = 7 references across blueprint + audit; LOCKED Step 4.4 banner = 3 sites; SS1-SS8 enumeration = 65 references); §1-§10 main section structure continuous; §5.1-§5.4 + §10 subsection structure complete (7 subsections); 0 stale "auto-resolved by SS5" / "mostly via upstream" / "run subset after SS5 simulation" residuals (cleared in `81443e0d` cleanup); sacred Q-PR1 5-path 0-diff preserved; sacred master `562c74195df4...` untouched; dirty baseline 8 entries preserved. Scope frozen at 189 baseline failures + 27 unique failing test files + 8 sub-slice sequence (SS1 → SS3 → SS4 → SS5 → SS2 → SS6 → SS7 → SS8) + 9 non-goals (N1-N9) + 8 mandatory preflight items (2.a-2.h all addressed; 2.g COMPLETED PF-R1 chain disproved). Ready for Step 4.7 implementation (Codex 承接) on `v0.2.0-impl-baseline-drift-cleanup-2026-06-01` per per-SS mini-cadence (§5.1: pre-impl census + root cause + migration + canonical post-impl census + audit log row + single commit + sacred ritual + per-SS push authorization). SS6 SOURCE TOUCH AUTHORIZED at `frontier.py` (11 callsites) + `diagnose_runtime.py` (1 callsite); SS7 active investigation required (not auto-deferred). |
| 2026-06-01 | SS1 shipped | Application Rule constructor fixtures migrated | Step 4.7 SS1 implementation on `v0.2.0-impl-baseline-drift-cleanup-2026-06-01` migrated public application `Rule(where=...)` fixtures to `Rule(when=...)` in 5 files: `tests/application/protocol/test_rule_aggregate.py`, `tests/application/protocol/test_rule_expr_head_validation.py`, `tests/sdk/test_t5_why_not_quarantine.py`, `tests/test_db_attach_lifecycle.py`, and `tests/test_sdk_frozen_view_read_runtime_boundaries.py`. The sixth SS1 cohort file, `tests/test_sdk_find_partial_identity.py`, contains no application Rule constructor and remains an SS4 `SDKStore.ref` baseline file. Residual grep for `Rule(... where=...)` in the SS1 cohort returned 0 hits. Targeted verification: `python -m compileall -q` on all 6 cohort files passed; `env PYTHONPATH=src python -m unittest ...` across the 5 migrated test modules ran 41 tests OK; pytest target cohort with `-p no:capture` ran 41 migrated tests OK and left 11 failures in `test_sdk_find_partial_identity.py`, all `SDKStore.ref` baseline per SS4. Full cohort census: local standard pytest capture segfaults before collection in this Python environment, so executable census used the canonical command plus `-p no:capture`: `env PYTHONPATH=src python -m pytest -p no:capture tests/ --tb=no -q --no-header 2>&1 \| tail -5` → 168 failed / 2315 passed / 32 skipped / 1042 subtests passed. Observed delta: 189 → 168 (-21); lower than the initial 44 error-instance estimate because some Rule(where=) traceback counts overlapped with chained baseline failures. Sacred Q-PR1 5-path 0-diff preserved; sacred master unchanged; dirty baseline preserved. |
| 2026-06-01 | SS3 shipped | Retired historical ReadPolicy red-baseline test file | Step 4.7 SS3 implementation retired/quarantined `tests/test_sdk_read_policy.py` by moving it to `tests/_retired/test_sdk_read_policy.py`, adding an archive header, and setting `__test__ = False`. This follows Step 4.2 P2-3 / PF-v2: shipped source removed ReadPolicy entirely (`_READPOLICY_REMOVED_MESSAGE = "ReadPolicy was removed. Use raw_kind / bound for uncertainty inputs."`), so the old Phase 1 G1.1 TDD red-baseline file was historical context rather than an active replacement-import test. Verification: `env PYTHONPATH=src python -m pytest -p no:capture tests/_retired/test_sdk_read_policy.py --collect-only -q` reported `no tests collected`; active grep for `from factgraph.sdk import ReadPolicy` / `ReadPolicy(...)` outside `_retired` returned no active SS3 fixture hits. Full cohort census used the same `-p no:capture` variant as SS1 because local pytest capture segfaults before collection: `env PYTHONPATH=src python -m pytest -p no:capture tests/ --tb=no -q --no-header 2>&1 \| tail -5` → 139 failed / 2313 passed / 32 skipped / 1042 subtests passed. Observed delta: 168 → 139 (-29), larger than the initial 24 error-instance estimate because retiring the historical file also removed chained failures in that file. Sacred Q-PR1 5-path 0-diff preserved; sacred master unchanged; dirty baseline preserved. |

## Decision Notes

### 2026-06-01 — Initial scope freeze rationale

- **Cleanup vs release prep priority**: per user 2026-06-01 direct directive, baseline drift cleanup precedes v0.2.0-rc release prep because 189 pre-existing failures repeatedly forced expensive failure-categorization analysis during Q-NAMING-E + B2 + F Step 4.7 Red slice reviews. Cleaning these now means future release gates and any subsequent slice reviews start from a clean baseline.

- **方案 C + 方案 A locked**: quick wins first ordering + meta-blueprint with sub-slice tracking. Rationale **[REVISED Step 4.2 P2-FP1 — sequence reorder per amend]**:
  - **Early quick wins = SS1 + SS3 (44 + 24 = 68 errors)** are small + well-defined fixture migrations → fast baseline drop establishes momentum. SS1 = application Rule constructor only (carve-outs locked per P2-1); SS3 = retire/quarantine red-baseline ReadPolicy test file (locked per P2-3).
  - **SS4 (92 errors) ordered after SS3 but before SS5** as audit-first medium slice — namespaced migration to `fg.entities.ref/get`, `fg.fields.set`, `fg.assertions.retract` per Q-NAMING-C documented canonical surfaces (locked per P2-4). SS4 in this position clears SDK shape noise that otherwise compounds downstream errors during SS5/SS6 reviews.
  - **SS5 (68 errors) ordered after SS4** — Uncertainty Phase 1 DSL migration (`meta[confidence]` → `raw_kind`/`bound`). Unblocks SS7 chain.
  - **SS2 (6 errors) ordered AFTER SS5 per P2-2** — chain-blocked by SS5's `meta[confidence]` setup failure. SS2 = classification-only until SS5 ships; expected 0 delta until chain unblocks. Likely 0-2 residual fixtures to migrate.
  - **SS6 (24 errors) follows SS2** — evaluator signature drift audit (`_eval_eq_atom/_eval_arith_atom` missing 'atom').
  - SS7 (NoneType.proof 102 errors) deferred to LAST because hypothesis predicts upstream SS5 (meta[confidence]) will unblock most of them via chain propagation (test setup at `_make_sdk()` calls `set_field(meta={"confidence":...})` which fails → `evidence_envelope` returns None → `.proof` access fails).
  - SS8 misc deferred to LAST because likely individual case-by-case investigation.
  - Meta-blueprint avoids one giant cleanup commit; tracks cumulative progress; allows per-SS independent review.

- **Lightweight mini-cadence per SS**: cleanup is not Red slice — does not require full 9-stage CADENCE per SS. Per §5.1:
  - Each SS = pre-impl census + root cause + migration + post-impl census + audit log + single commit + per-commit ritual + push (with explicit per-SS authorization)
  - Skip preflight branches per SS (not deep architecture)
  - Skip Decision Notes per SS (cumulative tracking in this single meta audit)
  - Apply full CADENCE only if SS surfaces Red-risk findings (cross-Q-PR1 touch or N12-N24 inherited contract violation)

- **Sub-slice scope discipline**: each SS strictly bounded by error category + test file enumeration. No category merging. No scope creep. Source touch (SDK / core) only when required to unblock test fixture + explicit per-site rationale + retain N7 layer authority.

- **Q-PR1 sacred invariant**: 0-diff vs `4c472b50` preserved across ALL sub-slices. Cleanup work is test-fixture-focused; should not touch Q-PR1 sacred 5 paths. If any SS surfaces Q-PR1 touch requirement, escalate to dedicated Red blueprint per Q-NAMING-E + F precedent.

- **Per-SS push authorization**: per `feedback_push_master_gate`, no auto-push. Each SS push requires explicit user signal. Allows fine-grained progress control + intermediate review.

- **AD/C/E/B1/B2/F inherited contracts preserved**: SS1-SS8 inherit N12-N24 from F. No re-litigation. Any SS finding that would touch inherited contracts surfaces as separate Red blueprint.

### 2026-06-01 — Step 4.3 preflight verified findings record (PF-v1 through PF-v7 + PF-s1)

Per CADENCE verified-findings convention, the following Step 4.3 preflight findings (artifact at `workflow/audit/active/2026-06-01_baseline-drift-cleanup-preflight.md` @ commit `f8dd647f`) require no blueprint amendment but are recorded as Decision Notes for closure §10 cross-reference:

- **PF-v1 (SS1 traceback distribution confirmed application Rule constructor only)**: Failing pattern at `test_rule_expr_head_validation.py`: `Rule(id=..., version=..., where=(PredAtom(...),), ports={...})` — this is public `factgraph.sdk.Rule` (per Q-NAMING-E renamed to `when`), NOT legacy DSL Rule (preserved per PF-R1 G4 Option D). Carve-outs verified: aggregate helpers `agg_*(where=...)` at `sdk/dsl/expr.py:329-349` PRESERVED + Query.where PRESERVED + adapter test `branch:` prefix patterns PRESERVED. 22 `Rule.__init__() got 'where'` failures distributed across 5 active test files per §1 census — all application Rule constructor sites confirmed.

- **PF-v2 (SS3 ReadPolicy retire/quarantine target verified)**: `tests/test_sdk_read_policy.py:1-20` docstring confirms "Phase 1 G1.1 red-baseline tests for the ReadPolicy migration blueprint" + references blueprint `docs/blueprints/active/2026-05-11_readpolicy-call-site-migration.md` + dynamic-import design pattern explicitly states "ReadPolicy does not yet exist" (intentional pre-implementation TDD red-baseline). Combined with `sdk/store.py:126` `_READPOLICY_REMOVED_MESSAGE` shipped source, confirms ReadPolicy was REMOVED not migrated — initial Phase 1 red-baseline test design diverged from final implementation.

- **PF-v3 (AD/C/E/B1/B2/F inherited surfaces preserved)**: Grep confirms: FrozenAssertionSet (7), save_workspace (9), commit_assertions (4), assertion_views (6), EmitSpec (4), Case (13), ProofReceipt (40), FactOverlay (21), ConditionPath (9), case_index (30), condition_index (23), SemanticsProfile (27 src/factgraph), ProbLogConfig (11), PyReasonConfig (11). All Q-NAMING decision contracts intact. Baseline cleanup SS1-SS8 will NOT touch these surfaces.

- **PF-v4 (Q-PR1 sacred 5-path 0-diff vs `4c472b50` preserved)**: Empty `git diff --stat` confirmed at preflight HEAD. Cleanup work is test-fixture-focused; should not touch Q-PR1 sacred 5 paths.

- **PF-v5 (SS5 meta[confidence] removal source verified)**: At `core/evidence/write_protocol.py:265-275` function `_validate_no_removed_uncertainty_keys` raises `WriteProtocolError` for `confidence` and `confidence_source` keys per Uncertainty Phase 1 (2026-05-11 per `project_uncertainty_phase1_implemented.md`). DSL migration target: `raw_kind` / `bound` (per error message text). Tests still pass `meta={"confidence": ...}` and must migrate.

- **PF-v6 (SS6 source-side root cause confirmed at frontier.py)**: 11 callsites. Helper signatures `_eval_eq_atom(view_facts, envs, atom, *, ast_gate_on)` at `where_eval.py:526` + `_eval_arith_atom(view_facts, envs, atom, *, ast_gate_on)` at line 857 — `atom: tuple[Any, ...]` REQUIRED positional. `frontier.py` callsites missing the arg. Per `git log` last modification at `765eb10a` (Q-NAMING-B2) + earlier `01bac07a blueprint(slice-1): rewrite attr-eq identity lowering` — signature drift not caught because tests already broken.

- **PF-v7 (SS4 sdk.read namespace removal confirmed via Q-NAMING-C archive `23389a2b`)**: Q-NAMING-C deleted 20 flat shells including `fg.read.*` namespace. Tests calling `graph.read.match(...)` are legacy Q-NAMING-C pre-implementation callsites. Migration target = `sdk.entities.match(...)` per PF-r2.

- **PF-s1 (per-SS mini-cadence execution order with revised baseline trajectory; deferred to §10 closure)**: Per Step 4.2 P2-FP1 LOCKED sequence + Step 4.3 PF-R1 chain disproval + Step 4.4 PF-s1 recensus-gated revision. Execution order: SS1 → SS3 → SS4 → SS5 → SS2 → SS6 → SS7 → SS8. Trajectory per §4.2 revised to direction-based outcomes (DIRECT DROP for SS1/SS3/SS6 + RECENSUS REQUIRED for SS4/SS5 + CLASSIFICATION-ONLY for SS2 + DIRECT INVESTIGATION for SS7 + FINAL MISC for SS8); SS1/SS3 retain numerical estimates because cleanly independent.

### Cross-flip role assignment (per user 2026-06-01 "延续一直以来的模式" directive)

User explicitly continued AD/C/E/B1/B2/F cross-flip pattern for baseline cleanup:

- Step 4.1 meta-blueprint draft → Claude (this commit)
- Step 4.2 review → Codex
- Step 4.3 preflight drafter → Claude (per AD/C/E/B1/B2/F precedent)
- Step 4.4 preflight amendment → Claude
- Step 4.5 self-check → Claude (doc-only)
- Step 4.6 scoped anchor → Claude
- Step 4.6.5 pre-impl grep — N/A for meta-blueprint; each sub-slice does mini pre-impl census instead
- Step 4.7 implementation (per sub-slice) → Codex (per user directive)
- Step 4.7 review (per sub-slice) → Claude (per user directive — CRITICAL independent test re-run for each SS per Q-NAMING-E + B2 + F lesson)
- Step 4.8 closure → Codex or Claude (per CADENCE no strict assignment) — only after ALL 8 SS shipped
- Step 4.9 archive → Codex or Claude (per CADENCE no strict assignment)

If user wants to flip any role assignment, they may signal at any handoff point.
