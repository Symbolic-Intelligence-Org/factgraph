# Q-NAMING-E Blueprint Audit: Rule / Inference DSL rename (Branch→Case, where→when, target+head_vars→emits)

- Blueprint: [2026-05-31_q-naming-e.md](./2026-05-31_q-naming-e.md)

## Event Log

| Date | Stage | Event | Notes |
| --- | --- | --- | --- |
| 2026-05-31 | draft | Blueprint created | Initial scope recorded covering Q-NAMING decision §4.6 (Batch E) + Sub-Q2 hard-cut `.where()` → `.when()`. Phase 3 of 6 sub-slices per Q-NAMING §4.9 ordering. **Red risk** per audit §6 (358 hits / 115 files). Branch `v0.2.0-blueprint-q-naming-e-2026-05-31` forked from Q-NAMING-C archive HEAD `23389a2b` (inherits Q-NAMING-AD + Q-NAMING-C complete lifecycles + N1/N2/N7/N14/N15/N16 invariants). Sacred Q-PR1 5-path 0-diff preserved at draft commit; sacred master `562c74195df4...` unchanged; dirty baseline (4 M + 2 D + 2 untracked) preserved. |
| 2026-05-31 | draft (amended) | Step 4.2 review tightening | Codex Step 4.2 review surfaced 4 Required + 2 Recommended findings; all 6 applied in this single doc-only amend commit. (P2-1) Public `Rule` is `factgraph.application.protocol.Rule` re-exported via `sdk/__init__.py:33`; legacy `sdk.dsl.rule.Rule` is NOT advertised (per `sdk/dsl/__init__.py:10` comment). Split Rule into public/application vs legacy/internal. G2 corrected to anchor on `application/protocol/rule.py:53+` (public surface); added G4 for legacy `sdk.dsl.rule.Rule.where` with preflight 2.k options (Option A migrate / Option B carve out / Option C active-test-only). Updated §4 Current Context + §2 G2/G4 + Step 4.3 preflight 2.k. (P2-2) Q-NAMING §4.6.1 + shipped application Rule bridge `_reject_or_shape(...)` at `sdk/dsl/application_rule.py:85` align: `Branch` (renaming to `Case`) is **Inference-only at application boundary**. Step 4.1 draft's "Default A global rename with Rule accepting Case" was incorrect — it would soften shipped rejection semantics. Reframed G1 as symbol rename ONLY; application Rule bridge MUST continue rejecting `Case`. §5.3 rewritten with resolved scope + rejected alternative. Step 4.3 preflight 2.a verifies rejection behavior continues + updates error string. (P2-3) Distinguish SDK authoring fields (in scope) from compiled derivation protocol fields (out of scope unless preflight surfaces them as public SDK surface). G5/G6 refined to "legacy SDK DSL authoring fields only". Added scope carve-out: `Inference.head` field (separate from `target`/`head_vars`) preserved + `CompiledHeadCall.target_pred_id` / `CompiledHeadCall.head_var_names` at `application/protocol/derivation.py:21+` preserved per N7 layer authority + `HeadCall` normalization preserved. Step 4.3 preflight 2.l mandatory classification. (P2-4) Added N17 invariant: `Query.where` / `Query.where_ir` at `sdk/dsl/rule.py:198+` is OUT OF SCOPE; Q-NAMING §4.6 targets Rule + Inference DSLs, NOT Query. §5.4.3 documents Query carve-out. Step 4.3 preflight 2.m enumerates Query hits + grep discrimination. (P3-1) `Pred` is `def Pred(pred_id, *terms) -> PredAtom` at `sdk/dsl/expr.py:314` (function not class). Step 4.1 draft's "0 class hits → preflight clarifies what Pred is" replaced with shipped reality. Updated §4 Current Context + G9. (P3-2) Aggregate helpers `agg_count/sum/min/max/mean(*, where=[...])` at `sdk/dsl/expr.py:329+` accept `where=` keyword but are NOT Rule/Inference dataclass fields. §5.4.2 documents 5-helper carve-out. Step 4.3 preflight 2.n confirms aggregate `where=` callsites preserved. Sacred Q-PR1 0-diff preserved through amend; master unchanged; dirty baseline preserved. |
| 2026-05-31 | draft (amended) | Step 4.2 wording residual cleanup | Codex Step 4.2 fast-pass surfaced 2 Required wording residuals (P2-1 §5.1 G2/G4 table + §7 acceptance + §8 Step 7.2/7.4 still anchored on Step 4.1 draft framing; P2-2 §1 item 1 still wrote "BOTH new Rule DSL and legacy Inference DSL") + 1 Cosmetic (P3-1 §4 Branch context still had stale "default — global rename" wording contradicting §5.3 rejection). All 3 fixed in this single doc-only cleanup commit. (P2-1) §5.1 G1/G2/G3/G4 rows refactored: G1 includes export update + bridge rejection preservation; G2 now anchored on public `application/protocol/rule.py:53+` (factgraph.sdk.Rule); G3 unchanged (Inference); G4 anchored on legacy `sdk/dsl/rule.py:73` with preflight 2.k options. §7 acceptance G1/G2/G3/G4 parallel updates. §8 Step 7.1 expanded (export + bridge wording); 7.2 now public app Rule.where; 7.4 now legacy SDK DSL Rule.where preflight-locked. (P2-2) §1 item 1 rewritten: where→when targets public application Rule + legacy Inference; legacy SDK DSL Rule.where preflight-locked via 2.k; sweeping carve-outs cited (Query, aggregate helpers, assertion filter methods). (P3-1) §4 Branch context wording replaced: docstring "rule or inference" reflects type-level shareability NOT runtime semantics; preflight 2.a verifies symbol rename + preserved rejection (NOT "global rename"). No change to substantive scope, invariants, or acceptance count beyond wording-consistency cleanup. Sacred Q-PR1 0-diff; master unchanged; dirty baseline preserved. |

## Decision Notes

### 2026-05-31 — Initial scope freeze rationale

- **Red-risk Sub-Q2 hard-cut acknowledged**: per Q-NAMING decision §4.8.1 + Sub-Q2, `.where()` → `.when()` is hard-cut at v0.2 with no alias support. Examples / tests / docs teaching query syntax all migrate in this single slice (audit §6 hidden dependency). Tests/example same-slice migration is a hard requirement, not optional.
- **Goals follow Q-NAMING §4.6 per-item adoption**:
  - §4.6.1 ADOPT — `Branch` → `Case` (G1; user 2026-05-30 framed as "Inference only" but shipped docstring at `sdk/dsl/branch.py:14` reads "rule or inference" — Step 4.3 preflight 2.a resolves)
  - §4.6.2 ADOPT — `where` → `when` (G2 + G3) for BOTH Rule and Inference DSL, hard-cut per Sub-Q2
  - §4.6.3 ADOPT — `target + head_vars` → `emits` (G5 + G6) for **legacy Inference only** (new Rule has no `target` field; Step 4.3 preflight 2.c locks emit DTO shape Option A/B/C)
  - §4.6.4 REJECT — Rule → NamedQuery (G7 invariant; user explicit 2026-05-30)
  - §4.6.5 REJECT — all other Module 4 proposals (G8 invariant; user explicit 2026-05-31)
  - §4.6.6 VERIFY-only — Pred docs (G9; shipped scan returned 0 `class Pred` hits — preflight 2.d clarifies what "low-level escape hatch" refers to) + Not(condition) syntax (G10; verified shipped `def Not(body)` at `sdk/dsl/expr.py:302`)
- **Critical Step 4.3 preflight findings identified at draft time** (not deferred):
  - **2.a Branch scoping** — shipped docstring "a rule or inference" vs user "Inference only" framing diverge. Default Option A (global rename matching shipped reality) per Q-NAMING-C PF-v1 precedent (shipped truth dominates pre-impl estimates).
  - **2.c emit DTO shape** — G5+G6 restructure target+head_vars → emits with 3 candidate shapes (Option A single combined / B nested / C structural alternative). Default Option A.
  - **2.d Pred clarification** — 0 class hits requires preflight to identify what "Pred" actually is in shipped code.
- **Q-NAMING-C precedent reuse** (per §5.8): per-rename classification (4-bucket: E-field-rename / E-class-rename / E-restructure / E-docs-only); N7 layer authority preservation; hard-cut + same-slice tests; Sub-Q3-style single-slice for related renames.
- **Q-NAMING-AD precedent reuse**: N16-style invariant introduction pattern for protected surfaces (N14 "target noisy term" carve-out + §5.4 where-method carve-out follow this pattern).
- **Heavy carve-outs surfaced at draft time** (Red-risk precaution):
  - **§5.4 where-method carve-out**: 4 shipped `def where(...)` methods on AssertionView/AssertionRecordSet/AssertionsManager (Q-NAMING-AD domain) are NOT in scope. Q-NAMING-E renames `where` FIELD on Rule/Inference dataclasses, NOT `.where(...)` METHOD on assertion classes. Pre-impl grep must distinguish.
  - **§5.5 target noisy-term carve-out (N14)**: per audit §6 "target is a noisy term in the repo. The implementation must scope to Inference DSL and protocol fields rather than all target-like variables." Many `target=EntitySelector(...)` in entity ops, `target: Any` in audit private helpers (Q-NAMING-AD `_resolve_record_asrt_id`/etc.), `target = getattr(...)` in runtime resolution — all out of scope. Pre-impl grep pins exact Inference-field-only pattern.
- **AD + C inherited state preservation locked** (N1, N2 + their detailed sub-items): all AD surfaces (`fg.assertion_views`, `FrozenAssertionSet` core + SDK + alias, `_SDKAuditManager.explain/.conflicts` + private helpers, `by_ids(strict=)`, `save_workspace/.load_workspace`) and C surfaces (20 deleted flat shells, 4 reachable manager rewires, 3 dead `_SDKWhatIf*` deletion, `commit_assertions` byte-identical, N15+N16 enforcement) untouched.

### Cross-flip role assignment (per user 2026-05-31 推进 directive + AD/C precedent)

User directive "推进" 2026-05-31 implicitly preserves AD + C cross-flip pattern unless explicitly changed:

- Step 4.1 blueprint draft → Claude (this commit)
- Step 4.2 review → Codex
- Step 4.3 preflight drafter → TBD (Claude or Codex; decided after Step 4.2 per AD/C precedent — Claude typically drafts after blueprint amends)
- Step 4.4 preflight amendment → drafter of Step 4.3 (typically Claude per AD/C precedent)
- Step 4.5 self-check → Claude (doc-only)
- Step 4.6 scoped anchor → Claude (AD/C precedent)
- Step 4.6.5 pre-impl grep (mandatory for Red slice) → Codex (承接 implementation prep, AD/C precedent)
- Step 4.7 implementation → Codex (per user directive)
- Step 4.7 review → Claude (per user directive)
- Step 4.8 closure → Codex or Claude (per CADENCE no strict assignment)
- Step 4.9 archive → Codex or Claude (per CADENCE no strict assignment)

If user wants to flip any role assignment, they may signal at any handoff point.
