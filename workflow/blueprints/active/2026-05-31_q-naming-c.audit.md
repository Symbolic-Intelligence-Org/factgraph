# Q-NAMING-C Blueprint Audit: Legacy SDK flat shell hard-cut delete

- Blueprint: [2026-05-31_q-naming-c.md](./2026-05-31_q-naming-c.md)

## Event Log

| Date | Stage | Event | Notes |
| --- | --- | --- | --- |
| 2026-05-31 | draft | Blueprint created | Initial scope recorded covering Q-NAMING decision §4.4 (Batch C, 20 flat shells + commit_assertions TBD) + Sub-Q3 single-slice refactor+delete pattern. Branch `v0.2.0-blueprint-q-naming-c-2026-05-31` forked from Q-NAMING-AD archive HEAD `7c48648f` (which inherits Q-NAMING decision adopted state + Slice 7B Option A recovery + Q-NAMING-AD complete lifecycle). Sacred Q-PR1 5-path 0-diff preserved at draft commit; sacred master `562c74195df4...` unchanged; dirty baseline (4 M + 2 D + 2 untracked) preserved. |
| 2026-05-31 | draft (amended) | Step 4.2 review tightening | Codex Step 4.2 review surfaced 3 Required + 2 Recommended findings; all 5 applied in single doc-only amend commit. (P2-1) Added `_SDKSchemaManager` (`ingest`, `validate_provenance` at [`sdk/store.py:645+`](../../../src/factgraph/sdk/store.py)) and `_SDKRulesManager.inspect` (at [`sdk/store.py:1272+`](../../../src/factgraph/sdk/store.py)) to live rewire scope — reframed G2 (was `_SDKWhatIfFactOverlayManager`) as `_SDKSchemaManager` rewire, G3 (was `_SDKWhatIfRuleManager`) as `_SDKRulesManager` rewire, G4 (was `_SDKWhatIfManager`) as `_SDKAuditManager.diff_proof_frames` rewire, G5 (was `_SDKAuditManager.diff_proof_frames`) as `_SDKWhatIf*` classification. Total live rewire surface now 7 methods across 4 reachable managers (was claimed 12 across 5 managers, including 3 unreachable). Updated §4 current context + §5.1 rewire table + §5.2 delete table + §7 acceptance + Step 4.3 preflight 2.a. (P2-2) Reframed `_SDKWhatIf*` classes (`_SDKWhatIfManager`, `_SDKWhatIfFactOverlayManager`, `_SDKWhatIfRuleManager`) as dead-code / non-public-leftover candidates, NOT live rewire targets. Shipped evidence: `SDKStore` has no `what_if` property; `tests/sdk/test_t5_legacy_hard_cut.py` asserts `hasattr(graph, "what_if") == False`. Added N15 invariant (do NOT add `SDKStore.what_if` property or `_what_if_manager` initialization); G5 classification at preflight; default subtractive (delete dead classes if no internal reference). Updated §1 problem + §3 non-goals (N15) + §4 current context + §5.1 G5 row + §6 invariants + §7 acceptance + Step 4.3 preflight 2.f. (P2-3) Added G6 four-bucket per-shell classification (C-live / C-tombstone / C-passthrough / C-documented). Initial Step 4.1 scan confirmed 9 tombstone shells at lines 2164/2199/2247/2304/2341/2401/2471/2535/2778 (`check`/`diagnose`/`why_not`/`check_fact_overlay`/`recheck_proof_frame`/`check_rule_disable`/`check_rule_literal_replace`/`check_rule_add_condition`/`run` — all raise `SDKStoreError(_T5_LEGACY_SHELL_REMOVED.format(...))` via constant at [`sdk/store.py:160`](../../../src/factgraph/sdk/store.py)). Updated §1 problem + §2 G6 + §4 current context + §5.2 classification table + §7 acceptance + Step 4.3 preflight 2.g + Step 4.3 preflight 2.i (T5 hard-cut test inventory: `test_t5_legacy_hard_cut.py`, `test_sdk_redesign_invariants.py`, `test_sdk_g2_invariants.py`, `test_sdk_fact_overlay.py`, `test_sdk_proof_frame.py`, `test_sdk_proof_frame_diff.py`). (P3-1) Strengthened G7a default: `commit_assertions` G7a (keep) is recommended default; G7b/G7c require Required preflight finding rationale (not implementation-time judgment). G7 choice MUST be locked at Step 4.3 preflight 2.c before Step 4.6 scoped anchor. Updated §2 G7 + §7 acceptance + Step 4.3 preflight 2.c. (P3-2) Added SDKStore docstring sweep to Step 4.3 preflight 2.h: shipped `SDKStore` docstring suspected to list `what_if` as user-facing namespace; Q-NAMING-C cleans stale references in same docs sweep. Updated Step 4.3 preflight 2.h + §7 acceptance G9. Sacred Q-PR1 0-diff preserved through amend; master unchanged; dirty baseline preserved. |
| 2026-05-31 | draft (amended) | Step 4.2 P2-4 wording cleanup | Codex fast-pass review surfaced P2-4 Required: 9 wording residuals from old "G5 rewire target" framing inconsistent with new "G5 = `_SDKWhatIf*` classification". All 9 fixed in single doc-only cleanup commit: (1) Deleted duplicate §1 Problem paragraph at original lines 31-33; (2-7) §2 Goals heading + §3 N12/N13 + §5.4 step 2 + §5.7 + §8 Step 7.1 updated from "G1-G5 rewires" to "G1-G4 rewires + G5 classification" (and "diff_proof_frames (G5)" to "(G4)"); (8-9) audit log Decision Notes §"Initial scope freeze rationale" PF-R1 line and P2-2 invariant line updated to cite 4 reachable managers + G5 dead-code handling. No change to Goals content or Step 4.3 preflight items. Sacred Q-PR1 0-diff preserved; master unchanged; dirty baseline preserved. |
| 2026-05-31 | draft (amended) | Step 4.4 preflight amendment | Step 4.3 preflight at `072005c2` surfaced 1 Required + 1 Recommended findings (5 Verified + 1 Scoped-detail + 0 Abandonment within CADENCE healthy distribution); Codex Step 4.3 review pass at `072005c2` with 3 P3 cosmetic notes (P3-1 PF-r1 wording 3→4 sites; P3-2 callsite wording; P3-3 Step 4.6.5 enumeration). All applied in this single doc-only amend commit on blueprint branch (NOT preflight branch — per Slice 7B Option A learning). (PF-R1 LOCK) G7 `commit_assertions` LOCKED at G7a (keep). Blueprint §2 G7 reframed from "default + 3 options" to "LOCKED G7a + rejected G7b/G7c"; new §3 N16 invariant added (no delete, no migrate, no rename `SDKStore.commit_assertions` per shipped evidence at `sdk/store.py:1614 docstring + 1989-2000 body + self._database.commit_assertions delegation`); §7 acceptance G7 simplified to "G7a LOCKED; commit_assertions preserved untouched; independent grep returns same definition". (PF-r1) Blueprint §9 Docs To Update expanded with 4 live stale `what_if` references in `sdk/store.py` (lines 1417 `_SDKAuditManager` class docstring, 1609 SDKStore class docstring listing, 2942/2945 error messages) + 6 within-dead-class sites at 1336/1342/1354/1360/1376/1390 auto-cleaned by G5 default subtractive. (PF-v1) Blueprint §5.2 classification table updated: `accept` (3520-3522) and `accept_many` (3524-3532) moved from "TBD at preflight" to "C-tombstone (PF-v1 confirmed)" — total tombstone count refined from 9 (Step 4.1 scan estimate) to 11 (Step 4.3 PF-v1 confirmed); §2 G6 initial classification text updated to final 11 tombstone + 2 passthrough + 7 live + 1 documented = 21 total. PF-v2/v3/v4/v5 verified findings recorded in this Decision Notes section (no blueprint amend per CADENCE verified-findings convention). PF-s1 scoped-detail recorded for Step 4.7 closure §10. (P3-3 note carry-forward) Blueprint §8 Step 4.6.5 wording strengthened to clarify that the pre-impl grep is the authoritative full-active-test-consumer enumeration (preflight 2.i covered only T5 hard-cut absence-assertion tests, not the broader sdk.<method> test migration set). Sacred Q-PR1 0-diff preserved through amend; master unchanged; dirty baseline preserved; preflight branch at `072005c2` untouched. |
| 2026-05-31 | draft (amended) | Step 4.4 P2-1 + P3-1 wording cleanup | Codex Step 4.4 fast-pass review surfaced P2-1 Required (5 G7 lock wording residuals across §5.3/§5.4/Step 4.3 2.c/Step 4.6 note/Step 7.3) + P3-1 Cosmetic (Current Context §4 still wrote "9 shells confirmed tombstone / remaining 11 shells preflight classifies"). All 6 fixed in this single doc-only cleanup commit: (1) §5.3 rewritten from "Three options + default if ambiguous" to "G7a LOCKED by Step 4.3 PF-R1; G7b/G7c REJECTED and not implementation options"; (2) §5.4 internal source caller migration explicitly EXCLUDES `commit_assertions` per §3 N16 / §5.3 G7a lock; (3) Step 4.3 mandatory preflight 2.c reframed from future-tense "Lock G7 decision" to "COMPLETED at PF-R1: G7a locked; historical record"; (4) Step 4.6 scoped anchor note "G7 decision must be locked" → "G7a already LOCKED by PF-R1; no gating decision before scope-freeze"; (5) Step 7.3 "Apply G7 decision (a/b/c)" → "Preserve `SDKStore.commit_assertions` unchanged per G7a lock + §3 N16; no edit to lines 1989-2000 / 1614 / class docstring / no rename / no migration"; (6) Current Context §4 "9 shells confirmed tombstone / remaining 11" → "11 shells confirmed tombstone (with `accept`/`accept_many` added per PF-v1) + remaining 9 classified (2 passthrough + 7 live) + 1 documented (commit_assertions G7a LOCKED) = 21 total". No content change to Goals, Acceptance, or Step 4.3 preflight items beyond wording-consistency cleanup. Sacred Q-PR1 0-diff preserved; master unchanged; dirty baseline preserved. |

## Decision Notes

### 2026-05-31 — Initial scope freeze rationale

- **Sub-Q3 single-slice locked**: per Q-NAMING decision §4.4.3, all four steps (refactor delegation + caller migration + delete shells + docs/tests) land in one blueprint. Matches Slice 3a Step 7 precedent and Q-NAMING-AD §5.4.1 Option (a) precedent. Avoids inter-slice coordination overhead.
- **Q-NAMING-AD precedent fully leverageable**:
  - PF-R1 Option (a) private-helper rewire pattern: `_SDKAuditManager.explain` + `.conflicts` already use this pattern (Q-NAMING-AD `36e2a4a7`). G4 extends to `diff_proof_frames`; G1/G2/G3 extend to the other 3 reachable namespaced managers (`_SDKEvalManager`, `_SDKSchemaManager`, `_SDKRulesManager`).
  - N7 layer authority: core `_queries.*` / application functions stay private under existing names; only SDK public boundary changes.
  - P2-2 invariant: namespaced managers NEVER call `self._sdk.<flat_shell>(...)`. AD established this for `_SDKAuditManager.explain/.conflicts`; Q-NAMING-C extends it to all 4 reachable live managers (`_SDKEvalManager`, `_SDKSchemaManager`, `_SDKRulesManager`, `_SDKAuditManager`). `_SDKWhatIf*` classes handled by G5 dead/non-public classification (not rewire).
- **G7 `commit_assertions` decision deferred to Step 4.6 scoped anchor (latest) or Step 4.3 preflight (preferred)**: per Q-NAMING §4.4.2 explicit TBD. Three sub-options (G7a keep / G7b migrate / G7c delete+helper) enumerated in §5.3. Default if ambiguous: G7a (keep) given real source usage (37 hits / 12 files including 6 source callers per audit §4).
- **Hard-cut per §4.8.1**: no aliases, no dual-emit, old flat shells removed in same commit as rewires land. Alpha release; no historical user protection burden.
- **AD inherited state untouched**: §3 N1 + §6 invariants lock that `fg.assertion_views`, `FrozenAssertionSet`, `_SDKAuditManager.explain/.conflicts`, `save_workspace`/`load_workspace` are preserved. Q-NAMING-C is purely a subtractive + internal-refactor slice within the same Phase 1 sub-slice cluster as AD.
- **Step 4.6.5 pre-impl grep mandatory for subtractive slices**: per CADENCE explicit recommendation. The 20 flat shell deletion grep is the critical N-finding step (per Slice 6 precedent that surfaced 5 consumers missed by preflight).

### Cross-flip role assignment (per user 2026-05-31 推进 directive + AD precedent)

User directive "推进" 2026-05-31 implicitly preserves AD's cross-flip pattern unless explicitly changed:

- Step 4.1 blueprint draft → Claude (this commit)
- Step 4.2 review → Codex
- Step 4.3 preflight drafter → TBD (Claude or Codex; decided after Step 4.2 per AD precedent)
- Step 4.4 preflight amendment → drafter of Step 4.3 (typically Claude per AD precedent)
- Step 4.6 scoped anchor → Claude (AD precedent)
- Step 4.6.5 pre-impl grep → Codex (承接 implementation prep, AD precedent)
- Step 4.7 implementation → Codex (per user directive)
- Step 4.7 review → Claude (per user directive)
- Step 4.8 closure → Codex or Claude (per CADENCE no strict assignment)
- Step 4.9 archive → Codex or Claude (per CADENCE no strict assignment)

If user wants to flip any role assignment, they may signal at any handoff point.
