# v0.2.0 Release Surface Audit Blueprint: Audit Log

- Blueprint: [2026-06-01_release-surface-audit.md](./2026-06-01_release-surface-audit.md)

## Event Log

| Date | Stage | Event | Notes |
| --- | --- | --- | --- |
| 2026-06-01 | draft | Audit blueprint created post Q-NAMING + baseline cleanup closure | Step 4.1 draft on `v0.2.0-blueprint-release-surface-audit-2026-06-01` (forked from baseline cleanup archive HEAD `be0f2351`). User directive 2026-06-01: "可以考虑 A, 不过要先看关于 push 为 feature 分支的要求, 尤其是'我们只推 feature 分支', 具体的 release 由队友来实现" + clarification "我们指的是发布仓库 factgraph; origin 可以自由的 merge 因为它不是发布仓库, 此外同意 A'". Three governance anchors consulted: `feedback_push_master_gate` (master sacred, feature branches OK to push with auth), `project_release_branch_invariants` ("agent role = keep stack ready, NOT execute release"; factgraph publish repo is separate from `hnsm-backend/origin`), `feedback_release_workflow_traps` (6 traps T1-T6 from v0.1.0-rc.1 dry-run iterations). Scope: 7-bucket read-only audit (G1 allowlist freshness, G2 test exclusion, G3 deny-pattern, G4 EN-only, G5 typing-extensions informational, G6 release.sh dry-run feasibility, G7 v0.2.0 fresh additions delta vs v0.1.0-rc.1 `1aa157cc`). Out of scope per 11 non-goals (N1-N11): NO release.sh execution / NO factgraph repo push / NO release/0.1.x or 0.2.x touching / NO master mod / NO v0.1-oss-prep touching / NO Q-PR1 touching / NO release execution tasks (RELEASE_CHANGES.md / version bump / tag / PyPI / GitHub Release / milestone creation / factgraph projection commit) / NO new public-API surface creation / NO auto-decide on drift Options / NO dirty baseline touching / NO sacred precedent amend. Cross-flip per "延续一直以来的模式": Step 4.1 Claude draft (this), Step 4.2 Codex review, Step 4.3 Claude preflight, Step 4.4 Claude amend, Step 4.5 Claude self-check, Step 4.6 Claude scope freeze, Step 4.7 conditional (skip if 0 drift; Codex implements if drift found with Claude review), Step 4.8 closure, Step 4.9 archive. Sacred state at draft: Q-PR1 5 paths 0-diff vs `4c472b50`; master `562c74195df43e933bed92a3ff25de94dd8ce666` unchanged; dirty baseline 8 entries (4 M + 2 D + 2 untracked) preserved. |
| 2026-06-01 | draft (amended) | Step 4.2 review tightening | Codex Step 4.2 review verdict AMEND REQUIRED with 5 Required (P2-1..P2-5) + 2 Recommended (P3-1, P3-2), all source-grounded via shipped re-read of `scripts/release.sh` + `scripts/release_surface_allowlist.txt` (exactly 271 lines confirmed) + `scripts/project_release_surface.sh`. All 7 applied in this single doc-only amend commit on blueprint branch per Slice 7B Option A learning (NOT on a separate preflight branch). (P2-1) Step 4.3 2.a audit target ref was incorrectly pointed at sacred `master 562c74...`; corrected to v0.2.0 feature-line release source ref candidate (current branch HEAD on baseline cleanup archive lineage at or after `be0f2351`) — master remains referenced as sacred invariant only, NOT projection source. (P2-2) G6 release.sh dry-run feasibility tightened to ASSESSMENT-ONLY: shipped `release.sh --dry-run` is NOT read-only (creates local milestone branch + worktrees + release branch/tag, cleanup-restores refs); requires CLEAN tracked working tree (incompatible with dirty baseline 4M+2D+2untracked preserved by design); current worktree is permanently OFF-TARGET for any execution; any future dry-run requires explicit auth + clean isolated worktree distinct from current state. (P2-3) `docs/api/openapi.yaml` was asserted shipped in Related Modules + §4.2 inventory; shipped grep confirms file exists but is NOT in `scripts/release_surface_allowlist.txt`; reclassified as Step 4.3 preflight 2.b finding row pending classification (likely `(c) shape conflict` or `(d) genuinely new`). (P2-4) G2 split into G2.a release-verify (the 11 named focused unittest modules from `release.sh:213-224`: `tests.application.protocol.test_rule` + `.test_rule_expr` + `tests.sdk.test_ruleexpr_inspect` + `.test_rule_naming` + `tests.application.protocol.test_rule_aggregate` + `tests.test_branch_identity_rule_inspect` + `.test_rule_expr_lowering` + `.test_rule_expr_lowering_adapter` + `tests.sdk.test_rule_expr_evaluate` + `.test_rule_expr_head_validation` + `tests.test_sdk_assertion_record_set_view_filters`) vs G2.b excluded dev tests (informational classification, not pass/fail). (P2-5) G3 deny-pattern fully grounded in shipped: 31-entry `deny_patterns` array from `project_release_surface.sh:56-87` + 16-token `bad_link_pattern` regex from line 100 including `examples/`, `samples/`, `src/kernel`, `kernel.sdk`, `from kernel`, `tools/`, `requirements/dev.txt`; G3 split into G3.a deny-list path check + G3.b bad-link doc-content grep. (P3-1) G1 vs G7 ownership clarified: G1 owns current allowlist projection consistency (regression check for Q-NAMING-C/F + baseline cleanup deletions/renames); G7 owns forward delta (new public-API files since `1aa157cc` requiring new allowlist entries) — no overlap. (P3-2) Push wording fixed: "per-commit authorization" → "per-push authorization"; commits are local, pushes require auth. Sacred Q-PR1 5-path 0-diff preserved through amend; master `562c74195df4...` unchanged; dirty baseline 8 entries preserved. |
| 2026-06-01 | draft (amended) | Step 4.2 fast-pass cleanup (3 P2-FP1 residual sites) | Codex Step 4.2 final fast-pass verdict surfaced 3 residual P2-1 propagation sites still implying `master` is the release projection source; all 3 fixed in this single doc-only cleanup commit. Site 1 (blueprint §4.2 inventory line 81): "deletions of source files on `master` require simultaneous allowlist edits" → reframed to "deletions on the release source ref (the `--source-ref` selected at release execution time; v0.2.0 feature-line release source ref candidate per P2-1 LOCK)"; explicit "`master` referenced here is sacred invariant only, NOT the implied projection source" added. Site 2 (blueprint §7 Acceptance line 158): "Every allowlist entry resolves to an existing file in `master` HEAD source" → "Every allowlist entry resolves to an existing file at the v0.2.0 feature-line release source ref candidate (per P2-1 LOCK; `master` referenced as sacred invariant only)". Site 3 (audit Decision Notes line 24): "Sacred master will be referenced at preflight (Step 4.3 2.a) as the master HEAD baseline that the projection actually consumes" → "Per Step 4.2 P2-1 LOCK: `master` is sacred invariant only (must remain immutable through this audit); the projection audit consumes the v0.2.0 feature-line state (current branch HEAD on baseline cleanup archive lineage at or after `be0f2351`). The `--source-ref` selected at future release execution is teammate decision, NOT this audit's projection target." All 7 prior amendments (P2-1..P2-5 + P3-1/P3-2) verified by Codex as correctly landed; only this propagation cleanup needed. Sacred Q-PR1 5-path 0-diff preserved through cleanup; master `562c74195df4...` unchanged; dirty baseline 8 entries preserved. |
| 2026-06-01 | draft (amended) | Step 4.4 preflight amendment | Step 4.3 preflight artifact at `5b822b6a` on `v0.2.0-release-surface-audit-preflight-2026-06-01` (forked from blueprint scope-frozen audit source ref `497051a8`) executed 7-bucket investigation per blueprint §8 implementation plan. Codex Step 4.3 review surfaced 3 Required (P2-1, P2-2, P2-3) + 3 Recommended (P3-1, P3-2, P3-3); Claude amend `6bd941d3` + fast-pass cleanup `5b822b6a` applied all 6 on preflight branch (artifact lineage `b2ba64d8` → `6bd941d3` → `5b822b6a`). This blueprint Step 4.4 amend propagates Required + Recommended LOCKs from preflight artifact to blueprint scope (§5.3 + §6 Boundaries + §7 Acceptance + §8 Implementation Plan + §10 Outcome carry-forward), per Slice 7B Option A learning (LOCKs land on blueprint branch, NOT preflight branch). (PF-R1 LOCK G6 source-ref handoff) `release.sh` defaults `--source-ref master` but feature line is 915+ commits ahead at audit source `497051a8` (916 at preflight artifact HEAD `5b822b6a`; final at Step 4.8 closure ref); teammate handoff MUST document `--source-ref <final-v0.2.0-rc-feature-line-ref>` = post-Step-4.7 fix slice ref. `be0f2351` is historical lower bound, NOT recommended ref if any fix slice landed. (PF-R2 LOCK 4 G7-SRC-BLOCKER ADDs) 4 src files are projection-completeness BLOCKERs imported by already-allowlisted modules: `src/factgraph/application/entity_visibility.py` (← `application/__init__.py`), `application/retract_guard.py` (← `entity_write.py` + `ingest_runtime.py` + `sdk/store.py`), `application/value_validation.py` (← `entity_write.py`), `sdk/match_runtime.py` (← `sdk/store.py`). Step 4.7 is MANDATORY (not conditional). Step 4.7 fix slice MUST add 4 allowlist entries; without them projection import fails. (PF-r2 12 quickstart docs candidate) `docs/official/kernel/index.md` + 11 quickstart docs (`assertions.md`, `database.md`, `evidence.md`, `first-factgraph.md`, `index.md`, `namespace-map.md`, `persistence.md`, `read-write.md`, `rules-and-inferences.md`, `schema.md`, `semantics.md`) per `CLAUDE.md` "public quickstart docs target" designation are candidate ADDs; G3.b spot-check at preflight returned 0 bad-link hits; per-row confirmation required at Step 4.7. Q4 user decision pending. (PF-r3 LOCK Q2/Q3 docs bad-link gate) bare ADD of `docs/README.md` (refs `workflow/AGENTS.md` at :17 + `src/factgraph/AGENTS.md` at :23) or `docs/api/openapi.yaml` (refs `src/service/docs/06_frontend_integration.md` at :22) will fail G3.b projection check. Default = DEFER for both; Option A inline cleanup only with user authorization. PF-v1..PF-v8 verified findings recorded in Decision Notes below per CADENCE verified-findings convention. Q1 retracted (was 4 src files inclusion — now mandatory per PF-R2); Q5 resolved (G6 source-ref handoff phrasing — now PF-R1 LOCK); Q2/Q3/Q4 remain user decisions but do NOT block stack-ready determination. Sacred Q-PR1 5-path 0-diff preserved through amend; master `562c74195df4...` unchanged; dirty baseline preserved; preflight artifact branch at `5b822b6a` untouched. |

## Decision Notes

### 2026-06-01 — Initial scope rationale (audit-first feature blueprint per A' direction)

- **Why audit-first, not impl-first**: per `project_release_branch_invariants` "agent's role is to keep stack ready, not to push it through to release", and per user 2026-06-01 directive "具体的 release 由队友来实现", the agent's deliverable is a STACK-READY REPORT, not a release execution. Audit-first cadence produces evidence + classification + recommendation; impl only fires conditionally if drift surfaced.

- **Why feature branch + cross-flip cadence**: matches Q-NAMING + baseline cleanup precedent established 2026-05-31 / 2026-06-01. User explicitly continued the cross-flip pattern via "延续一直以来的模式" earlier in the session; this audit inherits the same Claude-drafts / Codex-reviews-or-implements structure.

- **Why 7 G-buckets (not fewer)**: 6 from `feedback_release_workflow_traps` T1-T6 + 1 from delta-vs-v0.1.0-rc.1 (G7) needed to cover both regression risk (allowlist/file deletions from Q-NAMING-C/F + baseline cleanup) AND forward risk (new additions since v0.1.0-rc.1 publish lineage at `1aa157cc`). G5 (typing-extensions) intentionally INFORMATIONAL because that's environment hygiene not source/doc fix; including it surfaces the check explicitly so it doesn't get forgotten at handoff time.

- **Why 11 non-goals (high count)**: release-adjacent work has high collateral damage risk; explicit enumeration of what is OUT of scope protects against scope creep into actual release execution territory. N7 (NOT performing release execution tasks) is the load-bearing boundary that separates this audit from teammate work.

- **Predecessor commit lineage**: blueprint forked from `be0f2351` (baseline cleanup archive HEAD) NOT from `master` (which is at sacred `562c74195df4...`, pre-v0.2 line). This is intentional — the audit operates on the v0.2 feature-line state because that's what represents v0.2 publish candidate. Per Step 4.2 P2-1 LOCK: `master` is sacred invariant only (must remain immutable through this audit); the projection audit consumes the **v0.2.0 feature-line state** (current branch HEAD on baseline cleanup archive lineage at or after `be0f2351`). The `--source-ref` selected at future release execution is teammate decision, NOT this audit's projection target.

- **Cross-flip role assignment notes for this audit**: Step 4.2 review by Codex is critical because Claude drafted the scope; Codex's source-grounded review (Rule 1) is the first cross-check. Step 4.3 preflight by Claude is the actual finding-table production — independent artifact branch per Slice 7B Option A. Step 4.7 implementation conditional: if drift found, Codex implements + Claude reviews per established 8-SS pattern; if no drift, blueprint becomes audit-only deliverable.

- **Push gate posture**: this blueprint will produce 1 + N commits (where N = preflight commits + optional fix slice commits + closure/archive). Per `feedback_push_master_gate`, no auto-push of any commit. Each push decision requires user signal. Feature branches push to `hnsm-backend/origin` is OK per user clarification 2026-06-01; projection to `factgraph` publish repo NEVER by agent.

### 2026-06-01 — Cross-flip role assignment (per user "延续一直以来的模式" directive)

User explicitly continued AD/C/E/B1/B2/F + Baseline Drift Cleanup cross-flip pattern for this audit:

- Step 4.1 audit blueprint draft → Claude (this commit)
- Step 4.2 review → Codex
- Step 4.3 preflight drafter → Claude (per all prior precedents)
- Step 4.4 preflight amendment → Claude
- Step 4.5 self-check → Claude (doc-only)
- Step 4.6 scoped anchor → Claude
- Step 4.7 implementation **conditional**: Codex if drift found + Claude review per cross-flip; skip if 0 drift
- Step 4.8 closure → Codex or Claude (per CADENCE no strict assignment)
- Step 4.9 archive → Codex or Claude

If user wants to flip any role assignment, they may signal at any handoff point.

### 2026-06-01 — Step 4.3 preflight verified findings record (PF-v1 through PF-v8)

Per CADENCE verified-findings convention, the following Step 4.3 preflight findings (artifact at `workflow/audit/active/2026-06-01_release-surface-vs-shipped.md` @ `5b822b6a` on `v0.2.0-release-surface-audit-preflight-2026-06-01`) require no blueprint amendment beyond Step 4.4 §10 carry-forward but are recorded as Decision Notes for closure §10 cross-reference:

- **PF-v1 (G1 allowlist current-projection consistency CLEAN)**: All 271 entries in `scripts/release_surface_allowlist.txt` resolve to existing files at audit source ref `497051a8`. Q-NAMING-C archive `23389a2b` 20-shell deletion did NOT introduce stale allowlist entries; Q-NAMING-F archive `10b10981` renames (12-cluster) did NOT break paths (file names preserved, classes renamed inside); baseline cleanup SS3 retired file `tests/test_sdk_read_policy.py` (→ `tests/_retired/`) correctly NOT in allowlist. 0 missing / 0 stale.

- **PF-v2 (G2.a 11 focused unittest modules CLEAN)**: All 11 modules from `scripts/release.sh:213-224` (`tests.application.protocol.test_rule` / `.test_rule_expr` / `tests.sdk.test_ruleexpr_inspect` / `.test_rule_naming` / `tests.application.protocol.test_rule_aggregate` / `tests.test_branch_identity_rule_inspect` / `.test_rule_expr_lowering` / `.test_rule_expr_lowering_adapter` / `tests.sdk.test_rule_expr_evaluate` / `.test_rule_expr_head_validation` / `tests.test_sdk_assertion_record_set_view_filters`) exist at audit source ref. All 11 import only from `factgraph.*` (no `kernel`, `service`, `agent`, `examples` imports); import-chain compatible with allowlist projection.

- **PF-v3 (G2.b excluded tests informational CLEAN)**: 156 `tests/**/*.py` + 6 `src/domains/*/tests/` files all stay on feature-line, never project. The 6 domain tests correctly caught by `src/domains/*` deny_pattern.

- **PF-v4 (G3.a deny-list path check CLEAN)**: 0 of 271 allowlist entries match any of 31 `deny_patterns` entries from `scripts/project_release_surface.sh:56-87`. Allowlist hygiene fully clean.

- **PF-v5 (G3.b bad-link doc-content grep on existing allowlisted docs CLEAN)**: 0 hits across 41 allowlisted `.md`/`.yaml` docs grep'd for 16-token `bad_link_pattern` regex from `scripts/project_release_surface.sh:100`. Existing shipped doc-content hygiene clean per T3. (Note: bad-link hits exist in `docs/README.md` + `docs/api/openapi.yaml` per PF-r3, but those are NOT in current allowlist — they would only fail G3.b if user authorizes ADD without inline cleanup.)

- **PF-v6 (G4 EN-only convention preserved)**: 0 `*.zh.md` / 0 `*.cn.md` entries in allowlist. Top-level `README.md` is single-name EN-only (per v0.1.0-rc.1 force-retag precedent `a9e1ad3 → 1aa157c`). Sampled allowlisted `*.en.md` files (`audit/docs/01_overview.en.md`, `core/docs/01_architecture.en.md`, `sdk/docs/00_user_guide.en.md`, `sdk/docs/04_api_surface.en.md`) have no CN siblings. `project_release_surface.sh:104` greps both `STAGING/README.md` and `STAGING/README.en.md` for bad links — current state has only `README.md` so `README.en.md` grep is harmless no-op.

- **PF-v7 (G5 typing-extensions environment healthy)**: typing-extensions 4.15.0 installed at `/Users/zhenzhili/miniforge3/lib/python3.10/site-packages` via `importlib.metadata`. No metadata-generation-failed signs at preflight time. T5 trap precondition fix `rm -rf <site-packages>/typing_extensions* && pip install --upgrade typing-extensions` remains documented for teammate use if metadata corruption surfaces at release execution time.

- **PF-v8 (Q-PR1 sacred 5-path 0-diff vs `4c472b50` at audit source ref AND preflight artifact HEAD)**: empty `git diff` confirmed at audit source `497051a8` and at preflight artifact lineage (`b2ba64d8` → `6bd941d3` → `5b822b6a`). Read-only audit + doc-only amendments did not touch Q-PR1 5 paths. Sacred invariant maintained throughout.

### 2026-06-01 — Step 4.4 Open Questions disposition

Per Step 4.3 review + amend lifecycle, the Q1-Q5 set updated:

- **Q1 (4 src files inclusion)** RETRACTED per P2-1 LOCK → now PF-R2 mandatory ADD (Step 4.7 fix slice scope)
- **Q2 (`docs/README.md` ADD)** OPEN per PF-r3; default DEFER unless user authorizes Option A inline cleanup
- **Q3 (`docs/api/openapi.yaml` ADD)** OPEN per PF-r3; default DEFER per `feedback_narrow_public_api` preview policy
- **Q4 (12 `docs/official/kernel/*` quickstart ADDs)** OPEN per PF-r2; working hypothesis ADD ALL per `CLAUDE.md` public quickstart docs designation; per-row G3.b confirmation at Step 4.7
- **Q5 (G6 source-ref handoff phrasing)** RESOLVED by P2-2 LOCK / PF-R1; handoff package documents `--source-ref <final-v0.2.0-rc-feature-line-ref>` as mandatory teammate parameter

Q2/Q3/Q4 are nice-to-have inclusion decisions, not stack-ready blockers. Step 4.7 fix slice scope = mandatory PF-R2 (4 ADDs) + optional Q4 (12 ADDs if user authorizes) + optional Q2/Q3 Option A inline cleanup (default DEFER both).
