# Q-NAMING-B1 Blueprint Audit: Safe class renames (top-level DTO + descriptor/locator classes)

- Blueprint: [2026-05-31_q-naming-b1.md](./2026-05-31_q-naming-b1.md)

## Event Log

| Date | Stage | Event | Notes |
| --- | --- | --- | --- |
| 2026-05-31 | draft | Blueprint created | Initial scope recorded covering Q-NAMING decision §4.3.1 (4 top-level class renames) + §4.3.2 (8 descriptor/locator/added/TypeAlias renames including P2-1/P2-2 corrections dropping `Rule` prefix). Phase 4 of 6 sub-slices per Q-NAMING §4.9 ordering. **Yellow risk** per audit §3 + Q-NAMING §4.9 Phase 4 classification (class-level only, no wire field changes). Branch `v0.2.0-blueprint-q-naming-b1-2026-05-31` forked from Q-NAMING-E archive HEAD `121a375d` (inherits Q-NAMING-AD + Q-NAMING-C + Q-NAMING-E complete lifecycles + N1/N2/N3/N7/N14/N15/N16/N17 invariants). Sacred Q-PR1 5-path 0-diff preserved at draft commit; sacred master `562c74195df4...` unchanged; dirty baseline (4 M + 2 D + 2 untracked) preserved. |
| 2026-05-31 | draft (amended) | Step 4.2 review tightening | Codex Step 4.2 review surfaced 3 Required + 2 Recommended findings, all shipped-code-grounded; all 5 applied in this single doc-only amend commit. (P2-1) Public export surfaces under-specified: shipped `application/protocol/__init__.py` re-exports 11 B1 names at lines 12/21/25/26/33-37/42/54/77 imports + 124/132/159/164-165/188/203-204/209-210/227 `__all__`; `sdk/__init__.py` re-exports `AtomDescriptor` at line 34 + line 124 `__all__`; SupportArtifact NOT in application/protocol/__init__.py (lives at core/store/_support.py). Added §5.1.1 explicit export coverage with full line citations; added §7 acceptance row + §8 Step 7.12a implementation step; updated Step 4.3 preflight 2.b for export module enumeration and 2.f for naming conflict re-verification. (P2-2) B1/B2 boundary must include class-coupled lower-snake helpers/fields/dict-keys to avoid over-rename: `support_artifact_*` family (helpers), `_row_support_artifacts`, `literal_path` (field/parameter/key), `added_atom` (field/parameter/key), `atom_verdicts` (field/parameter/key). Extended §5.2 with explicit preservation list + implementation discipline ("Class symbol rename updates only" + "Lower-snake helper / function / field / parameter / dict key names are PRESERVED unchanged"); added §7 acceptance row for B1/helper boundary; extended Step 4.3 preflight 2.d. (P2-3) SupportArtifact helper policy explicit: `_support.py` exports `support_artifact_to_dict` (line 229), `support_artifact_from_dict` (line 265), `support_artifact_bytes` (line 302), `compute_support_digest` (line 311); B1 renames type `SupportArtifact` → `ProofReceipt` but PRESERVES helper function names unchanged + updates only type annotations referencing the class symbol. Added §2 G1 helper policy + clarified scope. (P3-1) Error message policy distinguishing class names from field paths: class-name token UPDATES (e.g., `"expected SupportArtifact"` → `"expected ProofReceipt"`); field-path / instance-field / dict-key token PRESERVES (e.g., `"literal_path must be ..."` stays; `"$.support_artifact[0].atom_index"` stays). Added §5.2.1 error message policy section; added §7 acceptance row; extended Step 4.3 preflight 2.d with error-message-site enumeration requirement. (P3-2) Step 4.6.5 docs/test inventory more concrete: §5.6 N-3 enumerated current-truth doc directories (`application/docs/`, `sdk/docs/07_walker_and_advanced.en.md` + sdk/docs/*, `core/docs/`, `audit/docs/`, `authoring/docs/`, `adapters/docs/`, `core/store/docs/`, `docs/official/kernel/quickstart/`, root quickstart) + explicit historical carve-out for `docs/references/working/**`. Sacred Q-PR1 0-diff preserved through amend; master unchanged; dirty baseline preserved. |
| 2026-05-31 | draft (amended) | Step 4.4 preflight amendment | Step 4.3 preflight at `e37ef437` on independent artifact branch `v0.2.0-q-naming-b1-preflight-2026-05-31` (forked from `2c342c59`) surfaced 0 Required + 1 Recommended (PF-r1) + 8 Verified (PF-v1-v8) + 1 Scoped-detail (PF-s1) + 0 Abandonment findings — CADENCE healthy distribution, notably 0 Required (contrast Q-NAMING-E's 1 Required PF-R1 Q-PR1 trap). Codex Step 4.3 review pass verdict at `e37ef437`. Both applicable findings (PF-r1 + PF-s1) applied in this single doc-only amend commit on blueprint branch (NOT preflight branch — per Slice 7B Option A learning). (PF-r1 LOCK Sub-Q3 single-slice cadence) Blueprint §1 + §10 amended with explicit single-slice lock citing 114 deduped file footprint (53 src + 41 tests + 20 docs, refined from Step 4.1 hits-only estimate) + 12 mechanical class symbol renames + 0 Q-PR1 sacred blockers (PF-v1) + clean B1/B2 lower-snake helper boundary (PF-v3) + 0 shipped naming conflicts (PF-v2). Slice may fragment only if Step 4.6.5 pre-impl grep surfaces unexpected consumer clusters. (PF-s1 LOCK 8-class cluster rename ordering) Blueprint §10 amended with implementation discipline carry-forward: (1) TypeAliases first (`derivation_fact_overlay.py:31,34`) → (2) classes whose annotations depend on TypeAliases (lines 160, 202) → (3) classes that reference renamed classes via field types (180, 213, 264) → (4) independent renames (104, 123) → (5) `__all__` block last (line 420). Cross-file ordering: AtomDescriptor downstream in `sdk/__init__.py` after `application/protocol/__init__.py`; SupportArtifact downstream: 4 `_support.py` helper signatures (lines 229/265/302/311) update parameter+return type annotations only, helper function names preserved. Per-class synchronized 3-site update (host class declaration + protocol `__init__.py` import + protocol `__init__.py` `__all__`); AtomDescriptor adds 2 more sites; SupportArtifact requires only 1 module-level site. PF-v1-v8 verified findings recorded in Decision Notes below per CADENCE verified-findings convention. PF-s1 deferred to Step 4.7 §10 closure for implementation choice recording. Sacred Q-PR1 0-diff preserved through amend; master `562c74195df4...` unchanged; dirty baseline preserved; preflight branch at `e37ef437` untouched. |
| 2026-05-31 | scoped | Preflight amendments and self-check passed | PF-r1 single-slice cadence LOCK + PF-s1 8-class cluster ordering LOCK + PF-v1-v8 verified + 0 Required, all covered by `9aaa2405` (Step 4.4 amend). Step 4.5 self-check confirmed: PF coverage matrix consistent (PF-r1 = 2 sites §1 + §10; PF-s1 = 3 sites §1 fragment + §10 ordering + §10 carry-forward; PF-v1-v8 = 10 references in audit log); invariant coverage heavy (Q-PR1 = 26 mentions; N12-N17 = 15 mentions; e37ef437 preflight HEAD = 5 references); §1-§10 section structure continuous; §5.1-§5.7 subsection structure complete; 0 stale "preflight 2.k" / "Options A/B/C" residuals (B1 has no PF-R1 force-option lock — those were Q-NAMING-E specific); sacred Q-PR1 5-path 0-diff preserved; sacred master `562c74195df4...` untouched; dirty baseline 8 entries preserved. Scope frozen; ready for Step 4.6.5 pre-impl grep (Codex 承接, recommended for class-rename slices per Q-NAMING-AD/C/E precedent — full active test consumer enumeration + cross-class internal cluster confirmation + N-3 current vs historical doc carve-out verification) + Step 4.7 implementation on `v0.2.0-impl-q-naming-b1-2026-05-31`. |

## Decision Notes

### 2026-05-31 — Initial scope freeze rationale

- **Yellow-risk scope per Q-NAMING §4.9 Phase 4**: Top-level class renames that do NOT alter wire field names. Class symbols + their TypeAliases only; instance fields + wire-serialized field names + diagnostic key strings deferred to Q-NAMING-B2 (Phase 5, Red, stop-gated after B1).
- **12 renames per §4.3.1 (4) + §4.3.2 (8 including P2-1/P2-2 corrections)**:
  - G1-G4 (§4.3.1): SupportArtifact → ProofReceipt; EvaluationOverlay → FactOverlay; FactValueOverride → ReplaceFact; FactRemoveAction → RemoveFact
  - G5-G8 (§4.3.2 descriptor/locator/verdict): AtomDescriptor → ConditionDescriptor; DiagnoseAtomLocator → DiagnoseConditionLocator; WhyNotAtomLocator → WhyNotConditionLocator; ProofFrameAtomVerdict → ProofFrameConditionVerdict
  - G9-G10 (§4.3.2 + P2-1 corrections drop Rule prefix): RuleAddedAtom → **AddedCondition** (not RuleAddedCondition); RuleAddedAtomKind TypeAlias → **AddedConditionKind**
  - G11-G12 (§4.3.2 + P2-2 corrections drop Rule prefix): RuleLiteralPath → **ConditionPath** (not RuleConditionPath); RuleLiteralPathKind TypeAlias → **ConditionPathKind**
- **B1/B2 class-vs-field boundary** (§5.2 critical): class symbol rename only. Instance fields like `atom_index`, `atom_key`, `branch_index`, `engine_payload`, `b{branch}.a{atom}` diagnostic keys all deferred to Q-NAMING-B2. The "atom" prefix in *class names* is what Q-NAMING-B1 renames; the "atom" in *field names* / *key strings* is Q-NAMING-B2 scope.
- **No naming conflicts verified at Step 4.1 scan**: all 12 new names return 0 shipped occurrences in `src/`. No shadowing risk.
- **Initial Q-PR1 boundary observation**: 11 of 12 classes live in `application/protocol/` (not Q-PR1 sacred). `SupportArtifact` lives at `core/store/_support.py:97` — `_support.py` is NOT in Q-PR1 sacred list per §4.8.6. Preflight 2.a explicitly verifies this; no PF-R1-style blocker expected.
- **8-class concentration in derivation_fact_overlay.py**: FactValueOverride, FactRemoveAction, EvaluationOverlay, RuleAddedAtom, RuleAddedAtomKind, RuleLiteralPath, RuleLiteralPathKind plus their internal cross-references all in single file. Preflight 2.e verifies cross-rename ordering consistency.
- **Q-NAMING-E precedent reuse**: PF-R1 Q-PR1 detection discipline (verify each class symbol against sacred paths before lock), single-slice cadence per Sub-Q3, AD/C/E inherited preservation (N12/N13/N14 explicit).
- **Hard-cut per §4.8.1**: no aliases, no dual-emit, old class names removed in same commit as new names introduced. Alpha release; no historical user protection burden.
- **Sub-Q3 single-slice cadence**: all 12 class renames likely land in one feat commit unless Step 4.3 preflight surfaces blocker.

### 2026-05-31 — Step 4.3 preflight verified findings record (PF-v1 through PF-v8 + PF-s1)

Per CADENCE verified-findings convention, the following Step 4.3 preflight findings (artifact at `workflow/audit/active/2026-05-31_q-naming-b1-preflight.md` @ commit `e37ef437`) require no blueprint amendment but are recorded as Decision Notes for closure §10 cross-reference:

- **PF-v1 (Q-PR1 sacred 5-path immunity)**: All 12 B1 old class names {`SupportArtifact`, `EvaluationOverlay`, `FactValueOverride`, `FactRemoveAction`, `AtomDescriptor`, `DiagnoseAtomLocator`, `WhyNotAtomLocator`, `ProofFrameAtomVerdict`, `RuleAddedAtom`, `RuleAddedAtomKind`, `RuleLiteralPath`, `RuleLiteralPathKind`} return 0 hits across Q-PR1 sacred 5 paths (`core/evidence/write_protocol.py`, `core/store/ledger.py`, `core/store/_builders.py`, `adapters/pyreason/`, `core/derivation/accept.py`). **No PF-R1-style Q-PR1 blocker** (contrast Q-NAMING-E PF-R1 which had `pyreason/rule_ext.py:101` `rule.where` trap forcing G4 Option D). Q-PR1 0-diff invariant remains automatically preserved through B1 implementation. This is the structural reason B1 ships as a **Yellow** slice rather than Red.

- **PF-v2 (zero naming conflicts in shipped surfaces)**: All 12 new names {`ProofReceipt`, `FactOverlay`, `ReplaceFact`, `RemoveFact`, `ConditionDescriptor`, `DiagnoseConditionLocator`, `WhyNotConditionLocator`, `ProofFrameConditionVerdict`, `AddedCondition`, `AddedConditionKind`, `ConditionPath`, `ConditionPathKind`} return 0 hits in `src/` + `tests/`. 6 names appear in `docs/references/working/**` only (ProofReceipt=1 file, FactOverlay=2 files, ReplaceFact=1, RemoveFact=1, AddedCondition=1, ConditionPath=1) per Q-NAMING §4.8.5 historical / working-paper carve-out. No shipped runtime/test consumer collides. B1 has zero shadowing risk.

- **PF-v3 (B1/B2 helper boundary verified)**: `core/store/_support.py` ships 4 lower-snake helpers (`support_artifact_to_dict` line 229, `support_artifact_from_dict` line 265, `support_artifact_bytes` line 302, `compute_support_digest` line 311) whose **function names are PRESERVED** in B1; only `SupportArtifact` parameter type annotations update to `ProofReceipt`. `_row_support_artifacts` field at `evaluate_result.py:183` PRESERVED. Instance fields `literal_path` (`derivation_fact_overlay.py:185`), `added_atom` (line 217), `atom_verdicts` (41 src hits), `atom_index` (187 hits), `atom_key` (220 hits), `branch_index` (291 hits) all PRESERVED — B2 scope. Implementation discipline: class symbol rename ONLY at top-level `class Foo:` declaration + downstream type annotation updates + isinstance() updates + error-message class-name tokens; everything lower-snake stays.

- **PF-v4 (8-class cluster topology in derivation_fact_overlay.py)**: 7 B1 entries (5 classes + 2 TypeAliases) at lines 31 (`RuleLiteralPathKind`), 34 (`RuleAddedAtomKind`), 104 (`FactValueOverride`), 123 (`FactRemoveAction`), 160 (`RuleLiteralPath`), 202 (`RuleAddedAtom`), 264 (`EvaluationOverlay`). 7 non-B1 classes preserved untouched in same file: lines 143 (`RuleDisableAction`), 180 (`RuleLiteralReplaceAction`), 213 (`RuleAddConditionAction`), 282 (`FactOverlayCheckRequest`), 311 (`OverlayCheckPhase`), 340 (`OverlayCheckDiff`), 367 (`FactOverlayCheckResult`). Internal cross-references at line 161 (`kind: RuleLiteralPathKind`), line 185 (`literal_path: RuleLiteralPath`), line 217 (`added_atom: RuleAddedAtom`), error messages at lines 195–196 + 224–225 + 243, `__all__` block at line 420.

- **PF-v5 (export module enumeration)**: `application/protocol/__init__.py` ships 11 of 12 B1 classes (lines 12 `DiagnoseAtomLocator`, 21 `EvaluationOverlay`, 25 `FactRemoveAction`, 26 `FactValueOverride`, 33–37 `RuleAddedAtom`/`RuleAddedAtomKind`/`RuleLiteralPath`/`RuleLiteralPathKind`, 42 `WhyNotAtomLocator`, 54 `ProofFrameAtomVerdict`, 77 `AtomDescriptor` imports + corresponding `__all__` at lines 124, 132, 159, 164–165, 188, 203–204, 209–210, 227). SupportArtifact host at `core/store/_support.py:97`, NOT re-exported through `core/store/__init__.py` (0 hits). `sdk/__init__.py` re-exports only `AtomDescriptor` (line 34 import + line 124 `__all__`). Per-class synchronized site count: 11 protocol-package classes require 3 sites each; AtomDescriptor adds 2 more sites (sdk re-export); SupportArtifact requires only 1 module-level site.

- **PF-v6 (footprint refined to 114 deduped files)**: 53 unique `src/` files + 41 unique `tests/` files + 20 unique `docs/` files = **114 total deduped files** touched by 12 renames. Per-class file counts: SupportArtifact 88 files (42 src + 31 tests + 15 docs — heaviest), EvaluationOverlay 55 (21 + 23 + 11), FactValueOverride 35 (9 + 19 + 7), ProofFrameAtomVerdict 19 (9 + 10 + 0), RuleLiteralPath 19 (9 + 8 + 2), RuleAddedAtom 16 (8 + 6 + 2), FactRemoveAction 15 (7 + 7 + 1), DiagnoseAtomLocator 12 (4 + 5 + 3), AtomDescriptor 6 (5 + 1 + 0), WhyNotAtomLocator 6 (4 + 2 + 0), RuleLiteralPathKind 2 (2 + 0 + 0), RuleAddedAtomKind 2 (2 + 0 + 0). Revision from Step 4.1 hits-only estimates (1110 hits total) to file-counted footprint.

- **PF-v7 (AD/C/E inherited surface preservation)**: Grep on `src/factgraph` confirms preservation: FrozenAssertionSet (3 files), save_workspace (3 files), commit_assertions (2 files), assertion_views (1 file), EmitSpec (3 files), emits (3 files). Q-NAMING-B1 scope (12 class symbol renames) does NOT cross into AD assertion-API, C workspace-API, or E rule/inference DSL surfaces. N12 (FrozenAssertionSet preserved) + N13 (workspace lifecycle preserved) + N14 (Inference EmitSpec preserved) + N15/N16/N17 (no fg.what_if, commit_assertions byte-identical, Query carve-out) all remain intact through B1.

- **PF-v8 (error-message class-name token policy)**: ~20 sites enumerated across `src/factgraph/**/*.py`. By UPDATE-target class: **SupportArtifact 10 sites** (`_artifact_sidecar.py:52`, `runtime.py:109`, `_validation.py:141`, `rule_literal_replace.py:76`, `derivation_check.py:79`, `evaluate_result.py:1215`, `proofframe.py:91`, `rule_add_condition.py:76`, `rule_disable.py:74`, plus class declaration). **EvaluationOverlay 6 sites** (`_validation.py:112+166`, `rule_literal_replace.py:78`, `proofframe.py:93`, `rule_add_condition.py:78`, `rule_disable.py:76`). **RuleLiteralPath 1 site** (`derivation_fact_overlay.py:196`). **RuleAddedAtom 1 site** (line 225). **{FactValueOverride|FactRemoveAction} combined 1 site** (line 243). **WhyNotAtomLocator 1 site** (`derivation_why_not.py:235`). **ProofFrameAtomVerdict 1 site** (`proofframe.py:80`). Per §5.2.1 policy: class-name tokens UPDATE; field-name tokens like `support_artifact must be ...`, `literal_path must be ...`, `added_atom must be ...`, `engine_payload must be ...`, `_row_support_artifacts values must be ...`, `atom_locator must be ...`, `overlay must be ...` all PRESERVE the lower-snake/parameter-name part while UPDATING only the class-name part. No mixed-policy ambiguity surfaced.

- **PF-s1 (8-class cluster rename ordering — deferred to Step 4.7 §10)**: Single-file dependency chain in `derivation_fact_overlay.py`: (1) TypeAliases first — `RuleLiteralPathKind` (line 31) → `ConditionPathKind`; `RuleAddedAtomKind` (line 34) → `AddedConditionKind`. (2) Classes whose annotations depend on TypeAliases — `RuleLiteralPath` (line 160; `kind: RuleLiteralPathKind` at line 161 becomes `kind: ConditionPathKind`) and `RuleAddedAtom` (line 202; `kind: RuleAddedAtomKind`). (3) Classes that reference renamed classes via field types — `RuleLiteralReplaceAction` (line 180; preserves class name but `literal_path: RuleLiteralPath` field type annotation updates to `ConditionPath` + line 195–196 isinstance + error msg); `RuleAddConditionAction` (line 213; preserves class name but `added_atom: RuleAddedAtom` field type annotation updates to `AddedCondition` + line 224–225 isinstance + error msg); `EvaluationOverlay` (line 264; verdict field type annotations may reference `FactValueOverride`/`FactRemoveAction`). (4) Independent renames `FactValueOverride` (line 104), `FactRemoveAction` (line 123) — no internal dependencies in this file beyond the error msg at line 243. (5) `__all__` block (line 420) last. **Cross-file**: AtomDescriptor descendant updates downstream in `sdk/__init__.py` after `application/protocol/__init__.py`. SupportArtifact downstream: 4 `_support.py` helper signatures (lines 229/265/302/311) update `SupportArtifact` → `ProofReceipt` in parameter+return type annotations only; helper names preserved. Implementation strategy: rename per-class with synchronized 3-site update (host class declaration + protocol `__init__.py` import + protocol `__init__.py` `__all__`); record final ordering choices in §10.

### Cross-flip role assignment (per user 2026-05-31 推进 directive + AD/C/E precedent)

User directive "推进" 2026-05-31 implicitly preserves AD/C/E cross-flip pattern unless explicitly changed:

- Step 4.1 blueprint draft → Claude (this commit)
- Step 4.2 review → Codex
- Step 4.3 preflight drafter → Claude (per AD/C/E precedent)
- Step 4.4 preflight amendment → Claude (per AD/C/E precedent)
- Step 4.5 self-check → Claude (doc-only)
- Step 4.6 scoped anchor → Claude (per AD/C/E precedent)
- Step 4.6.5 pre-impl grep → Codex (承接 implementation prep, AD/C/E precedent)
- Step 4.7 implementation → Codex (per user directive)
- Step 4.7 review → Claude (per user directive)
- Step 4.8 closure → Codex or Claude (per CADENCE no strict assignment)
- Step 4.9 archive → Codex or Claude (per CADENCE no strict assignment)

If user wants to flip any role assignment, they may signal at any handoff point.
