# Q-NAMING-B2 Blueprint Audit: Field/wire/key cascade (atom→condition + branch→case + engine_payload→proof + diagnostic keys + WhyNot wording)

- Blueprint: [2026-05-31_q-naming-b2.md](./2026-05-31_q-naming-b2.md)

## Event Log

| Date | Stage | Event | Notes |
| --- | --- | --- | --- |
| 2026-05-31 | draft | Blueprint created | Initial scope recorded covering Q-NAMING decision §4.3.2 (4 atom-cascade field-level renames: `atom_index` / `atom_key` / `_atom_index_from_key` / `materialized_atom_index`) + §4.3.4 (3 branch-cascade renames `branch_index` / `runtime_branch_index` / `engine_payload` + 10-site diagnostic key format change `b{branch}.a{atom}` → `c{case}.c{condition}`) + §4.3.5 (WhyNot wording narrow target, Step 4.2 lock pending) + §4.3.6 hard-cut per Sub-Q1. Phase 5 of 6 sub-slices per Q-NAMING §4.9 ordering. **Red risk** per audit §3 + Q-NAMING §4.9 Phase 5 classification (wire/persistence + cross-cutting cascade), but Step 4.1 grep confirms **0 Q-PR1 sacred hits across all 7 field names** (positive finding inherited from Q-NAMING-B1 precedent — no PF-R1-style Q-PR1 blocker expected). Branch `v0.2.0-blueprint-q-naming-b2-2026-05-31` forked from Q-NAMING-B1 archive HEAD `30bd31c8` (inherits Q-NAMING-AD + C + E + B1 complete lifecycles + N1/N2/N3/N7/N14/N15/N16/N17/N18/N19 invariants). Sacred Q-PR1 5-path 0-diff preserved at draft commit; sacred master `562c74195df4...` unchanged; dirty baseline (4 M + 2 D + 2 untracked) preserved. Step 4.1 grep summary: `atom_index` 278 hits / 41 files, `atom_key` 141/33, `branch_index` 436/60 (heaviest), `engine_payload` 46/17, 10 diagnostic key emit sites enumerated, WhyNot wording 8 sites. Deduped initial footprint: 44 src + 42 tests = 86 unique files (excluding WhyNot). Likely Step 4.7 3-commit pattern needed (main feat + P1 fix + docs align per Q-NAMING-E Red precedent). |
| 2026-05-31 | draft (amended) | Step 4.2 review tightening | Codex Step 4.2 review surfaced 4 Required + 2 Recommended findings, all shipped-code-grounded; all 6 applied in this single doc-only amend commit. (P2-1) G8 Q-PR1 check under-scoped — blueprint only verified 7 long-form field names but Q-PR1 sacred contains diagnostic-prefix variants: `pyreason/where_compile.py:56` uses `f"{base_name}_b{branch_idx}"` rule_name suffix + `branch_idx`/`atom_idx` short-form locals across `pyreason/where_compile.py` + `pyreason/rule_ext.py` = 32 hits Q-PR1 sacred. Added N20 explicit short-form preservation; amended §1 + §5.4 Q-PR1 statement; added §8 preflight 2.a Q-PR1 diagnostic-prefix variant grep; added §5.5 step 3 Category C carve-out; added G8 Q-PR1 sacred carve-out line. (P2-2) G8 diagnostic-key ecosystem incomplete — shipped parser/carrier/export surfaces must coordinate with format change: `application/walker/keys.py` ships `_ATOM_KEY_RE` regex (line 12) + `class AtomKeyView` (line 15) + `def parse_atom_key` (line 156); exports at 8 sites across `walker/__init__.py:39,40,53,68` + `application/__init__.py:58,66,118,189`; carrier fields `PredWitness.pred_atom_key` + `RuleRefEdge.ruleref_atom_key` at `_support.py:37,67` + `ruleref_types.py:31` + ~15 callsites in `_candidate_evidence_tree.py`; 2 local parser site clusters at `diagnose_runtime.py:764-806` + `derivation_check_runtime.py:382-420`; `step_key` PRESERVED (no atom suffix). Added G14 ecosystem migration goal + §5.5.1 detailed ecosystem decision section + §8 preflight 2.k mandatory; added §7 acceptance row. (P2-3) WhyNot target locked to align with shipped Diagnose convention — `why_not_runtime.py:388` already uses `if diagnose_result.status == "passed":`. Locked G9 default to `green→passed`, `red→failed`, `WhyNotRedRow→WhyNotFailedRow`, error code `WHY_NOT_DIAGNOSE_PASSED_FAILED_BINDING` (rejected initial `holds/misses` proposal as introducing new vocabulary not grounded in shipped code; rejected `accepted/rejected` alternative). Updated §2 G9 + §5.6 (covered indirectly via §5.5.1 ecosystem) + §7 acceptance. (P2-4) `engine_payload → proof` collision under-classified — multiple structured `"proof"` keys belong to separate provenance/evidence-tree schemas: `adapters/souffle/provenance.py:268` Souffle proof JSON key, 2 tests JSON fixtures `tests/test_souffle_provenance_v0.py` + `tests/test_souffle_evidence_graph.py`, `_candidate_evidence_tree_narrative.py:90+346+350` role tuple/key, `_candidate_evidence_tree_summary.py:26+27+125+128` role mapping. Added G15 narrow scope + §2 G7 NARROWED clarification + §8 preflight 2.f classification table + §7 acceptance. (P3-1) `branch_id=f"b{idx}"` namespace separate decision — `rule_expr_lowering.py:723` uses `branch_id` field (preserve per N19) but `b{idx}` template inside is namespace identifier distinct from canonical diagnostic key format. Added N21 + §5.5 step 3 3-category split (A canonical key / B branch-id namespace / C Q-PR1 sacred) + §8 preflight 2.h extended + §7 G16 acceptance. (P3-2) Step 4.1 footprint should include spillover subdirs — fresh grep confirms `src/service` 2 files (`atom_key`) + `examples/` 4-8 dedup files + `tutorials/` 1-2 files + `docs/` 10-15 current docs. Added §4 broader scope spillover finding; extended §5.7 enumeration with all 8 subdirs; updated G11 acceptance. Sacred Q-PR1 0-diff preserved through amend; master unchanged; dirty baseline preserved; preflight branch fork still pending (Step 4.3). |
| 2026-05-31 | draft (amended) | Step 4.4 preflight amendment | Step 4.3 preflight at `5b40fa2e` on independent artifact branch `v0.2.0-q-naming-b2-preflight-2026-05-31` (forked from `e9a4ef4f`) surfaced 1 Required (PF-R1) + 2 Recommended (PF-r1, PF-r2) + 10 Verified (PF-v1-v10) + 1 Scoped-detail (PF-s1) + 0 Abandonment findings — CADENCE healthy distribution; Required count higher than B1's 0 (reflecting Red slice ecosystem complexity), still lower than typical Red slices (E had 1 Required PF-R1 Q-PR1 trap). Codex Step 4.3 review PASS verdict at `5b40fa2e`. All 3 amendments + Decision Notes applied in this single doc-only amend commit on blueprint branch (NOT preflight branch — per Slice 7B Option A learning). (PF-R1 LOCK G14 ecosystem expansion) Blueprint §5.5.1 expanded to enumerate full 57 deduped files vs Step 4.2 ~15 estimate — new clusters: `core/rules/_trace.py` (22 hits), `core/rules/rule_ir.py` (5 hits), `core/rules/frontier.py` (1 hit), `core/annotation/_certainty.py`, `adapters/souffle/{where_compile,engine_eval}.py`, `sdk/shells/diagnose.py`, 8 `application/` runtime/walker files, 6 `src/domains/ecss/tests/` files, 2 `src/service/` files. Full B2 deduped footprint = 132 files (vs B1's 114). Added persistence JSON dict-key cascade section + 3-commit pattern budget per Q-NAMING-E Red precedent. (PF-r1 LOCK Category B Option A) Blueprint §5.5 step 3 Category B + §8 preflight 2.h marked LOCKED: `branch_id=f"b{idx}"` template at `rule_expr_lowering.py:722` (`_assign_branch_ids` lines 718-722) + `fallback_id = f"b{idx}"` at `sdk/store.py:3639` (Case.id fallback) → `f"c{idx}"` for Case class supremacy from Q-NAMING-E. Field name `branch_id` PRESERVED per N21. Alternative PRESERVE rejected (no strong reason given Case supremacy). (PF-r2 LOCK persistence JSON dict-key cascade) Blueprint §5.5.1 explicit section + §10 carry-forward: `pred_atom_key`/`ruleref_atom_key` are persisted JSON dict keys via B1-preserved `support_artifact_to_dict`/`_from_dict`; G14 ecosystem rename = wire/persistence hard-cut category like G7; test fixture + JSON snapshot regeneration budgeted. PF-v1-v10 verified findings recorded in Decision Notes below per CADENCE verified-findings convention. PF-s1 G14 ecosystem rename ordering deferred to Step 4.7 §10 closure for implementation choice recording. Sacred Q-PR1 0-diff preserved through amend; master `562c74195df4...` unchanged; dirty baseline preserved; preflight branch at `5b40fa2e` untouched. |
| 2026-05-31 | scoped | Preflight amendments and self-check passed | PF-R1 G14 ecosystem expansion LOCK + PF-r1 Category B Option A LOCK + PF-r2 persistence JSON dict-key cascade LOCK + PF-v1-v10 verified + 1 Required addressed, all covered by `30940896` (Step 4.4 amend). Step 4.5 self-check confirmed: PF coverage matrix consistent (PF-R1 = 20 mentions across §5.5.1 + §10; PF-r1 = 4 sites §5.5 step 3 + §8 preflight 2.h + §10; PF-r2 = 3 sites §5.5.1 + §10; PF-v1-v10 = 12 references in audit log); invariant coverage heavy (Q-PR1 = 29 mentions; N12-N21 inherited = 42 mentions; `5b40fa2e` preflight HEAD = 9 references across blueprint + audit); §1-§10 main section structure continuous; §5.1-§5.9 subsection structure complete; 0 stale "TBD per Step 4.2" / "pending Step 4.2" / "preflight 2.k re-verifies" residuals; sacred Q-PR1 5-path 0-diff preserved; sacred master `562c74195df4...` untouched; dirty baseline 8 entries preserved. Scope frozen at 132-file deduped footprint (vs B1's 114) with 16 goals (G1-G16) + 21 non-goals (N1-N21) + 12 mandatory preflight items (2.a-2.l) all locked. Ready for Step 4.6.5 pre-impl grep (Codex 承接, MANDATORY for Red slice per Q-NAMING-AD/C/E/B1 precedent — full 57-file G14 ecosystem enumeration + Category B template confirmation + persistence JSON dict-key cascade verification + N-7 examples/tutorials proactive scan + 8-subdir spillover enumeration per P3-2) + Step 4.7 implementation on `v0.2.0-impl-q-naming-b2-2026-05-31` (3-commit pattern budgeted per Q-NAMING-E Red precedent). |
| 2026-05-31 | scoped (amended) | Step 4.6.5 pre-impl grep inventory | Codex populated §5.8 on implementation branch `v0.2.0-impl-q-naming-b2-2026-05-31` after forking from scoped blueprint `ac0ccb05`. Authoritative grep union found 137 matched files before carve-outs, with 20 historical/dirty carve-outs (`docs/references/working/**`, `examples/archive/**`, dirty notebooks `examples/01_sdk_check_diagnose.ipynb` + `examples/02_overlay_why_not_frontier.ipynb`) and 117 active implementation files. Inventory breakdown: N-1 active tests = 56 files; N-2 active source = 57 files; N-3 current docs/examples/tutorials = 11 files; N-4 Q-PR1 long-form fields remain 0 hits while N20 short-form pyreason variants are preserved; N-5 branch_index+atom_index co-occurrence = 56 active files; N-8 fixture/snapshot/persistence material = 82 active files. Status remains `scoped` per CADENCE Option 2 (no rollback, implementation not started); sacred Q-PR1 0-diff preserved; master unchanged; dirty baseline preserved. Claude fast-pass PASS with 1 N-3 omission note: `src/factgraph/application/walker/docs/README.md` (4 hits documenting `AtomKeyView`/`parse_atom_key`/canonical `b{branch}.a{atom}` format) added to Step 4.7 N-7 proactive scan. |
| 2026-05-31 | implementing | Step 4.7 main feat landed | Codex `765eb10a feat(naming): migrate diagnostic fields to case condition terms` (127 files +1842/-1839) migrated all 7 long-form B2 fields + G8 diagnostic key format + G9 WhyNot LOCKED + G14 ecosystem (57 files) + G15 proof narrow scope + G16 Category B branch-id namespace. Claude Step 4.7 independent review identified P1 fix required: `cranch:` adapter over-rename typo at `adapters/problog/rule_ext.py:165` (parser rejected ALL valid `branch:N` input) + 5 categories of stale test fixtures (b{N}.add0:lt rule_add_condition assertions, WhyNot green/red constructor kwargs, Category B b0/b1 literals, `_certainty.py` b0.cN literals, problog test fixtures). Full `tests/` cohort pre-P1: 211 failed / 2171 passed. |
| 2026-05-31 | implementing | Step 4.7 P1 fix landed | Codex `9d544425 test(naming): complete B2 fixture migration` resolved all 6 P1 categories — `cranch:` → `branch:` adapter typo reverted (single-char fix); `b0.add0:*` fixtures → `c0.add0:*`; `b0.c*` certainty fixtures → `c0.c*`; WhyNot fixtures `green/red` → `passed/failed`; Category B fallback fixtures `b0/b1` → `c0/c1`; ProBlog/PyReason docs/test expectations synchronized. Claude Step 4.7 P1 re-review PASS: B2 priority cohort (28 files) 531 passed + 2 failed (both `test_pyreason_branch_bounds_carrier.py` pre-existing `SDKStore.ref` baseline); full `tests/` cohort delta −22 failures / +123 passes (211 → 189 failed; 2171 → 2294 passed). Zero NEW B2 regressions remaining. Adapter parser verified empirically: `_parse_profile_branch_target("branch:0")` returns 0 correctly. |
| 2026-05-31 | implemented | Scoped implementation landed | Implementation `765eb10a` (main feat) + `9d544425` (P1 fix) migrated all 7 long-form B2 fields + G8 diagnostic key cascade + G9 WhyNot LOCKED + G14 ecosystem (57 files) + G15 proof narrow scope + G16 Category B branch-id namespace across 127 files (+1842/-1839) + 14 additional P1 fix files. Claude Step 4.7 review + P1 re-review both PASS: 0 NEW B2 regressions remaining; 189 baseline failures all pre-existing per §10.6 (102 `'NoneType' object has no attribute 'proof'` inherits pre-B2 `engine_payload` AttributeError pattern; 68 `meta[confidence]` Uncertainty Phase 1 baseline; 66 `SDKStore.ref` pre-existing SDK shape; 44 `Rule.__init__() got 'where'` Q-NAMING-E migration leftover; etc.). 2-commit pattern actually used (main feat + P1 fix) — 3-commit pattern budget per Q-NAMING-E precedent was conservative. Blueprint status flipped to `implemented`; §7 acceptance 27/27 boxes checked; §10 Outcome §10.1-§10.9 filled. Sacred Q-PR1 5-path 0-diff vs `4c472b50` preserved; sacred master `562c74195df4...` unchanged; dirty baseline (4 M + 2 D + 2 untracked) preserved; branch remains unpushed pending user authorization. |

## Decision Notes

### 2026-05-31 — Initial scope freeze rationale

- **Red-risk scope per Q-NAMING §4.9 Phase 5**: Field/wire/key cascade with hard-cut at v0.2 per Sub-Q1 + §4.3.6 (no alias, no dual-emit, recorded payloads and diagnostic keys change in v0.2; acceptable because alpha). Class symbols already done in B1; B1/B2 boundary now reversed (B1 was class-only; B2 is field-only).
- **13 goal entries per §4.3.2 (4) + §4.3.4 (3 + 1 diagnostic key cascade) + §4.3.5 (1 WhyNot narrow target LOCKED at Step 4.2 P2-3) + Step 4.2 added G14 ecosystem + G15 proof narrow + G16 branch-id namespace**:
  - G1-G4 (§4.3.2): atom-cascade field-level — `atom_index → condition_index`; `atom_key → condition_key`; `_atom_index_from_key → _condition_index_from_key`; `materialized_atom_index → materialized_condition_index`
  - G5-G7 (§4.3.4 wire/persistence): `branch_index → case_index`; `runtime_branch_index → runtime_case_index`; `engine_payload → proof` (hard-cut, 0 hits in Q-PR1 sacred; **[NARROWED Step 4.2 P2-4]** scope = `EvidenceEnvelope.engine_payload`-style payload field ONLY; separate Souffle/ProbLog/candidate-evidence-tree `"proof"` schemas PRESERVED — see G15)
  - G8 (§4.3.4 diagnostic key format): `f"b{branch_index}.a{atom_index}:..."` → `f"c{case_index}.c{condition_index}:..."` across 10 enumerated emit sites + prefix-match consumers + variant-form sites (preflight 2.h 3-category split per Step 4.2 P3-1)
  - G9 (§4.3.5 WhyNot narrow target): **[LOCKED Step 4.2 P2-3]** `green → passed`, `red → failed`, `WhyNotRedRow → WhyNotFailedRow`, error code `WHY_NOT_DIAGNOSE_PASSED_FAILED_BINDING` (aligns with shipped Diagnose convention at `why_not_runtime.py:388`; rejected `holds/misses` and `accepted/rejected` as introducing new vocabulary not grounded in shipped code)
  - G14 (added Step 4.2 P2-2): Diagnostic key ecosystem migration — `AtomKeyView→ConditionKeyView`, `parse_atom_key→parse_condition_key`, `_ATOM_KEY_RE→_CONDITION_KEY_RE`, `pred_atom_key→pred_condition_key`, `ruleref_atom_key→ruleref_condition_key`; 8 export sites + 5 carrier field sites + 2 local parser site clusters; `step_key` PRESERVED per N19
  - G15 (added Step 4.2 P2-4): `proof` collision boundary — only `EvidenceEnvelope.engine_payload` and equivalent payload field renamed; Souffle provenance `"proof"` keys + candidate evidence tree role `"proof"` keys + ProbLog proof tree role keys PRESERVED
  - G16 (added Step 4.2 P3-1): Branch-id namespace decision per preflight 2.h Category B — `branch_id=f"b{idx}"` namespace identifier locked at preflight; N21 `branch_id` field name PRESERVED
- **B2 field-vs-substring-containment boundary** (§5.2 critical): rename ONLY the 7 explicit field symbols + 10 diagnostic key sites + WhyNot narrow target + G14 ecosystem. Substring-containment fields NOT in spec are PRESERVED per N18+N19: `added_atom` (B1 field), `atom_verdicts` (proofframe field), `atom_locator` (parameter), `literal_path` (B1 field), `support_artifact_*` (4 B1 helpers), `_row_support_artifacts` (B1 field), `atom_bounds` (PyReason-domain per §4.3.3 + N8), `step_key` (no atom suffix), `branch_id` (when not branch_index per N21 + preflight 2.h Category B).
- **No naming conflicts verified at Step 4.1 scan + Step 4.2 P2-4 refinement**: 6 of 7 new field names return 0 shipped occurrences. `proof` (G7) has 23 src + 5 tests files, but per Step 4.2 P2-4 narrow classification, multiple structured uses belong to separate provenance/evidence-tree schemas and are PRESERVED unchanged (Souffle `adapters/souffle/provenance.py:268` + tests JSON fixtures + `_candidate_evidence_tree_narrative.py:90+346+350` + `_candidate_evidence_tree_summary.py:26+27+125+128`). G7 rename scope is `EvidenceEnvelope.engine_payload`-style payload field only. **No symbol/class/function module-level definition collision.** Preflight 2.f classifies each `"proof"` use per spot check.
- **Critical positive Step 4.1 finding**: All 7 B2 field names return **0 hits across Q-PR1 sacred 5 paths** (`write_protocol.py`, `ledger.py`, `_builders.py`, `adapters/pyreason/`, `accept.py`). This contrasts with Q-NAMING-E which had PF-R1 G4 forcing Option D preserve (legacy `Rule.where` touched sacred adapter). B2's Red classification stems from wire/persistence scale + cascade coordination, NOT from Q-PR1 sacred trap. Slice cadence implications: single-feat-commit viable (per Q-NAMING-B1 precedent); 3-commit pattern only if Step 4.7 review surfaces P1 fix (Q-NAMING-E lesson).
- **Cross-rename coordination requirement**: `branch_index` and `atom_index` co-occur in 10 diagnostic key f-string sites + many function signatures + dataclass field combinations. Implementation must rename both simultaneously + rewrite f-string template in same edit (avoid intermediate broken states). Preflight 2.e enumerates co-occurrence cluster.
- **Q-NAMING-E + B1 precedent reuse**: PF-R1 Q-PR1 detection (verify each field name against sacred paths before lock), Sub-Q3 single-slice cadence default, AD/C/E/B1 inherited preservation (N12-N19 explicit), N-7 proactive examples/tutorials extension during implementation (B1 precedent), 3-commit pattern budget for Red slice (E precedent).
- **Hard-cut per §4.8.1 + §4.3.6**: no aliases, no dual-emit, old field names + old diagnostic key format removed in same commit as new format introduced. Alpha release; no historical user protection burden. Recorded payloads change in v0.2 acceptable.
- **Sub-Q3 single-slice cadence default**: all 11 field renames + diagnostic key format + WhyNot wording likely land in one feat commit unless Step 4.6.5 pre-impl grep surfaces unexpected consumer cluster or Step 4.7 review surfaces P1 fix.
- **Persistence schema digest cross-ref**: §4.5 AD pre-lock at `4874a76e` cleared no blocker on `branch_index → case_index` impact on persisted digests. Preflight 2.j re-verifies no blocker has emerged since.

### 2026-05-31 — Step 4.3 preflight verified findings record (PF-v1 through PF-v10 + PF-s1)

Per CADENCE verified-findings convention, the following Step 4.3 preflight findings (artifact at `workflow/audit/active/2026-05-31_q-naming-b2-preflight.md` @ commit `5b40fa2e`) require no blueprint amendment but are recorded as Decision Notes for closure §10 cross-reference:

- **PF-v1 (Q-PR1 sacred 5-path immunity for 7 B2 long-form field names)**: All 7 fields {`atom_index`, `atom_key`, `_atom_index_from_key`, `materialized_atom_index`, `branch_index`, `runtime_branch_index`, `engine_payload`} return 0 hits across Q-PR1 sacred 5 paths. **No PF-R1-style Q-PR1 blocker for B2 long-form field renames** (positive finding inherited from Q-NAMING-B1 precedent; contrast Q-NAMING-E which had PF-R1 force-Option-D Q-PR1 trap on legacy `Rule.where`).

- **PF-v2 (Q-PR1 sacred diagnostic-prefix variants confirmed preserved per N20)**: Step 4.3 grep across Q-PR1 sacred 5 paths returned `branch_idx\b` = 27 hits (local iter variable + parameter in `pyreason/where_compile.py` + `pyreason/rule_ext.py`); `atom_idx\b` = 10 hits (similar pattern); `_b{branch_idx}` = 1 site at `pyreason/where_compile.py:56` rule_name suffix. Total **38 hits all PRESERVED per N20 + Q-PR1 0-diff** (refined from Step 4.2 P2-1 estimate of ~32 hits). These short-form `_idx` identifiers + the rule_name suffix pattern are PyReason adapter internal naming, NOT in B2 scope per N7 layer authority.

- **PF-v3 (zero naming conflicts in shipped surfaces)**: All 6 of 7 new field names (`condition_index`, `condition_key`, `_condition_index_from_key`, `materialized_condition_index`, `case_index`, `runtime_case_index`) return 0 hits in `src/factgraph` + `tests/` + `src/service` + `src/domains` + `examples/` + `tutorials/`. `condition_index` + `case_index` have 1 hit each in `docs/references/working/change-requests-2026-05-27/change-requests.md` — but this is explicit §4.8.5 historical carve-out + part of dirty baseline (untracked) per §4.8.8; not a runtime/test conflict. `proof` classified separately per PF-v9.

- **PF-v4 (B1 inherited surfaces preserved per N18/N19/N20/N21)**: 12 B1 class symbols all present at expected sites (ProofReceipt 40 files, FactOverlay 21, ReplaceFact 9, RemoveFact 7, ConditionDescriptor 5, DiagnoseConditionLocator 4, WhyNotConditionLocator 4, ProofFrameConditionVerdict 9, AddedCondition 8, AddedConditionKind 2, ConditionPath 9, ConditionPathKind 2). 4 `support_artifact_*` + `compute_support_digest` B1 helpers preserved (`def` declarations at `_support.py:229/265/302/311`). 8 B1/N19 preserved fields all present: `added_atom` (24 hits), `atom_verdicts` (44), `atom_locator` (15), `literal_path` (20), `_row_support_artifacts` (9), `step_key` (44), `branch_id` (41), `atom_bounds` (19).

- **PF-v5 (AD/C/E inherited surfaces preserved)**: Grep confirms preservation: FrozenAssertionSet (7 files), save_workspace (9), commit_assertions (4), assertion_views (6), EmitSpec (4), Case (13), `Inference.when` (2). Q-NAMING-B2 scope does NOT touch any AD/C/E contract surface. N12/N13/N14/N15/N16/N17 all intact through B2 scope.

- **PF-v6 (10 diagnostic key emit sites confirmed at expected locations)**: per blueprint §5.1 G8 enumeration — `core/rules/ruleref_substrate.py:149`, `core/store/_support.py:212+226`, `core/store/_support_capture.py:44` (prefix-match consumer), `authoring/rule_compile.py:203`, `sdk/store.py:3639` (Category B), `application/rule_literal_replace_runtime.py:322`, `application/rule_add_condition_runtime.py:301` (variant `add{action_index}`), `application/rule_disable_runtime.py:213`, `application/protocol/rule_expr_lowering.py:723` (Category B). All canonical emit sites confirmed at expected lines.

- **PF-v7 (Prefix-match consumer enumeration extended beyond blueprint §5.5 step 2)**: Step 4.3 grep `startswith.*"b"` found 6 sites total: `_support_capture.py:44` (canonical per §5.5), `diagnose_runtime.py:787` (local parser per §5.5.1 ecosystem), `derivation_check_runtime.py:406` (sibling local parser), plus 3 different-scheme sites NOT in B2 scope: `pyreason/rule_ext.py:188` `startswith("branch:")` + `pyreason/rule_ext.py:199` `startswith("body_atom:")` + `problog/rule_ext.py:165` `startswith("branch:")` (PyReason/ProbLog adapter target string patterns, different from `b{...}` key prefix). Local parsers already covered under G14 ecosystem per §5.5.1.

- **PF-v8 (WhyNot narrow target sites confirmed per §5.6 LOCKED table)**: All 8 enumerated sites present at expected lines: `derivation_why_not.py:131, 133, 209, 284, 312, 313, 318, 319` + `why_not_runtime.py:391`. **`class WhyNotRowDiagnostic` at line 209 content verified**: uses `status: WhyNotRowStatus` literal + `atom_locator: WhyNotConditionLocator | None` (already renamed by B1) — **NO direct `green`/`red` field references in class body**. Therefore `WhyNotRowDiagnostic` class name PRESERVED (not in G9 rename scope); only `green`/`red` field tokens in `WhyNotUniverseResult` (lines 312-319) + `WhyNotRedRow→WhyNotFailedRow` (line 284) + error code (line 391) update per G9 LOCKED targets.

- **PF-v9 (`proof` collision classification per Step 4.2 P2-4 NARROWED scope)**: All 5 schemas re-read and confirmed PRESERVED per §5.3 + §8 preflight 2.f table: `adapters/souffle/provenance.py:268` Souffle JSON key (Souffle provenance schema), `_candidate_evidence_tree_summary.py:26+27+125+128` role mapping + count key, `_candidate_evidence_tree_narrative.py:90+346+350` role tuple + dict access, 2 test JSON fixtures (mirror Souffle schema). G7 narrow scope = `EvidenceEnvelope.engine_payload` only. **No module-level `def proof` / `class Proof` / top-level field `proof: Type` collision**.

- **PF-v10 (Full B2 deduped footprint refined to 132 files)**: 53 src/factgraph + 42 tests + 24 spillover + 57 ecosystem overlap deduped union = **132 files**. Per-cluster heaviest: `branch_index` 76 files (30+30+16), `ecosystem` 57 files (PF-R1 expansion), `atom_index` 51 files (23+18+10). **Bigger than B1 (114 files)** + larger ecosystem cascade — confirms Red classification and 3-commit pattern budget per Q-NAMING-E precedent (PF-R1 cross-ref).

- **PF-s1 (G14 ecosystem rename ordering, deferred to Step 4.7 §10)**: Per-cluster implementation ordering for largest clusters: (1) Carrier classes first — `PredWitness.pred_atom_key → pred_condition_key` + `RuleRefEdge.ruleref_atom_key → ruleref_condition_key` at `_support.py:37, 67` + `ruleref_types.py:31`; (2) Propagate through `_candidate_evidence_tree.py` (21 sites) + `_trace.py` (22 sites) + `rule_ir.py` (5 sites); (3) Update `walker/keys.py` regex pattern + class + parser; (4) Update 8 export sites in `walker/__init__.py` + `application/__init__.py`; (5) Update local parsers in `diagnose_runtime.py:764-806` + `derivation_check_runtime.py:382-420`; (6) Update B1-preserved `support_artifact_to_dict`/`_from_dict` JSON dict-key emission/parsing per PF-r2 persistence cascade; (7) Regenerate test JSON fixtures + canonical golden snapshots.

### Cross-flip role assignment (per user 2026-05-31 推进 directive + AD/C/E/B1 precedent)

User directive "推进" 2026-05-31 implicitly preserves AD/C/E/B1 cross-flip pattern unless explicitly changed:

- Step 4.1 blueprint draft → Claude (this commit)
- Step 4.2 review → Codex
- Step 4.3 preflight drafter → Claude (per AD/C/E/B1 precedent)
- Step 4.4 preflight amendment → Claude (per AD/C/E/B1 precedent)
- Step 4.5 self-check → Claude (doc-only)
- Step 4.6 scoped anchor → Claude (per AD/C/E/B1 precedent)
- Step 4.6.5 pre-impl grep → Codex (承接 implementation prep, AD/C/E/B1 precedent — MANDATORY for Red slice)
- Step 4.7 implementation → Codex (per user directive)
- Step 4.7 review → Claude (per user directive — CRITICAL independent test re-run for Red slice per Q-NAMING-E lesson)
- Step 4.8 closure → Codex or Claude (per CADENCE no strict assignment)
- Step 4.9 archive → Codex or Claude (per CADENCE no strict assignment)

If user wants to flip any role assignment, they may signal at any handoff point.
