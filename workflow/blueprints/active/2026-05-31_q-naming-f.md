# Q-NAMING-F Blueprint: Engine config / semantics rename (ProbLog/PyReason Semantics→Config + SemanticsProfile→EngineProfile + semantics=→config= + branch_*→case_*)

- Status: draft
- Created: 2026-05-31
- Last Updated: 2026-05-31
- Related Modules:
  - `src/factgraph/core/semantics/profile.py` (`SemanticsProfile` class definition at line 26 — primary host; `core/semantics/` is a package, not a module)
  - `src/factgraph/sdk/semantics.py` (`ProbLogSemantics`, `PyReasonSemantics` wrapper class definitions — primary host)
  - `src/factgraph/application/protocol/` (`SemanticsProfile` field consumers + semantics= keyword sites)
  - `src/factgraph/adapters/problog/` + `src/factgraph/adapters/pyreason/` (consumers — Q-PR1 sacred for pyreason, NOT sacred for problog)
  - `src/factgraph/sdk/` (`inspect_semantics` helper + facade exports + digest field)
- Related Docs:
  - [`workflow/design/decisions/active/2026-05-31_q1-naming-api-polish-decision.md`](../../design/decisions/active/2026-05-31_q1-naming-api-polish-decision.md) — Q-NAMING decision §4.7 (Batch F) + §4.8 invariants (adopted at `f1a017ec`)
  - [`workflow/audit/active/2026-05-31_naming-polish-feasibility.md`](../../audit/active/2026-05-31_naming-polish-feasibility.md) — feasibility audit §7 (Batch F) at `10de33b9`
  - [`workflow/blueprints/archive/2026-05-31_q-naming-ad.md`](../archive/2026-05-31_q-naming-ad.md) — Q-NAMING-AD Phase 1 precedent
  - [`workflow/blueprints/archive/2026-05-31_q-naming-c.md`](../archive/2026-05-31_q-naming-c.md) — Q-NAMING-C Phase 2 precedent
  - [`workflow/blueprints/archive/2026-05-31_q-naming-e.md`](../archive/2026-05-31_q-naming-e.md) — Q-NAMING-E Phase 3 Red-slice precedent: PF-R1 Q-PR1 trap + Option D preserve pattern
  - [`workflow/blueprints/archive/2026-05-31_q-naming-b1.md`](../archive/2026-05-31_q-naming-b1.md) — Q-NAMING-B1 Phase 4 precedent
  - [`workflow/blueprints/archive/2026-05-31_q-naming-b2.md`](../archive/2026-05-31_q-naming-b2.md) — Q-NAMING-B2 Phase 5 precedent: G14 ecosystem expansion + persistence JSON dict-key cascade + Step 4.7 P1 fix patterns
  - [`workflow/foundations/architecture_principles.md`](../../foundations/architecture_principles.md) §2.1 layer authority
- Audit Log:
  - [2026-05-31_q-naming-f.audit.md](./2026-05-31_q-naming-f.audit.md)

## 1. Problem

Q-NAMING decision §4.7 locks the engine config / semantics rename for v0.2.0 alpha. Per Q-NAMING §4.9, this is **Phase 6 (Q-NAMING-F)**: final sub-slice of the 6-phase routemap, stop-gated after B2. User entered Phase 6 via "推进" 2026-05-31 after Q-NAMING-B2 archive at `0b4c8aad`.

Q-NAMING-F renames the engine-facing config vocabulary across SDK + application protocol + adapter consumption + persistence digests. Per Sub-Q1 hard-cut, this includes wire/persistence/digest format changes — acceptable because alpha (no historical consumers).

**Risk classification**: per §4.9 Phase 6 + initial Step 4.1 grep findings:

- Footprint deduped at Step 4.1: **~63 unique files** (58 src/factgraph + tests + src/service + src/domains + 5 examples/tutorials/docs) — significantly smaller than Q-NAMING-B2 (132 files) due to scope concentration in semantics modules
- Heaviest single symbol: `SemanticsProfile` 51 unique files
- Per-symbol footprint: SemanticsProfile (51 files), ProbLogSemantics (28), PyReasonSemantics (29), branch_probabilities (18), branch_bounds (14), inspect_semantics (10), semantics_digest (11)
- Wire/persistence change: `semantics_digest` field renamed to match `config` naming per §4.7.7; adapter-facing payload field names change
- **CRITICAL**: Step 4.1 grep surfaces **PF-R1-style Q-PR1 sacred trap** on `SemanticsProfile` (29 hits in `adapters/pyreason/engine_eval.py` — same pattern as Q-NAMING-E `Rule.where` PF-R1)

**[POTENTIAL PF-R1 trap per Step 4.1 — extended Step 4.2 P2-1 — preflight 2.a verifies + locks resolution]**: Q-PR1 sacred TWO files in `adapters/pyreason/` actively use `SemanticsProfile`:

**`adapters/pyreason/engine_eval.py`** (15 sites):
- Line 9: `from factgraph.core.semantics import SemanticsProfile`
- Line 54, 287, 384: parameter type annotations `semantics_profile: SemanticsProfile | None`
- Lines 293, 387: `isinstance(semantics_profile, SemanticsProfile)` checks
- Lines 295, 299, 389, 393: error message strings (`"semantics_profile must be SemanticsProfile or None"`, `"PyReason consumption expected SemanticsProfile.engine='pyreason'"`)
- Lines 310, 315, 319, 339, 372: carrier identifier strings (`"SemanticsProfile.temporal_projection.fixed_timesteps"`, `f"SemanticsProfile.temporal_projection.{mode}"`, `"SemanticsProfile.iteration_count"`)

**`adapters/pyreason/rule_ext.py`** (added Step 4.2 P2-1; 7 additional sites):
- Line 7: `from factgraph.core.semantics import SemanticsProfile`
- Lines 78, 142: parameter type annotations `semantics_profile: SemanticsProfile | None`
- Line 90: carrier identifier string `"SemanticsProfile.rule_projection.pyreason and PyReasonRuleExt"`
- Line 146: `isinstance(semantics_profile, SemanticsProfile)` check
- Lines 148, 152: error message strings (`"semantics_profile must be SemanticsProfile or None"`, `"PyReason consumption expected SemanticsProfile.engine='pyreason'"`)
- Line 169: carrier identifier string `f"SemanticsProfile.rule_projection.pyreason[{idx}]"`

**Total Q-PR1 sacred SemanticsProfile hits: ~29 total (15 in engine_eval.py + 7 in rule_ext.py + remaining via line-count vs distinct-site enumeration)** — G1 Option D preserve is even stronger than initial draft because BOTH Q-PR1 sacred adapter files require `SemanticsProfile` class symbol preservation for imports + type checks + carrier strings.

Per Q-PR1 0-diff invariant, `adapters/pyreason/` is sacred and CANNOT be modified. Per Q-NAMING-E precedent (PF-R1 G4 Option D preserve), this likely forces a similar locked option:

- **Option A — Rename SemanticsProfile and all consumers**: BLOCKED (sacred adapter cannot update import / type annotations / isinstance / error strings / carrier identifiers)
- **Option B — Rename SemanticsProfile + add alias `SemanticsProfile = EngineProfile`**: BLOCKED by §4.8.1 hard-cut no-alias policy
- **Option C — Carve out only Q-PR1-touched consumers**: BLOCKED because adapter uses the EXACT class name in `isinstance(...)` check (would need adapter modification)
- **Option D — Preserve `SemanticsProfile` entirely (don't rename)**: viable per Q-NAMING-E PF-R1 precedent. F §4.7.1 SemanticsProfile rename DEFERRED indefinitely; other §4.7 renames (ProbLogSemantics/PyReasonSemantics class names, semantics= keyword, branch_*→case_*, inspect_semantics→preview_config, semantics_digest→config_digest) proceed.

**Step 4.3 preflight LOCKS final option** — Option D is the default expectation per Q-NAMING-E precedent. ProbLogSemantics + PyReasonSemantics may or may not have similar Q-PR1 traps (preflight verifies); they are SDK wrappers so likely safer than core SemanticsProfile.

Q-PR1 sacred 5-path Step 4.1 scan for other F symbols:
- `ProbLogSemantics`: 0 Q-PR1 hits ✓
- `PyReasonSemantics`: 0 Q-PR1 hits ✓
- `branch_probabilities` / `branch_bounds`: 0 Q-PR1 hits ✓
- `inspect_semantics`: 0 Q-PR1 hits ✓
- `semantics_digest`: 0 Q-PR1 hits ✓
- `head_bound`: **21 Q-PR1 hits** — but per §4.7.5 + §4.8.3 head_bound is CLEANUP not 1:1 rename; adapter projection semantics PRESERVED (not renamed; preflight 2.a confirms no scope intersection)
- `atom_bounds`: **EXCLUDED** per §4.7.6 + B2 N8 (PyReason-domain syntax preserved)

Per Q-NAMING §4.8.1 hard-cut: no aliases, no dual-emit, old names removed in same commit as new names introduced (subject to PF-R1 Option D carve-out per preflight). Per N7 layer authority: lower-level core / adapter internal names preserved where they don't appear on the renamed public surface.

The Q-NAMING-AD + C + E + B1 + B2 precedents (PF-R1 Q-PR1 detection + Option D preserve + hard-cut + per-symbol classification + AD/C/E/B1/B2 inherited preservation + Step 4.7 P1 fix budget for Red slices) inform this slice's discipline.

## 2. Goals

**Per Q-NAMING §4.7.1 — class/wrapper renames (3 classes)**:

- G1: **[LOCKED Step 4.4 PF-R1 Option D PRESERVE]** — preserve `SemanticsProfile` class name entirely. **Rationale**: Step 4.3 preflight `ddbeec94` confirmed Q-PR1 sacred 29 distinct SemanticsProfile hits across TWO sacred files (21 sites in `adapters/pyreason/engine_eval.py` + 8 sites in `adapters/pyreason/rule_ext.py`). Per Q-PR1 0-diff invariant + §4.8.1 no-alias rule + isinstance-check blocking carve-out, **Option D preserve is the only viable resolution** (same structural pattern as Q-NAMING-E PF-R1 G4 `Rule.where` precedent). Original rename target `SemanticsProfile → EngineProfile` at [`src/factgraph/core/semantics/profile.py:26`](../../../src/factgraph/core/semantics/profile.py) DEFERRED INDEFINITELY. G1 implementation: **NO migration code change**. Original Step 4.1 footprint 153 hits / 51 unique files reduces to 0 files for migration purposes; all 51 files retain `SemanticsProfile` references unchanged. **Export sites under Option D ALL PRESERVED unchanged**: [`core/semantics/__init__.py:3`](../../../src/factgraph/core/semantics/__init__.py) `from .profile import SemanticsProfile, inspect_semantics_profile` + line 5 `__all__ = ["SemanticsProfile", "inspect_semantics_profile"]`; [`sdk/__init__.py:51`](../../../src/factgraph/sdk/__init__.py) `from factgraph.core.semantics import SemanticsProfile` + line 97 `__all__` entry `"SemanticsProfile"`. All other 7 F renames (G2-G8) proceed independently. Public surface inconsistency between preserved `SemanticsProfile` and renamed `ProbLogConfig`/`PyReasonConfig`/`EngineProfile`-less ecosystem acknowledged but acceptable per Q-NAMING-E precedent (legacy DSL `Rule.where` coexists with public application `Rule.when`).
- G2: Rename `ProbLogSemantics` → `ProbLogConfig` SDK wrapper class at [`src/factgraph/sdk/semantics.py`](../../../src/factgraph/sdk/semantics.py). Step 4.1 footprint: 30 hits / 28 unique files. Step 4.1 grep: 0 Q-PR1 sacred hits — proceed.
- G3: Rename `PyReasonSemantics` → `PyReasonConfig` SDK wrapper class at [`src/factgraph/sdk/semantics.py`](../../../src/factgraph/sdk/semantics.py). Step 4.1 footprint: 55 hits / 29 unique files. Step 4.1 grep: 0 Q-PR1 sacred hits — proceed.

**Per Q-NAMING §4.7.2 — keyword rename**:

- G4: Rename `semantics=` keyword argument → `config=` across SDK + service + application APIs. Step 4.1 footprint: 32 src + 12 tests = 44 hits. Disambiguation from existing `config=` keyword in unrelated code is docs/test responsibility within F (audit §7 caveat). Preflight 2.f maps disambiguation per-site.

**Per Q-NAMING §4.7.3 — engine payload field renames (cross-ref §4.3.4 B2)**:

- G5: Rename `branch_probabilities` → `case_probabilities` payload field. Step 4.1 footprint: 30 src + 23 tests / 18 unique files. Hard-cut per §4.7.7 wire/persistence (alpha, no historical consumers; cross-references B2's branch→case cascade locked at `0b4c8aad`).
- G6: Rename `branch_bounds` → `case_bounds` payload field. Step 4.1 footprint: 26 src + 13 tests / 14 unique files. Hard-cut per §4.7.7. Cross-refs B2.

**Per Q-NAMING §4.7.4 — SDK helper rename**:

- G7: Rename `fg.eval.inspect_semantics(...)` namespaced manager method → `fg.eval.preview_config(...)`. Step 4.1 footprint: 7 src + 2 tests / 10 unique files. **[REWORDED Step 4.2 P3-1]**: shipped reality is `fg.eval.inspect_semantics` (namespaced manager at [`sdk/store.py:1302`](../../../src/factgraph/sdk/store.py)) + private SDK helper `_inspect_semantics` at [`sdk/store.py:2263`](../../../src/factgraph/sdk/store.py) + error message `"inspect_semantics(profile) expects SemanticsProfile or SDK public semantics"` at line 2272. **No flat top-level `fg.inspect_semantics` shell exists post Q-NAMING-C** (Q-NAMING-C archived at `23389a2b` deleted 20 flat shells). G7 scope: rename namespaced manager method + private helper + docstring/error message strings + docs/tests references; preserve `inspect_semantics_profile(...)` core helper function (at `core/semantics/profile.py` per Option D if locked — separate function from SDK manager method).

**Per Q-NAMING §4.7.7 — wire/persistence/digest compatibility**:

- G8: Rename `semantics_digest` stored field → `config_digest` to match `config` naming. **[REFINED Step 4.4 PF-r1 LOCK: 11 deduped files]** (vs Step 4.1 estimate 10). Hard-cut per Sub-Q1; persisted digest field name changes. Acceptable because alpha. **[EXPANDED Step 4.2 P2-3 — digest cluster decision table + Step 4.4 PF-r1 11-file enumeration]**: G8 scope is broader than single `semantics_digest` field. Cluster covers 11 deduped files: `application/protocol/evaluate_result.py` (DTO field + helper function + dict keys + validation), `sdk/store.py` (import + helper call + dict keys + kwargs), `src/service/runtime_v1.py` (service serialization), `audit/docs/02_evidence_graph.md` (doc), `sdk/docs/03_rules_and_inferences.en.md` (doc), `src/service/docs/03_runtime_queries_policy.md` (service doc), `tests/test_problog_semantics_profile_migration.py`, `tests/sdk/test_rule_expr_evaluate.py`, `tests/application/protocol/test_evaluate_result_dtos.py`, `tests/application/protocol/test_evaluate_result_digests.py`, `docs/official/kernel/quickstart/evidence.md` (quickstart doc). Per-site detail:
  - **`semantics_digest` DTO field** at [`application/protocol/evaluate_result.py:173`](../../../src/factgraph/application/protocol/evaluate_result.py) (EvaluateResult dataclass) + line 73 `__all__` + line 207-208 validation + line 349/358-359/373/474/488 helper functions → RENAME to `config_digest`
  - **`semantics_digest_for(profile)` helper function** at [`application/protocol/evaluate_result.py:511`](../../../src/factgraph/application/protocol/evaluate_result.py) + `__all__` at line 1416 + import at `sdk/store.py:53` + call site at `sdk/store.py:2681` → RENAME to `config_digest_for`
  - **`evaluate_semantics_digest` / `explain_semantics_digest` dict keys** at [`sdk/store.py:2531, 2532`](../../../src/factgraph/sdk/store.py) + [`application/protocol/evaluate_result.py:1247, 1248`](../../../src/factgraph/application/protocol/evaluate_result.py) → RENAME to `evaluate_config_digest` / `explain_config_digest` (per B1 P2-1 export naming convention extended)
  - **Service serialization key `"semantics_digest"`** at [`src/service/runtime_v1.py:2451`](../../../src/service/runtime_v1.py) → RENAME to `"config_digest"` (wire/persistence change per §4.7.7)
  - **`sdk/store.py:2529`** `"semantics_digest": result.semantics_digest,` (sibling JSON dict key) → RENAME to `"config_digest"` plus field reference rename to `result.config_digest`
  - **`sdk/store.py:2687, 2718, 2731`** `semantics_digest=semantics_digest` kwargs in callsite passing → RENAME to `config_digest=config_digest`
  - **Doc references** at `audit/docs/02_evidence_graph.md:100` + `sdk/docs/03_rules_and_inferences.en.md:561` → migrate text
  - **Persistence digest hard-cut per §4.7.7** acknowledged: cross-references B2 PF-r2 persistence JSON dict-key cascade pattern (B1-preserved helper function names propagate field renames into persisted dict keys; same hard-cut category as B2's `pred_atom_key → pred_condition_key`).

**Cross-cutting per §4.8 invariants**:

- G9: Hard-cut per §4.8.1 — no aliases, no dual-emit, old names removed in same commit as new names introduced (subject to G1 Option D carve-out per PF-R1).
- G10: Update affected module docs (`src/factgraph/*/docs/`) + current SDK quickstarts / examples (`docs/`) + tests + N-7-style proactive scan for current tutorials in same slice per CADENCE Stage 4.8.
- G11: Q-PR1 sacred 5 paths 0-diff vs `4c472b50` preserved through all commits; sacred master `562c74195df4...` unchanged; dirty baseline 8 entries preserved.

## 3. Non-goals

- N1: Q-NAMING-AD scope re-litigation (all AD work archived; F inherits AD state untouched).
- N2: Q-NAMING-C scope re-litigation (all C work archived; inherits unchanged).
- N3: Q-NAMING-E scope re-litigation (all E work archived; Branch→Case, Rule.when, Inference.when, EmitSpec, legacy DSL Rule.where preserved per PF-R1 Option D).
- N4: Q-NAMING-B1 scope re-litigation (all B1 class symbol renames archived; 12 new B1 class symbols preserved per N18 inherited).
- N5: Q-NAMING-B2 scope re-litigation (all B2 field/wire/key cascade archived at `0b4c8aad`; 7 long-form field renames + G8 diagnostic key format + G9 WhyNot LOCKED + G14 ecosystem + G15 proof narrow + G16 Category B branch-id namespace all preserved per N22).
- N6: PDF Module 1 Schema renames per §4.1 REJECT.
- N7: Lower-level core / authoring / adapter internal symbol name renames where they do NOT appear on the renamed public surface. Per architecture_principles.md §2.1 layer authority + Q-NAMING N7 precedent inherited from AD/C/E/B1/B2.
- N8: `atom_bounds` PyReason-domain syntax per §4.7.6 + §4.3.3 (B2 N8 inherited). The `atom_bounds` field on `PyReasonSemantics → PyReasonConfig` wrapper at `sdk/semantics.py:195` and SDK→adapter translation at `sdk/store.py:4055+` preserve `atom_*` naming because they reflect PyReason's own native terminology, not FactGraph rule-DSL terminology. Cross-reference: Q-NAMING-B2 N8 + §5.2 boundary discipline.
- N9: `head_bound` 1:1 rename per §4.7.5. Per §4.8.3 + Step 4.1 grep: head_bound has 38 hits total + 21 hits in Q-PR1 sacred `adapters/pyreason/{where_compile,rule_ext}.py` (plus `branch_head_bounds` related field). Per §4.7.5 head_bound is CLEANUP not rename; adapter projection semantics PRESERVED. **[TIGHTENED Step 4.2 P3-2]**: **F does NOT touch `head_bound` in any way during implementation** — no code change, no docs cleanup, no related field migration. The §4.7.5 + §4.8.3 "CLEANUP" language refers to potential future Q-NAMING followup decision OUTSIDE F scope. F implementation discipline: scope grep must verify 0 `head_bound` modifications; preflight 2.a confirms 21 Q-PR1 sacred hits remain 0-diff vs `4c472b50`. F implementers MUST NOT attempt any "cleanup" code change inside sacred adapter paths even if pattern looks safe.
- N10: Historical workflow / archive material per §4.8.5 carve-out (`docs/references/working/**`, `workflow/heritage/`, `workflow/blueprints/archive/`, `workflow/audit/archive/`, `examples/archive/`). Only current SDK docs, quickstarts, current examples, and active tests migrate.
- N11: Q-PR1 sacred 5 paths — must remain 0-diff vs `4c472b50` (`core/evidence/write_protocol.py`, `core/store/ledger.py`, `core/store/_builders.py`, `adapters/pyreason/`, `core/derivation/accept.py`).
- N12: Adding new public methods, namespaced managers, or restructuring class hierarchies. Q-NAMING-F is a **pure rename slice** — class symbols + field names + keyword names change but class fields + methods + bodies + semantics unchanged.
- N13: Reverting or modifying Q-NAMING-AD inherited surfaces (per B1/B2 N12).
- N14: Reverting or modifying Q-NAMING-C inherited surfaces (per B1/B2 N13).
- N15: Reverting or modifying Q-NAMING-E inherited surfaces (per B1/B2 N14).
- N16: Resurrecting `fg.what_if` public access (Q-NAMING-C N15 inherited).
- N17: Modifying `SDKStore.commit_assertions` (Q-NAMING-C N16 inherited; G7a LOCKED preserved).
- N18: Renaming `Query.where` / `Query.where_ir` (Q-NAMING-E N17 inherited; Query carve-out preserved).
- N19: Reverting or modifying Q-NAMING-B1 inherited surfaces — 12 B1 class symbols + 4 `support_artifact_*` helpers + 8 B1/N19 preserved fields preserved.
- N20: Q-PR1 sacred short-form variables PRESERVE per B2 N20 (`branch_idx`/`atom_idx` short-form locals + `_b{branch_idx}` suffix in pyreason adapter ~38 hits).
- N21: `branch_id` field name preserved per B2 N21 (only template string `b{idx}→c{idx}` shipped in B2; field name itself unchanged).
- **N22 (added F)**: Reverting or modifying Q-NAMING-B2 inherited surfaces — 7 long-form field renames (`condition_index`/`condition_key`/etc. + `case_index`/`runtime_case_index`/`proof`) + G8 diagnostic key format `c{case_index}.c{condition_index}:...` + G9 WhyNot LOCKED (`passed`/`failed`/`WhyNotFailedRow`/`WHY_NOT_DIAGNOSE_PASSED_FAILED_BINDING`) + G14 ecosystem (`ConditionKeyView`/`parse_condition_key`/`pred_condition_key`/`ruleref_condition_key`) + G15 PRESERVED `proof` schemas (Souffle/ProbLog/candidate-evidence-tree) + G16 Category B `f"c{idx}"` template all preserved untouched in F.
- N23: F §4.7.3 `branch_probabilities → case_probabilities` + `branch_bounds → case_bounds` cross-references B2's branch→case cascade — names align with B2's `case_index` semantic supremacy. NO conflict with N22 because B2 did NOT rename `branch_probabilities`/`branch_bounds` (deferred to F per §4.7.3 scope).
- **N24 (added Step 4.2 P2-4)**: G4 `semantics= → config=` keyword rename has narrow scope = **public-facing semantics keyword arg ONLY** (SDK + service + application call-site `semantics=` that flows into `SemanticsProfile`/`EngineProfile` carrier). The shipped code already uses `config=` keyword arg INTERNALLY for adapter `PyReasonRunConfig` plumbing — these are OUT of G4 scope per Q-PR1 0-diff + N7 layer authority:
  - **`adapters/pyreason/runner.py:412`** `config=config` (calling pyreason runner with `PyReasonRunConfig` instance)
  - **`adapters/pyreason/engine_eval.py:97`** `config=config` (similar adapter-internal plumbing)
  - **`PyReasonRunConfig` class** at `adapters/pyreason/runner.py:29` used as type at lines 46/345/351; imported at `engine_eval.py:11`; constructor calls at `engine_eval.py:135/154`; resolved by `resolve_pyreason_run_config(engine_options: dict[str, Any] | None) -> PyReasonRunConfig` at `engine_eval.py:133`
  - **G4 implementation discipline**: preflight 2.e enumerates per-call-site distinguishing "public semantics/config keyword" (RENAME target) vs "adapter-internal PyReasonRunConfig plumbing keyword" (PRESERVE per N24 + Q-PR1). Per-site classification table mandatory before Step 4.7.
  - **G4 acceptance check**: Q-PR1 sacred 5 paths `adapters/pyreason/runner.py + engine_eval.py + rule_ext.py + where_compile.py + ...` 0-diff vs `4c472b50` after G4 keyword rename; no adapter modifications.

## 4. Current Context

- **7 F primary rename targets — Step 4.1 grep**:
  - `SemanticsProfile`: 153 hits / 51 unique files (heaviest in F)
  - `ProbLogSemantics`: 30 hits / 28 files
  - `PyReasonSemantics`: 55 hits / 29 files
  - `semantics=` keyword: 32 src + 12 tests = 44 hits
  - `branch_probabilities`: 30 src + 23 tests / 18 files
  - `branch_bounds`: 26 src + 13 tests / 14 files
  - `inspect_semantics`: 7 src + 2 tests / 10 files
  - `semantics_digest`: 11 unique files

- **Deduped F footprint** (Step 4.1): 58 src/factgraph + tests + src/service + src/domains files + 5 examples/tutorials/docs = ~63 unique files. **Significantly smaller than B2 (132 files)** due to scope concentration in semantics-related modules.

- **No naming conflicts for 7 new F names** (Step 4.1 grep): all 0 hits in `src/factgraph` + `tests/`:
  - `ProbLogConfig` (G2): 0
  - `PyReasonConfig` (G3): 0
  - `EngineProfile` (G1): 0
  - `case_probabilities` (G5): 0
  - `case_bounds` (G6): 0
  - `preview_config` (G7): 0
  - `config_digest` (G8): 0

- **Q-PR1 sacred 5-path Step 4.1 scan** (per §1 detailed PF-R1 analysis):
  - `SemanticsProfile`: **~22 distinct hits in Q-PR1 sacred across TWO files per Step 4.2 P2-1** — `adapters/pyreason/engine_eval.py` (15 sites: line 9 import + lines 54/287/384 type annotations + lines 293/387 isinstance + lines 295/299/389/393 error messages + lines 310/315/319/339/372 carrier strings) + `adapters/pyreason/rule_ext.py` (7 sites: line 7 import + lines 78/142 type annotations + line 146 isinstance + lines 148/152 error messages + lines 90/169 carrier strings). **Likely PF-R1 trap** forcing G1 Option D preserve.
  - `ProbLogSemantics`: 0 Q-PR1 hits ✓
  - `PyReasonSemantics`: 0 Q-PR1 hits ✓
  - `branch_probabilities` / `branch_bounds`: 0 Q-PR1 hits ✓
  - `inspect_semantics`: 0 Q-PR1 hits ✓
  - `semantics_digest`: 0 Q-PR1 hits ✓
  - `head_bound`: 21 Q-PR1 hits — but per §4.7.5 N9 head_bound is NOT renamed in F (no scope intersection)
  - `atom_bounds`: EXCLUDED per N8 (4 files in active code, preserved)

- **AD/C/E/B1/B2 inherited contracts**: all surfaces preserved per N13-N23.

- **当前已知约束**:
  - Q-PR1 sacred 5 paths at 0-diff vs `4c472b50`
  - Sacred `master` at `562c74195df43e933bed92a3ff25de94dd8ce666`
  - Dirty baseline (4 M + 2 D + 2 untracked) preserved
  - Hard-cut per §4.8.1 (subject to G1 Option D carve-out per PF-R1)
  - Layer authority per architecture_principles.md §2.1
  - F §4.7.3 cross-references B2 branch→case semantic supremacy

- **当前相关历史蓝图**:
  - Q-NAMING-AD archived at `7c48648f` — Phase 1 (Yellow)
  - Q-NAMING-C archived at `23389a2b` — Phase 2 (Yellow)
  - Q-NAMING-E archived at `121a375d` — Phase 3 Red (PF-R1 Q-PR1 trap on legacy Rule.where → Option D preserve precedent)
  - Q-NAMING-B1 archived at `30bd31c8` — Phase 4 Yellow
  - Q-NAMING-B2 archived at `0b4c8aad` — Phase 5 Red (G14 ecosystem 57-file expansion + persistence JSON dict-key cascade + Step 4.7 P1 fix `9d544425` cranch typo + fixture migration patterns)
  - Slice 6 form-i debt cleanup at `4c472b50` — Q-PR1 sacred baseline

## 5. Proposed Shape

### §5.1 Rename layout per Goal

8 symbol renames (G1-G8 + G9-G11 cross-cutting; preflight 2.c verifies):

| Goal | Old | New | File:line scope | Approx hits |
|---|---|---|---|---|
| G1 | `SemanticsProfile` | `EngineProfile` | [`core/semantics/profile.py:26`](../../../src/factgraph/core/semantics/profile.py) — class def + exports at [`core/semantics/__init__.py:3+5`](../../../src/factgraph/core/semantics/__init__.py) + [`sdk/__init__.py:51+97`](../../../src/factgraph/sdk/__init__.py) + all imports/uses across `application/`, `sdk/`, tests, docs. **[LIKELY Option D PRESERVE per PF-R1]** | 153 / 51 files |
| G2 | `ProbLogSemantics` | `ProbLogConfig` | [`sdk/semantics.py`](../../../src/factgraph/sdk/semantics.py) | 30 / 28 |
| G3 | `PyReasonSemantics` | `PyReasonConfig` | [`sdk/semantics.py`](../../../src/factgraph/sdk/semantics.py) | 55 / 29 |
| G4 | `semantics=` | `config=` | SDK + service + application call-site keyword arg | 44 hits |
| G5 | `branch_probabilities` | `case_probabilities` | payload field across DTOs + runtime + tests | 53 / 18 files |
| G6 | `branch_bounds` | `case_bounds` | payload field | 39 / 14 |
| G7 | `inspect_semantics` | `preview_config` | `fg.eval.inspect_semantics` namespaced manager method at [`sdk/store.py:1302`](../../../src/factgraph/sdk/store.py) + private `_inspect_semantics` SDK helper at [`sdk/store.py:2263`](../../../src/factgraph/sdk/store.py) + docs/tests; **no flat top-level shell post Q-NAMING-C**; preserve `inspect_semantics_profile(...)` core helper at `core/semantics/profile.py` (separate function) per Option D if G1 locked | 9 / 10 |
| G8 | `semantics_digest` | `config_digest` | persisted digest field + helper imports | 11 files |

### §5.2 G1 SemanticsProfile Q-PR1 PF-R1 risk + Option lock (critical) **[LOCKED Step 4.4 PF-R1 Option D PRESERVE]**

**LOCKED Step 4.4 PF-R1 per Step 4.3 preflight `ddbeec94`**: G1 SemanticsProfile rename BLOCKED by Q-PR1 sacred 29 distinct hits across TWO sacred files (engine_eval.py 21 sites + rule_ext.py 8 sites). **Option D preserve LOCKED**. G1 implementation = NO migration code change.

**Verified Q-PR1 sacred SemanticsProfile sites** (per Step 4.3 preflight fresh re-read):

- `adapters/pyreason/engine_eval.py:9` imports `SemanticsProfile` directly
- `engine_eval.py:54+287+384` use SemanticsProfile in type annotations
- `engine_eval.py:293+387` `isinstance(..., SemanticsProfile)` runtime checks
- `engine_eval.py:295+299+389+393` error message strings mention `SemanticsProfile`
- `engine_eval.py:310+315+319+339+372` carrier strings reference `SemanticsProfile.temporal_projection.*` / `SemanticsProfile.iteration_count`

**Locked outcome per Step 4.4 PF-R1 LOCK** (per Step 4.3 preflight `ddbeec94`): **Option D preserve SemanticsProfile entirely** — same rationale as Q-NAMING-E G4 `Rule.where` (Q-PR1 sacred adapter usage cannot be modified, no-alias rule blocks Option B, isinstance-check blocks Option C). G1 implementation outcome:

- G1 reframed to **preserve `SemanticsProfile` class name unchanged** — no migration code change, no isinstance update, no type annotation update, no carrier string update
- All other §4.7 renames (G2-G8) proceed independently (ProbLogSemantics→ProbLogConfig, PyReasonSemantics→PyReasonConfig, semantics=→config= per N24 carve-out, branch_*→case_*, inspect_semantics→preview_config, semantics_digest→config_digest cluster)
- `SemanticsProfile` remains as the underlying value object name; `ProbLogConfig`/`PyReasonConfig` are SDK wrappers that lower to `SemanticsProfile` instances
- Public surface inconsistency acknowledged but acceptable (matches Q-NAMING-E precedent where legacy DSL `Rule.where` co-exists with public application `Rule.when`)
- §10 documents Option D rationale for §10.4 PF Alignment

### §5.3 Class-vs-field boundary + helper preservation

**Q-NAMING-F renames target**:

- Class symbol definition (`class OldName:` → `class NewName:`) for G2/G3 (and G1 if not Option D)
- Field name annotations + dataclass declarations for G5/G6/G8
- Keyword argument name in function signatures for G4 (`def f(*, semantics=...)` → `def f(*, config=...)`)
- SDK method name for G7 (`def inspect_semantics(...)` → `def preview_config(...)`)
- All callsite imports / type hints / kwarg usages
- Test fixture method calls + kwarg usages
- Docstring + comment references
- `__all__` exports list entries

**Q-NAMING-F does NOT rename**:

- `atom_bounds` PyReason-domain syntax per N8 (4 files preserved)
- `head_bound` per N9 (38 hits including 21 Q-PR1; per §4.7.5 CLEANUP not rename)
- B2 inherited surfaces per N22 (7 long-form fields + G8 diagnostic key + G9 WhyNot LOCKED + G14 ecosystem + G15 proof PRESERVED + G16 Category B template)
- N19 inherited B1 8 preserved fields per N19
- AD/C/E inherited contracts per N13-N18
- Internal local variable names within method bodies unrelated to the renamed surface

### §5.4 Q-PR1 boundary verification (critical, per Q-NAMING-E PF-R1 precedent)

Step 4.1 grep results:

- G1 `SemanticsProfile`: **29 distinct hits across TWO Q-PR1 sacred files** = `adapters/pyreason/engine_eval.py` 21 sites + `adapters/pyreason/rule_ext.py` 8 sites → **PF-R1 trap CONFIRMED; LOCKED Option D PRESERVE** per Step 4.4 (preflight `ddbeec94`)
- G2 `ProbLogSemantics`: 0 hits ✓
- G3 `PyReasonSemantics`: 0 hits ✓
- G4 `semantics=`: 0 hits in Q-PR1 sacred ✓
- G5 `branch_probabilities`: 0 hits ✓
- G6 `branch_bounds`: 0 hits ✓
- G7 `inspect_semantics`: 0 hits ✓
- G8 `semantics_digest`: 0 hits ✓
- N9 `head_bound`: 21 hits — NOT in F scope per §4.7.5 + N9
- N8 `atom_bounds`: EXCLUDED preserved
- N20 `branch_idx`/`atom_idx` short-form: preserved per B2

Step 4.3 preflight 2.a re-verifies + locks G1 Option D per PF-R1 precedent.

### §5.5 Wire/persistence/digest hard-cut per §4.7.7

Per §4.7.7 + Sub-Q1:

- `semantics_digest` field renamed to `config_digest` (G8) — stored digest field name changes
- Adapter-facing payload field names change (`branch_probabilities → case_probabilities`, `branch_bounds → case_bounds`) — G5/G6
- Hard-cut at v0.2; no alias support; no dual-emit
- Acceptable because alpha — no external adapter consumers
- Cross-references B2's hard-cut precedent for `engine_payload → proof` field rename + Persistence JSON dict-key cascade for `pred_atom_key`/`ruleref_atom_key`

### §5.6 Docs + tests + examples migration

Per Q-NAMING decision §4.8.5 + B1/B2 precedent (N-3 enumeration extended):

- `src/factgraph/*/docs/` — current SDK docs (Step 4.6.5 enumerates exact set; likely 01_concepts.en.md, 04_api_surface.en.md, 03_rules_and_inferences.en.md for SDK + 02_evidence_graph.md for audit semantics_digest doc ref)
- `docs/official/kernel/quickstart/` — current quickstarts (semantics-related)
- `docs/references/external/` — verify external reference docs
- `src/service/docs/` — service-layer semantics references
- `tests/` — active test cohort
- `examples/` + `tutorials/` — N-7 proactive scan per B1/B2 precedent

Historical material under `workflow/heritage/` / `workflow/blueprints/archive/` / `workflow/audit/archive/` / `docs/references/working/` / `examples/archive/` is carved out per §4.8.5.

Step 4.6.5 pre-impl grep mandatory for this Red slice — distinguishes current vs historical doc paths + full test cohort + N-3 + N-7 enumeration.

### §5.7 Step 4.6.5 pre-implementation grep inventory

To be populated at Step 4.6.5 per Q-NAMING-AD + C + E + B1 + B2 precedent (mandatory for Red slice per CADENCE). Categories:

- **N-1 Active tests**: `tests/` + `src/domains/` + `src/service/tests/` files referencing any of 8 F symbols
- **N-2 Active source**: `src/factgraph/` + `src/service/` + `src/agent/` Python files for each old name
- **N-3 Current docs**: per Q-NAMING-B2 N-3 enumeration extended (`audit/docs/02_evidence_graph.md` semantics_digest reference is known)
- **N-4 Q-PR1 verification**: re-grep all 8 names against Q-PR1 sacred 5 paths; lock G1 PF-R1 Option D per §5.2
- **N-5 Cross-cutting cluster**: SemanticsProfile + Probelog/PyReasonSemantics + semantics= keyword co-occurrence
- **N-6 Historical carve-outs**: per Q-NAMING §4.8.5
- **N-7 Examples/tutorials proactive scan**: per B1/B2 precedent
- **N-8 Persistence schema digest**: per §4.7.7 — `semantics_digest → config_digest` impact on persisted digest format; B2-precedent JSON snapshot regeneration if applicable
- **N-9 head_bound carve-out**: verify no scope intersection per §4.7.5 + N9

### §5.8 Q-NAMING-AD + C + E + B1 + B2 precedent reuse

Patterns from prior Phase 1/2/3/4/5 slices applicable here:

- **Hard-cut per Sub-Q1 + §4.8.1** (no alias, no dual-emit, old removed in same commit; subject to G1 Option D carve-out per PF-R1)
- **N7 layer authority preservation** (AD/C/E/B1/B2 inherited): core IR + low-level protocol details retain internal names where they don't appear on the renamed public surface
- **PF-R1 Q-PR1 sacred detection discipline** (Q-NAMING-E precedent): preflight verifies each symbol against Q-PR1 paths before lock; G1 SemanticsProfile likely locks Option D per same precedent as Q-NAMING-E G4 Rule.where
- **Test/example same-slice migration** (audit §3 mandate per Sub-Q3 single-slice cadence)
- **AD/C/E/B1/B2 inherited preservation invariants** (N13-N23 explicit)
- **Step 4.7 P1 fix pattern** (Q-NAMING-E + B2 precedent): Codex independent test re-run during Step 4.7 review will likely surface fixture migration misses requiring P1 fix commit; budget for 2-3 commit pattern (main feat + P1 fix + optional docs align)
- **N-7 proactive examples/tutorials extension** (B1/B2 precedent): implementation surfaces current examples/tutorials beyond initial §5.6 enumeration; migrate in same feat commit

## 6. Boundaries And Invariants

- **必须保持的边界**:
  - Q-PR1 sacred 5 paths: 0-diff vs `4c472b50` after every commit (G1 Option D preserves this)
  - Sacred master `562c74195df43e933bed92a3ff25de94dd8ce666` — never modified
  - Dirty baseline preserved — never `git add` baseline files
  - Layer authority per architecture_principles.md §2.1: SDK + application protocol field/class symbol rename; core IR preserved per N7
  - **AD slice contracts** (N13)
  - **C slice contracts** (N14)
  - **E slice contracts** (N15)
  - **B1 slice contracts** (N19)
  - **B2 slice contracts** (N22) — 7 long-form fields + G8 diagnostic key + G9 WhyNot LOCKED + G14 ecosystem + G15 PRESERVED proof + G16 Category B template
  - **F field-vs-substring boundary**: rename ONLY the 8 explicit F symbols; `atom_bounds` per N8 + `head_bound` per N9 + N20 short-form variants all PRESERVED
- **明确不做的内容**:
  - N1-N23 above
  - Any new public method, namespaced manager, or class hierarchy restructure
  - Any method body restructure (rename-only slice)
  - Any new class introduction
- **兼容性约束**:
  - Hard-cut per §4.8.1 — no aliases, no dual-emit, no deprecation shims (subject to G1 Option D carve-out)
  - Alpha release, no historical user protection burden
  - Old name removed in same commit as new name introduced
  - `semantics_digest → config_digest` is hard-cut wire/persistence change (recorded digests change v0.2; per §4.7.7 acceptable)
  - All renames + G4 keyword + G5/G6 field + G7 helper + G8 digest land in same single feat commit unless Step 4.7 review surfaces P1 fix (2-3 commit pattern per B1/B2 precedent)

## 7. Acceptance

- [ ] G1: **[LOCKED Step 4.4 PF-R1 Option D PRESERVE]** — `SemanticsProfile` class name preserved entirely; NO migration code change; 51-file SemanticsProfile reference cluster preserved unchanged; export sites at `core/semantics/__init__.py:3+5` + `sdk/__init__.py:51+97` PRESERVED. Per Q-PR1 0-diff invariant + 29 distinct hits across `engine_eval.py` + `rule_ext.py` Q-PR1 sacred (same pattern as Q-NAMING-E PF-R1 G4 `Rule.where` precedent).
- [ ] G2: `ProbLogSemantics` removed; `ProbLogConfig` present at `sdk/semantics.py` + all imports/uses migrated
- [ ] G3: `PyReasonSemantics` removed; `PyReasonConfig` present at `sdk/semantics.py` + all imports/uses migrated
- [ ] G4: `semantics=` keyword removed; `config=` present at all call-site keyword arg sites
- [ ] G5: `branch_probabilities` removed; `case_probabilities` present at all payload field + DTO + runtime + test sites
- [ ] G6: `branch_bounds` removed; `case_bounds` present at all payload field sites
- [ ] G7: `inspect_semantics` removed; `preview_config` present at `fg.eval.preview_config` namespaced manager method + private SDK helper + docs/tests; **no flat top-level shell** (per Q-NAMING-C archived flat-shell deletion); `inspect_semantics_profile(...)` core helper preserved (separate function) per Option D if G1 locked
- [ ] G8: `semantics_digest` removed; `config_digest` present at persisted digest field + helper import sites
- [ ] G9: Hard-cut honored — no aliases, no dual-emit, old names removed same commit (subject to G1 Option D carve-out per PF-R1)
- [ ] G10: Module docs + current SDK docs + current quickstarts + current examples + N-7 tutorials + active tests migrated
- [ ] G11: Q-PR1 sacred 5 paths 0-diff vs `4c472b50` confirmed at impl commit HEAD
- [ ] Sacred master `562c74...` unchanged through slice
- [ ] Dirty baseline preserved (no baseline file in `git diff --cached`)
- [ ] All targeted Q-NAMING-F verification completed; known baseline failures recorded in §10 with no new regression
- [ ] No push without explicit user authorization
- [ ] AD slice contracts preserved (N13 honored)
- [ ] C slice contracts preserved (N14 honored)
- [ ] E slice contracts preserved (N15 honored)
- [ ] B1 slice contracts preserved (N19 honored)
- [ ] B2 slice contracts preserved (N22 honored)
- [ ] F field-vs-substring boundary honored — no `atom_bounds` rename per N8; no `head_bound` rename per N9; N20 short-form preserved
- [ ] Error/log message policy honored per B1 §5.2.1 precedent — class-name/field-name tokens update; field-path tokens preserve as appropriate
- [ ] Scope grep verify: 0 occurrences of any 8 old F names in `src/` + `tests/` + current docs (excluding historical/archive carve-outs + Option D-preserved G1 if locked)
- [ ] Q-PR1 boundary verify: 0 references to any 8 old F names within Q-PR1 sacred paths (subject to G1 Option D preserve carve-out)

## 8. Implementation Plan

1. **Step 4.2 review (Codex)**: surface 3-6 tightenings per CADENCE Rule 1 + Rule 2 + scope discipline. Cross-check AD/C/E/B1/B2 inherited state untouched. Particularly probe: (a) §5.2 G1 PF-R1 trap depth + Option D lock; (b) §5.3 boundary clarity for atom_bounds N8 + head_bound N9; (c) §4.7.3 cross-ref B2 branch→case cascade; (d) §4.7.7 persistence digest impact; (e) cross-rename ordering between SemanticsProfile (if not Option D) + ProbLogSemantics + PyReasonSemantics + semantics= keyword; (f) potential `config=` keyword collision with existing unrelated `config=` per §4.7.2 caveat.
2. **Step 4.3 preflight (Claude per AD/C/E/B1/B2 precedent)**: independent branch `v0.2.0-q-naming-f-preflight-2026-05-31`. Re-read all blueprint-referenced shipped files at preflight-row drafting time per Rule 1. Build 5-bucket severity findings table. **Mandatory preflight findings**:
   - 2.a **Q-PR1 sacred verification (per Q-NAMING-E PF-R1 precedent + Step 4.2 P2-1 extension + LOCKED Step 4.4 PF-R1)**: Step 4.3 preflight `ddbeec94` confirmed Q-PR1 sacred trap across BOTH `adapters/pyreason/engine_eval.py` (21 distinct sites) AND `adapters/pyreason/rule_ext.py` (8 distinct sites) = **29 total distinct sites** (refined from Step 4.2 P2-1 estimate of ~22). **LOCKED Option D preserve SemanticsProfile entirely** — no migration code change. N9 head_bound + N20 short-form + N8 atom_bounds carve-outs verified intact per PF-v2. Per P3-2 tightening: F implementation does NOT touch head_bound code or docs anywhere.
   - 2.b **Exact footprint distribution + export module list (extended Step 4.2 P2-2)**: refine Step 4.1 approximate hit counts; verify export sites that need migration OR preserve per Option D. Explicit export sites: [`core/semantics/__init__.py:3+5`](../../../src/factgraph/core/semantics/__init__.py) (re-export SemanticsProfile + inspect_semantics_profile + `__all__`) — under Option D both PRESERVED unchanged; [`sdk/__init__.py:51+97`](../../../src/factgraph/sdk/__init__.py) (re-export SemanticsProfile + `__all__`) — under Option D PRESERVED; [`application/protocol/__init__.py`](../../../src/factgraph/application/protocol/__init__.py) (verify re-exports for G2/G3 SDK wrappers if any); [`application/protocol/evaluate_result.py:73`](../../../src/factgraph/application/protocol/evaluate_result.py) `__all__` for `semantics_digest` field name + line 1416 `semantics_digest_for` (G8 RENAME or preserve per scope lock).
   - 2.c **Per-symbol classification**: verify each F target is class symbol / field name / keyword arg / method name discriminated per blueprint G1-G8.
   - 2.d **Cross-rename coordination**: SemanticsProfile (if not Option D) + ProbLogSemantics + PyReasonSemantics may co-occur in same files; ordering for clean intermediate states.
   - 2.e **§4.7.2 `config=` keyword collision per audit §7 caveat (extended Step 4.2 P2-4 + N24 + LOCKED Step 4.4 PF-r2)**: Step 4.3 preflight `ddbeec94` confirmed 2 Q-PR1 sacred adapter-internal `config=` sites + PyReasonRunConfig type system PRESERVED per N24. **LOCKED N24 per-call-site classification table** for Step 4.7 implementation: shipped `config=` adapter-internal usage at `adapters/pyreason/runner.py:412` + `engine_eval.py:97` + PyReasonRunConfig type at `runner.py:29/46/345/351` + import at `engine_eval.py:11/133/135/154` MUST stay 0-diff vs `4c472b50`. G4 SDK/service/application `semantics=` → `config=` RENAME applies only to non-adapter call-sites; Codex Step 4.6.5 pre-impl grep + Step 4.7 implementation must enumerate per-call-site UPDATE vs PRESERVE per N24 carve-out.
   - 2.f **Naming conflict re-verification**: re-grep 7 new F names against full `src/` + `tests/` + `docs/` to confirm 0 shipped occurrences.
   - 2.g **AD + C + E + B1 + B2 inherited state preservation**: confirm F scope does NOT touch any AD/C/E/B1/B2 contract surface; cross-refs §4.7.3 branch→case cascade aligns with B2 N22 semantic supremacy.
   - 2.h **`atom_bounds` N8 + `head_bound` N9 carve-out re-verification**: per Step 4.1 grep counts (atom_bounds 4 files, head_bound 38 hits including 21 Q-PR1); confirm no scope intersection with F renames.
   - 2.i **Persistence digest impact per §4.7.7 (extended Step 4.2 P2-3 + LOCKED Step 4.4 PF-r1 11-file enumeration)**: Step 4.3 preflight `ddbeec94` refined G8 digest cluster footprint from blueprint Step 4.1 estimate of 10 → **11 deduped files** (cross-ref §5.1 G8 enumeration). Cluster covers: `application/protocol/evaluate_result.py` (DTO field + helper function + dict keys + validation), `sdk/store.py` (import + helper call + dict keys + kwargs), `src/service/runtime_v1.py` (service serialization), `audit/docs/02_evidence_graph.md` (doc), `sdk/docs/03_rules_and_inferences.en.md` (doc), `src/service/docs/03_runtime_queries_policy.md` (service doc), `tests/test_problog_semantics_profile_migration.py`, `tests/sdk/test_rule_expr_evaluate.py`, `tests/application/protocol/test_evaluate_result_dtos.py`, `tests/application/protocol/test_evaluate_result_digests.py`, `docs/official/kernel/quickstart/evidence.md`. Per B2 PF-r2 precedent: persistence JSON dict-key cascade applies; budget test fixture + JSON snapshot regeneration. No blocker beyond what B2 PF-r2 surfaced.
   - 2.j **Sub-Q3 cadence decision**: confirm all 8 renames can land in one feat commit (subject to G1 Option D carve-out) OR identify defensible split. Budget 2-3 commit pattern per Q-NAMING-E + B2 Red precedent.
3. **Step 4.4 preflight amendment (on blueprint branch)** ✓ COMPLETED at `936f248f`: applied PF-R1 LOCK G1 Option D preserve + PF-r1 LOCK G8 11-file cluster + PF-r2 LOCK N24 config= per-call-site classification + PF-v1-v8 + PF-s1 Decision Notes.
4. **Step 4.5 self-check (lightweight)**: PF coverage verification, no commit.
5. **Step 4.6 scoped anchor**: `Status: draft` → `Status: scoped` single small commit + audit log event row.
6. **Step 4.6.5 pre-impl grep amendment (MANDATORY for Red slice)**: pre-impl grep covering all 8 F names + carve-out verification + cross-class cluster confirmation + persistence digest sites. Surface any consumer beyond §5.7 enumeration. Apply CADENCE Option 2.
7. **Step 4.7 implementation (Codex 承接)** on branch `v0.2.0-impl-q-naming-f-2026-05-31`:
   - 7.1 G1 SemanticsProfile **LOCKED Option D preserve — NO code changes** per Step 4.4 PF-R1 LOCK (29 distinct Q-PR1 sacred hits across engine_eval.py + rule_ext.py)
   - 7.2 G2 ProbLogSemantics → ProbLogConfig
   - 7.3 G3 PyReasonSemantics → PyReasonConfig
   - 7.4 G4 semantics= → config= (handle disambiguation per §4.7.2 + preflight 2.e)
   - 7.5 G5 branch_probabilities → case_probabilities
   - 7.6 G6 branch_bounds → case_bounds
   - 7.7 G7 inspect_semantics → preview_config
   - 7.8 G8 semantics_digest → config_digest
   - 7.9 Update `__init__.py` export modules (preflight 2.b confirmed)
   - 7.10 Migrate tests + N-7 proactive examples/tutorials per §5.6
   - 7.11 Update module docs + current SDK docs + quickstarts + audit/docs/02_evidence_graph.md (semantics_digest doc reference)
   - 7.12 Lint pass (`ruff check` targeted)
   - 7.13 Single feat commit (or 2-3 commit pattern per CADENCE if Step 4.7 review surfaces P1 fix per Q-NAMING-E + B2 precedent). **Implementation discipline reminder** (per §5.3 + Q-NAMING-B2 §5.2.1 error message policy): rename only the 8 explicit F symbols + adapter consumption preserve per Option D; N8 atom_bounds + N9 head_bound + N20 short-form + N22 B2 surfaces all PRESERVED.
8. **Step 4.7 review (Claude)**: independent test re-run (CRITICAL for Red slice per Q-NAMING-E + B2 lesson — Codex's test counts must be verified independently), scope grep (verify 7 old F names + old semantics_digest field → 0 in current code modulo Option D preserve), cross-check PF / N findings, cross-check AD + C + E + B1 + B2 contract preservation, verify atom_bounds + head_bound + short-form carve-outs.
9. **Step 4.8 closure**: `Status: scoped` → `Status: implemented` + §10 Outcome / Deviations filled + audit log "implemented" event.
10. **Step 4.9 archive**: `git mv` blueprint + audit log pair from `active/` to `archive/` + `INVENTORY.md` entry + audit log archived event row. Q-NAMING 6-phase routemap closes.

## 9. Docs To Update

- `src/factgraph/sdk/docs/` (Step 4.6.5 enumerates; semantics/config-related docs)
- `src/factgraph/application/docs/`
- `src/factgraph/core/docs/`
- `src/factgraph/audit/docs/02_evidence_graph.md` (known `semantics_digest` reference)
- `docs/quickstart/` (or equivalent)
- `docs/official/kernel/quickstart/`
- `docs/references/external/`
- `src/service/docs/`
- N-7 examples/tutorials per Step 4.6.5
- `docs/README.md` — only if new persistent docs entry introduced (none expected)
- Historical material — NOT touched per Q-NAMING §4.8.5

## 10. Outcome / Deviations

**Step 4.4 PF locks** (per Step 4.3 preflight `ddbeec94`):

- **PF-R1 G1 Option D PRESERVE LOCK**: SemanticsProfile rename DEFERRED INDEFINITELY per Q-PR1 sacred 29-hit trap (engine_eval.py 21 + rule_ext.py 8 distinct sites). Same structural pattern as Q-NAMING-E PF-R1 G4 Rule.where preserve precedent. G1 implementation footprint reduces from 51 files to 0 files for migration; all SemanticsProfile references preserved unchanged. Public surface coexistence acknowledged (similar to Q-NAMING-E legacy `Rule.where` coexisting with public application `Rule.when`).
- **PF-r1 G8 digest cluster 11-file enumeration LOCK**: refined from blueprint §5.1 estimate of 10 to **11 deduped files** per Step 4.3 preflight grep. Per B2 PF-r2 precedent: persistence JSON dict-key cascade applies; budget test fixture + JSON snapshot regeneration within Step 4.7.
- **PF-r2 N24 config= per-call-site classification LOCK**: Step 4.7 implementation must enumerate per-call-site distinguishing public SDK/service/application `semantics= → config=` (RENAME) from Q-PR1 sacred adapter-internal `config=` (PRESERVE at `runner.py:412` + `engine_eval.py:97` + PyReasonRunConfig type system).

**Step 4.7 implementation discipline carry-forward** (per Step 4.3 preflight PF-s1 at `ddbeec94`):

- **Cross-rename ordering for Step 4.7**: (1) G1 SemanticsProfile PRESERVE entirely (no code changes — PF-R1 Option D LOCK); (2) G2 ProbLogSemantics → ProbLogConfig (class rename at `sdk/semantics.py:119` + sdk/__init__.py:31 import + __all__:104); (3) G3 PyReasonSemantics → PyReasonConfig (class rename at line 173 + sdk/__init__.py:31 + __all__:105); (4) G4 semantics= → config= keyword (with N24 carve-out per per-call-site classification — preserve Q-PR1 adapter `config=` usage); (5) G5 branch_probabilities → case_probabilities (field rename on PyReasonConfig at line 137); (6) G6 branch_bounds → case_bounds (field rename on PyReasonConfig at line 186); (7) G7 inspect_semantics → preview_config (namespaced manager method at `sdk/store.py:1302` + private helper at 2263; preserve core helper `inspect_semantics_profile` per Option D); (8) G8 semantics_digest → config_digest cluster (11 files coordinated rename including DTO field + helper function + dict keys + service + tests + docs).
- **G1 Option D simplification**: PRESERVE entirely eliminates SemanticsProfile reference chain coordination overhead. No isinstance check updates. No carrier string updates. No type annotation updates. Significant simplification vs original draft scope.
- **Cadence budget**: 2-3 commit pattern (main feat + likely P1 fix + optional docs align) per Q-NAMING-E + B2 Red precedent. Under PF-R1 Option D LOCK, actual implementation migration footprint reduces from 58 deduped files to ~30-40 files (51-file SemanticsProfile cluster preserved) — smaller risk surface than B2.

(Step 4.8 closure outcome / deviations to be filled at implementation closure.)
