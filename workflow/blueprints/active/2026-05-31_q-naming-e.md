# Q-NAMING-E Blueprint: Rule / Inference DSL rename (Branch→Case, where→when, target+head_vars→emits)

- Status: draft
- Created: 2026-05-31
- Last Updated: 2026-05-31
- Related Modules:
  - `src/factgraph/sdk/dsl/` (Rule + Inference DSL primary surfaces)
  - `src/factgraph/sdk/` (SDK facade — secondary blast radius for `where` field consumers)
  - `src/factgraph/application/protocol/` (application Rule + Inference DTOs — verify at preflight whether parallel rename needed)
  - `src/factgraph/core/rules/` (core rule IR / compile path — N7 layer authority means likely preserved internally; verify)
- Related Docs:
  - [`workflow/design/decisions/active/2026-05-31_q1-naming-api-polish-decision.md`](../../design/decisions/active/2026-05-31_q1-naming-api-polish-decision.md) — Q-NAMING decision §4.6 + Sub-Q2 hard-cut `.where()` + §4.8 invariants (adopted at `f1a017ec`)
  - [`workflow/audit/active/2026-05-31_naming-polish-feasibility.md`](../../audit/active/2026-05-31_naming-polish-feasibility.md) — feasibility audit §6 (Batch E) at `10de33b9`: Red risk, 358 hits / 115 files
  - [`workflow/blueprints/archive/2026-05-31_q-naming-ad.md`](../archive/2026-05-31_q-naming-ad.md) — Q-NAMING-AD Phase 1 precedent
  - [`workflow/blueprints/archive/2026-05-31_q-naming-c.md`](../archive/2026-05-31_q-naming-c.md) — Q-NAMING-C Phase 2 precedent: subtractive + Sub-Q3 single-slice
  - [`workflow/foundations/architecture_principles.md`](../../foundations/architecture_principles.md) §2.1 layer authority
- Audit Log:
  - [2026-05-31_q-naming-e.audit.md](./2026-05-31_q-naming-e.audit.md)

## 1. Problem

Q-NAMING decision §4.6 locks Rule / Inference DSL surface rename for v0.2.0 alpha. Audit §6 (Batch E) at `10de33b9` measured the blast radius: **Red risk, 358 hits / 115 files** (41 src, 18 test, 55 docs/workflow plus 1-tier indirect). Audit §6 hidden dependencies surfaced:

1. **Rule and Inference share DSL naming surfaces** but have **different compatibility needs**: per Q-NAMING §4.6, `Branch`→`Case` rename and `target`+`head_vars`→`emits` rename apply to **legacy Inference class only**; `where`→`when` rename applies to **BOTH new Rule DSL and legacy Inference DSL**. Implementation must avoid accidentally renaming Rule internals.
2. **Test/example teach-syntax cohort**: per audit §6, if `where` changes on both Rule and Inference, examples and tests that teach query syntax will all need to move in the same slice (180 test files with `where=[...]` patterns).
3. **`target` is a noisy term in the repo** (audit §6): implementation must scope rename to Inference DSL field only, NOT to all `target=`-style variable assignments across the codebase.

Per Sub-Q2 hard-cut decision (Q-NAMING §4.8.1): `.where(...)` → `.when(...)` is **hard-cut at v0.2** with no alias support. Examples / tests / docs teaching query syntax all migrate in this single slice. This matches the alpha hard-cut posture established by Q-NAMING-AD (`.save`/`.load` rename) and Q-NAMING-C (20 flat shell delete).

The Q-NAMING-AD precedent (PF-R1 Option (a) private-helper rewire) and Q-NAMING-C precedent (per-shell C-bucket classification + Sub-Q3 single-slice combined refactor + delete) inform this slice's approach: classify each rename target, identify both **shipped reality** (what's renamed) and **carve-outs** (what stays), and surface all hidden-dependency blockers at Step 4.3 preflight before scope-freeze.

## 2. Goals

**Per Q-NAMING §4.6.1 — `Branch` → `Case` (legacy Inference scope, per user)**:

- G1: Rename DSL class `Branch` → `Case` at [`src/factgraph/sdk/dsl/branch.py:13`](../../../src/factgraph/sdk/dsl/branch.py). **Step 4.3 preflight 2.a must classify**: per user 2026-05-31 decision the rename targets "legacy Inference only", but shipped docstring at line 14 states "Named alternative body inside **a rule or inference**". Preflight resolves whether (a) Branch is renamed globally with Rule DSL accepting `Case` (preferred per shipped reality), or (b) Branch is preserved as alias for Rule DSL while only Inference DSL uses `Case`. Default: (a) global rename matches shipped reality; user 2026-05-30 framing was historical context, not a final scope constraint.

**Per Q-NAMING §4.6.2 — `where` → `when` (both Rule and Inference DSL, hard-cut)**:

- G2: Rename `Rule.where` field → `Rule.when` at [`src/factgraph/sdk/dsl/rule.py:73`](../../../src/factgraph/sdk/dsl/rule.py). Hard-cut per Sub-Q2; no alias.
- G3: Rename `Inference.where` field → `Inference.when` at [`src/factgraph/sdk/dsl/rule.py:134`](../../../src/factgraph/sdk/dsl/rule.py). Hard-cut per Sub-Q2; no alias.
- G4: Rename corresponding fields in application protocol `Rule` and `Inference` DTOs at [`src/factgraph/application/protocol/rule.py:53+`](../../../src/factgraph/application/protocol/rule.py). Preflight 2.b verifies exact field names + propagation requirements.

**Per Q-NAMING §4.6.3 — `target + head_vars` → `emits` (legacy Inference only)**:

- G5: Rename `Inference.target` field → `Inference.emits.target` (or equivalent emit DTO shape per preflight design) at [`src/factgraph/sdk/dsl/rule.py:137`](../../../src/factgraph/sdk/dsl/rule.py). Preserve as Inference-only; `Rule.target` (if any) is NOT renamed. Preflight 2.c verifies Rule class has no `target` field (initial scan shows `Rule` has `id`, `version`, `select`, `where` — no `target`).
- G6: Rename `Inference.head_vars` field → folded into `Inference.emits` per emit DTO at [`src/factgraph/sdk/dsl/rule.py:138`](../../../src/factgraph/sdk/dsl/rule.py). Preflight 2.c specifies exact emit-DTO shape (single combined field vs nested vs structural alternative).

**Per Q-NAMING §4.6.4 / §4.6.5 — REJECT carve-outs**:

- G7 REJECT: Rule → NamedQuery is REJECTED per user. No rename of Rule class to NamedQuery.
- G8 REJECT: All other Module 4 proposals beyond 4.1 + 4.2 + emit rename are REJECTED per user.

**Per Q-NAMING §4.6.6 — docs-only verifications**:

- G9: `Pred` documentation. Per user "Pred is low-level escape hatch — docs work". **Step 4.3 preflight 2.d must clarify**: initial scan returned 0 `class Pred` hits. Preflight identifies whether `Pred` exists as a class, a function, a string literal, or a documentation-only term. Implementation polishes docs to clarify "low-level escape hatch" wording.
- G10: `Not(condition)` syntax. Per user "verify whether grammar supports it". **Preflight 2.e confirms**: shipped `Not(body)` function exists at [`src/factgraph/sdk/dsl/expr.py:302`](../../../src/factgraph/sdk/dsl/expr.py). Implementation is docs-only — refresh examples + ensure `Not(condition)` is documented as supported.

**Cross-cutting per §4.8 invariants + Red-risk Sub-Q2 hard-cut**:

- G11: Hard-cut per §4.8.1 — no aliases, no dual-emit, old field/class names removed in same commit as new names introduced.
- G12: Update affected module docs (`src/factgraph/sdk/dsl/docs/`, `src/factgraph/sdk/docs/`, possibly `src/factgraph/application/protocol/docs/`) + current SDK quickstart / examples (`docs/`) + Inference / Rule teaching tutorials in same slice per CADENCE Stage 4.8.
- G13: Q-PR1 sacred 5 paths 0-diff vs `4c472b50` preserved through all commits; sacred master `562c74195df4...` unchanged; dirty baseline 8 entries preserved.

## 3. Non-goals

- N1: Q-NAMING-AD scope re-litigation (`fg.views` / `FrozenAssertionView` / `by_ids strict` / `audit.explain/conflicts` / `save_workspace`/`load_workspace`). All AD work is archived; Q-NAMING-E inherits AD state untouched.
- N2: Q-NAMING-C scope re-litigation (20 flat shell delete + `_SDKWhatIf*` deletion + commit_assertions preservation). All C work is archived; Q-NAMING-E inherits C state untouched.
- N3: §4.3 cascade renames (atom → condition, branch_index → case_index, branch → case at field/wire level, class renames like `SupportArtifact`→`ProofReceipt`). Reserved for Q-NAMING-B1 / B2 Phase 4-5.
- N4: §4.7 engine config renames (ProbLog/PyReason Semantics → Config, SemanticsProfile → EngineProfile, semantics= → config=, branch_probabilities → case_probabilities). Reserved for Q-NAMING-F Phase 6.
- N5: PDF Module 1 Schema renames per §4.1 REJECT.
- N6: All Module 4 Rules/Inferences proposals beyond §4.6.1 (4.1) + §4.6.2 (4.2) + §4.6.3 (target+head_vars→emits Inference only). Per §4.6.5 REJECT.
- N7: Lower-level core rule IR rename (`core/rules/rule_ir.py`, `core/rules/rule_ast_validate.py` internal symbol names). Public DSL surface changes only; core IR names preserved per architecture_principles.md §2.1 layer authority + Q-NAMING N7 precedent inherited from AD/C.
- N8: Persistence schema digest renames. Reserved for Q-NAMING-F.
- N9: Historical workflow / archive material per §4.8.5 carve-out. Only current SDK docs, quickstarts, current examples, and active tests migrate.
- N10: Q-PR1 sacred 5 paths — must remain 0-diff vs `4c472b50`.
- N11: Adding new public methods or new namespaced managers. Q-NAMING-E is a **rename slice**; no new public surface introduced.
- N12: Reverting or modifying Q-NAMING-AD inherited `_SDKAuditManager.explain/.conflicts` + their private helpers (`_resolve_record_asrt_id`, `_claim_for_audit_target`, `_conflict_cell_for_target`).
- N13: Reverting or modifying Q-NAMING-C inherited rewires (G1-G4 manager rewires + 20 flat shell deletion + `_SDKWhatIf*` class deletion + `commit_assertions` preservation).
- N14: Renaming `target=` variable bindings elsewhere in the codebase. Per audit §6 explicit caveat: "target is a noisy term in the repo. The implementation must scope to Inference DSL and protocol fields rather than all target-like variables." Only `Inference.target` field is in scope; all other `target=` assignments (e.g., `target=EntitySelector(...)` in entity ops, `target: Any` parameter in audit private helpers) are out of scope.
- N15: Resurrecting `fg.what_if` public access (Q-NAMING-C N15 inherited).
- N16: Modifying `SDKStore.commit_assertions` (Q-NAMING-C N16 inherited; G7a LOCKED preserved).

## 4. Current Context

- **Rule + Inference DSL classes** ([`sdk/dsl/rule.py`](../../../src/factgraph/sdk/dsl/rule.py)):
  - `class Rule` at line 54 — fields: `id`, `version`, `select`, `where: list[Any]` (line 73). NO `target` field. NO `head_vars` field. **Rule is the new DSL** per user 2026-05-30 — keeps `select`/`where` semantics.
  - `class Inference` at line 120 — fields: `id`, `version`, `where: list[Any]` (line 134), `head: Any = None` (line 135), `target: str | None = None` (line 137), `head_vars: list[Any] | None = None` (line 138), `status: str | None = None` (line 139). **Inference is legacy** per user.

- **`Branch` DSL class** ([`sdk/dsl/branch.py:13`](../../../src/factgraph/sdk/dsl/branch.py)):
  - Frozen dataclass with `atoms: list[Any]` (required) + optional `id: str | None` (KW_ONLY).
  - Docstring line 14: "Named alternative body inside **a rule or inference**" — **shared between Rule and Inference DSL** per shipped reality. Preflight 2.a resolves whether user "Inference only" framing overrides shipped (rare) or shipped reality wins (default — global rename).
  - Validation: `Branch.atoms` must be non-empty list; `Branch.id` must match `^[A-Za-z_][A-Za-z0-9_]*$`.

- **`def where` shipped surfaces** (4 hits all unrelated to Rule/Inference DSL):
  - [`sdk/facade.py:136`](../../../src/factgraph/sdk/facade.py) — `AssertionView.where(...)` (filter assertion records)
  - [`sdk/facade.py:288`](../../../src/factgraph/sdk/facade.py) — `AssertionRecordSet.where(...)` (similar filter API)
  - [`sdk/store.py:503`](../../../src/factgraph/sdk/store.py) — `AssertionsManager.where(...)` or similar
  - [`sdk/store.py:921`](../../../src/factgraph/sdk/store.py) — another assertion manager `.where(...)`
  - **These are NOT in Q-NAMING-E scope** — they're assertion filter APIs (Q-NAMING-AD domain). Q-NAMING-E renames the `where` **field** on Rule/Inference dataclasses, NOT the `.where(...)` filter method on assertion managers. Preflight 2.f explicitly carves these out.

- **`Not(condition)` shipped** ([`sdk/dsl/expr.py:302`](../../../src/factgraph/sdk/dsl/expr.py)):
  - `def Not(body: list[Any]) -> NotExpr:` — function returning `NotExpr`. Verified existing per user "verify grammar" ask. G10 implementation is docs-only.

- **`Pred` class search**: 0 hits for `class Pred` in `src/factgraph/`. Preflight 2.d clarifies what user's "Pred is low-level escape hatch" refers to. Initial hypothesis: literal predicate identifier strings or `Predicate` class with different name; preflight pins exact reference.

- **Test/example impact** (audit §6 hidden dependency confirmed by spot-check):
  - 180 tests/ files contain `where=[...]` patterns — heavy migration cohort.
  - 6 test files import Rule/Inference/Branch DSL classes directly.

- **`head_vars` reference scale**: 120 lines across src/factgraph/ (verified by Step 4.1 scan). Heavy footprint. Preflight 2.b enumerates exact distribution (src/ vs tests/ vs docs/).

- **当前已知约束**:
  - Q-PR1 sacred 5 paths at 0-diff vs `4c472b50` — must hold through every commit
  - Sacred `master` at `562c74195df43e933bed92a3ff25de94dd8ce666` — never modified
  - Dirty baseline (4 M + 2 D + 2 untracked) preserved at session start
  - Hard-cut per §4.8.1 + Sub-Q2: no aliases, no dual-emit
  - Layer authority per architecture_principles.md §2.1: SDK boundary + application DTO rename; core IR preserved per N7
  - AD slice contracts preserved (N1)
  - C slice contracts preserved (N2)

- **Current related historical blueprints**:
  - Q-NAMING-AD archived at `7c48648f` — Phase 1 precedent (PF-R1 Option (a) private-helper pattern + N15/N16-style invariant introduction)
  - Q-NAMING-C archived at `23389a2b` — Phase 2 precedent (per-shell classification + Sub-Q3 single-slice + Red-risk subtractive lifecycle + cross-flip Claude drafts / Codex implements pattern)
  - Slice 6 form-i debt cleanup at `4c472b50` — Q-PR1 sacred baseline.

## 5. Proposed Shape

### §5.1 Rename layout per Goal

| Goal | Old | New | File:line scope | Risk |
|---|---|---|---|---|
| G1 | `Branch` DSL class | `Case` DSL class | [`sdk/dsl/branch.py:13`](../../../src/factgraph/sdk/dsl/branch.py) — class definition; preflight 2.a pins all consumers (Rule + Inference both accept per shipped docstring) | Yellow (small class, ~2 def hits per audit §6; but consumer base broader than user 2026-05-30 framing suggests) |
| G2 | `Rule.where` field | `Rule.when` field | [`sdk/dsl/rule.py:73`](../../../src/factgraph/sdk/dsl/rule.py) — dataclass field; all `Rule(where=[...])` constructor sites + `rule.where` attribute access | Red (heavy: 180 test sites + many src + docs) |
| G3 | `Inference.where` field | `Inference.when` field | [`sdk/dsl/rule.py:134`](../../../src/factgraph/sdk/dsl/rule.py) — dataclass field; all `Inference(where=[...])` + `.where` access | Red (shared 180 test cohort with G2) |
| G4 | application protocol `Rule.where` / `Inference.where` | `.when` | [`application/protocol/rule.py:53+`](../../../src/factgraph/application/protocol/rule.py) — preflight 2.b pins exact field names + propagation requirements | Red (application DTO layer; verify no wire format dependency) |
| G5 | `Inference.target` field | `emits.target` (inside emit DTO) | [`sdk/dsl/rule.py:137`](../../../src/factgraph/sdk/dsl/rule.py) — preflight 2.c specifies emit DTO shape | Red (audit §6 "target is noisy term"; must scope to Inference field only — N14) |
| G6 | `Inference.head_vars` field | folded into `emits` DTO | [`sdk/dsl/rule.py:138`](../../../src/factgraph/sdk/dsl/rule.py) — preflight 2.c specifies emit DTO shape | Red (head_vars 120 src lines + many tests/docs) |

### §5.2 Emit DTO shape decision (G5 + G6)

Per user "target + head_vars → emits", the combined emit DTO shape is **not yet pinned** at draft time. Preflight 2.c specifies one of:

- **(Option A — single combined field)** `Inference.emits: EmitSpec | None = None` where `EmitSpec` is a new dataclass with `target: str` + `vars: list[Any]` fields. Two old fields collapse into one new field.
- **(Option B — nested fields)** `Inference.emits.target` + `Inference.emits.vars` — both fields retained but namespaced under `emits` attribute (requires nested dataclass).
- **(Option C — structural alternative)** Drop both `target` and `head_vars`; replace with single `emits` field whose type is the same as the prior combined data.

Step 4.3 preflight 2.c locks one option. Default if ambiguous: **Option A** (single combined `EmitSpec` dataclass) — clearest hard-cut semantics, easiest test migration pattern.

### §5.3 Branch class scope clarification (G1 → preflight 2.a critical)

**Shipped reality vs user framing diverge**:
- User 2026-05-30: "4.1 Branch → Case for legacy Inference only;new Rule class 不受影响"
- Shipped docstring at `sdk/dsl/branch.py:14`: "Named alternative body inside **a rule or inference**" — used by BOTH

Preflight 2.a investigates and resolves at one of:
- **(Default A — global rename matching shipped)** Rename `Branch` → `Case` globally; both Rule and Inference DSL accept `Case` in `where`/`when` field. Justification: shipped reality dominates user historical framing per Q-NAMING-C PF-v1 precedent (Codex caught Step 4.1 scan estimate of 9 tombstones vs actual 11 — preflight refines based on shipped truth).
- **(Alternative B — Inference-only with Rule alias)** Rename `Branch` → `Case`; Rule DSL `where`/`when` field accepts both `Branch` (alias) and `Case` (canonical). Justification: honor user 2026-05-30 framing literally.
- **(Alternative C — abandon G1)** If preflight finds `Branch` is structurally so tightly coupled to Rule DSL semantics that Inference-only rename creates more confusion than clarity, abandon G1 with rationale recorded in §10.

Recommendation: Default A unless preflight finds blocker.

### §5.4 Where-field vs where-method carve-out

**Critical N14-style carve-out**: 4 shipped `def where(...)` methods on assertion-filter classes (AssertionView, AssertionRecordSet, AssertionsManager) are **NOT** in Q-NAMING-E scope:
- [`sdk/facade.py:136`](../../../src/factgraph/sdk/facade.py) `AssertionView.where`
- [`sdk/facade.py:288`](../../../src/factgraph/sdk/facade.py) `AssertionRecordSet.where`
- [`sdk/store.py:503`](../../../src/factgraph/sdk/store.py) `AssertionsManager.where`
- [`sdk/store.py:921`](../../../src/factgraph/sdk/store.py) another assertion `.where`

These are Q-NAMING-AD domain (assertion filter API); preserved untouched. Q-NAMING-E renames only the `where` **dataclass field** on Rule + Inference, NOT the `.where(...)` filter method. Preflight 2.f explicitly verifies the carve-out and ensures pre-impl grep distinguishes field vs method.

### §5.5 `target` noisy-term carve-out (N14 reinforcement)

Per audit §6: `target=` is noisy in repo. Q-NAMING-E renames `Inference.target` field ONLY. Out of scope:
- `target=EntitySelector(...)` in entity ops (`sdk/ingest.py:399`, `sdk/store.py:1056/1197/2179`)
- `target: Any` parameter in audit private helpers (`sdk/store.py:1326/1334/1341` from Q-NAMING-AD)
- `target = getattr(derivation, ...)` runtime resolution
- All other `target` as variable name / parameter / dict key not on Inference dataclass

Preflight 2.g pins exact Inference-field-only grep pattern.

### §5.6 Docs + tests + examples migration

Per audit §6: 358 hits / 115 files = 41 src + 18 test + 55 docs/workflow + 1-tier indirect.

- **In scope**: current SDK docs (`src/factgraph/*/docs/`), current public docs (`docs/`), current quickstarts, current example notebooks not in dirty baseline, current tutorials.
- **Out of scope** (per Q-NAMING §4.8.5): `workflow/heritage/`, `workflow/blueprints/archive/`, `workflow/audit/archive/`, `docs/references/working/`, `examples/archive/`.

Step 4.6.5 pre-impl grep produces the exact current/archive split for each touched doc; CADENCE Option 2 amendment folds the current set into scope.

### §5.7 Step 4.6.5 pre-implementation grep inventory (Red slice mandatory)

To be populated at Step 4.6.5 per Q-NAMING-AD + Q-NAMING-C precedent. Categories:

- **N-1 Tests**: which `tests/` files use `Rule(where=[...])` / `Inference(where=[...])` / `Branch(...)` / `head_vars=` / `target=` (Inference-scope only) patterns
- **N-2 Source**: which `src/factgraph/` Python files reference the renamed fields/classes
- **N-3 Current docs**: which `src/factgraph/*/docs/` + `docs/` files reference Rule/Inference DSL syntax
- **N-4 Application protocol**: exact field-name set on `application/protocol/rule.py` Rule/Inference DTOs
- **N-5 Core IR / authoring**: any `core/rules/` / `authoring/` references that consume DSL fields (likely propagate via authoring payload normalization)
- **N-6 Historical carve-outs**: per Q-NAMING §4.8.5

### §5.8 Q-NAMING-AD + Q-NAMING-C precedent reuse

Patterns from prior Phase 1/2 slices applicable here:

- **Per-rename classification** (Q-NAMING-C G6 four-bucket model adapted to E):
  - **E-field-rename**: simple dataclass field rename (G2, G3 `where`→`when`)
  - **E-class-rename**: top-level class rename + all imports/refs (G1 `Branch`→`Case`)
  - **E-restructure**: field-to-DTO restructuring (G5+G6 `target`+`head_vars`→`emits`)
  - **E-docs-only**: documentation polish (G9 `Pred`, G10 `Not(condition)`)
- **N7 layer authority preservation** (AD/C inherited): core IR + low-level protocol details retain internal names where they don't appear on public DSL surface.
- **Hard-cut per Sub-Q2 + §4.8.1** (no alias, no dual-emit, old removed in same commit).
- **Test/example same-slice migration** (audit §6 mandate): query-syntax-teaching tests + examples migrate with code rename.

## 6. Boundaries And Invariants

- **必须保持的边界**:
  - Q-PR1 sacred 5 paths: 0-diff vs `4c472b50` after every commit
  - Sacred master `562c74195df43e933bed92a3ff25de94dd8ce666` — never modified
  - Dirty baseline preserved
  - Layer authority per architecture_principles.md §2.1: SDK DSL + application protocol DTO rename; core IR preserved per N7
  - **AD slice contracts** (N1, expanded): `fg.assertion_views`, `FrozenAssertionSet`, `DatabaseFrozenAssertionSet` alias, `_SDKAuditManager.explain/.conflicts` + private helpers, `by_ids(strict=)`, `save_workspace/.load_workspace`
  - **C slice contracts** (N2, expanded): 20 flat shells deleted, 4 reachable manager rewires honored, `_SDKWhatIf*` 3 dead classes deleted, `commit_assertions` byte-identical, N15+N16 enforced
  - **Hard-cut Sub-Q2**: `.where()` → `.when()` rename (G2+G3) has NO alias support
  - **Audit §6 N14 "target noisy term" carve-out**: Inference dataclass field only
  - **§5.4 where-method carve-out**: assertion filter `.where(...)` methods (Q-NAMING-AD domain) preserved
- **明确不做的内容**:
  - N1-N16 above
  - Rule class internals (`select`, `id`, `version` fields) renames
  - Branch class deletion (G1 is rename only; class still exists with new name)
  - Core IR internal symbol renames
- **兼容性约束**:
  - Hard-cut per §4.8.1 + Sub-Q2 — no aliases, no dual-emit, no deprecation shims
  - Alpha release, no historical user protection burden
  - Old field/class name removed in same commit as new name introduced
  - Tests/examples/docs that teach query syntax migrate **in same slice** as DSL rename (audit §6 hidden dependency)

## 7. Acceptance

- [ ] G1: `Branch` class renamed → `Case` at `sdk/dsl/branch.py:13`; preflight 2.a default A confirmed (global rename) OR alternative B/C documented with rationale
- [ ] G2: `Rule.when` field replaces `Rule.where` at `sdk/dsl/rule.py:73`; all `Rule(where=[...])` constructor sites + `rule.where` access migrated
- [ ] G3: `Inference.when` field replaces `Inference.where` at `sdk/dsl/rule.py:134`; all migration parallel to G2
- [ ] G4: application protocol Rule/Inference DTO `.when` field replaces `.where` at `application/protocol/rule.py:53+`
- [ ] G5: `Inference.target` removed; replaced by emit DTO per preflight 2.c locked Option A/B/C
- [ ] G6: `Inference.head_vars` removed; folded into emit DTO per preflight 2.c locked Option A/B/C
- [ ] G7 invariant: `Rule` class NOT renamed to NamedQuery; no Rule→NamedQuery references introduced
- [ ] G8 invariant: no other Module 4 proposals beyond 4.1/4.2/target+head_vars→emits introduced
- [ ] G9: Pred documentation polished per preflight 2.d clarification
- [ ] G10: Not(condition) syntax verified; docs refreshed
- [ ] G11: Hard-cut honored — no aliases, no dual-emit, old names removed same commit
- [ ] G12: Module docs + current SDK docs + current quickstarts + current examples + active tests migrated
- [ ] G13: Q-PR1 sacred 5 paths 0-diff confirmed at impl commit HEAD
- [ ] Sacred master `562c74...` unchanged through slice
- [ ] Dirty baseline preserved (no baseline file in `git diff --cached`)
- [ ] All Q-NAMING-E targeted tests pass; full-kernel test suite acknowledged baseline failures recorded in §10
- [ ] No push without explicit user authorization
- [ ] AD slice contracts preserved (N1 fully honored)
- [ ] C slice contracts preserved (N2 fully honored)
- [ ] N14 "target noisy term" carve-out: Inference field only; entity ops + audit helpers + runtime `target` references untouched
- [ ] §5.4 where-method carve-out: 4 assertion filter `.where(...)` methods untouched

## 8. Implementation Plan

1. **Step 4.2 review (Codex)**: surface 2-4 tightenings per CADENCE Rule 1 + Rule 2 + scope discipline. Cross-check AD + C inherited state untouched. Particularly probe: (a) G1 Branch-shared-with-Rule shipped reality vs user framing, (b) G5+G6 emit DTO shape, (c) `where` field vs method carve-out wording.
2. **Step 4.3 preflight (TBD drafter)**: independent branch `v0.2.0-q-naming-e-preflight-2026-05-31`. Re-read all blueprint-referenced shipped files at preflight-row drafting time per Rule 1. Build 5-bucket severity findings table. **Mandatory preflight findings**:
   - 2.a **G1 Branch scoping**: resolve user "Inference only" framing vs shipped docstring "rule or inference" — lock Default A (global rename) or alternative
   - 2.b **G4 application protocol pin**: exact `application/protocol/rule.py` Rule/Inference DTO field names + propagation requirements
   - 2.c **G5+G6 emit DTO shape**: lock Option A (single `EmitSpec`) / B (nested) / C (structural alternative)
   - 2.d **Pred clarification (G9)**: identify what "Pred is low-level escape hatch" refers to in shipped code (class? function? literal? doc term?)
   - 2.e **Not(condition) verification (G10)**: confirm `sdk/dsl/expr.py:302 def Not(body)` is the supported grammar; identify docs to refresh
   - 2.f **§5.4 where-method carve-out**: confirm 4 assertion filter `.where(...)` methods at `sdk/facade.py:136/288`, `sdk/store.py:503/921` are out of scope; pre-impl grep must distinguish field vs method
   - 2.g **§5.5 target noisy-term carve-out (N14)**: enumerate all `target` references in src/; classify as in-scope (Inference field) vs out-of-scope (entity ops, audit helpers, runtime, other dataclass fields)
   - 2.h **head_vars distribution**: enumerate 120 src lines + tests + docs; verify all are Inference-field references vs unrelated `head_vars` variable names
   - 2.i **Test migration cohort**: confirm 180 `where=[...]` test sites are query-syntax-teaching (in scope) vs unrelated assertion filter chains (out of scope)
   - 2.j **AD + C inherited state preservation**: confirm Q-NAMING-E scope does NOT touch any AD or C contract surface
3. **Step 4.4 preflight amendment** (on blueprint branch): apply Required + Recommended PFs.
4. **Step 4.5 self-check** (lightweight): PF coverage verification, no commit.
5. **Step 4.6 scoped anchor**: `Status: draft` → `Status: scoped` single small commit + audit log event row. G1 Branch scoping + G5+G6 emit shape must be locked before this step.
6. **Step 4.6.5 pre-impl grep amendment (mandatory for Red slice)**: pre-impl grep distinguishing Rule/Inference DSL fields vs assertion filter methods; full enumeration of touched tests/docs/examples; surface any consumer beyond §5 enumeration as N-1, N-2, ... Apply Option 2 (no status rollback) if minor.
7. **Step 4.7 implementation (Codex 承接)** on branch `v0.2.0-impl-q-naming-e-2026-05-31`:
   - 7.1 Rename `Branch` → `Case` class + all consumers per Step 4.6.5 inventory (G1)
   - 7.2 Rename `Rule.where` → `Rule.when` field + all constructor/access sites (G2)
   - 7.3 Rename `Inference.where` → `Inference.when` field + all sites (G3)
   - 7.4 Rename application protocol Rule/Inference `.where` → `.when` (G4)
   - 7.5 Restructure `Inference.target` + `Inference.head_vars` → `emits` per locked Option (G5+G6)
   - 7.6 Pred docs polish (G9)
   - 7.7 Not(condition) docs refresh (G10)
   - 7.8 Migrate tests: one-pass per file, code + test together (G12)
   - 7.9 Update module docs + current SDK docs + quickstarts + current examples + tutorials (G12)
   - 7.10 Lint pass (`ruff check` targeted)
   - 7.11 Single feat commit (or 3-commit pattern per CADENCE if Step 4.7 review surfaces P1 fix)
8. **Step 4.7 review (Claude)**: independent test re-run, scope grep (verify renames complete + carve-outs honored), cross-check PF / N findings, cross-check AD + C contract preservation.
9. **Step 4.8 closure**: `Status: scoped` → `Status: implemented` + §10 Outcome filled + audit log "implemented" event.
10. **Step 4.9 archive**: `git mv` blueprint + audit log pair from `active/` to `archive/` + `INVENTORY.md` entry + audit log archived event row.

## 9. Docs To Update

- `src/factgraph/sdk/dsl/docs/` (verify exists) — DSL syntax reference
- `src/factgraph/sdk/docs/03_rules_and_inferences.en.md` — Rule + Inference teaching doc (primary surface)
- `src/factgraph/sdk/docs/00_user_guide.en.md` — user guide (likely covers query syntax)
- `src/factgraph/sdk/docs/04_api_surface.en.md` — API surface listing
- `src/factgraph/application/protocol/docs/` (verify exists) — application DTO docs
- `docs/quickstart/rules-and-inferences.md` (or similar) — public quickstart
- `docs/official/kernel/quickstart/` — public quickstart docs (verify scope in preflight)
- Tutorial pages teaching query syntax (preflight 2.i identifies)
- `docs/README.md` — only if new persistent docs entry introduced (none expected)
- Historical material under `workflow/heritage/` / `workflow/blueprints/archive/` / `workflow/audit/archive/` / `docs/references/working/` — NOT touched per Q-NAMING §4.8.5

## 10. Outcome / Deviations

(To be filled at Step 4.8 closure.)
