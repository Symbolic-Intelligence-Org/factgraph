# Session Handoff — 2026-05-13

> Purpose: resume after the 2026-05-13 rc.3 / schema / cleanup /
> release-documentation day without rebuilding state from chat history. Read
> `memory/current.md` first, then this handoff, then the linked implementation
> memories and archived blueprints.

## 1. Current Stage

This day shipped one release candidate and three post-rc.3 slices, then cleaned
up a parallel reference thread and locally prepared a release-documentation
readiness slice.

Published to origin:

| Unit | Source / release | Milestone / tag |
| --- | --- | --- |
| `v0.1.0-rc.3` | source `f49ccc58`, release commit `e996aa5b` | `v0.1.0-rc.3`, `milestone/rc-0.1.0-rc.3-2026-05-13` |
| Schema mutation lifecycle | `f0279791` | `milestone/schema-mutation-lifecycle-2026-05-13` |
| Schema field-add lifecycle | `925354c6` | `milestone/schema-field-add-lifecycle-2026-05-13` |
| Confidence / evidence meta cleanup | `843cf515` | `milestone/confidence-evidence-meta-release-cleanup-2026-05-13` |
| Rule/query/inference head semantics reference capture | `0395acab` | none |

Current remote state:

- `origin/master = 0395acab`
- `origin/release/0.1.x = e996aa5b`
- `v0.1.0-rc.1 = f6242605`
- `v0.1.0-rc.2 = 523a549f`
- `v0.1.0-rc.3 = efb44868`
- Milestone branch refs on origin: 28

Current local state:

- local `HEAD = 2a1d4e3a`
- local branch is 4 docs-only commits ahead of `origin/master`
- worktree is clean

The local ahead commits are intentional and not pushed yet:

```text
09e6364e docs(blueprints): scope release documentation readiness
5b832e65 docs(release): refresh README public surface
0b19b3af docs(service): align release documentation scope
2a1d4e3a docs(blueprints): archive release documentation readiness
```

These commits are accepted local work, but the user asked to avoid expanding
release docs until the official documentation plan is scoped. Do not treat them
as dirty files; decide explicitly whether to push, amend, or leave local.

## 2. Capability Baseline

The lifecycle/assets sequence from 2026-05-12 remains complete:

- Public SDK vocabulary is `Inference`.
- Public wire / registry vocabulary is `inference`.
- Authoring assets are persisted through `fg.rules.*` and `fg.inferences.*`.
- Workspaces are persisted through `FactGraph.create(path=...)`,
  `fg.save(...)`, and `FactGraph.load(path, schema_classes=[...])`.
- Application runtime authority is established through:
  - `kernel.application.authoring_runtime`
  - `kernel.application.workspace_runtime`
  - `kernel.application.schema_mutation_runtime`

Schema mutation state after the two 2026-05-13 slices:

- `fg.schema.add(EntityCls)` and `fg.schema.add(schema_classes=[...])`
  support additive entity-class schema extension.
- Same-entity replacement classes may add non-identity fields.
- `SchemaAddResult(old_digest, new_digest, added_entities, added_fields)` is
  public through `kernel.sdk`.
- Superseded classes/descriptors raise `SDKStoreError`; use the post-add class.
- Existing assertions are not backfilled. Missing added single fields read as
  `None`; missing added multi fields read as `()`.
- Ledger and registry schema digests update immediately after validation.
- Workspace manifest schema digest remains save-time state and updates only on
  explicit `fg.save(...)`.

Confidence / evidence cleanup state:

- `CandidateSet.confidence` and `CandidateSet.confidence_kind` remain
  internal/session compatibility carriers.
- Service candidate DTOs and candidate inventory omit those legacy fields by
  default.
- Runtime accept still parses old echoed `confidence` / `confidence_kind`
  fields, but does not persist them into assertion meta by default.
- Generic `meta.confidence` remains a ledger meta row only; it is not projected
  into shared derived annotations.
- Legacy confidence differences do not affect duplicate detection.
- ProbLog export no longer falls back to generic `meta.confidence`.
- PyReason keeps adapter-native semantic rows and filters generic confidence
  metadata from shared projections.
- Candidate evidence trees do not lift generic assertion `meta.confidence` into
  default evidence/certainty display.

Release boundary:

- The release artifact is kernel-only (`kernel` package).
- Do not document `service`, `agent`, extraction, domains, or HTTP routes as
  part of the official `factpy-kernel` public release surface.

## 3. Validation Baseline

Latest focused validations:

- Confidence cleanup baseline: 11/11 OK.
- Full kernel discovery after confidence cleanup: 2198 OK / 1 skipped.
- Schema field-add suite: 33/33 OK.
- Schema/lifecycle preservation + SDK invariant stack at field-add close:
  217/217 OK.
- Schema mutation lifecycle suite: 39/39 OK.
- rc.3 release dry-run projection before publish: 361 files, 1633 OK /
  1 skipped.
- Release-documentation readiness local slice:
  - focused stale-grep gate: 0 hits;
  - OpenAPI YAML parse + internal `$ref` check: OK;
  - `git diff --check`: OK.

## 4. Commit Landmarks

Published recent source history:

```text
0395acab docs(references): capture rule query inference head semantics
798f515f docs(memory): add 2026-05-13 session handoff
e6059a47 docs(memory): record confidence evidence meta cleanup publish
843cf515 docs(blueprints): archive confidence evidence meta cleanup
632c7093 docs: sync confidence evidence cleanup contract
9be04319 fix(core): stop default confidence meta propagation
5717c10f test(core): add confidence evidence cleanup baseline
fd7a4db6 docs(blueprints): clarify confidence evidence cleanup scope
2e3dcf2b docs(memory): record schema field add lifecycle publish
925354c6 docs(blueprints): archive schema field add lifecycle
88728ed3 docs(sdk): document schema field add lifecycle
fae2d142 feat(sdk): wire schema field add replacement
e88bfad3 feat(application): extend schema mutation for field add
4a9abdc6 test(sdk): add schema field add lifecycle baseline
```

Local unpushed release-documentation readiness commits:

```text
09e6364e docs(blueprints): scope release documentation readiness
5b832e65 docs(release): refresh README public surface
0b19b3af docs(service): align release documentation scope
2a1d4e3a docs(blueprints): archive release documentation readiness
```

Memory detail files:

- `memory/project_v0_1_0_rc3_published.md`
- `memory/project_lifecycle_assets_remaining_after_rc3.md`
- `memory/project_schema_mutation_lifecycle_implemented.md`
- `memory/project_schema_field_add_lifecycle_implemented.md`
- `memory/project_confidence_evidence_meta_release_cleanup_implemented.md`

Archived blueprints to read for details:

- `docs/blueprints/archive/2026-05-13_schema-mutation-lifecycle.md`
- `docs/blueprints/archive/2026-05-13_schema-field-add-lifecycle.md`
- `docs/blueprints/archive/2026-05-13_confidence-evidence-meta-release-cleanup.md`
- `docs/blueprints/archive/2026-05-13_release-documentation-readiness.md`

## 5. Working Tree State

At this handoff update, `git status --short` was clean before editing this
handoff.

If this file has been committed as a handoff update, the expected local state is
one additional docs/memory commit ahead of `2a1d4e3a`.

## 6. Frozen Contracts

Core lifecycle/assets contracts:

- `FactGraph.create(schema_classes=[...])` is the canonical first-contact SDK
  constructor.
- `FactGraph.create(path=...)`, `fg.save(...)`, and
  `FactGraph.load(path, schema_classes=[...])` own workspace lifecycle.
- `from_schema_classes(...)` remains lower-level and does not accept `path=`.
- `SavedRuleRef` and `SavedInferenceRef` are load handles, not runtime
  selectors.
- `RuleRef` remains a where-clause carrier, not a saved-asset handle.
- Public service / registry vocabulary is `inference`; candidate/internal
  substrate may still use `derivation_id`.

Schema mutation contracts:

- Schema mutation is additive-only.
- Entity-add may add new entity classes only.
- Field-add may replace same-name entity classes only to add non-identity
  fields.
- Identity changes, field removal, type/cardinality changes, relationship
  target rewrites, and predicate-id collisions reject before mutation.
- Superseded classes/descriptors reject at SDK boundaries.
- No default/backfill semantics exist for field-add.
- Ledger and registry schema digests update immediately; workspace manifest
  updates on explicit `fg.save(...)`.

Confidence cleanup contracts:

- Legacy generic confidence is parse-compatible but not emitted by default.
- Generic confidence does not become assertion meta, shared semantic
  annotation, adapter semantic input, or evidence/certainty display by default.
- Adapter-native ProbLog/PyReason semantic lanes remain the only adapter export
  source for uncertainty semantics.

## 7. Process Notes

Patterns confirmed:

- Capability slices use full G0-G4 blueprint cadence with explicit decision
  tables, red baselines, bottom-up G2 decomposition, G3 docs sync, G4 archive,
  milestone branch ref, and memory sync.
- Cleanup-style slices can be narrative scoped when no new public API/result
  DTO/application runtime is introduced. They still need behavior matrix,
  compatibility contract, focused baseline, docs sync, G4 archive, and
  milestone publish.
- Documentation-slice cadence is not yet fully established. Treat the upcoming
  official docs/docstring slice similarly to a cleanup slice, but still use G0
  to enumerate docstring style, coverage gates, Markdown layout, and content
  boundaries. Add `feedback_documentation_slice_cadence` only after one full
  G0-G4 pass.
- Use `git push origin HEAD:master` for master publish.
- Milestones are branch refs, not tags.
- Direct local `.git/refs` writes or `git branch` may need sandbox escalation.
- Session continuity material belongs in `memory/session_handoffs/`, not
  `docs/`.

## 8. Recommended Next Work

The user wants to create the new official documentation from the current kernel
API, not preserve compatibility with the previous public docs. The old official
quickstart at `https://factpy.docs.symbolic-intelligence.de/docs/quickstart/`
is only a structural/style reference, not implementation truth.

Recommended first task: open a draft blueprint named
`official-kernel-docstrings-and-tutorials`.

Scope to capture in that draft:

1. **Docstring layer first**
   - Public `kernel.sdk` hover docs are part of the user experience.
   - Current audit found most public SDK exports lack docstrings, including
     `Entity`, `Field`, `Identity`, `FactGraph` / `SDKStore`, `Rule`,
     `Inference`, `Query`, `Branch`, semantics wrappers, schema compile helpers,
     ingest DTOs, facade DTOs, and errors.
   - G0 should lock docstring coverage and style before editing code.
   - Suggested G1 gate: `inspect.getdoc(...)` is non-empty for every
     `kernel.sdk.__all__` export and for selected `FactGraph` namespace methods.

2. **Official Markdown tutorial layer second**
   - Create a new path such as `docs/official/kernel/`.
   - Keep it kernel-only: no service/agent/extraction/domain docs as official
     `factpy-kernel` tutorial surface.
   - Follow Pydantic-like structure: tutorials, concepts, how-to, reference.
   - Use plain Markdown.
   - Content must derive from current code and module docs, not historical docs.

3. **Likely documentation tree**
   ```text
   docs/official/kernel/
     index.md
     quickstart/
       index.md
       first-factgraph.md
       schema.md
       read-write.md
       rules-and-inferences.md
       semantics.md
       persistence.md
     concepts/
       factgraph.md
       schema-ledger-assertions.md
       rules-queries-inferences.md
       semantics.md
       persistence.md
       public-vs-advanced.md
     how-to/
       define-schema.md
       mutate-schema.md
       save-load-workspace.md
       save-rules-inferences.md
       use-problog.md
       use-pyreason.md
       inspect-rules.md
     reference/
       sdk-surface.md
       namespace-map.md
       errors.md
   ```

4. **G0 decision themes**
   - Docstring coverage scope.
   - Docstring style and length.
   - Whether to cover only `kernel.sdk.__all__` or also namespace methods.
   - Whether public API docstrings can mention advanced importables.
   - Markdown path (`docs/official/kernel/` recommended).
   - Tutorial/concepts/how-to/reference split.
   - Runnable examples policy.
   - Kernel-only boundary.
   - Whether to push current local release-documentation commits before or
     after official docs blueprint work.

## 9. Cautions

- Do not treat `memory/` as implementation truth; it is operational continuity.
- Do not assume `docs/references/working` has current behavior; migrate any
  adopted conclusion into module docs or a blueprint.
- Avoid starting destructive schema work in the same context without a fresh
  source audit.
- Preserve rc tags and release branch semantics. `release/0.1.x` may advance
  only through the release workflow; existing rc tags are immutable.
- The local release-documentation readiness commits include service/OpenAPI
  changes. The user later clarified official release docs should be kernel-only;
  those local commits are accepted but should not be used as the scope model for
  the upcoming official tutorial docs.
