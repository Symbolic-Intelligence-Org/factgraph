# Session Handoff: 2026-03-28

Supersedes `2026-03-27-d.md`. **This is a closing handoff** — the multi-engine semantic delivery mother blueprint is complete and archived.

## 1. Current Stage

This session completed the final two deliverables of the mother blueprint and closed it out:

**A. engine_options runtime dispatch** (`5f4d9fb`)
- `engine_options: dict[str, Any] | None` threaded through `Store.evaluate()` → `evaluate_store()` → `Store.evaluate_engine()` → `SDKStore.evaluate()`
- `mode="native"` rejects non-empty `engine_options`
- PyReason v1 exposes `timesteps` only; unknown keys fail fast
- `PyReasonRunConfig` stays adapter-internal

**B. ProbLog semantic annotation parity — L4** (`e3db27d`)
- `evaluate_problog()` now writes `problog/semantic/probability` annotations via pending state
- `persist_problog_annotations()` binds `asrt_id` post-accept
- Enters existing audit package / static HTML consumer pipeline (L2) without new ProbLog-specific UI
- 499 tests green

**C. Mother blueprint archived** (`67f6538` + this commit)
- `2026-03-27_multi-engine-semantic-delivery.md` → `implemented` → `archive/`
- All gates (1-3) closed, all direction lines (L1-L4) complete

**Current position**: No active mother blueprint. The multi-engine framework is operational:
```
SDK authoring → compile/lower → evaluate dispatch (souffle/pyreason/problog)
  → CandidateSet → accept → Ledger + Annotation Store
  → audit package (assertion_annotations.jsonl) → static HTML (annotation panel)
```

## 2. Capability Baseline

### Complete subsystems (all carried forward)
- Certainty v1 (FROZEN — 16 contracts)
- Souffle Provenance (complete)
- Provenance Coverage + Bridge (complete)
- Assertion Annotation Store (complete)
- PyReason Adapter (complete through Gate 3 + bounded materialization)
- ProbLog semantic annotation parity (complete)
- engine_options runtime dispatch (complete)
- Annotation consumer pipeline (audit + static HTML)

### Multi-engine execution surface

| Engine | evaluate | annotation | audit consumer | engine_ext | engine_options | provenance |
|--------|----------|-----------|----------------|------------|----------------|------------|
| Souffle | ✅ | ✅ (shared/*) | ✅ | N/A | N/A | ✅ (proof tree) |
| PyReason | ✅ | ✅ (pyreason/* + shared/*) | ✅ | ✅ | ✅ (timesteps) | ❌ (degraded) |
| ProbLog | ✅ | ✅ (problog/* + shared/*) | ✅ | ❌ | ❌ | ❌ (degraded) |

## 3. Git State

- Branch: `master`
- Working tree: `examples/dora_compliance_demo.ipynb` modified (unrelated), `.claude/launch.json` untracked
- **499 tests, all green**

Session commits (from `dc0ff07` baseline through this session):
- `4873064` — Annotation Store v0
- `febce81` — PyReason session batch API
- `731e8b0` — PyReason accept helper
- `6c61888` — PyReason runner
- `d669019` — PyReason rule extensions
- `8ba7580` — Execution surface decision (9 contracts)
- `833075c`→`fa35c54` — Execution surface Steps 1-5
- `9b78ec6` — engine_ext guards + persist helper
- `5025a1c` — Mother blueprint scoped
- `a8cb170` — Gate 1 passed (real engine)
- `bcbfb5f` — Gate 2 passed (L2 annotation consumer)
- `a6fc4ef` — L3a decisions (5 frozen + D-VC4 closed)
- `c21dd17` — Gate 3 passed (L3b bounded materialization)
- `5f4d9fb` — engine_options runtime dispatch
- `e3db27d` — **L4: ProbLog semantic annotation parity**

## 4. Blueprint Status

### Active (decision references only — no implementation work pending)

| Blueprint | Status | Notes |
|-----------|--------|-------|
| `2026-03-22_ecss-domain-validation-and-souffle-provenance-poc.md` | implemented | Old mother blueprint |
| `2026-03-22_architectural-decisions-v2.md` | scoped | ADR reference |
| `2026-03-26_assertion-annotation-store-decision.md` | scoped | 6 annotation decisions |
| `2026-03-27_multi-engine-execution-surface-decision.md` | scoped | 9 execution decisions |
| `2026-03-27_multi-engine-execution-surface-design-rationale.md` | approved | Design rationale |
| `2026-03-27_value-carrying-semantics-v1-decision.md` | scoped | L3a — 5 decisions |
| `2026-03-27_engine-options-runtime-dispatch-decision.md` | scoped | 6 engine_options decisions |

### Key Archived (this session + prior)

Mother blueprint + all implementation sub-blueprints archived.

## 5. Test Baseline

- **499 tests, all green**
- `PYTHONPATH=src python -m unittest discover -s src/factpy_kernel/tests -p "test_*.py"`

## 6. Frozen Contracts

**63 frozen contracts total** (1-57 from prior + this session):

58. **engine_options is call-time only** (D-EO2) — never enters Derivation, engine_ext, authoring payload, or Ledger
59. **Shared core treats engine_options as opaque** (D-EO3) — adapters own validation
60. **PyReason v1 engine_options: timesteps only** (D-EO4) — default 2, positive int
61. **PyReasonRunConfig stays adapter-internal** (D-EO5) — core never imports it
62. **ProbLog annotation = probability only** — `problog/semantic/probability` written post-accept
63. **persist_problog_annotations skips dry-run** — pending state not cleared during dry_run accept

## 7. Future Work (not current obligations)

These are potential next directions, **not pending tasks**:

- **ProbLog provenance** — real derivation trace (currently `engine_no_witness_v1`)
- **ProbLogExt** — engine-specific rule extensions (probability annotations on rules)
- **Query.engine_ext** — currently only `Derivation` has `engine_ext`
- **body_confidences shared-core debt** — ProbLog-specific param in core signature (ADR-14c.1)
- **PyReason provenance** — event log → support artifact mapping
- **Entity identity alignment** — synthetic `idref_v1:Type:id` → real entity registry
- **Old mother blueprint formal archive** — `2026-03-22_ecss-domain-validation-and-souffle-provenance-poc.md` still in `active/`

## 8. What the Next Agent Should Do

### First action
```bash
PYTHONPATH=src python -m unittest discover -s src/factpy_kernel/tests -p "test_*.py"
```
Expected: **499 tests**, OK.

### Recommended next direction

**Open a new mother blueprint** — the current one is archived. The next phase should be driven by a real domain use case, not by framework symmetry. Possible themes:

1. **Domain validation** — run a real compliance/audit scenario end-to-end with PyReason bounded semantics
2. **Provenance quality** — upgrade from `engine_no_witness_v1` to real provenance for PyReason/ProbLog
3. **Core cleanup** — archive old mother blueprint, resolve `body_confidences` debt

### What NOT to do

- Do not reopen the archived mother blueprint
- Do not reopen D-VC4 (closed as infeasible, contract #54)
- Do not serialize engine_options into authoring payloads (contract #58)
- Do not add ProbLog-specific UI to static HTML (unnecessary — generic annotation panel handles it)
- Do not reopen any of the 63 frozen contracts

## 9. Minimal Startup Reading List

1. **This handoff** (`memory/session_handoffs/2026-03-28.md`)
2. **Archived mother blueprint** (`docs/blueprints/archive/2026-03-27_multi-engine-semantic-delivery.md`)
3. **Core architecture** (`src/factpy_kernel/core/docs/01_architecture.md`)
4. **PyReason adapter docs** (`src/factpy_kernel/adapters/docs/03_pyreason_adapter.md`)
5. **ProbLog adapter docs** (`src/factpy_kernel/adapters/docs/02_problog_adapter.md`)
6. **Archive index** (`docs/blueprints/archive/README.md`)
