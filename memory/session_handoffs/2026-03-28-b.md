# Session Handoff: 2026-03-28-b

Supersedes `2026-03-28.md`. **Phase boundary handoff**: Multi-Engine Semantic Delivery mother blueprint fully implemented and archived. Design-code audit complete — all 10 gaps closed. Examples cleaned and demos updated with real bound propagation.

## 1. Current Stage

This session completed the remaining Multi-Engine Semantic Delivery direction lines (L3b bounded materialization, engine_options, L4 ProbLog parity), then conducted a 4-agent design-code audit that found 10 gaps and closed all of them. Examples were also refactored to remove legacy schema hacks.

**Key deliverables (this session)**:

| Commit | Description |
|--------|-------------|
| `c21dd17` | Bounded materialization for PyReason (L3b) |
| `5f4d9fb` | engine_options runtime dispatch + PyReason timesteps |
| `e3db27d` | ProbLog semantic annotation parity (L4) |
| `0e121c6` | Archive mother blueprint (all gates closed) |
| `f23e07a` | Clean examples + fix schema_compile owner_type |
| `345810f` | Close 10 design-code gaps (bound routing + annotation origin + revocation) |
| `ddc2378` | PyReasonFactDef.bound + demo bound propagation |

**Current position**: Phase boundary. 503 tests green. 0 design-code gaps. Next step: real PyReason E2E validation in clean session, then new mother blueprint.

## 2. Capability Baseline

### Core Framework (unchanged)
- Certainty v1 (FROZEN — 16 contracts)
- Souffle Provenance (complete)
- Provenance Coverage + Bridge (complete)

### Annotation Store (complete, gaps closed)
- `AnnotationRow` DDL + dual-write + legacy meta_rows projection
- `confidence` now `origin="derived"` with derivation field
- Revocation dual-writes annotations
- Accept helpers validate origin/derivation consistency

### Multi-Engine Execution Surface (complete)
- `Store.evaluate(mode="pyreason")` functional
- `EngineExtBase` → `PyReasonRuleExt` → `Derivation.engine_ext`
- `engine_options` dispatch (PyReason: `timesteps` only)
- WhereIR → PyReason compiler (pred atoms)
- Post-accept annotation persistence

### Bounded Materialization (complete)
- Schema-driven routing: `pyreason_bounded: true` + numeric type_domain
- `build_pyreason_graph()` reads `fact["bound"]` lower
- `PyReasonFactDef.bound` → `"atom : [lo, hi]"` in `pr.add_fact()`
- D-VC4 closed as infeasible (PyReason parser limitation)

### ProbLog Semantic Parity (complete)
- `persist_problog_annotations()` writes `problog/semantic/probability`

### Examples (cleaned)
- All demos use SDK Entity/Field (no schema_ir hacks)
- `schema_compile.py` auto-generates `owner_type` for relationships
- PyReason notebooks have `_PYREASON_OK` flag + real data fallback

## 3. Git State

- Branch: `master`
- HEAD: `ddc2378`
- Working tree: **clean** (only `.claude/launch.json` untracked)
- **503 tests, all green**

## 4. Blueprint Status

### Active (decision/reference only — no implementation pending)

| Blueprint | Notes |
|-----------|-------|
| `2026-03-22_architectural-decisions-v2.md` | ADR reference |
| `2026-03-22_ecss-domain-validation-and-souffle-provenance-poc.md` | Original mother (implemented) |
| `2026-03-26_assertion-annotation-store-decision.md` | 6 annotation decisions |
| `2026-03-27_multi-engine-execution-surface-decision.md` | 9 decisions (D1-D9) |
| `2026-03-27_multi-engine-execution-surface-design-rationale.md` | Design rationale |
| `2026-03-27_value-carrying-semantics-v1-decision.md` | D-VC1~5 + D-VC4 closed |
| `2026-03-27_engine-options-runtime-dispatch-decision.md` | engine_options design |

**No active implementation blueprints.**

## 5. Test Baseline

- **503 tests, all green**
- `PYTHONPATH=src python -m unittest discover -s src/factpy_kernel/tests -p "test_*.py"`

## 6. Frozen Contracts

Prior contracts 1-51 remain frozen. This session adds:

52. **confidence origin="derived"** with derivation field
53. **revocation dual-writes annotations**
54. **accept helper validates origin/derivation consistency**
55. **PyReasonFactDef.bound** — `"atom : [lo, hi]"` encoding
56. **build_pyreason_graph reads fact["bound"]** lower for bounded predicates
57. **D-VC4 closed as infeasible** — value variables in PyReason rule syntax not possible in v1

## 7. Backlog (not started)

- **Real PyReason E2E validation** — RECOMMENDED NEXT
- body_confidences shared-core debt (ADR-14c.1)
- ProbLog provenance carrier
- Rule compile expansion (cmp/not/ruleref)
- Query.engine_ext

## 8. What the Next Agent Should Do

### First action
```bash
PYTHONPATH=src python -m unittest discover -s src/factpy_kernel/tests -p "test_*.py"
```
Expected: **503 tests**, OK.

### Recommended next direction
**Real PyReason E2E validation** using `factpy` conda env (Python 3.10):
1. Run `examples/ecss_pyreason_demo.py` with real PyReason
2. Verify bound propagation, annotation persistence, audit readback
3. Use results to decide whether next mother blueprint targets "product polish" or "semantic research"

### What NOT to do
- Do not open new mother blueprint before E2E validation
- Do not reopen any of 57 frozen contracts
- Do not serialize engine_ext into to_authoring_payload()
- Do not put engine-specific params in shared meta_rows

## 9. Reading List

1. This handoff
2. `src/factpy_kernel/adapters/docs/03_pyreason_adapter.md`
3. `docs/blueprints/active/2026-03-27_value-carrying-semantics-v1-decision.md`
4. `examples/README.md`
5. `examples/ecss_pyreason_demo.py` — E2E validation target
6. `src/factpy_kernel/adapters/pyreason/runner.py` — bounded graph build
